# gen-max — סיכום מלא (מה נבנה, מה נלמד, השוואה למחולל המקורי)

**מיקום מדויק:** `/tmp/quarry-iso/machtzev/generator/gen-max.mjs` · 1,067 שורות · ‏111 KB
**קטלוג-הקלט:** `master-particles.json` — 40,854 חלקיקים (7 op: transform·effect·format·collection·predicate·measure·guard)
**סביבה:** בודד ב-`/tmp/quarry-iso`; שני הריפו (`maor-system`, `buildsmart`) קריאה-בלבד פרט ל-3 תיקוני-באג מאושרים.

---

## 1. מה gen-max עושה
מחולל-אוטונומי שלוקח מנועים מקוד-מאור/buildsmart ו: (א) מגלה באגי-נכונות · (ב) משכתב מבנה · (ג) מחבר מנועים בשרשראות — **בלי להמציא** (הרכבה טהורה של חלקיקי-קיימים), **בלי לשבור** (ברירת-מחדל ביט-זהה), **בלי אובדן-יכולת**.

### המצבים (17)
| מצב | מה עושה |
|---|---|
| `--maxall` | מפקד-כנות: 846 מנועים → 783 במקסימום · 5 ניתנים-לשדרוג (fixpoint · pulled-not-wired) |
| `--deepfiles`/`--deepall`/`--deepmax` | גלאי-סיכוני-נכונות: בליעה-שקטה `a.f\|\|b.f` · רצף-מונה `seq++` · כסף-בצפים · תאריך-UTC · JSON.parse-לא-מוגן |
| `--autofix`/`--fixfile=`/`--fixmagic=` | תיקון-מכני של קוד-מאור החי (‏toISOString→isoToday · מספר-קסם→opts) |
| `--switch --file=` | switch⇒טבלת-חיפוש (API זהה, אפס-אובדן) |
| `--restructure`/`--restructure-scan` | סולם-if⇒טבלת-נתונים+find |
| `--dedup-scan` | גלאי-כפילויות (regex/פרדיקט זהים) |
| `--board`/`--board-scan` | **מנוע-הלוח:** חיבור מונחה-טיפוס — BFS שרשרת `X→…→Z` + ניתוח-רכזות |
| `--domain=`/`--goal=`/`--empire` | צרור-דומיין · פירוק-מטרה ל-op · מפת-אימפריה |

### 3 באגים אמיתיים שתוקנו במאור (ענף `claude/p0-bundle-data-migration-vg7z6b`)
1. **תאריך-UTC** — `CashRegister.tsx` · `App.tsx` · `ReenrollView.tsx`: `new Date().toISOString().slice(0,10)`→`isoToday()` (סביב חצות יצא יום שגוי).
2. **בליעה-שקטה** — `dedup.ts mergeSupporterInto`: ת"ז/הו"ק שונה בין כרטיסי-כפילות נזרקה בשקט → נשמרת בהערות (הראשית לא זזה, אפס-השפעה על קבלות).
3. רתמות-זהב (ratchet) בעברית לכל תיקון.

---

## 2. המחולל המקורי (GENMAX / מחצב) — איך הוא בונה
**שתי שכבות:**
- `compose-engine.mjs` (המוח): טבלת-ATOM (op→אטום+seam הוכחת-file:line) · `ops(formula.kind)` תרשים-חיווט קבוע · FAKERS חסומים (throw).
- `render-module.mjs` (הידיים): `select`→סגירת-תלויות · `socketPlan`=שקע (אתר-קריאה) · `siteResolvable`=התאמת-טיפוס · `syntheticBuild`→`DsScaffold(children)`=לוח · `--gate` round-trip ביט-לביט 9/9.

**מיפוי שקע/תקע/אטום/לוח:** אטום=leaf+seam · שקע=firstIn/אתר-קריאה · תקע=out · לוח=שרשור תקע→שקע · תרשים-חיווט=ops(formula).

**מה שכבר יש אצלו (אימות בהרצה):**
- `new/dart-maor/` — **665 מנועי-מאור מפורטים ל-Dart** + **659 רתמות-זהב** (זהות-ביט למקור-ה-JS) + `new/atoms/` **1,160 קופסאות + 1,160 חוזים**.
- `deep-purity-scan` (טוהר: heb·table·domstr·magic) · `box-magic-lift`/`box-data-lift` (הרמת-קבועים) · `chisel`/`chisel-all` (fix+upgrade) · `rethread-boxes`/`box-assemble`/`one` (חיווט/הרכבה) · `dedup-deep` (regex זהים) · `match.mjs` (TF-IDF) · `search-record` (fuzzy דו-לשוני מוכח).

---

## 3. מה יש לי שאין לו (מאומת ברמת-הבייט — 2 בלבד)
1. **עריכה+תיקון של קוד-מאור החי (.ts) וקומיט** — אף אחד מ-146 כלי-מחצב לא כותב לתוך src-מאור (הם קוראים ומפרטים ל-`new/`); gen-max כותב 9 אתרים + 3 תיקונים.
2. **גלאי-באגי-נכונות סמנטיים** (לא רק טוהר) — `deep-purity-scan` שלו = דאטה-במנגנון בלבד; אין silent-swallow/seq/money/UTC-כבאג/JSON.parse. (‏`synth.mjs:202` שלו עצמו נופל בבאג-UTC בלי גלאי.)

**נוסף — סגנון-חיפוש בלעדי לי:** חיפוש-מסלול על גרף-טיפוסים (`--board` BFS `X→…→Z` + `--board-scan` רכזות). לו יש רק `formula→ops` קבוע — אין pathfinding על טיפוסים.

## 4. מה שחשבתי שלי אבל **לא** (תוקן אחרי אימות)
- dedup-regex → **יש לו** (`dedup-deep`).
- board/חיבור בכללי → **יש לו ובוגר** (compose-engine+one, 665 אטומים+659 טסטים).
- switch/if→טבלה → **חופף** לטיהור שלו (PLAN-100: switch⇒Map).
- שכבת-קופסאות למאור → **כבר קיימת** (1,160 קופסאות+חוזים).

## 5. השוואת סגנונות-חיפוש
- **לפי-משמעות/כוונה:** הוא, בגדול — TF-IDF + כיסוי + fuzzy דו-לשוני + חיפוש-מוכח + אורקל בנוי-מראש. שלי exact-token בלבד.
- **לפי-טיפוס/מבנה (X→Z):** אני — pathfinding על גרף-טיפוסים + ניתוח-רכזות + חיפוש-לפי-צורת-באג.

## 6. המסקנה
שני מחוללים **משלימים**, אותו גרעין-טוהר ("חוצבים ומרכיבים, לא ממציאים" · דטרמיניזם · אפס-אובדן · חוסמי-מזייפים):
- **הוא** = מפעל-ייצור בשל: source→אטומים-מתועדים-עם-חוזה-וטסט→הרכבת-מסכי-Flutter. חזק במציאה-לפי-משמעות.
- **אני** = סוגר-לולאה על הקוד-החי: מגלה באגי-נכונות, מתקן במקום, ומחבר לפי-טיפוס בשרשראות.

**האינטגרציה הנכונה (לא-מומשה):** להריץ את ה-board/deepfiles שלי מעל ה-substrate שלו (אטומים+חוזים+טסטי-זהב) — כל באג שאני מוצא נכנס כדוגמת-חוזה + רתמת-זהב; והחיבור-לפי-טיפוס שלי רץ מעל אטומים מוכחים במקום קריאה-חיה.
