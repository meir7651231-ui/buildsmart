# RequiredDocsPolicyNotifier.addRequirement — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `required_docs_policy` · `../../../app_flutter/lib/state/required_docs_policy.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String employerId, String name) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kRequiredDocsKey↝` · `mounted↝`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `normalizeDocName` · `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — n = name.trim()
- **precond** — if n.isEmpty → return (void)
- **compute** — current = state[employerId] ?? const <String>[]
- **compute** — key = normalizeDocName(n)
- **precond** — if current.any((m) => normalizeDocName(m) == key) → return (void)
- **effect** — _userTouched = true
- **effect** — state = {...state, employerId : [...current, n]}
- **effect** — _persist()

## floor
- `any` · `trim`

## חוזה · contract
- **input:** `(String employerId, String name)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if n.isEmpty → return (void)
  - if current.any((m) => normalizeDocName(m) == key) → return (void)
- **throws:** _none_
