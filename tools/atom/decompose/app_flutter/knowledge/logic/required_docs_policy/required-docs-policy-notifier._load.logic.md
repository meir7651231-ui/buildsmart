# RequiredDocsPolicyNotifier._load — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `required_docs_policy` · `../../../app_flutter/lib/state/required_docs_policy.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kRequiredDocsKey` · `_userTouched` · `mounted`
- **writes:** `state:state`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — prefs = await SharedPreferences.getInstance()
- **precond** — if !mounted || _userTouched → return (void)
- **compute** — raw = prefs.getString(kRequiredDocsKey)
- **precond** — if raw == null → return (void)

## floor
- `getInstance` · `getString` · `jsonDecode`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if !mounted || _userTouched → return (void)
  - if raw == null → return (void)
- **throws:** _none_
