# bsRoleFrom — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `rbac` · `../../../app_flutter/lib/state/rbac.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `({required bool signedIn, required String? email, required List<String> serverRoles}) -> BsRole`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `roleFromClaim`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !signedIn → return BsRole.contractor
- **precond** — if isOwnerEmail(email) → return BsRole.admin
- **precond** — if serverRoles.length == 1 → return roleFromClaim(serverRoles.first)
- **return** — BsRole.contractor

## floor
- `isOwnerEmail`

## חוזה · contract
- **input:** `({required bool signedIn, required String? email, required List<String> serverRoles})`
- **output:** `BsRole`
- **purity:** pure
- **precond:**
  - if !signedIn → return BsRole.contractor
  - if isOwnerEmail(email) → return BsRole.admin
  - if serverRoles.length == 1 → return roleFromClaim(serverRoles.first)
- **throws:** _none_
