# TasksNotifier.proposeTask — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({required String name, required int worker, String detail = '', int days = 1, List<String> steps = const [], String assignedWorkerUid = '', String employerId = '', DateTime? scheduledStart, String kind = 'task', String location = '', String severity = ''}) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPersonaTasks↝` · `kTaskClockSideMapKey↝` · `kTasksRuntimeKey↝` · `kTasksScreenKey↝` · `persist↝`
- **writes:** `field:_loaded↝` · `io:prefs.setString↝` · `state:state`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — id = state.isEmpty ? 1 : (state.map((t) => t.id).reduce((a, b) => a > b ? a : b) + 1)
- **compute** — emp = employerId.trim()
- **compute** — uid = assignedWorkerUid.trim()
- **effect** — state = [...state, TaskItem(id: id, name: name, detail: detail, worker: worker, status: '…
- **return** — id

## floor
- `TaskItem` · `map` · `reduce` · `trim`

## חוזה · contract
- **input:** `({required String name, required int worker, String detail = '', int days = 1, List<String> steps = const [], String assignedWorkerUid = '', String employerId = '', DateTime? scheduledStart, String kind = 'task', String location = '', String severity = ''})`
- **output:** `int`
- **purity:** side-effecting
- **throws:** _none_
