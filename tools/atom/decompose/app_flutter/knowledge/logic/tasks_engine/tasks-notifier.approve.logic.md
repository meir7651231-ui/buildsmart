# TasksNotifier.approve — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int id) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kTaskApprovalCoins` · `_ref` · `ordersEngineProvider` · `rewardsProvider` · `workerNotifsProvider`
- **writes:** `state:state↝`
- **calls:** `_byId` · `_patch`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = _byId(id)
- **precond** — if t == null || t.status != 'review' → return (void)
- **compute** — orderId = t.orderId
- **effect** — _patch(id, (x) => x.copyWith(status: 'done'))
- **compute** — r = _ref
- **precond** — if r == null → return (void)
- **branch** — if orderId != null
- **effect** — r.read(rewardsProvider.notifier).awardCoins(kTaskApprovalCoins)
- **effect** — r.read(workerNotifsProvider.notifier).addForWorker(t.worker, emoji: '✅', title: 'המשימה א…

## floor
- `addForWorker` · `advance` · `awardCoins` · `copyWith` · `read`

## חוזה · contract
- **input:** `(int id)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if t == null || t.status != 'review' → return (void)
  - if r == null → return (void)
- **throws:** _none_
