# taskClockLabel — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(TaskItem t) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — start = t.startedAt
- **precond** — if start == null → return null
- **compute** — d = (t.completedAt ?? DateTime.now()).difference(start)
- **branch** — if d.isNegative
- **precond** — if d.inMinutes < 1 → return 'פחות מדקה'
- **precond** — if d.inMinutes < 60 → return '${d.inMinutes} דק׳'
- **branch** — if d.inHours < 24
- **compute** — h = d.inHours % 24
- **return** — h == 0 ? '${d.inDays} ימים' : '${d.inDays} ימים $h שע׳'

## floor
- `difference` · `now`

## חוזה · contract
- **input:** `(TaskItem t)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if start == null → return null
  - if d.inMinutes < 1 → return 'פחות מדקה'
  - if d.inMinutes < 60 → return '${d.inMinutes} דק׳'
- **null when:** start == null · or search exhausted
- **throws:** _none_
