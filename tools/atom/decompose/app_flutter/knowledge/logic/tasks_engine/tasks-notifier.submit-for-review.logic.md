# TasksNotifier.submitForReview — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int id, {String? note, String? workerUid, String? employerId}) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_ref` · `workerNotifsProvider`
- **writes:** `state:state↝`
- **calls:** `_byId` · `_patch` · `_stampIdentity`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = _byId(id)
- **precond** — if t == null → return (void)
- **precond** — if t.status != 'active' && t.status != 'rejected' → return (void)
- **effect** — _patch(id, (x) => _stampIdentity(x.copyWith(status: 'review', photo: x.photo ?? 'demo', n…
- **compute** — next = state.where((x) => x.worker == t.worker && x.status == 'pending').toList()
- **branch** — if next.isNotEmpty

## floor
- `addForWorker` · `copyWith` · `now` · `read` · `toList` · `where`

## חוזה · contract
- **input:** `(int id, {String? note, String? workerUid, String? employerId})`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if t == null → return (void)
  - if t.status != 'active' && t.status != 'rejected' → return (void)
- **throws:** _none_
