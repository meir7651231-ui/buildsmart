# TaskItem.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> TaskItem?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `assignedWorkerUid` · `completedAt` · `createdBy` · `days` · `detail` · `doneSteps` · `employerId` · `kind` · `location` · `note` · `orderId` · `photo` · `scheduledStart` · `severity` · `startedAt` · `status` · `steps` · `worker`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — id = raw['id']
- **compute** — name = raw['name']
- **precond** — if id is! int || name is! String → return null
- **return** — TaskItem(id: id, name: name, detail: raw['detail'] is String ? raw['detail'] as String : …

## floor
- `TaskItem` · `toInt` · `tryParse`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `TaskItem?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if id is! int || name is! String → return null
- **null when:** raw is! Map · id is! int || name is! String · or search exhausted
- **throws:** _none_
