# TasksNotifier.approveProposal — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int id) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_ref` · `workerNotifsProvider`
- **writes:** `state:state↝`
- **calls:** `_byId` · `_patch`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = _byId(id)
- **precond** — if t == null || t.status != 'proposed' → return (void)
- **effect** — _patch(id, (x) => x.copyWith(status: 'active'))
- **effect** — _ref?.read(workerNotifsProvider.notifier).addForWorker(t.worker, emoji: '✅', title: 'ההצע…

## floor
- `addForWorker` · `copyWith` · `read`

## חוזה · contract
- **input:** `(int id)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if t == null || t.status != 'proposed' → return (void)
- **throws:** _none_
