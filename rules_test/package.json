{
  "name": "buildsmart-rules-test",
  "version": "1.0.0",
  "description": "S5.8 emulator suite for /firestore.rules — @firebase/rules-unit-testing v4 over node --test. See README.md.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-buildsmart \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0"
  }
}
