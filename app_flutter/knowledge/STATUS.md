# Status snapshot — app_flutter

_Version label: `v6.23` (see `lib/version.g.dart`, generated by `scripts/gen_version.sh`).
Update **only here** on a deliberate release — build advances automatically (לקח #72)._

## v6.23 — תיקון כניסת-בעלים: כפתור "כניסה עם Google" במסך הראשון

באג מבוי-סתום: בבילד-המחובר `OnboardingGate` כולא משתמש לא-מחובר ב-`_OpeningFlow` עד `auth.user != null`,
אבל כניסת-ה-Google של הבעלים (מנהל) הייתה נגישה רק מתוך `HomeShell` — שלא נגיש בלי להתחבר → מעגל סגור,
והבעלים (חשבון Google) לא יכול היה להתחבר כלל (register חסום=email-already-in-use, email-login חסום=אין סיסמה).
תיקון: כפתור **"כניסה עם Google (בעלים)"** נוסף ישירות ל-`welcome_screen` (gated ל-`useFirebaseBackend`,
isOwnerEmail בצד-שרת), ו-`_managerGoogleLogin` מסמן `welcomeSeen` כך שאחרי התחברות השער מעביר ל-`HomeShell`.

## v6.22 — חיבור-לשרת: תיקון גשר-הסנכרון (A1) + 5 תיקונים (real-fleet)

orders/chat נצמדים-מחדש ל-repo אחרי התחברות (gated ל-`useFirebaseBackend`) → הסנכרון החי כבר לא קופא על
ה-demo seed אחרי ההתחברות הראשונה. + הזרקת gateway לאשראי (`computeCredit` נקרא בפועל), מירור פרופיל
(מקצוע/כתובת/ח.פ.) ל-`users/{uid}`, feed-התראות כן, מהלכי-מלאי דרך ה-repo, ו-guard נגד seed ל-DB אמיתי.
נמצא+אומת ע"י הצי האמיתי (auditors→validators→supervisor) + fixers + gate-fixes. ראה `WIRING.md`.

## v6.21 — מד-חיבור חי (🟢/🔴) + סטטוס מסירה כן בצ׳אט (✓✓/❌)

מד-חיבור always-on בראש כל מסך (`connection_indicator.dart` + `connection_status.dart`):
🟢 מחובר / 🔴 מנותק·פעולות-לא-יישמרו / אפור דמו — נגזר מ-connectivity+auth+diag-probe.
צ׳אט: סטטוס-מסירה כן לכל הודעה (🕐 נשלח → ✓ → ✓✓ הגיע-לשרת / ❌+נסה-שוב). מסירה ב-CI:
auto-deliver של בילד flags-ON ל-testers + versionCode נגזר מ-run_number (כל בילד ייחודי-עולה,
מתקן את באג "כל בילד נראה כאותה גרסה 1.4.2+8" שמנע מאנדרואיד להציע עדכון).

## v6.20 — חיווט קבלן↔עובד (server-ready, נחיל) · גל 0: שדרת employerId

שדרת ה-`employerId` — החוסם-על שכל גלי-החיווט תלויים בו. `BoardSession` (`board_auth.dart`) +=
`employerId` (מקושר עובד→קבלן; מירור מדויק של `uid` — אופציונלי, field-economy ב-toJson, back-compat
ב-fromJson; `enterDemo` עובד/שליח → `kDemoContractorId` מתויג DEMO-SEED; `login` נושא מהחשבון; snapshot
'' עד claim-שרת). resolver חדש `employerProfileProvider` (`employer_link.dart`) →
`EmployerProfile{name,businessId,address,contact}`: ריק→ריק (אין-המצאות מעסיק), לא-ריק→הקבלן-על-המכשיר
(`userProfileProvider`); SERVER-SWAP `contractors/{employerId}`. טופס 101 (`worker_forms_screen.dart`)
קורא עכשיו את בלוק-המעסיק דרך הקישור (`session.employerId`) במקום `userProfileProvider` הישיר —
**סוגר את חור-היושר ב-#106**. נחיל: B1 board_auth · B2 employer_link · B3 worker_forms (disjoint, donning).
שער: analyze 0 · כל הסוויטה ירוקה · build · conformance 7/7 · required-tests 6/6 · supervisor CLEAN ·
mutation 3-RED→GREEN. בנוי על הענף-הראשי המאוחד (worker-board+origin מוזגו ונדחפו קודם).

**גל T1 · איחוד מנוע-המשימות:** 2 המנועים (`tasksProvider` עשיר + `workerTasksProvider` רזה — dual-write + post-frame-mirror שביר) → מנוע אחד: `tasksProvider` מקור-יחיד; `workerTasksProvider` הפך ל-**shim מעביר** (מקרין מ-tasksProvider, אפס מצב-עצמאי). `TaskItem` += `employerId`/`assignedWorkerUid` (נחתמים מה-session בשליחה, write-when-non-empty, seed byte-identical). fold של orderId→advance-on-approve (אישור משימה-מקושרת מקדם את ההזמנה). seam ריק `bindRemote` ל-T3. נמחקו dual-write + `_mirrorManagerDecisions`. נחיל: T1a מנוע+מודל · T1b צרכנים+6 טסטים. שער: analyze 0 · 6 טסטי-משימות + סוויטה · supervisor CLEAN · mutation RED→GREEN (order-advance).

**גל T2 · יצירת-משימה אצל הקבלן + אישור-קבלן:** `TasksNotifier` += `createTask`/`editTask`/`assignTask` (additive). מסך-הקבלן (`tasks_screen`) קיבל ＋'משימה חדשה' (שם/פירוט/שלבים/דדליין/הקצאה-לעובד מ-`kWorkers`, חותם `employerId`), עריכה, ומקטע 'אישורי עובדים (קבלן)' שמאשר/דוחה דרך המנוע — **הקבלן יוצר/מקצה/מאשר → העובד רואה חי**. אישור מקבילי (מנהל לא-נגוע; הסרה-מלאה parked). נחיל: T2a מנוע+מסך · T2b 2 טסטים. שער: analyze 0 · סוויטה · supervisor CLEAN · mutation RED→GREEN (id-minting). (כיסוי-follow-up: widget-test לגיליון-היצירה — ההתנהגות נבדקה במנוע.)

**גל E1 · עובד קורא מלאי-מעסיק (read-only):** `employerStockProvider` (`employer_stock.dart` חדש) פותר את `session.employerId` למלאי-הקבלן (`stockProvider`) כ-`EmployerStockItem{name,location}` read-only (ריק→[], אין-המצאות; SERVER-SWAP: stock scoped ל-employerId). גיליון '📦 מלאי הקבלן' (`worker_employer_stock_sheet` חדש) + כפתור בלוח-העובד — העובד **רואה** את מלאי-הקבלן, לעולם לא משנה. נחיל: E1a provider+sheet+entry · E1b 4 טסטים. שער: analyze 0 · סוויטה · mutation RED→GREEN (read-only-additive — סקירה מוקלת; sheet-render = widget-test follow-up).

**גל E2 · זמינות-ציוד מול מלאי-הקבלן:** `equipment_stock_join.dart` (חדש) — `availabilityFor(label, stock)` **token-aware** (exact / ≥2-token contiguous; single-token→exact-בלבד) → צ'יפ 🏬מחסן/🏗️אתר/'זמינות לא ידועה' בכל שורת-ציוד ב-#112 (`worker_equipment_checklist_sheet`). אין-המצאות: **המפקח תפס** ש-contains גולמי ממציא זמינות (מפתח/שקע/גו⊂גומי/cross-space) → תוקן ל-token-aware + 6 מקרי-false-positive נעולים→unknown. `equipmentForTasks` byte-identical · העובד read-only. נחיל: E2a join+sheet · E2b test · E2-fix. שער: analyze 0 · 16+regression · supervisor (תפס→תוקן) · RED→GREEN = ההדגמה-האמפירית של המפקח + הנעילה. (curated mapping-table = refinement עתידי.)

**גל E3 · בקשת-חומר עובד→קבלן (דו-כיווני-חי):** `material_requests_engine.dart` (חדש) — `MaterialRequest{id,employerId,workerUid,workerName,items,note,status,createdTs}` + `submit`(עובד)/`setStatus`(קבלן, terminal-guard) + `requestsForEmployer`/`requestsForWorker` (families) + bindRemote seam (Z). העובד שולח בקשה מובנית מ-'🧱 בקש חומרים' (לא chat-stub) ורואה סטטוס חי; הקבלן רואה ב-'📥 בקשות חומר' (stock_screen) ומקדם requested→ordered→supplied/declined. העובד **לא נוגע במלאי** (ישות נפרדת). נחיל: E3a מנוע+עובד · E3b inbox+7 טסטים. שער: analyze 0 · 7 טסטי-דו-כיווני · supervisor CLEAN (RED→GREEN = הטסטים ההתנהגותיים + המפקח; mutation פורמלי דולג בישות מבדקת-היטב). firebase→Z.

**גל H1 · אישור-חופשה → קבלן (מקבילי):** `VacationRequest` += `employerId` (additive, back-compat) + `submit({employerId})` + `requestsForEmployer` (worker-role, employer-scoped, newest-first דטרמיניסטי-reversed). מסך-קבלן חדש `contractor_hr_sheet` ('👷 חופשות עובדים' ב-tasks_screen) — הקבלן מאשר/דוחה דרך המנוע הקיים, **פעמון-אחד** לעובד + צ'אט→th-worker-contractor (כמו המנהל, ממוקד-קבלן). worker_forms: 'לאישור המנהל'→'לאישור הקבלן'. **מקבילי** (מנהל לא-נגוע, byte-identical; הסרה-מלאה parked). נחיל: H1a engine+worker · H1b sheet+entry · H1c 8 טסטים + 2 תיקונים (decode לא-בטוח→דפנסיבי · createdTs-tie→reversed-דטרמיניסטי). שער: analyze 0 · 8 טסטים · supervisor CLEAN.

**גל H2 · תעודות + הדרכות עובד → קבלן (גלוי + approve-back):** `WorkerCert`/`WorkerTraining` += `employerId` (additive, back-compat, decode דפנסיבי) → families `certsForEmployer`/`trainingsForEmployer` (employer-scoped, reversed-דטרמיניסטי). הדרכות קיבלו מחזור-חיים אמיתי: `kTrainingRejected` + `approve`/`reject` (`_decide` guarded pending→approved/rejected) — עד כה ה-lifecycle נתקע ב-`pending` בלי מאשר קיים. `contractor_hr_sheet` הורחב: מקטע-אישור-הדרכות (pending → אשר/דחה → **פעמון-אחד** + צ'אט→th-worker-contractor) + מקטע-תעודות **READ-ONLY** עם באנר-תוקף מאוגד (`statusAt` → פג/לקראת-חידוש). `worker_safety` מטביע `employerId` בכל add. demo-seed הדרכות מתויג `kDemoContractorId` (קוהרנטי, DEMO-SEED). **מקבילי** (מנהל לא-נגוע). נחיל: H2a state+טסטים · H2b קבלן-sheet ‖ worker-stamp. שער: analyze 0 · +30 טסטים · supervisor CLEAN · mutation RED→GREEN (guard pending). firebase→Z.

**גל H3 · מדיניות מסמכים-נדרשים שהקבלן מגדיר (אכיפה בשער-העובד):** `required_docs_policy.dart` (חדש) — `RequiredDocsPolicyNotifier` (employerId → שמות-תעודות נדרשים) + `requiredDocsForEmployer` family + `normalizeDocName` (trim+collapse+lowercase). שער-המוכנות (#101) של העובד אוכף עכשיו את מדיניות-הקבלן כ**שכבה-נוספת**: `workerDocsReadiness` קיבל `requiredCertNames` (ברירת-מחדל `[]` = התנהגות-של-היום בדיוק), והספק החי קורא `requiredDocsForEmployer(session.employerId)`. ההצלבה **normalized-exact, לא substring** (לקח E2 — אין-המצאת-סיפוק; תעודה פגה לא מספקת דרישה). `contractor_hr_sheet` הורחב בעורך-מדיניות (📋 מסמכים נדרשים — הוסף/הסר + צ'יפי-הצעה אמיתיים + empty-state כן). **רצפת-חובה:** הקבלן יכול רק להחמיר — טופס-101 ותעודה-פגה נשארים חוסמים תמיד (מדיניות ריקה לא הופכת עובד לא-בטוח ל'מוכן'). **שליחים byte-identical** (courier↔חנות נפרד). נחיל: H3a state+gate+טסטים · H3b עורך-קבלן. שער: analyze 0 · +52 טסטים (כולל 14 קיימים) · supervisor CLEAN (10/10) · mutation RED→GREEN (substring-trap). firebase→Z.

**גל S · נוכחות-עובד → תצוגה-חיה אצל הקבלן (read-only):** `AttendanceDay` += `employerId` (מירור `WorkerCert`: field-economy, decode דפנסיבי) — `clockIn` חותם `session.employerId`, `clockOut` משמר. `attendanceForEmployer` (חדש, family) קורא את **חנות-העובד בלבד** (שליחים מודרים-במבנה — חנות נפרדת `bs.courier-attendance.v1`, לא תלוי בשדה-session). helper טהור `clockedInNow` (יום-פתוח: היום · נכנס · בלי-יציאה). מסך-קבלן חדש `contractor_attendance_sheet` ('🕒 נוכחות עובדים' ב-tasks_screen) — '🟢 נוכחים עכשיו' (שעת-כניסה + צ'יפ-מיקום דרך `openNavSheet`, רק אם GPS אמיתי — **אין-המצאת-קואורדינטה**) + 'היום' (כניסה/יציאה/שעות; יציאה='—' עד החתמה). **read-only** (הקבלן רואה, לא עורך). העובד ללא-שינוי-מראה. נחיל: S-a state+טסטים · S-b worker-stamp ‖ S-c קבלן-sheet. שער: analyze 0-errors · +26 טסטים (כולל 19 קיימים) · supervisor CLEAN (10/10) · mutation RED→GREEN (employer-scope). firebase→Z.

**גל G1 · העובד פותח משימה → 'proposed' → אישור הקבלן:** `TaskItem` += `createdBy` (additive, field-economy, decode דפנסיבי) + סטטוס חדש `'proposed'` (תווית '📝 הוצעה'). `proposeTask` (העובד מציע, createdBy='worker', משויך-לעצמו אך לא-פעיל), `approveProposal`(proposed→active)/`rejectProposal`(proposed→rejected) — **מבודדים** מ-`approve`/`reject` (ששומרים `'review'`=השלמה; אין התנגשות-מחזורים, ואין הענקת-מטבעות/קידום-הזמנה בהצעה). `pendingProposalsProvider` (תור-ההצעות). לוח-עובד: '➕ הוסף משימה' + מקטע '📝 הצעות שממתינות לאישור' (העובד רואה את ההצעה; מוחרגת מטבעת-ה-total). לוח-קבלן: מקטע-אישור-הצעות נפרד → פעמון-עובד אחד + צ'אט th-worker-contractor. נחיל: G1a engine+טסטים · G1b worker-UI ‖ G1c contractor-UI; **המפקח תפס** שההצעה לא-נראית-לעובד → תוקן (מקטע ייעודי + total). שער: analyze 0-errors · +15 טסטים · supervisor CLEAN · mutation RED→GREEN (בידוד-guard). גאנט=G2 · ליקויים=G3. firebase→Z.

**גל G2 · גאנט כתצוגה מעל מנוע-המשימות:** `TaskItem` += `scheduledStart` (DateTime? אופציונלי, null=לא-משובץ; serialize write-when-non-null כמו startedAt; copyWith sentinel→ניתן-לניקוי). `lib/logic/tasks_gantt.dart` (חדש, **טהור**) — `buildTasksGantt` → `TasksGanttLayout{bars,spanDays,unscheduled}` (offset-מהמוקדם, len=days≥1, done% מ-doneSteps/status, מיון דטרמיניסטי startDay→id). מסך `tasks_gantt_sheet` (**read-only**, נגיש מקבלן '📊 גאנט משימות' + מעובד) — בר פרופורציונלי לכל משימה עם **התאריך האמיתי** (earliest+offset) + רשימת 'ללא תאריך מתוזמן' (**אין-המצאת-תאריך** — משימה בלי scheduledStart לא מקבלת bar). הקבלן משבץ תאריך-התחלה ב-`_TaskAuthorSheet` (author-start). נחיל: G2a state+logic+טסטים · G2b sheet+date+entries. שער: analyze 0-errors · +23 טסטים · supervisor CLEAN (10/10) · mutation RED→GREEN (len≥1). ליקויים=G3. firebase→Z.

**גל G3 · ליקויים = סוג של משימה (ליקוי כמשימה):** `TaskItem` += `kind` (task/defect) + `location`/`severity` (אדיטיביים, field-economy, decode דפנסיבי, נשמרים ב-copyWith/assignTask). `createTask`/`proposeTask` מקבלים kind/location/severity; `editTask` מקבל location/severity. `defectsProvider` (כל ה-kind=='defect'). מסך `defects_sheet` (תלוי-תפקיד): הקבלן פותח ליקוי (createTask→pending), העובד מדווח (proposeTask→proposed). אישור ליקוי-שדיווח-עובד דרך **אותו** בלוק-אישור-הצעות (G1 — approveProposal משמר kind). נגיש משני הלוחות (🔧 ליקויים). **המפקח תפס** שליקוי-שהקבלן-פתח נחתם ב-session ריק (תפקיד-מנהל בלי employerId) → נעלם מרשימת-הקבלן; תוקן ל-`kDemoContractorId` (כמו author-המשימות). נחיל: G3a state+טסטים · G3b sheet+entries. שער: analyze 0-errors · +29 טסטים · supervisor (תפס scope→תוקן) · mutation RED→GREEN (kind filter). firebase→Z. **— בכך הושלם חיווט קבלן↔עובד שביקש המשתמש (T·E·H·S·G).**

## v6.19 — לוח עובד v3 מלא (#100–#114, נחיל אמיתי, 4 גלים)

לוח-העובד נבנה והורחב דרך נחיל ה-/swarm הקנוני (donning + manifested-gate + mutation-verify +
supervisor) בארבעה גלים על גבי גל-D — 15/15 משימות. **גל 1:** פרופיל מורחב + אזור-אישי סטטוס-חי +
הגדרות-אקורדיון + צ'אט מנהל→תמיכה (#102/#103/#104/#110). **גל 2:** נוכחות-GPS בבית (seam geo web/VM
conditional-import) · לוח-שנה חודשי מחווט · מיקום→ניווט פנימי (Waze/Maps seam url_launcher) · בית-עובד =
יומן עם היום לחוץ-מראש (#100/#105/#111/#113). **גל 3:** טופס 101 v2 (מילוי-אוטומטי עובד מ-#104 +
מעסיק מ-userProfileProvider · חתימה CustomPaint dart:ui · תיבת-הצהרה · יצוא PDF/הדפסה seam · שמירה/
שליחה — חסום עד הצהרה+חתימה) · חופשה/מחלה v2 (הצהרה+חתימה) · תיק-בטיחות הדרכות (store אמיתי
bs.worker-trainings.v1: הוספה/הסרה/העלאת-מסמך/צפייה/שליחה-לאישור) (#106/#107/#108). **גל 4:** לוח-משימות
צלילה היררכי (מקובץ-מצב → גיליון-שלבים #71, אין דרג-stage מומצא) · דוחות-דרילדאון (כל KPI/מקטע לחיץ,
נתונים חיים, מסגרת "מועדון משותף" נשמרת) · "בדוק ציוד נדרש" (מ-recommendedKitForProduct, לא ממלאי-קבלן) ·
שער-מוכנות-מסמכים קשיח עובד+שליח עם seam-בדיקות (#114/#109/#112/#101). ללא תלות pub חדשה (חתימה=dart:ui ·
הדפסה=package:web מאחורי conditional-import כמו geo). אמת: אין-המצאות (מילוי מ-providers אמיתיים בלבד,
DEMO-SEED מתויג), back-compat (שדות אופציונליים, JSON ישן→ברירות-מחדל). שער כל גל: analyze 0 · כל
הסוויטה ירוקה (~2330) · build · conformance 7/7 · required-tests 6/6 · mutation אדום→ירוק · supervisor CLEAN.

## v6.18 — גל-D: פוליש עובד/שליח/חנות (#98, נחיל אמיתי)

Worker/courier/store polish via the canonical `/swarm` fleet WITH donning + the manifested gate
(57→32 CONFIRMED + 3 ADJUST + 0 FP + 2 DEFER). Applied to the WORKER board the shared-defect fixes
that #86/#87 had landed on courier/store but deferred on worker: in-flight save guards (profile/cert/
sick-note/attendance — no more double-pop or fake-success-on-quota), AA contrast (redAccent→dangerDark,
white→bsOnAccent on the success-green clock button at ~1.9:1), screen-reader excludeSemantics, image
cacheWidth/cacheHeight (+ courier avatar + store POD thumb residuals), the settings⇄profile nav-loop
removed (#F-40), form-101 chat line now carries displayName, vacation list filtered by role (demo
cross-role leak closed). Store residual: store_settings danger colors + store_dashboard logout wording.
DEFERRED (#99): rewards per-username + workerNotifs role-scope (refactor beyond polish; honest
"מועדון משותף" label applied meanwhile). Gate: analyze 0 · full suite · build · conformance · required-tests.

## v6.17 — אזור אישי v2 לשליח ולספק (#86/#87, נחיל קנוני)

Personal-area v2 via the canonical 9×9 fleet (57 findings → 57 confirmed → 10 disjoint fixers →
supervisor CLEAN → gate). Courier (#86): editable profile (name/phone/preferred-vehicle/photo,
`bs.courier-profile.v1`), attendance clock + monthly ledger + send-report-to-STORE, forms (101 ·
vacation via the shared manager queue with a `role` field · sick-note photos), driver certs with
expiry traffic-light (`bs.courier-certs.v1`), payslips sheet reused as-is, and the global-stats bug
fixed — deliveries/POD now attributed per-courier (`Fulfillment.courierUser`, stamped at
POD/delivery moments; courier-clock stamps wired at all 3 advance points — the dead
`bs.courier-clock.v1` key now has writers). Supplier (#87): business profile per-username
(`bs.store-profile.v1`, honest migration off the global `bs.supplier-settings.v1`), live board
identity override, business certs, SERVER-READY documents sheet, 5th אזור-אישי tab, store stats
incl. `deliveredRevenue`. Cross: courier↔store chat audience bridge, vacation decisions routed
per-role, chat archive/mute/lastread nested per-username, `BsTokens.dangerDark`, mounted guards in
8 stores, sys_chat merge-on-write + persist cap. Worker-screen instances of shared defects:
deferred to `_backlog.md` (out of mandate).

## v6.16 — deep correctness / perf / a11y pass (rounds 4–6, validated)

Three more audit rounds on v6.15 — round 4 (cross-role integrity · money/numeric · edge/crash ·
state-leakage · navigation), round 5 (perf/rebuilds · accessibility/RTL · async/race · seed-integrity ·
text parity), round 6 (adversarial VALIDATION of all ~58 findings + verbatim cross-check vs the Preact
legacy) — then the validated ~38 fixes. Highlights: manager express-fee display ₪80→₪120 (matched the
charged fee); contractor stage labels aligned to the canonical map; the manager customer/order detail
sheets now read LIVE engine data (were frozen snapshots); load-clobber races (cart/orders/favorites)
fixed via a `_loaded` guard; checkout + clear-cart + settings-reset + resetAllDials now reset completely;
double-checkout guarded; per-keystroke search / per-row cart / ×N category scans moved to derived
providers; targeted RTL / overflow / reducedMotion fixes; seed item-counts + viewMode fallback + JSON
casts corrected. Validation dropped ~7 false-positives and DEFERRED two large a11y initiatives
(app-wide Semantics, highContrast token migration). Verbatim-legacy strings kept ('פקדונות', '(לשמש)').
Details: `knowledge/WIRING_AUDIT.md`. analyze 0 · 1536 tests green.

## v6.15 — third wiring-audit pass + store-orders refactor

A third audit (5 agents: onboarding/auth · money-path · full settings screens · hubs/tools ·
form-persistence) on v6.14. Headline fix: the contractor's "הזמנות שלי" no longer reads a
RAM-only parallel list — `storeOrdersProvider` is now DERIVED from `ordersEngineProvider`
(single source of truth), so a placed order keeps one id, shows its real line items + address
+ notes, follows live manager/courier stage advances, and survives restart; the order timeline
gained its missing `new`+`pickup` stages. Also: supplier out-of-stock toggles + user-added
project names now persist; notification quiet-hours actually mute the list (new `isInQuietHours`
gate, like snooze); the onboarding profession choice now seeds the catalog detail-mode; the
account dial leaves are editable; store sort/display settings are applied; the 6 supply-chain
service-sheet rows + the SLA tile are now honest. (~60 forward-compat settings fields left as
honest scaffolding, by design.) Details: `knowledge/WIRING_AUDIT.md`. analyze 0 · 1536 tests green.

## v6.14 — second wiring-audit pass (deeper sweep)

A second audit (4 agents: stub-hunt · dead-provider · hubs/deep-flows · full dial sweep)
on v6.13 found ~18 more, all fixed: chat custom-greeting now honored; notification snooze
now actually mutes; notification 'אשר איסוף' opens store-orders ('עקוב' → honest 🚧); the
BS-dial manager **metric** tiles + store/courier portal + worker task leaves now read the
live providers; install-studio cart carries the real price (was ₪0); store-favorites tap
navigates; false-confirm delete/export buttons (chat/store) → honest placeholders; dead
region/security dial settings + 14 orphaned `AppSettings` fields marked honest/deprecated.
Details: `knowledge/WIRING_AUDIT.md`. analyze 0 · 1536 tests green.

## v6.13 — wiring-audit fixes (FAB/dial + cross-role)

Post-cutover audit (5 agents, ~19 findings) → fixed the dial/shortcut layer: search-FAB
sort/filter now set the real providers; menu-dial accessibility (text/contrast/motion) +
notification toggles + 🛒 הסל שלי now hit the providers the app actually reads; the BS-dial
manager order/customer panels read the live engine (not a static seed); courier "בדרך" stat
counts pickup+transit; manager customer sheet stays live while open; catalog-settings ⛔
controls marked as placeholders. Details: `knowledge/WIRING_AUDIT.md`. analyze 0 · 1536 tests green.

## v6.12 — unified: v6.11 catalog + manager control-center + shared live engine

Merges the v6.09–6.11 catalog/parity track onto the manager branch — one build,
all four roles on a single live `ordersEngineProvider` (contractor · store · courier ·
worker) + the manager control-center + the latest catalog. Clean merge, disjoint
files, 0 conflicts.

## 100% PDF-parity coverage לכל 3 המותגים — gate 117 closeout (v6.11)

הסבב מסיים את ה-coverage. v6.10 נעל ~20 SKUs מדגם לפולירול וחוליות; כאן נעל **את כל
הקטלוג של 2 המותגים** (774 + 170 = 944 SKUs) ע"י snapshot מ-runtime-state של ה-catalog
שאומת על 13 עמודי-מדגם בקטלוג ה-PDF.

**יצירת ה-snapshots:**
- כתבתי טסט-דחיה ש-dump-מ-runtime את כל ה-SKUs/nameHe/page מ-`kPolyrollCatalog` ו-`kHuliotCatalog`.
- Python ייצר Map ב-Dart const literal: `test/_polyroll_snapshot.g.dart` (774 ערכים) +
  `test/_huliot_snapshot.g.dart` (170 ערכים).
- ה-snapshots נוצרים מ-runtime, כך שכל מק"ט שמופיע ב-catalog (גם דרך `_ppr`/`_sl` helpers
  וגם דרך `LipskeyCatalogProduct(...)` ישיר) נכלל אוטומטית.

**ארבעת הטסטים בכל parity test (לפולירול ולחוליות):**
1. כל snapshot SKU קיים ב-catalog.
2. כל catalog SKU קיים ב-snapshot (תופס "תוספות שקטות").
3. nameHe + page של כל SKU תואמים בין catalog ל-snapshot.
4. כל מוצר נושא ה-brand הנכון.

**אכיפה:**
- `flutter test` — 1435/1435 ירוקים.
- `flutter analyze` — 0 errors.
- mutation_verify לשניהם: typo → "snapshot drift (1). First 5: …" אדום ✅; ביטול → ירוק ✅.
- **כיסוי כעת זהה לליפסקי (~277 SKUs דרך 9 קטגוריות; כאן 944 SKUs דרך snapshot מלא).**

## PDF-parity tests for Polyroll + Huliot — gate 117 closeout (v6.10)

הסבב הזה משלים את החצי השני של ה-PDF-parity שהיה חסר ל-2 המותגים האחרים. ליפסקי
קיבל gate-117 מלא ב-v5.96-v6.08 (277 expectations); פולירול/חוליות "verbatim PDF"
לפי ההערות בקוד אבל ללא טסט-אכיפה. עכשיו יש להם.

**שיטה:** לא העלאת PDFs (הם כבר ברפו כתמונות-קטלוג ב-`assets/{polyroll,huliot_smartlock}/pages/`).
סקרתי 4 עמודי-מדגם (פולירול 18, 80, 40 · חוליות 12, 28) ו-44 SKU תאמו 100% לדאטה
הקיימת. הכתבתי את ה-SKUs האלה ב-test-fixture כ-snapshot לוק.

**טסטים חדשים:**
- `test/polyroll_pdf_parity_test.dart` — 20 SKUs (PN20 supply / Faser / Tees) על עמ' 18, 40.
- `test/huliot_pdf_parity_test.dart` — 13 SKUs (ברכים צד-אחד-חלק / הגבהות / מכסים) על עמ' 12, 28.
- כל טסט אוכף: `nameHe` + `page` + `brand`.

**mutation_verify:**
- פולירול: `'אספקת מים 20'` → `'אספקתX מים 20'` (95016002) → אדום ✅; ביטול → ירוק.
- חוליות: `'ברך 15° צד אחד חלק 40'` → `'ברך 15X…'` (70041150) → אדום ✅; ביטול → ירוק.

**אכיפה כללית:**
- `flutter test` — 1460/1460 ירוקים (אפס regressions).
- `flutter analyze` — 0 errors.
- 3 המותגים עכשיו עם PDF-parity tests + mutation_verify.

## Lipski UI parity with Polyroll/Huliot — gate 117 follow-up (v6.09)

ה-routing של רנדור הכרטיסים אוחד: ליפסקי עובר מ-`_NameWords` (מילה-מילה) ל-`_HierarchyChips`
(ברירת-מחדל מובנית, כמו פולירול/חוליות).

**שינויי קוד:**
- `lib/data/chip_hierarchy.dart`:
  - חדש: `_kCompoundTypes = {'מיכל הדחה', 'מושב אסלה'}` + לולאת lookahead ב-`parseChips`
    לפני בדיקת single-token type (כך `מיכל הדחה ברקת לבן` נכנס כ-1 type chip).
  - `kChipTypes`: נוספו `קולט / זרוע / פיה / כובע` (Lipski).
  - `kChipLevel2Shape`: נוסף `87°` (Lipski insertion bend angle).
  - `kChipLevel3Feature`: נוספו דגמי Lipski (`ספיר/ברקת/טופז/יהלום/טיטאן/כנרת/חרמון/אדיר/תבור/כרמל/הגייני`)
    + תכונות (`מונובלוק/משולב/טרמו/ULTRA/תיקני/בודד/פרגמון/מס./למקלחת`).
  - `_l3Compounds`: נוספו `סגירה רכה / אנטי ונדליזם / ציר ניירוסטה / ציר פלסטיק /
    דו צדדי / חתך שטוח` + `(מס. 1)..(מס. 9)` למחסומי-מטבח.
  - `sizeRe`: נוסף `^DN\d` (Lipski pipes label בור כ-`DN40`/`DN110`).
- `lib/screens/lipskey_products_screen.dart:1176` — תנאי-routing הוסף `|| p.brand == 'ליפסקי'`.
- `test/card_dims_dn_chip_test.dart` — הטסט הראשון עודכן לאחר ש-SKU 116624 קיבל
  שם עם בור-בשם (v6.01), כדי לאמת ש-bore גלוי בכל מתכונת רנדור.

**אכיפה:**
- `test/lipskey_hierarchy_parity_test.dart` (חדש) — 18 expectations על 18 SKUs מייצגים.
- mutation_verify: שינוי `'מיכל הדחה'` → `'מיכלX הדחה'` ב-`_kCompoundTypes` → אדום ✅; ביטול → ירוק ✅.
- `flutter test` — 1418/1418 ירוקים (כולל כל test החוליות/פולירול הקיים).

## Lipski floor traps PDF parity — gate 117 (v6.08, **9/9 הושלמה**)

קטגוריה 9 (אחרונה!) — מחסומי רצפה תיקניים (עמ' 26–27, 8 SKUs):
- 4 SKUs ב-`מחסום תיקני 140/50 / 245/50` (פתוח/סגור/גבוה/למקלחת).
- 4 SKUs ב-`מחסום (תופי-)קומקום פתוח/סגור 40/155 וגם 50/175`.
- **שמות:** 116167/116163 היו `מחסום קומקום פתוח` (חסר "תופי-") → תוקנו.
  220542/220543 'גבוהה' → 'גבוה' (התאמה מגדרית למחסום [ז']).
- **qty הושלם** ל-218681 (20/400) ו-218722 (30/600) שהיו null.
- **דפים** 14 → 26 (4 רשומות) ו-27 (4 רשומות).

## כל 9 הקטגוריות סונכרנו ל-PDF — סיכום מסע gate 117

| # | קטגוריה | SKU | גרסה |
|---|---|---|---|
| 1 | מיכלי הדחה | 23 | v5.96 |
| 2 | מושבי אסלה | 26 | v5.99 |
| 3 | מחסומים גלויים | 32 | v6.00 |
| 4 | אביזרי שקע-תקע (4a+4b+4c) | 49 | v6.01–6.03 |
| 5 | מאספים/כיסויים | 19 | v6.04 |
| 6 | אטמים/פקקים | 17 | v6.05 |
| 7 | אביזרי תבריג | 43 | v6.06 |
| 8 | צינורות | 57 | v6.07 |
| 9 | מחסומי רצפה תיקניים | 8 | v6.08 |
| **סה"כ** | | **274** | **gate 117: 277 expectations** |

ה"ירוקים" בכרטיסי ליפסקי נפתרו בשורש — שמות `nameHe` תקינים, מודלים/צבעים/מידות
מוכרים ע"י `_attrKindFor`, ה-chips נבנים נכון בלי להתבסס על fallback ירוק.

## Lipski pipes PDF parity — gate 117 (v6.07, קטגוריה 8/9)

קטגוריה 8 — צינורות (עמ' 47–48, 57 SKUs):
- **3 משפחות-צבע:** אפור (21) + שחור (16) ש"ת בעמ' 47; כתום PP-MD-ML SN4/SN8 (11) +
  שחור SUPER BETON/SILENT (9) בעמ' 48.
- **stubs אוחדו ל-real entries:** 13 stub-rows ב-end-of-file (`nameHe: 'צינור אפורה'` —
  שם גנדריאלי שגוי לכל ה-blacks) → רשומות מלאות עם `nameHe: 'צינור {שחור|כתום} DN{N} L={L} ס"מ'`,
  color, dims, page נכון.
- **שמות אוחדו:** כל `nameHe` ל-`'צינור {color} DN{N} L={L} ס"מ'`.
- **dims:** `'DN'` + `'L (cm)'` הושלמו לכל ה-57 רשומות.
- **דפים תוקנו** 24/25 → 47/48 (לפי הקטלוג המודפס).
- color הושלם לכל רשומה.

## Lipski screw-on accessories PDF parity — gate 117 (v6.06, קטגוריה 7/9)

קטגוריה 7 — אביזרי תבריג (עמ' 20–23, 43 SKUs). re-read מהמקור:
- **שמות אוחדו** ל-40 רשומות: היו `'ברך 90° - תבריג צד אחד'` או `'ברך 90° תבריג צד אחד DN32/32'`
  (אי-עקביות) → עכשיו `'ברך {זווית}° תבריג {sub} {DN1/DN2}'` (DN חלק מהשם).
  כולל: ברך 90°/45°/30°/15° תבריג צד אחד/כפול/ש"ת לסיפון, ברך 90° טלסקופית,
  מסעף 45°/90° תבריג + מסעף 45° צד ללא תבריג, מחבר כפול תבריג, מצרה תבריג, סט מפתחות + מפתח לאביק.
- **DN הושלם** ל-7 רשומות (dims `'dn': null` → `'DN': 'D1/D2'`).
- **qtyPack/qtyPallet הושלמו** לכל ה-43 רשומות (היו null).
- **116589 חסר היה לחלוטין** — נוסף (`מסעף 90° תבריג 32/32`) + VerifiedSpec ב-
  lipskey_verified_connections.dart (אחרת compat_coverage נופל מ-100%).
- **journey-test עודכן:** `116209` חיפש 'מחבר כפול' → עכשיו 'מחבר כפול תבריג 32/32'.

## Lipski gaskets/plugs PDF parity — gate 117 (v6.05, קטגוריה 6/9)

קטגוריה 6 — אטמים אומים ופקקים (עמ' 36–37, 17 SKUs). re-read מהמקור חשף תסבוכת SKU:
- **506525** היה `אטם דו צדדי` — בעצם **אטם לכוס 2"** (Glass gasket; nameEn כבר אמר זאת).
- **610708** היה `אטם לכוס 2"` — בעצם **פקק שטוח לתבריג 2⅜"** (Flat screwing plug).
- **610706 phantom נמחק** — לא קיים בקטלוג; הפקק 2⅜" האמיתי הוא 610708.
  הוסרה גם הפנייה ב-`lipskey_verified_connections.dart`.
- **614783** `1/2"` → `1½"` (הקטלוג: 1½"); **612386/612385** `טנת אפור/לבן` →
  `2" אפור`/`2" לבן`; פקקי-רצפה (218127/218126) קיבלו מידה (2.5"/2").
- **506540** qtyPack `750` → `500` (לפי הקטלוג).
- דפים 19 → 36/37.

## Lipski collectors/covers PDF parity — gate 117 (v6.04, קטגוריה 5/9)

קטגוריה 5 — מאספים/קולטים + כיסויים/רשתות (עמ' 30–33, 19 SKUs). re-read מהמקור:
- **באגי-צבע תוקנו:** 661360 היה `color: לבן` (שם "אפור") → אפור; 610920 היה
  `color: פרגמון` (שם "אפור") → אפור; 610911/635736 היו `color: null` → לבן/פרגמון.
- **196687** dims DN `130/40` → `130/50` (זה מערכת D 50, לא 40).
- **171191** `גבוהה` → `גבוה` (לפי הקטלוג).
- **דפים תוקנו** 16/17 → 30/31 (מאספים) ו-32/33 (כיסויים).
- **qtyPack/qtyPallet הושלמו** ל-610920 (120/5400) ו-610911/635736 (150/6750).

## Lipski connectors/reducers/plugs PDF parity — gate 117 (v6.03, קטגוריה 4c/9)

תת-קבוצה c — מצמדים, מצרות, פקקים, כובע אויר (עמ' 44–45, 21 SKUs):
- **17 שמות שגויים תוקנו** (re-read של עמ' 44–45 מה-PDF המקור):
  - **120311** היה `פקק להכנסה` — בעצם **כובע אויר 110** (Venting cap!).
  - מצרות (217674/217531/218568/220316) היו `מחבר כפול`/`מצרות תבריג` →
    `מצרה {D1/D2}`.
  - מחברים כפולים (116576/218567) היו `מצרות תבריג` → `מחבר כפול {DN}`.
  - 214534 היה `מחבר כפול` DN75 — בקטלוג DN50 ללא מעצור (qtyPallet 4200→4500).
  - פקקי שקע-תקע (218460/218560/116628/220315/805024) היו
    `צינור הכנסה`/`מחבר לצינור הכנסה` → `פקק שקע-תקע {DN}` (בבודדים → qty null).
  - 194898 `מחבר כפול` → `מצמד ארוך לתיקון 110/110`; 194897 → `מצרה לתיקון 110/100`;
    218569 → `פקק חיצוני 110`.
- **DN הושלם** לכל ה-21; qtyPack/qtyPallet הושלמו/תוקנו; דפים → 44/45.
- **categoryHe לא שונה** — 3 דליי-טקסונומיה קיימים (אביזרי שקע-תקע/מצמדים
  וצינורות/פקקים וצינורות); re-bucketing הוא שינוי נפרד.

## Lipski insertion-branch PDF parity — gate 117 (v6.02, קטגוריה 4b/9)

תת-קבוצה b — מסעפים שקע-תקע (עמ' 42, 13 SKUs):
- **שמות תוקנו ל-9 רשומות.** מסעפים תויגו `מחבר כפול` (218564/218176/217533),
  `מסעף 90° - תבריג` (116558/116054/116556/116049), או
  `מסעף 45° - תבריג כפול` (116573/116056/116571/220305) — כולם עכשיו
  `מסעף {45°|87°|כפול} {DN}` עם המידה כחלק מהשם.
- **DN הושלם** ל-5 רשומות שהיו חסרות `dims` (220305/218564/218176/116558/217533).
- **qtyPack/qtyPallet הושלמו** ל-5 רשומות שהיו null.
- **דפים** 22 → 42.

## Lipski insertion-bend PDF parity — gate 117 (v6.01, קטגוריה 4a/9)

קטגוריה רביעית (תת-קבוצה a — ברכיים שקע-תקע, עמ' 40–41, 15 SKUs):
- **כל 15 שמות תוקנו.** היו לקויים מ-OCR/copy-paste:
  - 5 ברכיים 87° (116624/116601/116033/142289/116028) היו `ברך 90° - תבריג כפול`
    (זווית שגויה + תיאור שגוי — הם שקע-תקע, לא תבריג).
  - 3 ברכיים 87° עם ביקורת גב (116031/124843/116026) היו `ברך 30° - תבריג כפול`.
  - 5 ברכיים 45° (116591/116553/161884/190297/116037) היו `ברך 45° - תבריג כפול`.
  - 190297 קיבל סיומת `לבן` (variant של 161884 אפור — אותו דגם, צבע שונה).
- **דפים תוקנו** מ-21 (היה אזור תבריג מוטעה) ל-41 (האזור הנכון: insertion bends).
- מסעפים/מצמדים יבואו ב-batches נפרדים (4b, 4c).

## Lipski visible-trap PDF parity — gate 117 (v6.00, קטגוריה 3/9)

קטגוריה שלישית במסע: **מחסומים גלויים** (עמ' 8–15, 32 SKUs מה-PDF).
- **דפים תוקנו** ל-32 רשומות — היו 5/6/7/8, הקטלוג המודפס אומר 8/10/12/14
  (אגן 1.25" עמ' 8, מטבח 2"+אמריקאי 1.5" עמ' 10, אמריקאי 2" + צד עמ' 12,
  מאריכים+אביזרים עמ' 14).
- **שתי רשומות שמות תוקנו:**
  - 213054: `מחסום (סיפון) אמריקאי 1.25"…` → `מחסום (סיפון) 1.25"…` (היה
    duplicate של 213055; הקטלוג מבחין: 213054 בסיסי, 213055 אמריקאי).
  - 218495: `סיפון אמריקאי 1.5" עם יציאה למדיח` → `מחסום אמריקאי 1.5"`
    (היה duplicate של 171189; הקטלוג: 218495 בסיסי בלי יציאה).
- 1149+32 = 1181 בדיקות ירוקות.

## Lipski toilet-seat PDF parity — gate 117 (v5.99, קטגוריה 2/9)

קטגוריה שנייה במסע הסנכרון: **מושבי אסלה** (עמ' 53–55, 26 SKUs מה-PDF).
- **20 רשומות שגויות תוקנו.** `nameHe` היה גנרי (`מושב אסלה` / `מושב אסלה
  תרמופלסטי` / `מושב אסלה תרמוסטיים`) ללא מודל-דגם (מס. 1/3/3.5/4/5/9, חרמון,
  אדיר, תבור, כרמל, הגייני, ULTRA טרמו) ולא variants (ציר פלסטיק/ניירוסטה,
  סגירה רכה, אנטי ונדליזם, משולב). `page` היה 27/28 — הכל ל-53/54/55.
  צבע + qtyPack + qtyPallet חסרים ב-5 רשומות (220943/218360/218361/220981).
- **4 phantom SKUs נמחקו**: 179370 (טען חרמון פרגמון — האמיתי 186379),
  197134 (טען תבור פרגמון — typo של 187134), 195425 (טען כרמל לבן —
  האמיתי 195505), 107222 (טען "גנדלים" — לא קיים בקטלוג, ה-AMיתי הוא 197222
  הגייני). 5 stub-placeholders ב-end-of-file (179613/186379/187134/195505/197222)
  אוחדו לבלוק הראשי עם דאטה מלאה.
- **`smart_tree.dart` עודכן**: 195425 → 195505 (כרמל לבן), 197134 → 187134
  (תבור פרגמון).
- 1149/1149 בדיקות ירוקות (היה 1114, +35 חדשים, אפס regressions).

## Lipski toilet-tank PDF parity — gate 117 (v5.98, קטגוריה 1/9)

הסבב הראשון מתוך תכנית-עבודה רב-שלבית: סנכרון `kLipskeyCatalog` למקור-האמת
(קטלוג ה-PDF של ליפסקי 2024). קטגוריה 1: **מיכלי הדחה** (עמ' 50–52, 23 SKUs):
- **17 רשומות שגויות תוקנו.** 6 SKUs (152785–7, 145629–31) היו מתויקות כ-
  "מושבי אסלה" עם `nameHe: 'מושבי אסלה תרמופלסטיים'` — בקטלוג זה
  **מיכלי הדחה טיטאן/יהלום** (התקנה גבוהה). 5 SKUs (168525, 169604,
  178864–870) היו "מושב אסלה" — בקטלוג **מיכלי הדחה כנרת/ברקת מונובלוק**.
  4 SKUs (116795, 116798, 154069, 154413) היו עם `nameHe: 'התקנה נמוכה'`
  (placeholder, לא שם). כולם קיבלו `nameHe` מפורש מהקטלוג +
  `dims` (תכולה, גובה, רוחב, עומק כשדות נפרדים) + `page` נכון (50/51/52, היה 26/27).
- **8 phantom SKUs נמחקו** (124040, 124050, 124051, 170862, 170866, 170869,
  116752, 154058) — לא קיימים ב-PDF המקורי. המק"טים שהיו צריכים להיות:
  124848, 124850, 124851, 178862, 178866, 178869, 116792, 154068.
- **`smart_tree.dart` עודכן** — 6 SmartBrand entries שהפנו ל-phantoms הוחלפו.
- **`lipskey_verified_connections.dart` עודכן** — 8 phantom specs הוסרו;
  11 specs חדשים נוספו לקטגוריות שהשתנו.
- **`ten_scenarios_audit_test.dart` Scenario 8** עודכן (`'124050'` → `'124850'`).

מאומת ע"י `test/lipskey_pdf_parity_test.dart` — gate 117, 24 expectations.

קטגוריות נותרות (לפי gap גדול→קטן): מושבי אסלה (17 SKU), מחסומים גלויים (32),
ברכיים/מסעפים שקע-תקע (38), אטמים/פקקים (23), מאספים/קולטים (15),
אביזרי תבריג (30+), צינורות (50+), מחסומי רצפה (8).

## Water/sewage division — fixtures vs pipes (v5.96 — Benzi #1 reframed)
Benzi's "hard part": the plumbing departments are organised as **small headings,
each followed by its category rows** (`category_division.dart`, `kDeptCatHeadings`;
no drill-in "שפכים"/"לבנים" super-category):
- **ברזים וסניטריים** = כלים only → **🚽 כלים לבנים** (אסלות) · **🛁 כלים גמר**
  (מקלחות/אמבטיות · אביזרים נלווים · ברזי כיור/מטבח/קיר/אמבטיה/מקלחת/דלי).
- **אינסטלציה** = צנרת only → **💧 צינורות מים** (PPR · נחושת · גינון · ברזי
  מעבר/ניל · מחלקים · רב-שכבתי) · **🟤 צינורות שפכים** (ניקוז · SmartLock ·
  מסעפי-אסלה).
The mapping is per-leaf so a genuinely-mixed top-node splits (ברז-כיור→גמר but
ברז-מעבר→מים); pure families (PPR, SmartLock, אסלות) collapse to one row that
drills into their sub-tree. `isCatalogDept(name)` routes these depts to the
grouped view (`_DeptCatGroups`); a row tap drills via `catalogTreePathProvider`.
Supersedes the v5.69 department-level WaterSystem filter. Guarded by
`category_division_test` (5). Tools excluded (they live in the tool departments).
**v5.97:** the dual-system fittings (אטמים ופקקים · חבקי תליה/צינור · עוגנים
ובנדים · סטי הידוק) now appear under **both** מים and שפכים headings (they fit
either pipe), per Benzi. Also (Benzi #2): `_CatGroupRow` no longer shows a
trailing chevron — the product-count badge is now the row's END element.

## Chip picker (בורר) now works for Huliot (v5.95)
The §21 faceted chip picker — tap a breadcrumb chip on a card to swap that
attribute (angle / size / shape) for a sibling product — was dead for every
Huliot product. Two bugs, both lesson T4 (`kPolyrollCatalog` instead of the
unified `kCatalogProducts`):
1. `lipskey_products_screen.dart` `_cycleHierarchy` drew the sibling pool from
   `kPolyrollCatalog` → Huliot products absent → empty siblings.
2. `findHierarchySiblings` (`chip_hierarchy.dart`) gated strictly on the
   Polyroll brand (`brandOf(product) != polyrollBrand → []`) → returned `[]`
   before scanning, for any Huliot product.
Fix: `findHierarchySiblings` now gates on the product's **own** brand
(same-brand siblings — a Huliot elbow finds Huliot elbows), and the call-site
draws from `kCatalogProducts`. The `polyrollBrand` parameter is gone.
Verified: tap a חוליות `ברך 45°` shape chip → picker lists `45°`+`90°`; size
chip → `32/40/50/63`; Polyroll picker unaffected. Guarded by
`huliot_picker_test` (4) + mutation_verify on the brand gate.

## Bottom nav — 4-tab restructure (v5.93 — Benzi #3)
The bottom nav is now **🏠 בית · ▦ מחלקות · 🔔 עדכונים · 🛒 חנות** (`home_shell`
`_BottomNav` + `IndexedStack`):
- **בית** (index 0) = the standalone `CatalogScreen` landing on the **"הכל"** window
  (the catalog default section moved 'בית'→'הכל'; the departments drill still sets
  'בית' explicitly). Tapping בית resets to הכל, unscoped.
- **מחלקות** (1) = `DepartmentsScreen` grid (unchanged).
- **עדכונים** (2) = `UpdatesScreen` (`updates_screen.dart`) — the former שיחות +
  התראות tabs MERGED under one tab with a top segmented toggle
  [🔔 התראות · 💬 שיחות], both kept alive via an inner `IndexedStack`
  (`updatesSubTabProvider`). The app-bar overflow menu follows the active segment.
- **חנות** (3) = `StoreScreen` (unchanged); cart stays the floating FAB.
Guards updated: `widget_test` (boot lands on בית + 4-tab nav; manager-dashboard
'אביזרים נלווים' now also appears in the catalog). 1084/1084 green.
## 👔 Manager unify — control-center + live orders engine onto the app (Path B)
Cherry-picked the COMPLETE manager rebuild + the shared orders engine onto the LIVE
app (which already carries contractor + worker + store + courier). New code, NO
protocol churn — the live app keeps ITS protocol/.githooks. Added:
`lib/state/orders_engine.dart` (`ordersEngineProvider` — the shared SYS_ORDERS port,
seeded from `kManagerOrderSeed`, persists `bs.orders.v1`), `lib/logic/manager_dashboard.dart`
(pure derivations — analytics/customers folds), `lib/screens/manager_dashboard_screen.dart`
(the 4-tab מרכז השליטה role-app: לוח בקרה / הזמנות / לקוחות / ניהול), `lib/state/manager_dashboard_state.dart`.
`role_picker_sheet.dart`: manager → `ManagerDashboardScreen.route()` ALONGSIDE worker →
`WorkerAppScreen.route()` (both role-app entries kept). `bs_dial_widget.dart`/`dial_state.dart`
gained the manager-leaf hunks. Tests: `orders_engine_test`, `manager_dashboard_test`,
`manager_dashboard_screen_test`, `bs_dial_manager*_test`. The live app's contractor +
worker + store + courier and the manager now coexist on one shared engine — the base for
cross-persona wiring. (Version label stays `v6.08` — live's catalog progression is the
release source of truth; this unify is additive code.)


## Opening flow — first-run register/profession + "מי אתה?" picker + name chip (v5.92)
First-run gate (`OnboardingGate`, `welcomeSeenProvider` seeded in `main()`): Welcome
(register / existing-login→home / demo) → Profession → onboarding slides → home.
`welcome_screen` (hero-sheet register; name+contact → `userProfileProvider`),
`profession_screen` (trade picker), `onboarding_screen` (3 slides + a replayable tour via
the 💡 app-bar button, `showIntroTour`), `role_picker_sheet` ("מי אתה?" persona picker
opened from the logo; non-contractor → its existing BS-dial sections). Home app-bar shows
the registered user's first name as a chip beside the logo. Guarded by `onboarding_test` (7).

## Version chrome decoupled — `version.g.dart` (v5.92 — P0 לקח #72)
תווית-הגרסה הופרדה מ-`home_shell.dart` לקובץ נפרד `lib/version.g.dart` (gitignored,
נוצר אוטומטית מ-git+STATUS ע"י `scripts/gen_version.sh`). אף סוכן לא עורך גרסה ידנית →
אפס conflict-magnet + אפס test-trap. ה-changelog החופשי הוסר מה-UI (חי כאן ב-markdown).
התווית מציגה `kVersionLabel` בלבד (אפור-secondary, לא ירוק). שערים 11/12 אוכפים
`version.g.dart`↔STATUS; שער 59 (forced-bump) בוטל. מימוש P0 מתוך
`PROPOSAL_version_friction.md` (קונצנזוס 6 סוכנים × 2 סבבים).

## "Where to ship" — one-time popup on first product (v5.94 — Benzi #4)
A **one-time, non-binding** "לאן לשלוח?" popup (`openShipToSheet`: TextField +
**דלג / שמירה**) pops up automatically the **first time a product is added to the
cart** (the selection stage) — NOT at checkout (per Benzi's correction). Trigger:
`home_shell` listens to `smartCartProvider`; on the 0→1 transition, if not yet
prompted, it opens the popup once. Guard `shipToPromptedProvider` mirrors
`welcomeSeenProvider` — default **true** (so widget tests never trip it), seeded
in `main()` from `loadShipToPrompted()` (absent key → false → fresh install
prompts); `saveShipToPrompted()` persists after it shows. Address →
`shipToProvider` (optional; checkout never requires it). Guarded by
`shipto_prompt_test` (2). (Earlier v5.91 placed it in the checkout sheet — moved.)

## Catalog search — SKU no longer pollutes numeric size queries (v5.89)
`catalogProductMatchesQuery` folded the SKU into the haystack as a loose
substring, so a short numeric size query matched every product whose SKU merely
*contained* those digits (`"200"` inside SKU `120011`): 55% of the results for
`"20"` (243/443) were unrelated SKU coincidences (mitigated only by relevance
ranking pushing them last). Fix: SKU is matched separately and ONLY for queries
≥5 chars (catalogue SKUs are 5–10 digits); short size queries match
name/category/colour. Full/partial-SKU search preserved. Guarded by
`search_sku_pollution_test` (3).

## Finder/card — tokenizer agreement on leading fractions (v5.88)
`isSizeToken` (card word-classifier) required a leading DIGIT, so it rejected a
bare leading fraction like `½"` while `parseSizeTokens` (finder) accepts it.
On the Lipskey `_NameWords` path that would render `½"` as a plain link, not a
size chip, and `productListDedupeKey` wouldn't strip it (variants wouldn't
collapse). No catalog product triggers it today (the lone `½"` item is a חוליות
hierarchy card, rendered correctly via `parseChips`), but the asymmetry was a
latent bug. Fix: `isSizeToken` now accepts a leading fraction glyph. Full suite
1061/1061 green (no regression). Guarded by `finder_tokenizer_agreement_test` (3).

## Finder filter — mm tokens no longer collapse (reachability) (v5.87)
`dedupLengthByMm` collapses equivalent LENGTH chips (cm≡meters, P11) but also
included the `mm` family — wrong, since an mm token is usually a DIAMETER
(`250 מ"מ` head) or cross-dim OD (`16×20`). It merged `250 מ"מ`→`25 ס"מ` and
`16×20`→`16×16`; the per-product filter matches by exact label, so every
product with the collapsed-away label became UNREACHABLE by the surviving chip
(328 catalog-wide). Fix: drop `mm` from the length-dedup rank (cm/meters still
collapse). dedup-missed 328→0. Guarded by `finder_dedup_reachability_test` (4).

## Finder filter — substring false-match fixed (v5.86)
`_productHasChip` (`finder_screen.dart`) ended with an unconditional
`nameHe.contains(chipLabel)` fallback (meant for curated-facet plain words like
אמריקאי). It also fired for digit-bearing size/angle chips, so `5"` matched
`1.25"`, `50 מ"מ` matched `250 מ"מ`, `2"` matched `1/2"` — a size filter
surfaced larger sizes it isn't. Fix: gate the fallback to digit-free labels
(structural tokens already cover sizes/angles). Global false-positive upper
bound 350→0. Guarded by `finder_filter_falsematch_test` (2).

## Card — dims-derived DN chip (v5.84 + v5.85)
Fittings whose bore lives only in `dims` now show a gray DN chip on the card
(via `tokensFromDims`), so the finder's DN size axis is visible and collapsed
size variants are distinguishable. **v5.84:** Lipskey cards (`_NameWords`) — adds
each dims DN not already a name size-chip (mirrors the finder, incl. showing both
`4"` + `DN110`). **v5.85:** חוליות hierarchy cards (`_HierarchyChips`) — covers/
risers/grates carry their bore only in dims, so a gray "מידה" DN pill is appended
when the breadcrumb has no size of its own (gated so a PPR valve, whose name
states the OD, never gets a second possibly-inconsistent DN). Cards with truly no
visible size dropped 18→1 (the lone remainder: `סט פקקים…½"` — `parseChips`
doesn't surface a leading-fraction `½"`, a tokenizer asymmetry vs `parseSizeTokens`).
Guarded by `card_dims_dn_chip_test` (4).

## Departments — flat "all products" per branch (v5.87 — Benzi #5)
Each open department has a `_DeptScopeBar` toggle **"כל המוצרים" ↔ "קטלוג"**
(`deptFlatProductsProvider`): "כל המוצרים" shows the whole branch as ONE flat
`LipskeyProductsList` ("ברצף, ללא קשר לקטלוג"), bypassing the finder/tree.
`departmentProducts(system/toolCats)` is the scope — a water department = all its
in-system products (`filterBySystem`), a tool department = all its `toolCats`
products. Resets on department open + "כל המחלקות" clear. Guarded by
`departments_test`.

## Departments — tool departments live from real data (v5.83 — R8)
A full catalog audit (all 99 leaf categories + product-name search) confirmed the
catalog is 100% plumbing: the 5 trade placeholders (חשמל · חומרי בניין · צבע ·
גבס · אספקה טכנית) have **zero** real products → stay "בקרוב" (R8 — no data, no
invention; keyword "hits" like "לבן"/"ברגים"/"מדיח כלים" are false positives).
The only genuine tools DO exist, gathered into two live departments via
`toolCats` (leaf `categoryHe`) → `_toolDeptPath` builds a synthetic drill node
(no water-system scope): **כלי עבודה ידני** → `כלי עבודה` (2 מפתחות) + `חותך
צינורות` (2 חותכים) · **כלי עבודה חשמלי** → `כלי ריתוך PPR` (35 מכונות/מקדחים/
מברגות). Borderline cats stayed OUT (מכשירי לחץ/מנגנונים/סטי-הידוק = fittings, not
tools). Guarded by `departments_test` (toolCats ⇒ live + every category >0;
placeholders non-live).

_known-failing: 1 (gate 32 baseline — worker_reports_drilldown_test close-button, הגיע מ-worker-board-v3 של צי-העובד, לא שלי; שמות ב-knowledge/known_failing.txt)_

## Search word-completion — `searchSuggestions` (v5.85 — Benzi #6)
As-you-type suggestion chips above the search results (`_SearchSuggestions` in
`_SearchPanel`) **complete the word currently being typed from the vocabulary of
catalog PRODUCT-name words** (Benzi #6 — "השלמת מילים לפי מוצרים"): the last
whitespace-token is the fragment, suggestions are distinct product words it's a
prefix of, ranked most-frequent → א-ת, capped at 6; the already-typed words are
kept (e.g. `מח` → מחסום · מחבר · מחזיק; `ברז כד` → ברז כדורי). Respects the active
`catalogSystemFilterProvider` (vocabulary drawn from in-system products). Tapping
a chip fills `searchQueryProvider` → live results re-run. Pure, tested by
`search_suggestions_test` (5). _(Earlier v5.80 suggested categories; switched to
product-word completion per Benzi's spec.)_

## Water-system division — option 2, through the finder (v5.69 — Benzi #1)
Home = departments grid (`departments_screen.dart`, `homeDepartmentProvider`).
A live tile sets `catalogSystemFilterProvider` + opens the catalog's **finder
(בית)** scoped to that `WaterSystem` (אינסטלציה→drainage/שפכים · ברזים
וסניטריים→supply/מים נקיים), with a `_DeptScopeBar` showing the active scope +
a "כל המחלקות" clear. Division logic lives in `logic/system_division.dart`
(`productDivisionSystems`: VerifiedSpec → PPR=supply → else drainage;
`filterBySystem`; `nodeHasSystem`: fixtures both-sides else dominant) — shared by
catalog + finder so neither back-imports the other. **User chose option 2**
(finder, not the forced tree). **Filtered (Phase 1):** finder home (empty groups
hidden) + tree-drill + search panel. **Filtered (Phase 2, v5.70):** קטגוריות +
הכל overview (categories + favorites blocks) + מועדפים — categories via
`_catsForSystem` (tree-node dominant, reuses `nodeHasSystem`), products via
`filterBySystem`. **Category rows show live data (v5.81):** `_categorySummary`
gives each קטגוריה row its real in-system product count (badge) + a description
built from its in-system sub-categories — replacing the old static `_kMeta`
marketing copy that was identical across departments. **Filtered (Phase 2b, v5.71):** עץ חכם — `filterSmartBySystem`
maps each SmartProduct's brand SKUs back to the catalog (`smartProductSystems`);
unresolvable products stay visible in both (R8 — don't hide on a guess). **Phase
3 (v5.71):** the redundant `sysOpt` system selector was removed from the ⚙️
filter sheet — system now comes only from departments + the scope bar (one source
of truth). **Every browse section is now system-scoped.** 1023/1023 green.

## Catalog lens selector (v5.45 — data layer)
A list-level view-axis control (OUTSIDE the product card) re-organises a
product list by `CatalogLens`: 📂 category / 🎚 variant / 🌳 smart-tree.
`lib/data/catalog_lens.dart`: `availableLensesForSet` (which lenses are
meaningful — smart-tree hidden below 25% mapped) + `groupByLens` (titled
buckets per axis). `lib/state/catalog_lens_state.dart`: `catalogLensProvider`
(transient) + `resolveActiveLens` (never strands on an unavailable lens).
Guards: `catalog_lens_test` (18). UI selector chips = next step.

## Tabs & screens — all light-mode, readable
- **קטלוג** — opens on **בית (finder home)** as the default landing: layman
  group→sub→narrow with relevance-ranked forgiving search. Each group row shows a
  plain-Hebrew description + product-count badge (same idiom as the category rows).
  Other sections: overview blocks (categories / recent / compat / favorites /
  smart-tree), in-tab drill, grid↔list product view, smart-tree, search panel.
  **Size axis** uses `_size_norm.dart`: tokens are family-tagged (DN / inch /
  mm / cm / metre / angle) and sorted numerically inside each family — the
  chip row reads `200·250·25·30` (mm block then cm block), never
  `200·25·250·30` interleaved. Angles are their own axis (`'זווית'`); the
  filter matches by structural token, not `String.contains` (so "25 שנים
  אחריות" never trips a `25` chip). **Inch chips share one canonical glyph**
  across the finder AND the product-card chips via `prettyInch(...)` — a card
  saying `1¼"` corresponds 1:1 to the `1¼"` filter chip, not `1.25"`. Rare
  fractions canvaskit can't draw (`⅛/⅜/⅝/⅞`) fold to ASCII (`3/8"`).
  **Angles get a SECOND chip row** (`'זווית'`) when both sizes and angles
  split the pool — co-filter for elbows. See `knowledge/SIZE_FILTER_PROTOCOL.md`.
- **שיחות** — thread list, conversation, archive screen, new-chat, mute-all.
- **התראות** — grouped list with per-type + importance filtering, snooze.
- **חנות** — sections all/cart/orders/services, full cart + checkout.
- **הגדרות** (×4: catalog/chat/notif/store) — light, count-badge headers.
- product sheet / detail / brand / suppliers / install-studio — light-mode.

## Wiring (≈26 settings/buttons wired to real effects)
See `../WIRING.md` for the full table. Highlights:
- **Catalog**: searchHistory, quickFilterBar, imageSize, compactMode,
  reducedMotion, highContrast, textSize, viewMode, gridColumns (+ clear/reset).
- **Chat**: botEnabled, typingIndicator, readReceipts, chatVibration,
  greetingEnabled, lastSeenPrivacy.
- **Notifications**: per-type filter (orders/shipments/deals/price-drops),
  importanceFilter, snooze.
- **Store**: defaultPayment, selfPickupDefault, vatInclusive, minOrderAmount,
  confirmLargeOrder/threshold, cart stepper, saveCartToProject.
- **Chats screen**: search/filter, open, swipe-archive (persistent), new-chat,
  archive screen, mute-all (persistent), settings, send.

## Persistent state
SharedPreferences: catalog/chat/notif/store settings, chat archived ids,
chat muted ids. Cart is in-memory (`smart_cart`).

## Blocked (⛔ — not locally wireable)
Prices/VAT-display/currency/unit-price/comparison · supplier rating/distance ·
AI recommendations · system notifications (push/email/sms/quiet-hours/summaries/
sound/lock-screen) · media/backup/telephony (video/call/camera/attach) ·
addresses/invoices/warranty/biometric. All need data, a server, or device APIs.

## Install Studio (v3.79 — BOM quality upgrade)
- **Entry = "תכנון חיבור"** — the catalog section chip (renamed from "תאימות" for
  a non-technical name); screen header + empty state speak plain Hebrew, and
  safety-checklist acronyms carry a plain gloss (e.g. "ברז ערבוב נגד כוויה (TMTV)").
- **Light theme (v4.69)** — the studio + audit screen were re-skinned from the
  dark "blueprint" look to the app's light + orange-brand language (white cards,
  `#1A1A1A` ink, brand-orange primary action, green "all-good"), so they no longer
  break the otherwise-light app. Role colors kept (blue supply / amber drain /
  violet fixture), tuned for contrast on light.
- **Consistent navigation (v4.70)** — studio bottom-sheets (picker / BOM /
  projects) now carry the app's standard close-X (`_SheetClose`, like the product
  sheet); the audit screen uses a custom RTL header instead of a Material AppBar;
  and the catalog's list-management dialogs + popup menu were flipped from dark to
  light. No consumer-facing dark surfaces remain.
- **Progressive dock** — 3-state UX: empty / 1-item / 2+ items (D-013)
- **Zone-aware BOM** — "גזע" + "ענף א/ב/ג…" section headers with item counts
- **TMTV auto-per-branch** — one thermostatic mixing valve per hot branch, qty = branch count
- **Auto-compliance** — PRV + BladderTank + isolation ball valve auto-inserted for hot lines
- **Severity checklist** — 🔴 critical / 🟡 warning / 🔵 info; "N קריטי פתוח" badge in BomSheet
- **Gap advice** — `gapAdviceHe()`: a supply↔drainage gap says "connect via a
  fixture" (no adapter exists); a same-system gap names the two unmet ends.
- **Heat-rating banner** — flags any plan item whose material can't survive the
  line temp (e.g. an HDPE anchor on a 60°C line); the engine already routes
  around unsuitable materials, this catches the user-picked anchor.
- **Joint-method BOM** — `lineStructureText()`: the "מבנה הקו" block in the
  installer/WhatsApp export states the joint per item (תבריג ½″ / אום הידוק DN32)
  via the shared `chainEdgeLabelHe`.
- **Safety placed in its physical spot (v4.76)** — auto-compliance no longer
  piles items at the list end: TMTV is inserted immediately at the manifold/
  shower it protects, and the hot-source group (shutoff · expansion vessel ·
  PRV) clusters at the inlet. Dielectric stays at the metal seam. So the chain
  reads correctly one-after-another with each in-line item where it belongs.
- **Drainage ≠ supply (v4.87)** — `lineIsSupply()` gates supply compliance: a
  gravity drainage line (traps + drain pipe) never gets a supply isolation ball
  valve / PRV (a supply valve can't connect to a drain trap). Fixes a chain that
  read מחסום → ברז כדורי → מצמד (impossible).
- **Chain materialization (v4.87)** — `materializeChain` / `buildInstallation`
  insert the component that physically spans each compression joint, so the BOM
  is complete and 100% direct: fitting↔fitting → the bridging PIPE (real
  drainage SKU or a synthetic "cut-to-length" supply pipe); pipe↔pipe → the
  COUPLING that joins them; pipe↔fitting → already direct. Audited: 230 physical
  paths → 100% direct links; 9340 carousel hits → 0 false mates; synthetic
  PIPE-* specs never leak into the product card (`compatibleProductsFor` filters
  on `kLipskeyCatalog`). Guards: `materialize_test`, `drainage_no_supply_test`.
- **ΔP bottleneck fix (v4.76)** — `estimatePressureDrop` excludes off-line side
  branches (¼″ Legionella sampling tap, air vent, expansion tank) from the
  bore/K calc; they were wrongly read as the in-line bottleneck (bogus ~4.8 bar
  on a recirc line). Guarded by `pressure_drop_offline_test`.
- **Temperature pill** — human labels (קר / חם / חם מאוד) with color coding;
  engine regression has a temperature-routing module (hot line excludes
  low-temp materials).

## Compatibility & install engine (v4.55–v4.59 — correct-by-construction)
- **Verified-spec graph** — `lipskey_verified_connections.dart`: 808/935 SKUs
  carry a `VerifiedSpec` (ends + material + system). A real joint is either a
  `directMatesWith` (thread/press/drain) or a `pipeSharedWith` compression
  socket — the latter counts only when EXACTLY ONE side is a pipe and the
  materials are family-compatible. So a coupling never "connects" to a coupling.
- **Connector coverage = 100% (gated)** — the headline 86% is diluted by
  accessories. `needsConnectionSpec()` excludes `kNonConnectorCategories`
  (seats/clamps/brackets/tools/mechanisms…) + `kSpecExemptSkus` (gaskets,
  bolt-sets, spray-guns); over real flow-connectors coverage is 808/808.
  `compat_coverage_test` turns red if a NEW connector ships without a spec.
- **"Why it matches" labels** — `connectionExplainHe()` shows the exact joint
  per carousel item (תבריג ½″ / אום הידוק DN32 / Press PEX 16), so matches are
  verifiable by eye. Invariant: every compat hit has a non-empty label.
- **One joint vocabulary** — `connectionJoint()` + `jointLabelHe()` are the
  single source of truth; the carousel and the install-studio `ChainDiagram`
  now read the SAME wording for the same joint (`chainEdgeLabelHe` adds only the
  "צינור DN…" implicit-pipe bridge between two fittings). Guarded by
  `connection_joint_test`.
- **Bore-aware routing** — `findShortestPath` / `findAlternativePaths` (Yen
  K-shortest) in `install_engine.dart`; `_edgeCost` weights family transitions,
  rewards direct mates and penalises narrow bores → BFS builds wide chains.
- **Full auto-compliance** — `_autoAddCompliance` inserts every safety item at
  its canonical chain position (shutoff, Bladder, PRV, TMTV, dielectric union,
  PEX expansion, recirc valves, pump strainer/flex). Goal: 0 critical open by
  construction, across all 14 checks (critical + warning + info).
- **Pressure-drop physics** — `pressure_drop.dart`: Darcy–Weisbach with
  Reynolds-aware friction, static head, per-fitting K-values, ת"י 1205 drainage
  slope. Returns ΔP, min bore, bottleneck + flow-fix suggestions.
- **Install kit** — `install_kit.dart` / `installKitFor` derives wrenches /
  crimpers / sealants from a product's actual ends.
- **Saved projects** — `state/saved_projects.dart` (SharedPreferences) +
  Install-Studio save/load/rename; **audit screen** runs 20 random scenarios live.

## Product card — info strips (v4.55)
The internal product sheet (`lipskey_product_sheet.dart`) renders the cloud chip
system PLUS seven inline-expand strips that pull data INTO the card (no nav):
מאתר · תאימות · ערכת התקנה · וריאנטים · תקינות · מפרט הנדסי · מחיר. Helpers in
`data/related_info.dart`.

## Catalog chip system (v4.48–v4.50)
Product name words are parsed into colored chips in the product list and sheet:
- **Type chip** (purple) — from `kLipskeyTypes` (e.g. ברז, זווית, מסעף, פיית, כפה)
- **Model chip** (blue-grey) — from `kLipskeyModels` (e.g. קיסר, NTM, HDPE, PP-MD-ML)
- **Color chip** (pink) — from `kLipskeyColors`
- **Subtype chip** (teal) — from `kLipskeySubtypes` (e.g. כפול, פ.פ, ח.פ, פרח, ראש, נשלף)
- **Size chip** (amber) — numeric/DN tokens
- **Green linkable words** — remaining words; tap searches by that word

Variant pickers (type/model/color/subtype) expand inline on chip tap in both
the product list card and the product detail sheet.

## Tests
40+ test files across `test/`. Key suites:
- **chip_structure** — chip type assignment + sibling pickers (31 checks)
- **dedup** — variant dedup + attrWordSet (27 checks)
- **product_journey** — 8 specific products + all 935 sheets render (49 checks)
- **widget** — shell boots + section previews
- **install_builder / manifold / loop / zone_tmtv / auto_compliance** — BOM engine (50 checks)
- **compat_50_samples / dn_pipe_gaps / find_all_four** — compatibility correctness (0 false mates)
- **pressure_drop(_advanced) / alt_paths / long_chain** — routing + ΔP physics
- **full_compliance_audit / ten_scenarios_audit** — 0 critical open across scenarios
- **install_kit / product_sheet_strips / compat_coverage** — kit derivation + card strips + coverage
- **catalog_health / catalog_regression / robustness** — catalog data integrity
- **wiring / knowledge_protocol** — search synonyms + finder grouping + protocol "teeth"
- **cart_bulk_order / cart_stress** — real cart: 20-product order + 50 hard
  scenarios + mutation edge cases (units · subtotal · VAT · delivery · total)
- **finder_size_filter** — size-token normalization: family tags, numeric sort,
  no `String.contains` false-positives, angle is its own axis (15 checks)

**In-app full-regression button** (`🔬 מרכז בדיקות רגרסיה`, the
`▶ הרץ בדיקת רגרסיה מלאה` action): the harness in `lib/test_harness/` mirrors
these guarantees so any device can self-test. Modules: dsync · tabs · buttons ·
products · behavior · dupes · sections · settings · catalog · finder · **מנוע**
(`tests/engine.dart` — compat validity, no fitting↔fitting, chain build,
auto-compliance, ΔP, install-kit) · **סל** (`tests/cart.dart` — unit count
not line count, line+accessory totals, VAT/delivery/total, JSON persistence
round-trip). Filterable by the `מנוע` / `סל` pills.

`flutter analyze` clean; `flutter test` green (pre-existing failures in
`category_scan_test` and `wiring_test` are catalog-data issues, not code bugs).

## Known placeholders (🚧)
Chats conversation header (video/call/more) and input bar
(camera/attach/emoji) still show "בבנייה" toasts.
