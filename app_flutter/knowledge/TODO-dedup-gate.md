# TODO (חובה לטיפול) — שער אנטי-כפילות בפרוטוקול

> נרשם לבקשת בעל-המוצר (2026-06-07): *"תרשום לך להמשך שזה חייב טיפול."*
> סטטוס: **OPEN — חובה.** לא לסגור עד שמיושם שער או צ'ק-ליסט אכיף.

## הפער
לפרוטוקול (~100 שערים ב-`.githooks/pre-commit` + `central-verify.sh` + `protocol-enforce.yml`)
יש כיסוי מלא ל**נכונות ועקביות** (analyze 0 · tests · build · verbatim · version-sync ·
no-dark-surfaces · hooks intact · filenames), אבל **אפס** כיסוי ל**כפילות פיצ'רים/מסכים**.

הבדיקות היחידות עם "dedup" (`dedup_test`, `no_duplicate_specs_test`) הן על **נתוני קטלוג**
(מק"טים/specs כפולים) — לא על מסכים, כפתורים, או נתיבי-כניסה.

תוצאה: ב-Phase-1, סוכנים מקבילים בנו מסכים נאמנה מהפרוטוטייפ (`index.html`) בלי לבדוק
את אפליקציית ה-Flutter הקיימת → נוצרו כפילויות שעברו את כל 100 השערים (כל אחת קוד תקין).

## דוגמאות שהתגלו (אודיט 2026-06-07)
- 📐 סריקת תוכנית — `_ScanPlanSheet` (קטלוג, home_shell.dart) ↔ `ScanMenuScreen` + AI hub `_PlanScan`.
- 💡 חלופות זולות — `_CheaperAlternativesSheet` (home_shell.dart) ↔ AI hub `_Alternatives`.
- 📷 ברקוד · 🎤 קול — search_dial + catalog ↔ AI hub (נתיב שלישי).
- 🛒 רכש (8/8) — כבר קיים בטאב החנות.
- ⚙️ הגדרות (~50%) — כבר במסכי ההגדרות הייעודיים.

## כיווני פתרון (להחליט בעת המימוש)
1. **שער אוטומטי:** צ'ק שמסמן כש-leaf בתפריט מנתב ל-destination שכבר נגיש מכפתור/sheet קיים
   (מיפוי leaf-id → opener, מול אינדקס של openers קיימים ב-home_shell/search_dial/catalog).
2. **צ'ק-ליסט ידני (Inspector):** שלב חובה "dedup review" לפני חיווט — האם הפיצ'ר כבר קיים?
3. **מקור-אמת יחיד:** מימוש אחד לכל פיצ'ר + מספר נתיבי-כניסה (לא מספר מימושים).

## הקשר
התגלה בזמן חיווט Phase-1 (commit `ac3073d`). האחריות: שלב האורקסטרציה היה צריך
לבדוק חפיפה לפני חיווט. השער ימנע הישנות.
