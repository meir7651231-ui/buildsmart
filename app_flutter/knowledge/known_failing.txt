# known_failing — שמות הבדיקות הידועות-ככושלות (baseline ל-gate 32)
#
# כלל: מספר השורות כאן (לא הערות/ריקות) חייב להיות שווה ל-known-failing ב-STATUS.md.
# שער 32 חוסם אם known-failing > 0 אך הרשימה לא תואמת — מונע baseline-phantom.
# פורמט: שורה אחת לכל בדיקה — "test_file.dart :: שם הבדיקה".
#
# מאומת 2026-06-01: הסוויטה המלאה = 930 ✅ / 0 ✗ → הרשימה ריקה (known-failing: 0).
# אם תוסיף baseline — פרט כאן את השמות המדויקים (לא מספר בלבד), אחרת agents
# נתקעים בלי לדעת אילו כשלים ידועים ואילו חדשים.

# 2026-06-14→fixed 2026-06-23 (v6.58): worker_reports_drilldown_test היה כושל
# (close-button לא נמצא ע"י find.bySemanticsLabel('סגור') בלי semantics-tree פעיל).
# תוקן: הבדיקה משתמשת ב-find.byIcon(Icons.close).last (חסין, בודק אותה התנהגות).
# הוסר מה-baseline → known-failing ירד 2→1.

# 2026-06-17→fixed 2026-06-23 (v6.71): kb_golden_test היה כושל ב-setUpAll (path קשיח
# של Windows `C:/flutter/.../materialicons-regular.otf` שזרק על Linux/CI).
# תוקן: resolver חוצה-פלטפורמה (FLUTTER_ROOT / נגזר מ-resolvedExecutable) + הגולדנים
# מדלגים by-default (host-specific — font hinting/AA שונה בין OS); רצים בכוונה עם
# RUN_GOLDENS=1 בהוסט-הייחוס. הוסר מה-baseline → known-failing 1→0 (הרשימה ריקה כעת).

# 2026-06-30→fixed 2026-07-27: 2 כשלים רעועים-תחת-עומס (state_loadrace_guards ·
# favorite_tile_opens_sheet) — עברו בבידוד תמיד, נפלו רק בסוויטה-המלאה (concurrency=4).
# יוצבו: (1) המתנה דטרמיניסטית ל-notifier.ready (hook @visibleForTesting על 3 ה-
# notifiers: product_favorites/card_filter/card_acc) במקום Future.delayed(60ms);
# (2) finder-לפי-שם-מוצר + pump-until-מופיע במקום star-icon.first השביר.
# הוסרו מה-baseline → known-failing 2→0 (הרשימה ריקה כעת). [ראה גם: stock_local קיבל
# פטור מתועד ב-stage2_scale — offender דטרמיניסטי שנבלע בתקציב עד לאודיט 2026-07-27.]

# ── 2026-08-02 (atom-engine slice-1 re-integration onto v7.01) ──────────────
# 11 ENVIRONMENTAL failures in THIS headless container (Flutter 3.44.8, no
# system-font metrics). ALL green in CI (system fonts). NONE caused by
# atom-engine slice-1 (it adds 0 net failures). Reversible: drop these lines +
# reset known-failing:0 once the container ships fonts / on the CI runner.
# The 8 REAL pre-existing branch failures were FIXED (not baselined) in the
# same session — see the branch-repair commit. Zero-tolerance preserved for
# real failures.
#
# 10× product_journey_test — RenderFlex overflow (font width) rendering all 935
# product sheets at large-text/narrow-phone; deterministic here, green in CI.
product_journey_test.dart :: journey · 116209 — מחבר כפול תבריג 32/32
product_journey_test.dart :: journey · 118220 — מסעף 45° DN110 עם תבריג · כולל 3 אומים ו-3 אטמים
product_journey_test.dart :: journey · 217861 — מחסום (סיפון) אמריקאי 1.25" לכיור רחצה
product_journey_test.dart :: journey · 610918 — מכסה עגול עליון קבוע לבן
product_journey_test.dart :: journey · 77001191 — מחבר טלסקופי כפול 1/2"
product_journey_test.dart :: journey · 77110166 — דיור מערכת שטיפה דולפין פלוס ניקל
product_journey_test.dart :: journey · 77405428 — זווית פנימית NTM 20×3/4"
product_journey_test.dart :: journey · 77777482 — מצוף נחושת 3/4"
product_journey_test.dart :: journey · 77Z3080A — מכסה נחושת עגול 4"
product_journey_test.dart :: journey · 9102501231 — זווית HDPE הברגה חיצונית 25×1/2"
# 1× worker_attendance_geo_test — flaky ONLY under full-suite concurrency=4;
# passes in isolation (11/11). Load-flake, not a logic regression.
worker_attendance_geo_test.dart :: P-15 send-report — after sending the button shows "הדוח נשלח ✓" and a second tap does NOT post a duplicate report
