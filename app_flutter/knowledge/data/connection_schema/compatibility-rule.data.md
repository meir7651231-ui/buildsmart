# CompatibilityRule — data decomposition

> Auto-generated by `tools/atom/decompose` (data). Read-only —
> re-run the decomposer. Module: `connection_schema` · `/home/user/buildsmart/app_flutter/lib/domain/connection_schema.dart`.

- **kind:** const-value

## עצם · object (fields)
| field | type | flags |
|---|---|---|
| `id` | `String` | required |
| `tradeId` | `String` | required |
| `aTypeId` | `String` | required |
| `bTypeId` | `String` | required |
| `sizeMatch` | `SizeMatch` | required |
| `sizeTable` | `List<List<String>>?` | nullable · collection |
| `methodLabelHe` | `String` | required |
| `onMismatch` | `RuleSeverity` | default=RuleSeverity.warning |
| `materialGroup` | `String?` | nullable |
| `incompatibleMaterialGroups` | `List<String>?` | nullable · collection |
| `hashCode` | `int` | DERIVED |

## חיבורים · connections (relations)
- **fk** `id` → `catalog`
- **fk** `tradeId` → `catalog`
- **fk** `aTypeId` → `catalog`
- **fk** `bTypeId` → `catalog`
- **embed** `sizeMatch` → `SizeMatch`
- **embed** `sizeTable` → `List<String>` (many)
- **embed** `onMismatch` → `RuleSeverity`

## התנהגות · behaviour
- **decoders:** `fromJson` · `toJson`
- **methods:** _none_

## חוזה · contract
- **required:** `id` · `tradeId` · `aTypeId` · `bTypeId` · `sizeMatch` · `methodLabelHe`
- **defaults:** `onMismatch=RuleSeverity.warning`
