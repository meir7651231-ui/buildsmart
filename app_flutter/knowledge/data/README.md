# knowledge/data — the entity layer, opened

> DECOMP-DEPTH phase D output. Auto-generated by the **read-only** data decomposer
> (`tools/atom/decompose --data <file> --out app_flutter/knowledge/data`).
> One directory per module; do not hand-edit — re-run the tool.

Where `knowledge/logic/` opens the **engines**, this opens the **data** they read:
catalogs, schemas, seeds, records. For each entity (class / enum): **object**
(fields — type · required · default · nullable · collection · **derived**) ·
**connections** (relations — FK / embed / enum) · **behaviour** (decoders +
methods, with a `TOLERANT` flag when a `fromJson` degrades bad input) · **contract**
(required fields · defaults · invariants).

**62 modules · 159 entities.** Function-only modules (no data fields) belong to
`knowledge/logic/`, not here.

## Golden-anchored entities

| entity | module | what it shows |
|---|---|---|
| `VerifiedSpec` | `lipskey_verified_connections` | the physical-connection record — `sku`(FK) · `ends`(embed `ConnectorEnd`) · `material` · `maxTempC=40` · `systemOverride`(enum); `endSystems` is DERIVED; predicates `compatibleWith`/`suitableForTemp` |
| `LipskeyCatalogProduct` | `lipskey_catalog` | the spine — 16 stored fields + **~15 DERIVED** name→data getters (hard-case #2); `sku` PK/FK; `brand`='ליפסקי' |

## The debts — see [HARD-CASES.md](HARD-CASES.md)
The decomposer surfaced (and **did not** refactor):
- **#54 the homeless price** — 3 disconnected representations, none canonical (owner decision).
- **#2 name→data** — connection geometry regexed out of `nameHe` at read time.
- **#1 string classifiers** — category tables that belong in schema.
- **#7 tolerant decoders** — malformed records decode silently (no validation seam).
- **#8 dual model** — `VerifiedSpec` (tier 2) vs `ConnectorType`/`CompatibilityRule` (tier 3).

**Guards:** read-only · zero app-code change · debt = documented owner-decisions ·
safety/price never invented. Each module dir holds `module.data.json` + one
`<entity>.data.md`.
