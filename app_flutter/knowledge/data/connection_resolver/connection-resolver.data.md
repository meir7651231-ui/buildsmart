# ConnectionResolver — data decomposition

> Auto-generated by `tools/atom/decompose` (data). Read-only —
> re-run the decomposer. Module: `connection_resolver` · `/home/user/buildsmart/app_flutter/lib/domain/connection_resolver.dart`.

- **kind:** value

## עצם · object (fields)
| field | type | flags |
|---|---|---|
| `rules` | `List<CompatibilityRule>` | required · collection |
| `connectorTypes` | `List<ConnectorType>` | collection · default=const [] |
| `systems` | `List<SystemDef>` | collection · default=const [] |
| `completionRules` | `List<CompletionRule>` | collection · default=const [] |
| `_memo` | `Map<String, ConnectResult>` | collection |
| `_systemIdByTypeId` | `Map<String, String?>` | collection |
| `_systemById` | `Map<String, SystemDef>` | collection |

## חיבורים · connections (relations)
- **embed** `rules` → `CompatibilityRule` (many)
- **enum** `connectorTypes` → `ConnectorType` (many)
- **embed** `systems` → `SystemDef` (many)
- **embed** `completionRules` → `CompletionRule` (many)
- **embed** `_memo` → `ConnectResult`
- **fk** `_systemIdByTypeId` → `catalog`
- **embed** `_systemById` → `SystemDef`

## התנהגות · behaviour
- **decoders:** _none_
- **methods:** `canConnect` · `_endPairMemoized` · `_endPair` · `_sizeOk` · `completion` · `systemCoherence`

## חוזה · contract
- **required:** `rules`
- **defaults:** `connectorTypes=const []` · `systems=const []` · `completionRules=const []`
