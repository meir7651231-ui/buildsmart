# prompt_sanitize — primitive contracts

> Auto-generated by `tools/atom/decompose` (primitives). Read-only —
> re-run the decomposer. Source: `../../../app_flutter/lib/logic/prompt_sanitize.dart`.

> The floor turned from a NAME into a CONTRACT: each row is one
> floor-primitive — signature · returns · edge cases · purity.

| # | primitive | line | signature | returns | pure? |
|---|---|---|---|---|---|
| 1 | `promptSafeText` | 19 | `(String raw, {int maxLen = 600, bool collapseWhitespace = false}) → String` | `String` | ✅ pure |

## Edge contracts (step 70 → assertions)

### `promptSafeText` — `(String raw, {int maxLen = 600, bool collapseWhitespace = false}) → String`
- **over the cap → truncated to maxLen** — _signal_ `substring(0, maxLen)`
- **whitespace RUNS collapsed to a single space (conditional)** — _signal_ `replaceAll(\s+ → " ")`
- **surrounding whitespace trimmed** — _signal_ `.trim()`

