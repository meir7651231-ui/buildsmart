# fuzzy_match — primitive contracts

> Auto-generated by `tools/atom/decompose` (primitives). Read-only —
> re-run the decomposer. Source: `../../../app_flutter/lib/logic/fuzzy_match.dart`.

> The floor turned from a NAME into a CONTRACT: each row is one
> floor-primitive — signature · returns · edge cases · purity.

| # | primitive | line | signature | returns | pure? |
|---|---|---|---|---|---|
| 1 | `damerauLevenshtein` | 16 | `(String a, String b) → int` | `int` | ✅ pure |
| 2 | `fuzzyTolerance` | 54 | `(int len) → int` | `int` | ✅ pure |
| 3 | `fuzzyMatch` | 58 | `(String query, String candidate) → bool` | `bool` | ✅ pure |
| 4 | `fuzzyNameMatch` | 68 | `(String query, String candidate) → bool` | `bool` | ✅ pure |
| 5 | `fuzzyScore` | 78 | `(String query, String candidate) → int` | `int` | ✅ pure |
| 6 | `_min3` | 87 | `(int a, int b, int c) → int` | `int` | ✅ pure |

## Edge contracts (step 70 → assertions)

### `damerauLevenshtein` — `(String a, String b) → int`
- **identical inputs → 0 (the trivial short-circuit)** — _signal_ `a==b → 0`
- **one side empty → the other length (edit distance to "")** — _signal_ `isEmpty → other.length`

### `fuzzyTolerance` — `(int len) → int`
- **integer floor division (tolerance = len ~/ 3 + 1)** — _signal_ `~/ (floor-div)`

### `fuzzyMatch` — `(String query, String candidate) → bool`
- **empty input → false / no-match (short-circuits before the work)** — _signal_ `isEmpty → false`

### `fuzzyNameMatch` — `(String query, String candidate) → bool`
- _no boundary edges detected (total function)_

### `fuzzyScore` — `(String query, String candidate) → int`
- **no match → -1 (sentinel, distinct from a real score of 0)** — _signal_ `return -1`

### `_min3` — `(int a, int b, int c) → int`
- _no boundary edges detected (total function)_

