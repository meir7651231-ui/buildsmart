# dn_scale — primitive contracts

> Auto-generated by `tools/atom/decompose` (primitives). Read-only —
> re-run the decomposer. Source: `../../../app_flutter/lib/features/catalog_config/dn_scale.dart`.

> The floor turned from a NAME into a CONTRACT: each row is one
> floor-primitive — signature · returns · edge cases · purity.

| # | primitive | line | signature | returns | pure? |
|---|---|---|---|---|---|
| 1 | `snapOdToDn` | 37 | `(num od) → int` | `int` | ✅ pure |
| 2 | `canonicalDn` | 57 | `(String token) → String` | `String` | ✅ pure |
| 3 | `dnNumber` | 70 | `(String label) → int` | `int` | ✅ pure |

## Edge contracts (step 70 → assertions)

### `snapOdToDn` — `(num od) → int`
- **negative input → absolute value (caller prepends the sign)** — _signal_ `.abs()`

### `canonicalDn` — `(String token) → String`
- **surrounding whitespace trimmed** — _signal_ `.trim()`

### `dnNumber` — `(String label) → int`
- _no boundary edges detected (total function)_

