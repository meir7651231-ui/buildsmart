# input_validators — primitive contracts

> Auto-generated by `tools/atom/decompose` (primitives). Read-only —
> re-run the decomposer. Source: `../../../app_flutter/lib/logic/input_validators.dart`.

> The floor turned from a NAME into a CONTRACT: each row is one
> floor-primitive — signature · returns · edge cases · purity.

| # | primitive | line | signature | returns | pure? |
|---|---|---|---|---|---|
| 1 | `validIsraeliMobile` | 11 | `(String input) → bool` | `bool` | ✅ pure |
| 2 | `validEmail` | 17 | `(String input) → bool` | `bool` | ✅ pure |
| 3 | `validBusinessId` | 29 | `(String input) → bool` | `bool` | ✅ pure |
| 4 | `normalizePhone` | 48 | `(String input) → String` | `String` | ✅ pure |
| 5 | `validBoardCode` | 59 | `(String input) → bool` | `bool` | ✅ pure |
| 6 | `waMeDigits` | 74 | `(String input) → String` | `String` | ✅ pure |
| 7 | `validPositiveAmount` | 91 | `(num? value) → bool` | `bool` | ✅ pure |
| 8 | `validDateRange` | 95 | `(DateTime start, DateTime end) → bool` | `bool` | ✅ pure |

## Edge contracts (step 70 → assertions)

### `validIsraeliMobile` — `(String input) → bool`
- **a regex shape gate (returns the boolean match)** — _signal_ `RegExp.hasMatch`

### `validEmail` — `(String input) → bool`
- **surrounding whitespace trimmed** — _signal_ `.trim()`
- **a regex shape gate (returns the boolean match)** — _signal_ `RegExp.hasMatch`

### `validBusinessId` — `(String input) → bool`
- **invalid check digit → false (1-2-1-2 weighted checksum, mod 10)** — _signal_ `% 10 == 0`
- **a regex shape gate (returns the boolean match)** — _signal_ `RegExp.hasMatch`

### `normalizePhone` — `(String input) → String`
- **no usable characters → "" (caller decides what empty means)** — _signal_ `isEmpty → ""`

### `validBoardCode` — `(String input) → bool`
- **a regex shape gate (returns the boolean match)** — _signal_ `RegExp.hasMatch`

### `waMeDigits` — `(String input) → String`
- **no usable characters → "" (caller decides what empty means)** — _signal_ `isEmpty → ""`

### `validPositiveAmount` — `(num? value) → bool`
- **null argument → false / degrades (never throws on null)** — _signal_ `null-guard`

### `validDateRange` — `(DateTime start, DateTime end) → bool`
- **boundary is strict (end must be strictly after start)** — _signal_ `.isAfter(`

