# text_normalize — primitive contracts

> Auto-generated by `tools/atom/decompose` (primitives). Read-only —
> re-run the decomposer. Source: `../../../app_flutter/lib/logic/text_normalize.dart`.

> The floor turned from a NAME into a CONTRACT: each row is one
> floor-primitive — signature · returns · edge cases · purity.

| # | primitive | line | signature | returns | pure? |
|---|---|---|---|---|---|
| 1 | `normSearch` | 24 | `(String t) → String` | `String` | ✅ pure |
| 2 | `normName` | 36 | `(String s) → String` | `String` | ✅ pure |

## Edge contracts (step 70 → assertions)

### `normSearch` — `(String t) → String`
- **case-folded to lowercase** — _signal_ `.toLowerCase()`
- **Hebrew final letters folded to base form (ך→כ …)** — _signal_ `final-fold table`
- **Hebrew diacritics (niqqud) / punctuation stripped** — _signal_ `RegExp char-class strip`
- **surrounding whitespace trimmed** — _signal_ `.trim()`

### `normName` — `(String s) → String`
- **ALL whitespace removed (tight dedup key)** — _signal_ `replaceAll(\s → "")`

