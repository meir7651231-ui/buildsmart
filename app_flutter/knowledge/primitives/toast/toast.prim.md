# toast — primitive contracts

> Auto-generated by `tools/atom/decompose` (primitives). Read-only —
> re-run the decomposer. Source: `../../../app_flutter/lib/widgets/toast.dart`.

> The floor turned from a NAME into a CONTRACT: each row is one
> floor-primitive — signature · returns · edge cases · purity.

| # | primitive | line | signature | returns | pure? |
|---|---|---|---|---|---|
| 1 | `showToast` | 19 | `(BuildContext context, String message, {Duration duration = BsTokens.toastDuration}) → void` | `void` | ❌ shows a SnackBar (UI side effect) |
| 2 | `showGlobalToast` | 34 | `(String message, {Duration duration = BsTokens.toastDuration}) → void` | `void` | ❌ shows a SnackBar (UI side effect) |
| 3 | `_toastBar` | 46 | `(String message, Duration duration) → SnackBar` | `SnackBar` | ✅ pure |

## Edge contracts (step 70 → assertions)

### `showToast` — `(BuildContext context, String message, {Duration duration = BsTokens.toastDuration}) → void`
- **target not mounted → silent no-op (never a throw)** — _signal_ `null → no-op`

### `showGlobalToast` — `(String message, {Duration duration = BsTokens.toastDuration}) → void`
- **target not mounted → silent no-op (never a throw)** — _signal_ `null → no-op`

### `_toastBar` — `(String message, Duration duration) → SnackBar`
- _no boundary edges detected (total function)_

