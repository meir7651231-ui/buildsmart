# money_format — primitive contracts

> Auto-generated by `tools/atom/decompose` (primitives). Read-only —
> re-run the decomposer. Source: `../../../app_flutter/lib/logic/money_format.dart`.

> The floor turned from a NAME into a CONTRACT: each row is one
> floor-primitive — signature · returns · edge cases · purity.

| # | primitive | line | signature | returns | pure? |
|---|---|---|---|---|---|
| 1 | `groupThousands` | 19 | `(int n) → String` | `String` | ✅ pure |
| 2 | `formatNis` | 31 | `(int n, {String prefix = ''}) → String` | `String` | ✅ pure |

## Edge contracts (step 70 → assertions)

### `groupThousands` — `(int n) → String`
- **negative input → absolute value (caller prepends the sign)** — _signal_ `.abs()`

### `formatNis` — `(int n, {String prefix = ''}) → String`
- **sign is placed BEFORE the symbol (never "symbol-minus")** — _signal_ `n<0 ? …`

