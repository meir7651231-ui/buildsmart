# Pillar 3 — Live Customer Intelligence · Steps 86–100 (phase 6, FINAL block)

> Decomposed from `knowledge/studio-plan/03-live-customer-intelligence.md`.
> Branch `claude/whats-happening-LyY9G` · `app_flutter/` (Flutter 3.29 · Riverpod).
> **EXACTLY 15 atomic, dependency-ordered steps.** Step 100 = platform-wide GA lock (the "100%, not 99" finish).
>
> ⚠️ קנוני = מקטע 🔧 תיקוני Red-Team R1 בסוף `03-live-customer-intelligence.md` + `RED-TEAM-R1/R2.md`. ה-appendix גובר על כל שורה סותרת כאן.
> Constraints baked into every step: instrumentation zero-regression + near-zero perf
> (fire-and-forget, batched) · privacy **default-DENY** (`privAnalytics`/`privPresence` = **false**); forward נפתח רק כש-`consentedPolicyVersion >= kCurrentPolicyVersion` (גרסה נשמרת, לא bool צף) · Hebrew/RTL/a11y ·
> 100-gate protocol (gates 31/32/33/46/94 + P3's reserved **#120** — P1=118, P4=119) · governance #84 (owner/manager-only reads; presence = לקוחות-בלבד, לא צוות).
> Each step is independently shippable and gate-passing. Format:
> `N. <action ≤14 words> · <file:seam> · <gate/test> · <zero-reg note>`

86. Ship privacy-policy text **+ consent modal** (REAL build-step, not 💡) · `lib/data/legal_texts.dart:4-13` revise "no analytics SDK" + תיקון 13 §11/§13/§14 + `kCurrentPolicyVersion`; new consent dialog quotes the text, on accept writes `privAnalytics=true` **AND** `consentedPolicyVersion=kCurrentPolicyVersion` in one txn; default-DENY `privAnalytics:false` (`app_settings.dart:105` true→false; `:236` `!=false`→`==true`) + new `consentedPolicyVersion` field · `legal_screen` + consent-flow test (pre-consent inert / post-consent forward / post-bump inert again) · forward gated `consentedPolicyVersion>=kCurrentPolicyVersion`, NOT bare bool.
87. Add `IntelEvents` registry + `IntelEvent` immutable record · new `lib/state/intel/intel_events.dart` + `lib/state/intel/intel_event.dart` (extends `TelemetryEvents` idea, `telemetry.dart:139-148`) · `test/intel/intel_events_test.dart` (unique/snake_case/≤40/no-PII-key) · pure data, no wiring yet.
88. Add `IntelLogNotifier` ring buffer (cap ~1000, newest-first) · new `lib/state/intel/intel_log.dart` (`analytics_log.dart:33-38` trim invariant) · unit test record/trim/recent · in-memory only, never persisted.
89. Add `IntelBus` one-line seam fanning local + telemetry + (inert) sink · new `lib/state/intel/intel_bus.dart` (`telemetry.dart:47`, `read` not `watch`) · bus no-op test `returnsNormally` w/o Firebase · never awaits/throws; demo = local append only.
90. Add consent-gated batched `IntelSink` (N/T/paused flush, requeue) · new `lib/state/intel/intel_sink.dart` gated `consentedPolicyVersion >= kCurrentPolicyVersion && useFirebaseBackend` (version, not bare bool — `app_settings.dart:82`, `backend.dart:12-17`); wire carries `actorKey`/`uid` only, no `displayName` · sink fake test (sync enqueue/flush/requeue) · inert no-op off-backend (`connection_status.dart:82` idiom).
91. Add anon-but-stable `actorKey` (per-install uuid) + sign-in stitch · `lib/state/intel/intel_sink.dart` + SharedPreferences (`recently_viewed.dart` idiom) · unit test stitch anon→uid continuity · off-backend key generated, never written.
92. Adopt `screen_view` via RouteObserver + shell tab listener · `lib/main.dart:210-218` (navigatorObservers) + `lib/screens/home_shell.dart:90-97` one `ref.listen(mainTabProvider)` · widget test fires on tab/route change · `read`-only, no rebuilds; ~3 edits not 270.
93. Adopt catalog interaction events (search/no-result/view/cart) · `catalog_screen.dart:1614` `search_submit` · `:2229` `search_no_result` · `:4067` `product_view` · `:6374` `add_to_cart` · widget test each emits once · single statements, no constructor/control-flow change.
94. Adopt store funnel events (cart/checkout-start/step) · `store_screen.dart:1565` `_CartView` cart_view · `:2693` `checkout_start` · `~:2800-2825` `checkout_step` (order_placed `:2916` already wired) · widget test emits · additive single lines, demo byte-identical.
95. Add deterministic funnel + stuck/abandon/dead-end detectors · new `lib/logic/intel/funnels.dart` + `lib/logic/intel/intel_config.dart` thresholds (`manager_dashboard.dart:29-36` fold) · pure-logic tests on hand-built event lists · pure Dart, no Firebase/widgets.
96. Add pure segments + retention cohorts **keyed by uid/actorKey** (NOT displayName) · new `lib/logic/intel/segments.dart` keyed `uid`/`actorKey`, fall back to name only when uid empty (today's `manager_dashboard.dart:276/278/285` `byBuyer[o.who]` MERGES two same-name customers + double-counts — that join must move to uid) · cohort/roll-up unit tests incl. two same-name distinct-uid customers stay separate · pure fold, deterministic.
97. Add session tracker + heartbeat presence (connection_status clone) · new `lib/state/intel/session_tracker.dart` + `lib/state/intel/presence.dart`, root observer `main.dart:210-218`, liveness 2.5× rule · lifecycle/liveness tests w/ fake clock+source · inert ctor off-backend (`connection_status.dart:82`).
98. Add manager intel read providers + 5th tab `_IntelTab` (Hebrew/RTL) · new `lib/state/intel/intel_read.dart` + `lib/screens/intel/intel_tab.dart`; `manager_dashboard_screen.dart:65` 4→5, `:216-231`, `:3452-3463` (📡 מודיעין לקוחות) reusing `_MetricTile`/`_CreditBar`/`_StagePill` · widget test RTL + `Semantics` + demo data renders · existing 4 tabs untouched, demo folds local buffer.
99. Add per-customer journey timeline + privacy **Gate #120** + erasure-sweep + WIRING/docs · `manager_dashboard_screen.dart:2017-2018` `_CustomerDetailSheet` "מסע הלקוח" section (join by **uid/actorKey**, name resolved owner-side, live `watch`); add **Gate #120** (grep `lib/state/intel/` PII key — P3's reserved gate, P1=118/P4=119) to `GATE_REGISTRY.md`+`.githooks/pre-commit` (register 118/119/120, next-free→121); erasure sweep keys on `actorKey` AND `uid` AND `presence/{uid}` AND displayName-only rows, fed by the `actorStitch/{uid}` anon→uid map; events/presence carry `actorKey`/`uid` only — `displayName` off the wire; update `WIRING.md` + README/ARCHITECTURE/DECISIONS · realtime-wiring + **erasure-completeness integrity test** (seed anon+uid+presence+name → assert zero residue) + `knowledge_protocol_test` green (gate 94) · additive drill section.
100. Platform-wide GA lock — **staged-flip** forward ON (~12 flags), full gate + owner-approval · sequence the ~12 flags as **N sub-flips, each its own deploy+soak** (bisectable — NOT big-bang; M4 canary = config-publish %, a different axis); per sub-flip: `flutter analyze` 0 + full suite green + APK flags-ON smoke + per-module owner-approval (governance #84) + every `WIRING.md`/knowledge doc synced + reflexive studio-usage instrumented (edit-sessions/blocked-AI-ops/draft-abandon, step 99-bis) · gates 31/32/33/46/94/**120** ALL pass on device · default-DENY→ON only after step 86 policy+consent shipped; the "100%, not 99" finish.
