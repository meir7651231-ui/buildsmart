# Pillar #5 — Scale, Data, Backend & Publish-to-All — 18 atomic build-steps (global 51–68, phase 4)

> ⚠️ קנוני = מקטע 🔧 תיקוני Red-Team R1 (בסוף `05-scale-data-backend.md`) + `RED-TEAM-R1.md`/`RED-TEAM-R2.md`. ה-appendix גובר. השורות כאן כבר הופצו לפי ההכרעות הקנוניות (R1 1–9 + R2 hardenings).
> Source plan: `knowledge/studio-plan/05-scale-data-backend.md`. Branch `claude/whats-happening-LyY9G`, region `me-west1`.
> Prime directive: flags-OFF build is **byte-identical** to today; every mechanism ships behind a default-OFF flag/seam on the proven `backend.dart` stack (FirestoreCachedRepo, gen2 me-west1, `product_images` R2 pattern) — **no new stack**.
> Each step is shippable + gate-passing. Gate = `flutter analyze` 0-err + `flutter test` (incl. `knowledge_protocol_test`/`protocol_security_test`) + `flutter build web --release` unless a heavier gate is named. Governance #84: owner/manager-claim gates every config/catalog write — **publish-to-all דורש `isOwnerEmail`/dual-control + re-check live allow-flag** (R1-5), לא claim לבדו.
>
> Line format: `N. <action ≤14 words> · <file/seam> · <gate/test> · <zero-reg note>`

51. Append `kCatalogServerSearch`/`kStudioLive`/`kCatalogBaseUrl` flags + docs · `lib/data/repositories/backend.dart:189` (`bool.fromEnvironment`) · `backend_flag_test.dart` asserts all default OFF/empty · flags-OFF resolves local impls, byte-identical.
52. Document four new collections + 8 indexes in schema · `knowledge/firestore-schema.md`, `firestore.indexes.json` (`fieldOverrides:[]` kept) · `firebase deploy --only firestore:indexes` dry-validate · doc-only, no client/runtime change.
53. Add `PagedQuery<T>` cursor helper over remote seam · new `lib/data/paged_query.dart` (`RemoteCollectionSource` `firestore_cached_repo.dart:61`) · fake-source test, `startAfterDocument`/`limit(40)` never `offset` · flags-OFF returns full local list in one sync page.
54. Add `StudioConfigRepository` seam (abstract + local + firebase) · new `studio_config_repository.dart`/`_local.dart`/`_firebase.dart` (extends `FirestoreCachedRepo`) · define-less fake-source test of ON branch · local seed = bundled tree, dormant until flagged.
55. Implement Firestore `ConfigSink` write via born-seeded cache base · `studio_config_firebase.dart` (guarded `set` `firestore_cached_repo.dart:258`, LWW `:241`) · fake-source upsert/reconcile test · `guardWrite` swallows errors, optimistic cache shown.
56. Add `publishConfig` callable — CAS on `expectedBaseVersion` + dual-control/`isOwnerEmail` + live allow-flag re-check + per-owner rate-limit · new `functions/src/studio.ts`; export at `functions/src/index.ts:151` · functions test: stale `expectedBaseVersion`→`failed-precondition` (no overwrite, R1-4), single-manager-no-owner→denied (R1-5), happy-path+denial-audit (mirrors `advanceOrderStage` `orders.ts:50`, rate-limit `claude.ts:75` on `_publishRate/{uid}`) · NOT LWW — compare-and-set rejects CONFLICT if `published.version != expectedBaseVersion`; coalescing UX (stage-many → publish-once), `FirebaseFunctionsException` surfaces honestly.
57. Add `revertIllegalConfigWrite` trigger on `studioConfig/published` · `functions/src/studio.ts` (`onDocumentUpdated`, loop-guard like `orders.ts:145,158`) · functions test reverts a forged publish · callable is the only sanctioned write path; no client behaviour change.
58. Wire single published-pointer `snapshots()` listener + version diff + schema-guard · `studio_config_firebase.dart` `attach()` (guarded `:204`) · fake-source test: newer `version` swaps tree; `published.schema > maxKnownSchema` → keep last-good + "update app" (no parse, R2-7) · cache-bust = version bump; born-seed keeps cold-start non-empty.
59. Pull immutable shards + checksum + atomic swap + ingestion-failure recovery · `studio_config_firebase.dart` (cached-by-id reads, `notifyListeners` `:241`) · fake test: checksum-mismatch → keep last-good + retry-once (never swap corrupt); `fromJson`/`migrate()` throw → try/catch → fall back last-good/seed (never blank app, R2-7) · in-flight reader never torn (snapshots immutable).
60. Add `catalogProducts/{sku}` per-SKU collection + paged browse; price OUT to role-scoped sub-doc; tombstone-archive · new `catalog_firebase.dart` (`PagedQuery` + `toDoc`/`fromDoc` adds fields like `orders_firebase.dart:70`; **NO `price` in public doc** → `catalogProducts/{sku}/pricing/{audience}`, R1-6; delete = `tombstone:true`+`active:false`, R1-9) · fake-source paging test + price-not-in-public-doc assertion · `LipskeyCatalogProduct` model unchanged, doc-id=SKU idempotent.
61. Wire OFF-by-default seed importer — stage-to-pending, flip-live on FULL success only · reuse `pushCacheToRemote` (`firestore_cached_repo.dart:327`, gated `kSeedFreshBackend`) into a `catalogPending/*` draft, atomic-flip to `catalogProducts` only when complete (R2-9) · emulator seed test idempotent + mid-batch-abort leaves NO half-public catalog (resumable cursor + tombstone partials) · prod never auto-seeds (`!kSeedFreshBackend` guard).
62. Add `SearchRepository` seam; local wraps `fuzzySearchProducts` verbatim · new `search_repository.dart`/`_local.dart`/`_firebase.dart` (`SynchronousFuture` local, `fuzzy_search.dart:16`) · search-parity golden top-N **answer-equivalent מול `catalog_regression` fixtures** (R1-7 — NOT "ranking identical") · Layer-A offline path byte-identical, zero latency change.
63. Server search + `onCatalogProductWrite` indexer (tokens from `nameHe` ONLY) + token-freq map + archive cleanup · `search_firebase.dart` (rarest-token via `catalogTokenFreq` `arrayContains`→fuzzy re-rank) + new `functions/src/catalog.ts` (NFKD tokens **from `nameHe` only**, matches today's fuzzy; `tokensVersion` loop-guard; tombstone-branch clears `nameTokens`+decrements `catalogTokenFreq`, R1-7/R1-9) · golden: server top-N **answer-equivalent** to local fuzzy on fixtures · `CATALOG_SERVER_SEARCH` OFF degrades to Layer-A.
64. Wire paged catalog data-cache + `CATALOG_BASE_URL` (image-pattern twin) · new providers + `lib/data/product_images.dart` pattern copied (NOT modified) · paged virtualization test using sentinel `capped ? 1 : 0` (`catalog_screen.dart:2267`) · empty `CATALOG_BASE_URL` → bundled const, no listener on 10K.
65. Add §5 rules blocks: owner-write/world-read config+catalog, append-only analytics, price sub-doc claim-gated, `presenceSummary` owner-read · `firestore.rules` before catch-all `:517` (reuse `isManager()` `:83`; `catalogProducts/{sku}/pricing/{aud}` read `isManager()||hasRole(aud)` R1-6; `studioConfig/published` write `if false` = callable-only; `presenceSummary/current` read `isManager()` write `if false` R1-3) · `rules_test/` emulator: owner-write OK, non-owner+draft-read+competitor-price-read denied · deny-by-default + "rules-not-filters" (`:43`) preserved.
66. Add cost guardrails: `maxInstances:10` (concurrency NOT frequency), per-owner publish rate-limit, distributed counters, daily-rollup + `presenceSummary` rollup fn, hard Cloud-Billing cap · `functions/src/studio.ts`/`analytics.ts` (caps like `claude.ts:115`, rate-limit `:75` on `_publishRate/{uid}` R1-2; `presenceSummary/current` rolled server-side R1-3) · functions test: rollup writes one `analyticsDaily/{day}` + one `presenceSummary/current` · cost table incl. egress (~$144/mo high-end, R1-1); `STUDIO_LIVE` kill-switch keeps listener dark until owner opts in.
67. Wire analytics/presence write-only repos; owner reads server-rolled `presenceSummary` (NOT live listener over all online) · new `analytics_repository.dart`/`presence_repository.dart` (fire-and-forget `guardWrite`, debounced ≥30s heartbeat; owner-dashboard reads `presenceSummary/current` doc, R1-3) · fake-source write test, offline buffers in `analytics_log.dart` ring; dashboard shows "עודכן לפני Xs / לא-זמין" when `presenceSummary.updatedAt` stale (R2-12) · no write ever throws into UI; presence TTL sweep.
68. Land Phase-3 flip + full zero-regression gate suite + deploy-ordering gate · `--dart-define` swap (providers like `orders_firebase.dart:44`) + `WIRING.md`/`GATE_REGISTRY.md`; **gate `--only functions` on index-deploy SUCCESS + poll index state READY before server-search flip** (`firebase-deploy.yml:88,110` today both `continue-on-error` → indexer/rollup must not go live before its composite index, else `FAILED_PRECONDITION`, R2-10) · `backend_flag_test` byte-identical + `scripts/audit_gates.sh` + Inspector subagent (governance #84) · shipped default stays flags-OFF until owner approves cutover.

> **שלבים-משלימים קנוניים (R1/R2) — מתועדים ב-`detail/051-068.md`, נספחים על השלבים לעיל ולא משנים את מספור 51–68):**
> - **R1-8 גיבוי** · `exportStudioConfig`/`importStudioConfig` callables (`functions/src/studio.ts`, R2-pattern `r2.ts:127`): export full config+trades JSON; import = publish-as-new-version דרך CAS של step 56. **חי מחוץ ל-Firestore** (מעבר ל-ring-30).
> - **R2-11 import re-validation** · `importStudioConfig` re-runs server-side `validateSafe`+critical-lock **before** the pointer flip — backup may smuggle a hidden nav element; reject on fail.
> - **R2-8 TRADE publish fan-out (HIGH)** · today `Trade.published` writes only local `bs.trades.v1` → owner-device-only. Route trade go-live through a published-pointer + per-client listener (same shape as config step 58), so other users + open mid-sessions receive it (extends step 54/58 to a `studioConfig`-routed trade channel).
