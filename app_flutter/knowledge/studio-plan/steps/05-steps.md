# Pillar #5 — Scale, Data, Backend & Publish-to-All — 18 atomic build-steps (global 51–68, phase 4)

> Source plan: `knowledge/studio-plan/05-scale-data-backend.md`. Branch `claude/whats-happening-LyY9G`, region `me-west1`.
> Prime directive: flags-OFF build is **byte-identical** to today; every mechanism ships behind a default-OFF flag/seam on the proven `backend.dart` stack (FirestoreCachedRepo, gen2 me-west1, `product_images` R2 pattern) — **no new stack**.
> Each step is shippable + gate-passing. Gate = `flutter analyze` 0-err + `flutter test` (incl. `knowledge_protocol_test`/`protocol_security_test`) + `flutter build web --release` unless a heavier gate is named. Governance #84: owner/manager-claim gates every config/catalog write.
>
> Line format: `N. <action ≤14 words> · <file/seam> · <gate/test> · <zero-reg note>`

51. Append `kCatalogServerSearch`/`kStudioLive`/`kCatalogBaseUrl` flags + docs · `lib/data/repositories/backend.dart:189` (`bool.fromEnvironment`) · `backend_flag_test.dart` asserts all default OFF/empty · flags-OFF resolves local impls, byte-identical.
52. Document four new collections + 8 indexes in schema · `knowledge/firestore-schema.md`, `firestore.indexes.json` (`fieldOverrides:[]` kept) · `firebase deploy --only firestore:indexes` dry-validate · doc-only, no client/runtime change.
53. Add `PagedQuery<T>` cursor helper over remote seam · new `lib/data/paged_query.dart` (`RemoteCollectionSource` `firestore_cached_repo.dart:61`) · fake-source test, `startAfterDocument`/`limit(40)` never `offset` · flags-OFF returns full local list in one sync page.
54. Add `StudioConfigRepository` seam (abstract + local + firebase) · new `studio_config_repository.dart`/`_local.dart`/`_firebase.dart` (extends `FirestoreCachedRepo`) · define-less fake-source test of ON branch · local seed = bundled tree, dormant until flagged.
55. Implement Firestore `ConfigSink` write via born-seeded cache base · `studio_config_firebase.dart` (guarded `set` `firestore_cached_repo.dart:258`, LWW `:241`) · fake-source upsert/reconcile test · `guardWrite` swallows errors, optimistic cache shown.
56. Add `publishConfig` callable (manager-only, transactional, audited) · new `functions/src/studio.ts`; export at `functions/src/index.ts:151` · functions happy-path + denial-audit test (mirrors `advanceOrderStage` `orders.ts:50`) · `kServerCallables` posture, `FirebaseFunctionsException` surfaces honestly.
57. Add `revertIllegalConfigWrite` trigger on `studioConfig/published` · `functions/src/studio.ts` (`onDocumentUpdated`, loop-guard like `orders.ts:145,158`) · functions test reverts a forged publish · callable is the only sanctioned write path; no client behaviour change.
58. Wire single published-pointer `snapshots()` listener + version diff · `studio_config_firebase.dart` `attach()` (guarded `:204`) · fake-source test: newer `version` swaps in-memory tree · cache-bust = version bump; born-seed keeps cold-start non-empty.
59. Pull immutable snapshot shards by ref + validate checksum + atomic swap · `studio_config_firebase.dart` (cached-by-id reads, `notifyListeners` `:241`) · fake test: checksum-mismatch rejected, re-pull only on version change · in-flight reader never torn (snapshots immutable).
60. Add `catalogProducts/{sku}` per-SKU collection + paged browse impl · new `catalog_firebase.dart` (`PagedQuery` + `toDoc`/`fromDoc` adds fields like `orders_firebase.dart:70`) · fake-source paging test · `LipskeyCatalogProduct` model unchanged, doc-id=SKU idempotent.
61. Wire OFF-by-default seed importer for catalog + bundled tree · reuse `pushCacheToRemote` (`firestore_cached_repo.dart:327`, gated `kSeedFreshBackend`) · emulator seed test idempotent (re-runnable) · prod never auto-seeds (`!kSeedFreshBackend` guard).
62. Add `SearchRepository` seam; local wraps `fuzzySearchProducts` verbatim · new `search_repository.dart`/`_local.dart`/`_firebase.dart` (`SynchronousFuture` local, `fuzzy_search.dart:16`) · search-parity golden top-N · Layer-A offline path byte-identical, zero latency change.
63. Implement server search + `onCatalogProductWrite` token indexer · `search_firebase.dart` (rarest-token `arrayContains`→fuzzy re-rank) + new `functions/src/catalog.ts` (NFKD tokens, `tokensVersion` loop-guard) · golden: server top-N == local fuzzy · `CATALOG_SERVER_SEARCH` OFF degrades to Layer-A.
64. Wire paged catalog data-cache + `CATALOG_BASE_URL` (image-pattern twin) · new providers + `lib/data/product_images.dart` pattern copied (NOT modified) · paged virtualization test using sentinel `capped ? 1 : 0` (`catalog_screen.dart:2267`) · empty `CATALOG_BASE_URL` → bundled const, no listener on 10K.
65. Add §5 rules blocks: owner-write/world-read config+catalog, append-only analytics · `firestore.rules` before catch-all `:517` (reuse `isManager()` `:83`) · `rules_test/` emulator: owner-write OK, non-owner+draft-read denied · deny-by-default + "rules-not-filters" (`:43`) preserved.
66. Add cost guardrails: `maxInstances:10`, distributed counters, daily-rollup fn · `functions/src/studio.ts`/`analytics.ts` (caps like `claude.ts:115`, rate-limit `:75`) · functions test: rollup writes one `analyticsDaily/{day}` · `STUDIO_LIVE` kill-switch keeps listener dark until owner opts in.
67. Wire analytics/presence write-only repos with offline buffering + sweep · new `analytics_repository.dart`/`presence_repository.dart` (fire-and-forget `guardWrite`, debounced ≥30s heartbeat) · fake-source write test, offline buffers in `analytics_log.dart` ring · no write ever throws into UI; presence TTL sweep.
68. Land Phase-3 flip + full zero-regression gate suite · `--dart-define` swap (providers like `orders_firebase.dart:44`) + `WIRING.md`/`GATE_REGISTRY.md` · `backend_flag_test` byte-identical + `scripts/audit_gates.sh` + Inspector subagent (governance #84) · shipped default stays flags-OFF until owner approves cutover.
