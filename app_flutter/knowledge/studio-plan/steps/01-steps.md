# Pillar 1 — Config Engine + Studio Shell · Steps 1–30 (global 1–30)

> **Pillar:** No-Code Config Engine + Studio Shell + content coverage.
> **Phases covered:** 0 (grounding/flag), 1 (model·store·wrappers·edit-mode·Studio·draft/publish/versions·theme·find-replace·safety), 2 (adoption).
> **Owns global steps 1–30** of the 100-step BuildSmart Studio plan.
> Source: `knowledge/studio-plan/01-config-engine-studio.md` (§§2–15).
>
> **קנוני:** ההכרעות פה ממוזגות מ-`RED-TEAM-R1.md`+`RED-TEAM-R2.md` + מקטע 🔧-R1 בגוף-העמוד (ה-appendix גובר). השלבים-למטה כבר משקפים אותן.
> **Invariants baked into every step:** Riverpod · Hebrew/RTL/a11y (Semantics pass-through, no LTR subtree — gates 62/63/65/95) · gated `kStudioFlag` **default OFF ⇒ answer-equivalent מול fixtures + golden-render / zero-regression** (clone `backend.dart:12`) · server-ready (all writes hit the shared `ConfigDoc` via the `ConfigSink` seam, never a direct store) · governance #84 owner+manager-only edit (`isOwnerEmail` `board_accounts_local.dart:102` ∧ persona-key = `roleKeyOf(role)=='manager'` via `roleProvider` (String?, null=קבלן) `board_auth.dart:297`; `BoardRole` ממופה ב-adapter בלבד) · hand-rolled JSON, no new pub dep (gate 60), millis-id (no `uuid`).
> **100-gate protocol per step:** `flutter analyze` 0 (31) + `flutter test` 0-regress (32) + `knowledge_protocol_test` (94) + `build web` (33); UI steps add visual_log (116). Each step is independently shippable and gate-passing.

`N. <action> · <file/seam> · <gate/test> · <zero-reg note>`

## Phase A — data model + store (no UI)

1. Add `CfgNode`/`CfgStyle`/`CfgAction` value objects + tolerant JSON · NEW `lib/state/studio/config_node.dart` (clone `_enum`/null-fallback `catalog_settings.dart:360-367`) · `config_node_test` round-trip incl. unknown-key `extra` survives · pure value objects, no wiring, all-null node = identity ⇒ no behavior.
2. Add `ConfigDoc`/`ConfigLayer`/`ConfigVersion` + `schemaVersion` + `migrate()` · NEW `lib/state/studio/config_doc.dart` (`ConfigDoc.empty`; forward-compat append like `home_content_order.dart:72-75`) · `config_doc_test` empty-doc round-trips, old-schema upgrades in place · `ConfigDoc.empty` = no published/draft/versions ⇒ inert.
3. Add **pure** `mergeNode(id, doc, roleKey, includeDraft)` (default ⊕ published ⊕ persona ⊕ draft) · NEW `lib/state/studio/config_merge.dart` (the §1 merge order, no Flutter import; `roleKey` = `String` via `roleKeyOf(role)`, `roleKeyOf(null)=='contractor'` canonical sentinel — **persona keyed by the canonical String, never raw null/`BoardRole`**; `CfgStyle`/`CfgAction` merge **field-by-field** so a draft that changes one prop keeps the published prop, not whole-unit overwrite) · `config_merge_test` table: default-only, global, persona-beats-global, draft-beats-published, **per-field style/action merge (draft fontScale keeps published colorToken)**, critical `hidden` ignored, unknown-key preserved · empty doc ⇒ returns all-null identity node.
4. Add `kStudioFlag` const (`bool.fromEnvironment`) + `kStudioFlagName` runtime flag name · NEW `lib/state/studio/studio_flags.dart` (verbatim `backend.dart:12-13` invariant + `feature_flags.dart:23` naming) · `studio_flags_test` default false · no consumer yet; const default OFF = answer-equivalent (compiler-guaranteed — no consumer/tree-shaken).
5. Register `kStudioFlagName` for no-rebuild owner staging · `lib/state/feature_flags.dart:63` (leave `_forcedOnFlags` empty — do NOT add to it, mirrors `kKbLiveMirrorFlag:23`) · `feature_flags` existing test stays green (normal build answer-equivalent) · flag absent from forced set ⇒ off by default for everyone.
6. Add `ConfigStore` notifier + `ConfigSink`/`LocalPrefsSink` + **public op-API `applyOps(List<ConfigOp>)` + undo/redo-stack (`undo()`/`redo()`)** + `resetDraftNode`/`discardDraft`/`publish`/`rollback` + providers; **`editDraft(id, fn)` demoted to an internal primitive that `applyOps` is built on (not a public seam)** · NEW `lib/state/studio/config_store.dart` (clone `_load`/`_persist` `catalog_settings.dart:374-389`, key `'bs.studio-config.v1'`; sink injected = Pillar-5 seam; **parse the persisted config doc off-thread (`compute`/isolate) + lazy-hydrate `history[]` so cold-start isn't blocked**) · `config_store_test` applyOps+undo/redo round-trips, publish prunes-empties+versions, discard, rollback-is-versioned, fake `ConfigSink` (per `backend.dart` fake-gateway) · nothing reads the store yet; empty doc persists nothing new.

## Phase B — read path + wrappers + edit-mode + pilot

7. Add `resolvedNodeProvider` family (viewer persona + edit-preview aware) · NEW in `config_store.dart` (persona from `roleProvider` (String?, null=קבלן) → `roleKeyOf(role)`; **`.select`s ONLY this id's 3 layer-slices — `global[id]`, `persona[roleKey][id]`, `draft[id]` — never watches the whole `ConfigDoc`** so a keystroke/publish invalidates only the touched ids (kills the O(live-wrappers) re-resolve); previewDraft is **debounced + the in-place TextField is decoupled from the doc until commit** so per-char edits don't re-resolve the screen; calls `mergeNode`) · `config_merge_test`/`config_store_test` cover resolution + assert a single-id edit doesn't invalidate sibling-id providers · empty doc + no edit-mode ⇒ identity node ⇒ wrappers identical.
8. Add `EditModeController` + `EditModeState` gated to owner+manager+flag · NEW `lib/state/studio/edit_mode.dart` (`isEditing` flips ONLY when `kStudioFlag` ∧ `isOwnerEmail(session.email)` ∧ `roleKeyOf(ref.read(roleProvider))=='manager'` (the canonical String key — never compares a raw `BoardRole`/null); imports reused `board_accounts_local.dart:102`) · `zero_regression_test` controller cannot flip for non-owner/role (incl. contractor=null) · permanently inert off-gate (governance #84).
9. Add `EditHandle.maybe(ref, id, {child, badge})` + in-place popover stub · NEW `lib/widgets/studio/edit_handle.dart` (returns `child` unchanged unless `editModeProvider.isEditing`; dashed brand outline + tap→`studioSelect(id)` only when editing) · `cfg_wrappers_test` no-op off edit-mode · short-circuits on `isEditing==false` ⇒ zero added widgets.
10. Add `CfgText(id, fallback)` content wrapper (emoji + `applyStyle`, Semantics) · NEW `lib/widgets/studio/cfg_text.dart` (`n.text ?? fallback`, RTL-inheriting, `textScaler`-safe) · `cfg_wrappers_test` fallback when doc empty, applies override · empty/OFF ⇒ renders verbatim Hebrew literal (gate 61/64) ⇒ identical.
11. Add `CfgVisible`/`CfgList`/`CfgBox`/`CfgAction` wrappers · NEW `lib/widgets/studio/cfg_visible.dart`+`cfg_list.dart`+`cfg_box.dart`+`cfg_action.dart` (`CfgVisible` generalises `hidden_catalog_sections`; `CfgList` reorders by `order`, ReorderableListView in edit-mode like `home_content_order.dart:91-101`; `CfgAction` falls back to original `onTap`) · `cfg_wrappers_test` hide-only-when-published+not-editing, reorder-by-order, action-falls-through · no override ⇒ child unchanged, declaration order kept.
12. Add `ElementDescriptor` (**6-field contract**: `editableProps` · `allowedActions` · `allowedValues` · `kImmutable` · `kRoleFloor` · `ElementKind` — supersedes the old `axes`/`critical`/`personas`, which fold in as derived/`kImmutable`) + `kElementRegistry` (const) + `elementRegistryProvider` (concat `domainElementsProvider` Pillar-2 seam) · NEW `lib/state/studio/element_registry.dart` (`labelHe` + the 6 fields) · **`descriptorFor(id)` is FAIL-CLOSED — id absent from the registry ⇒ not editable / `validateSafe` rejects (no vacuous-green)** · `registry_contract_test` ids unique + every wrapper-id resolves a 6-field descriptor · const list, no runtime cost (§11); pure data ⇒ no behavior. **This 6-field contract is FROZEN in the new Phase-0 step 12.5 (below) BEFORE step 30 — Pillars 2/4 ground on it.**

12.5. **(NEW Phase-0 freeze)** Freeze the `ElementDescriptor` 6-field contract + a `descriptor_contract_test` (fake↔real fail-closed) · `element_registry.dart` + NEW `test/studio/descriptor_contract_test.dart` (asserts the 6 fields exist, absent-id rejects, a fake-registry can't self-certify green against the real one) · **gate stays #118 for P1** (`id ⊆ registry`); this step only freezes the shape so P4's `validateSafe` grounds on a real contract, not a vacuous one · contract-only, no behavior; must land before step 30 seam-freeze.
13. Insert `StudioOverlay` once into the `main.dart` builder Stack · `lib/main.dart:395` beside `const ConnectionIndicator()` (one line; NEW `lib/widgets/studio/studio_overlay.dart` builds `SizedBox.shrink()` off-gate, inert like `ConnectionIndicator`; also hosts the single edit-mode overlay hit-test, not 150 GestureDetectors) · `zero_regression_test` overlay builds to `SizedBox` off-gate · floats over every screen but inert ⇒ answer-equivalent + golden-render (gate 38/39 no rewrite).
14. Adopt ~10 pilot ids + their registry rows (manager KPI titles + cart CTA) · `manager_dashboard_screen.dart` cockpit `Text(` KPI titles + cart-CTA `Text(`→`CfgText` (literal stays as fallback) · `registry_contract_test` pilot ids ⊆ registry; visual_log (116) empty diff · OFF=**answer-equivalent + golden-render** (same pixel on pilot screens), revert is pure `Cfg→Text` (§9.2). **Note:** prefer pilot ids that are non-`const` `Text('ליטרל')`; wrapping a `const Text` site costs the `const` (rebuild) — record the const-loss.
15. Add `zero_regression_test` + finalize `cfg_wrappers_test` · NEW `test/studio/zero_regression_test.dart`+`cfg_wrappers_test.dart` (resolvedNode==identity, overlay==SizedBox, editMode cannot flip non-owner incl. contractor=`roleKeyOf(null)=='contractor'` — §14 codified) · gates 31/32/94 · proves OFF answer-equivalent + golden-render for all personas.

## Phase C — Studio shell + draft/publish/versioning

16. Add `studio_screen` scaffold + segmented panes (IndexedStack) + RTL/white-AppBar chrome · NEW `lib/screens/studio/studio_screen.dart` (manager chrome `manager_dashboard_screen.dart:79-90`; `StudioScreen.route()`) · widget test builds RTL, no LTR subtree (gate 65) · standalone route, unreachable until step 20 wires entry ⇒ no behavior.
17. Add top-bar (טיוטה badge · "פרסם לכולם" · "בטל טיוטה" · edit-mode toggle) + R9 inline publish-note field · `studio_screen.dart` top bar (NEW `lib/screens/studio/panes/`; inline note, no modal — R9 `MANAGER-BUILD-PLAN.md:99`) · widget test publish disabled on empty draft · **all draft mutations route through `applyOps` (undoable); never call the `editDraft` primitive directly** · **publish requires a scope-confirm sheet "ישפיע על כל המשתמשים — N שינויים" + a "view-as-persona" (צפה כפי שקבלן/שליח יראה) preview BEFORE broadcast** (parity with discard, which already confirms) ⇒ users unaffected pre-publish.
18. Add Pane A tree (registry-driven, virtualised `screen→area→element`) + persona selector + search · NEW `lib/screens/studio/panes/tree_pane.dart` (lazy `ExpansionTile`/`ListView.builder` §11; persona scopes `persona[roleKey][id]` where `roleKey=roleKeyOf(role)`, contractor=`'contractor'`) · widget test tree renders from `elementRegistryProvider`, search filters · read-only listing; selecting opens inspector, no live edit yet.
19. Add Pane B inspector (R9 inline per-axis: תוכן/נראות/עיצוב/התנהגות + live preview + "אפס אלמנט" + diff line) · NEW `lib/screens/studio/panes/inspector_pane.dart` (axes gated by `descriptor.editableProps`/`allowedActions`/`allowedValues`; נראות switch disabled+lock if `kImmutable`; behavior = Pillar-4 mount-point read-only v1) · widget test immutable id visibility-switch disabled · **every edit is emitted as a `ConfigOp` through `applyOps` (undoable); `resetDraftNode` likewise — the inspector never calls the `editDraft` primitive directly; the in-place TextField stays decoupled until commit (step 7)** ⇒ not live.
20. Wire manager-tab "🎨 סטודיו" accordion row + edit-mode toggle behind `kStudioFlag` · `manager_dashboard_screen.dart` `_ManageTab` rows region (~`:2413`, mirrors catalog/role-assign rows) · widget test row hidden when `kStudioFlag` OFF · gated entry; OFF ⇒ row absent ⇒ answer-equivalent (§10).
21. Add Pane D version history list + on-the-fly diff + one-tap "שחזר" rollback + "תצוגה מקדימה" · NEW `lib/screens/studio/panes/history_pane.dart` (`history[]` note·time·byEmail; `rollback(id)` itself versioned, forward-only undoable §7) · `config_store_test` rollback restores snapshot as new version · history empty on fresh doc; rollback is explicit owner action only.
22. Finalize `config_store_test` (publish/rollback transitions) + `registry_contract_test` and **claim gate 118** · `test/studio/registry_contract_test.dart` (every `CfgText('…')`/`CfgVisible('…')` literal id in `lib/` ⊆ `kElementRegistry`, ids unique, warn unwired) + `GATE_REGISTRY.md:11` next-free→119 + `.githooks/pre-commit` row "# שער 118" (protocol `GATE_REGISTRY.md:133`) · gate 118 green in suite + hook · source-scan only, no behavior; hook+registry committed together.

## Phase D — theme editor + find&replace + safety

23. Add `CfgTheme` override + `AppTheme` `ThemeExtension` glue · NEW `lib/state/studio/config_theme.dart` + add to `extensions:` list `app_theme.dart:62` (mechanism mirrors `BsSemanticColors`, read via accessor like `bsOnAccent`) · theme test default values unchanged · no override ⇒ defaults preserve current look ⇒ answer-equivalent + golden-render (same pixel).
24. Add Pane C theme editor (brand/surface/ink/radius/fontScale, live whole-app preview, AA contrast block) · NEW `lib/screens/studio/panes/theme_pane.dart` (reuse contrast logic behind `a11y_contrast_theme_test`; block AA-failing combos) · `a11y_contrast_theme_test` still passes; safety test rejects failing combo · **theme edits route through `applyOps` (a `CfgTheme` op, undoable), not a raw `editDraft`** ; unpublished ⇒ users unaffected.
25. Add Pane D global find-&-replace over content → draft (preview every hit by `labelHe`) · `theme_pane.dart`/NEW `lib/screens/studio/panes/find_replace_pane.dart` (registry-driven substring search; replace into DRAFT never published) · widget test preview lists hits, replace targets draft only · **replace emits ONE `applyOps(List<ConfigOp>)` batch (single undo unit via the undo-stack), not N raw `editDraft` calls** · OFF=identical; nothing reaches users until explicit פרסם.
26. Add critical-id set (bottom-nav/auth/manager-entry/edit-exit/logout) + publish-time validator (`mergeNode` ignores `kImmutable` `hidden`) · `element_registry.dart` (`kImmutable:true` seeds — the canonical critical-id flag in the 6-field contract) + `config_store.dart` publish validator (reject doc hiding a `kImmutable` id) · `safety` test publish-validator rejects hiding `kImmutable` · defence-in-depth: model+inspector+publish ⇒ owner cannot break nav (§8.1).
27. Add 3-scope reset + behavior whitelist (`noop` + guarded `navigate` to registered routes only) + write-time validation (length caps, emoji-from-legacy gate 64, no-LTR gate 65) · `config_store.dart` ("אפס הכל"=publish empty layer) + NEW `cfgActionRegistryProvider` (no external URLs gate 51, Pillar-4 fills kinds) · `safety` test reset recoverable + unknown action falls back · always recoverable to code baseline; unknown kind ⇒ original handler ⇒ no behavior change.
28. Add safety test suite (critical-can't-hide · tokens bounded to `BsTokens` · theme passes contrast · reset/validation) · NEW `test/studio/safety_test.dart` (style tokens resolve within `BsTokens`; LTR-injection refused) · safety suite green + gate 94 · protects `a11y_contrast_theme_test`; no production wiring.

## Phase E — adoption + seams + docs

29. Roll content adoption across high-value screens section-by-section + add registry auto-extract `dart` codemod-assist script · `manager_dashboard_screen.dart`→`home_shell.dart`→`catalog_screen.dart` section `Text('ליטרל')`→`CfgText` (each its own commit; literal=fallback) + NEW `tool/studio_extract_ids.dart` (greps `Cfg*('id',…)`, scaffolds reviewable registry rows, no runtime reflection) · `registry_contract_test` (118) all ids ⊆ registry; visual_log (116) empty per batch · adoption commits visually inert (OFF=**answer-equivalent + golden-render** §9.5), revert is `Cfg→Text`. **Scope = the ~532 literal `Text('…')` content axis (NOT 2,361 raw `Text(`); computed/interpolated/`Text.rich` are out-of-v1 templating; wrapping any of the 721 `const Text` sites costs `const` — log the rebuild trade-off per batch.**
30. Finalize + document the four pillar seams (§13) on a frozen interface + update knowledge · WIRING.md (engine+wrappers, gate 24/40) + `GATE_REGISTRY.md` + `manager-dashboard-MAP.md`; freeze `ConfigSink`/`editDraft`/`publish`/`elementRegistryProvider`/`cfgActionRegistryProvider` for Pillars 2–5 · full 100-gate + 10K-id virtualised tree perf check + knowledge_protocol_test (94) · docs-only + frozen seams; default OFF until owner sign-off (`MANAGER-BUILD-PLAN.md:5,17`).
