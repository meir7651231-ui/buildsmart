# Pillar 4 — AI Co-Editor + Behavior/Component Builder · 17 ATOMIC STEPS (69–85)

> ⚠️ קנוני = מקטע 🔧 תיקוני Red-Team R1 (בסוף `04-ai-coeditor-behavior.md`) + RED-TEAM-R1/R2.md. ה-appendix גובר.
> השורות למטה הוחלו עם הכרעות-R1/R2 (gate **#119** · `roleProvider` String? · `applyOps`+undo של P1 · validateSafe-בשרת · fail-closed-on-missing-registry · contract-test fake↔real חובה). כל שייר-עיצוב-ישן — בטל.

> **Branch:** `claude/whats-happening-LyY9G` · **Project:** `app_flutter/` · **Phase 5 of the 100-step Studio plan.**
> **Plan:** `knowledge/studio-plan/04-ai-coeditor-behavior.md`. Every cited `file:line` re-confirmed on this worktree (2026-06-23).
> **Owns global steps 69–85 (exactly 17).** Each step is shippable + gate-passing on its own.
>
> **Zero-hallucination spine (every step honors):** the model only NAMES keys from a CLOSED SET; deterministic Dart builds the diff. Reuse the exact-then-longest-contained validator (`matchRecipe` `describe_to_cart_screen.dart:50-67`; `matchAssistantRecipeKey` `assistant_intent.dart:79-95`), the TOTAL parser shape (`parseAssistantIntent` `assistant_intent.dart:168-211`), `promptSafeText` (`prompt_sanitize.dart:19-27`), and the `ClaudeGateway`/`claudeGatewayProvider` seam (`claude_functions.dart:126-136`) — **add no new gateway**.
> **Flag `kStudioCoEditor`** (`bool.fromEnvironment('STUDIO_CO_EDITOR')`, default OFF, mirrors `kClaudeAi` `backend.dart:138`) → byte-identical demo build. Manual builder works with NO AI; NL pane shows honest `AiOffState` (`ai_result_states.dart:18-28`) when `claudeGatewayProvider==null`. Hebrew/RTL/a11y throughout. 100-gate protocol every commit. Governance #84 (no HR / no auth-bypass — closed by omission). Reserved gate **#119 = P4 AI-grounded-config** (P1=#118, P3=#120; `GATE_REGISTRY.md:11-13`).
>
> **Canonical seams (R1/R2 — override anything below):** persona is keyed on **`roleProvider` (String?, null=contractor)**, NOT `BsRole` — `SetVisible` & role-floor resolve against `roleProvider`; enums are display-adapter only. P4 writes ONLY via **P1's `applyOps(List<ConfigOp>)` + undo-stack** (no P4-local apply). The closed sets (`allowedActions`/`allowedValues`/`kImmutable`/`kRoleFloor`/`ElementKind`) come from P1's **frozen `ElementDescriptor`** — **fail-closed when a field is absent** (never a vacuous `validateSafe`). Client `validateSafe` is **advisory/bypassable**; the full `validateSafe` (role-floor · action-legality · critical-lock · contrast · batch-ceiling) **re-runs server-side inside `publishConfig`**. `nav.screen` is limited to ~38 no-arg routes; typed-arg routes are out-of-v1. The `registryViewContract` (fake↔real) test is **MANDATORY** and `studio_safety_test` must pass against P1's **REAL frozen registry** before gate-119 closes.
>
> **Gate baseline every step:** `flutter analyze` (0 err, #31) · `flutter test` (0 reg, #32) · `flutter build web` clean (#33) · `knowledge_protocol_test` (#94) · `WIRING.md` (#24). Listed below is the step's ADDED gate/test.
> **Line format:** `N. <action ≤14 words> · <new file OR existing file:line seam> · <gate/test> · <zero-reg note>`

---

69. Add closed `ConfigOp` sealed family + JSON `toJson`/`fromJson` · NEW `lib/logic/studio/config_op.dart` · unit round-trips every variant · pure data only, no imports beyond enums; nothing wired.
70. Define fake in-memory `ElementRegistry` + frozen query interface for tests · NEW `lib/logic/studio/registry_view.dart` (interface + fake; mirrors fake-gateway discipline) · fake exposes elementIds/propKeysFor/allowedValues/actionIdsFor/componentTypes · fake = scaffolding ONLY; the REAL grounding source is P1's frozen `ElementDescriptor` (R1-2) — a `registryViewContract(RegistryView Function())` BOTH fake and P1's real adapter MUST pass (MANDATORY, not 💡).
71. Add registry closed-set matchers (exact→longest-contained, null-on-miss) · `lib/logic/studio/registry_view.dart` (copy `describe_to_cart_screen.dart:50-67`) · `test/studio_registry_view_test.dart`: invented id/prop/value→null, prefix-collision picks longest · pure resolvers; missing registry field → fail-closed (drop-all), never vacuous-pass.
72. Build static action-catalog grounded in real `route()`s + `open*Sheet`s · NEW `lib/logic/studio/action_catalog.dart` (sources `manager_copilot_screen.dart:32`, `contractor_tools_sheets.dart:26,39`, `ai_assistant_screen.dart:210-222`, `reject_reason_screen.dart:110-117`, `ai_hub_screen.dart:113`) · `test/studio_action_catalog_test.dart`: every ActionId→effect, `nav.screen` targets limited to ~38 no-arg routes; typed-arg routes are out-of-v1 — the manual builder GREYS them out with "צריך פרמטרים — לא זמין" (not a silent drop) · static data; no nav/auth action present (closed by omission).
73. Build static component-palette templates with typed required-prop schemas · NEW `lib/logic/studio/component_palette.dart` · `test/studio_palette_test.dart`: allowedContainers vs element.kind enforced · static data; rendering stays Pillar-1's wrapper job.
74. Add grounded prompt builder embedding closed sets + strict-JSON ops grammar · NEW `lib/logic/studio/edit_prompt.dart` (compact `id=he` per `describe_to_cart_screen.dart:34`; `promptSafeText` cap) · `test/studio_edit_prompt_test.dart`: sets embedded, utterance capped, Stage-A scope-classifier returns a CLOSED-SET token from the registry ("מעורפל→שאל"), Stage-B scope <8000 chars (`claude.ts:40`) · pure string build; no gateway call.
75. Implement TOTAL `parseConfigEdit` validating EVERY field vs registry, dropping invented · NEW `lib/logic/studio/edit_intent.dart` (copy `parseAssistantIntent` `assistant_intent.dart:168-211`) · `test/studio_edit_intent_test.dart`: valid parses; invented id/prop/value/action/component dropped; malformed→empty, never throws; **truncated JSON** (finish_reason≠stop / missing `}`) → "לא הצלחתי" not a partial preview · anti-hallucination core; deterministic, gateway-free.
76. Expand closed `scope` tokens to concrete ids via registry (not model enumeration) · `lib/logic/studio/edit_intent.dart` (scope set all-buttons/every:/single:) · `test/studio_edit_intent_test.dart`: `all-buttons`→N grounded SetProp, ceiling honored · broadcast = N individually-validated ops over REAL ids.
77. Implement `validateSafe` nav/auth immutability + `criticalBusiness` floor + per-element/value/action legality · NEW `lib/logic/studio/edit_safety.dart` (kImmutable rationale `manager_dashboard_screen.dart:200-209`, `sys_chat.dart` isolation; `kCriticalBusiness` from registry) · `test/studio_safety_test.dart`: immutable element rejects all ops; type/action mismatch blocked; can't hide price / relabel "אשר הזמנה" / fail WCAG-AA contrast (contrast-check runs INSIDE validateSafe); `SetProp(color)` limited to a per-element-kind token subset · advisory/bypassable client backstop — re-runs server-side in `publishConfig`; fail-closed when registry field absent.
78. Add role-visibility floor + batch ceiling + session-budget to `validateSafe` (Hebrew reasons) · `lib/logic/studio/edit_safety.dart` (kRoleFloor over `roleProvider` map [String?, null=contractor], NOT `BsRole`; kStudioMaxBatch per-utterance + kStudioSessionBudget cumulative + kStudioMaxRegistryFraction ">K% of registry" guard) · `test/studio_safety_test.dart`: can't hide from manager / from EVERY persona / over per-utterance ceiling / over cumulative session-budget / over registry-fraction; blocked carry reasonHe · SafetyVerdict{applied,blocked}; same ceilings re-enforced server-side (R1-1).
79. Add pure Hebrew `summarizeDiff` preview rows (broadcast count + blocked count) · NEW `lib/logic/studio/diff_preview.dart` · `test/studio_diff_preview_test.dart`: correct rows, "N שינויים", "נחסמו K" · pure Dart, zero model — re-preview is free.
80. Add `kStudioCoEditor` flag + `studioCoEditorProvider` (enabled × ai axes) · `lib/data/repositories/backend.dart:138` seam (add const beside `kClaudeAi`) · `test/studio_gating_test.dart`: false→dark · default OFF → demo byte-identical (`kClaudeAi` invariant).
81. Add Studio manager-only cockpit hero + `StudioScreen` route shell + off-states · `lib/screens/manager_dashboard_screen.dart:420` (`_CopilotHero` seam) + NEW `lib/screens/studio_screen.dart` (`route()` per `manager_copilot_screen.dart:32`; guard `:73-75`) · `test/studio_gating_test.dart`: hero only when flag+manager · hero hidden when flag OFF.
82. Build manual no-model builder: pick element→prop/visibility/component/action→preview→confirm→undo · NEW `lib/screens/studio_component_builder.dart` (confirm gate per `ai_assistant_screen.dart:208-229`; typed-arg routes greyed "צריך פרמטרים — לא זמין") · `test/studio_screen_behavior_test.dart`: confirmed diff calls **P1 `applyOps(List<ConfigOp>)` once**; undo reverts via **P1's undo-stack** (our op-stack is a mirror only, P1=SSOT) · works fully with `kClaudeAi` OFF.
83. Wire NL co-editor pane: `claudeGatewayProvider.ask`→parse→safe→preview→confirm, honest off-state · `lib/screens/studio_screen.dart` (SHARED seam `claude_functions.dart:126-136`; race `||_loading` `describe_to_cart_screen.dart:104`, mounted, 200-empty retry; Stage-A scope shown in preview "מתוך: לשונית רכש"; confirm→**P1 `applyOps`**) · `test/studio_screen_behavior_test.dart`: gateway-null→`AiOffState`+manual usable; model never triggers `applyOps` · behind `kStudioCoEditor && kClaudeAi`.
84. Add closed-set rules/automation model + `parseRule` + manual/NL rules screen · NEW `lib/logic/studio/rules_model.dart` + NEW `lib/screens/studio_rules_screen.dart` (read-only advisory eval over `ordersEngineProvider`/`managerAnalyticsProvider` `manager_copilot_screen.dart:53-64`) · `test/studio_rules_test.dart`: "תקועה >2 ימים→התרע למנהל" round-trips; invented trigger/action drops · mutating actions deferred behind same confirm gate.
85. Register gate **#119** + injection-sanitize assertions + governance #84 audit; update protocol docs · `.githooks/pre-commit` + `knowledge/GATE_REGISTRY.md:11-13` (#119=P4 reserved; P1=#118, P3=#120 — already reserved) + `WIRING.md`/`STATUS.md`/`ROADMAP.md` · gate-119 REQUIRES (not 💡): `registryViewContract` (fake↔real) green **and** `studio_safety_test` passing against P1's **REAL frozen `ElementDescriptor`** (fake-registry self-certification is vacuous); the grounding property-invariant (∀op every field ∈ registry, or empty, never throw) run as a REQUIRED FUZZ over **thousands of random replies** — not enumerated samples; assert HR/role/auth ids absent from EVERY closed set; utterance+labels `promptSafeText`'d · seals zero-hallucination + governance invariants.

---

### Dependency spine (why this order)
`ConfigOp` (69) is the shared currency. The registry adapter + matchers (70–71) gate everything; the fake is scaffolding, but the `registryViewContract` (fake↔real) and `studio_safety_test` MUST pass against P1's **REAL frozen registry** before gate-119 closes. Action-catalog + palette (72–73) populate the closed sets the prompt (74) embeds. `parseConfigEdit` (75) — the grounded NL→ops core — needs matchers + catalog + palette to validate against; scope expansion (76) extends it over real ids. `validateSafe` (77–78) is the **advisory** client backstop (re-run server-side in `publishConfig`) on the parsed ops. `summarizeDiff` (79) renders them. Flag + cockpit (80–81) make the surface visible (manager-only, dark by default). The manual builder (82) ships a working no-code path with NO model, writing via **P1 `applyOps`**; the NL pane (83) adds speech as an upgrade through the shared gateway. Rules (84) reuse the whole spine. The capstone (85) registers gate **#119** and seals the anti-hallucination + governance #84 invariants.
