# Pillar 4 — AI Co-Editor + Behavior/Component Builder · 17 ATOMIC STEPS (69–85)

> **Branch:** `claude/whats-happening-LyY9G` · **Project:** `app_flutter/` · **Phase 5 of the 100-step Studio plan.**
> **Plan:** `knowledge/studio-plan/04-ai-coeditor-behavior.md`. Every cited `file:line` re-confirmed on this worktree (2026-06-23).
> **Owns global steps 69–85 (exactly 17).** Each step is shippable + gate-passing on its own.
>
> **Zero-hallucination spine (every step honors):** the model only NAMES keys from a CLOSED SET; deterministic Dart builds the diff. Reuse the exact-then-longest-contained validator (`matchRecipe` `describe_to_cart_screen.dart:50-67`; `matchAssistantRecipeKey` `assistant_intent.dart:79-95`), the TOTAL parser shape (`parseAssistantIntent` `assistant_intent.dart:168-211`), `promptSafeText` (`prompt_sanitize.dart:19-27`), and the `ClaudeGateway`/`claudeGatewayProvider` seam (`claude_functions.dart:126-136`) — **add no new gateway**.
> **Flag `kStudioCoEditor`** (`bool.fromEnvironment('STUDIO_CO_EDITOR')`, default OFF, mirrors `kClaudeAi` `backend.dart:138`) → byte-identical demo build. Manual builder works with NO AI; NL pane shows honest `AiOffState` (`ai_result_states.dart:18-28`) when `claudeGatewayProvider==null`. Hebrew/RTL/a11y throughout. 100-gate protocol every commit. Governance #84 (no HR / no auth-bypass — closed by omission). New gate **#118** (GATE_REGISTRY next-free, confirmed `GATE_REGISTRY.md:10`).
>
> **Gate baseline every step:** `flutter analyze` (0 err, #31) · `flutter test` (0 reg, #32) · `flutter build web` clean (#33) · `knowledge_protocol_test` (#94) · `WIRING.md` (#24). Listed below is the step's ADDED gate/test.
> **Line format:** `N. <action ≤14 words> · <new file OR existing file:line seam> · <gate/test> · <zero-reg note>`

---

69. Add closed `ConfigOp` sealed family + JSON `toJson`/`fromJson` · NEW `lib/logic/studio/config_op.dart` · unit round-trips every variant · pure data only, no imports beyond enums; nothing wired.
70. Define fake in-memory `ElementRegistry` + frozen query interface for tests · NEW `lib/logic/studio/registry_view.dart` (interface + fake; mirrors fake-gateway discipline) · fake exposes elementIds/propKeysFor/allowedValues/actionIdsFor/componentTypes · test-only fake, no Pillar-1 dep yet.
71. Add registry closed-set matchers (exact→longest-contained, null-on-miss) · `lib/logic/studio/registry_view.dart` (copy `describe_to_cart_screen.dart:50-67`) · `test/studio_registry_view_test.dart`: invented id/prop/value→null, prefix-collision picks longest · pure resolvers; tested vs fake registry FIRST.
72. Build static action-catalog grounded in real `route()`s + `open*Sheet`s · NEW `lib/logic/studio/action_catalog.dart` (sources `manager_copilot_screen.dart:32`, `contractor_tools_sheets.dart:26,39`, `ai_assistant_screen.dart:210-222`, `reject_reason_screen.dart:110-117`, `ai_hub_screen.dart:113`) · `test/studio_action_catalog_test.dart`: every ActionId→effect, nav targets registry-known only · static data; no nav/auth action present (closed by omission).
73. Build static component-palette templates with typed required-prop schemas · NEW `lib/logic/studio/component_palette.dart` · `test/studio_palette_test.dart`: allowedContainers vs element.kind enforced · static data; rendering stays Pillar-1's wrapper job.
74. Add grounded prompt builder embedding closed sets + strict-JSON ops grammar · NEW `lib/logic/studio/edit_prompt.dart` (compact `id=he` per `describe_to_cart_screen.dart:34`; `promptSafeText` cap) · `test/studio_edit_prompt_test.dart`: sets embedded, utterance capped, two-stage scope <8000 chars (`claude.ts:40`) · pure string build; no gateway call.
75. Implement TOTAL `parseConfigEdit` validating EVERY field vs registry, dropping invented · NEW `lib/logic/studio/edit_intent.dart` (copy `parseAssistantIntent` `assistant_intent.dart:168-211`) · `test/studio_edit_intent_test.dart`: valid parses; invented id/prop/value/action/component dropped; malformed→empty, never throws · anti-hallucination core; deterministic, gateway-free.
76. Expand closed `scope` tokens to concrete ids via registry (not model enumeration) · `lib/logic/studio/edit_intent.dart` (scope set all-buttons/every:/single:) · `test/studio_edit_intent_test.dart`: `all-buttons`→N grounded SetProp, ceiling honored · broadcast = N individually-validated ops over REAL ids.
77. Implement `validateSafe` nav/auth immutability + per-element/value/action legality · NEW `lib/logic/studio/edit_safety.dart` (kImmutable rationale `manager_dashboard_screen.dart:200-209`, `sys_chat.dart` isolation) · `test/studio_safety_test.dart`: immutable element rejects all ops; type/action mismatch blocked · pure backstop atop closed-set grounding.
78. Add role-visibility floor + batch ceiling to `validateSafe` (Hebrew reasons) · `lib/logic/studio/edit_safety.dart` (kRoleFloor over `BsRole` `sys_chat.dart:45`; kStudioMaxBatch) · `test/studio_safety_test.dart`: can't hide from manager / from EVERY role / over ceiling; blocked carry reasonHe · SafetyVerdict{applied,blocked}; nothing orphaned.
79. Add pure Hebrew `summarizeDiff` preview rows (broadcast count + blocked count) · NEW `lib/logic/studio/diff_preview.dart` · `test/studio_diff_preview_test.dart`: correct rows, "N שינויים", "נחסמו K" · pure Dart, zero model — re-preview is free.
80. Add `kStudioCoEditor` flag + `studioCoEditorProvider` (enabled × ai axes) · `lib/data/repositories/backend.dart:138` seam (add const beside `kClaudeAi`) · `test/studio_gating_test.dart`: false→dark · default OFF → demo byte-identical (`kClaudeAi` invariant).
81. Add Studio manager-only cockpit hero + `StudioScreen` route shell + off-states · `lib/screens/manager_dashboard_screen.dart:420` (`_CopilotHero` seam) + NEW `lib/screens/studio_screen.dart` (`route()` per `manager_copilot_screen.dart:32`; guard `:73-75`) · `test/studio_gating_test.dart`: hero only when flag+manager · hero hidden when flag OFF.
82. Build manual no-model builder: pick element→prop/visibility/component/action→preview→confirm→undo · NEW `lib/screens/studio_component_builder.dart` (confirm gate per `ai_assistant_screen.dart:208-229`; op-stack undo) · `test/studio_screen_behavior_test.dart`: confirmed diff calls draft.apply once; undo reverts · works fully with `kClaudeAi` OFF.
83. Wire NL co-editor pane: `claudeGatewayProvider.ask`→parse→safe→preview→confirm, honest off-state · `lib/screens/studio_screen.dart` (SHARED seam `claude_functions.dart:126-136`; race `||_loading` `describe_to_cart_screen.dart:104`, mounted, 200-empty retry) · `test/studio_screen_behavior_test.dart`: gateway-null→`AiOffState`+manual usable; model never triggers apply · behind `kStudioCoEditor && kClaudeAi`.
84. Add closed-set rules/automation model + `parseRule` + manual/NL rules screen · NEW `lib/logic/studio/rules_model.dart` + NEW `lib/screens/studio_rules_screen.dart` (read-only advisory eval over `ordersEngineProvider`/`managerAnalyticsProvider` `manager_copilot_screen.dart:53-64`) · `test/studio_rules_test.dart`: "תקועה >2 ימים→התרע למנהל" round-trips; invented trigger/action drops · mutating actions deferred behind same confirm gate.
85. Register gate #118 + injection-sanitize assertions + governance #84 audit; update protocol docs · `.githooks/pre-commit` + `knowledge/GATE_REGISTRY.md:10` (bump next-free→119) + `WIRING.md`/`STATUS.md`/`ROADMAP.md` · grounding+safety suites green; assert HR/role/auth ids absent from EVERY closed set; utterance+labels `promptSafeText`'d · seals zero-hallucination + governance invariants.

---

### Dependency spine (why this order)
`ConfigOp` (69) is the shared currency. The registry adapter + matchers (70–71) gate everything and are proven against a **fake registry first**. Action-catalog + palette (72–73) populate the closed sets the prompt (74) embeds. `parseConfigEdit` (75) — the grounded NL→ops core — needs matchers + catalog + palette to validate against; scope expansion (76) extends it over real ids. `validateSafe` (77–78) is the guardrail backstop on the parsed ops. `summarizeDiff` (79) renders them. Flag + cockpit (80–81) make the surface visible (manager-only, dark by default). The manual builder (82) ships a working no-code path with NO model; the NL pane (83) adds speech as an upgrade through the shared gateway. Rules (84) reuse the whole spine. The capstone (85) registers gate #118 and seals the anti-hallucination + governance #84 invariants.
