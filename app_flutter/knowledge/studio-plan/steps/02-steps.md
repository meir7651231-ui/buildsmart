# Pillar #2 — Domain / Vertical Builder · build-steps 31–50 (phase 3)

> ⚠️ קנוני = מקטע 🔧 תיקוני Red-Team R1 (בסוף `02-domain-vertical-builder.md`) + RED-TEAM-R1/R2.md. ה-appendix גובר.
> Source plan: `knowledge/studio-plan/02-domain-vertical-builder.md`. Branch `claude/whats-happening-LyY9G`.
> Global slice: **steps 31–50** of the 100-step BuildSmart-Studio decomposition (exactly 20 here; the Phase-G integrity steps Eα/Eβ/Eγ + FK-validation are additive after 50).
> Stack: Flutter 3.29 / Dart 3.7 / Riverpod · RTL/Hebrew/a11y · gated default-OFF · server-ready · 100-gate protocol · governance #84.
> **Keystone (the zero-reg pivot):** step 38 — the generated plumbing seed is asserted **answer-equivalent** to the live consts against the existing fixtures (`kCatalogProducts` is `final`, no bytes; compat = galvanic logic, not data pairs). Nothing trade-agnostic ships until it is green.
> **Plumbing physics stays hard-coded FOREVER:** the `install_engine.dart` `'plumbing'` branch (`_galvanicallyDissimilar:117` + conditional compliance: anti-scald/TMTV, Legionella sampling, dielectric-union, recirc isolation≥3) is NEVER generalized into the matrix — plumbing simply never routes through the resolver. Only NEW trades use authored `CompatibilityRule`/`CompletionRule`.
> **Scale to 10Ks (indexed search, Firestore sharding, list virtualization) is OUT — deferred to Pillar-5 steps.**
>
> Per-step gate baseline (every step, in addition to its named gate): `flutter analyze` 0 errors · `flutter test` (incl. `knowledge_protocol_test`) green · `WIRING.md` updated for any new wired symbol. Each step is independently shippable behind a default-OFF flag → demo/live build stays byte-identical (compile-flag OFF). The plumbing answer-stability is proven by fixtures, not byte-diff.
> Line format: `N. <action> · <file / file:line seam> · <gate/test> · <zero-reg note>`

---

### Schema + flags + store (31–34) — pure additive, no read-path touched

31. Add three default-OFF flags + flag-helper consts · new `lib/state/trade_builder_flags.dart` (`kTradeBuilderFlag`,`kTradeStudioFlag`,`kTradeImportFlag`) via `feature_flags.dart:36` `FeatureFlagsNotifier` · `feature_flags_test` extended (all three default OFF, not in `_forcedOnFlags`) · no consumer reads a flag yet → zero call-sites changed.

32. Define trade/category/attribute/product/accessory/fixture schema **+ capture+freeze the current variant snapshot EARLY** · new `lib/domain/trade_schema.dart` (`Trade` incl. `schemaVersion`,`TradeCategory`,`AttributeDef`,`AttributeValue`,`AttributeKind`,`TradeProduct`,`AccessoryRule`,`SmartFixture`,`InstallStage`) · new `trade_schema_test` (toJson/fromJson roundtrip, stable string ids) **+ new `variant_families_snapshot_test` that captures & freezes every family `allVariantFamilies()` produces today (frame+kind+brand+category+members)** before the regex is deleted in step 45 · pure new file, imported nowhere live; the frozen snapshot is the baseline step 45 must reproduce.

33. Define connection schema (authored matrix, replaces closed enum) · new `lib/domain/connection_schema.dart` (`ConnectorType`,`SystemDef`,`ProductConnectorSpec`,`ProductEnd`,`CompatibilityRule`,`SizeMatch`,`CompletionRule`,`RuleSeverity`) **— `CompatibilityRule` gains `final String? materialGroup`/`final List<String>? incompatibleMaterialGroups` (else it cannot reproduce HDPE↔PVC / galvanic copper-group↔iron-group physics); `CompletionRule` gains `incompatibleMaterialGroups`/`requiredInterposerWhyHe`; `ProductConnectorSpec` adds derived `materialGroupId`** · `connection_schema_test` (JSON roundtrip; severity≡`CheckSeverity` mapping; material-group fields survive roundtrip) · `lipskey_verified_connections.dart:24` `EndType`/`:41` `WaterSystem` left untouched (they become seed types in 37).

34. Add authored-trade store (deltas only, SharedPreferences) · new `lib/state/trades_store.dart` `StateNotifier<TradesDoc>` (key `bs.trades.v1`) mirroring `catalog_settings.dart:8` `_kStorageKey`/`_load`/`_persist` idiom · **G-roundtrip** new `trades_store_roundtrip_test` (mirror `persistence_roundtrip_test`) · starts empty → no published trade → live app unaffected.

### Generated plumbing seed = the zero-reg keystone (35–38)

35. Add adapter `TradeProduct.toLegacy()`→`LipskeyCatalogProduct` (1:1; `attributes`→`dims`) · new `lib/domain/trade_product_adapter.dart` against `lipskey_catalog.dart:4` `LipskeyCatalogProduct` · `trade_product_adapter_test` (`==` + render-field identity on sampled `kCatalogProducts`) · adapter is the only screen-facing type → screens still see `LipskeyCatalogProduct`.

36. Write generator: consts → plumbing `Trade` doc + categories/products/fixtures **+ seed CI gate (two distinct checks)** · new `scripts/gen_plumbing_seed.dart` reading `polyroll_catalog.dart:1513` `kCatalogProducts`, `catalog_tree.dart:36` `kCatalogTree`, `catalog.dart:5` `kCatalogCats`, `smart_tree.dart:153` `kSmartProducts`, `brands.dart:25` `kBrands` · **CI gate #1 (stale-seed): re-run `gen_plumbing_seed.dart` then `git diff --exit-code` on the committed seed (fails if the checked-in file drifted from the generator).** CI gate #2 (seed↔const answer-equivalence) lives in step 38 — they are SEPARATE checks · script only; no lib import yet.

37. Commit generated seed incl. `CompatibilityRule`s derived from 891 specs · new `lib/domain/seeds/plumbing_trade_seed.dart` (emitted from `lipskey_verified_connections.dart:77` `VerifiedSpec` mating pairs; `EndType`→`ConnectorType`, `WaterSystem`→`SystemDef`; galvanic groups from `_galvanicallyDissimilar:117` → `materialGroup`/`incompatibleMaterialGroups` so the answer is even *expressible* in fixtures) · file compiles; analyze clean · authored store still empty → plumbing supplied by baked seed; app stays answer-equivalent (compile-flag OFF = byte-identical build).

38. **KEYSTONE — assert seed answer-equivalent to live consts against existing fixtures** · new `test/trade_seed_equivalence_test.dart` pipes `compat_50_samples_test`+`full_compliance_audit_test`+`catalog_regression_test` **AND the FULL physics suite (`ten_scenarios_audit_test`/`auto_compliance_test`/`install_plan_coverage_test`)** through the seed path, asserting same answer (mates?/method/severity/compliance-list — the slim fixtures omit galvanic/Legionella/dielectric, so the full suite is mandatory) + product/category/spec count (891) + render-field identity for a single product + the step-32 variant snapshot · **G-seed (hard gate to proceed); CI gate #2 = seed↔const equivalence, distinct from step-36 stale-seed `git diff`** · `git`-no-diff on const files is only a side-check (not the equivalence proof) · red = plumbing regression; no later step lands until green.

### Trade-agnostic resolver (39–41) — pure logic, proven against old engine

39. Build pure trade-agnostic engine (no UI) · new `lib/domain/connection_resolver.dart` `canConnect(a,b,ruleSet)`/`completion(line)`/`systemCoherence(line)` over `CompatibilityRule`/`CompletionRule` · unit `connection_resolver_unit_test` (SizeMatch exactSame/anyToAny/tableLookup) · pure functions, called nowhere live yet.

40. **G-resolver parity** — run resolver on seeded matrix vs old answers · new `test/connection_resolver_parity_test.dart` feeding `compat_50_samples_test` + `full_compliance_audit_test` fixtures through resolver, asserting equality with `lipskey_verified_connections.dart` results · **G-resolver (gate)**; old `compat_*`/`full_compliance_audit_test` untouched & green · resolver proven equal before any screen calls it.

41. Wrap engine connect/compliance behind flag, plumbing branch kept FOREVER · `install_engine.dart:151` `lineComplianceChecklist` + `:90` `connectionMethodLabel` delegate to resolver only when `tradeId!='plumbing'` & `kTradeStudioFlag` ON · `'plumbing'` is NEVER seeded into the matrix — it simply never routes through the resolver; its hand-coded branch (galvanic + anti-scald/Legionella/dielectric/recirc) is permanent, not deletable · `install_engine_*` (b5/b6/b8/b10/b11/b13/safety/hardening) all stay green · **DoD (was a 💡): assert `tradeId=='plumbing'` can NEVER enter the resolver branch** → OFF path = today.

### Repository tradeId seam + active-trade (42–43) — default reproduces today

42. Add `tradeId` param to repo reads, default `'plumbing'` = seed · `catalog_repository.dart:36` interface (`allProducts`/`catalogCategories`/`smartTreeCats` + new `categoryTree`) + impl `catalog_local.dart:44` `LocalCatalogRepository` filtering seed by trade · `repositories_test`+`catalog_regression_test` green (plumbing seed≡`kCatalogProducts`) · default arg → existing callers unchanged.

43. Add `activeTradeProvider` (defaults `'plumbing'`; switcher hidden when count==1) · new provider in `lib/state/trades_store.dart` consumed via `catalogRepositoryProvider` (`catalog_local.dart:103`) · `active_trade_provider_test` (default plumbing; switcher hidden at count==1) · single published trade → no switcher UI → zero visible change.

### Hebrew no-code authoring screens (44–47) — gated manager tab, RTL/a11y

44. Add gated manager entry + trade list + define-step (Trade draft) · new `lib/screens/trade_builder/trade_builder_home.dart` + `trade_define_step.dart`; entry in `manager_dashboard_screen.dart` ("🏗️ בונה ענפים", `kTradeBuilderFlag`) writing via `tradesEditProvider` over `trades_store.dart` · **G-authoring** widget test (RTL directionality + semantics + `textScaler` clamp 1.35) · tab hidden when flag OFF → `manager_dashboard_screen_test` unchanged.

45. Add category-tree + attribute-schema editors **+ delete the `variant_families.dart` regex only after parity vs the step-32 frozen snapshot** · new `lib/screens/trade_builder/category_tree_editor.dart` (RTL reorder add/rename/move/delete `TradeCategory`) + `attribute_schema_editor.dart` (`AttributeDef` kind/values/"ציר וריאנט?"/unit — authored replacement for `variant_families.dart:9` `AttrKind`) · widget tests RTL+a11y; live name-decompose preview · **DoD: the authored `matchTokens` axes must reproduce the step-32 `variant_families_snapshot_test` baseline EXACTLY (same families, same members) BEFORE `_isSizeToken:25`/`kindOf:32`/`allVariantFamilies:77` are removed; plumbing keeps `allVariantFamilies()` until snapshot is green** · writes only via `tradesEditProvider`; const files untouched.

46. Add product + accessory authoring editors · new `lib/screens/trade_builder/product_authoring_screen.dart` (edit `attributes` vs schema; non-virtualized list, ≤10K — Pillar-5 owns scale) + `accessory_rule_editor.dart` (`AccessoryRule` name/why/must/price/linkSku) · widget tests RTL+a11y · authored deltas only → live catalog reads seed unchanged.

47. Add connection-rule studio + publish sheet **with referential FK validation** · new `lib/screens/trade_builder/connection_rule_studio.dart` (`ConnectorType`/`SystemDef` + A×B matrix + `CompletionRule` + live test-bench calling `connection_resolver.dart`) + `trade_publish_sheet.dart` (validation: every category≥1 product, every variant axis has values, no orphan compat rule, **+ publish-time referential FK check: every `product.categoryId`/`tradeId` resolves AND every rule `aTypeId`/`bTypeId`/`*TypeId` resolves — else reject publish** → flips `published`) · widget tests RTL+a11y + publish-validation blocks bad trade **+ blocks a dangling-FK trade** · draft never affects live app.

### Bulk import contract (48) — mapping/validation only, at-scale = Pillar 5

48. Add import template-export + column-map + dry-run + gated commit · UI in `product_authoring_screen.dart` + new `scripts/` template generator from `AttributeDef`s · **G-import** new `bulk_import_test` (template→map→dry-run→commit on synthetic CSV; required/enum/sku-unique **+ "categoryId exists" referential check per row**; **no write on error**) behind `kTradeImportFlag` · 10K-row pipeline/Firestore batches/indexed search explicitly deferred to Pillar 5.

### Gated install-studio + brand refactor + electrician acceptance (49–50)

49. Abstract install-studio baked physics + brand if-ladders at named seams (gated) · `install_studio_screen.dart:37` system colors→`SystemDef.color`, `:100` picker→`smartFixtures`, `:137` compliance labels→`CompletionRule.whyHe`, `:439` temp pills→envelope, `:558` `canConnect`→`connection_resolver`, `:2438` kit, `:2319` slope→optional `TradePhysicsConfig`; `related_info.dart:53/54/446/449/500/527/624/645` + `lipskey_catalog.dart:50` if-ladders→`BrandProfile` (`brands.dart:4`) · **`BrandProfile` must map EVERY if-ladder payload — spec-envelope (Polyroll 90°C / Huliot 95°C) · bore-parse (diRangeMax | dnDirect) · image-dir (`_brandDir:49-53`) · finder emoji/label · kit-count/systemHint — and a `brand_profile_parity_test` must assert the profile returns the SAME answer as the if-ladder for all 5 groups BEFORE deletion** · note: plumbing keeps its hand-coded physics constants/branch (the envelope/slope config-read only replaces them for a NEW trade) · **G-flag-off** golden/snapshot diff vs current (all `kTrade*` OFF) green · OFF/`'plumbing'`→identical constants; `if` ladders deleted only after G-seed green AND `brand_profile_parity_test` green.

50. **G-newtrade acceptance — author "חשמלאי" end-to-end, catalog+studio function** · new `test/trade_newtrade_acceptance_test.dart` (in-test author 2 categories, 1 variant axis, 2 products, 1 compat rule, **+ author ≥1 `CompletionRule`** → publish → assert catalog list + install-studio connect for electrician **+ ASSERT the `CompletionRule` FIRES (a violating line surfaces it) and BLOCKS publish when violated**) + sync `WIRING.md`/`02-domain-vertical-builder.md` · **G-newtrade (acceptance)** with full suite green; per-trade rollout default-OFF until owner approval (governance #84) · the literal "תוסיף חשמלאי בעצמי" proof; plumbing path unchanged throughout.

### Phase-G integrity — referential FK guard + schema migration + archive/tombstone (additive after 50, all gated, plumbing reserved & non-archivable)

FK. Add `onCatalogProductWrite` referential validator (publish-time + write-trigger) · new `lib/domain/trade_referential_validator.dart` mirroring the step-47 publish FK check at the write seam (Pillar-5 backs it with a server `onWrite`): every `product.categoryId`/`tradeId` and every rule `typeId` must resolve, **else reject/quarantine the write** · new `trade_referential_validator_test` (dangling categoryId/tradeId/typeId → quarantined, never live; valid → passes) · plumbing seed already FK-clean (G-seed) → no live change.

Eα. Add real `Trade.schemaVersion` + `migrate(json, fromVersion)` (not a no-op) · `trades_store.dart` gets a genuine upgrade path with a tested migration · new `trade_migration_test` raises an old-version doc to current and asserts field-by-field correctness · ingestion guards keep-last-good on `migrate()`/`fromJson` throw → a bad doc never bricks the store.

Eβ. Add tombstone/archive model for trade + product · `Trade.archivedAt`/`TradeProduct.archivedAt` (or a separate tombstone record with `replacedBy`) hides from live but retains for audit/migrate · **`replacedBy` resolution needs a visited-set/cycle guard** (A→B→A must not loop) · new `trade_archive_test` (archived hidden from live; audit retained; `replacedBy` cycle is detected & broken, not infinite) · plumbing (reserved) is NOT archivable → keystone safe.

Eγ. Add orphan fan-out cleanup + migrate-map on archive · on archive, fan-out over every reference to the dead id — `AccessoryRule.linkSku`/`appliesToProductId`, `CompatibilityRule.aTypeId/bTypeId`, `CompletionRule.*TypeId`, search-index/synonyms, orders/carts — and apply a `{deletedId → replacementId|null}` migrate-map (re-point or mark-orphan, never crash) · new `archive_fanout_test` (every dependent ref re-pointed or flagged; no dangling reference survives) · all via `tradesEditProvider`, default-OFF.

---

> **Gate ledger (31–50 + Phase-G):** G-roundtrip (34) · **G-seed/keystone answer-equivalence + full physics suite (38, hard gate to proceed)** · G-resolver (40) · G-authoring (44–47) · publish FK-validation (47) · G-import + categoryId-exists (48) · G-flag-off + `brand_profile_parity_test` (49) · **G-newtrade + CompletionRule fires/blocks (50, acceptance)** · referential `onCatalogProductWrite` (FK) · `trade_migration_test` (Eα) · `trade_archive_test` + cycle-guard (Eβ) · `archive_fanout_test` (Eγ). Every step ships behind a default-OFF flag → demo/live build byte-identical (compile-flag OFF); plumbing answer-stable via fixtures, hard-coded physics permanent, plumbing reserved & non-archivable (keystone safe).
