# BuildSmart Studio — פירוט עמוק לשלבים 16–30

> **Pillar 1 — Config Engine + Studio Shell.** Phase C (16–22: shell + draft/publish/versioning),
> Phase D (23–28: theme editor + find&replace + safety), Phase E start (29–30: adoption + seams).
> מקור התכנון: `knowledge/studio-plan/01-config-engine-studio.md` §§5–15 +
> `knowledge/studio-plan/steps/01-steps.md` (one-liners 16–30).
>
> **⚠️ קנוני (R1+R2 merged):** מקור-הסמכות = `studio-plan/RED-TEAM-R1.md` + `RED-TEAM-R2.md` + מקטע 🔧-R1 בגוף-העמוד. הלוטים-למטה כבר משקפים: draft-API=`applyOps`+undo/redo (inspector/find-replace/theme עוברים דרכו; `editDraft`=primitive פנימי) · persona=`roleKeyOf`/`persona[roleKey]` · `kImmutable` (לא `critical`) · "answer-equivalent" (לא byte-identical) · ציר-תוכן=~532 ליטרלים · publish דורש confirm-scope + view-as-persona.
>
> **אינוריאנטים מוטמעים בכל שלב (אומתו מול הקוד החי):**
> - **גידור `kStudioFlag` default OFF ⇒ answer-equivalent מול fixtures + golden-render / zero-regression** — clone של
>   `lib/data/repositories/backend.dart:12` (`kUseFirebaseBackendFlag = bool.fromEnvironment`),
>   + flag-name runtime `lib/state/feature_flags.dart:23` (`kKbLiveMirrorFlag`), `_forcedOnFlags` ריק `:37`. ("byte-identical" הוסר — R1-B4.)
> - **ממשל #84** — owner+manager בלבד: `isOwnerEmail` `lib/data/board_accounts_local.dart:102`
>   (`kOwnerEmails={'meir7651231@gmail.com'}` `:98`) ∧ `roleKeyOf(ref.read(roleProvider))=='manager'`
>   (persona מקור-אמת-יחיד = `roleProvider` (String?, null=קבלן), `roleKeyOf(null)=='contractor'` קנוני; `BoardRole` `lib/state/board_auth.dart:297` enum `:24` ממופה ב-adapter בלבד — R1-A2). (`knowledge/MANAGER-BUILD-PLAN.md:14`,§1.)
> - **Hebrew/RTL/a11y** — RTL root `lib/main.dart:373`, מסכי-role מאשררים מחדש
>   (`manager_dashboard_screen.dart:79`); אסור LTR-subtree (gate 65), מספרים ב-LTR-isolate (gate 95),
>   `Semantics` pass-through (gates 62/63), emoji מלגאסי בלבד (gate 64).
> - **server-ready** — כל כתיבה עוברת ב-`applyOps(List<ConfigOp>)`/`publish` (seam `ConfigSink`), לא ל-store ישיר; `editDraft` הוא primitive פנימי בלבד (R1-A3).
> - **hand-rolled JSON** (gate 60 — אין dep חדש), millis-id (אין `uuid`).
> - **100-gate per step:** `flutter analyze` 0 (31) + `flutter test` 0-regress (32) +
>   `knowledge_protocol_test` (94) + `flutter build web` (33); שלב-UI מוסיף `visual_log` (116/107).
> - **R9 inline** — כל עריכה = שדה inline, אין modal/prompt (`MANAGER-BUILD-PLAN.md:99`).
> - GATE_REGISTRY "הבא הפנוי" = **118** (`knowledge/GATE_REGISTRY.md:11`); פרוטוקול הוספה `:133`.
>
> כל הקבצים תחת `lib/screens/studio/`, `lib/state/studio/`, `lib/widgets/studio/` הם **NEW** —
> אומת ב-`git`: התיקיות עדיין לא קיימות (שלבים 1–30 כולם בתכנון). Anchors מאומתים file:line מול הקוד החי.

---

## Phase C — Studio shell + draft/publish/versioning (16–22)

### שלב 16 — `studio_screen` scaffold + segmented panes (IndexedStack) + RTL/white-AppBar chrome

1. 🎯 **היעד** — מסך-בסיס לסטודיו: `Directionality.rtl` + scaffold בהיר + AppBar לבן + מתג segmented
   ל-4 panes דרך `IndexedStack`. route עצמאי (`StudioScreen.route()`), בלתי-נגיש עד שלב 20 מחווט כניסה ⇒ אפס-התנהגות.
2. 🔧 **איך בונים** — NEW `lib/screens/studio/studio_screen.dart`. clone chrome מ-
   `manager_dashboard_screen.dart:79-90` (`textDirection: rtl` → `Scaffold(backgroundColor: BsTokens.bgLight)` →
   `AppBar(backgroundColor: BsTokens.cardLight, elevation:0, automaticallyImplyLeading:false)`).
   `IndexedStack` כמו `manager_dashboard_screen.dart` / `home_shell.dart` (precedent קיים בריפו).
3. ⚠️ **תקלות צפויות** — IndexedStack בונה את כל 4 ה-panes מראש ⇒ panes כבדים (tree/theme) מתחילים מיד;
   `route()` שמחזיר MaterialPageRoute עלול לאבד את ה-`ProviderScope` אם ייפתח מחוץ לעץ הקיים.
4. ✅ **פתרון** — panes פנימיים lazy: כל pane גוף-`Consumer` שקורא provider רק כשהוא ה-`index` הפעיל
   (או `KeyedSubtree` + `Visibility(maintainState:true)` רק לפעיל); route נשאר בתוך עץ ה-`MaterialApp`
   הקיים (נפתח מתוך manager-tab בשלב 20), כך ה-scope משותף.
5. 🧪 **בדיקות** (שמות-קבצים) — NEW `test/studio/studio_screen_test.dart`: widget בונה ב-RTL,
   `find.byType(Directionality)` עם `rtl`, אין `TextDirection.ltr` subtree (gate 65), AppBar לבן.
6. 📈 **שיפור** — segmented עם `Semantics(selected:)` לכל טאב; tooltip עברית; שמירת ה-pane הפעיל
   ב-`StateProvider` כך שחזרה לסטודיו פותחת באותו pane.
7. 🤔 **ריאלי?** — כן. scaffold+IndexedStack הוא דפוס מוכח בריפו (manager_dashboard, home_shell);
   אין תלות חיצונית, רק העתקת chrome קיים.
8. 🔒 **ווידוא-פיקס** (DoD) — `flutter analyze` 0 (31) · suite 0-regress (32) · `build web` (33) ·
   `studio_screen_test` ירוק · אין רגרסיה (route בלתי-נגיש) · `visual_log` ריק (אין כניסה עדיין).
9. 💡 **תוספת-א** — `kStudioFlag`-guard ב-`route()` עצמו: אם OFF מחזיר `null`/no-op כך שאפילו deep-link לא נכנס.
10. 💡 **תוספת-ב** — keyboard-shortcut (`Cmd/Ctrl+1..4`) למעבר-pane דרך `Shortcuts`/`Actions` — נוחות-בעלים בלבד.

### שלב 17 — top-bar (טיוטה badge · "פרסם לכולם" · "בטל טיוטה" · edit-mode toggle) + R9 inline publish-note

1. 🎯 **היעד** — שורת-על: badge "טיוטה" (ספירת nodes ששונו) · כפתור-ראשי **"פרסם לכולם"** ·
   **"בטל טיוטה"** · מתג "מצב עריכה"; פרסום פותח שדה-הערה inline ("מה שינית?") → `publish(note, ownerEmail)`. **לפני שידור-פרסום: confirm-sheet "ישפיע על כל המשתמשים — N שינויים" + תצוגת "צפה כפי שקבלן/שליח יראה" (view-as-persona) — R2-#13 (discard כבר מאשר, publish חייב גם).**
2. 🔧 **איך בונים** — top-bar ב-`studio_screen.dart`; NEW `lib/screens/studio/panes/` (תיקיית-panes).
   ה-note הוא `TextField` inline ולא modal — R9 `MANAGER-BUILD-PLAN.md:99`. **כל מוטציית-טיוטה עוברת `applyOps` (`config_store.dart` משלב 6); הפרסום עצמו דרך `publish`** ⇒ משתמשים לא מושפעים לפני פרסום. **ה-confirm-scope + view-as-persona** מוצגים inline לפני `publish` (re-resolve הטיוטה תחת `roleKey` נבחר).
3. ⚠️ **תקלות צפויות** — ספירת-טיוטה יקרה אם סורקים את כל ה-doc בכל rebuild; "פרסם" אקטיבי על טיוטה ריקה
   ⇒ גרסה ריקה; double-tap על פרסום ⇒ שתי גרסאות; **publish-לכולם בלי אישור-היקף (R2-#13).**
4. ✅ **פתרון** — `draftChangedCount` כ-`select` מ-`configStoreProvider` (recompute רק כשהטיוטה משתנה);
   "פרסם" `onPressed:null` כש-count==0; debounce/`_publishing` guard סביב `publish`; **confirm-sheet עם N-השינויים + view-as-persona חובה לפני broadcast.**
5. 🧪 **בדיקות** — `test/studio/studio_screen_test.dart`: publish disabled על draft ריק, **publish פותח confirm-sheet עם N-השינויים + view-as-persona לפני שמבצע** (R2-#13);
   `config_store_test.dart`: כל-מוטציה דרך `applyOps`; `publish` עם note כותב `ConfigVersion(note,byEmail)` ומנקה טיוטה.
6. 📈 **שיפור** — badge עם count חי + צבע-brand; "בטל טיוטה" מאשר inline ("לבטל N שינויים?") לפני discard.
7. 🤔 **ריאלי?** — כן. top-bar הוא Row של widgets קיימים; הקושי היחיד הוא O(1) של ה-count, שנפתר ב-`select`; ה-confirm/view-as-persona משתמשים ב-preview-mechanism הקיים.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · publish-empty-disabled + confirm-scope test ירוק · note→version מאומת ·
   `visual_log` (top-bar הוא UI) · אין כתיבה ל-published מחוץ ל-`publish`; מוטציות דרך `applyOps` בלבד.
9. 💡 **תוספת-א** — `ownerEmail` נשאב מ-`board_auth` session (לא literal) כדי שה-`byEmail` יהיה audit אמיתי (#84).
10. 💡 **תוספת-ב** — note אופציונלי-עם-ברירת-מחדל ("עריכה ידנית — {timestamp}") אם הבעלים השאיר ריק, כך ל-history תמיד יש תווית.

### שלב 18 — Pane A tree (registry-driven, virtualised `screen→area→element`) + persona selector + search

1. 🎯 **היעד** — עץ נגלל `screen→area→element` מ-`elementRegistryProvider`, וירטואלי (10K+ ids);
   בורר-persona בראש (`persona[roleKey][id]` כאשר `roleKey=roleKeyOf(role)`, קבלן=`'contractor'`); תיבת-חיפוש שמסננת לפי `labelHe`/id/טקסט-נוכחי. קריאה-בלבד; בחירה פותחת inspector.
2. 🔧 **איך בונים** — NEW `lib/screens/studio/panes/tree_pane.dart`. lazy `ExpansionTile` +
   `ListView.builder` (§11 — וירטואליזציה). הקבצה לפי `screen`/`area` כאינדקס. persona-selector
   כותב ל-`editModeProvider`/scope state; search מסנן לפני build.
3. ⚠️ **תקלות צפויות** — `ExpansionTile` בונה את כל הילדים בהרחבה ⇒ קריסה ב-100K; חיפוש שמריץ filter על כל
   rebuild ⇒ jank; persona-selector שמשנה scope אך ה-tree לא מתרענן.
4. ✅ **פתרון** — `ListView.builder` עם flattened-visible-rows (לא nested-Column); חיפוש מ-debounce
   + memoized index לפי `screen`; persona ב-`StateProvider` שה-tree `watch`-ים.
5. 🧪 **בדיקות** — NEW `test/studio/tree_pane_test.dart`: tree נבנה מ-`elementRegistryProvider` (mock registry),
   חיפוש מסנן שורות, החלפת persona מעדכנת scope.
6. 📈 **שיפור** — badges-per-leaf (מוסתר/ערוך/per-persona) ישירות בעץ; "הצג רק ששונו" toggle (diff-only view).
7. 🤔 **ריאלי?** — כן (חלקי-זהירות): הוירטואליזציה קריטית — אם נכתב nested-ExpansionTile נאיבי לא יחזיק 10K.
   הפתרון (flattened builder) ידוע אך דורש משמעת.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · `tree_pane_test` ירוק · perf-check 10K rows (גם בשלב 30) ·
   `visual_log` · אין כתיבה (קריאה-בלבד) ⇒ אין רגרסיה.
9. 💡 **תוספת-א** — sticky-headers ל-`screen` בזמן גלילה (`SliverPersistentHeader`) לאוריינטציה בעץ ארוך.
10. 💡 **תוספת-ב** — keyboard-nav (חיצים) + הדגשת ה-id שנבחר; deep-link `studio?select=<id>` שפותח את הענף.

### שלב 19 — Pane B inspector (R9 inline per-axis: תוכן/נראות/עיצוב/התנהגות + live preview + "אפס אלמנט" + diff)

1. 🎯 **היעד** — inspector לאלמנט-נבחר, רק ה-axes ב-`descriptor.editableProps`/`allowedActions`/`allowedValues`: תוכן (TextField+emoji) · נראות
   (switch, נעול אם `kImmutable`) · עיצוב (token-pickers + live-preview) · התנהגות (Pillar-4 mount, v1 read-only);
   footer "אפס אלמנט" + שורת-diff מול ברירת-מחדל. עריכות נכנסות לטיוטה ⇒ לא-חי.
2. 🔧 **איך בונים** — NEW `lib/screens/studio/panes/inspector_pane.dart`. axes מגודרים ב-6-שדות-ה-descriptor (`editableProps`/`allowedActions`/`allowedValues`);
   נראות-switch `disabled`+lock אם `kImmutable`; **כל עריכה נפלטת כ-`ConfigOp` דרך `applyOps` (undoable); "אפס אלמנט" דרך `resetDraftNode` — ה-inspector לא קורא ל-primitive `editDraft` ישירות (R1-A3 · R2-#18 undo-first-class)**. R9 — הכל inline.
   token-pickers מ-`BsTokens` (`lib/theme/tokens.dart`), fontScale-slider clamp 0.8–1.6.
3. ⚠️ **תקלות צפויות** — TextField שמאבד-פוקוס בכל rebuild (live-preview מריץ setState); switch ל-`kImmutable`
   שכן-ניתן-להזיז אם רק ה-UI חוסם; emoji לא-מלגאסי נכנס דרך-input (gate 64).
4. ✅ **פתרון** — `TextEditingController` יציב **ומנותק מה-doc עד commit** (re-resolve לא רץ פר-תו — R2-#2), עדכון טיוטה ב-`onChanged` debounced דרך `applyOps`;
   חסימת-`kImmutable` גם במודל (`mergeNode` מתעלם מ-`hidden` ל-immutable, שלב 26) — defence-in-depth;
   ולידציית-emoji (whitelist לגאסי) write-time.
5. 🧪 **בדיקות** — NEW `test/studio/inspector_pane_test.dart`: id `kImmutable` ⇒ switch-נראות disabled;
   עריכת-תוכן ⇒ **`applyOps` נקרא (ניתן-undo)**; "אפס אלמנט" ⇒ `resetDraftNode`.
6. 📈 **שיפור** — live-preview של האלמנט-עצמו בתוך ה-pane (mini-render); diff-line צבעוני (ירוק=הוסף/אדום=הוסר).
7. 🤔 **ריאלי?** — כן (חלקי): per-axis-gating + R9-inline + live-preview הוא היקף-בינוני; ה-focus-stability
   הוא המלכוד הקלאסי, נפתר עם controller-יציב.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · `inspector_pane_test` ירוק (kImmutable-disabled + applyOps-undoable) ·
   `visual_log` · gate 64 (emoji) + 65 (RTL ב-TextField) · עריכות→טיוטה-בלבד מאומת.
9. 💡 **תוספת-א** — "מצב נוכחי מול ברירת-מחדל" כ-side-by-side (fallback↔resolved) להבנה מיידית מה שונה.
10. 💡 **תוספת-ב** — undo-per-field נופל ישירות ממחסנית-ה-undo של `applyOps` (R1-A3) — חץ-קטן ליד כל axis מבצע `undo()` של ה-op האחרון לאותו ציר בלי לאפס הכל.

### שלב 20 — חיווט manager-tab "🎨 סטודיו" accordion row + edit-mode toggle מאחורי `kStudioFlag`

1. 🎯 **היעד** — שורת-אקורדיון "🎨 סטודיו" ב-`_ManageTab` → `StudioScreen.route()` + מתג מצב-עריכה,
   מגודר `kStudioFlag`. OFF ⇒ השורה נעדרת ⇒ answer-equivalent (§10).
2. 🔧 **איך בונים** — `manager_dashboard_screen.dart` באזור שורות ה-`_ManageSection` (~`:2405-2500`,
   שם יושבות קטגוריות/הגדרות/עץ/מותגים/שיוך-תפקידים). `if (kStudioFlag) _ManageSection(sectionKey:'studio',
   emoji:'🎨', title:'סטודיו', sub:'עריכת No-Code של המסכים', open:..., onTap:..., child:_StudioBody(...))`
   — חיקוי מדויק של דפוס-השורות הקיים (`_ManageSection` חתימה `:2536`).
3. ⚠️ **תקלות צפויות** — `if (kStudioFlag)` בתוך `children:[...]` של list spread דורש `...[if(...)x]`;
   שכחת ה-guard ⇒ השורה דולפת ב-OFF ⇒ רגרסיה; route שנפתח מחוץ ל-scope (ראה שלב 16).
4. ✅ **פתרון** — `...[if (kStudioFlag) _ManageSection(...)]` בתוך ה-children; הבדיקה מאמתת היעדר ב-OFF;
   `route()` נפתח דרך `Navigator.of(context)` מתוך העץ הקיים (scope משותף, כמו `RegressionPanelScreen.route()` `:2475`).
5. 🧪 **בדיקות** — `test/studio/zero_regression_test.dart` (קיים משלב 15): שורת-סטודיו נעדרת כש-`kStudioFlag` OFF;
   (אופציונלי) ON ⇒ קיימת ופותחת route.
6. 📈 **שיפור** — runtime-gate נוסף: גם `featureFlagsProvider.isOn(kStudioFlagName)` כך שהבעלים מדליק staging בלי rebuild.
7. 🤔 **ריאלי?** — כן. שורה-אחת בתבנית-קיימת; ה-`_ManageSection` כבר עוטף `Semantics`+`HelpTarget` (`:2570`), אז a11y מובנה.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · `zero_regression_test` (row-hidden-OFF) ירוק · `visual_log`
   (כניסה חדשה — אבל ריקה ב-OFF) · gates 38/39 (אין מחיקה) · OFF=identical.
9. 💡 **תוספת-א** — gating כפול: compile (`kStudioFlag`) ∧ owner (`isOwnerEmail`+manager) — אפילו ב-ON השורה רק לבעלים.
10. 💡 **תוספת-ב** — badge ספירת-טיוטה על השורה עצמה (כמו `_ManageSection.badge` `:2560`) כדי שהבעלים יראה "יש טיוטה לא-מפורסמת" מבלי להיכנס.

### שלב 21 — Pane D version-history list + on-the-fly diff + one-tap "שחזר" rollback + "תצוגה מקדימה"

1. 🎯 **היעד** — רשימת-גרסאות (`history[]`: note·time·byEmail), כל שורה → "תצוגה מקדימה" (snapshot)
   + **"שחזר"** `rollback(id)` בלחיצה; ה-rollback עצמו מגורס (forward-only, undoable §7). diff מחושב on-the-fly.
2. 🔧 **איך בונים** — NEW `lib/screens/studio/panes/history_pane.dart`. קורא `ConfigDoc.history`
   (`config_doc.dart` משלב 2 — `ConfigVersion{id,snapshot,publishedAtMs,note,byEmail}`); "שחזר" קורא
   `rollback(id)` (`config_store.dart` משלב 6). diff = newVsOld computed (לא נשמר).
3. ⚠️ **תקלות צפויות** — rollback הרסני (דורס בלי לגרס) ⇒ אובדן-היסטוריה; diff על snapshots-גדולים בכל-render ⇒ jank;
   ring-buffer N=30 שמאבד גרסאות ישנות בלי-אזהרה.
4. ✅ **פתרון** — `rollback` יוצר `ConfigVersion` חדש מה-snapshot (forward-only, ניתן-לביטול);
   diff מ-memoize per-row (מחושב רק כשנפתח); buffer-cap מתועד ב-UI ("נשמרות 30 גרסאות אחרונות").
5. 🧪 **בדיקות** — `test/studio/config_store_test.dart`: `rollback` משחזר snapshot כגרסה-חדשה
   (history גָדֵל ב-1, published==snapshot); fresh-doc ⇒ history ריק.
6. 📈 **שיפור** — diff-view מקובץ (added/changed/removed ids עם `labelHe`); "תצוגה מקדימה" משתמשת ב-`previewDraft`-mechanism כדי להראות חי ללא פרסום.
7. 🤔 **ריאלי?** — כן. `history[]` הוא list של snapshots-sparse (N≤30, §11) ⇒ זול; rollback הוא publish-של-snapshot, מנגנון פשוט.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · `config_store_test` (rollback-versioned) ירוק ·
   `visual_log` · rollback==owner-action-explicit · history ריק ב-fresh-doc מאומת.
9. 💡 **תוספת-א** — אישור-inline לפני "שחזר" ("לשחזר לגרסה מ-{time} מאת {email}?") — מניעת-rollback-בטעות.
10. 💡 **תוספת-ב** — "השווה שתי גרסאות" (בחר A+B) — diff בין שתי נקודות-זמן, לא רק מול-נוכחי.

### שלב 22 — Finalize `config_store_test` (publish/rollback) + `registry_contract_test` + **claim gate 118**

1. 🎯 **היעד** — לסגור את `config_store_test` (מעברי publish/rollback) + `registry_contract_test`
   (כל `CfgText('…')`/`CfgVisible('…')` literal-id ב-`lib/` ⊆ `kElementRegistry`, ids ייחודיים, warn-unwired)
   ולתבוע **gate 118**.
2. 🔧 **איך בונים** — NEW `test/studio/registry_contract_test.dart` (source-scan כמו
   `test/knowledge_protocol_test.dart`); `knowledge/GATE_REGISTRY.md:11` "הבא הפנוי"→**119**; הוספת שורה
   "# שער 118" ל-`.githooks/pre-commit` (פרוטוקול `GATE_REGISTRY.md:133`). scan-only, אין התנהגות.
3. ⚠️ **תקלות צפויות** — regex ש-false-positive על `CfgText(` בתוך מחרוזת/הערה; ה-hook לא מותקן ב-`.git/hooks`
   (התיקייה `.githooks/` ריקה — מסונכרן פר-clone, gate 15 `hooksPath=.githooks`); claim-של-118 בלי לעדכן "הבא הפנוי".
4. ✅ **פתרון** — regex ממוקד (`Cfg(Text|Visible)\(\s*'([^']+)'`) שמתעלם מהערות; הוספת `# שער 118` ל-hook
   + עדכון "הבא הפנוי"→119 + commit משותף (hook+GATE_REGISTRY ביחד, צעד 6 בפרוטוקול); `cp .githooks/pre-commit .git/hooks/`.
5. 🧪 **בדיקות** — `test/studio/registry_contract_test.dart` (gate 118 בסוויטה) + `test/studio/config_store_test.dart`
   (publish/rollback transitions); שניהם בירוק.
6. 📈 **שיפור** — ה-test מדפיס דוח "ids בלי wrapper" (warn) + "wrappers בלי registry-row" (error) — מצפן-adoption.
7. 🤔 **ריאלי?** — כן. source-scan-test הוא בית-הסגנון (`knowledge_protocol_test` כבר עושה זאת); claim-gate הוא נוהל מתועד.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite (118 ירוק) · hook מריץ-118 · "הבא הפנוי"=119 · אין התנהגות (scan-only) ·
   commit כולל hook+registry יחד.
9. 💡 **תוספת-א** — ה-118 בודק גם שכל `kImmutable:true` ב-registry מופיע ב-critical-set של ה-publish-validator (שלב 26) — עקביות-בטיחות. (gate נשאר **#118** ל-P1 — R2-#11.)
10. 💡 **תוספת-ב** — ה-test כותב artifact `studio_id_coverage.txt` (% adoption) ש-CI יכול לעקוב-מגמה לאורך-זמן.

---

## Phase D — theme editor + find&replace + safety (23–28)

### שלב 23 — `CfgTheme` override + `AppTheme` `ThemeExtension` glue

1. 🎯 **היעד** — אובייקט-override `CfgTheme` (brand/surface/ink/radius/fontScale) + חיבורו כ-`ThemeExtension`
   ל-`AppTheme`. אין override ⇒ defaults שומרים מראה-נוכחי ⇒ answer-equivalent + golden-render (אותו פיקסל).
2. 🔧 **איך בונים** — NEW `lib/state/studio/config_theme.dart` + הוספה ל-`extensions:` של
   `lib/theme/app_theme.dart:62` (היום: `BsSemanticColors`). מנגנון זהה ל-`BsSemanticColors` (`:77-116`),
   נקרא דרך accessor כמו `bsOnAccent(context)` `:109` / `bsSuccess(context)` `:114`.
3. ⚠️ **תקלות צפויות** — `ThemeExtension` חייב `copyWith`+`lerp` (אחרת קומפילציה/אנימציה נשברות, כמו
   `BsSemanticColors.lerp` `:98`); ערך-default שונה מהקיים ⇒ מראה-משתנה ב-OFF (רגרסיה).
4. ✅ **פתרון** — מימוש `copyWith`+`lerp` מדויק; defaults של `CfgTheme` = ערכי-`BsTokens` הנוכחיים בדיוק
   (`brand=#FF7A18` `tokens.dart:57`), כך `extension<CfgTheme>()?.x ?? BsTokens.x` מחזיר זהה.
5. 🧪 **בדיקות** — NEW `test/studio/config_theme_test.dart`: ערכי-default לא-משתנים (theme==קודם);
   `test/a11y_contrast_theme_test.dart` (קיים) ממשיך לעבור.
6. 📈 **שיפור** — accessors ייעודיים (`cfgBrand(context)`, `cfgRadius(context)`) שמרכזים את ה-fallback-ל-`BsTokens`.
7. 🤔 **ריאלי?** — כן. תקדים מדויק קיים (`BsSemanticColors` נקרא ב-`app_theme.dart:62`); זו הרחבת-רשימה אדיטיבית.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · `config_theme_test` (defaults-unchanged) ירוק ·
   `a11y_contrast_theme_test` עובר · אין override ⇒ identical (gate-zero-reg).
9. 💡 **תוספת-א** — `CfgTheme` נכלל ב-`ConfigLayer` (global בלבד, לא per-persona) — theme גלובלי-אחד, מונע פיצול-מותג.
10. 💡 **תוספת-ב** — high-contrast-aware: `CfgTheme` מכבד את `highContrast` הקיים (`app_theme.dart:56`) כך ש-a11y-mode גובר על override-בעלים.

### שלב 24 — Pane C theme editor (brand/surface/ink/radius/fontScale + live whole-app preview + AA-contrast block)

1. 🎯 **היעד** — עורך-theme: brand/surface/ink/radius/fontScale, preview-חי-של-כל-האפליקציה, חסימת-קומבינציות-AA-נכשלות.
   עריכות→טיוטת-`CfgTheme` **דרך `applyOps` (op-`SetTheme`, undoable)**; לא-מפורסם ⇒ משתמשים לא-מושפעים.
2. 🔧 **איך בונים** — NEW `lib/screens/studio/panes/theme_pane.dart`. שימוש-חוזר בלוגיקת-contrast מאחורי
   `test/a11y_contrast_theme_test.dart` (`:26` — `#1A1A1A on #FF7A18 ≈ 6.7:1`); חסימת-קומבינציה שנכשלת AA. **כתיבת-ה-theme עוברת `applyOps`, לא `editDraft` ישיר (R1-A3).**
3. ⚠️ **תקלות צפויות** — color-picker חופשי שמייצר contrast<4.5 ⇒ UI לא-קריא (נשבר gate-a11y); preview-חי
   שמריץ rebuild-של-כל-העץ בכל-tick ⇒ jank; radius-קיצוני שמעוות layout.
4. ✅ **פתרון** — picker מתוך palette-token מוגבל (`BsTokens` colors, §2.2 — לא hex-חופשי) + ולידציית-AA
   שחוסמת inline ("ניגודיות נמוכה — נחסם"); preview ב-`ProviderScope`-מקומי שמזריק את הטיוטה בלבד; radius clamp.
5. 🧪 **בדיקות** — NEW `test/studio/theme_pane_test.dart`: קומבינציה-נכשלת-AA נדחית; `a11y_contrast_theme_test` עדיין עובר.
6. 📈 **שיפור** — "תצוגה מקדימה על מסך-אמיתי" (פתח home/manager בתוך הסטודיו עם הטיוטה) במקום mock-block.
7. 🤔 **ריאלי?** — כן (חלקי): live-whole-app-preview הוא ה-effort האמיתי — דורש הזרקת-theme לעץ-תצוגה ממוקד.
   הפתרון (ProviderScope-מקומי) מוכר אך לא-טריוויאלי.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · `theme_pane_test` (AA-reject) ירוק · `a11y_contrast_theme_test` עובר ·
   `visual_log` · עריכות→טיוטה-בלבד.
9. 💡 **תוספת-א** — תצוגת-יחס-ניגודיות מספרית ליד כל בחירה (LTR-isolate למספר, gate 95) — שקיפות לבעלים.
10. 💡 **תוספת-ב** — presets ("כהה-עדין"/"high-contrast"/"מותג-חזק") כנקודות-פתיחה בטוחות שכולן עוברות-AA מראש.

### שלב 25 — Pane D global find-&-replace על תוכן → draft (preview כל hit לפי `labelHe`)

1. 🎯 **היעד** — חיפוש-מחרוזת-עברית על כל הטקסטים-המ-resolved (registry-driven), preview כל-hit עם `labelHe`,
   החלפה ל-**טיוטה** (לעולם לא ישר-ל-published) — סקירה ואז פרסום. OFF=identical; כלום לא-מגיע למשתמשים עד "פרסם".
2. 🔧 **איך בונים** — NEW `lib/screens/studio/panes/find_replace_pane.dart` (או ב-`theme_pane.dart` כ-D-משותף).
   substring-search registry-driven (כל id ב-`elementRegistryProvider` עם טקסט-נוכחי); **replace = `applyOps(List<ConfigOp>)` batch יחיד (כל ה-hits ב-op-list אחד = יחידת-undo אחת), לא N קריאות-`editDraft` (R1-A3 · R2-#18)**.
3. ⚠️ **תקלות צפויות** — replace-all שכותב ל-published (עוקף את הטיוטה); hit על-`kImmutable`/auth-text ⇒ שינוי-מסוכן;
   substring-עברי עם ניקוד/RTL שמפספס-התאמות; replace-עיוור בלי-preview ⇒ נזק-המוני.
4. ✅ **פתרון** — replace תמיד ל-DRAFT (`applyOps`, מעולם לא `publish`); preview-חובה לפני-החלה (כל-hit מסומן,
   בחירה סלקטיבית); השוואת-normalize (trim) להתאמת-עברית; `kImmutable`-ids מסומנים-לקריאה-בלבד גם כאן.
5. 🧪 **בדיקות** — NEW `test/studio/find_replace_test.dart`: preview מציג-hits; replace פוגע-בטיוטה-בלבד (published ללא-שינוי).
6. 📈 **שיפור** — regex אופציונלי (toggle) + "החלף-בנבחרים-בלבד" (checkbox per-hit) למניעת-over-replace.
7. 🤔 **ריאלי?** — כן. זה לולאה שבונה `List<ConfigOp>` ואז `applyOps`-יחיד; ה-effort בעיקר ב-preview-UI הסלקטיבי.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · `find_replace_test` (replace→draft-only, batch=undo-יחיד) ירוק · `visual_log` ·
   published-לא-נגעע מאומת · `kImmutable` מוגן.
9. 💡 **תוספת-א** — "כמה-hits" + אזהרה אם >N (למשל >50) "החלפה רחבה — לסקור היטב" לפני-החלה.
10. 💡 **תוספת-ב** — undo-של-find-replace כיחידה-אחת = `undo()` בודד על ה-batch (R1-A3) — נופל ישירות ממחסנית-ה-undo, ללא snapshot-ידני.

### שלב 26 — critical-id set + publish-time validator (`mergeNode` מתעלם מ-critical `hidden`)

1. 🎯 **היעד** — set-קריטי (bottom-nav/auth/manager-entry/edit-exit/logout) + validator-בזמן-פרסום שדוחה doc
   שמסתיר id-קריטי. defence-in-depth: model+inspector+publish ⇒ הבעלים לא-יכול-לשבור-ניווט (§8.1).
2. 🔧 **איך בונים** — `lib/state/studio/element_registry.dart` (**`kImmutable:true` seeds — זהו ה-flag הקנוני ל-critical-id ב-חוזה-6-השדות, R1-A1**) + `lib/state/studio/config_store.dart`
   publish-validator (reject אם doc מסתיר id `kImmutable`). `mergeNode` (`config_merge.dart` משלב 3) כבר מתעלם מ-`hidden` ל-`kImmutable`.
3. ⚠️ **תקלות צפויות** — critical-set חלקי (שכחת logout/auth) ⇒ פרצה; validator שרץ רק-ב-UI ולא-במודל ⇒
   עקיפה דרך find-replace/AI/Pillar-2; auth-ids לא-מתועדים (איפה ה-login?).
4. ✅ **פתרון** — שלוש-שכבות: model (`mergeNode` מתעלם), inspector (switch-disabled+lock, שלב 19), publish-validator
   (דוחה+הודעה inline); critical seed מ-`welcome_screen.dart` (auth), bottom-nav, manager-entry, edit-exit, logout.
5. 🧪 **בדיקות** — NEW `test/studio/safety_test.dart`: publish-validator דוחה הסתרת-critical; `config_merge_test`
   מאמת ש-`hidden` ל-critical-id מתעלם.
6. 📈 **שיפור** — `critical` מסומן-ויזואלית בעץ (אייקון-מנעול) כך שהבעלים מבין-מראש מה-נעול.
7. 🤔 **ריאלי?** — כן. זה bool-flag + בדיקת-set ב-publish; ה-effort האמיתי הוא זיהוי-מלא של ה-critical-ids (סקירה-ידנית).
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · `safety_test` (critical-can't-hide) ירוק · `config_merge_test` (ignore-hidden) ·
   gate 94 · אין wiring-production (model+validator בלבד).
9. 💡 **תוספת-א** — gate-118 (שלב 22) בודק ש-`kImmutable:true` ב-registry == critical-set ב-validator (אין-drift בין השניים).
10. 💡 **תוספת-ב** — `kImmutable` גם חוסם-`action`-override (לא רק `hidden`) ל-id-ניווטי, כך שאי-אפשר re-route את ה-logout.

### שלב 27 — 3-scope reset + behavior-whitelist (`noop` + guarded `navigate`) + write-time validation

1. 🎯 **היעד** — reset ב-3-scopes + whitelist-התנהגות (`noop` + `navigate` ל-routes-רשומים-בלבד) + ולידציית-כתיבה
   (length-caps, emoji-מלגאסי gate 64, no-LTR gate 65). תמיד-ניתן-לשחזר-לבסיס-קוד; kind-לא-מוכר ⇒ handler-מקורי ⇒ אפס-שינוי.
2. 🔧 **איך בונים** — `lib/state/studio/config_store.dart` ("אפס הכל"=publish-layer-ריק) + NEW
   `cfgActionRegistryProvider` (אין URLs-חיצוניים gate 51 `GATE_REGISTRY.md:56`; Pillar-4 ממלא kinds).
   3-scopes: per-element (`resetDraftNode`), discard (`discardDraft`), all (`publish` empty-layer).
3. ⚠️ **תקלות צפויות** — `navigate` ל-route-שרירותי/חיצוני (gate 51); "אפס הכל" שמוחק-היסטוריה ⇒ לא-recoverable;
   ולידציה שמפספסת LTR-injection (gate 65) או emoji-זר (gate 64).
4. ✅ **פתרון** — `navigate` רק ל-whitelist-של-routes-קיימים-רשומים; "אפס הכל" = publish-של-layer-ריק (גרסה-חדשה ⇒ undoable, לא-הרסני);
   write-validator: length-cap + emoji-whitelist-לגאסי + refuse-LTR-chars; kind-לא-מוכר → original `onTap`.
5. 🧪 **בדיקות** — `test/studio/safety_test.dart`: reset-recoverable (אפס-הכל ⇒ ניתן-rollback); unknown-action ⇒ fallback;
   LTR-injection refused; length-cap enforced.
6. 📈 **שיפור** — "אפס הכל" עם אישור-כפול-inline ("לאפס את כל ההגדרות? ניתן-לשחזר מההיסטוריה"); preview-לפני-reset.
7. 🤔 **ריאלי?** — כן. reset=publish-empty-layer (מנגנון-קיים); whitelist-`navigate` הוא map-קטן; ולידציה הם guards-מחרוזת.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · `safety_test` (reset-recoverable + unknown-fallback + LTR-refused) ירוק ·
   gates 51/64/65 · always-recoverable-to-baseline מאומת.
9. 💡 **תוספת-א** — `noop` כ-default-בטוח לכל `CfgAction` ללא-handler (לעולם-לא-crash על kind-ריק).
10. 💡 **תוספת-ב** — write-validator מחזיר שגיאת-R9-inline ספציפית ("טקסט ארוך מדי — מקס 80 תווים") במקום rejection-עיוור.

### שלב 28 — safety test-suite (critical-can't-hide · tokens bounded · theme passes contrast · reset/validation)

1. 🎯 **היעד** — סוויטת-בטיחות מלאה: critical-לא-ניתן-להסתרה · style-tokens מוגבלים-ל-`BsTokens` · theme-עובר-contrast · reset/validation.
   מגן על `a11y_contrast_theme_test`; אין-wiring-production.
2. 🔧 **איך בונים** — NEW `test/studio/safety_test.dart` (מאחד את הבדיקות משלבים 26–27): style-tokens resolve-תוך-`BsTokens`
   (`lib/theme/tokens.dart`); LTR-injection refused; critical-can't-hide; reset-recoverable.
3. ⚠️ **תקלות צפויות** — בדיקה-שטחית שלא-מכסה את כל ה-vectors (find-replace/AI/Pillar-2); token-validation שמקבל
   token-לא-קיים בשקט; suite-איטית שמאטה pre-commit.
4. ✅ **פתרון** — table-tests מקיפים (כל-vector: inspector/find-replace/publish-validator); token-resolution שזורק/דוחה
   על token-לא-מוכר (לא-fallback-שקט); unit-only (מהיר, ללא-widget-pump-כבד).
5. 🧪 **בדיקות** — `test/studio/safety_test.dart` (ה-suite עצמו) + gate 94 (`knowledge_protocol_test`) ירוקים.
6. 📈 **שיפור** — property-test (fuzz) על tokens: כל-token-שרירותי או-ב-`BsTokens` או-נדחה — מכסה-מקרי-קצה.
7. 🤔 **ריאלי?** — כן. סוויטת-unit שמאחדת guards-קיימים; ה-effort הוא כיסוי-מקיף, לא-מורכבות-טכנית.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite (safety ירוק) · gate 94 · `a11y_contrast_theme_test` עדיין-עובר ·
   אין-production-wiring (בדיקות-בלבד).
9. 💡 **תוספת-א** — ה-suite מתועד ב-`knowledge_protocol_test` כ-"safety-contract" כך ש-drift-עתידי נתפס אוטומטית.
10. 💡 **תוספת-ב** — golden-test קטן: theme-default ⇒ pixel-זהה-לבסיס (מאשש answer-equivalent + golden-render ויזואלית, gate 116).

---

## Phase E — adoption + seams (29–30)

### שלב 29 — content-adoption על מסכים high-value section-by-section + registry auto-extract codemod-assist

1. 🎯 **היעד** — גלגול-adoption (`Text('ליטרל')`→`CfgText`) על מסכים-בעלי-ערך section-by-section
   (`manager_dashboard_screen.dart`→`home_shell.dart`→`catalog_screen.dart`), כל-section-commit-נפרד (literal=fallback)
   + NEW `tool/studio_extract_ids.dart` (grep `Cfg*('id',…)`, scaffold registry-rows reviewable, ללא-reflection). adoption ויזואלית-אינרטי (OFF=answer-equivalent+golden §9.5). **היקף-הציר = ~532 ליטרלים בלבד (לא 2,361 raw-`Text(`) — R1-B6.**
2. 🔧 **איך בונים** — `manager_dashboard_screen.dart`→`home_shell.dart`→`catalog_screen.dart`: section `Text('ליטרל')`→`CfgText`,
   כל-batch-commit-משלו; NEW `tool/studio_extract_ids.dart` (greps call-sites, מייצר registry-rows לסקירה). מגה-קבצים
   (`catalog_screen.dart` ~330KB, `manager_dashboard_screen.dart` 136KB §0) — בזהירות, section-by-section. **רק `Text('ליטרל')` סטטי; computed/interpolated/`Text.rich` = templating נפרד out-of-v1; עטיפת אחד מ-721 אתרי-`const Text` עולה `const` — לתעד את ה-rebuild פר-batch (R1-B6).**
3. ⚠️ **תקלות צפויות** — big-bang שמפוצץ-visual-gate (116); `CfgText` שמשנה-emoji/RTL ⇒ gate 64/65;
   מגה-קובץ שעריכה-רחבה-בו שוברת-build; id-churn ⇒ orphan-overrides; literal שמוסר ⇒ grep-נשבר (gate 61).
4. ✅ **פתרון** — opt-in-incremental (אף-section-לא-big-bang); literal-נשאר-כ-fallback (grep+gate 61/64 עוברים);
   כל-batch: analyze+suite+build+118+visual_log; ids-append-only-ב-registry; revert=pure-`Cfg→Text` (§9.2).
5. 🧪 **בדיקות** — `test/studio/registry_contract_test.dart` (gate 118): כל-id-מאומץ ⊆ registry; `visual_log` (116) empty-diff per-batch.
6. 📈 **שיפור** — `tool/studio_extract_ids.dart` מציע גם `labelHe` מתוך-הליטרל (טקסט-עברי) ⇒ registry-rows כמעט-מוכנות.
7. 🤔 **ריאלי?** — חלקי. מכנית-וזולה per-section, אבל ~532-ליטרלים (לא 2,361, §0/R1-B6) ⇒ זו עבודה-מתמשכת רב-commits; ה-codemod
   מאיץ אך לא-מבטל את-הסקירה-הידנית.
8. 🔒 **ווידוא-פיקס** — analyze 0 · suite · build web · gate 118 (ids⊆registry) · `visual_log` empty-per-batch ·
   gates 64/65/61 · OFF=answer-equivalent+golden (revert=`Cfg→Text`).
9. 💡 **תוספת-א** — codemod מסמן `kImmutable`-suspect (auth/nav-text) ל-`kImmutable:true`-review במקום-לאמץ-עיוור.
10. 💡 **תוספת-ב** — adoption-dashboard (מ-artifact של-118, שלב 22) מציג %-לכל-מסך כך שהבעלים רואה-התקדמות.

### שלב 30 — Finalize + תיעוד 4 pillar-seams (§13) על interface-קפוא + עדכון-ידע

1. 🎯 **היעד** — לתעד-ולהקפיא את ה-seams (§13: `ConfigSink`/**`applyOps`+undo/redo (לא `editDraft` — הוא primitive פנימי, R1-A3)**/`publish`/`elementRegistryProvider` עם **חוזה-6-השדות שכבר הוקפא ב-step 12.5**/`cfgActionRegistryProvider`)
   ל-Pillars 2–5 + עדכון-ידע. docs-only + seams-קפואים; default-OFF עד-אישור-בעלים (`MANAGER-BUILD-PLAN.md:17`).
2. 🔧 **איך בונים** — עדכון `WIRING.md` (engine+wrappers, gate 24/40 — `GATE_REGISTRY.md:37,48`) +
   `knowledge/GATE_REGISTRY.md` + `knowledge/manager-dashboard-MAP.md`; הקפאת-החתימות של ה-seams לחוזה-יציב (`applyOps`/`publish`/`ConfigSink`/`elementRegistryProvider`-6-fields/`cfgActionRegistryProvider`). **`editDraft` לא נחשף כ-seam.**
3. ⚠️ **תקלות צפויות** — seam שמשתנה אחרי-ש-Pillar-2/4 בנו-עליו ⇒ שבירת-תלות; `WIRING.md` (508KB §) שלא-מסונכרן
   עם `wiring_test`; perf-check 10K-ids שמתגלה-איטי רק-עכשיו (מאוחר).
4. ✅ **פתרון** — חתימות-frozen מתועדות-מפורש ("FROZEN — Pillars 2–5 depend"); `WIRING.md`↔`wiring_test` בסנכרון
   (שינוי-התנהגות=עדכון-שניהם, כלל-הקובץ); perf-check 10K-id virtualised-tree מורץ-כחלק-מ-DoD (לא-נדחה).
5. 🧪 **בדיקות** — full 100-gate + 10K-id virtualised-tree perf-check + `test/knowledge_protocol_test.dart` (94) ירוקים.
6. 📈 **שיפור** — דיאגרמת-seams (§13 table) ב-`WIRING.md` עם file:line לכל-נקודת-תקיעה כך ש-Pillars 2–5 מתחילים-מיידית.
7. 🤔 **ריאלי?** — כן. docs+freeze הוא low-risk; ה-perf-check 10K הוא הבדיקה-המהותית-היחידה (וירטואליזציה משלב 18 כבר-תומכת).
8. 🔒 **ווידוא-פיקס** — analyze 0 · full-suite (94 ירוק) · build web · 10K-perf-check עובר · `WIRING.md`↔test בסנכרון ·
   default-OFF-עד-sign-off · docs-only (אין-קוד-חדש).
9. 💡 **תוספת-א** — "seam-contract-test" קטן שמאמת שחתימות-ה-5-providers לא-השתנו (compile-guard מול-drift עתידי).
10. 💡 **תוספת-ב** — owner-sign-off-checklist ב-`WIRING.md` (gate-status + perf + a11y + audit) כתנאי-מפורש ל-flip-`kStudioFlag`-ON ל-GA.

---

> **סיכום-אינוריאנט (אומת מול קוד חי · R1+R2 merged):** כל 15 השלבים אדיטיביים-בלבד (gates 38/39/40/89/90 — אין-מחיקה);
> `kStudioFlag` OFF ⇒ **answer-equivalent מול fixtures + golden-render** (clone `backend.dart:12`; "byte-identical" הוסר R1-B4); ממשל-#84 owner+manager בלבד
> (`board_accounts_local.dart:102` ∧ **`roleKeyOf(roleProvider)=='manager'`**, persona מקור-יחיד `roleProvider` String?/null=קבלן, `roleKeyOf(null)=='contractor'`); RTL/a11y נשמר (RTL-root `main.dart:373`,
> gates 62/63/65/95); כל-כתיבה דרך seam **`applyOps`+undo/redo** (`editDraft`=primitive פנימי)/`publish`; **`ElementDescriptor`=6-שדות fail-closed, מוקפא ב-step 12.5; publish דורש confirm-scope + view-as-persona; merge field-by-field; per-id `.select`**; **gate-118 נשאר ל-P1** (`GATE_REGISTRY.md:11`→119; 119=P4 · 120=P3);
> hand-rolled JSON (gate 60), millis-id. כל שלב independently-shippable + gate-passing.
