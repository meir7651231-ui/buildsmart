# BuildSmart Studio · Pillar #2 — Domain/Vertical Builder · DEEP detail · steps 31–50

> ⚠️ קנוני = מקטע 🔧 תיקוני Red-Team R1 (בסוף `02-domain-vertical-builder.md`) + RED-TEAM-R1/R2.md. ה-appendix גובר.
> מקור: `knowledge/studio-plan/steps/02-steps.md` + `knowledge/studio-plan/02-domain-vertical-builder.md`. ענף `claude/whats-happening-LyY9G`.
> סטאק: Flutter 3.29 / Dart 3.7 / Riverpod · RTL/עברית/a11y · gated default-OFF · zero-regression · 100-gate · governance #84.
> **Keystone = step 38**: ה-seed המיוצר של האינסטלציה **answer-equivalent** לקבועים החיים מול ה-fixtures הקיימים (`kCatalogProducts` הוא `final` — אין בייטים; תאימות = לוגיקת-galvanic, לא זוגות-דאטה). שום שלב trade-agnostic לא נוחת עד שהוא ירוק.
> **פיזיקת-אינסטלציה קשיחה לנצח:** ענף-`'plumbing'` ב-`install_engine.dart` (`_galvanicallyDissimilar:117` + compliance מותנה: anti-scald/TMTV, Legionella, dielectric-union, recirc isolation≥3) **לעולם** אינו מוכלל למטריצה — אינסטלציה פשוט לא עוברת ב-resolver. רק תחומים-חדשים = `CompatibilityRule`/`CompletionRule` authored.
> **מסגרת קבועה לכל שלב** (מעבר ל-gate הנקוב): `flutter analyze` 0 errors · `flutter test` (כולל `knowledge_protocol_test`) ירוק · `WIRING.md` מעודכן לכל symbol מחווט חדש · ענף האינסטלציה נשאר **answer-equivalent מול ה-fixtures הקיימים** (ה-keystone; בילד דמו/חי byte-identical בזכות דגל-קומפילציה OFF, לא בזכות השוואת-seed).
>
> **עוגנים שאומתו מול הקוד החי (file:line):** `lipskey_verified_connections.dart` — `EndType`:24 · `kBspInchToMm`:32 · `WaterSystem`:41 · `ConnectorEnd.directMatesWith`:49 · `VerifiedSpec`:77 · `kVerifiedSpecs`:232 (**891 specs**) · `lipskey_catalog.dart` — `LipskeyCatalogProduct`:4 · `_brandDir`:49 · `connectionSizes`:110 · `polyroll_catalog.dart:1513 kCatalogProducts` · `catalog_tree.dart` `CatalogNode`:10 / `kCatalogTree`:36 · `catalog.dart:5 kCatalogCats` · `smart_tree.dart` `SmartStage`:4 / `SmartAcc`:97 / `SmartProduct`:115 / `kSmartProducts`:153 · `variant_families.dart` `AttrKind`:9 / `variantValue`:46 / `VariantFamily`:51 / `allVariantFamilies`:77 · `install_engine.dart` `chainProvider`:38 / `lineMaxTempProvider`:42 / `connectionMethodLabel`:90 / `_galvanicallyDissimilar`:117 / `lineComplianceChecklist`:151 (1529 שורות) · `install_studio_screen.dart` (3485 שורות) `_supply/_drain`:37-38 / system-color:79 / temp-pills:475 / `canConnect`:558 / slope ת"י-1205:2319 · `install_kit.dart` `KitItem`:15 / `KitKind{tool,sealant,safety}`:34 / `recommendedKitForProduct`:42 / `recommendedKitFor`:155 · `brands.dart` `Brand`:4 / `kBrands`:25 · `related_info.dart` if-ladders 53,54,446,449,500,527,624,645 · `catalog_repository.dart` abstract:36 / `allProducts`:41 / `smartTreeCats`:76 / `catalogCategories`:80 (**`categoryTree` עדיין לא קיים → step 42 אדיטיבי**) · `catalog_local.dart` `LocalCatalogRepository`:44 / `allProducts→kCatalogProducts`:50 / `catalogCategories→kCatalogCats`:84 / `catalogRepositoryProvider`:103 · `feature_flags.dart` `FeatureFlagsNotifier`:36 / `_forcedOnFlags`:63 / `isOn`:80 · `catalog_settings.dart` `_kStorageKey`:8 · `contractor_seeds.dart` `PlanType`:84 (חשמל:199) · governance #84 → `04-ai-coeditor-behavior.md:242`.

---

## חלק א׳ — Schema + flags + store (31–34) · אדיטיבי טהור, ה-read-path לא נגוע

### Step 31 — שלושה דגלי default-OFF + flag-helper consts
1. 🎯 **היעד** — שלושה דגלים `kTradeBuilderFlag`/`kTradeStudioFlag`/`kTradeImportFlag`, כולם OFF, ש"מחזיקים מקום" לכל הפילר; אף branch חי לא קורא אותם עדיין.
2. 🔧 **איך בונים** — קובץ חדש `lib/state/trade_builder_flags.dart` עם 3 string-consts; נצרכים דרך `feature_flags.dart:36 FeatureFlagsNotifier.isOn()`. מחקים את `kKbLiveMirrorFlag` (feature_flags.dart:23) — string-const שמונע stringly-typing.
3. ⚠️ **תקלות צפויות** — מישהו מוסיף את הדגל ל-`_forcedOnFlags` (feature_flags.dart:63) ושובר default-OFF; או typo בשם הדגל בין declare ל-call-site.
4. ✅ **פתרון** — `_forcedOnFlags` נשאר `{}` (או word-finder בלבד); כל call-site מייבא את ה-const, אף פעם לא literal; assertion בטסט ששלושתם לא ב-`_forcedOnFlags`.
5. 🧪 **בדיקות** — `test/feature_flags_test.dart` מורחב (מחקה את 13-40: "default empty", "NOT force-enabled") + `test/trade_builder_flags_test.dart` חדש: שלושתם OFF ב-notifier טרי.
6. 📈 **שיפור** — `enum TradeFlag` עם extension `.name` במקום 3 strings מנותקים → מקור-אמת יחיד שמונע drift.
7. 🤔 **ריאלי?** כן — תוספת של קובץ + 3 שורות-consts; אפס call-sites משתנים; ה-notifier כבר prefs-driven.
8. 🔒 **ווידוא-פיקס (DoD)** — `analyze` נקי · `feature_flags_test`+`trade_builder_flags_test` ירוקים · `grep` מאשר אפס references לדגלים ב-`lib/` מחוץ לקובץ ה-consts · `WIRING.md` מתעד את 3 השמות.
9. 💡 **תוספת-א** — תיעוד doc-comment ליד כל const עם "מי יקרא אותו ובאיזה step" (כמו ה-doc ב-feature_flags.dart:16-23).
10. 💡 **תוספת-ב** — בדיקת lint שאף דגל-trade לא נכנס ל-`_forcedOnFlags` (golden-set test על תוכן הקבוצה).

### Step 32 — schema של trade/category/attribute/product/accessory/fixture
1. 🎯 **היעד** — טיפוסי הליבה trade-agnostic: `Trade`,`TradeCategory`,`AttributeDef`,`AttributeValue`,`AttributeKind`,`TradeProduct`,`AccessoryRule`,`SmartFixture`,`InstallStage` — כולם `@immutable`, `toJson/fromJson`, keyed by stable string ids.
2. 🔧 **איך בונים** — קובץ חדש `lib/domain/trade_schema.dart` לפי §1.1 בפלאן; superset של `LipskeyCatalogProduct` (lipskey_catalog.dart:4) ושל `SmartProduct`/`SmartStage`/`SmartAcc` (smart_tree.dart:115/:4/:97). `AttributeKind` מכליל את `AttrKind` (variant_families.dart:9).
3. ⚠️ **תקלות צפויות** — שימוש ב-index במקום id (שובר roundtrip-יציבות); `Map<String,dynamic> dims` לא נשמר → render-parity ל-image/spec נשבר אחר כך.
4. ✅ **פתרון** — כל id הוא string מפורש; `TradeProduct.dims` נשמר 1:1 לצד `attributes` (כפי §1.1 — "kept for legacy image/spec render parity").
5. 🧪 **בדיקות** — `test/trade_schema_test.dart` חדש: toJson→fromJson roundtrip לכל טיפוס; יציבות string-ids; שדות nullable שורדים round-trip · **+ `test/variant_families_snapshot_test.dart` חדש (capture+freeze): מצלם ומקבע את כל המשפחות ש-`allVariantFamilies()` (variant_families.dart:77) מייצר כיום — frame+kind+brand+category+members — לפני שה-regex נמחק ב-45. זהו ה-baseline ששלב-45 חייב לשחזר בדיוק.**
6. 📈 **שיפור** — `freezed`/`copyWith` ידני אחיד לכל הטיפוסים → פחות boilerplate ב-editors (44–47).
7. 🤔 **ריאלי?** כן — קובץ חדש, מיובא בשום מקום חי; אפס סיכון לרגרסיה. ה-snapshot הוא read-only על const קיים.
8. 🔒 **ווידוא-פיקס (DoD)** — `analyze` נקי · `trade_schema_test`+`variant_families_snapshot_test` ירוקים · אימות `grep` שאף `lib/screens` או `lib/logic` לא מייבא את הקובץ עדיין · `==`/`hashCode` עקביים · ה-snapshot קפא (כל שינוי מכוון בו דורש עדכון מודע).
9. 💡 **תוספת-א** — `schemaVersion` בכל doc (כמו ב-`Trade.schemaVersion` בפלאן) — ה-migrate האמיתי שלו ב-**Step-Eα** (לא no-op).
10. ✅ **קנוני (R1-4 · was 💡)** — `AttributeDef.matchTokens` (פלאן §1.1) כבר עכשיו, כדי ש-step 37/45 יזריע את אותם tokens שה-regex של `variant_families.dart:25` מצא; ה-snapshot של §5 הוא הראיה ש-45 משחזר חלוקה-זהה לפני מחיקת ה-regex.

### Step 33 — schema של חיבורים (מטריצה מחוברת מחליפה enum סגור) · **+ שדה material/galvanic-group (R1-3)**
1. 🎯 **היעד** — `ConnectorType`,`SystemDef`,`ProductConnectorSpec`,`ProductEnd`,`CompatibilityRule`,`SizeMatch`,`CompletionRule`,`RuleSeverity` — הכללה של ה-enum הסגור `EndType` (lipskey_verified_connections.dart:24) למטריצה מחוברת. **`CompatibilityRule` חייב שדה-חומר — בלעדיו הוא אינו יכול לבטא HDPE↔PVC או galvanic copper-group{נחושת,פליז}↔iron-group{פלדה,נירוסטה} (`_galvanicallyDissimilar:117`).**
2. 🔧 **איך בונים** — קובץ חדש `lib/domain/connection_schema.dart` לפי §1.2; `RuleSeverity{info,warning,critical}` ממופה ל-`CheckSeverity` של המנוע; `EndType`:24 ו-`WaterSystem`:41 **נשארים נוגעים-לא** (הופכים לטיפוסי-seed ב-37). **`CompatibilityRule` מקבל `final String? materialGroup` + `final List<String>? incompatibleMaterialGroups`; `CompletionRule` מקבל `final List<String>? incompatibleMaterialGroups` + `final String? requiredInterposerWhyHe`; `ProductConnectorSpec` מקבל `final String? materialGroupId` נגזר (`materialId` כבר קיים).**
3. ⚠️ **תקלות צפויות** — מיפוי severity לא תואם ל-`CheckSeverity` של `install_engine.dart` → רגרסיה בתוויות compliance; `sizeTable` מבולבל סדר [a,b]; **שדה-החומר נשמט → ה-fixtures של 37/38 לא יכולים לתאר את תשובת-ה-galvanic (נחושת↔פלדה דורש dielectric).**
4. ✅ **פתרון** — טבלת-מיפוי severity מפורשת + טסט-זהות מולה; `CompatibilityRule.aTypeId/bTypeId` עם סדר מתועד ו-`sizeTable: List<List<String>>` כ-[aSize,bSize]; **שדות-החומר חלק מה-shape כבר עכשיו, אחרת answer-equivalence ל-galvanic אינו ניתן-לביטוי.**
5. 🧪 **בדיקות** — `test/connection_schema_test.dart` חדש: JSON roundtrip; `RuleSeverity≡CheckSeverity` מיפוי דו-כיווני; `SizeMatch` enum-roundtrip · **+ `materialGroup`/`incompatibleMaterialGroups` שורדים roundtrip.**
6. 📈 **שיפור** — `CompatibilityRule.symmetric` flag כדי לא לחייב את הבעלים להזין A×B וגם B×A.
7. 🤔 **ריאלי?** חלקי — הטיפוסים קלים, אבל ה"הכללה הקשה" (enum→matrix) מוכחת רק ב-39–40; כאן רק ה-shape (כולל ממד-החומר).
8. 🔒 **ווידוא-פיקס (DoD)** — `analyze` נקי · `connection_schema_test` ירוק · `EndType`/`WaterSystem` ב-lipskey_verified_connections.dart ללא diff (git) · severity-map מאומת · **שדות material-group קיימים ב-`CompatibilityRule`/`CompletionRule` ושורדים roundtrip.**
9. 💡 **תוספת-א** — `ConnectorType.systemId` אופציונלי שמקשר ל-`SystemDef` (פלאן §1.2) — מאפשר supply/drainage גנרי.
10. 💡 **תוספת-ב** — `envelope: Map<String,num>` ב-`ProductConnectorSpec` עם מפתחות trade-defined ({'maxTempC':40} vs {'maxAmp':16}) — כבר עכשיו, כדי ש-step 49 לא ידרוש שינוי-schema.

### Step 34 — authored-trade store (deltas בלבד, SharedPreferences)
1. 🎯 **היעד** — `StateNotifier<TradesDoc>` שמחזיק **רק** את ה-deltas שהבעלים חיבר; מתחיל ריק → אין trade מפורסם → האפליקציה החיה ללא שינוי.
2. 🔧 **איך בונים** — קובץ חדש `lib/state/trades_store.dart`, key `bs.trades.v1`, מחקה את idiom של `catalog_settings.dart` (`_kStorageKey`:8 + async `_load` + `_persist`) ושל `feature_flags.dart:66 _load`/:75 `_persist`.
3. ⚠️ **תקלות צפויות** — מוטטור לא-idempotent → state-churn ו-`_persist` כפול; JSON corrupt ב-prefs מקריס את `_load`.
4. ✅ **פתרון** — מוטטורים idempotent (כמו `enable` ב-feature_flags.dart:82); `_load` עוטף `jsonDecode` ב-try/catch → fallback ל-`TradesDoc.empty()`.
5. 🧪 **בדיקות** — `test/trades_store_roundtrip_test.dart` חדש (**G-roundtrip**), מחקה את `persistence_roundtrip_test.dart`: ריק→הוסף trade→persist→reload→שווה.
6. 📈 **שיפור** — debounce ל-`_persist` (כתיבה אחת אחרי batch של מוטטורים) — חשוב ל-bulk-import (step 48).
7. 🤔 **ריאלי?** כן — ה-idiom כבר קיים פעמיים בריפו (catalog_settings, feature_flags); העתקה ישירה.
8. 🔒 **ווידוא-פיקס (DoD)** — `analyze` נקי · `trades_store_roundtrip_test` ירוק · `TradesDoc.empty()` מוודא אפס trades בהתחלה · אף screen לא צורך עדיין · `WIRING.md` מתעד את ה-provider.
9. 💡 **תוספת-א** — `tradesEditProvider` (Notifier) כ-write-seam יחיד (פלאן §2) — כל הכתיבות עוברות דרכו, אף פעם לא ל-const.
10. 💡 **תוספת-ב** — schema-version בתוך `TradesDoc` + מיגרציה no-op v1→v1, כתשתית ל-Pillar-5 swap ל-Firestore (mirror `firestore_cached_repo.dart`).

---

## חלק ב׳ — Seed האינסטלציה המיוצר = ה-keystone (35–38)

### Step 35 — adapter `TradeProduct.toLegacy()` → `LipskeyCatalogProduct`
1. 🎯 **היעד** — מתאם 1:1 שממיר `TradeProduct`→`LipskeyCatalogProduct` (attributes→dims), כך שהמסכים ממשיכים לראות `LipskeyCatalogProduct` בלבד ולא נכתבים מחדש.
2. 🔧 **איך בונים** — קובץ חדש `lib/domain/trade_product_adapter.dart` מול `lipskey_catalog.dart:4`; ממפה כל שדה (sku,nameHe,nameEn,color,qtyPack/Pallet,categoryHe/En/Emoji,page,dims,image*,spec*,brand); `attributes`→`dims` היכן שמפתחות legacy מצופים.
3. ⚠️ **תקלות צפויות** — `dims` keys של אינסטלציה (`'DN'`,`'di קוטר פנימי'`,`'PN'`) לא משוחזרים מדויק → `connectionSizes` (lipskey_catalog.dart:110) מחזיר אחרת; `brand` default אובד.
4. ✅ **פתרון** — מוצרי אינסטלציה **הם** ה-seed → שומרים את ה-`LipskeyCatalogProduct` המדויק שלהם (לא ממירים בפועל); `==` ו-render זהים; `brand` ברירת-מחדל `'ליפסקי'` נשמר (lipskey_catalog.dart:46).
5. 🧪 **בדיקות** — `test/trade_product_adapter_test.dart` חדש: `==` + render-field identity על דגימה מ-`kCatalogProducts` (polyroll_catalog.dart:1513); `connectionSizes` זהה אחרי toLegacy.
6. 📈 **שיפור** — `fromLegacy()` הופכי (`LipskeyCatalogProduct`→`TradeProduct`) — מנוצל ע"י הגנרטור ב-36.
7. 🤔 **ריאלי?** כן — מיפוי-שדות שטוח; ה-adapter הוא ה-linchpin של zero-regression (פלאן §1.4).
8. 🔒 **ווידוא-פיקס (DoD)** — `analyze` נקי · `trade_product_adapter_test` ירוק · המסכים עדיין רואים `LipskeyCatalogProduct` (אפס שינוי-טיפוס בקריאות) · round-trip של דגימת SKUs זהה byte.
9. 💡 **תוספת-א** — golden של רשימת dims-keys לכל קטגוריה, כדי לתפוס אם attribute חדש משנה את מפת ה-dims בעתיד.
10. 💡 **תוספת-ב** — `assert` בזמן debug שכל מפתח-dims שמצופה ע"י `related_info.dart`/`install_studio` קיים אחרי toLegacy.

### Step 36 — גנרטור: consts → doc אינסטלציה + categories/products/fixtures
1. 🎯 **היעד** — script שקורא את הקבועים החיים ומייצר את ה-`Trade('plumbing')` המלא + categories/products/fixtures — בלי לכתוב את האינסטלציה ביד.
2. 🔧 **איך בונים** — קובץ חדש `scripts/gen_plumbing_seed.dart` שקורא `polyroll_catalog.dart:1513 kCatalogProducts`, `catalog_tree.dart:36 kCatalogTree`, `catalog.dart:5 kCatalogCats`, `smart_tree.dart:153 kSmartProducts`, `brands.dart:25 kBrands`; פולט את הקובץ ב-37.
3. ⚠️ **תקלות צפויות** — סדר לא-דטרמיניסטי של פריטים → diff רועש בכל ריצה ושבירת byte-equality; emoji/Hebrew נחתכים בקידוד.
4. ✅ **פתרון** — מיון יציב לפי id/sku לפני פליטה; כתיבת UTF-8 מפורשת; הגנרטור פולט אותו קובץ בדיוק בכל ריצה (idempotent).
5. 🧪 **בדיקות** — `test/gen_plumbing_seed_determinism_test.dart` חדש: שתי ריצות → פלט זהה byte; הגנרטור רץ נקי ב-CI (script-only, אין import מ-lib).
6. ✅ **קנוני (R2-10 · seed CI gate #1 · was 💡)** — **CI gate #1 (stale-seed): להריץ מחדש `gen_plumbing_seed.dart` ואז `git diff --exit-code` על הקובץ המופקד — נכשל אם ה-seed שב-repo סטה מהגנרטור (catch stale committed seed).** זהו check **נפרד** מ-CI gate #2 (seed↔const answer-equivalence) שחי ב-step 38 — שני בדיקות מובחנות.
7. 🤔 **ריאלי?** כן — קריאה של consts ופליטת Dart; אין import חי; הקבועים כולם top-level ונגישים.
8. 🔒 **ווידוא-פיקס (DoD)** — `analyze` נקי · הגנרטור רץ ללא שגיאה · `determinism_test` ירוק · **`git diff --exit-code` על ה-seed המופקד עובר (CI gate #1)** · אין שום import של הסקריפט מ-`lib/` (script בלבד).
9. 💡 **תוספת-א** — header `// Auto-generated — do not edit manually. Source: scripts/gen_plumbing_seed.dart` (מחקה lipskey_catalog.dart:1-2).
10. 💡 **תוספת-ב** — הגנרטור פולט גם מניפסט-ספירה (#products,#categories,#fixtures) לתוך comment, כדי שסקירה אנושית תתפוס drop בכמות.

### Step 37 — commit ל-seed המיוצר כולל `CompatibilityRule`s מ-891 specs
1. 🎯 **היעד** — קובץ seed מופקד שמכיל את doc האינסטלציה + `CompatibilityRule`s שנגזרו מ-891 ה-`VerifiedSpec`; ה-store נשאר ריק → האינסטלציה מסופקת מה-seed האפוי → האפליקציה **answer-equivalent** (בילד דמו/חי byte-identical בזכות דגל-קומפילציה OFF, לא בזכות השוואת-seed).
2. 🔧 **איך בונים** — קובץ חדש `lib/domain/seeds/plumbing_trade_seed.dart`, פלט מ-`lipskey_verified_connections.dart:77 VerifiedSpec` mating-pairs; `EndType`(:24)→`ConnectorType`, `WaterSystem`(:41)→`SystemDef`; כללי ה-mating נגזרים מ-`ConnectorEnd.directMatesWith`(:49) ו-`pipeSharedWith`(:61).
3. ⚠️ **תקלות צפויות** — `pipeSharedWith` (hdpeCompression pipe-bridged, :61-64) שונה semantically מ-`directMatesWith` → אובדן זוגות; `kBspInchToMm`(:32) לא נשמר → size-lookup נשבר; `systemOverride`(:95) מתעלם.
4. ✅ **פתרון** — שני סוגי-כללים: direct-mate (exactSame) + pipe-shared (anyToAny דרך אותו DN); שימור `kBspInchToMm` כ-`sizeTable`/`SizeMatch.tableLookup`; כיבוד `systemOverride` ב-`SystemDef` per-SKU.
5. 🧪 **בדיקות** — `test/plumbing_seed_compiles_test.dart` (smoke: הקובץ מתקמפל, doc לא-ריק); הזהות המלאה נבדקת ב-38; `phaseb_seeds_test.dart` נשאר ירוק.
6. 📈 **שיפור** — comment-back-reference מכל `CompatibilityRule` ל-SKU/spec-מקור, כדי שדיבוג של mismatch ב-38 יהיה מיידי.
7. 🤔 **ריאלי?** חלקי — גזירת direct+pipe-shared+galvanic מ-891 specs היא העבודה הכבדה; ה-shape ברור אבל הכיסוי המלא מוכח רק ב-38.
8. 🔒 **ווידוא-פיקס (DoD)** — `analyze` נקי · הקובץ מתקמפל · ה-store עדיין ריק (אינסטלציה מה-seed האפוי) · `git` מאשר אפס diff בקבצי const (side-check, **לא** הוכחת-תאימות) · `phaseb_seeds_test` ירוק.
9. ✅ **קנוני (R1-3 · was 💡)** — שמירת `maxTempC`(:89)/`material`(:80)/`pexType`(:85) לתוך `ProductConnectorSpec.envelope`/`materialId`+`materialGroupId` כדי שבדיקת hot-water (step 41) ותיאור-ה-galvanic ב-fixtures יעבדו מה-seed.
10. ✅ **קנוני (R1-3 · was 💡)** — קידוד galvanic-groups (מ-`_galvanicallyDissimilar` install_engine.dart:117) כ-`materialGroup`/`incompatibleMaterialGroups` ב-`CompatibilityRule`/`CompletionRule` + severity, כדי ש-38/40 יוכלו לאמת את תשובת ה-dissimilar מהנתונים (גם אם אינסטלציה עצמה רצה בענף-הקשיח, ה-fixtures חייבים שדה זה כדי לבטא את התשובה).

### Step 38 — **KEYSTONE** · אסרט seed **answer-equivalent** לקבועים החיים מול ה-fixtures הקיימים
1. 🎯 **היעד** — שער-קשיח: ה-seed המיוצר מחזיר **את אותן תשובות** כמו הקבועים החיים — לא "byte-identical" (`kCatalogProducts` הוא `final`, אין בייטים לבדוק; תאימות = לוגיקת-galvanic, לא זוגות-דאטה). ה-seed משחזר את `kCatalogProducts`/`kCatalogTree`/`kCatalogCats`/`kSmartProducts` (כ-count+render-field identity למוצר בודד דרך ה-adapter) + **כל** תשובת-mating/compliance של `kVerifiedSpecs` — אחרת אדום = רגרסיית אינסטלציה.
2. 🔧 **איך בונים** — קובץ חדש `test/trade_seed_equivalence_test.dart` (**G-seed**): מזרים את ה-fixtures הקיימים דרך נתיב-ה-seed ומאשר תשובה-זהה (mates?/method/severity/compliance-list). **חובה לצנרר את כל חבילת-הפיזיקה המלאה — `ten_scenarios_audit_test`/`auto_compliance_test`/`install_plan_coverage_test` — ולא רק `compat_50_samples_test`/`full_compliance_audit_test` (האחרונים משמיטים galvanic/Legionella/dielectric, ולכן engine-מוכלל יכול לסטות בלי שייתפס).** בנוסף: ספירת products/categories/specs (891) + ה-variant-snapshot של step-32; לצד `catalog_regression_test.dart`+`phaseb_seeds_test.dart` שנשארים ירוקים.
3. ⚠️ **תקלות צפויות** — סדר-רשימות שונה גורם false-fail; השוואת `Map dims` רגישה לסדר-מפתחות; O(n²) על 891×891 איטי מדי; **כיסוי-fixtures דק (רק compat_50/full_compliance) שמחמיץ galvanic/Legionella/dielectric.**
4. ✅ **פתרון** — השוואת set/sorted-by-id (לא לפי index); השוואת dims כ-canonical-JSON ממוין; דגימת זוגות חכמה + כיסוי מלא של ה-mating-pairs הקיימים בלבד (לא מכפלה ריקה); **הזרמת חבילת-הפיזיקה המלאה (10-scenarios + auto-compliance + install-plan-coverage) כדי לכסות את ה-3 מקרי-הפיזיקה המותנית.**
5. 🧪 **בדיקות** — `trade_seed_equivalence_test` (G-seed, hard-gate) + `catalog_regression_test.dart` + `phaseb_seeds_test.dart` + `compat_50_samples_test.dart` + **`full_compliance_audit_test.dart` + `ten_scenarios_audit_test.dart` + `auto_compliance_test.dart` + `install_plan_coverage_test.dart`** + `variant_families_snapshot_test.dart` כולם ירוקים יחד.
6. 📈 **שיפור** — דוח-diff קריא (מאיזה SKU/node/scenario סטה ובמה) במקום `expect` סתום, כדי שכישלון keystone יתוקן מהר.
7. 🤔 **ריאלי?** כן — זו עצם ה-pivot של הפילר; כל הנתונים וה-fixtures קיימים; הקושי הוא הקפדנות, לא הישימות.
8. 🔒 **ווידוא-פיקס (DoD)** — **G-seed ירוק = שער לכל 39–50**; אדום → שום שלב מאוחר לא נוחת · **CI gate #2 = seed↔const answer-equivalence, מובחן מ-CI gate #1 (ה-`git diff` של step-36)** · אפס diff בקבצי const (git) הוא רק side-check, **לא** הוכחת-התאימות · regression+compat+**full-physics** suites ירוקים · ספירת products/categories/specs תואמת בדיוק (891).
9. 💡 **תוספת-א** — תג-זמן/hash של ה-seed בתוך הטסט, כדי שכל שינוי-seed מכוון יחייב עדכון מודע של ה-baseline.
10. 💡 **תוספת-ב** — הוספת ה-keystone ל-`knowledge/GATE_REGISTRY.md` כ-gate בלוקֵר רשמי + ל-`WIRING.md`, כדי שאף PR עתידי לא יעקוף אותו.

---

## חלק ג׳ — Resolver trade-agnostic (39–41) · לוגיקה טהורה, מוכחת מול המנוע הישן

### Step 39 — מנוע trade-agnostic טהור (ללא UI)
1. 🎯 **היעד** — `canConnect(a,b,ruleSet)`/`completion(line)`/`systemCoherence(line)` כפונקציות טהורות מעל `CompatibilityRule`/`CompletionRule` — המנוע מפסיק "לדעת פיזיקה" ומתחיל להעריך כללים מחוברים.
2. 🔧 **איך בונים** — קובץ חדש `lib/domain/connection_resolver.dart`; `canConnect` עובר על כל (endA,endB), מוצא `CompatibilityRule(aType,bType)`, מחיל `SizeMatch` (exactSame/anyToAny/tableLookup) → method-label+severity. הכללה של `install_engine.dart:90 connectionMethodLabel` + `VerifiedSpec.compatibleWith`.
3. ⚠️ **תקלות צפויות** — `tableLookup` עם size מנורמל שונה (`'1/2'` מול `'1/2"'`) מחמיץ; אי-דטרמיניזם בסדר-בדיקת-ends; חוסר-טיפול ב-end ללא כלל.
4. ✅ **פתרון** — נרמול-size מרכזי (strip `"`+trim, כמו `kBspInchToMm` doc ב-:31); סדר-ends דטרמיניסטי; end ללא-כלל → "לא מתחבר" עם severity מתועד (לא exception).
5. 🧪 **בדיקות** — `test/connection_resolver_unit_test.dart` חדש: `SizeMatch.exactSame`/`anyToAny`/`tableLookup` כל אחד; `systemCoherence` דוחה ערבוב supply/drainage (כמו `WaterSystem` :67).
6. 📈 **שיפור** — memoization של (aType,bType,size) → תשובה (מחקה את `compat_memo_test.dart` הקיים) לקראת קווים ארוכים.
7. 🤔 **ריאלי?** כן — פונקציות טהורות, נקראות בשום-מקום-חי עדיין; testable בבידוד.
8. 🔒 **ווידוא-פיקס (DoD)** — `analyze` נקי · `connection_resolver_unit_test` ירוק · אפס call-site חי (grep) · פונקציות דטרמיניסטיות (אותו input→אותו output).
9. 💡 **תוספת-א** — `explain()` שמחזיר את הכלל שהתאים (back-ref ל-SKU/rule) — מקביל ל-`compat_explain_test.dart` הקיים.
10. 💡 **תוספת-ב** — `systemCoherence` מחזיר את ה-`SystemDef` החורג (לא רק bool), כדי ש-UI יוכל להצביע על האלמנט הבעייתי.

### Step 40 — **G-resolver parity** · resolver על המטריצה המוזרעת מול התשובות הישנות
1. 🎯 **היעד** — להוכיח שה-resolver החדש נותן **בדיוק** את תשובות `kVerifiedSpecs` הישנות לפני שמסך כלשהו קורא לו.
2. 🔧 **איך בונים** — קובץ חדש `test/connection_resolver_parity_test.dart` (**G-resolver**): מזרים את fixtures של `compat_50_samples_test.dart` + `full_compliance_audit_test.dart` דרך `connection_resolver.dart`, ומאשר שוויון מול תוצאות `lipskey_verified_connections.dart`.
3. ⚠️ **תקלות צפויות** — fixture-coupling (הטסט הישן בנוי סביב טיפוסי legacy) → קשה להזרים; אי-התאמת severity-mapping מ-step 33.
4. ✅ **פתרון** — שכבת-adapter בטסט שממירה fixture→`ProductConnectorSpec`; השוואה רק על (mates?,method,severity) המנורמלים; severity דרך מפת step 33.
5. 🧪 **בדיקות** — `connection_resolver_parity_test` (G-gate) + `compat_50_samples_test.dart`+`full_compliance_audit_test.dart`+`compat_coverage_test.dart` נשארים **untouched & green**.
6. 📈 **שיפור** — הרחבת ה-parity לכל 891 (לא רק 50-sample) כ-`@Tags(['slow'])` שרץ ב-CI מלא בלבד.
7. 🤔 **ריאלי?** חלקי — ההזרמה דרך fixtures ישנים דורשת adapter-glue; השוויון עצמו דטרמיניסטי וברור.
8. 🔒 **ווידוא-פיקס (DoD)** — **G-resolver ירוק** = תנאי לפני step 41 (חיווט המנוע) · הטסטים הישנים `compat_*`/`full_compliance_audit_test` ללא diff וירוקים · 0 mismatches מדווחים.
9. 💡 **תוספת-א** — דוח כיסוי שמראה כמה specs נוגעו ע"י ה-parity (gap-finder), כדי לוודא שלא נשארו זוגות לא-נבדקים.
10. 💡 **תוספת-ב** — property-based test (random valid pairs) שמשווה resolver↔legacy, מעבר ל-fixtures הקבועים.

### Step 41 — עיטוף connect/compliance מאחורי דגל, branch אינסטלציה **קשיח לנצח** (R1-2)
1. 🎯 **היעד** — `lineComplianceChecklist`(install_engine.dart:151) + `connectionMethodLabel`(:90) מאצילים ל-resolver **רק** כש-`tradeId!='plumbing'` וגם `kTradeStudioFlag` ON; OFF-path = היום. **`'plumbing'` אינו "מוזרע" למטריצה — הוא פשוט לא עובר ב-resolver; הענף-הקשיח הוא הקוד, לא דאטה.**
2. 🔧 **איך בונים** — ב-`install_engine.dart` להוסיף branch מאצולה ל-`connection_resolver.dart`; `'plumbing'` שומר את ה-branch הכתוב-ביד **verbatim, לנצח, אינו ניתן-למחיקה** (כולל `_galvanicallyDissimilar`:117 + threshold 60°C + anti-scald/TMTV + Legionella-sampling + dielectric-union + recirc isolation≥3). ה-`CompatibilityRule`/`CompletionRule` ה-authored משמשים **רק** ל-`tradeId != 'plumbing'`.
3. ⚠️ **תקלות צפויות** — מצב mixed (אינסטלציה+trade חדש באותו קו) מבלבל branch; `chainProvider`(:38) element-type משתנה ושובר את כל ה-BOM.
4. ✅ **פתרון** — `chainProvider` נשאר `List<LipskeyCatalogProduct>` דרך ה-adapter (step 35) → קוד ה-chain לא נגוע; branch לפי `activeTradeProvider` (step 43), לא per-element.
5. 🧪 **בדיקות** — `install_engine_b5/b6/b8/b10/b11/b13_test.dart` + `install_engine_safety_test.dart` + `install_engine_hardening_test.dart` + `polyroll_install_engine_test.dart` כולם ירוקים ללא שינוי.
6. 📈 **שיפור** — לוג-טלמטריה (debug-only) "delegated-to-resolver" vs "legacy-branch" כדי לאמת ב-runtime שה-OFF-path אכן legacy.
7. 🤔 **ריאלי?** כן — branch מגודר; ה-element-type נשמר ע"י ה-adapter; אפס שינוי ל-OFF-path.
8. 🔒 **ווידוא-פיקס (DoD)** — כל 9 טסטי `install_engine_*`/`polyroll_install_engine` ירוקים · עם דגל OFF התשובות זהות byte (golden) · `'plumbing'` branch ללא diff סמנטי · **assertion (היה 💡 — עכשיו חובה): `tradeId=='plumbing'` לעולם לא נכנס ל-branch של ה-resolver (שמירת keystone)** · `WIRING.md` מתעד את ההאצלה.
9. ✅ **קנוני (R1-2 · הועלה ל-DoD §8)** — ה-assert שאינסטלציה לעולם לא עוברת ב-resolver אינו "תוספת" אלא חוזה-keystone מחייב.
10. 💡 **תוספת-ב** — feature-kill-switch: גם אם `kTradeStudioFlag` ON אך ה-resolver זורק (לתחום-חדש), fallback אוטומטי ל-legacy + toast שקט (robustness).

---

## חלק ד׳ — תפר tradeId ברפוזיטורי + active-trade (42–43) · ברירת-מחדל משחזרת היום

### Step 42 — פרמטר `tradeId` לקריאות repo, default `'plumbing'` = seed
1. 🎯 **היעד** — `allProducts`/`catalogCategories`/`smartTreeCats` + `categoryTree` חדש מקבלים `tradeId` עם default `'plumbing'`; קוראים קיימים לא משתנים.
2. 🔧 **איך בונים** — `catalog_repository.dart:36` interface (`allProducts`:41/`catalogCategories`:80/`smartTreeCats`:76 + `categoryTree` חדש — **אומת שלא קיים עדיין**); impl `catalog_local.dart:44 LocalCatalogRepository` מסנן את ה-seed לפי trade (`allProducts→kCatalogProducts`:50, `catalogCategories→kCatalogCats`:84).
3. ⚠️ **תקלות צפויות** — הוספת מתודה ל-abstract שוברת impls אחרים (firestore/cached); סינון לפי trade על seed האינסטלציה ≠ `kCatalogProducts` המקורי.
4. ✅ **פתרון** — default-arg `tradeId='plumbing'` → קוראים קיימים ללא שינוי; ל-`categoryTree` לתת default-impl ב-interface או לממש בכל impl; seed-אינסטלציה ≡ `kCatalogProducts` מובטח ע"י G-seed (step 38).
5. 🧪 **בדיקות** — `repositories_test.dart` (קיים) + `catalog_regression_test.dart` ירוקים (אינסטלציה-seed ≡ `kCatalogProducts`); הוספת case: `allProducts('plumbing')`==`allProducts()`.
6. 📈 **שיפור** — caching per-tradeId ברמת ה-repo (lazy) כדי לא לסנן בכל קריאה — מתחבר ל-Pillar-5 scale.
7. 🤔 **ריאלי?** כן — default-arg הוא שינוי תואם-לאחור; הסינון טריוויאלי כל עוד יש trade אחד.
8. 🔒 **ווידוא-פיקס (DoD)** — `repositories_test`+`catalog_regression_test` ירוקים · כל הקוראים הקיימים מתקמפלים ללא שינוי · `allProducts('plumbing')` byte-equal ל-`kCatalogProducts` · `WIRING.md` מתעד את החתימה החדשה.
9. 💡 **תוספת-א** — `categoryTree(tradeId)` שמחזיר `List<CatalogNode>` (catalog_tree.dart:10) דרך ה-adapter, כדי שאיטרציית `for (final n in kCatalogTree)` (catalog_screen.dart:1186/2669) תוכל להפנות ל-repo בלי שינוי-טיפוס.
10. 💡 **תוספת-ב** — assertion-test שכל מתודות ה-interface שמרו על אותה תשובה כאשר `tradeId` מושמט (חוזה תאימות-לאחור פורמלי).

### Step 43 — `activeTradeProvider` (default `'plumbing'`; switcher מוסתר כש-count==1)
1. 🎯 **היעד** — provider שמכריע איזה trade פעיל; ברירת-מחדל אינסטלציה; ה-switcher מוסתר כשיש trade מפורסם יחיד → אפס שינוי ויזואלי.
2. 🔧 **איך בונים** — provider חדש ב-`lib/state/trades_store.dart`, נצרך דרך `catalog_local.dart:103 catalogRepositoryProvider`; מחזיר `'plumbing'` כל עוד `publishedTrades.length<=1`.
3. ⚠️ **תקלות צפויות** — state פעיל נשמר ל-trade שבוטל-פרסום → catalog ריק; switcher מהבהב כשעוברים מ-1 ל-2 trades.
4. ✅ **פתרון** — אם ה-active-trade כבר לא published → fallback ל-`'plumbing'`; ה-switcher נגזר reactive מ-count, ללא state נפרד.
5. 🧪 **בדיקות** — `test/active_trade_provider_test.dart` חדש: default plumbing; switcher hidden at count==1; fallback ל-plumbing כשה-active מבוטל-פרסום.
6. 📈 **שיפור** — persist של ה-active-trade ב-prefs (key נפרד) כדי שמשתמש-בעלים יישאר על ה-trade האחרון שלו בין sessions.
7. 🤔 **ריאלי?** כן — provider קטן מעל ה-store הקיים; ה-hidden-at-1 שומר אפס-רגרסיה ויזואלית.
8. 🔒 **ווידוא-פיקס (DoD)** — `active_trade_provider_test` ירוק · count==1 → אין UI-switcher (golden) · `catalogRepositoryProvider` מקבל את ה-tradeId הנכון · אפס שינוי ויזואלי בבילד דמו.
9. 💡 **תוספת-א** — `activeTradeProvider` חושף גם את אובייקט ה-`Trade` המלא (לא רק id) ל-emoji/color בכותרת.
10. 💡 **תוספת-ב** — guard שאינסטלציה (`'plumbing'`) **תמיד** ברשימת ה-published (reserved trade) — לא ניתן לבטל-פרסום ולהשאיר catalog ריק.

---

## חלק ה׳ — מסכי authoring no-code עבריים (44–47) · טאב-מנהל מגודר, RTL/a11y

### Step 44 — entry מגודר במנהל + רשימת trades + define-step (טיוטת Trade)
1. 🎯 **היעד** — כניסת "🏗️ בונה ענפים" בלוח-המנהל (מאחורי `kTradeBuilderFlag`), רשימת trades, ושלב-הגדרה ראשון שכותב טיוטת `Trade`.
2. 🔧 **איך בונים** — קבצים חדשים `lib/screens/trade_builder/trade_builder_home.dart` + `trade_define_step.dart`; entry ב-`manager_dashboard_screen.dart` (4 הטאבים :225-228, `_ManageTab`); כתיבה דרך `tradesEditProvider` מעל `trades_store.dart`.
3. ⚠️ **תקלות צפויות** — הטאב מופיע גם כש-flag OFF → שובר `manager_dashboard_screen_test`; כיווניות LTR בטעות; emoji-picker שובר טקסט עברי.
4. ✅ **פתרון** — ה-entry מגודר ב-`isOn(kTradeBuilderFlag)` (feature_flags.dart:80) → מוסתר ב-OFF; `Directionality.rtl` מפורש; `textScaler` clamp 1.35 (חוזה a11y של מנהל-פלאן §4).
5. 🧪 **בדיקות** — `test/trade_builder_home_test.dart` חדש (**G-authoring**): RTL directionality + semantics + `textScaler` clamp; `manager_dashboard_screen_test.dart` ללא שינוי (טאב מוסתר ב-OFF).
6. 📈 **שיפור** — שכפול רכיבי ה-KPI-tile/card מהמנהל (פלאן §2.1) במקום widgets חדשים → עקביות ויזואלית.
7. 🤔 **ריאלי?** כן — entry מגודר + 2 מסכים; ה-write-seam (`tradesEditProvider`) כבר קיים מ-step 34.
8. 🔒 **ווידוא-פיקס (DoD)** — `trade_builder_home_test` ירוק · `manager_dashboard_screen_test` ללא diff · flag OFF → הטאב נעדר (widget-absent assertion) · `WIRING.md` מתעד את ה-entry.
9. 💡 **תוספת-א** — wizard-progress (6 שלבים) בכותרת, כדי שהבעלים יראה היכן הוא ב-"add electrician".
10. 💡 **תוספת-ב** — persona-dropdown מ-`kPersonas` כבר ב-define-step (פלאן §2.2), כדי ש-go-live (Pillar §6) יקבל personaId.

### Step 45 — עורכי עץ-קטגוריות + סכמת-מאפיינים
1. 🎯 **היעד** — `category_tree_editor` (reorder/add/rename/move/delete RTL) + `attribute_schema_editor` (kind/values/"ציר וריאנט?"/unit) — תחליף מחובר ל-`variant_families.dart:9 AttrKind`.
2. 🔧 **איך בונים** — קבצים חדשים `lib/screens/trade_builder/category_tree_editor.dart` + `attribute_schema_editor.dart`; כותבים `TradeCategory`/`AttributeDef` דרך `tradesEditProvider`; preview חי של פירוק-שם (parity ל-chip-hierarchy); קבצי const **לא נגעים**.
3. ⚠️ **תקלות צפויות** — reorder ב-RTL הופך כיוון-גרירה; preview של פירוק-שם לא תואם ל-`variantValue` (variant_families.dart:46) → בלבול בעלים; **מחיקת ה-regex לפני שה-`matchTokens` ה-authored משחזר את החלוקה → איחוד/פיצול-משפחות שקט (drift) שמשנה את מקטע-הוריאנטים גם ב-OFF/אינסטלציה.**
4. ✅ **פתרון** — `ReorderableListView` עם `Directionality.rtl` ובדיקת אינדקסים מפורשת; ה-preview קורא לאותו אלגוריתם פירוק (או ל-`matchTokens` של ה-`AttributeDef`) → תואם ל-step 32 §10; **ה-regex של `variant_families.dart` (`_isSizeToken:25`/`kindOf:32`/`allVariantFamilies:77`) נמחק רק אחרי שה-axes ה-authored עוברים את ה-`variant_families_snapshot_test` של step-32 בדיוק (אותן משפחות, אותם חברים); אינסטלציה שומרת את `allVariantFamilies()` עד שה-snapshot ירוק.**
5. 🧪 **בדיקות** — `test/category_tree_editor_test.dart` + `test/attribute_schema_editor_test.dart` חדשים: RTL+a11y; live name-decompose preview נכון; const files ללא diff · **+ ה-`variant_families_snapshot_test` (מ-step 32) חייב לעבור עם ה-axes ה-authored לפני מחיקת ה-regex.**
6. 📈 **שיפור** — undo/redo על עריכות-עץ (מחקה את undo של MANAGER-BUILD-PLAN §M3).
7. 🤔 **ריאלי?** חלקי — עורך-עץ RTL גורר-ושחרר הוא ה-UI הכבד ביותר; ה-data-write פשוט, ה-interaction עדין.
8. 🔒 **ווידוא-פיקס (DoD)** — שני הטסטים ירוקים · **`variant_families_snapshot_test` ירוק עם ה-axes ה-authored — תנאי-קדם למחיקת ה-regex** · const files (variant_families/catalog_tree) ללא diff עד שה-snapshot עובר · semantics-labels לכל פעולה · `textScaler` clamp נשמר.
9. 💡 **תוספת-א** — long-press context-menu (idiom של `catalog_screen` `_SectionChipsRow:710`) ל-rename/move/delete.
10. 💡 **תוספת-ב** — ולידציה-בזמן-עריכה: "ציר וריאנט בלי values" מסומן צהוב מיד (לא רק ב-publish).

### Step 46 — עורכי authoring של מוצרים + אביזרים
1. 🎯 **היעד** — `product_authoring_screen` (עריכת `attributes` מול schema, רשימה לא-virtualized ≤10K) + `accessory_rule_editor` (`AccessoryRule` name/why/must/price/linkSku).
2. 🔧 **איך בונים** — קבצים חדשים `lib/screens/trade_builder/product_authoring_screen.dart` + `accessory_rule_editor.dart`; deltas בלבד דרך `tradesEditProvider`; הכללה של `SmartAcc` (smart_tree.dart:97). live-catalog ממשיך לקרוא seed ללא שינוי.
3. ⚠️ **תקלות צפויות** — `attributes` שלא תואם ל-`AttributeDef` (value מחוץ ל-enum); `linkSku` מצביע ל-SKU לא-קיים → orphan; רשימה ארוכה תוקעת UI (>10K).
4. ✅ **פתרון** — ולידציה מול schema בזמן עריכה (value חייב ב-`AttributeValue` או free/number); `linkSku` עם picker מתוך מוצרי ה-trade בלבד; הערה מפורשת ש-Pillar-5 בעל ה-virtualization (כאן ≤10K לא-virtualized).
5. 🧪 **בדיקות** — `test/product_authoring_screen_test.dart` + `test/accessory_rule_editor_test.dart` חדשים: RTL+a11y; value מחוץ-לסכמה נחסם; live-catalog seed ללא diff.
6. 📈 **שיפור** — bulk-edit (החלת attribute על קבוצת מוצרים) — מקדים את ה-import (step 48).
7. 🤔 **ריאלי?** כן — generalize של M3 steps 35-36 הקיימים; deltas-only שומר zero-regression.
8. 🔒 **ווידוא-פיקס (DoD)** — שני הטסטים ירוקים · live catalog קורא seed (אפס diff) · value-out-of-schema נחסם · `accessory.linkSku` מצביע ל-TradeProduct קיים בלבד.
9. 💡 **תוספת-א** — חיווי "מוצרים ללא תמונה/spec" כ-checklist קדם-publish.
10. 💡 **תוספת-ב** — `AccessoryRule.mustHave` מסומן ויזואלית שונה (חובה vs מומלץ) — parity ל-`SmartAcc.must` (smart_tree.dart:97).

### Step 47 — סטודיו כללי-חיבור + sheet פרסום · **+ ולידציית FK רפרנציאלית בפרסום (R2-7)**
1. 🎯 **היעד** — `connection_rule_studio` (`ConnectorType`/`SystemDef` + מטריצת A×B + `CompletionRule` + test-bench חי הקורא ל-`connection_resolver`) + `trade_publish_sheet` שמולידציה חוסמת trade פגום **או trade עם FK תלוי (categoryId/tradeId/typeId שלא נפתר)**.
2. 🔧 **איך בונים** — קבצים חדשים `lib/screens/trade_builder/connection_rule_studio.dart` + `trade_publish_sheet.dart`; ה-test-bench קורא `lib/domain/connection_resolver.dart` (step 39) → אותה תשובה כמו install-studio; publish מאמת (כל קטגוריה≥1 מוצר, כל ציר-וריאנט עם values, אין compat-rule יתום, **+ ולידציית FK בזמן-פרסום: כל `product.categoryId`/`tradeId` נפתר וכל `aTypeId`/`bTypeId`/`*TypeId` של כל rule נפתר — אחרת דחיית-פרסום**) → מהפך `published`.
3. ⚠️ **תקלות צפויות** — מטריצת A×B גדולה לא-נגישה ב-RTL; test-bench נותן תשובה שונה מ-install-studio (drift); publish מאשר trade עם orphan-rule **או עם FK-משובש (categoryId/typeId מת)**.
4. ✅ **פתרון** — מטריצה עם semantics לכל תא + גלילה RTL; test-bench **קורא לאותו** `connection_resolver` שה-studio יקרא (מקור-אמת יחיד); publish-validation מריץ את אותם בדיקות-orphan **+ FK-resolve על כל product/rule** כטסט (מקור-אמת משותף עם ה-`onCatalogProductWrite` של Step-FK).
5. 🧪 **בדיקות** — `test/connection_rule_studio_test.dart` + `test/trade_publish_sheet_test.dart` חדשים: RTL+a11y; publish-validation **חוסם** trade פגום **+ חוסם trade עם FK תלוי**; draft לא משפיע על live.
6. 📈 **שיפור** — "הסבר-תא" שמראה את הכלל המתקבל (back-ref ל-`explain()` מ-step 39 תוספת-א).
7. 🤔 **ריאלי?** חלקי — מטריצת A×B ויזואלית + test-bench חי הם המסך המורכב; ה-resolver כבר מוכח (step 40).
8. 🔒 **ווידוא-פיקס (DoD)** — שני הטסטים ירוקים · publish חוסם trade-לא-תקין **ו-trade עם FK תלוי** (assertion) · test-bench==install-studio answer (אותו resolver) · draft לא נראה ב-live (אפס diff).
9. 💡 **תוספת-א** — `CompletionRule` wizard ("מה משלים מה") עם דוגמה חיה ("יש מאמ"ת 16A אבל אין מפסק-פחת").
10. 💡 **תוספת-ב** — publish dry-run report (כמה קטגוריות/מוצרים/כללים) לפני האישור הסופי.

---

## חלק ו׳ — חוזה bulk-import (48) · mapping/validation בלבד, at-scale = Pillar 5

### Step 48 — template-export + column-map + dry-run + commit מגודר
1. 🎯 **היעד** — חוזה ייבוא: ייצוא תבנית מ-`AttributeDef`s → מיפוי עמודות → dry-run ולידציה → commit מגודר; **אין כתיבה בשגיאה**.
2. 🔧 **איך בונים** — UI ב-`product_authoring_screen.dart` (step 46) + מחולל-תבנית חדש ב-`scripts/` מתוך `AttributeDef`s; הכל מאחורי `kTradeImportFlag`; commit דרך `tradesEditProvider` (batched).
3. ⚠️ **תקלות צפויות** — כתיבה חלקית כשחלק מהשורות נכשלות; כפילות SKU שלא נתפסת; ערך enum לא-מוכר נכתב בשקט; **`categoryId` שלא קיים נכתב בשקט → מוצר יתום**; קובץ ענק תוקע (10K).
4. ✅ **פתרון** — commit אטומי: ולידציה מלאה לפני **כל** כתיבה (no-write-on-error); required/enum/sku-unique **+ "categoryId exists" (referential)** כחוקים מפורשים; הערה ש-10K-row pipeline/Firestore-batches/indexed-search **נדחים ל-Pillar 5**.
5. 🧪 **בדיקות** — `test/bulk_import_test.dart` חדש (**G-import**): template→map→dry-run→commit על CSV סינתטי; required/enum/sku-unique **+ "categoryId exists" per-row**; **no write on error** (assertion שה-store לא השתנה בכישלון).
6. 📈 **שיפור** — שורות-שגיאה להורדה (CSV) עם סיבה-לכל-שורה (פלאן §4.3 "✅ 9842 · ⚠️ 137 · ❌ 21").
7. 🤔 **ריאלי?** חלקי — חוזה ה-mapping/validation ריאלי כאן; ה-at-scale (10K, dedup, batches) **מפורשות Pillar 5**.
8. 🔒 **ווידוא-פיקס (DoD)** — `bulk_import_test` ירוק · שגיאה → store ללא diff (no-write) · מאחורי `kTradeImportFlag` (OFF→אין entry) · auto-add-enum מתועד ומאושר-בעלים.
9. 💡 **תוספת-א** — variant auto-grouping אחרי import לפי הצירים המחוברים (פלאן §4.5) — בלי regex-שמות.
10. 💡 **תוספת-ב** — preview של 5 שורות ראשונות אחרי mapping, כדי שהבעלים יאמת מיפוי לפני dry-run מלא.

---

## חלק ז׳ — install-studio מגודר + refactor מותגים + acceptance חשמלאי (49–50)

### Step 49 — הפשטת פיזיקה-אפויה + if-ladders של מותגים בתפרים נקובים (מגודר) · **BrandProfile ממפה הכל + parity-test לפני מחיקה (R1-5)**
1. 🎯 **היעד** — הפיכת 8 ה-seams האפויים ב-install-studio + if-ladders של מותגים ל-trade-config reads; OFF/`'plumbing'` → קבועים זהים; **ה-if-ladders נמחקים רק אחרי (a) G-seed/answer-equivalence ירוק וגם (b) `brand_profile_parity_test` ירוק.**
2. 🔧 **איך בונים** — `install_studio_screen.dart`: system-colors `_supply/_drain`:37-38→`SystemDef.color`; picker:100→`smartFixtures`; compliance-labels:137→`CompletionRule.whyHe`; temp-pills:475→envelope; `canConnect`:558→`connection_resolver`; kit:2438; slope ת"י-1205:2319→`TradePhysicsConfig` אופציונלי. **`BrandProfile` (`brands.dart:4 Brand`) חייב למפות את כל 5 קבוצות ה-if-ladder payload לפני מחיקה: (1) spec-envelope temps — Polyroll `maxTempC:90` (`related_info.dart:500-521`) · Huliot `maxTempC:95` (`:527-537`) → `specEnvelope`; (2) bore-parse — Polyroll di-range-max ("13.6–14.7"→14.7, `:501-513`) · Huliot `DN`→double (`:528-529`) → `boreParse{diRangeMax|dnDirect}`; (3) image-dir — `lipskey_catalog.dart:49-53 _brandDir` (פולירול→polyroll · חוליות→huliot_smartlock · else→lipskey) + fallback `page_*` → `imageDir`; (4) finder emoji/label — `:53-54` (פולירול→🚰"אספקת מים" · חוליות→🟢"דלוחין SmartLock") → `finderEmoji`+`finderLabel`; (5) kit-count/systemHint — `:446-454` + `install_kit.dart:48,91` + `system_division.dart:25` (פולירול→{supply}) + `lipskey_product_sheet.dart:2055-2077` → `kitStrategy`+`systemHint`+`specStrips`.**
3. ⚠️ **תקלות צפויות** — blast-radius במסך 3485 שורות; **מחיקת if-ladder לפני שה-`BrandProfile` מכסה את כל 5 הקבוצות ל-3 המותגים (פולירול/חוליות/ליפסקי) → רגרסיית-spec lossy (envelope/bore/image/finder/kit)**; פאנל-slope לא-מוסתר לטרייד בלי flow-physics.
4. ✅ **פתרון** — לעולם לא fork; רק redirect; **`BrandProfile` משכפל את ההתנהגות המדויקת של 3 המותגים על כל 5 הקבוצות, ו-`brand_profile_parity_test` מאשר שהפרופיל מחזיר תשובה-זהה ל-if-ladder לכל קבוצה לפני המחיקה**; `TradePhysicsConfig` null → הפאנל מוסתר; **אינסטלציה ממשיכה לקרוא את הקבועים/הענף הכתוב-ביד — ה-config-read רק מחליף אותם לתחום-חדש (אינסטלציה אינה "מזריעה" כלום)**; מחיקת if-ladders **רק אחרי** G-seed (step 38) **וגם** parity-test.
5. 🧪 **בדיקות** — `test/install_studio_flag_off_golden_test.dart` חדש (**G-flag-off**): golden/snapshot diff מול הנוכחי כשכל `kTrade*` OFF (מכסה strips/finder/image-dir) · **+ `test/brand_profile_parity_test.dart` חדש: 3 מותגים × 5 קבוצות, פרופיל==if-ladder** · כל `install_engine_*`/`compat_*` ירוקים ללא שינוי.
6. 📈 **שיפור** — `BrandProfile` עם `kitStrategy`/`finderEmoji` (פלאן §3.3) → trade חדש מספק פרופיל משלו, אפס fall-through ל-Lipskey.
7. 🤔 **ריאלי?** חלקי — 8 seams + 8 if-ladders במסכים גדולים; כל seam קטן, אבל הסך גדול ודורש זהירות golden + parity.
8. 🔒 **ווידוא-פיקס (DoD)** — **G-flag-off ירוק** (golden זהה, כל הדגלים OFF) · **`brand_profile_parity_test` ירוק (5 קבוצות × 3 מותגים) — תנאי-קדם למחיקת if-ladders** · `'plumbing'`/OFF → קבועים זהים byte · if-ladders נמחקו **רק** עם G-seed ירוק **וגם** parity ירוק · `install_engine_*`/`compat_*` ללא diff.
9. ✅ **קנוני (R1-2)** — `TradePhysicsConfig` עם slope-min/ΔP-threshold כ-config (2% ב-:2319, 1-bar): אינסטלציה **ממשיכה לקרוא את הקבועים הכתובים-ביד** (אינה "מזריעה" אותם); רק תחום-חדש מחליפם דרך ה-config-read.
10. 💡 **תוספת-ב** — kit trade-aware: אינסטלציה שומרת `install_kit.dart` (`recommendedKitFor`:155, `KitKind{tool,sealant,safety}`:34); trade חדש מחבר kit כ-`AccessoryRule` מתוייג tool/sealant/safety.

### Step 50 — **G-newtrade acceptance** · חיבור "חשמלאי" end-to-end, catalog+studio עובדים
1. 🎯 **היעד** — ההוכחה הסופית "תוסיף חשמלאי בעצמי": בתוך-טסט לחבר 2 קטגוריות, ציר-וריאנט אחד, 2 מוצרים, כלל-compat אחד **+ לפחות `CompletionRule` אחד** → לפרסם → לאשר catalog-list + install-studio connect לחשמלאי **+ לאשר שה-`CompletionRule` נורה (קו מפר מציף אותו) וחוסם פרסום כשהוא מופר**; נתיב האינסטלציה ללא שינוי לכל אורך הדרך.
2. 🔧 **איך בונים** — קובץ חדש `test/trade_newtrade_acceptance_test.dart` (**G-newtrade**): in-test authoring דרך `tradesEditProvider`→publish→assert על `catalogRepositoryProvider`(catalog_local.dart:103) ב-`activeTrade='electrician'` ועל `connection_resolver.canConnect`+`completion()`; **חיבור `CompletionRule` (למשל "מאמ"ת 16A דורש מפסק-פחת"), assert ש-`completion(line)` מצביע עליו כשחסר, ו-assert ש-`trade_publish_sheet` חוסם פרסום כשהכלל מופר**; sync ל-`WIRING.md`+`02-domain-vertical-builder.md`. (מתבסס על `contractor_seeds.dart:84 PlanType` חשמל:199 כהוכחה שהקודבייס כבר חושב "trade = data".)
3. ⚠️ **תקלות צפויות** — ה-acceptance "מדליק" התנהגות לכל המשתמשים בלי אישור-בעלים; חשמלאי-trade דולף לבילד הדמו; rollout עוקף governance #84; **ה-`CompletionRule` "נכתב" אך לא נבדק שהוא בפועל נורה/חוסם.**
4. ✅ **פתרון** — per-trade rollout **default-OFF עד אישור-בעלים** (governance #84 → `04-ai-coeditor-behavior.md:242`: presentation/behavior בלבד, אפס HR/role/auth); הטסט מפרסם trade **בתוך הטסט בלבד**, לא בבילד; אינסטלציה answer-equivalent מאומת מקבילית; **assertion דו-כיווני על ה-`CompletionRule`: קו-תקין עובר, קו-מפר מציף-ונחסם.**
5. 🧪 **בדיקות** — `trade_newtrade_acceptance_test` (G-newtrade, acceptance) עם **כל ה-suite ירוק**: `catalog_regression_test`+`compat_*`+`install_engine_*`+`knowledge_protocol_test`+`feature_flags_test`+`trade_seed_equivalence_test` (G-seed) כולם ירוקים יחד · **+ assert ש-`CompletionRule` נורה וחוסם פרסום.**
6. 📈 **שיפור** — להפוך את ה-acceptance ל-template (`gen_trade_smoketest`) שכל trade עתידי מקבל smoke דומה אוטומטית.
7. 🤔 **ריאלי?** כן — כל אבני-הבניין (schema/store/resolver/seed/screens) נבנו ב-31–49; זו הרכבה+assertion, לא יכולת חדשה.
8. 🔒 **ווידוא-פיקס (DoD)** — **G-newtrade ירוק** + full-suite ירוק · אינסטלציה ללא diff (keystone שמור) · rollout default-OFF (governance #84 assertion: אין HR/role/auth) · `WIRING.md`+`02-domain-vertical-builder.md` מסונכרנים.
9. 💡 **תוספת-א** — assert שעם כל הדגלים OFF, החשמלאי **לא נראה** בקטלוג/studio (gated go-live) — חוזה ה-default-OFF.
10. 💡 **תוספת-ב** — הוספת G-newtrade ל-`knowledge/GATE_REGISTRY.md` כ-acceptance-gate סוגר-פילר, + רישום ב-`02-domain-vertical-builder.md` §10 (Phase G) כתנאי-נעילה.

---

## חלק ח׳ — שלמות פאזה-G (אדיטיבי אחרי 50, מגודר) · FK רפרנציאלי + migrate אמיתי + archive/tombstone (R1-6 · R2-7 · R2-11)

> אינסטלציה (reserved) אינה ניתנת-ל-archive ולעולם FK-clean (G-seed) → ה-keystone שמור. הכל דרך `tradesEditProvider`, default-OFF.

### Step-FK — ולידטור רפרנציאלי `onCatalogProductWrite` (publish-time + write-trigger) (R2-7/R2-10)
1. 🎯 **היעד** — כל `product.categoryId`/`tradeId` וכל `typeId` של כל rule **חייבים להיפתר** — בזמן-פרסום (Step-47) וגם בכל כתיבה בודדת; אחרת דחייה/הסגר (quarantine), כדי שלא ייווצרו רפרנסים-תלויים.
2. 🔧 **איך בונים** — קובץ חדש `lib/domain/trade_referential_validator.dart` המשקף את בדיקת-ה-FK של Step-47 בתפר-הכתיבה (Pillar-5 מגבה אותו ב-server `onWrite`); מופעל מ-`tradesEditProvider` לפני commit.
3. ⚠️ **תקלות צפויות** — מוצר/כלל עם id-מת נכתב בשקט; ייבוא-bulk (Step-48) מזריק categoryId לא-קיים.
4. ✅ **פתרון** — fail-closed: write עם FK לא-פתור → quarantine (לא live), אף פעם לא נכתב לפרסום; מקור-אמת משותף עם Step-47 ו-Step-48 ("categoryId exists").
5. 🧪 **בדיקות** — `test/trade_referential_validator_test.dart` חדש: dangling `categoryId`/`tradeId`/`typeId` → quarantined; תקין → עובר; אינסטלציה-seed כבר FK-clean (אפס שינוי חי).
6. 🔒 **DoD** — `analyze` נקי · הטסט ירוק · כתיבה FK-משובשת לעולם לא live · `WIRING.md` מתעד את הולידטור.

### Step-Eα — `Trade.schemaVersion` + `migrate()` אמיתי (לא no-op) (R1-6)
1. 🎯 **היעד** — מסלול-שדרוג ממשי `migrate(json, fromVersion)` ב-`trades_store.dart`, לא no-op סמלי; doc-ישן עולה ל-current בלי לשבור.
2. 🔧 **איך בונים** — `migrate` עם step-functions פר-גרסה; `_load` עוטף `migrate`/`fromJson` ב-try/catch → keep-last-good (doc פגום אינו mבריק את ה-store).
3. ⚠️ **תקלות צפויות** — schema-חדש על client-ישן; `migrate`/`fromJson` זורק ומבריק קונפיג-שלם.
4. ✅ **פתרון** — keep-last-good + retry + "עדכן אפליקציה" (R2-7); מסלול-מיגרציה בָּדוּק שדה-אחר-שדה.
5. 🧪 **בדיקות** — `test/trade_migration_test.dart` חדש: מעלה doc-ישן ל-current ומאשר correctness שדה-אחר-שדה.
6. 🔒 **DoD** — `analyze` נקי · `trade_migration_test` ירוק · doc פגום → keep-last-good (לא crash) · אינסטלציה (reserved) ללא שינוי.

### Step-Eβ — מודל tombstone/archive לתחום ולמוצר · **+ cycle-guard ל-`replacedBy` (R2-11)**
1. 🎯 **היעד** — `Trade.archivedAt`/`TradeProduct.archivedAt` (או רשומת-tombstone נפרדת עם `replacedBy`) שמסתיר מ-live אך שומר ל-audit/migrate.
2. 🔧 **איך בונים** — שדה-archive + רשומת-tombstone; **רזולוציית-`replacedBy` עם visited-set/cycle-guard (A→B→A לא ייכנס ללולאה).**
3. ⚠️ **תקלות צפויות** — מחיקה משאירה יתומים (overrides/rules/search-index/orders/carts); **שרשרת `replacedBy` מעגלית → לולאה אינסופית.**
4. ✅ **פתרון** — archive מסתיר אך שומר; **visited-set שובר מעגל ומחזיר null/יתום מתועד במקום לולאה**; אינסטלציה (reserved) אינה ניתנת-ל-archive.
5. 🧪 **בדיקות** — `test/trade_archive_test.dart` חדש: archived מוסתר מ-live; audit נשמר; **`replacedBy` מעגלי נתפס ונשבר (לא אינסופי).**
6. 🔒 **DoD** — `analyze` נקי · `trade_archive_test` ירוק · cycle-guard מאומת · אינסטלציה לא-ניתנת-ל-archive (keystone).

### Step-Eγ — ניקוי-fan-out + migrate-map ב-archive (R1-6)
1. 🎯 **היעד** — בעת archive, fan-out על כל הפניה ל-id-המת + migrate-map `{deletedId → replacementId|null}` (re-point או mark-orphan, לעולם לא crash).
2. 🔧 **איך בונים** — מעבר על `AccessoryRule.linkSku`/`appliesToProductId`, `CompatibilityRule.aTypeId/bTypeId`, `CompletionRule.*TypeId`, search-index/synonyms (§6), orders/carts.
3. ⚠️ **תקלות צפויות** — הפניה-תלויה שנשארת מצביעה ל-id-מת → crash/יתום-שקט.
4. ✅ **פתרון** — migrate-map מפנה-מחדש או מסמן-יתום כל הפניה; הסרה-מ-union של search/finder.
5. 🧪 **בדיקות** — `test/archive_fanout_test.dart` חדש: כל הפניה-תלויה מופנית-מחדש או מסומנת; אף רפרנס-תלוי לא שורד.
6. 🔒 **DoD** — `analyze` נקי · `archive_fanout_test` ירוק · אפס dangling-ref אחרי archive · הכל דרך `tradesEditProvider`.

---

> **סיכום שערים (gate ledger) ל-31–50 + פאזה-G:** G-roundtrip (34) · **G-seed/keystone answer-equivalence + חבילת-פיזיקה מלאה (38, hard-gate-to-proceed)** · G-resolver (40) · G-authoring (44–47) · ולידציית-FK בפרסום (47) · G-import + "categoryId exists" (48) · G-flag-off + `brand_profile_parity_test` (49) · **G-newtrade + `CompletionRule` נורה/חוסם (50)** · `onCatalogProductWrite` FK (Step-FK) · `trade_migration_test` (Eα) · `trade_archive_test`+cycle-guard (Eβ) · `archive_fanout_test` (Eγ). כל שלב נשלח עצמאית מאחורי דגל default-OFF → בילד דמו/חי נשאר byte-identical (דגל-קומפילציה OFF); **אינסטלציה answer-stable מול fixtures, פיזיקה קשיחה לנצח, reserved ולא-ניתנת-ל-archive (ה-keystone שמור)**; governance #84 נשמר (presentation/behavior בלבד, אפס HR/role/auth).
