# BuildSmart Studio — DEEP detail, steps 69–85 (Pillar 4: AI Co-Editor + Behavior/Component Builder)

> ⚠️ קנוני = מקטע 🔧 תיקוני Red-Team R1 (בסוף `04-ai-coeditor-behavior.md`) + RED-TEAM-R1/R2.md. ה-appendix גובר.
> כל תת-שלב למטה הוחל עם הכרעות-R1/R2. בקצרה: **gate #119** (P1=#118, P3=#120) · פרסונה=`roleProvider` (String?, null=קבלן) **לא** `BsRole` · כתיבה רק דרך **`applyOps`+undo של P1** · `validateSafe` בצד-client = advisory, **רץ-שוב בשרת ב-`publishConfig`** · closed-sets מ-`ElementDescriptor` קפוא של P1, **fail-closed כשחסר** · `nav.screen` רק ~38 no-arg (typed-arg = GREYED "צריך פרמטרים — לא זמין") · `criticalBusiness`+contrast **בתוך** validateSafe · session-budget מצטבר + ">K% מהרישום" · `SetProp(color)` subset פר-kind · truncated-JSON → "לא הצלחתי" · `registryViewContract` (fake↔real) + `studio_safety_test` מול הרישום-האמיתי-הקפוא = **חובה לפני gate-119** · property-invariant fuzz = REQUIRED. כל שייר-עיצוב-ישן בגוף — בטל.

> **Branch:** `claude/whats-happening-LyY9G` · **Project:** `app_flutter/` (Flutter 3.29 · Dart 3.7 · Riverpod) · **Phase 5 of the 100-step Studio plan.**
> **Source:** `knowledge/studio-plan/steps/04-steps.md` + `knowledge/studio-plan/04-ai-coeditor-behavior.md`. Every `file:line` re-confirmed on this worktree (2026-06-23).
>
> **Zero-hallucination spine (כל צעד מכבד):** המודל רק **נוקב** במפתחות מתוך CLOSED SET; Dart דטרמיניסטי בונה את ה-diff המאומת. שימוש-חוזר: `matchRecipe` (`describe_to_cart_screen.dart:50-67`), `matchAssistantRecipeKey`/`matchAssistantCategory` (`assistant_intent.dart:58-95`), TOTAL parser `parseAssistantIntent` (`assistant_intent.dart:168-210`), `promptSafeText` (`prompt_sanitize.dart:19-27`), והתפר `ClaudeGateway`/`claudeGatewayProvider` (`claude_functions.dart:56-67,133-136`) — **אין gateway חדש**.
> **דגל `kStudioCoEditor`** (`bool.fromEnvironment('STUDIO_CO_EDITOR')`, ברירת-מחדל OFF, מראה את `kClaudeAi` `backend.dart:138`) → demo build byte-identical. ה-builder הידני עובד **בלי AI**. שרת: auth `claude.ts:117-119`, rate 40/min `claude.ts:49-50,75-103`, model-allowlist `claude.ts:35-38`, prompt-cap 8000 `claude.ts:40,127-132`, tokens-cap 2048 `claude.ts:41`. **`validateSafe` רץ-שוב בשרת בתוך `publishConfig`** (client = advisory בלבד, R1-1). 100-gate בכל commit. Governance #84 (no-HR/no-auth-bypass — closed by omission). שער שמור **#119 = P4 AI-grounded-config** (P1=#118, P3=#120; `GATE_REGISTRY.md:11-13`).
> **Gate baseline:** `flutter analyze` (#31) · `flutter test` (#32) · `flutter build web` (#33) · `knowledge_protocol_test` (#94) · `WIRING.md` (#24).

---

## Step 69 — closed `ConfigOp` sealed family + JSON round-trip

1. 🎯 **היעד** · מטבע-הקוד המשותף לכל ה-pillar: משפחת `ConfigOp` סגורה (`SetProp`/`SetVisible`/`AddComponent`/`SetAction`/`SetText`/`AddRule`) עם `toJson`/`fromJson`, כדי ש-Pillar 1 יוכל לאחסן בטיוטה. **`SetVisible{roleProvider: String?, visible}`** (null=קבלן) — הפרסונה מקודדת ב-`roleProvider`, **לא** ב-`BsRole` (R1-3).
2. 🔧 **איך בונים** · קובץ NEW `lib/logic/studio/config_op.dart`; sealed class בסגנון `smart_cart.dart` `toJson`/`fromJson`-per-line. data טהור בלבד — שדה-הפרסונה הוא `roleProvider` (String?, null=קבלן), לא enum; ה-enums (`BsRole`/`BoardRole`) הם adapter-תצוגה בלבד מחוץ למודל. שום widget/gateway. שדות מאומתים כבר במקור (step 75) לפני שהופכים ל-`ConfigOp`.
3. ⚠️ **תקלות צפויות** · round-trip לא-סימטרי (enum כ-`int` index שזז כשמוסיפים variant) → דריפט שקט; `fromJson` עם op-tag לא-מוכר → throw במקום degrade.
4. ✅ **פתרון** · serialize enums כ-**string name** לא index; `fromJson` על tag לא-מוכר מחזיר `null`/דורש סינון, לעולם לא throw (מראה את `parseAssistantIntent` `assistant_intent.dart:172-173`). round-trip-test לכל variant.
5. 🧪 **בדיקות** · `test/studio_config_op_test.dart`: `fromJson(toJson(op))==op` לכל ששת ה-variants; tag לא-מוכר→null/drop; JSON חלקי לא זורק. grounding-test לא נדרש כאן (data טהור).
6. 📈 **שיפור** · `schemaVersion` בתוך ה-JSON (כמו `'bs.feature-flags.v1'` `feature_flags.dart`) כדי ש-migration עתידי לא ישבור טיוטות מאוחסנות.
7. 🤔 **ריאלי?** · כן — מודל data טהור, אפס תלות, מראה idiom קיים של `smart_cart.dart`.
8. 🔒 **ווידוא-פיקס (DoD)** · `flutter analyze` 0; round-trip-test ירוק לכל variant; אפס imports מעבר ל-enums; `git grep` מאשר שאף op לא נשמר עם enum-index.
9. 💡 **תוספת-א** · `ConfigOp.kind` getter (enum) לקיבוץ ב-`summarizeDiff` (step 79) בלי `is`-laddering.
10. 💡 **תוספת-ב** · `==`/`hashCode` value-based (Dart 3.7 records בשדות) כדי שבדיקות undo/diff ישוו ב-value, לא ב-identity.

---

## Step 70 — fake in-memory `ElementRegistry` + frozen query interface (test-only)

1. 🎯 **היעד** · interface קפוא + fake-registry in-memory כדי שה-pillar ייבנה/ייבדק **לפני** ש-Pillar 1 נוחת (משמעת ה-fake-gateway, מוחלת על ה-registry).
2. 🔧 **איך בונים** · NEW `lib/logic/studio/registry_view.dart` — abstract `RegistryView` (`elementIds`, `propKeysFor`, `allowedValues`, `actionIdsFor`, `componentTypes`) + `FakeRegistryView`. מראה את משמעת `ClaudeGateway`/fake (`claude_functions.dart:56-67`). אין תלות ב-Pillar 1 עדיין.
3. ⚠️ **תקלות צפויות** · ה-fake "נדיב מדי" (מחזיר כל id) → בדיקות grounding ירוקות-שקר; **self-certification**: אם `validateSafe` נבדק רק מול fake של אותו-מחבר הוא ירוק-מובנה ו-vacuous (R2-15); interface churn כש-Pillar 1 נוחת מאלץ עריכה רוחבית.
4. ✅ **פתרון** · fake = **scaffolding בלבד**; מקור-ה-grounding האמיתי = `ElementDescriptor` **קפוא** של P1 (R1-2). `registryViewContract(RegistryView Function())` שגם ה-fake וגם **המתאם-האמיתי של P1** חייבים לעבור = **חובה** (לא 💡); **`studio_safety_test` חייב לעבור מול הרישום-האמיתי-הקפוא לפני שער-119** — fake-self-cert הוא vacuous (R2-15). שדה-registry חסר → **fail-closed** (drop-all), לא pass-all. **כל** ידע Pillar-1 מבודד בקובץ היחיד הזה — אם הצורה זזה, רק המתאם משתנה.
5. 🧪 **בדיקות** · `test/studio_registry_view_test.dart`: ה-fake מחזיר בדיוק את הסטים שהוזרקו; query על id לא-קיים→סט ריק/`null`, לא throw; `registryViewContract` ירוק ל-fake וגם למתאם-P1-האמיתי (MANDATORY).
6. 📈 **שיפור** · builder ל-fake (`FakeRegistryView.of(elements: …)`) כדי שכל בדיקת-grounding תבנה registry מינימלי משלה — בידוד-בדיקות.
7. 🤔 **ריאלי?** · כן — fake בלבד; ה-interface האמיתי הוא חוזה Phase-0 מול Pillar 1 (BLOCKING §11), והקובץ הזה הוא ה-seam היחיד.
8. 🔒 **ווידוא-פיקס (DoD)** · interface מתועד ב-`WIRING.md`; fake עובר את הבדיקות; **`registryViewContract` ירוק מול ה-fake וגם מול מתאם-P1-האמיתי-הקפוא (חובה, R2-15)**; `git grep` מאשר שאף קובץ studio אחר לא מייבא טיפוס קונקרטי של Pillar 1 — רק `RegistryView`.
9. 💡 **תוספת-א** · `RegistryView.frozen()` immutable-snapshot כדי ש-race בין prompt-build ל-parse יראה אותה תמונת-registry.
10. ✅ **חובה (R2-15, הועלה מ-💡)** · contract-test משותף (`registryViewContract(RegistryView Function())`) שגם ה-fake וגם המתאם-האמיתי של Pillar 1 **חייבים** לעבור — מונע ירוק-עצמי-מתעתע; שער-119 לא נסגר עד שהוא ירוק מול הרישום-האמיתי.

---

## Step 71 — registry closed-set matchers (exact→longest-contained, null-on-miss)

1. 🎯 **היעד** · resolvers שמהם המודל מוגבל: `matchElementId`/`matchPropKey`/`matchValue`/`matchActionId`/`matchComponentType` — exact ואז longest-contained, אחרת `null` (degrade).
2. 🔧 **איך בונים** · ב-`lib/logic/studio/registry_view.dart`; **העתקה 1:1** של `matchRecipe` (`describe_to_cart_screen.dart:50-67`) ושל `matchAssistantRecipeKey` (`assistant_intent.dart:80-95`). resolvers טהורים מעל ה-fake.
3. ⚠️ **תקלות צפויות** · prefix-collision (id קצר ⊂ id ארוך, כמו `faucet⊂kitchenFaucet` ב-`describe_to_cart_screen.dart:57-58`) → first-match בוחר את הקצר הלא-נכון; ריק/whitespace מחזיר match שגוי.
4. ✅ **פתרון** · longest-contained (כמו ב-מקור: `c.length > best.length`); guard מוקדם `if (r.isEmpty) return null` (`assistant_intent.dart:60,82`). שדה-registry חסר/ריק (`ElementDescriptor` עוד-לא-קפוא, R1-2) → **fail-closed**: מחזיר `null`/drop-all, **לעולם לא** pass-all (`validateSafe` ריק = vacuous). בדיקה מול ה-fake **וגם** מול מתאם-P1-האמיתי (`registryViewContract`).
5. 🧪 **בדיקות (grounding)** · `test/studio_registry_view_test.dart`: id/prop/value מומצא→`null`; prefix-collision בוחר את הארוך; reply עטוף-ב-prose עדיין מחזיר match נכון (מראה את `assistant_intent_test.dart:121-146`).
6. 📈 **שיפור** · נרמול קל (lowercase/trim של תווי-עברית מנוקדים) לפני ההשוואה כדי לתפוס וריאציות-ניסוח בלי לרכך את הסגירות.
7. 🤔 **ריאלי?** · כן — העתקה ישירה של דפוס מוכח ומבוקר (`matchRecipe`), מעל fake.
8. 🔒 **ווידוא-פיקס (DoD)** · invented-id-test ירוק (ה-defect-class המרכזי); prefix-collision-test ירוק; `flutter analyze` 0; ה-matchers הם הנתיב **היחיד** מ-string→key (אין השוואת-string ישירה במקום אחר).
9. 💡 **תוספת-א** · `matchAll(reply)` שמחזיר את כל ה-keys המוכלים (לא רק הטוב-ביותר) — שימושי ל-scope-expansion (step 76) כשה-utterance נוקב בכמה.
10. ✅ **חובה (R2-fuzz, הועלה מ-💡)** · property-test fuzz מעל **אלפי מחרוזות אקראיות** → תמיד `null` או key אמיתי, לעולם לא throw — חלק מ-property-invariant ה-REQUIRED של שער-119 (step 85), לא דגימות-ספורות.

---

## Step 72 — static action-catalog grounded in real `route()`s + `open*Sheet`s

1. 🎯 **היעד** · קטלוג סגור של "מה כפתור עושה" — כל `ActionId` ממופה ל-effect דטרמיניסטי, מעוגן בניווט/sheet האמיתי של האפליקציה.
2. 🔧 **איך בונים** · NEW `lib/logic/studio/action_catalog.dart`; מקורות: `manager_copilot_screen.dart:32` (route idiom), `contractor_tools_sheets.dart:26,39` (`openScanPlanSheet`/`openCheaperAlternativesSheet`), `ai_assistant_screen.dart:210-222` (`cart.add` write), `reject_reason_screen.dart:110-117` (`share.text` Clipboard), `ai_hub_screen.dart:113` (`cart.open` `mainTabProvider`). data סטטי; **אין** action ניווט/auth (closed by omission §7).
3. ⚠️ **תקלות צפויות** · `nav.screen` מצביע למסך שלא קיים → crash; מסך עם typed-arg (11/49) בלי arg-builder → ניחוש-arg/write שגוי; **drop שקט** של מסך-typed-arg מבלבל את הבעלים ("למה לא הופיע?").
4. ✅ **פתרון** · `nav.screen` תקף **רק ל-~38 מסכי-no-arg** (R1-5); `matchScreenId` מקרקע מול ה-closed-set של ה-no-arg בלבד. 11 מסכי-typed-arg = **out-of-v1** (דורשים arg-builders פר-מסך שאינם קיימים) — ה-builder הידני **מאפיר אותם** עם "צריך פרמטרים — לא זמין" (**לא drop שקט**); ב-NL הם degrade. אין arg חופשי שמגיע ל-effect.
5. 🧪 **בדיקות (grounding)** · `test/studio_action_catalog_test.dart`: כל `ActionId`→effect; יעדי `nav.screen` רק מ-~38 ה-no-arg-routes; מסך-typed-arg → GREYED-not-droppable ב-builder + degrade ב-NL; אף action בקטלוג אינו mutation מחוץ ל-confirm-gate.
6. 📈 **שיפור** · `ActionDescriptor.he` (תווית עברית) ליד ה-id, כדי ש-preview (step 79) וה-builder הידני (step 82) יציגו טקסט אנושי בלי טבלת-תרגום נפרדת.
7. 🤔 **ריאלי?** · חלקי — הקטלוג ההתחלתי (7 פעולות, §4) ריאלי לגמרי; כיסוי **כל** מסך נדחה לפי הצורך (closed-set גדל בזהירות, "Extend deliberately" `claude.ts:38`).
8. 🔒 **ווידוא-פיקס (DoD)** · בדיקת "no-auth/no-nav-structure action בקטלוג" ירוקה (governance #84); כל effect-id resolvable; `flutter analyze` 0.
9. 💡 **תוספת-א** · `actionIdsFor(elementId)` שמצמצם את הקטלוג לפי הקשר (כפתור ב-context קריאה-בלבד לא רואה mutators) — מקדים את safety §7.5.
10. 💡 **תוספת-ב** · golden-list test: קובץ-snapshot של כל ה-ActionIds, כך שהוספת/הסרת פעולה דורשת עדכון מפורש (שער נגד הרחבת-קטלוג שקטה).

---

## Step 73 — static component-palette templates with typed required-prop schemas

1. 🎯 **היעד** · סט סגור של רכיבים שאפשר להוסיף (`button`/`textBlock`/`badge`/`divider`/`infoCard`/`linkRow`), כל אחד עם סכמת-props נדרשת מטופסת.
2. 🔧 **איך בונים** · NEW `lib/logic/studio/component_palette.dart`; `ComponentTemplate{type, he, allowedContainers:Set<ElementKind>, requiredProps, optionalAction}` (§5). data סטטי; ה-rendering נשאר עבודת ה-wrapper של Pillar 1 — לא בונים widgets כאן.
3. ⚠️ **תקלות צפויות** · הוספת רכיב לתוך container לא-חוקי ("כפתור לתוך auth") → diff פגום/UI שבור; prop נדרש חסר → `AddComponent` חצי-בנוי.
4. ✅ **פתרון** · `allowedContainers` נאכף מול `element.kind` מה-registry (`card.order.kind==card`); `requiredProps` חייבים להיות מסופקים אחרת ה-op נדחה; auth-kind לא ב-`allowedContainers` של אף template (וגם §7.2 חוסם).
5. 🧪 **בדיקות** · `test/studio_palette_test.dart`: `allowedContainers` מול `element.kind` נאכף; template→container לא-חוקי נדחה; `requiredProps` חסר→drop.
6. 📈 **שיפור** · `label`/`text` props עוברים `promptSafeText` (`prompt_sanitize.dart:19-27`) — אזהרת injection-lever כש-owner עורך אלמנט אחר ב-NL שתוויתו מהדהדת (§5).
7. 🤔 **ריאלי?** · כן — 6 templates סטטיים, סכמה טהורה; ה-rendering מתואם עם Pillar 1, לא נבנה כאן.
8. 🔒 **ווידוא-פיקס (DoD)** · invalid-container-test ירוק; missing-required-prop-test ירוק; `flutter analyze` 0; אין widget-construction בקובץ (`git grep` של `Widget`/`build`).
9. 💡 **תוספת-א** · `optionalAction:true` ל-`button`/`linkRow` מאמת את ה-action המוצמד מול `actionIdsFor(container)` (step 72) — רכיב חדש לא יכול לקבל פעולה לא-חוקית להקשרו.
10. 💡 **תוספת-ב** · `maxPerContainer` ל-template (למשל `divider`) כדי למנוע הצפה אקראית של אותו רכיב מ-broadcast.

---

## Step 74 — grounded prompt builder embedding closed sets + strict-JSON ops grammar

1. 🎯 **היעד** · `studioEditPrompt(...)` + `studioEditSystem` שמטמיעים את הסטים הסגורים ודורשים JSON-ops קפדני; בונה-string טהור, **ללא** קריאת-gateway.
2. 🔧 **איך בונים** · NEW `lib/logic/studio/edit_prompt.dart`; קידוד compact `id=he` (idiom `describe_to_cart_screen.dart:34`, `assistant_intent.dart:128`); `promptSafeText` cap על ה-utterance (`prompt_sanitize.dart:19-27`); two-stage scope (§9.1) כדי להישאר מתחת 8000 (`claude.ts:40`).
3. ⚠️ **תקלות צפויות** · registry גדול חורג מ-8000-char (`claude.ts:127-132` → `invalid-argument`); utterance עוין מזריק "התעלם מההוראות"; המודל מקבל רשות לחבר ops חופשיים; Stage-A מסווג ל-אזור-חופשי (לא closed-set) → scope לא-מאומת; utterance מעורפל → ניחוש-scope שגוי.
4. ✅ **פתרון** · two-stage: **Stage-A = scope-classifier ל-closed-set token מהרישום** (R1-7) — לא חלוקת-טקסט-חופשי; utterance **מעורפל → "שאל הבהרה"** (לא ניחוש). Stage-B מטמיע רק את הפרוסה הרלוונטית (כמה עשרות ids); ה-scope-שזוהה **מוצג ב-preview** ("מתוך: לשונית רכש" / "מתוך: כל הכפתורים") לאישור-היעד לפני שמראים שינויים. `promptSafeText(utterance, maxLen:600, collapseWhitespace)` מקפל newlines (injection-lever, `prompt_sanitize.dart:8-9,22-23`); ה-system דורש "נקוב ops מהרשימות הסגורות; לעולם אל תמציא id/prop/value" (מראה את `assistantIntentSystem` `assistant_intent.dart:160-163`).
5. 🧪 **בדיקות (grounding)** · `test/studio_edit_prompt_test.dart`: הסטים מוטמעים בפלט; utterance מקופל+capped; **Stage-A מחזיר closed-set token מהרישום + ambiguous→clarify**; ה-scope-token מופיע בשורת-ה-preview ("מתוך: …"); two-stage scope <8000 chars ל-registry סינתטי גדול (מראה את cap-test של `manager_copilot_test.dart`).
6. 📈 **שיפור** · `maxTokens` מוצמד קטן (~256, כמו `ai_assistant_screen.dart:108`) ב-system-hints — תשובת-ops קצרה, רחוק מ-cap 2048 (`claude.ts:41`).
7. 🤔 **ריאלי?** · כן — בונה-string טהור; two-stage הוא ה-mitigation המתוכנן ל-8000-char (§12 Risk-table), מוכח-עקרון ע"י finder/assistant.
8. 🔒 **ווידוא-פיקס (DoD)** · prompt-fits-8000-test ירוק ל-registry סינתטי גדול; utterance-capped-test ירוק; אין קריאת-gateway בקובץ (`git grep` של `ask(`); `flutter analyze` 0.
9. 💡 **תוספת-א** · budget-assertion ב-debug (`assert(prompt.length<=8000)`) שנכשל מהר בפיתוח לפני round-trip לשרת.
10. 💡 **תוספת-ב** · few-shot זעיר (דוגמת-JSON אחת תקינה) בתוך ה-prompt — מעלה דיוק closed-set בלי לפרוץ את התקציב; הדוגמה עצמה מ-ids אמיתיים של ה-registry.

---

## Step 75 — TOTAL `parseConfigEdit` validating EVERY field vs registry, dropping invented

1. 🎯 **היעד** · הליבה האנטי-הזיה: `parseConfigEdit(String raw, RegistryView) → List<ConfigOp>` — מאמת **כל שדה** מול ה-registry, מפיל ops לא-חוקיים, **לעולם לא זורק**.
2. 🔧 **איך בונים** · NEW `lib/logic/studio/edit_intent.dart`; **העתקת** `parseAssistantIntent` (`assistant_intent.dart:168-210`): `indexOf('{')`/`lastIndexOf('}')`, `jsonDecode` ב-try, אימות-שדה-שדה דרך ה-matchers (step 71). **truncation-guard** קודם-כל: `finish_reason` שאינו `stop`/`end_turn` (נחתך ע"י `maxTokens`) או `}` חסר/סוגריים-לא-מאוזנים → "לא הצלחתי לבנות את השינוי, נסה שוב" (R1-10), **לא** preview-חלקי. דטרמיניסטי, gateway-free.
3. ⚠️ **תקלות צפויות** · המודל ממציא id/prop/value/action/component (הזיה); JSON עטוף-prose/חלקי/`{}` → throw; op עם שדה אחד תקין ואחד מומצא נכנס חצי-מאומת; **JSON תקף-תחבירית-אך-קטוע** נפרס חלקית ל-preview שגוי.
4. ✅ **פתרון** · truncated → fail-as-unit (לא ops-חלקיים, R1-10); כל שדה עובר `match*`→null-on-miss→**drop ה-op כולו** (מראה את `assistant_intent.dart:189-204`); כל-non-JSON/malformed→**list ריק** (לא throw, `assistant_intent.dart:172-173,207-208`); op נכנס רק כשכל שדותיו אומתו.
5. 🧪 **בדיקות (grounding)** · `test/studio_edit_intent_test.dart`: op תקין parses ונושא שדות-מאומתים; id/prop/value/action/component מומצא → drop כל אחד (מראה את `assistant_intent_test.dart:28-33`); malformed/non-JSON/partial/`{}`/prose-wrapped → list ריק, לעולם לא throw (`assistant_intent_test.dart:40-61`); **truncated-valid-JSON (finish_reason≠stop / `}` חסר) → "לא הצלחתי", לא preview-חלקי** (מקרה נפרד מ-non-JSON); longest-contained מנצח ב-reply עטוף (`assistant_intent_test.dart:121-146`).
6. 📈 **שיפור** · החזרת `(ops, droppedCount)` כדי ש-preview יציג "X לא הובנו" — שקיפות במקום בליעה שקטה (§7.8).
7. 🤔 **ריאלי?** · כן — זה לב ה-pillar והוא העתק-מבנה של פונקציה מוכחת-קרב (`parseAssistantIntent`); אותה ערובה כמו `matchRecipe`.
8. 🔒 **ווידוא-פיקס (DoD)** · invented-X-test ירוק לכל חמשת סוגי השדות; truncated-JSON→empty+fail-test ירוק; never-throws **property-invariant fuzz** (∀op כל שדה ∈ registry, או ריק, לעולם לא throw) רץ על **אלפי תשובות אקראיות** ירוק; `flutter analyze` 0; **שער #119** ("`parseConfigEdit` מפיל כל id/prop/value/action/component מומצא") נרשם (step 85).
9. 💡 **תוספת-א** · partial-op tolerance: op עם prop לא-חוקי אך target חוקי לא מפיל ops **אחרים** באותה תשובה (חוסן per-op, לא all-or-nothing).
10. ✅ **חובה (R2-fuzz, הועלה מ-💡)** · property-invariant: לכל op ב-`parseConfigEdit(reply, reg)`, **כל שדה** ∈ registry (`reg.elementIds().contains(op.target)` וכו'), או list ריק, **לעולם לא throw** — נאכף מכנית כ-**REQUIRED fuzz של שער-119 על אלפי תשובות אקראיות** (step 85), מעל ה-fake **וגם** מעל מתאם-P1-האמיתי (R2-15).

---

## Step 76 — expand closed `scope` tokens to concrete ids via registry (not model enumeration)

1. 🎯 **היעד** · broadcast ("כל הכפתורים", "בכל כרטיס-הזמנה") = הרחבת token-scope סגור ל-ids קונקרטיים דרך ה-registry — **לא** אמון במודל למנות ids.
2. 🔧 **איך בונים** · ב-`lib/logic/studio/edit_intent.dart`; scope-set סגור (`all-buttons`/`every:<id>`/`single:<id>`); הרחבה דטרמיניסטית `elementIds().where(kind==button)` מעל ה-fake. broadcast = N ops `SetProp` מאומתים ב-נפרד מעל ids אמיתיים.
3. ⚠️ **תקלות צפויות** · המודל מחזיר רשימת-ids ישירה (חלקם מומצאים); scope פוגע ב-40 כפתורים בלי תקרה → rewrite של חצי-אפליקציה (DoS/foot-gun §7.6).
4. ✅ **פתרון** · המודל נוקב **token** מסט-scope סגור בלבד; Dart מרחיב מעל ids אמיתיים (כל אחד עובר את הצינור של step 75); תקרת-batch (step 78) נאכפת בהמשך. broadcast ל-40 כפתורים = 40 `SetProp` בטוחים-בנפרד (§3.3).
5. 🧪 **בדיקות (grounding)** · `test/studio_edit_intent_test.dart`: `all-buttons`→N ops `SetProp` מעוגנים; תקרה מכובדת; `every:<id>` עם container לא-קיים→ריק; `single:<missing>`→drop.
6. 📈 **שיפור** · scope-token עם `he` (תווית) ב-preview ("כל הכפתורים: 12 שינויים") — מתחבר ל-broadcast-count של step 79.
7. 🤔 **ריאלי?** · כן — הרחבה דטרמיניסטית מעל ה-registry; ה-scope-set קטן וסגור (3 צורות), מאומת כמו כל closed-set.
8. 🔒 **ווידוא-פיקס (DoD)** · scope-expansion-test ירוק (N ids אמיתיים); אף id לא מגיע מהמודל ישירות (`git grep` מאשר שאין parse של מערך-ids מ-reply); `flutter analyze` 0.
9. 💡 **תוספת-א** · `every:<kind>` בנוסף ל-`every:<id>` ("כל הכרטיסים") — הרחבה לפי `ElementKind` מה-registry, עדיין ids אמיתיים בלבד.
10. 💡 **תוספת-ב** · dry-count מוקדם: לפני בניית N ops, ספירת היעדים והשוואה ל-`kStudioMaxBatch` (step 78) — דחייה מוקדמת חוסכת בנייה מיותרת.

---

## Step 77 — `validateSafe` nav/auth immutability + per-element/value/action legality

1. 🎯 **היעד** · backstop מפורש מעל ה-grounding: `validateSafe(List<ConfigOp>, RegistryView) → SafetyVerdict` — אי-שינוי nav/auth + floor `criticalBusiness` + contrast + חוקיות prop-type/action per-element. ⚠️ ה-client-`validateSafe` הוא **advisory/עקיף בלבד**; אותה לוגיקה **רצה-שוב בשרת בתוך `publishConfig`** (R1-1) — client שעבר אינו ערובה.
2. 🔧 **איך בונים** · NEW `lib/logic/studio/edit_safety.dart`; `kImmutable` (role-picker/login-boards/manager-session/bottom-nav) — rationale "המנהל לא מתנתק" `manager_dashboard_screen.dart:200-209` + board-isolation `sys_chat.dart` (§2.5); `kCriticalBusiness` + ה-color-subsets **נגזרים מ-`ElementDescriptor` הקפוא של P1** (R1-2), **fail-closed כשחסר**. טהור.
3. ⚠️ **תקלות צפויות** · owner מסתיר login / מוחק tab / משנה תווית "יציאה"; **legal-but-harmful**: מסתיר מחיר / משנה-מסתיר "אשר הזמנה" / יוצר כשל-ניגודיות דרך token-לגיטימי-אך-לא-מתאים; `SetProp(color)` עם hex שרירותי; `SetAction` מחווט mutation בהקשר קריאה-בלבד.
4. ✅ **פתרון** · אלמנט ב-`kImmutable` דוחה **כל** op (`SetVisible`/`SetProp`/`AddComponent`-לתוכו/`SetText`-על-תוויותיו, §7.2); floor **`criticalBusiness`** (R1-6): **אי-אפשר להסתיר מחיר**, **אי-אפשר לשנות/להסתיר "אשר הזמנה"**, ו-**contrast-check רץ בתוך `validateSafe`** — `SetProp(color)`/`SetText` שמוריד ניגודיות מתחת WCAG-AA על element קריטי → blocked עם נימוק-עברית (לא נשען על לינט-CI #61/#64); `SetProp(color)` מקבל רק ערך ב-`allowedValues(id,'color')` שהוא **subset פר-element-kind** (R1-9), חיתוך של `BsTokens` — לא כל הפלטה, אין hex מהמודל ("ירוק"→token-מותג רק אם חוקי-ל-kind §7.4); `SetAction` מאומת מול `actionIdsFor(id)` (§7.5).
5. 🧪 **בדיקות** · `test/studio_safety_test.dart`: אלמנט immutable דוחה את כל ה-ops; **hide-price / relabel-or-hide "אשר הזמנה" / contrast-fail על element קריטי → blocked** (R1-6); `SetProp(color)` out-of-subset-for-kind → drop/blocked (R1-9); type-mismatch (`SetProp` ערך מחוץ-לפלטה) חסום; action לא-חוקי-להקשר חסום; כל חסום נושא `reasonHe`.
6. 📈 **שיפור** · `SafetyVerdict{applied, blocked:List<(op,reasonHe)>}` — ה-blocked מוצג למשתמש (§7.8), לעולם לא נבלע בשקט.
7. 🤔 **ריאלי?** · כן — backstop טהור מעל closed-set שכבר מסונן; ה-`kImmutable`/`criticalBusiness` מעוגנים בכלל-מוצר קיים (`manager_dashboard_screen.dart:200-209`); אכיפת-האמת היא העתק-השרת ב-`publishConfig` (R1-1).
8. 🔒 **ווידוא-פיקס (DoD)** · immutable-rejects-all-test ירוק; hide-price/approve-order/contrast-blocked-test ירוק; color-subset-for-kind-test ירוק; type/action-mismatch-blocked-test ירוק; כל blocked מחזיר `reasonHe` לא-ריק; שדה-registry חסר → fail-closed; `flutter analyze` 0.
9. 💡 **תוספת-א** · `kImmutable` מקבל גם **prop-level** freeze (אלמנט גלוי-לעריכה אך ה-`visible` שלו נעול) — גרנולריות עדינה יותר מ-all-or-nothing.
10. 💡 **תוספת-ב** · audit-log טהור (`List<BlockedEntry>`) שאפשר לדמפ ל-`visual_log` (#107/#116) — עקבות-החלטה לכל חסימה.

---

## Step 78 — role-visibility floor + batch ceiling in `validateSafe` (Hebrew reasons)

1. 🎯 **היעד** · רצפת-נראות-לפי-פרסונה + תקרת-batch + **תקציב-סשן מצטבר** ל-`validateSafe`, עם סיבות עברית — מונע "הסתר הכל מכולם" ו-rewrite נרחב. (אכיפת-אמת = העתק-השרת ב-`publishConfig`, R1-1.)
2. 🔧 **איך בונים** · ב-`lib/logic/studio/edit_safety.dart`; `kRoleFloor` מחושב מעל **מפת-`roleProvider`** (String?, null=קבלן) — **לא** מעל `BsRole` enum שמשמיט קבלן (R1-3); `kStudioMaxBatch` per-utterance (~80, §7.6) **+ `kStudioSessionBudget`** (סך-ops מצטבר על-פני utterances באותו draft) **+ `kStudioMaxRegistryFraction`** (">K% מהרישום", למשל 25%, R1-8). `SafetyVerdict{applied, blocked}`; שום-דבר לא יתום.
3. ⚠️ **תקלות צפויות** · הסתרת surface קריטי מ-`manager`; הסתרת tab מ-**כל** הפרסונות (יתום); עריכה פר-קבלן (null) נופלת מה-enum → "נשמר, כלום לא קורה"; broadcast מעל התקרה → DoS/foot-gun/cost; הצטברות-ops על-פני utterances חורגת בלי per-utterance להפר.
4. ✅ **פתרון** · `kRoleFloor` מעל מפת-`roleProvider` מבטיח מינימום (manager-critical לא יוסתר מ-`manager`; tab לא יוסתר מכל הפרסונות **כולל קבלן=null** §7.3); `kStudioMaxBatch` דוחה הרחבה מעל התקרה ("השינוי נרחב מדי — צמצם"); `kStudioSessionBudget` דוחה כשהסך-המצטבר לסשן חורג; `kStudioMaxRegistryFraction` דוחה כש-ה-diff נוגע ב->K% מהרישום ("השינוי נרחב מדי לסשן"). כל חסום נושא `reasonHe`. אותן תקרות נאכפות-שוב בשרת (R1-1).
5. 🧪 **בדיקות** · `test/studio_safety_test.dart`: אי-אפשר להסתיר מ-`manager` / מ-EVERY persona (כולל קבלן) / מעל per-utterance-ceiling / **מעל session-budget מצטבר** / **מעל registry-fraction**; כל blocked נושא `reasonHe`; `SetVisible{roleProvider}` חוקי (הסתרה מפרסונה בודדת) עובר.
6. 📈 **שיפור** · התקרות הן גם guard-עלות (broadcast ענק = הרבה ops/diff) — מתחבר ל-§9 cost-notes ול-batched-call (step 85/Phase-5).
7. 🤔 **ריאלי?** · כן — בדיקות-גבול טהורות מעל ה-verdict; `roleProvider` (String?) הוא מקור-האמת-החי.
8. 🔒 **ווידוא-פיקס (DoD)** · hide-from-manager-blocked, hide-from-every-persona-blocked (כולל קבלן), over-utterance-ceiling, over-session-budget, over-registry-fraction — כולם ירוקים; כל blocked עם `reasonHe`; `flutter analyze` 0.
9. 💡 **תוספת-א** · `kRoleFloor` נגזר מהרישום (`roleScoped`/`kImmutable` מעל מפת-`roleProvider`) ולא קבוע-קשיח — מתעדכן כשהמודל גדל, בלי לגעת ב-safety.
10. 💡 **תוספת-ב** · "soft-ceiling" אזהרה (>40) לפני "hard-ceiling" דחייה (>80) — UX עדין: מאשרים-בכוונה batch בינוני, חוסמים ענק.

---

## Step 79 — pure Hebrew `summarizeDiff` preview rows (broadcast + blocked count)

1. 🎯 **היעד** · `summarizeDiff(List<ConfigOp>, RegistryView) → List<DiffLine>` — שורות-תצוגה עבריות אנושיות, broadcast-count + blocked-count; טהור, אפס מודל — re-preview חינם.
2. 🔧 **איך בונים** · NEW `lib/logic/studio/diff_preview.dart`; טהור Dart מעל ה-`he` של action-catalog (step 72) ו-palette (step 73). preview הוא חינם (§9 "Preview is free").
3. ⚠️ **תקלות צפויות** · broadcast של 40 ops מציג 40 שורות (רעש) במקום "40 שינויים"; ספירת-blocked שגויה מטעה את ה-owner; hard-coded LTR ב-מספרים (#62/#63).
4. ✅ **פתרון** · קיבוץ broadcast לשורה אחת "N שינויים"; ספירת-blocked מ-`SafetyVerdict.blocked` ("נחסמו K"); RTL מלא, אין left/right קשיח.
5. 🧪 **בדיקות** · `test/studio_diff_preview_test.dart`: שורות נכונות; broadcast→"N שינויים"; "נחסמו K" מוצג; טקסט עברי מ-legacy-emoji בלבד (#61/#64).
6. 📈 **שיפור** · diff-line נושא `op` המקורי (לא רק טקסט) → tap-to-edit/tap-to-remove ב-builder (step 82) בלי re-parse.
7. 🤔 **ריאלי?** · כן — מיפוי טהור ops→strings; אפס I/O, אפס מודל.
8. 🔒 **ווידוא-פיקס (DoD)** · broadcast-row + blocked-count-tests ירוקים; אין `Alignment.centerLeft`/hard-LTR (`git grep`); `flutter analyze` 0.
9. 💡 **תוספת-א** · קיבוץ-לפי-`ConfigOp.kind` (step 69 getter): "🎨 12 צבעים · 🙈 3 הסתרות · ➕ 1 רכיב" — סיכום קריא לפי סוג.
10. 💡 **תוספת-ב** · "before→after" לכל שורה (`btn.reorder: אפור → ירוק`) שואב מ-`allowedValues` — preview מדויק, לא רק "שונה".

---

## Step 80 — `kStudioCoEditor` flag + `studioCoEditorProvider` (enabled × ai axes)

1. 🎯 **היעד** · דגל-build `kStudioCoEditor` + `studioCoEditorProvider` המפריד שני צירים (enabled × ai); ברירת-מחדל OFF → demo byte-identical.
2. 🔧 **איך בונים** · ב-`lib/data/repositories/backend.dart:138` (const ליד `kClaudeAi`): `const bool kStudioCoEditor = bool.fromEnvironment('STUDIO_CO_EDITOR')`; provider מחזיר `(enabled: kStudioCoEditor && useFirebaseBackend, ai: claudeGatewayProvider!=null)` (§8).
3. ⚠️ **תקלות צפויות** · דגל ON בטעות בבילד demo → רגרסיה (לא byte-identical, שובר את אינווריאנט `kClaudeAi`); ערבוב שני הצירים (pillar-off מול gateway-off) → off-state שגוי.
4. ✅ **פתרון** · ברירת-מחדל OFF מ-`bool.fromEnvironment` (כמו `kClaudeAi` `backend.dart:138`); הפרדה מפורשת `enabled` (pillar) מ-`ai` (gateway) ב-provider (§8) — pillar-on/gateway-off הוא מצב חוקי מובחן.
5. 🧪 **בדיקות (gating)** · `test/studio_gating_test.dart`: דגל=false→dark (אין surface); provider מחזיר את שני הצירים נכון; default-OFF byte-identical (אינווריאנט `kClaudeAi`).
6. 📈 **שיפור** · תיעוד הדגל ב-`backend.dart` doc-comment כמו של `kClaudeAi` (`backend.dart:130-137`) + פקודת-build מדגימה (`--dart-define=STUDIO_CO_EDITOR=true`).
7. 🤔 **ריאלי?** · כן — const אחד + provider, מראה idiom מוכח (`kClaudeAi`/`kCloudPhotos`/`kServerCallables`).
8. 🔒 **ווידוא-פיקס (DoD)** · `flutter build web` ללא הדגל → אין שום סימן Studio (byte-identical); gating-test ירוק; `flutter analyze` 0; `WIRING.md` מעודכן (#24).
9. 💡 **תוספת-א** · `studioCoEditorProvider` חושף גם `manager` (`boardAuthProvider?.role==BoardRole.manager`) כציר שלישי — מרכז את guard ה-manager-only במקום אחד (step 81).
10. 💡 **תוספת-ב** · assert-test שמוודא ש-`kStudioCoEditor` **לא** מופיע בשום קובץ-config של demo/CI — שער-רגרסיה אקטיבי נגד flip בשוגג.

---

## Step 81 — Studio manager-only cockpit hero + `StudioScreen` route shell + off-states

1. 🎯 **היעד** · כרטיס-hero ל-Studio בקוקפיט המנהל (manager-only) + shell של `StudioScreen` עם route ו-off-states; hero מוסתר כשהדגל OFF.
2. 🔧 **איך בונים** · ב-`lib/screens/manager_dashboard_screen.dart:420` (ליד `_CopilotHero`, seam `:432`) + NEW `lib/screens/studio_screen.dart` (`static Route<void> route()` כמו `manager_copilot_screen.dart:32`; guard כמו `manager_dashboard_screen.dart:73-75`).
3. ⚠️ **תקלות צפויות** · hero מופיע ב-build demo (שובר byte-identical); נגיש ל-non-manager (פריצת isolation §2); off-state חסר → מסך ריק כש-gateway null.
4. ✅ **פתרון** · hero רק כש-`kStudioCoEditor && role==manager` (guard `manager_dashboard_screen.dart:73-75`); `AiOffState` כש-`claudeGatewayProvider==null` (`ai_result_states.dart:18-28`); ה-builder הידני עובד גם אז (§8). hero מוסתר כשהדגל OFF.
5. 🧪 **בדיקות (gating)** · `test/studio_gating_test.dart`: hero רק כש-flag+manager; non-manager→אין hero; flag OFF→אין hero (byte-identical dashboard).
6. 📈 **שיפור** · ה-shell עם tabs (co-editor / builder ידני / rules) מראה את מבנה `ManagerCopilotScreen` — עקביות-UI לקוקפיט.
7. 🤔 **ריאלי?** · כן — hero+route+guard הם idioms קיימים (`_CopilotHero` `manager_dashboard_screen.dart:432`, route `manager_copilot_screen.dart:32`, guard `:73-75`).
8. 🔒 **ווידוא-פיקס (DoD)** · hero-only-when-flag+manager-test ירוק; flag-OFF→byte-identical-test ירוק; `flutter analyze` 0; `WIRING.md` מעודכן (#24).
9. 💡 **תוספת-א** · ה-hero מציג badge "ניסיוני" + deep-link ל-`StudioScreen` הפותח ישירות בטאב ה-builder הידני (הנתיב שתמיד עובד), כך ש-gateway-off לא מאכזב.
10. 💡 **תוספת-ב** · ה-route guard מחזיר `WelcomeScreen(boardRole:manager)` ל-non-manager (כמו `manager_dashboard_screen.dart:74`) — fail-closed עקבי, לא דף-שגיאה.

---

## Step 82 — manual no-model builder: pick→prop/visibility/component/action→preview→confirm→undo

1. 🎯 **היעד** · builder no-code שעובד **בלי מודל**: בחר אלמנט → prop/נראות/רכיב/פעולה → preview → confirm → undo. עובד מלא עם `kClaudeAi` OFF.
2. 🔧 **איך בונים** · NEW `lib/screens/studio_component_builder.dart`; confirm-gate כמו `_confirmAdd` (`ai_assistant_screen.dart:208-229`); op-stack-UI = **mirror בלבד**. מניע `ConfigOp`s דרך `validateSafe`→preview→confirm→**P1 `applyOps(List<ConfigOp>)`** (R1-4). מסכי-typed-arg ב-action-picker **מאופרים** "צריך פרמטרים — לא זמין" (R1-5), לא נעלמים.
3. ⚠️ **תקלות צפויות** · confirm כותב פעמיים (double-tap); undo משאיר state לא-עקבי מול Pillar 1; builder שבור כש-gateway null (תלות-שווא ב-AI); P4 ממציא `apply` משלו במקום לעבור דרך P1.
4. ✅ **פתרון** · confirm הוא tap מפורש-יחיד tracked (כמו `_confirmedKitTurns` `ai_assistant_screen.dart:223`) → **`P1.applyOps(applied)` פעם אחת** (R1-4) — **לא** `apply` P4-מקומי; undo דרך **undo-stack של P1** (P1 = SSOT), ה-op-stack שלנו הוא mirror; ה-builder אינו קורא ל-gateway כלל — עובד עם `kClaudeAi` OFF.
5. 🧪 **בדיקות** · `test/studio_screen_behavior_test.dart`: confirmed-diff קורא **`P1.applyOps` פעם אחת**; undo משחזר דרך undo-stack של P1; preview מוצג לפני כל apply; מסך-typed-arg = GREYED-not-droppable (מראה את `manager_copilot_screen_behavior_test.dart`).
6. 📈 **שיפור** · re-preview חינם (`summarizeDiff` טהור §9) על כל שינוי-בחירה — feedback מיידי בלי עלות.
7. 🤔 **ריאלי?** · כן — זה ה-MVP של ה-pillar (no-code עובד **בלי AI**, §8/§11 Phase-2); כל הלוגיקה כבר נבנתה ב-steps 69–79.
8. 🔒 **ווידוא-פיקס (DoD)** · `P1.applyOps`-once-test + undo-via-P1-stack-test ירוקים; `git grep` מאשר שאין `apply` P4-מקומי (כתיבה רק דרך `applyOps`, R1-4); builder-usable-with-kClaudeAi-OFF מאומת ידנית+test; `flutter analyze` 0.
9. 💡 **תוספת-א** · undo/redo מלא (שני op-stacks ב-UI) — mirror של ה-undo-stack של Pillar 1 לכל צעד, לא מקור-אמת מקביל.
10. 💡 **תוספת-ב** · "draft vs published" badge מובחן ב-UI (§12: confusion preview/applied/published) — Studio נוגע רק ב-DRAFT, ה-publish הוא שער Pillar 1.

---

## Step 83 — wire NL co-editor pane: ask→parse→safe→preview→confirm, honest off-state

1. 🎯 **היעד** · חיווט פאנל NL: `claudeGatewayProvider.ask`→`parseConfigEdit`→`validateSafe`→preview→confirm, עם off-state כן. מאחורי `kStudioCoEditor && kClaudeAi`.
2. 🔧 **איך בונים** · ב-`lib/screens/studio_screen.dart`; ה-seam המשותף `claude_functions.dart:126-136` (אין gateway חדש); race-guard `||_loading` (`describe_to_cart_screen.dart:104`), `mounted` אחרי await, 200-empty→retry (`reject_reason_screen.dart:92-93`).
3. ⚠️ **תקלות צפויות (injection/hallucination)** · stale-response race (re-submit באמצע) מוחק preview חדש; reply ריק→תיבה ריקה; **המודל מפעיל apply** (פריצת gate); utterance עוין מזריק הוראות.
4. ✅ **פתרון** · `||_loading` חוסם submit כפול (`describe_to_cart_screen.dart:101-104`); `mounted`-guard אחרי כל await; 200-empty→honest-retry לא תיבה-ריקה, **truncated→"לא הצלחתי"** (R1-10) לא preview-חלקי; ה-scope-שזוהה (Stage-A) מוצג בראש ה-preview ("מתוך: לשונית רכש", R1-7) לאישור-היעד; **`P1.applyOps`** **רק** מ-confirm-tap מפורש (`ai_assistant_screen.dart:208-229`) — המודל לעולם לא מפעיל `applyOps` (R1-4); `promptSafeText` על ה-utterance (§7.9). gateway-null→`AiOffState` (`ai_result_states.dart:18-28`) וה-builder הידני עדיין שמיש.
5. 🧪 **בדיקות (grounding)** · `test/studio_screen_behavior_test.dart`: gateway-null→`AiOffState`+manual-usable; המודל **לעולם לא** מפעיל `applyOps` (fake-gateway מחזיר ops→preview בלבד); שורת-scope מופיעה בראש ה-preview; truncated→"לא הצלחתי"; race/empty guards מכוסים.
6. 📈 **שיפור** · re-preview של תיקון חינם (§9); model-tier opt-in ל-`claude-sonnet-4-6` (`claude.ts:37`) רק ל-rephrase קשה, ברירת-מחדל haiku (`claude.ts:31`).
7. 🤔 **ריאלי?** · כן — חיווט מעל seam ולוגיקה מוכחים; כל ה-guards הם carry-over ישיר מהמסכים שנשלחו (§3.2).
8. 🔒 **ווידוא-פיקס (DoD)** · gateway-null→AiOffState-test ירוק; model-never-calls-`applyOps`-test ירוק (ה-defect-class הקריטי); scope-in-preview + truncated→fail מכוסים; race/empty/mounted מכוסים; `flutter analyze` 0.
9. 💡 **תוספת-א** · "מה התכוונת?" — כש-`parseConfigEdit` מחזיר 0 ops, הצג את ה-droppedCount (step 75 תוספת-א) במקום כשל אילם.
10. 💡 **תוספת-ב** · debounce client-side על submit (§9.2 Phase-5) — מתחת ל-40/min (`claude.ts:50`) בכל מקרה, אך מונע הקלקות-בזבוז.

---

## Step 84 — closed-set rules/automation model + `parseRule` + manual/NL rules screen

1. 🎯 **היעד** · מודל rules/automation סגור + `parseRule` + מסך rules (ידני+NL), הערכה read-only advisory מעל המנועים החיים. mutating-actions נדחות מאחורי אותו confirm-gate.
2. 🔧 **איך בונים** · NEW `lib/logic/studio/rules_model.dart` (`Rule{trigger,condition,action}`, כל slot closed-set §6) + NEW `lib/screens/studio_rules_screen.dart`; eval מעל `ordersEngineProvider`/`managerAnalyticsProvider` (`manager_copilot_screen.dart:53-64`).
3. ⚠️ **תקלות צפויות** · trigger/action מומצא; rule שמבצע mutation על הזמנות בלי gate; condition עם field לא-חוקי.
4. ✅ **פתרון** · כל token מאומת (`order.stuck∈TriggerId`, `ageDays∈Condition.field`, `notify.manager∈RuleActionId` §6); trigger/action מומצא→drop ה-rule (degrade); Phase-1 read-only/advisory בלבד (compute+surface notice, §6); `flag.order`/`suggest.reorder` mutating נדחים מאחורי אותו confirm-gate (`ai_assistant_screen.dart:208-229`).
5. 🧪 **בדיקות (grounding)** · `test/studio_rules_test.dart`: "תקועה >2 ימים→התרע למנהל" round-trips ל-`Rule(order.stuck, ageDays>2, notify.manager)`; trigger/action/field מומצא→drop.
6. 📈 **שיפור** · `parseRule` הוא עוד מופע של אותו TOTAL-parser (step 75) — שימוש-חוזר במשמעת, לא קוד חדש.
7. 🤔 **ריאלי?** · חלקי — Phase-1 read-only/advisory ריאלי לגמרי; mutating-rules (`flag.order`) **נדחה** מפורשות לפאזה מאוחרת מאחורי confirm-gate (§6/§11 Phase-4).
8. 🔒 **ווידוא-פיקס (DoD)** · round-trip-test ירוק; invented-trigger/action-drop-test ירוק; eval read-only מאומת (`git grep` שאין mutation על orders); `flutter analyze` 0.
9. 💡 **תוספת-א** · advisory-eval מציג "כרגע 3 הזמנות תואמות" ב-preview (טהור, מעל `ordersEngineProvider`) — ה-owner רואה את ההשפעה לפני שמירה.
10. 💡 **תוספת-ב** · rules נשמרות ב-Pillar 1 draft (JSON `rules_model.dart` `toJson`, §6) כמו כל config — אותו staging, undo עקבי.

---

## Step 85 — register gate #119 + injection-sanitize assertions + governance #84 audit + protocol docs

1. 🎯 **היעד** · רישום שער **#119** + assertions ל-injection-sanitize + ביקורת governance #84; חותם את אינווריאנטי zero-hallucination + governance.
2. 🔧 **איך בונים** · `.githooks/pre-commit` (`/home/user/wt-build/.githooks/pre-commit`) + `knowledge/GATE_REGISTRY.md:11-13` (**#119 כבר שמור ל-P4; P1=#118, P3=#120 — אין bump**, "הבא הפנוי"=121) + `WIRING.md`/`STATUS.md`/`ROADMAP.md`. שער **#119** = "`parseConfigEdit` מפיל כל id/prop/value/action/component מומצא (grounding-test ירוק)".
3. ⚠️ **תקלות צפויות** · התנגשות-מספר-שער (לקח #66/#67 `GATE_REGISTRY.md:3`); id של HR/role/auth מחליק לתוך closed-set (פריצת governance #84); label לא-`promptSafeText`'d; **vacuous-cert**: שער-119 נסגר על fake-registry בלי לאמת מול הרישום-האמיתי-הקפוא של P1 (R2-15).
4. ✅ **פתרון** · רישום אטומי לפי add-protocol (`GATE_REGISTRY.md:6-7`: שמור→הוסף-טבלה→commit); **שער-119 לא נסגר עד ש-`registryViewContract` (fake↔real) ירוק וגם `studio_safety_test` עובר מול ה-`ElementDescriptor` האמיתי-הקפוא של P1** (R2-15 — fake-self-cert הוא vacuous); assert ש-HR/role/auth ids **נעדרים** מכל closed-set (action-catalog/palette/rules); utterance+labels `promptSafeText`'d (`prompt_sanitize.dart:19-27`) — מאומת ב-test.
5. 🧪 **בדיקות (grounding+governance)** · כל חבילות grounding+safety ירוקות; **`registryViewContract` ירוק מול fake וגם מול מתאם-P1-האמיתי (חובה)**; **property-invariant fuzz** (∀op כל שדה ∈ registry, או ריק, לעולם לא throw) רץ על **אלפי תשובות אקראיות** (לא דגימות-ספורות) = REQUIRED gate-119; `test/studio_safety_test.dart`: שום op נוגע ב-HR/role/auth-capability (assert absent מכל closed-set) **ורץ מול הרישום-האמיתי-הקפוא**; injection-test: utterance+labels מקופלים+capped לפני interpolation (מראה את `manager_copilot_test.dart` cap-test).
6. 📈 **שיפור** · ה-pre-commit מריץ את חבילת ה-grounding + ה-fuzz כשער-חי — רגרסיית-הזיה נחסמת ב-commit, לא ב-review (§10).
7. 🤔 **ריאלי?** · כן — capstone של docs+gate; closed-by-omission כבר אכף את governance #84 (אין capability כזו בקטלוגים), וה-#84 נסגר בהיעדרות (§7 governance-note).
8. 🔒 **ווידוא-פיקס (DoD)** · #119 ב-`GATE_REGISTRY.md` (P1=#118, P3=#120, "הבא הפנוי"=121); `registryViewContract` + `studio_safety_test` ירוקים **מול הרישום-האמיתי-הקפוא של P1** (לא רק fake, R2-15); property-invariant-fuzz (אלפי תשובות) ירוק; `.githooks/pre-commit` מריץ את ה-grounding-suite; no-HR/role/auth-in-closed-set-assert ירוק; `WIRING.md`/`STATUS.md`/`ROADMAP.md` מעודכנים; `knowledge_protocol_test` (#94) ירוק.
9. 💡 **תוספת-א** · `git grep`-assertion ב-pre-commit: דפוסי HR/role-grant/auth ids נעדרים מ-`lib/logic/studio/` — שער-מקור נגד דליפה עתידית, לא רק test.
10. 💡 **תוספת-ב** · "governance snapshot" ב-`STATUS.md` המונה את closed-sets ואת ה-capabilities הנעדרות במפורש — עקבות-ביקורת ל-#84 שנשמר בין sessions.

---

### Dependency spine (סיכום למה הסדר הזה)
`ConfigOp` (69) = מטבע משותף (`SetVisible{roleProvider}`, לא `BsRole`). registry-adapter+matchers (70–71) שולטים בכל; ה-fake הוא scaffolding, אך `registryViewContract` (fake↔real) ו-`studio_safety_test` **חייבים** לעבור מול ה-`ElementDescriptor` האמיתי-הקפוא של P1 לפני שער-119 (R2-15), **fail-closed כשחסר**. action-catalog+palette (72–73) מאכלסים את הסטים הסגורים ש-prompt (74, Stage-A=closed-set-classifier) מטמיע. `parseConfigEdit` (75) — ליבת NL→ops המעוגנת + truncation-guard — צריך matchers+catalog+palette לאמת מולם; scope-expansion (76) מרחיב מעל ids אמיתיים. `validateSafe` (77–78) הוא backstop **advisory** (criticalBusiness+contrast+role-floor-מעל-`roleProvider`+session-budget) **שרץ-שוב בשרת ב-`publishConfig`** (R1-1). `summarizeDiff` (79) מרנדר אותם. flag+cockpit (80–81) הופכים את ה-surface גלוי (manager-only, dark כברירת-מחדל). ה-builder הידני (82) שולח נתיב no-code עובד **ללא מודל**, כותב דרך **P1 `applyOps`**; פאנל ה-NL (83) מוסיף דיבור כשדרוג דרך ה-gateway המשותף. rules (84) ממחזרות את כל ה-spine. ה-capstone (85) רושם שער **#119** וחותם את אינווריאנטי האנטי-הזיה + governance #84.
