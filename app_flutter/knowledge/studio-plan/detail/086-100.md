# BuildSmart Studio — DEEP detail · Steps 86–100 (Pillar 3 · Live Customer Intelligence)

> Branch `claude/whats-happening-LyY9G` · `app_flutter/` (Flutter 3.29 · Riverpod).
> Decomposed from `knowledge/studio-plan/steps/03-steps.md` (steps 86–100) + design
> `knowledge/studio-plan/03-live-customer-intelligence.md`. Every `file:line` re-confirmed
> against live code in this pass (see "Ground-truth confirmations" below).
>
> **The block in one breath:** 86 = privacy build-step (legal text + consent modal + default-DENY
> BEFORE any live capture); 87–97 = build the intel layer (taxonomy → bus → batched sink → anon id +
> `actorStitch` map → instrumentation → pure funnel/segment logic keyed by uid → presence) one
> shippable, gate-passing, demo-inert step at a time; 98–99 = the manager UX (5th tab + per-customer
> journey keyed by uid + new privacy **Gate #120** + complete erasure-sweep + docs); 100 = the
> platform-wide GA lock — **staged-flip** forward ON and pass the full gate, the "exactly 100, not 99" finish.
>
> ⚠️ **קנוני** = מקטע `🔧 תיקוני Red-Team R1` בסוף `03-live-customer-intelligence.md` + `RED-TEAM-R1/R2.md`.
> ה-appendix **גובר** על כל שורה סותרת כאן; השורות למטה כבר הופצו אליו.
>
> **Invariants baked into EVERY step (do not relax):**
> • **Zero-regression / Firebase-free demo** — every live component is born INERT off-backend, the
>   exact `connection_status.dart:82` `if (!_active) return;` idiom; the 356-test suite stays
>   Firebase-free.
> • **Near-zero perf** — the bus `read`s (never `watch`s) → no rebuilds; all forwards
>   fire-and-forget + batched (`telemetry.dart:110-116` `.catchError` discipline).
> • **Privacy default-DENY (HIGH/legal)** — `privAnalytics` **default `false`** (`app_settings.dart:105`
>   true→false; `:236` `!=false`→`==true`, absent=false) + separate `privPresence` **default `false`**;
>   forward + presence gated on **`consentedPolicyVersion >= kCurrentPolicyVersion && useFirebaseBackend`**
>   (a persisted policy version, NOT a bare bool) — presence additionally on `privPresence`; the local
>   ring buffer (on-device, no transmission) is the only always-on piece. Step 86 is a REAL build-step
>   (consent modal), not a "💡 addition".
> • **100-gate protocol** — gates 31/32/33/46/94 + P3's reserved **#120** (P1=118 config-registry ·
>   P4=119 AI-grounded-config · P3=120 analytics-PII); register all three in
>   `knowledge/GATE_REGISTRY.md`, next-free→121. Hooks live at repo root `.githooks/pre-commit`
>   (one level above `app_flutter/`).
> • **Governance #84** — manager/owner-only reads (`isManager()` / `ownerId`); **presence written/read
>   for CUSTOMER actors only**, never staff (worker/courier) — staff behavioral surveillance is beyond #84.
> • **Privacy honesty** — no raw search text (hash + length), no PII/free-text/payment in `props`;
>   **`displayName` never on the wire** (events/presence carry `actorKey`/`uid` only; resolve the name
>   owner-side); uid never in user-visible logs (the `manager_role_assign_sheet.dart:172` rule).

---

## Ground-truth confirmations (this pass)

| Claim in plan | Live verification |
|---|---|
| Telemetry seam + gate + events | `telemetry.dart:40-52` `TelemetrySink`; `:71-82` `NoopTelemetrySink` default; `:90-125` `FirebaseTelemetrySink` lazy+swallow; `:132-135` `telemetryProvider` gate; `:139-148` `TelemetryEvents` (`order_placed`/`role_assigned`/`app_error`). ✅ |
| Master gate | `backend.dart:12-17` `useFirebaseBackend = kUseFirebaseBackendFlag && Firebase.apps.isNotEmpty`. ✅ |
| Inert-off-backend idiom | `connection_status.dart:75` `_active = useFirebaseBackend`, `:82` `if (!_active) return;`. ✅ |
| Local ring buffer shape | `analytics_log.dart:8-19` `AnalyticsEvent`; `:21-39` `record`+trim (cap 500, newest-first); `:46-60` `countByName`/`recent`. ✅ |
| privAnalytics consent flag | `app_settings.dart:82` `final bool privAnalytics;` (+ `privCrashReports :85`). **BUG being removed:** `:105` default `true` + `:236` `!=false` (absent=true) → canonical default-DENY: `:105` false, `:236` `==true`, + new `consentedPolicyVersion`/`privPresence` fields. ✅ |
| Legal honesty text | `legal_texts.dart:3-13` "no analytics SDK … future Firebase sync PLANNED + gated"; תיקון 13 (14.8.2025) §11/§13/§14 cited `:10-13`; `kLegalLastUpdated :19`. ✅ |
| Root ProviderScope (no navigatorObservers yet) | `main.dart:210-218` `runApp(ProviderScope(... child: BuildSmartApp()))` — confirmed NO observer installed today. ✅ |
| Tab shell | `home_shell.dart:90-97` `IndexedStack(index: tabIndex, children:[Catalog/Departments/Updates/Store])` — note local is `tabIndex`, sourced from the tab provider; the plan's "one `ref.listen`" lands here. ✅ |
| Catalog sites | `catalog_screen.dart:1619` `recentSearchesProvider.notifier).add(q)` (search_submit); `:2229` `filtered.isEmpty && products.isEmpty` (search_no_result); `:4067` `openSmartProductSheet` (product_view); `:6374` `smartCartProvider.notifier).add(` (add_to_cart). ✅ |
| Store funnel sites | `store_screen.dart:1565-1593` `_CartView` build (cart_view); `:2693/:2699` `_showCheckoutSheet` (checkout_start); `:2341`/`:2567` cartDelivery/cartPayment setters (checkout_step); `:2916` `TelemetryEvents.orderPlaced` **already wired** after `placeOrder :2901`. ✅ |
| Manager join (canonical: by uid/actorKey) | `orders_engine.dart:693` `managerCustomersProvider` + `manager_dashboard.dart:276/278/285` `byBuyer[o.who]` group-by **display-name** — the R2-#12 BUG (two same-name customers MERGE + double-count). Canonical: key journey/aggregate by `uid`/`actorKey`, fall back to name only when uid empty. `_CustomerDetailSheet :1998`/`:2018` join moves to uid; name resolved owner-side. ✅ |
| Manager tabs | `manager_dashboard_screen.dart:65` `tabCount = 4`; `:3452-3457` `_kManagerTabs` (4); `:3463` `_kManagerTabHelp`. ✅ |
| Gate registry / hooks | `GATE_REGISTRY.md:11` "הבא הפנוי"; P3 = reserved **#120** (P1=118 · P4=119 · P3=120 analytics-PII), register all three, next-free→121; hooks at repo-root `.githooks/pre-commit`. ✅ |
| Test idioms | `realtime_wiring_test.dart:34-49` `_FakeSource implements RemoteCollectionSource` (`sets`/`deletes`/`emit`) — the recording-fake pattern; `knowledge_protocol_test.dart:91-101` "WIRING.md references every enforced helper". ✅ |

> Drift notes carried into the steps: (a) shell local var is `tabIndex` not `mainTabProvider` —
> the listener attaches to whatever provider feeds it; (b) `order_placed` is `:2916` (plan said
> ~:2915), already wired; (c) `realtime_wiring_test.dart` already EXISTS — step 99 ADDS cases to
> it, not a new file.

---

## 86. Privacy text + consent modal + default-DENY (REAL build-step — קנוני R1-1)

> ⚠️ קנוני (R1-1): זהו **שלב-בנייה מלא** — לא "text-only precondition" ולא "💡". הטקסט, מודאל-ההסכמה,
> ברירת-המחדל DENY וה-version-gate הם כולם ליבת-השלב.

1. 🎯 **היעד** · (א) לעדכן את מסמך-המדיניות שיגלה ביושר אנליטיקה-מגודרת; (ב) להפוך את ברירת-המחדל ל-**DENY**; (ג) לבנות **מודאל-הסכמה** שמצטט את הטקסט וכותב הסכמה-מתוארכת-בגרסה. ה-flip ב-100 חסום עד ששלב זה נוחת.
2. 🔧 **איך בונים** (file:line) ·
   • **טקסט:** `lib/data/legal_texts.dart:3-13` ("no analytics SDK" → "אנליטיקה מגודרת, ברירת-מחדל כבויה, מופעלת רק לאחר הסכמה מדעת"); יידוע תיקון-13 §11/§13/§14; `kLegalLastUpdated :19`; קבוע חדש **`kCurrentPolicyVersion`** (`int`) ליד-הטקסט.
   • **default-DENY:** `app_settings.dart:105` `privAnalytics: true` → **`false`**; `:236` `priv['analytics'] != false` → **`== true`** (absent/null = false). זהו ה-באג שמוסר (`:105` default-true + `:236` `!=false` = forward-on לכל משתמש-ישן שלא ראה מסך-הסכמה).
   • **שדה גרסה:** `consentedPolicyVersion` (`int`, default `0`) ב-`AppSettings` (`:82-85` + `copyWith :129` + `toJson :189` + `fromJson :236`).
   • **מודאל:** דיאלוג-הסכמה חד-פעמי שמציג את `legal_texts.dart`; על "אני מסכים" כותב `privAnalytics=true` **וגם** `consentedPolicyVersion=kCurrentPolicyVersion` ב-txn אחת.
   • **gate:** ה-forward (step 3, §2.1) קורא **`consentedPolicyVersion >= kCurrentPolicyVersion && useFirebaseBackend`**, לא bool צף — bump עתידי של הגרסה מאפס הסכמה דה-פקטו עד re-opt-in (דרישת תיקון-13).
3. ⚠️ **תקלות צפויות** · "honesty drift" (הטקסט מבטיח יותר/פחות מהמוצר); placeholder `[שם החברה]` (`:9`); שבירת persist (משתמשים ישנים — absent עכשיו=false, נכון); מודאל שנפתח שוב-ושוב; כתיבת toggle בלי הגרסה (חצי-הסכמה).
4. ✅ **פתרון** · לנסח superset-מדויק של ההתנהגות (local-only היום, forward-מגודר); placeholders כמו-שהם (לא להמציא עובדות); כתיבת toggle+גרסה **אטומית**; המודאל נפתח רק כש-`consentedPolicyVersion < kCurrentPolicyVersion`; ה-local ring buffer (step 1) נשאר on-device תמיד, ללא תלות בהסכמה (אין שידור).
5. 🧪 **בדיקות** (שמות-קבצים) · `test/legal_screen_test.dart` (הטקסט החדש §11/§13/§14); `test/intel/consent_flow_test.dart` (לפני-הסכמה → forward inert; אחרי "אני מסכים" → forward enabled; אחרי bump-גרסה → forward inert-שוב); `knowledge_protocol_test.dart` ירוק.
6. 📈 **שיפור** · שורת-תאריך-revision גלויה + לינק "מה השתנה" — אמון-משתמש + הקטנת-חשיפה רגולטורית.
7. 🤔 **ריאלי?** · כן. טקסט + מודאל + 3 שדות-settings + gate-version — מקורקע מול `app_settings.dart`/`legal_texts.dart` קיימים.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + `flutter test` ירוק (legal+consent_flow+protocol) + ברירת-מחדל `privAnalytics=false` (grep `app_settings.dart:105`) + `consentedPolicyVersion` ב-`fromJson`/`toJson` + המודאל כותב צמד אטומי + ה-flip ב-100 חסום עד-כאן.
9. 💡 **תוספת-א** · גם `privPresence` (R1-2) מקבל toggle-נפרד default-false באותו שלב, כדי שכל ברירות-המחדל-DENY ינחתו יחד.
10. 💡 **תוספת-ב** · "מרכז-פרטיות" שמרכז את שתי ההסכמות (analytics/presence) + הגרסה-הנוכחית + כפתור "משוך הסכמה" (re-DENY).

## 87. `IntelEvents` registry + `IntelEvent` immutable record (pure data, no wiring)

1. 🎯 **היעד** · מקור-אמת אחד לשמות אירועים (snake_case, ≤40, ללא-PII) + רשומת-אירוע אחת immutable — בלי לגעת ב-`TelemetryEvents` הקיים.
2. 🔧 **איך בונים** (file:line) · קובץ חדש `lib/state/intel/intel_events.dart` (`abstract final class IntelEvents`, מרחיב את הרעיון של `telemetry.dart:139-148`) + `lib/state/intel/intel_event.dart` (`@immutable class IntelEvent`, משקף את `analytics_log.dart:8-19` עם `actorKey`/`uid`/`sessionId`/`screen`/`props`; **`displayName` הוא in-memory-בלבד** ל-local ring buffer ולעולם לא נכתב ל-Firestore — R1-4).
3. ⚠️ **תקלות צפויות** · drift של מחרוזות בין call-site/test/dashboard; שם > 40 תווים נדחה ב-GA; הגדרת מפתח-prop אסור (טקסט-חופשי/PII).
4. ✅ **פתרון** · קבועי `static const` בלבד (אין literal ב-call-site); registry-test (צעד 87 בדיקות) שכופה ייחודיות+פורמט+אורך+מפתחות-מותרים; להפריד `IntelEvents` מ-`TelemetryEvents` כדי ששמות G4 לא יזוזו.
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/intel_events_test.dart` (unique · snake_case · ≤40 · no-PII-key, בנוסח assertions של `telemetry_test.dart`).
6. 📈 **שיפור** · `enum`-backed names + `extension` למיפוי emoji+תווית עברית, כדי שה-UI וה-registry יישארו מסונכרנים מקור-אחד.
7. 🤔 **ריאלי?** · כן. data טהור, אפס wiring, אפס firebase/widgets.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + registry-test ירוק + אין מפתח-prop אסור באף אירוע + הקובץ לא מיובא משום מקום עדיין (additive).
9. 💡 **תוספת-א** · annotation `@allowedProps({...})` לכל אירוע שה-registry-test קורא רפלקטיבית/דקלרטיבית.
10. 💡 **תוספת-ב** · גנרציה אוטומטית של תיעוד הטקסונומיה ל-`WIRING.md` מתוך ה-registry (single source).

## 88. `IntelLogNotifier` ring buffer (cap ~1000, newest-first, in-memory)

1. 🎯 **היעד** · buffer מקומי בזיכרון שמניע גם את ה-demo וגם את ה-live-feed; לעולם לא נשמר (payload עלול להיות רגיש).
2. 🔧 **איך בונים** (file:line) · קובץ חדש `lib/state/intel/intel_log.dart` — `IntelLogNotifier extends StateNotifier<List<IntelEvent>>`, מעתיק את אינוריאנט ה-trim של `analytics_log.dart:33-38` (prepend ל-front, sublist ל-cap), עם cap ~1000.
3. ⚠️ **תקלות צפויות** · דליפת זיכרון אם ה-cap לא נאכף; שמירה בטעות ל-disk; O(n) על כל record אם משתמשים במבנה לא-יעיל.
4. ✅ **פתרון** · trim-on-record כמו ב-`analytics_log.dart`; אין persistence layer בכלל (in-memory only, מתועד בכותרת); prepend amortized O(1) עם sublist מדי פעם.
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/intel_log_test.dart` (record/trim ל-cap · recent newest-first · countByName).
6. 📈 **שיפור** · `recent({name, since})` עם חלון-זמן כדי שה-live-feed יסנן O(window) ולא O(buffer).
7. 🤔 **ריאלי?** · כן. clone ישיר ומוכח של `analytics_log.dart`.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + log-test ירוק + buffer מעולם לא נכתב ל-prefs/disk (grep) + cap נאכף תחת load סינתטי.
9. 💡 **תוספת-א** · `clear()` שמתחבר לכפתור "מחק היסטוריה מקומית" + לזרימת erasure (§7).
10. 💡 **תוספת-ב** · counters נגזרים (per-name/per-screen) שנשמרים incrementally כדי שה-KPI tiles לא יסרקו את ה-buffer בכל build.

## 89. `IntelBus` one-line seam (local + telemetry + inert sink fan-out)

1. 🎯 **היעד** · הדבר היחיד שמסך נוגע בו — `track(name, props)` שמפצל לשלושה יעדים, כל אחד בטוח בנפרד, ולעולם לא מפיל מסך.
2. 🔧 **איך בונים** (file:line) · קובץ חדש `lib/state/intel/intel_bus.dart` (`Provider<IntelBus>`). `track()` עושה: (1) `intelLogProvider.notifier).record(...)` תמיד; (2) `telemetryProvider).logEvent(...)` (`telemetry.dart:47`, no-op ללא Firebase); (3) `intelSinkProvider).enqueue(...)` (inert off-backend). הכול `read` לא `watch`.
3. ⚠️ **תקלות צפויות** · `watch` במקום `read` → rebuilds/jank; await/throw בתוך track → קריסת מסך; קריאה ל-sink לפני שהוא קיים (צעד 90).
4. ✅ **פתרון** · `read` בלבד (אפס rebuild); אף פעם לא await/throw — אותה דיסציפלינה כמו `FirebaseTelemetrySink` (`telemetry.dart:110-123`); `_context()` חותם actorKey/displayName/sessionId/screen כך ש-call-site מעביר רק name+props קטן.
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/intel_bus_test.dart` (ללא Firebase: `track` רושם מקומית, קורא ל-`NoopTelemetrySink`, sink inert, `returnsNormally` — בנוסח `telemetry_test.dart:65-78`). **זו הוכחת ה-zero-regression ל-demo.**
6. 📈 **שיפור** · `track` overload שמקבל `IntelEvent` מוכן (לזרימות שכבר בנו רשומה), כדי למנוע כפילות חתימה.
7. 🤔 **ריאלי?** · כן. seam דק שמרכיב מרכיבים קיימים; demo = local append בלבד.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + bus-test `returnsNormally` ללא Firebase + אפס `watch` בתוך הבאס (grep) + suite Firebase-free נשאר ירוק.
9. 💡 **תוספת-א** · rate-limit פנימי ל-`screen_view`/`heartbeat` (min interval) בתוך `track` כדי לא להציף את ה-buffer.
10. 💡 **תוספת-ב** · debug-only assertion שמפעיל אזהרה אם `props` מכיל מפתח לא-סקלרי/לא-מוכר (תפיסה מוקדמת של drift).

## 90. Consent-gated batched `IntelSink` (N/T/paused flush, requeue, inert off-backend)

1. 🎯 **היעד** · forward חי, batched, שמחזיר מיידית; ≤~6 כתיבות/דקה/משתמש פעיל worst-case — לא כתיבה-לכל-tap. No-op מוחלט ב-demo.
2. 🔧 **איך בונים** (file:line) · קובץ חדש `lib/state/intel/intel_sink.dart`, מגודר **`consentedPolicyVersion >= kCurrentPolicyVersion && useFirebaseBackend`** (R1-1 — גרסה נשמרת, לא `privAnalytics` bool צף; `app_settings.dart:82`, `backend.dart:12-17`). doc על-החוט נושא `actorKey`/`uid` בלבד — **ללא `displayName`** (R1-4). `enqueue()` append+return; flush ב-FIRST של N(`kBatchSize`=25)/T(`kFlushInterval`=10s)/paused → `WriteBatch` אחד. inert off-backend (`connection_status.dart:82` idiom).
3. ⚠️ **תקלות צפויות** · אובדן אירועים ב-blip רשת; flush שזורק; queue שגדל ללא-גבול; כתיבה כשההסכמה כבויה.
4. ✅ **פתרון** · failed-flush מ-re-queue (capped) ולעולם לא throw — סגנון `guardWrite` (`firestore_cached_repo.dart` HARD RULE #2); offline-persistence (`main.dart:133-134`) כבר מאגר; double-gate consent+backend; cap על ה-queue.
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/intel_sink_test.dart` עם recording fake (נוסח `realtime_wiring_test.dart:34-49` `_FakeSource`): enqueue סינכרוני · flush ב-N/T/paused · failed-flush re-queues · `returnsNormally`.
6. 📈 **שיפור** · backoff מעריכי על כשלי-flush חוזרים + `kSampleRate` (Pillar-1, default 1.0) ל-down-sample בצי ענק.
7. 🤔 **ריאלי?** · כן. ה-batching seam מודל ישיר על `FirebaseTelemetrySink` + `FirestoreCachedRepo.upsertLocalOnly`.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + sink-test (enqueue/flush/requeue) ירוק + sink inert כש-`!useFirebaseBackend` (אפס platform-channel) + אף אירוע לא נשלח כש-`consentedPolicyVersion < kCurrentPolicyVersion` + אף doc לא נושא `displayName` (grep).
9. 💡 **תוספת-א** · flush גם ב-`session_end`/logout כדי לא לאבד את הזנב לפני יציאה.
10. 💡 **תוספת-ב** · מטריקת self-telemetry (queue-depth/flush-latency) לדשבורד dev בלבד, מאחורי דגל debug.

## 91. Anon-but-stable `actorKey` (per-install uuid) + sign-in stitch

1. 🎯 **היעד** · מפתח-שחקן יציב: uid בכניסה, אחרת uuid פר-התקנה; stitch anon→uid כדי שמסע-הלקוח יישאר רציף.
2. 🔧 **איך בונים** (file:line) · ב-`lib/state/intel/intel_sink.dart` + SharedPreferences בנוסח `recently_viewed.dart`/`smart_input_usage.dart`. ב-sign-in (`currentUidProvider`) פולטים אירוע-stitch `identify` **וגם** כותבים שורת-מיפוי מתמשכת ל-collection ייעודי **`actorStitch/{uid}` `{anonKeys:[...], at}`** (לא רק event-בזרם) — כדי שה-erasure-sweep (§7/R1-3) יוכל לאחות אירועי-pre-login. ה-collection עצמו **erasable** (אותו sweep + TTL) ומנוקה ע"י `deleteAccount`. Off-backend המפתח נוצר אך לעולם לא נכתב.
3. ⚠️ **תקלות צפויות** · uid דולף ל-log גלוי (הפרת חוק `manager_role_assign_sheet.dart:172`); המפתח משתנה בין הפעלות (שובר רציפות); race בין late-load ל-event ראשון.
4. ✅ **פתרון** · uid לעולם לא ב-prop גלוי-למשתמש (רק actorKey פנימי); persist חד-פעמי עם `_userTouched` late-load guard (האידיום של `recently_viewed.dart`); אם prefs עוד לא נטען — להמתין/לדחות את ה-stitch, לא לייצר מפתח חדש.
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/intel_actor_key_test.dart` (יציבות פר-התקנה · רציפות anon→uid דרך אירוע-stitch · off-backend לא נכתב).
6. 📈 **שיפור** · גרסאנות-מפתח (key schema version) כדי לאפשר migration עתידי בלי לאבד היסטוריה.
7. 🤔 **ריאלי?** · כן. אידיום persistence מוכח בריפו; ה-stitch הוא אירוע נוסף בלבד.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + actor-key-test ירוק + uid לא מופיע באף log גלוי (grep) + off-backend אפס כתיבת-prefs של המפתח.
9. 💡 **תוספת-א** · erasure: מחיקת ה-actorKey המקומי כחלק מ"שכח אותי" שמנתק את הרציפות בצד-לקוח.
10. 💡 **תוספת-ב** · ה-`actorStitch/{uid}` (קנוני, §2) הוא גם מקור-האמת ל-backend-side איחוד anon→uid; Pillar 5 אוכף עליו TTL + מחיקה ב-`deleteAccount`.

## 92. `screen_view` via RouteObserver + shell tab listener (~3 edits, not 270)

1. 🎯 **היעד** · אירוע `screen_view` אוטומטי בכל מעבר route/tab בלי שכל מסך יזכור לפלוט — adoption של ~3 עריכות במקום 270.
2. 🔧 **איך בונים** (file:line) · `RouteObserver<ModalRoute>` ב-`MaterialApp.navigatorObservers` שמותקן פעם אחת ב-root (`main.dart:210-218`, היום אין שם observer); + `ref.listen` אחד ב-shell (`home_shell.dart:90-97`, מאזין לספק שמזין את `tabIndex`) שממפה tab-index→screen key ופולט `screen_view`.
3. ⚠️ **תקלות צפויות** · rebuilds/double-fire על אותו route; ה-shell משתמש ב-`tabIndex` מקומי (לא `mainTabProvider` כפי שכתוב בתוכנית) → להאזין לספק הנכון; observer שלא מתחבר כי `navigatorObservers` ריק היום.
4. ✅ **פתרון** · `read`-only בתוך ה-observer/listener (אפס rebuild); dedupe לפי route-name + min-interval; לאמת איזה provider מזין `tabIndex` ולהאזין לו ספציפית; להוסיף את ה-observer ל-`navigatorObservers` (התוספת הגלובלית היחידה).
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/screen_view_test.dart` (widget: מעבר-tab ומעבר-route פולטים `screen_view` פעם אחת כל אחד; אין rebuild של המסך).
6. 📈 **שיפור** · מיפוי route→Hebrew-screen-label מרוכז (אחד) שגם ה-journey timeline צורך — אין כפל-מחרוזות.
7. 🤔 **ריאלי?** · כן. listener אחד מחליף N קריאות; `read`-only, אפס rebuilds.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + screen_view-test ירוק (פעם אחת בדיוק) + אפס rebuild שנגרם מ-tracking + 4 ה-tabs הקיימים מתנהגים byte-identical.
9. 💡 **תוספת-א** · `prev` screen ב-prop (מאיפה הגיע) → מאפשר ניתוח נתיב-ניווט ו-back-loop detection (§3).
10. 💡 **תוספת-ב** · dwell-time per screen (חישוב מ-screen_view→screen_view) שמזין presence ו-stuck.

## 93. Catalog interaction events (search/no-result/view/cart) — single statements

1. 🎯 **היעד** · ארבעה אירועי-קטלוג ב-call-site אחד כל אחד, ללא שינוי constructor/control-flow, ביטים זהים ב-demo.
2. 🔧 **איך בונים** (file:line) · `catalog_screen.dart:1619` (אחרי `recentSearchesProvider.add(q)`) → `search_submit`; `:2229` (`filtered.isEmpty && products.isEmpty`) → `search_no_result`; `:4067` (`openSmartProductSheet`) → `product_view`; `:6374` (אחרי `smartCartProvider.notifier.add`) → `add_to_cart`. כל אחד `ref.read(intelBusProvider).track(...)` בשורה אחת.
3. ⚠️ **תקלות צפויות** · טקסט-חיפוש גולמי נשלח (הפרת §7); double-fire על rebuild של ה-empty-state; שינוי control-flow בטעות; sku חסר.
4. ✅ **פתרון** · `search_submit` נושא `q_len`+hash מלוח, לעולם לא raw; guard ש-`search_no_result` נפלט פעם-אחת-לשאילתה (לא בכל build); משפט-בודד שלא נוגע ב-control-flow; sku מ-`product.key`/`sku` בלבד.
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/catalog_events_test.dart` (widget: כל אחד מארבעת האירועים נפלט פעם אחת; אין raw-text ב-props).
6. 📈 **שיפור** · `source` ל-`product_view` (`catalog`/`search`/`history`) ו-`acc_count` ל-`add_to_cart` — מעשיר משפך בלי עלות.
7. 🤔 **ריאלי?** · כן. משפטים-בודדים additive; demo byte-identical (ה-track הוא local-append+no-op).
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + catalog-events-test ירוק (פעם אחת כל אחד) + grep מאשר אפס raw-query ב-props + ה-snapshot של מסך-הקטלוג ב-demo ללא-שינוי.
9. 💡 **תוספת-א** · `filter_change` (sort/lens/system) על שינוי פילטר — מזין את ניתוח ההתנהגות.
10. 💡 **תוספת-ב** · salt מסובב (rotating salt) ל-hash של החיפוש כדי שאי-אפשר יהיה לשחזר שאילתות נפוצות.

## 94. Store funnel events (cart/checkout-start/step) — additive single lines

1. 🎯 **היעד** · אירועי-משפך בחנות, additive בשורה-בודדת, demo byte-identical (`order_placed` כבר מחווט).
2. 🔧 **איך בונים** (file:line) · `store_screen.dart:1565-1593` `_CartView.build` → `cart_view` (פעם-אחת ב-build הראשון); `:2693/:2699` `_showCheckoutSheet` → `checkout_start`; `:2341`/`:2567` setters של `cartDeliveryProvider`/`cartPaymentProvider` → `checkout_step`; `:2916` `order_placed` **כבר מחווט** (אין שינוי).
3. ⚠️ **תקלות צפויות** · `cart_view` נפלט בכל rebuild (לא רק ראשון); double-wire של `order_placed`; ערכי-prop רגישים (סכום/פריטים זה בסדר; פרטי-תשלום אסור).
4. ✅ **פתרון** · guard `_emittedCartView` ב-state כך ש-`cart_view` נפלט פעם-אחת-למופע; לא לגעת ב-`:2916`; `checkout_step` נושא `step`+`value` סקלרי בלבד (לעולם לא מספר-כרטיס).
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/store_funnel_test.dart` (widget: cart_view פעם-אחת · checkout_start/step נפלטים · order_placed עדיין יחיד).
6. 📈 **שיפור** · `checkout_abandon` נגזר (§3) כשמושב נסגר אחרי checkout_start בלי order_placed — אות-תקיעה אמין שלקוח רעוע לא יכול לזייף.
7. 🤔 **ריאלי?** · כן. שורות-בודדות additive; `order_placed` כבר עובד; demo byte-identical.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + store-funnel-test ירוק + `order_placed` עדיין נפלט פעם-אחת (לא הוכפל) + אפס prop-תשלום (grep) + ביטי-demo ללא-שינוי.
9. 💡 **תוספת-א** · `checkout_step` עם `dur_s` מאז ה-step הקודם → מזהה היכן הלקוח מתעכב בתוך התשלום.
10. 💡 **תוספת-ב** · קישור order_id↔sessionId (כבר ב-`_context()`) כדי לחבר את ההזמנה למסע המלא בדשבורד.

## 95. Deterministic funnel + stuck/abandon/dead-end detectors (pure Dart)

1. 🎯 **היעד** · אלגוריתמי-משפך ותקיעה דטרמיניסטיים, pure Dart, unit-testable על רשימות-אירועים בנויות-ביד; אפס magic numbers.
2. 🔧 **איך בונים** (file:line) · קבצים חדשים `lib/logic/intel/funnels.dart` (fold step-retention מונוטוני, בנוסח `kManagerOrderFlow` ב-`manager_dashboard.dart:29-36`) + `lib/logic/intel/intel_config.dart` (ספים-קבועים: `kIdleEnd`=180s, `kRepeatNoResult`=3, `kBackLoop`=4). אין Firebase/widgets.
3. ⚠️ **תקלות צפויות** · ספים קסומים מפוזרים בקוד; אי-דטרמיניזם מ-`DateTime.now()`; off-by-one ב-step-retention; detector שמסתמך על אירוע-לקוח שניתן לזיוף.
4. ✅ **פתרון** · כל סף ב-`intel_config.dart` עם שם; הזמן מוזרק (fake clock) בבדיקות; `checkout_abandon`/`dead_end` הם **נגזרים** (נפלטים ע"י ה-detector בסגירת מושב או מחושבים בזמן-קריאה) ולכן client רעוע לא יכול לזייף.
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/funnels_test.dart` (אחוז-המרה דטרמיניסטי · biggest drop-off · abandoned-cart · repeated-no-result · dead-end loop — הכול על רשימות בנויות-ביד).
6. 📈 **שיפור** · ספים tunable דרך Pillar-1 (Studio config) במקום קבועים, בלי לשבור את הדטרמיניזם של ברירת-המחדל.
7. 🤔 **ריאלי?** · כן. pure-logic מוכח (אותה דיסציפלינה כמו `manager_dashboard.dart`/`install_engine.dart`).
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + funnels-test ירוק (כל ה-detectors) + אפס Firebase/widget import בקבצים האלה + כל סף ממוקור ב-`intel_config.dart`.
9. 💡 **תוספת-א** · "next-best-action" hint לכל stuck (לדוגמה "הצע משלוח חינם בעגלה ננטשת") שמוזן ל-UI.
10. 💡 **תוספת-ב** · time-to-convert percentiles (p50/p90) פר-stage כמדד-בריאות-משפך.

## 96. Pure segments + retention cohorts — key by uid/actorKey (NOT name · קנוני R2-#12)

> ⚠️ קנוני (R2-#12): ה-join חייב להיות לפי **`uid`/`actorKey`**, לא `displayName`. היום
> `manager_dashboard.dart:276/278/285` ממפה `byBuyer[o.who]` (שם-תצוגה) → שני-לקוחות-שם-זהה
> **מתמזגים + double-count**. הקנוני מזיז את המפתח ל-uid; נפילה-לשם רק כש-uid ריק.

1. 🎯 **היעד** · פילוחים + cohort-שימור טהורים, מקובצים לפי **`uid`/`actorKey`** (זהות-יציבה), מצטרפים לשורות-המנהל; השם נפתר owner-side ולא משמש מפתח-מיזוג.
2. 🔧 **איך בונים** (file:line) · קובץ חדש `lib/logic/intel/segments.dart` — group-by **`uid`/`actorKey`** (לא `o.who`); roll-up פר-לקוח `{sessions,lastSeen,screensTouched,stuckCount,converted}`; retention = cohort-by-first-seen-day → % חוזרים ביום N (date-bucket fold כמו `manager_dashboard.dart:272-296`). נפילה-לשם **רק כש-uid ריק** (anon ללא חשבון). הצטרפות ל-`managerCustomersProvider` (`orders_engine.dart:693-700`) דרך מפתח-uid; השם resolved owner-side.
3. ⚠️ **תקלות צפויות** · המיזוג-לפי-שם הישן (`byBuyer[o.who]`) שמתמזג שני-לקוחות-שונים; timezone משבש date-bucket; anon ללא-uid-ושם שובר את ה-fold.
4. ✅ **פתרון** · מפתח-ראשי = `uid`/`actorKey` (אף-פעם לא שם כמפתח-מיזוג); date-bucket בזמן-מקומי עקבי (אותו אזור לכל ה-fold); anon ללא-uid → דלי-"אנונימי" נפרד (לא מתמזג עם שם), לא קורס.
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/segments_test.dart` (roll-up פר-לקוח · cohort retention דטרמיניסטי · **שני-לקוחות-שם-זהה-uid-שונה נשארים נפרדים, ללא double-count** · join מתיישר עם managerCustomers דרך uid).
6. 📈 **שיפור** · RFM-style segments (Recency/Frequency/Monetary מתוך orders+events) לפילוח עשיר יותר.
7. 🤔 **ריאלי?** · כן. fold טהור דטרמיניסטי; המפתח עובר מ-`o.who` ל-uid — שינוי-מפתח, לא מבנה.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + segments-test ירוק (כולל מקרה-double-count) + ה-join לפי **uid** (לא שם) + אפס Firebase/widget import.
9. 💡 **תוספת-א** · churn-risk score פר-לקוח (recency + stuckCount) שמדגיש לקוחות בסיכון בדשבורד.
10. 💡 **תוספת-ב** · cohort export (CSV) למנהל — מאחורי governance #84, owner-only.

## 97. Session tracker + heartbeat presence (connection_status clone)

1. 🎯 **היעד** · מחזור-חיי-מושב + heartbeat נוכחות ("איפה הם עכשיו"), clone הנדסי של `ConnectionStatusNotifier`, inert ב-demo.
2. 🔧 **איך בונים** (file:line) · קבצים חדשים `lib/state/intel/session_tracker.dart` (`StateNotifier`+`WidgetsBindingObserver`) + `lib/state/intel/presence.dart`; observer ב-root (`main.dart:210-218` — אין app-lifecycle observer היום, זה ה-hook הגלובלי החדש היחיד, מגודר). כתיבת `presence/{...}` מגודרת **`privPresence == true && consentedPolicyVersion >= kCurrentPolicyVersion && useFirebaseBackend`** (R1-2 — toggle נפרד מ-`privAnalytics`, default-OFF) ו**רק עבור actor-לקוח** (`roleProvider==null`=קבלן/לקוח; worker/courier **לא** כותבים presence — R1-5, הרחבת-פיקוח-על-צוות מעבר ל-#84). ה-doc נושא `actorKey`/`uid` בלבד (ללא `displayName`, R1-4) + `expireAt = serverTs + kPresenceTtl` (**דקות**, לא 30–90 יום של `events/`; Firestore-TTL ב-Pillar 5). liveness = `now-lastBeat < kHeartbeat*2.5`.
3. ⚠️ **תקלות צפויות** · presence false-positive (טאב תקוע נראה "חי"); timer דולף אם לא נסגר; כתיבת heartbeat ב-demo / בלי-`privPresence` / על-צוות; ה-observer לא מוסר ב-dispose; doc ללא-TTL נשאר לנצח.
4. ✅ **פתרון** · liveness לפי טריות `lastBeat` עם סבילות 2.5× (לא עצם-קיום-doc); ctor inert off-backend (`if(!useFirebaseBackend) return;` — האידיום של `connection_status.dart:82`): אפס timer/observer/Firestore; gate כפול presence-consent + customer-only לפני כל heartbeat; `kPresenceTtl`-דקות על ה-doc; `session_end` ב-paused/idle(≥`kIdleEnd`)/logout **מוחק** את ה-doc מיידית (לא מסתמך רק על TTL); הסרת observer ב-dispose. בסיס-חוקי/מידתיות מתועד ליד `legal_texts.dart` (מטרה=תמיכה-בזמן-אמת, רק `screen`+`lastBeat`, ללא GPS).
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/session_tracker_test.dart` (fake clock+source: קצב heartbeat · session_end ב-idle/background/logout · כלל-liveness 2.5×).
6. 📈 **שיפור** · "typing/active" rich-presence (מסך + פעולה אחרונה) לתצוגת-מנהל מדויקת יותר.
7. 🤔 **ריאלי?** · כן. clone של דפוס-נוכחות-בזמן-אמת מוכח-ומשוחרר (`connection_status.dart`).
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + session-test ירוק (lifecycle+liveness) + tracker inert off-backend (אפס timer/Firestore/platform-channel) + אפס heartbeat כש-`privPresence=false` או actor=worker/courier + doc נושא `expireAt`-דקות ונמחק ב-`session_end` + suite Firebase-free נשאר ירוק.
9. 💡 **תוספת-א** · multi-device presence (אותו uid בכמה מכשירים) עם מיזוג ל-"חי אם אחד-מהם חי".
10. 💡 **תוספת-ב** · "last seen" עמיד גם offline (TTL על ה-doc, Pillar 5) כך שהמנהל רואה היעדרות אמיתית ולא ריק.

## 98. Manager intel read providers + 5th tab `_IntelTab` (Hebrew/RTL)

1. 🎯 **היעד** · ספקי-קריאה למנהל + tab חמישי "📡 מודיעין לקוחות" בעברית/RTL/a11y; 4 ה-tabs הקיימים ללא-נגיעה; demo מקפל את ה-buffer המקומי.
2. 🔧 **איך בונים** (file:line) · קבצים חדשים `lib/state/intel/intel_read.dart` (funnel/segments/retention/presence providers — fold §3 מעל המקור, **read-gate `isManager()`**, R1-5) + `lib/screens/intel/intel_tab.dart`. "מי מחובר כעת" מציג **לקוחות בלבד** (presence נכתב רק להם, R1-5); שמות resolved owner-side מ-`orders`/`customers` (לא מהאירוע, R1-4). עריכות ב-`manager_dashboard_screen.dart`: `tabCount :65` 4→5; `_kManagerTabs :3452-3457` להוסיף `_ManagerTab('📡','מודיעין לקוחות')`; `_kManagerTabHelp :3463` tuple; `IndexedStack.children :216-231` להוסיף `const _IntelTab()`. שימוש-חוזר ב-`_MetricTile`/`_CreditBar`/`_StagePill`.
3. ⚠️ **תקלות צפויות** · אי-לוקסטפ בין `tabCount`/`_kManagerTabs`/`_kManagerTabHelp`/`children` (ה-assert של המסך נשבר); חוסר Semantics/RTL; ה-toggle לא מתעדכן.
4. ✅ **פתרון** · לעדכן את כל ארבעת המקומות יחד (ה-`_ManagerToggle :295-378` נבנה מ-`.length` → לא צריך עריכה); `Directionality(rtl)`+`EdgeInsetsDirectional`+`Semantics(label)` כמו `_CreditBar :1974`; demo folds `intelLogProvider` כך שה-UI מלא-נתונים בלי Firebase.
5. 🧪 **בדיקות** (שמות-קבצים) · `test/intel/intel_tab_test.dart` (widget: render תחת `Directionality(rtl)` · `Semantics` נוכח · demo-data מוצג · `tabCount==5` + 4 הישנים ללא-שינוי).
6. 📈 **שיפור** · חלון-זמן בורר (היום/7d/30d) ב-tab כך שהקריאה החיה תמיד O(window) ולא O(history).
7. 🤔 **ריאלי?** · כן. שימוש-חוזר בווידג'טים קיימים (אין chart-lib); demo מקפל buffer מקומי.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + intel_tab-test ירוק (RTL+a11y+demo) + 4 ה-tabs הקיימים byte-identical + Gate 46 (אין משטח-כהה במסכים) עובר.
9. 💡 **תוספת-א** · deep-link מ"מי מחובר כעת" ישר ל-`_CustomerDetailSheet` (משטח-drill יחיד, צעד 99).
10. 💡 **תוספת-ב** · auto-refresh עדין ל-live-feed (stream של ה-buffer) בלי full-rebuild של ה-tab.

## 99. Per-customer journey + Gate #120 + complete erasure-sweep + WIRING/docs (קנוני R1-3/4/6, R2-#12)

> ⚠️ קנוני: gate = **#120** (לא 118; P1=118/P4=119) · join = **uid/actorKey** (לא שם) · `displayName`
> **לא** על-החוט · erasure-sweep **רב-מפתחי** עם **טסט-שלמות** · ניזון מ-`actorStitch/{uid}` (R1-3).

1. 🎯 **היעד** · ציר-מסע פר-לקוח (drill קיים, join לפי **uid/actorKey**) + **Gate #120** לפרטיות + **erasure-sweep שלם** + סנכרון WIRING/docs; section additive בלבד.
2. 🔧 **איך בונים** (file:line) · ב-`manager_dashboard_screen.dart:1998` `_CustomerDetailSheet` להוסיף section "מסע הלקוח" — timeline אנכי של IntelEvents מסוננים לפי **`uid`/`actorKey` של הלקוח** (`:2018` `o.who==c.name` → join-by-uid; השם resolved owner-side מ-`orders`/`customers`, R1-4), כבר `watch` חי, emoji+תווית-עברית+זמן-יחסי, stuck מודגש ב-`_StagePill`. להוסיף **Gate #120** (grep `lib/state/intel/` למפתח-PII/raw-text) ל-`knowledge/GATE_REGISTRY.md`+repo-root `.githooks/pre-commit`; **לרשום מראש 118/119/120** (P1/P4/P3), next-free→**121**. **erasure-sweep** (Cloud-Function, Pillar 5) שמוחק עבור נושא-נתון: `events/` ב-`where('actorKey', whereIn:[uid, ...anonKeys])` (anon-keys מתוך **`actorStitch/{uid}`**, R1-3) **+** `presence/{uid}` **וגם** `presence/{actorKey}` **+** `sessions/` (אם קיים) **+** `actorStitch/{uid}` עצמו **+** שורות-`displayName`-only (איפוס `displayName→''`). לעדכן `WIRING.md`+`README/ARCHITECTURE/DECISIONS`.
3. ⚠️ **תקלות צפויות** · ה-timeline סורק את כל ה-events (איטי); join-לפי-שם ממזג כפילי-שם (R2-#12); `presence/{uid}` נשכח מה-sweep; אירועי-anon-pre-stitch יתומים בלתי-נמחקים; שורות-`displayName`-only נשארות PII; WIRING.md לא מסונכרן → `knowledge_protocol_test.dart:91-101` נשבר; Gate #120 false-positive; שכחת `cp` ל-`.git/hooks/`.
4. ✅ **פתרון** · סינון ל-`uid`/`actorKey` של אותו לקוח + חלון-זמן (O(events-של-הלקוח)); ה-sweep קורא `actorStitch/{uid}` כדי לאחות anon→uid; allowlist-מפתחות מדויק ל-Gate #120; שורת-WIRING לכל אירוע מחווט; פרוצדורת add-a-gate כולל `cp` ל-`.git/hooks/`.
5. 🧪 **בדיקות** (שמות-קבצים) · cases ל-`test/realtime_wiring_test.dart` (`_FakeSource` `:34-49`, DOWN/UP) + `knowledge_protocol_test.dart` ירוק (gate 94) + `test/intel/journey_timeline_test.dart` (render + join-by-**uid** + כפילי-שם נפרדים) + **`test/intel/erasure_completeness_test.dart`** (חובה): seed אירועי-anon + אירועי-uid + `presence/{uid}` + `presence/{actorKey}` + שורת-`displayName`-only + `actorStitch/{uid}` → הרצת-sweep → **assert אפס שאריות** בכל הקולקציות לנושא (כולל pre-stitch-anon).
6. 📈 **שיפור** · "erase this customer" כפתור-מנהל שמפעיל את ה-sweep הרב-מפתחי (Pillar 5 Function) — זכות-מחיקה תיקון-13 §14 מעשית בלחיצה.
7. 🤔 **ריאלי?** · כן. section additive על drill קיים; ה-sweep מכני כי הסכמה תוכננה לכך (`actorKey`/`uid` + `actorStitch`), והשם owner-side בלבד.
8. 🔒 **ווידוא-פיקס** (DoD) · `flutter analyze` 0 + realtime-wiring+journey+**erasure-completeness**+protocol tests ירוקים + **Gate #120** ב-`GATE_REGISTRY.md`(118/119/120 רשומים, next-free→121)+`.githooks/pre-commit`+`cp` ל-`.git/hooks/` + grep `lib/state/intel/` אפס מפתח-PII + אפס `displayName` על-החוט + WIRING.md מסנכרן כל אירוע מחווט.
9. 💡 **תוספת-א** · ייצוא-מסע (PDF/CSV) לפניות-נושא-מידע (תיקון-13 §13 זכות-עיון) — owner-only, governance #84.
10. 💡 **תוספת-ב** · annotation על שורות-תקיעה ב-timeline עם ה-"next-best-action" (מצעד 95) ישר במסך-המנהל.

## 100. Platform-wide GA lock — STAGED-flip forward ON, full gate + owner-approval ("100, not 99")

> ⚠️ קנוני (R2-#17): ה-flip הפלטפורמתי מדליק ~12 דגלים — **לרצף אותם כ-N תת-flips, כל-אחד deploy+soak
> נפרד (bisectable)**, לא big-bang. M4 canary (אחוז-config-publish) הוא **ציר שונה**, לא תחליף.

1. 🎯 **היעד** · הנעילה הסופית: להדליק את ה-forward ב-**רצף תת-flips bisectable** ולעבור gate-מלא + owner-approval פר-מודול — סיום ה-"100%, לא 99". ה-flip מותר רק אחרי שטקסט-המדיניות **+ מודאל-ההסכמה** (86) נחתו.
2. 🔧 **איך בונים** (file:line) · ברירת-המחדל כבר **DENY** (שלב-86: `privAnalytics=false` `app_settings.dart:105` + `consentedPolicyVersion` gate) — ה-"flip" כאן = הדלקת ~12 הדגלים-הפלטפורמתיים **ברצף**, **כל דגל deploy+soak משלו** (אם משהו נשבר — bisect לדגל-הבודד), NOT בבת-אחת. forward נשאר triple-gated (`consentedPolicyVersion>=kCurrentPolicyVersion && useFirebaseBackend` + presence על `privPresence`); לוודא ש-`telemetryProvider`(`telemetry.dart:132-135`)+`intelSinkProvider`+`presence` כולם ON-path רק כש-backend+consent-בתוקף; לאמת על מכשיר flags-ON פר-תת-flip. **M4 canary (אחוז-config-publish) = ציר-rollout נפרד**, לא חלק מרצף-הדגלים.
3. ⚠️ **תקלות צפויות** · big-bang של 12 דגלים בלי-bisect (R2-#17); flip לפני שטקסט+מודאל-86 נחתו (חשיפה משפטית); רגרסיה ב-demo כש-flags-ON; מודול ללא owner-approval נדלק; doc לא-מסונכרן שובר gate; ערבוב canary-% עם flag-flip.
4. ✅ **פתרון** · **רצף תת-flips** מתועד, כל-אחד deploy+soak+gate ירוק לפני הבא; חסם-קשיח על precondition-86 (טקסט+מודאל); smoke flags-OFF (demo) **וגם** flags-ON (live) → demo byte-identical; owner-approval פר-מודול (governance #84) כתנאי-שער; canary-% מנוהל בציר-נפרד; כלי-סנכרון-docs לפני merge.
5. 🧪 **בדיקות** (שמות-קבצים) · כל החבילה (356+ כולל `intel/*`, `realtime_wiring_test.dart`, `erasure_completeness_test.dart`, `knowledge_protocol_test.dart`) + smoke APK `--dart-define=USE_FIREBASE_BACKEND=true` פר-תת-flip + smoke demo (flags-OFF) להוכחת אפס-רגרסיה.
6. 📈 **שיפור** · **studio-usage observability רפלקסיבי** (R1-7): להחיל את עדשת-המודיעין על הבעלים — instrument edit-sessions / blocked-AI-ops / draft-abandon (`studio_edit_session`/`studio_ai_op_blocked`/`studio_draft_abandon` דרך אותו `IntelBus`, תחת אותם-מעקות; actors=בעלים → **ללא presence**, R1-5) כשלב-99-bis לפני הנעילה.
7. 🤔 **ריאלי?** · חלקי — תלוי-תיאום: רצף-ה-flips בצד-הלקוח ריאלי, אך הפעלה-מלאה תלויה ב-Pillar 5 (rules/indexes/TTL/rollup/erasure-Function פרוסים) ובאישור-בעלים פר-מודול. בלי אלה — ON רק חלקית.
8. 🔒 **ווידוא-פיקס** (DoD — ה-GA gate המלא) · על מכשיר, **פר-תת-flip**: (a) `flutter analyze` **0** (Gate 31); (b) **כל** החבילה ירוקה מול `known_failing.txt` (Gate 32) כולל `knowledge_protocol_test` (Gate 94); (c) `flutter build web --release` נקי (Gate 33) + **APK flags-ON** smoke; (d) Gate 46 + **Gate #120** (grep אפס מפתח-PII ב-`lib/state/intel/`) עוברים; (e) **owner-approval פר-מודול** מתועד (governance #84); (f) **כל** `WIRING.md` + knowledge (README/ARCHITECTURE/DECISIONS) מסונכרנים; (g) ה-flip ל-ON מאושר **רק** אחרי שטקסט+מודאל-86 נחתו; (h) ברירת-מחדל DENY מאומתת (`privAnalytics=false`). גייטים 31/32/33/46/94/**120** — **כולם** עוברים על device, פר-תת-flip. זה ה-"100, לא 99".
9. 💡 **תוספת-א** · kill-switch (remote-config flag) שמכבה את ה-forward מיידית בפרודקשן בלי deploy, אם מתגלה תקלת-פרטיות/עלות.
10. 💡 **תוספת-ב** · GA-readiness checklist חתום (per-module owner + תאריך + סדר-תת-flips) ב-`knowledge/` כ-artifact של הנעילה, כך שה-"100" ניתן-לביקורת אחורה.
