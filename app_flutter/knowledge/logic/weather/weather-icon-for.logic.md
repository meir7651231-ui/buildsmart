# weatherIconFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `weather` · `lib/services/weather.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(int code) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `weatherDaysFromOpenMeteo`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if code <= 0 → return '☀️'
- **precond** — if code <= 3 → return '⛅'
- **precond** — if code <= 48 → return '🌫️'
- **precond** — if code <= 67 → return '🌧️'
- **precond** — if code <= 77 → return '❄️'
- **precond** — if code <= 82 → return '🌦️'
- **return** — '⛈️'

## floor
- _none_

## חוזה · contract
- **input:** `(int code)`
- **output:** `String`
- **purity:** pure
- **precond:**
  - if code <= 0 → return '☀️'
  - if code <= 3 → return '⛅'
  - if code <= 48 → return '🌫️'
  - if code <= 67 → return '🌧️'
  - if code <= 77 → return '❄️'
  - if code <= 82 → return '🌦️'
- **throws:** _none_
