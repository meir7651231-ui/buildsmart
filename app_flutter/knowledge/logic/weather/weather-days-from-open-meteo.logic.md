# weatherDaysFromOpenMeteo — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `weather` · `lib/services/weather.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Map<String, dynamic> json) -> List<WeatherDay>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_dayLabel` · `weatherIconFor` · `weatherNoteFor`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — daily = json['daily']
- **precond** — if daily is! Map → return const []
- **compute** — times = (daily['time'] as List?) ?? const []
- **compute** — codes = (daily['weather_code'] as List?) ?? const []
- **compute** — temps = (daily['temperature_2m_max'] as List?) ?? const []
- **init** — out = <WeatherDay>[]
- **loop** — for var i = 0; i < times.length && i < 4; i++
- **compute** — code = (i < codes.length && codes[i] is num) ? (codes[i] as num).toInt() : 0
- **compute** — t = (i < temps.length && temps[i] is num) ? temps[i] as num : null
- **effect** — out.add(WeatherDay(day: _dayLabel(i), ic: weatherIconFor(code), temp: t == null ? '—' : '…
- **return** — out

## floor
- `WeatherDay` · `add` · `round` · `toInt`

## חוזה · contract
- **input:** `(Map<String, dynamic> json)`
- **output:** `List<WeatherDay>`
- **purity:** pure
- **precond:**
  - if daily is! Map → return const []
- **throws:** _none_
