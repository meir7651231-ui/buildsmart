# weatherNoteFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `weather` · `lib/services/weather.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(int code) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `weatherDaysFromOpenMeteo`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if code <= 3 → return 'מזג אוויר טוב לעבודות חוץ'
- **precond** — if code <= 48 → return 'ערפל — זהירות בנהיגה לאתר'
- **precond** — if code <= 67 → return '⚠️ גשם — לדחות יציקות בטון'
- **precond** — if code <= 77 → return '⚠️ שלג — עבודות פנים בלבד'
- **precond** — if code <= 82 → return '⚠️ ממטרים — עבודות גמר בלבד'
- **return** — '⚠️ סופה — להימנע מעבודות חוץ'

## floor
- _none_

## חוזה · contract
- **input:** `(int code)`
- **output:** `String`
- **purity:** pure
- **precond:**
  - if code <= 3 → return 'מזג אוויר טוב לעבודות חוץ'
  - if code <= 48 → return 'ערפל — זהירות בנהיגה לאתר'
  - if code <= 67 → return '⚠️ גשם — לדחות יציקות בטון'
  - if code <= 77 → return '⚠️ שלג — עבודות פנים בלבד'
  - if code <= 82 → return '⚠️ ממטרים — עבודות גמר בלבד'
- **throws:** _none_
