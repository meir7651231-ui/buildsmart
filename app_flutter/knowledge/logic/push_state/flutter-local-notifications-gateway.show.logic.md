# FlutterLocalNotificationsGateway.show — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `push_state` · `lib/state/push_state.dart`.

## עצם · object
- **kind:** method
- **signature:** `(PushMessage message, {required String channelId}) -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPushChannels` · `_plugin`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — channel = kPushChannels.firstWhere((c) => c.id == channelId, orElse: () => kPushChannels.first)
- **compute** — details = NotificationDetails(android: AndroidNotificationDetails(channel.id, channel.name, channel…
- **compute** — id = DateTime.now().millisecondsSinceEpoch.remainder(100000)
- **effect** — await _plugin.show(id, message.title, message.body, details)

## floor
- `AndroidNotificationDetails` · `NotificationDetails` · `firstWhere` · `now` · `remainder` · `show`

## חוזה · contract
- **input:** `(PushMessage message, {required String channelId})`
- **output:** `Future<void>`
- **purity:** pure
- **throws:** _none_
