# pushToastText — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `push_state` · `lib/state/push_state.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(PushMessage message) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `PushController._handleForeground`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — title = message.title?.trim() ?? ''
- **compute** — body = message.body?.trim() ?? ''
- **precond** — if title.isEmpty && body.isEmpty → return null
- **precond** — if title.isEmpty → return body
- **precond** — if body.isEmpty → return title
- **return** — '$title · $body'

## floor
- `trim`

## חוזה · contract
- **input:** `(PushMessage message)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if title.isEmpty && body.isEmpty → return null
  - if title.isEmpty → return body
  - if body.isEmpty → return title
- **null when:** title.isEmpty && body.isEmpty · or search exhausted
- **throws:** _none_
