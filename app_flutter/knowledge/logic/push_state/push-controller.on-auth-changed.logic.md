# PushController.onAuthChanged — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `push_state` · `lib/state/push_state.dart`.

## עצם · object
- **kind:** method
- **signature:** `(AuthSnapshot snapshot) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kFcmTokenField↝` · `_disposed` · `_gateway↝` · `_local↝` · `_users↝`
- **writes:** `field:_chain↝` · `field:_uid`
- **calls:** `_enqueue` · `_unregister` · `_register`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if _disposed → return (void)
- **compute** — uid = snapshot.user?.uid
- **precond** — if uid == _uid → return (void)
- **compute** — previous = _uid
- **effect** — _uid = uid
- **branch** — if previous != null
- **branch** — if uid != null

## floor
- _none_

## חוזה · contract
- **input:** `(AuthSnapshot snapshot)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if _disposed → return (void)
  - if uid == _uid → return (void)
- **throws:** _none_
