# PushController._register — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `push_state` · `lib/state/push_state.dart`.

## עצם · object
- **kind:** method
- **signature:** `(String uid) -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kFcmTokenField` · `_gateway` · `_local` · `_uid` · `_users`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `PushController.onAuthChanged`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — g = _gateway
- **compute** — users = _users
- **precond** — if g == null || users == null → return (void)
- **precond** — if _uid != uid → return (void)
- **compute** — local = _local
- **branch** — if local != null
- **compute** — granted = await g.requestPermission()
- **branch** — if !granted
- **precond** — if _uid != uid → return (void)
- **branch** — if local != null
- **precond** — if _uid != uid → return (void)
- **compute** — token = await g.getToken()
- **branch** — if token == null || token.isEmpty
- **precond** — if _uid != uid → return (void)
- **effect** — await users.set(uid, <String, dynamic>{kFcmTokenField : token})

## floor
- `debugPrint` · `ensureInitialised` · `getToken` · `requestAndroid13Permission` · `requestPermission` · `set`

## חוזה · contract
- **input:** `(String uid)`
- **output:** `Future<void>`
- **purity:** pure
- **precond:**
  - if g == null || users == null → return (void)
  - if _uid != uid → return (void)
  - if _uid != uid → return (void)
  - if _uid != uid → return (void)
  - if _uid != uid → return (void)
- **throws:** _none_
