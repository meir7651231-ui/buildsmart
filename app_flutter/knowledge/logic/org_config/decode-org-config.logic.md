# decodeOrgConfig — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `org_config` · `lib/config/org_config.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String raw) -> OrgConfig?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_stringField` · `_jsonMapField` · `_boolMapField` · `_stringMapField`
- **called-by:** `resolveOrgConfig`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — root = 
- **precond** — if root is! Map → return null
- **compute** — version = root['v']
- **precond** — if version is! num || version != 1 → return null
- **return** — OrgConfig(slug: _stringField(root['slug']), orgName: _stringField(root['orgName']), theme…

## floor
- `OrgConfig` · `jsonDecode`

## חוזה · contract
- **input:** `(String raw)`
- **output:** `OrgConfig?`
- **purity:** pure
- **precond:**
  - if root is! Map → return null
  - if version is! num || version != 1 → return null
- **null when:** root is! Map · version is! num || version != 1 · or search exhausted
- **throws:** _none_
