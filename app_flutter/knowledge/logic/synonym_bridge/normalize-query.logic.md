# normalizeQuery — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `synonym_bridge` · `lib/features/word_finder/synonym_bridge.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String raw) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kHebrewQuerySynonyms` · `_kLatinQuerySynonyms`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `resolveQuery`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — s = raw
- **loop** — for final e in _kLatinQuerySynonyms.entries
- **effect** — s = s.replaceAll(RegExp('\\b${e.key}\\b', caseSensitive: false), e.value)
- **compute** — tokens = s.split(RegExp(r'\s+')).where((t) => t.isNotEmpty).map((t) => _kHebrewQuerySynonyms[t] ??…
- **return** — tokens.join(' ')

## floor
- `RegExp` · `join` · `map` · `replaceAll` · `split` · `toList` · `where`

## חוזה · contract
- **input:** `(String raw)`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
