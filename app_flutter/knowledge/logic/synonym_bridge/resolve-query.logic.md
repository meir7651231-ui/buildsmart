# resolveQuery — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `synonym_bridge` · `lib/features/word_finder/synonym_bridge.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String raw, WordLexicon lexicon) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kHebrewQuerySynonyms↝` · `_kLatinQuerySynonyms↝` · `kDivePool` · `kMaterials`
- **writes:** _none_
- **calls:** `normalizeQuery`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — tokens = normalizeQuery(raw).split(RegExp(r'\s+')).where((t) => t.isNotEmpty).toList()
- **precond** — if tokens.isEmpty → return const <String>[]
- **init** — perToken = <Set<String>>[]
- **loop** — for final tok in tokens
- **compute** — products = kMaterials.containsKey(tok) ? productsOfMaterial(kDivePool, tok) : resolveWord(tok, lexic…
- **effect** — perToken.add({for (final p in products) p.sku})
- **compute** — hits = perToken.first
- **loop** — for final s in perToken.skip(1)
- **effect** — hits = hits.intersection(s)
- **branch** — if hits.isEmpty
- **return** — [for (final p in kDivePool) if (hits.contains(p.sku)) p.sku]

## floor
- `RegExp` · `add` · `contains` · `containsKey` · `intersection` · `productsOfMaterial` · `resolveWord` · `skip` · `split` · `toList` · `where`

## חוזה · contract
- **input:** `(String raw, WordLexicon lexicon)`
- **output:** `List<String>`
- **purity:** pure
- **precond:**
  - if tokens.isEmpty → return const <String>[]
- **throws:** _none_
