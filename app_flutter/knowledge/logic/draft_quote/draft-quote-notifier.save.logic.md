# DraftQuoteNotifier.save — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `draft_quote` · `lib/state/draft_quote.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({required String label, required String text}) -> DraftQuote`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_key↝` · `_repo↝` · `maxEntries`
- **writes:** `field:_loaded↝` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — now = DateTime.now()
- **compute** — id = '${now.microsecondsSinceEpoch}_${label.hashCode}'
- **compute** — q = DraftQuote(id: id, label: label, text: text, savedAt: now)
- **compute** — filtered = state.where((d) => d.label != label).toList()
- **init** — next = [...filtered, q]
- **effect** — state = next.length > maxEntries ? next.sublist(next.length - maxEntries) : next
- **effect** — _persist()
- **return** — q

## floor
- `DraftQuote` · `now` · `sublist` · `toList` · `where`

## חוזה · contract
- **input:** `({required String label, required String text})`
- **output:** `DraftQuote`
- **purity:** side-effecting
- **throws:** _none_
