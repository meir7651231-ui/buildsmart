# ChatMessage.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `sys_chat` · `lib/state/sys_chat.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> ChatMessage?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `fromRole` · `fromUid` · `status`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — id = raw['id']
- **compute** — threadId = raw['threadId']
- **compute** — text = raw['text']
- **compute** — ts = DateTime.tryParse('${raw['ts']}')
- **compute** — role = BsRole.values.where((r) => r.name == raw['fromRole']).cast<BsRole?>().firstWhere((_) => t…
- **precond** — if id is! String || threadId is! String || text is! String || ts == null || role == null → return null
- **return** — ChatMessage(id: id, threadId: threadId, fromRole: role, text: text, ts: ts, fromUid: raw[…

## floor
- `ChatMessage` · `cast` · `firstWhere` · `tryParse` · `where`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `ChatMessage?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if id is! String || threadId is! String || text is! String || ts == null || role == null → return null
- **null when:** raw is! Map · id is! String || threadId is! String || text is! String || ts == null || role == null · or search exhausted
- **throws:** _none_
