# ChatEngineNotifier.send — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `sys_chat` · `lib/state/sys_chat.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String threadId, BsRole fromRole, String text, {String fromUid = ''}) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBotAutoReplies` · `kSysChatKey↝` · `kSysChatPersistCap↝` · `_loadSettled↝` · `_remote` · `currentUid↝` · `lookup↝` · `persist↝` · `uidScoped↝` · `mounted↝` · `unmodifiable↝`
- **writes:** `field:_loaded↝` · `io:prefs.setString↝` · `state:_dirtyThreadIds` · `state:_uidResolveInFlight↝` · `state:state`
- **calls:** `ensureParticipantUids` · `_mk`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **effect** — ensureParticipantUids(threadId)
- **compute** — r = _remote
- **branch** — if r != null
- **compute** — trimmed = text.trim()
- **precond** — if trimmed.isEmpty → return (void)
- **compute** — idx = state.indexWhere((t) => t.id == threadId)
- **precond** — if idx < 0 → return (void)
- **compute** — thread = state[idx]
- **compute** — now = DateTime.now()
- **init** — appended = <ChatMessage>[...thread.messages, _mk(threadId, fromRole, trimmed, now, fromUid: fromUid)]
- **branch** — if thread.isBot && fromRole != BsRole.bot
- **effect** — _dirtyThreadIds.add(threadId)
- **effect** — state = [for (var i = 0; i < state.length; i++) if (i == idx) thread.copyWith(messages: a…

## floor
- `Duration` · `add` · `copyWith` · `indexWhere` · `now` · `send` · `trim` · `where`

## חוזה · contract
- **input:** `(String threadId, BsRole fromRole, String text, {String fromUid = ''})`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if trimmed.isEmpty → return (void)
  - if idx < 0 → return (void)
- **throws:** _none_
