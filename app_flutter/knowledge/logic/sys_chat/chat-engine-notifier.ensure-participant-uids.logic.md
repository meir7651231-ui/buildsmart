# ChatEngineNotifier.ensureParticipantUids — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `sys_chat` · `lib/state/sys_chat.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String threadId) -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kSysChatKey↝` · `kSysChatPersistCap↝` · `_loadSettled↝` · `_remote` · `currentUid` · `lookup` · `persist↝` · `uidScoped` · `mounted` · `unmodifiable`
- **writes:** `field:_loaded↝` · `io:prefs.setString↝` · `state:_uidResolveInFlight` · `state:state`
- **calls:** _none_
- **called-by:** `ChatEngineNotifier.send`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !uidScoped → return (void)
- **precond** — if _uidResolveInFlight.contains(threadId) → return (void)
- **compute** — me = currentUid?.call()
- **precond** — if me == null || me.isEmpty → return (void)
- **compute** — idx = state.indexWhere((t) => t.id == threadId)
- **precond** — if idx < 0 → return (void)
- **precond** — if state[idx].participantUids.isNotEmpty → return (void)
- **compute** — roles = state[idx].participants
- **effect** — _uidResolveInFlight.add(threadId)

## floor
- `List` · `add` · `addAll` · `call` · `contains` · `copyWith` · `indexWhere` · `remove` · `setParticipantUids` · `uidsByRole`

## חוזה · contract
- **input:** `(String threadId)`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if !uidScoped → return (void)
  - if _uidResolveInFlight.contains(threadId) → return (void)
  - if me == null || me.isEmpty → return (void)
  - if idx < 0 → return (void)
  - if state[idx].participantUids.isNotEmpty → return (void)
- **throws:** _none_
