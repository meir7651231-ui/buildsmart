# ChatEngineNotifier.createOrGetThread — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `sys_chat` · `lib/state/sys_chat.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(List<String> participantUids, {String name = '', String avatar = '💬'}) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kSysChatKey↝` · `kSysChatPersistCap↝` · `_loadSettled↝` · `_remote` · `persist↝`
- **writes:** `field:_loaded↝` · `io:prefs.setString↝` · `state:_dirtyThreadIds` · `state:state`
- **calls:** `dmThreadUids` · `dmThreadId`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — uids = dmThreadUids(participantUids)
- **compute** — id = dmThreadId(participantUids)
- **compute** — r = _remote
- **precond** — if r != null → return r.createOrGetThread(participantUids, name: name, avatar: avatar)
- **precond** — if uids.length < 2 → return id
- **precond** — if state.any((t) => t.id == id) → return id
- **effect** — _dirtyThreadIds.add(id)
- **effect** — state = [...state, ChatThread(id: id, participants: const [], participantUids: uids, name…
- **return** — id

## floor
- `ChatThread` · `add` · `any` · `createOrGetThread`

## חוזה · contract
- **input:** `(List<String> participantUids, {String name = '', String avatar = '💬'})`
- **output:** `String`
- **purity:** side-effecting
- **precond:**
  - if r != null → return r.createOrGetThread(participantUids, name: name, avatar: avatar)
  - if uids.length < 2 → return id
  - if state.any((t) => t.id == id) → return id
- **throws:** _none_
