# computeStockForecast — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `ai_hub_logic` · `/home/user/buildsmart/app_flutter/lib/logic/ai_hub_logic.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<Order> orders, List<SmartCartLine> cart, {DateTime? now}) -> List<StockPred>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — anchor = now ?? DateTime.now()
- **init** — onHand = <String, int>{}
- **loop** — for final l in cart
- **effect** — onHand[l.productName] = (onHand[l.productName] ?? 0) + l.productQty
- **init** — consumed = <String, int>{}
- **init** — firstSeen = <String, DateTime>{}
- **init** — lastSeen = <String, DateTime>{}
- **init** — emojiByName = <String, String>{}
- **init** — unitPriceByName = <String, int>{}
- **loop** — for final o in orders
- **compute** — ts = o.createdAt ?? anchor
- **loop** — for final li in o.lines
- **init** — out = <StockPred>[]
- **loop** — for final name in consumed.keys
- **compute** — used = consumed[name]!
- **branch** — if used <= 0 → continue
- **compute** — span = lastSeen[name]!.difference(firstSeen[name]!).inDays
- **compute** — spanDays = span < 1 ? 1 : span
- **compute** — rate = (used / spanDays).round()
- **compute** — r = rate < 1 ? 1 : rate
- **compute** — stock = onHand[name] ?? 0
- **compute** — days = (stock / r).round()
- **effect** — out.add(StockPred(name: name, stock: stock, rate: r, days: days, emoji: emojiByName[name]…
- **effect** — out.sort((a, b) {final byDays = a.days.compareTo(b.days); return byDays != 0 ? byDays : a…
- **return** — out

## floor
- `StockPred` · `add` · `compareTo` · `difference` · `isBefore` · `now` · `round` · `sort`

## חוזה · contract
- **input:** `(List<Order> orders, List<SmartCartLine> cart, {DateTime? now})`
- **output:** `List<StockPred>`
- **purity:** pure
- **throws:** _none_
