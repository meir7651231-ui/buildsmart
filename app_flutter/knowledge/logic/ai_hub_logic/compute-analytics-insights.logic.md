# computeAnalyticsInsights — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `ai_hub_logic` · `/home/user/buildsmart/app_flutter/lib/logic/ai_hub_logic.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<Order> orders) -> List<Insight>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBudgetSpent` · `kBudgetTotal` · `kHomeProductBrands↝`
- **writes:** _none_
- **calls:** `aiAlternatives`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — out = <Insight>[]
- **compute** — orderCount = orders.length
- **compute** — totalSpend = orders.fold<int>(0, (s, o) => s + o.sum)
- **compute** — openCount = orders.where((o) => o.isOpen).length
- **compute** — deliveredCount = orderCount - openCount
- **branch** — if orderCount > 0
- **compute** — savings = aiAlternatives().fold<int>(0, (s, a) => s + a.save)
- **branch** — if savings > 0
- **compute** — budgetPctValue = kBudgetTotal > 0 ? (kBudgetSpent / kBudgetTotal * 100).round() : 0
- **compute** — budgetLeft = kBudgetTotal - kBudgetSpent
- **effect** — out.add(Insight(ic: '📊', title: 'ניצול תקציב: $budgetPctValue%', sub: 'נותרו ${fMoney(bu…
- **return** — out

## floor
- `Insight` · `add` · `fMoney` · `fold` · `round` · `where`

## חוזה · contract
- **input:** `(List<Order> orders)`
- **output:** `List<Insight>`
- **purity:** pure
- **throws:** _none_
