# aiAlternatives — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `ai_hub_logic` · `/home/user/buildsmart/app_flutter/lib/logic/ai_hub_logic.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> List<AiAlt>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kHomeProductBrands`
- **writes:** _none_
- **calls:** `_pricedSmartProduct`
- **called-by:** `computeAnalyticsInsights`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — out = <AiAlt>[]
- **init** — seen = <String>{}
- **loop** — for final pb in kHomeProductBrands
- **compute** — sp = _pricedSmartProduct(pb)
- **compute** — recI = sp.brands.indexWhere((b) => b.rec)
- **compute** — rec = sp.brands[recI >= 0 ? recI : 0]
- **compute** — alt = cheaperAlternativeBrand(sp, recI >= 0 ? recI : 0)
- **branch** — if alt == null || rec.price == null → continue
- **branch** — if !seen.add(pb.product) → continue
- **effect** — out.add(AiAlt(cat: pb.product, fromName: rec.name, fromPrice: rec.price!, toName: alt.nam…
- **loop** — for final CheaperAlt a in cheaperAlternativesAcrossCatalog()
- **branch** — if !seen.add(a.product) → continue
- **effect** — out.add(AiAlt(cat: a.product, fromName: a.recName, fromPrice: a.recPrice, toName: a.altNa…
- **effect** — out.sort((a, b) => b.save.compareTo(a.save))
- **return** — out.length > 5 ? out.sublist(0, 5) : out

## floor
- `AiAlt` · `add` · `cheaperAlternativeBrand` · `cheaperAlternativesAcrossCatalog` · `compareTo` · `indexWhere` · `sort` · `sublist`

## חוזה · contract
- **input:** `()`
- **output:** `List<AiAlt>`
- **purity:** pure
- **throws:** _none_
