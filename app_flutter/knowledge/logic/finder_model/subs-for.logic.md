# subsFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `finder_model` · `lib/atoms/finder_model.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(FinderGroup group, List<LipskeyCatalogProduct> base) -> List<FinderSub>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kFinderSubs`
- **writes:** _none_
- **calls:** `_cleanSub`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — present = <String>{for (final p in base) p.categoryHe}
- **compute** — curated = kFinderSubs[group.label]
- **precond** — if curated != null → return [for (final s in curated) if (s.cats.any(present.contains)) s]
- **init** — cats = <String, Set<String>>{}
- **init** — counts = <String, int>{}
- **loop** — for final p in base
- **compute** — l = _cleanSub(p.categoryHe)
- **effect** — (cats[l] ??= <String>{}).add(p.categoryHe)
- **effect** — counts[l] = (counts[l] ?? 0) + 1
- **compute** — labels = cats.keys.toList()..sort((a, b) => counts[b]!.compareTo(counts[a]!))
- **return** — [for (final l in labels) FinderSub(l, cats[l]!)]

## floor
- `FinderSub` · `add` · `any` · `compareTo` · `sort` · `toList`

## חוזה · contract
- **input:** `(FinderGroup group, List<LipskeyCatalogProduct> base)`
- **output:** `List<FinderSub>`
- **purity:** pure
- **precond:**
  - if curated != null → return [for (final s in curated) if (s.cats.any(present.contains)) s]
- **throws:** _none_
