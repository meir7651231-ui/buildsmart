# productsForGroup — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `finder_model` · `lib/atoms/finder_model.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(FinderGroup g) -> List<LipskeyCatalogProduct>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `claimedCats`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if g.cats.isEmpty → return catalogRepo().allProducts().where((p) => !claimedCats.contains(p.categoryHe)).toList()
- **return** — catalogRepo().allProducts().where((p) => g.cats.contains(p.categoryHe)).toList()

## floor
- `allProducts` · `catalogRepo` · `contains` · `toList` · `where`

## חוזה · contract
- **input:** `(FinderGroup g)`
- **output:** `List<LipskeyCatalogProduct>`
- **purity:** pure
- **precond:**
  - if g.cats.isEmpty → return catalogRepo().allProducts().where((p) => !claimedCats.contains(p.categoryHe)).toList()
- **throws:** _none_
