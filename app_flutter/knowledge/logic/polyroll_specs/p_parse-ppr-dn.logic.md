# _parsePprDn — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `polyroll_specs` · `lib/data/polyroll_specs.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `polyrollSpecFor`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **loop** — for final key in const ['dn נומינלי', 'מידה נומינלית', 'd', 'de קוטר חיצוני', 'D']
- **compute** — v = p.dims?[key]?.toString()
- **branch** — if v != null && v.isNotEmpty
- **compute** — size = p.dims?['מידה']?.toString()
- **branch** — if size != null
- **compute** — m = RegExp(r'(\d{2,3})\s*$').firstMatch(p.nameHe.replaceAll('°', ''))
- **precond** — if m != null → return m.group(1)
- **compute** — m2 = RegExp(r'(\d{2,3})\s*[×x]').firstMatch(p.nameHe)
- **return** — m2?.group(1)

## floor
- `RegExp` · `firstMatch` · `group` · `replaceAll` · `toInt` · `toString` · `tryParse`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if m != null → return m.group(1)
- **null when:** no result
- **throws:** _none_
