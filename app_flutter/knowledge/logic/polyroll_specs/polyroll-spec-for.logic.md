# polyrollSpecFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `polyroll_specs` · `lib/data/polyroll_specs.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> VerifiedSpec?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kElectroCats` · `_kFusionCats` · `_kPprMaxTempC` · `kPprPipesAC↝` · `kPprPipesFiber↝`
- **writes:** _none_
- **calls:** `_parsePprDn` · `_pprMaterial` · `_portCountFor` · `_pressureFromDims`
- **called-by:** `registerPolyrollSpecs`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !_kFusionCats.contains(p.categoryHe) && !_kElectroCats.contains(p.categoryHe) → return null
- **compute** — dn = _parsePprDn(p)
- **precond** — if dn == null → return null
- **compute** — material = _pprMaterial(p)
- **compute** — ports = _portCountFor(p)
- **init** — ends = List<ConnectorEnd>.generate(ports, (_) => ConnectorEnd(EndType.hdpeCompression, dn))
- **return** — VerifiedSpec(sku: p.sku, ends: ends, material: material, pressureRating: _pressureFromDim…

## floor
- `ConnectorEnd` · `List` · `VerifiedSpec` · `contains`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `VerifiedSpec?`
- **purity:** pure
- **precond:**
  - if !_kFusionCats.contains(p.categoryHe) && !_kElectroCats.contains(p.categoryHe) → return null
  - if dn == null → return null
- **null when:** !_kFusionCats.contains(p.categoryHe) && !_kElectroCats.contains(p.categoryHe) · dn == null · or search exhausted
- **throws:** _none_
