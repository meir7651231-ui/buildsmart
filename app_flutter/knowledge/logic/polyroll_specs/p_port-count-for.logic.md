# _portCountFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `polyroll_specs` · `lib/data/polyroll_specs.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `polyrollSpecFor`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = p.productType ?? ''
- **precond** — if t.contains('צינור') → return 2
- **precond** — if t.contains('פקק') → return 1
- **precond** — if t.contains('מסעף') || t.contains('רוכב') → return 3
- **precond** — if t.contains('אומגה') → return 2
- **return** — 2

## floor
- `contains`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `int`
- **purity:** pure
- **precond:**
  - if t.contains('צינור') → return 2
  - if t.contains('פקק') → return 1
  - if t.contains('מסעף') || t.contains('רוכב') → return 3
  - if t.contains('אומגה') → return 2
- **throws:** _none_
