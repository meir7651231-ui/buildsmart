# RewardsNotifier.claimChallenge — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `rewards_state` · `lib/state/rewards_state.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kMonthlyChallengesSeed↝` · `persist↝`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `_syncMe` · `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — c = 
- **loop** — for final x in state.challenges
- **branch** — if x.id == id
- **precond** — if c == null || !c.done → return (void)
- **effect** — _userTouched = true
- **compute** — coins = state.coins + c.reward
- **effect** — state = state.copyWith(coins: coins, challenges: [for (final x in state.challenges) if (x…
- **effect** — _persist()

## floor
- `copyWith`

## חוזה · contract
- **input:** `(String id)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if c == null || !c.done → return (void)
- **throws:** _none_
