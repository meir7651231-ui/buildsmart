# RewardsNotifier.redeem — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `rewards_state` · `lib/state/rewards_state.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(RwReward r) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kMonthlyChallengesSeed↝` · `persist↝`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `_syncMe` · `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if state.coins < r.cost → return false
- **effect** — _userTouched = true
- **compute** — coins = state.coins - r.cost
- **effect** — state = state.copyWith(coins: coins, leaderboard: _syncMe(coins))
- **effect** — _persist()
- **return** — true

## floor
- `copyWith`

## חוזה · contract
- **input:** `(RwReward r)`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if state.coins < r.cost → return false
- **throws:** _none_
