# FindSession.pool — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `find_session` · `lib/features/card_keyboard/find_session.dart`.

## עצם · object
- **kind:** getter
- **signature:** `get () -> List<LipskeyCatalogProduct>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kDivePool` · `stack`
- **writes:** `field:_poolCache`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — cached = _poolCache
- **precond** — if cached != null → return cached
- **compute** — p = kDivePool
- **loop** — for final s in stack
- **effect** — p = p.where(s.predicate).toList()
- **return** — _poolCache = p

## floor
- `toList` · `where`

## חוזה · contract
- **input:** `()`
- **output:** `List<LipskeyCatalogProduct>`
- **purity:** side-effecting
- **precond:**
  - if cached != null → return cached
- **throws:** _none_
