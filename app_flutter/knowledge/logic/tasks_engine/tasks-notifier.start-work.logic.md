# TasksNotifier.startWork — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int id, {String? workerUid, String? employerId}) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** `state:state↝`
- **calls:** `_byId` · `_patch` · `_stampIdentity`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = _byId(id)
- **precond** — if t == null → return (void)
- **precond** — if t.status != 'active' && t.status != 'rejected' → return (void)
- **precond** — if t.startedAt != null → return (void)
- **effect** — _patch(id, (x) => _stampIdentity(x.copyWith(startedAt: DateTime.now()), workerUid: worker…

## floor
- `copyWith` · `now`

## חוזה · contract
- **input:** `(int id, {String? workerUid, String? employerId})`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if t == null → return (void)
  - if t.status != 'active' && t.status != 'rejected' → return (void)
  - if t.startedAt != null → return (void)
- **throws:** _none_
