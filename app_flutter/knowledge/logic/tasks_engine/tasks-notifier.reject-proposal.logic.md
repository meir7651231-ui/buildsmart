# TasksNotifier.rejectProposal — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int id, {String? reason}) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kTaskRejectNoteSideMapKey↝` · `_ref` · `persist↝` · `workerNotifsProvider`
- **writes:** `io:prefs.setString↝` · `state:state↝`
- **calls:** `_byId` · `_patch` · `_saveRejectNote`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = _byId(id)
- **precond** — if t == null || t.status != 'proposed' → return (void)
- **effect** — _patch(id, (x) => x.copyWith(status: 'rejected'))
- **compute** — why = reason?.trim()
- **branch** — if why != null && why.isNotEmpty
- **effect** — _ref?.read(workerNotifsProvider.notifier).addForWorker(t.worker, emoji: '🔁', title: (why…

## floor
- `addForWorker` · `copyWith` · `read` · `trim`

## חוזה · contract
- **input:** `(int id, {String? reason})`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if t == null || t.status != 'proposed' → return (void)
- **throws:** _none_
