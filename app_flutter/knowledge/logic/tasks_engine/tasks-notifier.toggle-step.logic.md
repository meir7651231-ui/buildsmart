# TasksNotifier.toggleStep — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int taskId, int stepIndex) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `from`
- **writes:** `state:state↝`
- **calls:** `_byId` · `_patch`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = _byId(taskId)
- **precond** — if t == null → return (void)
- **precond** — if stepIndex < 0 || stepIndex >= t.steps.length → return (void)
- **effect** — _patch(taskId, (x) {final next = Set<int>.from(x.doneSteps); if (!next.add(stepIndex)) ne…

## floor
- `Set` · `add` · `copyWith` · `remove`

## חוזה · contract
- **input:** `(int taskId, int stepIndex)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if t == null → return (void)
  - if stepIndex < 0 || stepIndex >= t.steps.length → return (void)
- **throws:** _none_
