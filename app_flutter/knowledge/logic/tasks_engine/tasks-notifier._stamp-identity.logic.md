# TasksNotifier._stampIdentity — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(TaskItem t, {String? workerUid, String? employerId}) -> TaskItem`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `TasksNotifier.startWork` · `TasksNotifier.submitForReview`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — uid = workerUid?.trim()
- **compute** — emp = employerId?.trim()
- **precond** — if (uid == null || uid.isEmpty) && (emp == null || emp.isEmpty) → return t
- **return** — t.copyWith(assignedWorkerUid: (uid != null && uid.isNotEmpty) ? uid : null, employerId: (…

## floor
- `copyWith` · `trim`

## חוזה · contract
- **input:** `(TaskItem t, {String? workerUid, String? employerId})`
- **output:** `TaskItem`
- **purity:** pure
- **precond:**
  - if (uid == null || uid.isEmpty) && (emp == null || emp.isEmpty) → return t
- **throws:** _none_
