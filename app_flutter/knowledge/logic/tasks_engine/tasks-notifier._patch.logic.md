# TasksNotifier._patch — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_engine` · `../../../app_flutter/lib/state/tasks_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int id, TaskItem Function(TaskItem) f) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPersonaTasks↝` · `kTaskClockSideMapKey↝` · `kTasksRuntimeKey↝` · `kTasksScreenKey↝` · `persist↝`
- **writes:** `field:_loaded↝` · `io:prefs.setString↝` · `state:state`
- **calls:** _none_
- **called-by:** `TasksNotifier.approve` · `TasksNotifier.approveProposal` · `TasksNotifier.assignTask` · `TasksNotifier.attachPhoto` · `TasksNotifier.editTask` · `TasksNotifier.reject` · `TasksNotifier.rejectProposal` · `TasksNotifier.startWork` · `TasksNotifier.submitForReview` · `TasksNotifier.toggleStep`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — idx = state.indexWhere((t) => t.id == id)
- **precond** — if idx < 0 → return (void)
- **effect** — state = [for (var i = 0; i < state.length; i++) if (i == idx) f(state[i]) else state[i]]

## floor
- `f` · `indexWhere`

## חוזה · contract
- **input:** `(int id, TaskItem Function(TaskItem) f)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if idx < 0 → return (void)
- **throws:** _none_
