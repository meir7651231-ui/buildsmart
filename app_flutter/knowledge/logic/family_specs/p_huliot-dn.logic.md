# _huliotDn — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `family_specs` · `lib/data/family_specs.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_huliotSpecFor`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — n = p.nameHe
- **compute** — r = RegExp(r'(\d{2,4})\s*[×xX]\s*(\d{2,4})').firstMatch(n)
- **precond** — if r != null → return r.group(1)
- **compute** — o = RegExp(r'Ø\s*(\d{2,4})').firstMatch(n)
- **precond** — if o != null → return o.group(1)
- **compute** — t = RegExp(r'(\d{2,4})\s*$').firstMatch(n.replaceAll('°', ''))
- **precond** — if t != null → return t.group(1)
- **compute** — m = RegExp(r'\b(\d{2,4})\b').firstMatch(n)
- **return** — m?.group(1)

## floor
- `RegExp` · `firstMatch` · `group` · `replaceAll`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if r != null → return r.group(1)
  - if o != null → return o.group(1)
  - if t != null → return t.group(1)
- **null when:** no result
- **throws:** _none_
