# _huliotSpecFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `family_specs` · `lib/data/family_specs.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> VerifiedSpec?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kHuliotNonFitting` · `_kHuliotPorts`
- **writes:** _none_
- **calls:** `_huliotDn`
- **called-by:** `familySpecFor` · `registerFamilySpecs`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if p.brand != 'Huliot' → return null
- **precond** — if _kHuliotNonFitting.contains(p.categoryHe) → return null
- **compute** — dn = _huliotDn(p)
- **precond** — if dn == null → return null
- **compute** — ports = _kHuliotPorts[p.categoryHe] ?? 2
- **return** — VerifiedSpec(sku: p.sku, ends: List<ConnectorEnd>.generate(ports, (_) => ConnectorEnd(End…

## floor
- `ConnectorEnd` · `List` · `VerifiedSpec` · `contains`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `VerifiedSpec?`
- **purity:** pure
- **precond:**
  - if p.brand != 'Huliot' → return null
  - if _kHuliotNonFitting.contains(p.categoryHe) → return null
  - if dn == null → return null
- **null when:** p.brand != 'Huliot' · _kHuliotNonFitting.contains(p.categoryHe) · dn == null · or search exhausted
- **throws:** _none_
