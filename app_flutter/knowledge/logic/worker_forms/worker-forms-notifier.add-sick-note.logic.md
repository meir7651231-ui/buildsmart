# WorkerFormsNotifier.addSickNote — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_forms` · `lib/state/worker_forms.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({required String username, required String photo, String signature = '', bool declared = false}) -> Future<SickNote?>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_seq` · `repo↝` · `storageKey↝` · `mounted`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **effect** — _userTouched = true
- **compute** — note = SickNote(id: 'sick-${DateTime.now().microsecondsSinceEpoch}-${_seq++}', username: usernam…
- **compute** — before = state
- **effect** — state = state.copyWith(sickNotes: [...state.sickNotes, note])
- **compute** — ok = await _persist()
- **branch** — if !ok
- **return** — note

## floor
- `SickNote` · `copyWith` · `now`

## חוזה · contract
- **input:** `({required String username, required String photo, String signature = '', bool declared = false})`
- **output:** `Future<SickNote?>`
- **purity:** side-effecting
- **throws:** _none_
