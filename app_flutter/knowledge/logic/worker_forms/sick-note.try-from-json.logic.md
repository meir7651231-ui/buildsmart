# SickNote.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_forms` · `lib/state/worker_forms.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> SickNote?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `declared` · `signature`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — id = raw['id']
- **compute** — username = raw['username']
- **compute** — photo = raw['photo']
- **compute** — ts = DateTime.tryParse('${raw['ts']}')
- **precond** — if id is! String || username is! String || photo is! String || ts == null → return null
- **return** — SickNote(id: id, username: username, ts: ts, photo: photo, signature: raw['signature'] is…

## floor
- `SickNote` · `tryParse`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `SickNote?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if id is! String || username is! String || photo is! String || ts == null → return null
- **null when:** raw is! Map · id is! String || username is! String || photo is! String || ts == null · or search exhausted
- **throws:** _none_
