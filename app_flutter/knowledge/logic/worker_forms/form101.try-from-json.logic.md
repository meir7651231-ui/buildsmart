# Form101.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_forms` · `lib/state/worker_forms.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> Form101?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `declared` · `employerAddress` · `employerBusinessId` · `employerName` · `fullName` · `idNumber` · `maritalStatus` · `phone` · `savedTs` · `sentTs` · `signature` · `specialty`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — username = raw['username']
- **compute** — year = raw['year']
- **compute** — saved = DateTime.tryParse('${raw['savedTs']}')
- **precond** — if username is! String || year is! int || saved == null → return null
- **return** — Form101(username: username, year: year, fullName: s(raw['fullName']), idNumber: s(raw['id…

## floor
- `Form101` · `s` · `tryParse`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `Form101?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if username is! String || year is! int || saved == null → return null
- **null when:** raw is! Map · username is! String || year is! int || saved == null · or search exhausted
- **throws:** _none_
