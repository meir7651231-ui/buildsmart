# smartProjectProgress — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `smart_project_engine` · `lib/state/smart_project_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Set<String> doneKeys) -> ({int done, int total, int pct})`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPersonaTasks↝` · `kTaskDetail↝` · `kTaskSteps↝`
- **writes:** _none_
- **calls:** `buildDayStages`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — stages = buildDayStages()
- **compute** — total = stages.length
- **compute** — done = stages.where((s) => doneKeys.contains(s.key)).length
- **compute** — pct = total == 0 ? 0 : (done / total * 100).round()
- **return** — (done: done, total: total, pct: pct)

## floor
- `contains` · `round` · `where`

## חוזה · contract
- **input:** `(Set<String> doneKeys)`
- **output:** `({int done, int total, int pct})`
- **purity:** pure
- **throws:** _none_
