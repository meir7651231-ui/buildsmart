# layoutRoute — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `route_layout` · `lib/features/fittings/layout/route_layout.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<RunElement> route) -> List<Placement>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `math`
- **writes:** _none_
- **calls:** `_dirVec`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — p = const Vec3(0, 0, 0)
- **init** — f = const Vec3(1, 0, 0)
- **init** — u = const Vec3(0, 1, 0)
- **init** — out = <Placement>[]
- **loop** — for final el in route
- **compute** — delta = layoutDeltaFor(el.family.label, el.od, od2: el.od2)
- **branch** — if delta == null → continue
- **compute** — b = _dirVec(el.dir, f, u)
- **effect** — out.add(Placement(family: el.family.label, od: el.od, position: p, forward: f, up: u))
- **compute** — bendAxis = delta.turn != 0 ? b : u
- **effect** — p = p + f.scale(delta.ex) + bendAxis.scale(delta.ey)
- **branch** — if delta.turn != 0
- **branch** — if delta.terminal → break
- **return** — out

## floor
- `Placement` · `Vec3` · `add` · `cos` · `dot` · `layoutDeltaFor` · `norm` · `scale` · `sin`

## חוזה · contract
- **input:** `(List<RunElement> route)`
- **output:** `List<Placement>`
- **purity:** pure
- **throws:** _none_
