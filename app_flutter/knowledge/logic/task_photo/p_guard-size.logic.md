# _guardSize — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `task_photo` · `lib/services/task_photo.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(BuildContext context, String dataUrl) -> Future<String?>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kMaxPhotoDataUrlChars`
- **writes:** _none_
- **calls:** `_dataUrlBytes`
- **called-by:** `_guardAndDeliver`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if dataUrl.length <= kMaxPhotoDataUrlChars → return dataUrl
- **compute** — bytes = _dataUrlBytes(dataUrl)
- **compute** — smaller = bytes == null ? null : await downscaleToJpegDataUrl(bytes, maxWidth: 640, quality: 0.5)
- **precond** — if smaller != null && smaller.length <= kMaxPhotoDataUrlChars → return smaller
- **branch** — if context.mounted
- **return** — null

## floor
- `downscaleToJpegDataUrl` · `showToast`

## חוזה · contract
- **input:** `(BuildContext context, String dataUrl)`
- **output:** `Future<String?>`
- **purity:** pure
- **precond:**
  - if dataUrl.length <= kMaxPhotoDataUrlChars → return dataUrl
  - if smaller != null && smaller.length <= kMaxPhotoDataUrlChars → return smaller
- **throws:** _none_
