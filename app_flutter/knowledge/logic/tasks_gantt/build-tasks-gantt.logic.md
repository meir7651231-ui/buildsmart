# buildTasksGantt — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `tasks_gantt` · `/home/user/buildsmart/app_flutter/lib/logic/tasks_gantt.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<TaskItem> tasks) -> TasksGanttLayout`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_dateOnly` · `_donePercent`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — scheduled = <TaskItem>[]
- **init** — unscheduled = <TaskItem>[]
- **loop** — for final t in tasks
- **branch** — if t.scheduledStart != null
- **precond** — if scheduled.isEmpty → return TasksGanttLayout(bars: const [], spanDays: 0, unscheduled: unscheduled)
- **compute** — earliest = _dateOnly(scheduled.first.scheduledStart!)
- **loop** — for final t in scheduled
- **compute** — d = _dateOnly(t.scheduledStart!)
- **branch** — if d.isBefore(earliest)
- **init** — bars = <GanttBar>[for (final t in scheduled) GanttBar(taskId: t.id, name: t.name, startDay: () {…
- **compute** — spanDays = 0
- **loop** — for final b in bars
- **compute** — end = b.startDay + b.lenDays
- **branch** — if end > spanDays
- **return** — TasksGanttLayout(bars: bars, spanDays: spanDays, unscheduled: unscheduled)

## floor
- `GanttBar` · `TasksGanttLayout` · `add` · `compareTo` · `daysBetweenDst` · `isBefore` · `sort`

## חוזה · contract
- **input:** `(List<TaskItem> tasks)`
- **output:** `TasksGanttLayout`
- **purity:** pure
- **precond:**
  - if scheduled.isEmpty → return TasksGanttLayout(bars: const [], spanDays: 0, unscheduled: unscheduled)
- **throws:** _none_
