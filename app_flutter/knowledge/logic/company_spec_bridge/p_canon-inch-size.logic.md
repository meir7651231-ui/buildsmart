# _canonInchSize — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `company_spec_bridge` · `lib/data/company_spec_bridge.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String text) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBspInchToMm` · `_kInchValueToKey`
- **writes:** _none_
- **calls:** `_expandInchFractions`
- **called-by:** `_parseEnd`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = text
- **loop** — for final mark in const ['"', '״', "'", '׳', '″', '′']
- **effect** — t = t.replaceAll(mark, ' ')
- **effect** — t = _expandInchFractions(t)
- **effect** — t = t.replaceAllMapped(RegExp(r'(\d+)\s+\.(\d+)'), (m) => '${m[1]}.${m[2]}')
- **compute** — v = 
- **compute** — mixed = RegExp(r'(\d+)[\s-]+(\d+)\s*/\s*(\d+)').firstMatch(t)
- **branch** — if mixed != null
- **precond** — if v == null → return null
- **compute** — key = _kInchValueToKey[v]
- **precond** — if key == null || !kBspInchToMm.containsKey(key) → return null
- **return** — '$key"'

## floor
- `RegExp` · `containsKey` · `firstMatch` · `parse` · `replaceAll` · `replaceAllMapped` · `tryParse`

## חוזה · contract
- **input:** `(String text)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if v == null → return null
  - if key == null || !kBspInchToMm.containsKey(key) → return null
- **null when:** v == null · key == null || !kBspInchToMm.containsKey(key) · or search exhausted
- **throws:** _none_
