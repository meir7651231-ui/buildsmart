# _parseEnd — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `company_spec_bridge` · `lib/data/company_spec_bridge.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String text) -> ConnectorEnd?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBspInchToMm↝` · `_kBareMm` · `_kInchValueToKey↝`
- **writes:** _none_
- **calls:** `_canonInchSize` · `_mmSize`
- **called-by:** `companySpecFor`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — upper = text.toUpperCase()
- **branch** — if text.contains('תבריג') || text.contains('הברגה')
- **branch** — if text.contains('פקס') || upper.contains('PEX')
- **branch** — if text.contains('נחושת')
- **init** — socketWords = ['הדבקה', 'דבק', 'שקע', 'מופה', 'כניסה']
- **branch** — if socketWords.any(text.contains) || upper.startsWith('DN') || _kBareMm.hasMatch(text)
- **branch** — if text.contains('פתח ניקוז')
- **return** — null

## floor
- `ConnectorEnd` · `any` · `contains` · `hasMatch` · `replaceFirst` · `startsWith` · `toUpperCase` · `trim`

## חוזה · contract
- **input:** `(String text)`
- **output:** `ConnectorEnd?`
- **purity:** pure
- **null when:** no result
- **throws:** _none_
