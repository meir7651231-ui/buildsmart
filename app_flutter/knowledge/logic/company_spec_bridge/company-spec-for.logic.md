# companySpecFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `company_spec_bridge` · `lib/data/company_spec_bridge.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> VerifiedSpec?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kEngineMaterials↝` · `_kBareMm↝`
- **writes:** _none_
- **calls:** `_parseEnd` · `_normalizeMaterial` · `_pressureRating` · `_maxTempC`
- **called-by:** `registerCompanySpecs`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — ends = <ConnectorEnd>[]
- **loop** — for final key in const ['קצה 1', 'קצה 2', 'קצה 3']
- **compute** — cell = p.dims?[key]?.toString().trim()
- **branch** — if cell == null || cell.isEmpty → continue
- **compute** — end = _parseEnd(cell)
- **branch** — if end != null
- **precond** — if ends.isEmpty → return null
- **compute** — material = _normalizeMaterial(p.dims?['חומר']?.toString().trim() ?? '')
- **precond** — if material.isEmpty && ends.any((e) => e.type == EndType.hdpeCompression) → return null
- **compute** — pressure = _pressureRating(p)
- **compute** — maxTemp = _maxTempC(p)
- **precond** — if maxTemp != null → return VerifiedSpec(sku: p.sku, ends: ends, material: material, pressureRating: pressure, maxTem…
- **return** — VerifiedSpec(sku: p.sku, ends: ends, material: material, pressureRating: pressure)

## floor
- `VerifiedSpec` · `add` · `any` · `toString` · `trim`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `VerifiedSpec?`
- **purity:** pure
- **precond:**
  - if ends.isEmpty → return null
  - if material.isEmpty && ends.any((e) => e.type == EndType.hdpeCompression) → return null
  - if maxTemp != null → return VerifiedSpec(sku: p.sku, ends: ends, material: material, pressureRating: pressure, maxTem…
- **null when:** ends.isEmpty · material.isEmpty && ends.any((e) => e.type == EndType.hdpeCompression) · or search exhausted
- **throws:** _none_
