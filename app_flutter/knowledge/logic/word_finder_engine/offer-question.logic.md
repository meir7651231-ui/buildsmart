# offerQuestion — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `word_finder_engine` · `lib/features/word_finder/word_finder_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> pool, List<NewbieStep> stack, WordLexicon lexicon, String? subtype) -> NewbieQuestion`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kAxisFallbackQuestion` · `kAxisQuestion` · `kFirstQuestion` · `kFirstWordCount` · `kShowProductsThreshold`
- **writes:** _none_
- **calls:** `wordsByFrequency` · `distinctCardCount` · `distinctProducts` · `bestUnansweredAxis`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if stack.isEmpty → return AskWords(kFirstQuestion, wordsByFrequency(lexicon).take(kFirstWordCount).toList())
- **precond** — if pool.isNotEmpty && distinctCardCount(pool) <= 1 → return Resolve(pool.first, pool)
- **precond** — if distinctCardCount(pool) <= kShowProductsThreshold → return ShowProducts(distinctProducts(pool))
- **init** — answeredAxes = {for (final s in stack) s.axisLabel}
- **compute** — best = bestUnansweredAxis(pool, answeredAxes)
- **precond** — if best != null → return AskAxis(kAxisQuestion[best.label] ?? kAxisFallbackQuestion, best.chips, best.label)
- **compute** — ax = offerAxis(pool, subtype)
- **compute** — axIsUnanswered = ax.label.isEmpty || !answeredAxes.contains(ax.label)
- **precond** — if axIsUnanswered && ax.chips.isNotEmpty → return AskAxis(kAxisQuestion[ax.label] ?? kAxisFallbackQuestion, ax.chips, ax.label)
- **return** — ShowProducts(distinctProducts(pool))

## floor
- `AskAxis` · `AskWords` · `Resolve` · `ShowProducts` · `contains` · `offerAxis` · `take` · `toList`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> pool, List<NewbieStep> stack, WordLexicon lexicon, String? subtype)`
- **output:** `NewbieQuestion`
- **purity:** pure
- **precond:**
  - if stack.isEmpty → return AskWords(kFirstQuestion, wordsByFrequency(lexicon).take(kFirstWordCount).toList())
  - if pool.isNotEmpty && distinctCardCount(pool) <= 1 → return Resolve(pool.first, pool)
  - if distinctCardCount(pool) <= kShowProductsThreshold → return ShowProducts(distinctProducts(pool))
  - if best != null → return AskAxis(kAxisQuestion[best.label] ?? kAxisFallbackQuestion, best.chips, best.label)
  - if axIsUnanswered && ax.chips.isNotEmpty → return AskAxis(kAxisQuestion[ax.label] ?? kAxisFallbackQuestion, ax.chips, ax.label)
- **throws:** _none_
