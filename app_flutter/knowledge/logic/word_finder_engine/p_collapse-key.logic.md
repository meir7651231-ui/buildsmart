# _collapseKey — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `word_finder_engine` · `lib/features/word_finder/word_finder_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kWhitespace`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_buildReachUniverse` · `collapseKeyOf` · `distinctProducts`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — remove = <String>{}
- **loop** — for final t in parseSizeTokens(p.nameHe)
- **effect** — remove.add(t.label)
- **loop** — for final t in parseAngleTokens(p.nameHe)
- **effect** — remove.add(t.label)
- **compute** — color = p.color?.trim()
- **compute** — stripped = p.nameHe
- **loop** — for final r in remove
- **branch** — if r.isNotEmpty
- **branch** — if color != null && color.isNotEmpty
- **effect** — stripped = stripped.split(_kWhitespace).where((w) => w.isNotEmpty).join(' ')
- **return** — '${p.brand}||${p.categoryHe}||$stripped'

## floor
- `add` · `join` · `parseAngleTokens` · `parseSizeTokens` · `replaceAll` · `split` · `trim` · `where`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
