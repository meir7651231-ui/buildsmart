# testDupes — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `dupes` · `lib/test_harness/tests/dupes.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> List<TestResult>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBrands` · `kCatalogCats` · `kCatalogTree` · `kSmartProducts`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — checks = <TestCheck>[]
- **compute** — catIds = kCatalogCats.map((c) => c.id).toList()
- **compute** — catIdSet = catIds.toSet()
- **effect** — checks.add(TestCheck(name: 'אין כפילות ב-id של kCatalogCats', pass: catIdSet.length == ca…
- **init** — byTitle = <String, List<String>>{}
- **loop** — for final c in kCatalogCats
- **effect** — (byTitle[c.title] ??= []).add(c.id)
- **compute** — titleClashes = byTitle.entries.where((e) => e.value.length > 1).toList()
- **effect** — checks.add(TestCheck(name: 'אין קטגוריות בעלות title זהה', pass: titleClashes.isEmpty, de…
- **compute** — keys = kSmartProducts.map((p) => p.key).toList()
- **compute** — keySet = keys.toSet()
- **effect** — checks.add(TestCheck(name: 'אין כפילות ב-key של kSmartProducts', pass: keySet.length == k…
- **init** — byNameCat = <String, List<String>>{}
- **loop** — for final p in kSmartProducts
- **effect** — (byNameCat['${p.cat}::${p.name}'] ??= []).add(p.key)
- **compute** — ncClashes = byNameCat.entries.where((e) => e.value.length > 1).toList()
- **effect** — checks.add(TestCheck(name: 'אין מוצרים חכמים עם name+cat זהה', pass: ncClashes.isEmpty, d…
- **init** — accDupes = <String>[]
- **loop** — for final p in kSmartProducts
- **compute** — accNames = p.acc.map((a) => a.name).toList()
- **branch** — if accNames.toSet().length != accNames.length
- **effect** — checks.add(TestCheck(name: 'אביזרים בתוך כל מוצר חכם — ללא כפילות שמות', pass: accDupes.i…
- **compute** — brandIds = kBrands.map((b) => b.id).toList()
- **compute** — brandIdSet = brandIds.toSet()
- **effect** — checks.add(TestCheck(name: 'אין כפילות ב-id של kBrands', pass: brandIdSet.length == brand…
- **compute** — brandNames = kBrands.map((b) => b.name).toList()
- **compute** — brandNameSet = brandNames.toSet()
- **effect** — checks.add(TestCheck(name: 'אין כפילות ב-name של kBrands', pass: brandNameSet.length == b…
- **init** — nodeIds = <String>[]
- **loop** — for final n in kCatalogTree
- **effect** — walkTree(n)
- **compute** — nodeIdSet = nodeIds.toSet()
- **effect** — checks.add(TestCheck(name: 'אין כפילות ב-id של nodes בעץ הקטלוג', pass: nodeIdSet.length …
- **compute** — leaves = allLeaves()
- **init** — lipskeyCategories = <String>[]
- **loop** — for final l in leaves
- **branch** — if l.lipskeyCategory != null
- **compute** — lipskeyCatSet = lipskeyCategories.toSet()
- **effect** — checks.add(TestCheck(name: 'אין כפילות ב-lipskeyCategory בין עלים', pass: lipskeyCatSet.l…
- **return** — [TestResult(id: 'dupes:core', category: TestCategory.dupes, label: 'בדיקת זהויות וכפילויו…

## floor
- `TestCheck` · `TestResult` · `add` · `allLeaves` · `join` · `map` · `take` · `toList` · `toSet` · `walkTree` · `where`

## חוזה · contract
- **input:** `()`
- **output:** `List<TestResult>`
- **purity:** pure
- **throws:** _none_
