# ConnectionResolver._endPair — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `connection_resolver` · `/home/user/buildsmart/app_flutter/lib/domain/connection_resolver.dart`.

## עצם · object
- **kind:** method
- **signature:** `(ProductEnd endA, ProductEnd endB) -> ConnectResult`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_noRule` · `rules`
- **writes:** _none_
- **calls:** `_sizeOk`
- **called-by:** `ConnectionResolver._endPairMemoized`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — firstSizeMiss = 
- **loop** — for final rule in rules
- **compute** — forward = rule.aTypeId == endA.connectorTypeId && rule.bTypeId == endB.connectorTypeId
- **compute** — reverse = !forward && rule.aTypeId == endB.connectorTypeId && rule.bTypeId == endA.connectorTypeId
- **branch** — if !forward && !reverse → continue
- **branch** — if _sizeOk(rule, endA, endB, forward: forward) → return ConnectResult(mates: true, methodLabelHe: rule.methodLabelHe, rule: rule)
- **effect** — firstSizeMiss ??= rule
- **precond** — if firstSizeMiss != null → return ConnectResult(mates: false, methodLabelHe: '', severity: firstSizeMiss.onMismatch, rule: …
- **return** — _noRule

## floor
- `ConnectResult`

## חוזה · contract
- **input:** `(ProductEnd endA, ProductEnd endB)`
- **output:** `ConnectResult`
- **purity:** pure
- **precond:**
  - if firstSizeMiss != null → return ConnectResult(mates: false, methodLabelHe: '', severity: firstSizeMiss.onMismatch, rule: …
- **throws:** _none_
