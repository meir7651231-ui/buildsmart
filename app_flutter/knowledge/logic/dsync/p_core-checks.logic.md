# _coreChecks — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `dsync` · `lib/test_harness/tests/dsync.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> TestResult`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_validPersonaIds` · `kCatalogCats` · `kPersonas` · `kProfileEmptyCatalog` · `kSmartProducts` · `kSmartTreeCats`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `testDsync`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — checks = <TestCheck>[]
- **effect** — add('kPersonas מכיל 5 דמויות', kPersonas.length == 5, expected: '5', got: '${kPersonas.le…
- **loop** — for final p in kPersonas
- **effect** — add('persona id חוקי: ${p.id}', _validPersonaIds.contains(p.id), expected: _validPersonaI…
- **effect** — add('kCatalogCats אינו ריק', kCatalogCats.isNotEmpty, got: '${kCatalogCats.length}')
- **loop** — for final cat in kCatalogCats
- **effect** — add('קטגוריה ${cat.id} עם title', cat.title.isNotEmpty, got: cat.title)
- **effect** — add('קטגוריה ${cat.id} עם emoji', cat.emoji.isNotEmpty, got: cat.emoji)
- **effect** — add(kProfileEmptyCatalog ? 'kSmartProducts ריק (קונכייה נקייה)' : 'kSmartProducts אינו רי…
- **effect** — add('kSmartTreeCats אינו ריק', kSmartTreeCats.isNotEmpty, got: '${kSmartTreeCats.length}')
- **compute** — treeCats = kSmartTreeCats.toSet()
- **compute** — orphans = kSmartProducts.where((p) => !treeCats.contains(p.cat))
- **effect** — add('כל מוצר חכם משתייך לקטגוריה ידועה', orphans.isEmpty, expected: '0 יתומים', got: '${o…
- **loop** — for final cat in kSmartTreeCats
- **compute** — list = smartProductsForCat(cat)
- **effect** — add('smartProductsForCat("$cat") מחזיר ≥1', list.isNotEmpty, got: '${list.length}')
- **return** — TestResult(id: 'dsync:core', category: TestCategory.dsync, label: 'סנכרון נתונים-תצוגה (א…

## floor
- `TestCheck` · `TestResult` · `add` · `contains` · `join` · `smartProductsForCat` · `toSet` · `where`

## חוזה · contract
- **input:** `()`
- **output:** `TestResult`
- **purity:** pure
- **throws:** _none_
