# _brandsAndTreeChecks — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `dsync` · `lib/test_harness/tests/dsync.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> TestResult`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBrands` · `kCatalogProducts` · `kCatalogTree` · `kLipskeyCatalog`
- **writes:** `io:unknownBrandRefs.add`
- **calls:** _none_
- **called-by:** `testDsync`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — checks = <TestCheck>[]
- **effect** — add('kBrands מכיל ≥1 מותגים', kBrands.isNotEmpty, got: '${kBrands.length}')
- **loop** — for final b in kBrands
- **effect** — add('מותג ${b.id}: id לא ריק', b.id.isNotEmpty, got: b.id)
- **effect** — add('מותג ${b.id}: name לא ריק', b.name.isNotEmpty, got: b.name)
- **effect** — add('מותג ${b.id}: emoji לא ריק', b.emoji.isNotEmpty, got: b.emoji)
- **effect** — add('מותג ${b.id}: color != 0', b.color != 0, got: '0x${b.color.toRadixString(16)}')
- **loop** — for final b in kBrands
- **compute** — found = brandById(b.id)
- **effect** — add('brandById("${b.id}") מחזיר מותג', found != null, got: found?.id ?? 'null')
- **effect** — add('kCatalogTree מכיל ≥1 קטגוריות ראשיות', kCatalogTree.isNotEmpty, got: '${kCatalogTree…
- **compute** — leaves = allLeaves()
- **effect** — add('allLeaves() מחזיר ≥1 עלים', leaves.isNotEmpty, got: '${leaves.length}')
- **compute** — leavesNoBrands = leaves.where((l) => l.brandIds.isEmpty).toList()
- **effect** — add('כל עלה בעץ הקטלוג מכיל ≥1 brandId', leavesNoBrands.isEmpty, expected: '0 עלים בלי מו…
- **compute** — brandIdSet = kBrands.map((b) => b.id).toSet()
- **init** — unknownBrandRefs = <String>[]
- **loop** — for final l in leaves
- **loop** — for final bid in l.brandIds
- **effect** — add('כל brandId בעץ הקטלוג קיים ב-kBrands', unknownBrandRefs.isEmpty, expected: '0 הפניות…
- **init** — allIds = <String>[]
- **loop** — for final n in kCatalogTree
- **effect** — collectIds(n)
- **compute** — missingIds = allIds.where((id) => findCatalogNode(id) == null).toList()
- **effect** — add('findCatalogNode מוצא כל node בעץ', missingIds.isEmpty, expected: '0 nodes חסרים', go…
- **effect** — add('kLipskeyCatalog מכיל ≥1 מוצרים', kLipskeyCatalog.isNotEmpty, got: '${kLipskeyCatalog…
- **compute** — badLipskey = 0
- **loop** — for final p in kLipskeyCatalog
- **branch** — if p.sku.isEmpty || p.nameHe.isEmpty || p.categoryHe.isEmpty
- **effect** — add('כל מוצר ליפסקי עם sku+nameHe+categoryHe', badLipskey == 0, expected: '0 פגומים', got…
- **compute** — lipskeyCategoryNames = kCatalogProducts.map((p) => p.categoryHe).toSet()
- **compute** — leavesWithLipskey = leaves.where((l) => l.lipskeyCategory != null).toList()
- **compute** — unmatchedLeaves = leavesWithLipskey.where((l) => !lipskeyCategoryNames.contains(l.lipskeyCategory)).toList()
- **effect** — add('כל lipskeyCategory בעלים מצביע לקטגוריה קיימת', unmatchedLeaves.isEmpty, expected: '…
- **return** — TestResult(id: 'dsync:brands-tree', category: TestCategory.dsync, label: 'מותגים, עץ קטלו…

## floor
- `TestCheck` · `TestResult` · `add` · `allLeaves` · `brandById` · `collectIds` · `contains` · `findCatalogNode` · `join` · `map` · `take` · `toList` · `toRadixString` · `toSet` · `where`

## חוזה · contract
- **input:** `()`
- **output:** `TestResult`
- **purity:** side-effecting
- **throws:** _none_
