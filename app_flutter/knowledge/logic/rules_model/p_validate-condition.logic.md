# _validateCondition — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `rules_model` · `/home/user/buildsmart/app_flutter/lib/logic/studio/rules_model.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Object? raw) -> RuleCondition?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kRuleConditionFields↝` · `kRuleOps↝` · `num`
- **writes:** _none_
- **calls:** `matchConditionField` · `matchRuleOp`
- **called-by:** `parseRule`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — c = raw.map((k, v) => MapEntry(k.toString(), v))
- **compute** — field = matchConditionField((c['field'] ?? '').toString())
- **precond** — if field == null → return null
- **compute** — op = matchRuleOp((c['op'] ?? '').toString())
- **precond** — if op == null → return null
- **compute** — rawValue = c['value']
- **compute** — value = rawValue is num ? rawValue : num.tryParse((rawValue ?? '').toString())
- **precond** — if value == null → return null
- **return** — RuleCondition(field: field, op: op, value: value)

## floor
- `MapEntry` · `RuleCondition` · `map` · `toString` · `tryParse`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `RuleCondition?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if field == null → return null
  - if op == null → return null
  - if value == null → return null
- **null when:** raw is! Map · field == null · op == null · value == null · or search exhausted
- **throws:** _none_
