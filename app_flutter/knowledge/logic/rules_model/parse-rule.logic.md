# parseRule — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `rules_model` · `/home/user/buildsmart/app_flutter/lib/logic/studio/rules_model.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String raw) -> Rule?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kRuleActionIds↝` · `kRuleTriggerIds↝` · `num↝`
- **writes:** _none_
- **calls:** `matchTriggerId` · `matchRuleActionId` · `_validateCondition`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — text = raw.trim()
- **compute** — start = text.indexOf('{')
- **precond** — if start < 0 → return null
- **compute** — end = text.lastIndexOf('}')
- **precond** — if end <= start → return null
- **compute** — candidate = text.substring(start, end + 1)

## floor
- `MapEntry` · `Rule` · `indexOf` · `jsonDecode` · `lastIndexOf` · `map` · `substring` · `toString` · `trim`

## חוזה · contract
- **input:** `(String raw)`
- **output:** `Rule?`
- **purity:** pure
- **precond:**
  - if start < 0 → return null
  - if end <= start → return null
- **null when:** start < 0 · end <= start · or search exhausted
- **throws:** _none_
