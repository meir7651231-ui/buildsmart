# FulfillmentNotifier.capturePod — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `persona_fulfillment` · `lib/state/persona_fulfillment.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id, String dataUrl, {String? courierUser}) -> Future<bool>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kPodPhotosKey↝` · `kFulfillmentKey↝` · `_diskRead↝` · `persist↝` · `mounted`
- **writes:** `field:_loaded` · `io:prefs.setString↝` · `state:state`
- **calls:** `of` · `_persist` · `_mirrorPodPhoto`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — before = state
- **init** — next = {...state, id : of(id).copyWith(podPhoto: dataUrl, courierUser: courierUser)}
- **effect** — _loaded = true
- **effect** — super.state = next
- **compute** — ok = await _persist()
- **branch** — if !ok
- **compute** — mirrored = await _mirrorPodPhoto(id, dataUrl)
- **branch** — if !mirrored
- **return** — true

## floor
- `copyWith`

## חוזה · contract
- **input:** `(String id, String dataUrl, {String? courierUser})`
- **output:** `Future<bool>`
- **purity:** side-effecting
- **throws:** _none_
