# FulfillmentNotifier.missLine — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `persona_fulfillment` · `lib/state/persona_fulfillment.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id, int line) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** `state:state↝`
- **calls:** `of` · `_put`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — f = of(id)
- **compute** — cur = f.lineStatus[line] ?? LineStatus.pending
- **init** — next = {...f.lineStatus}
- **compute** — nowMissing = cur != LineStatus.missing && cur != LineStatus.pendingDecision
- **branch** — if nowMissing
- **compute** — stillPending = next.values.any((s) => s == LineStatus.pendingDecision)
- **effect** — _put(id, f.copyWith(lineStatus: next, heldForMissing: stillPending))
- **return** — nowMissing

## floor
- `any` · `copyWith`

## חוזה · contract
- **input:** `(String id, int line)`
- **output:** `bool`
- **purity:** side-effecting
- **throws:** _none_
