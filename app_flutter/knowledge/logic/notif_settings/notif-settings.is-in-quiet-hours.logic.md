# NotifSettings.isInQuietHours — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `notif_settings` · `lib/state/notif_settings.dart`.

## עצם · object
- **kind:** getter
- **signature:** `get () -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `quietEndHour` · `quietEndMin` · `quietHoursEnabled` · `quietStartHour` · `quietStartMin`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !quietHoursEnabled → return false
- **compute** — now = DateTime.now()
- **compute** — nowMins = now.hour * 60 + now.minute
- **compute** — startMins = quietStartHour * 60 + quietStartMin
- **compute** — endMins = quietEndHour * 60 + quietEndMin
- **precond** — if startMins < endMins → return nowMins >= startMins && nowMins < endMins

## floor
- `now`

## חוזה · contract
- **input:** `()`
- **output:** `bool`
- **purity:** pure
- **precond:**
  - if !quietHoursEnabled → return false
  - if startMins < endMins → return nowMins >= startMins && nowMins < endMins
- **throws:** _none_
