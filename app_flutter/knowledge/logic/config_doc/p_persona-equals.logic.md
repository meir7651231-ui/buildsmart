# _personaEquals — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_doc` · `lib/state/studio/config_doc.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Map<String, Map<String, CfgNode>> a, Map<String, Map<String, CfgNode>> b) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `ConfigLayer.==`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if a.length != b.length → return false
- **loop** — for final k in a.keys
- **compute** — bm = b[k]
- **branch** — if bm == null || !mapEquals(a[k], bm) → return false
- **return** — true

## floor
- `mapEquals`

## חוזה · contract
- **input:** `(Map<String, Map<String, CfgNode>> a, Map<String, Map<String, CfgNode>> b)`
- **output:** `bool`
- **purity:** pure
- **precond:**
  - if a.length != b.length → return false
- **throws:** _none_
