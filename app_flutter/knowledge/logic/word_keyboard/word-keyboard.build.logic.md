# WordKeyboard.build — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `word_keyboard` · `lib/features/word_finder/word_keyboard.dart`.

## עצם · object
- **kind:** method
- **signature:** `(BuildContext context) -> Widget`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_wordsPerRow` · `onAll↝` · `onType↝` · `onWordTap↝` · `primary↝` · `showUtilityRow` · `words`
- **writes:** _none_
- **calls:** `_buildWordRow` · `_buildUtilityRow`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — rowWidgets = <Widget>[]
- **loop** — for var i = 0; i < words.length; i += _wordsPerRow
- **compute** — end = (i + _wordsPerRow < words.length) ? i + _wordsPerRow : words.length
- **effect** — rowWidgets..add(_buildWordRow(words.sublist(i, end)))..add(const SizedBox(height: BsToken…
- **branch** — if showUtilityRow
- **return** — Directionality(textDirection: TextDirection.ltr, child: Material(color: BsTokens.surfaceM…

## floor
- `Column` · `Directionality` · `Material` · `Padding` · `SizedBox` · `add` · `all` · `sublist`

## חוזה · contract
- **input:** `(BuildContext context)`
- **output:** `Widget`
- **purity:** pure
- **throws:** _none_
