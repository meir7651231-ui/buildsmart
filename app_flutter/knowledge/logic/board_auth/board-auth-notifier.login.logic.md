# BoardAuthNotifier.login — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `board_auth` · `lib/state/board_auth.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(BoardRole role, String username, String code) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBoardAccounts` · `kBoardAuthKey↝`
- **writes:** `field:_userTouched` · `io:prefs.remove↝` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — u = username.trim().toLowerCase()
- **compute** — c = code.replaceAll(RegExp(r'[\s-]'), '')
- **loop** — for final a in kBoardAccounts
- **branch** — if a.role == role && a.username == u && a.code == c
- **return** — false

## floor
- `BoardSession` · `RegExp` · `replaceAll` · `toLowerCase` · `trim`

## חוזה · contract
- **input:** `(BoardRole role, String username, String code)`
- **output:** `bool`
- **purity:** side-effecting
- **throws:** _none_
