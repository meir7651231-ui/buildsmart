# BoardAuthNotifier.impersonate — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `board_auth` · `lib/state/board_auth.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(BoardRole role) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBoardAccounts↝`
- **writes:** `field:_impersonationReturn` · `field:_userTouched` · `state:state`
- **calls:** `_seedFor`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — current = state
- **precond** — if current == null || current.role != BoardRole.manager → return (void)
- **compute** — seed = _seedFor(role)
- **precond** — if seed == null → return (void)
- **effect** — _impersonationReturn = current
- **effect** — _userTouched = true
- **effect** — state = BoardSession(role: seed.role, username: seed.username, displayName: seed.displayN…

## floor
- `BoardSession`

## חוזה · contract
- **input:** `(BoardRole role)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if current == null || current.role != BoardRole.manager → return (void)
  - if seed == null → return (void)
- **throws:** _none_
