# TradesStoreNotifier.upsertTrade — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `trades_store` · `lib/state/trades_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(Trade t) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kTradesKey↝`
- **writes:** `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — i = state.trades.indexWhere((x) => x.id == t.id)
- **precond** — if i >= 0 && state.trades[i] == t → return (void)
- **init** — next = [...state.trades]
- **branch** — if i >= 0
- **effect** — state = state.copyWith(trades: next)
- **effect** — _persist()

## floor
- `add` · `copyWith` · `indexWhere`

## חוזה · contract
- **input:** `(Trade t)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if i >= 0 && state.trades[i] == t → return (void)
- **throws:** _none_
