# TradesStoreNotifier.removeConnectorType — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `trades_store` · `lib/state/trades_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kTradesKey↝`
- **writes:** `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !state.connectorTypes.any((x) => x.id == id) → return (void)
- **effect** — state = state.copyWith(connectorTypes: state.connectorTypes.where((x) => x.id != id).toLi…
- **effect** — _persist()

## floor
- `any` · `copyWith` · `toList` · `where`

## חוזה · contract
- **input:** `(String id)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if !state.connectorTypes.any((x) => x.id == id) → return (void)
- **throws:** _none_
