# TradesStoreNotifier.upsertProduct — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `trades_store` · `lib/state/trades_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(TradeProduct p) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kTradesKey↝`
- **writes:** `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — i = state.products.indexWhere((x) => x.id == p.id)
- **precond** — if i >= 0 && state.products[i] == p → return (void)
- **init** — next = [...state.products]
- **branch** — if i >= 0
- **effect** — state = state.copyWith(products: next)
- **effect** — _persist()

## floor
- `add` · `copyWith` · `indexWhere`

## חוזה · contract
- **input:** `(TradeProduct p)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if i >= 0 && state.products[i] == p → return (void)
- **throws:** _none_
