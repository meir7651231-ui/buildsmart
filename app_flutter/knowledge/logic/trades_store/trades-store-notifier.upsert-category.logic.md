# TradesStoreNotifier.upsertCategory — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `trades_store` · `lib/state/trades_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(TradeCategory c) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kTradesKey↝`
- **writes:** `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — i = state.categories.indexWhere((x) => x.id == c.id)
- **precond** — if i >= 0 && state.categories[i] == c → return (void)
- **init** — next = [...state.categories]
- **branch** — if i >= 0
- **effect** — state = state.copyWith(categories: next)
- **effect** — _persist()

## floor
- `add` · `copyWith` · `indexWhere`

## חוזה · contract
- **input:** `(TradeCategory c)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if i >= 0 && state.categories[i] == c → return (void)
- **throws:** _none_
