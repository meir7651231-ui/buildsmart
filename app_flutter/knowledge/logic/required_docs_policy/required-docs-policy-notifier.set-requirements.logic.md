# RequiredDocsPolicyNotifier.setRequirements — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `required_docs_policy` · `../../../app_flutter/lib/state/required_docs_policy.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String employerId, List<String> names) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kRequiredDocsKey↝` · `mounted↝`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `normalizeDocName` · `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — seen = <String>{}
- **init** — cleaned = <String>[]
- **loop** — for final raw in names
- **compute** — n = raw.trim()
- **branch** — if n.isEmpty → continue
- **branch** — if seen.add(normalizeDocName(n))
- **effect** — _userTouched = true
- **effect** — state = {...state, employerId : cleaned}
- **effect** — _persist()

## floor
- `add` · `trim`

## חוזה · contract
- **input:** `(String employerId, List<String> names)`
- **output:** `void`
- **purity:** side-effecting
- **throws:** _none_
