# requirePermission — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `rbac` · `../../../app_flutter/lib/state/rbac.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(WidgetRef ref, Permission perm, String label) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `auditTrailProvider` · `bsRoleProvider` · `currentUserProvider`
- **writes:** _none_
- **calls:** `permitAction` · `claimOf`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — role = ref.read(bsRoleProvider)
- **compute** — status = ref.read(currentUserProvider)?.status
- **precond** — if permitAction(role: role, perm: perm, status: status) → return true
- **compute** — blockedPending = perm != Permission.viewCatalog && (status == UserStatus.pending || status == UserStatus.s…
- **effect** — ref.read(auditTrailProvider.notifier).log(blockedPending ? 'הרשאה נדחתה (ממתין לאישור)' :…
- **return** — false

## floor
- `log` · `read`

## חוזה · contract
- **input:** `(WidgetRef ref, Permission perm, String label)`
- **output:** `bool`
- **purity:** pure
- **precond:**
  - if permitAction(role: role, perm: perm, status: status) → return true
- **throws:** _none_
