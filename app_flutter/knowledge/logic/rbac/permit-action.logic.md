# permitAction — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `rbac` · `../../../app_flutter/lib/state/rbac.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `({required BsRole role, required Permission perm, required UserStatus? status}) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `roleToPermissions↝`
- **writes:** _none_
- **calls:** `hasPermission`
- **called-by:** `requirePermission`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if perm != Permission.viewCatalog && (status == UserStatus.pending || status == UserStatus.s… → return false
- **return** — hasPermission(role, perm)

## floor
- _none_

## חוזה · contract
- **input:** `({required BsRole role, required Permission perm, required UserStatus? status})`
- **output:** `bool`
- **purity:** pure
- **precond:**
  - if perm != Permission.viewCatalog && (status == UserStatus.pending || status == UserStatus.s… → return false
- **throws:** _none_
