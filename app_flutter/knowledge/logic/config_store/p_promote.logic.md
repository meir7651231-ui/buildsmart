# _promote — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_store` · `lib/state/studio/config_store.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(ConfigLayer pub, ConfigLayer draft) -> ConfigLayer`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_overlayNode`
- **called-by:** `ConfigStore.publish`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — g = {...pub.global}
- **effect** — draft.global.forEach((id, dn) {final merged = _overlayNode(g[id], dn); if (merged.isEmpty…
- **init** — p = {for (final e in pub.persona.entries) e.key : {...e.value}}
- **effect** — draft.persona.forEach((role, m) {final inner = {...(p[role] ?? const {})}; m.forEach((id,…
- **return** — ConfigLayer(global: g, persona: p, structure: {...pub.structure, ...draft.structure})

## floor
- `ConfigLayer` · `forEach` · `remove`

## חוזה · contract
- **input:** `(ConfigLayer pub, ConfigLayer draft)`
- **output:** `ConfigLayer`
- **purity:** pure
- **throws:** _none_
