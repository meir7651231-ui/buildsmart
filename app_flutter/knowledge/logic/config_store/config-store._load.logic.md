# ConfigStore._load — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_store` · `lib/state/studio/config_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_sink` · `_undo` · `mounted`
- **writes:** `state:state`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — loaded = await _sink.load()
- **precond** — if loaded == null || !mounted || _undo.isNotEmpty → return (void)
- **effect** — state = state.published.isEmpty ? loaded : loaded.copyWith(published: state.published)

## floor
- `copyWith` · `load`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if loaded == null || !mounted || _undo.isNotEmpty → return (void)
- **throws:** _none_
