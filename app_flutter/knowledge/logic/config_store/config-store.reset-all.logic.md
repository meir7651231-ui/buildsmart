# ConfigStore.resetAll — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_store` · `lib/state/studio/config_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({String byEmail = '', int nowMs = 0}) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kHistoryCap` · `_sink↝`
- **writes:** `state:_redo` · `state:_undo` · `state:state`
- **calls:** `_nextVersionId` · `_save`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if state.published.isEmpty && state.draft.isEmpty → return false
- **compute** — vid = _nextVersionId(nowMs)
- **compute** — empty = ConfigLayer.empty
- **compute** — version = ConfigVersion(id: vid, snapshot: empty, publishedAtMs: nowMs, note: 'איפוס לברירת-מחדל', …
- **effect** — state = ConfigDoc(published: empty, draft: ConfigLayer.empty, history: [version, ...state…
- **effect** — _undo.clear()
- **effect** — _redo.clear()
- **effect** — _save()
- **return** — true

## floor
- `ConfigDoc` · `ConfigVersion` · `clear` · `take` · `toList`

## חוזה · contract
- **input:** `({String byEmail = '', int nowMs = 0})`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if state.published.isEmpty && state.draft.isEmpty → return false
- **throws:** _none_
