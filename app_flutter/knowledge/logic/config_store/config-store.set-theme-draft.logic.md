# ConfigStore.setThemeDraft — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_store` · `lib/state/studio/config_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(CfgTheme theme) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_sink↝`
- **writes:** `state:_redo` · `state:_undo` · `state:state`
- **calls:** `_save`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — next = state.draft.copyWith(structure: {...state.draft.structure, 'theme' : theme.toJson()})
- **precond** — if next == state.draft → return (void)
- **effect** — _undo.add(state.draft)
- **effect** — _redo.clear()
- **effect** — state = state.copyWith(draft: next)
- **effect** — _save()

## floor
- `add` · `clear` · `copyWith` · `toJson`

## חוזה · contract
- **input:** `(CfgTheme theme)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if next == state.draft → return (void)
- **throws:** _none_
