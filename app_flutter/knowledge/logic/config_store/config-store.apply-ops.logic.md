# ConfigStore.applyOps — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_store` · `lib/state/studio/config_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(List<ConfigOp> ops, {String? persona, Set<String> criticalIds = const {}}) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kCfgMaxTextLen↝` · `_sink↝`
- **writes:** `state:_redo` · `state:_undo` · `state:state`
- **calls:** `cfgOpError` · `_withNode` · `_nodeIn` · `_save`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if ops.isEmpty → return 0
- **compute** — d = state.draft
- **compute** — applied = 0
- **loop** — for final op in ops
- **branch** — if cfgOpError(op, criticalIds: criticalIds) != null → continue
- **compute** — before = d
- **effect** — d = _withNode(d, op.id, op.apply(_nodeIn(d, op.id, persona)), persona)
- **branch** — if d != before
- **precond** — if d == state.draft → return 0
- **effect** — _undo.add(state.draft)
- **effect** — _redo.clear()
- **effect** — state = state.copyWith(draft: d)
- **effect** — _save()
- **return** — applied

## floor
- `add` · `apply` · `clear` · `copyWith`

## חוזה · contract
- **input:** `(List<ConfigOp> ops, {String? persona, Set<String> criticalIds = const {}})`
- **output:** `int`
- **purity:** side-effecting
- **precond:**
  - if ops.isEmpty → return 0
  - if d == state.draft → return 0
- **throws:** _none_
