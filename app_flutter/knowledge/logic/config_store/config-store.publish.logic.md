# ConfigStore.publish — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_store` · `lib/state/studio/config_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({String note = '', String byEmail = '', int nowMs = 0, Set<String> criticalIds = const {}}) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kHistoryCap` · `_sink↝`
- **writes:** `state:_redo` · `state:_undo` · `state:state`
- **calls:** `_sanitizeCritical` · `_nextVersionId` · `_promote` · `_save`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if state.draft.isEmpty → return false
- **compute** — cleanDraft = _sanitizeCritical(state.draft, criticalIds)
- **precond** — if cleanDraft.isEmpty → return false
- **compute** — vid = _nextVersionId(nowMs)
- **compute** — newPublished = _promote(state.published, cleanDraft)
- **compute** — version = ConfigVersion(id: vid, snapshot: newPublished, publishedAtMs: nowMs, note: note, byEmail:…
- **effect** — state = ConfigDoc(published: newPublished, draft: ConfigLayer.empty, history: [version, .…
- **effect** — _undo.clear()
- **effect** — _redo.clear()
- **effect** — _save()
- **return** — true

## floor
- `ConfigDoc` · `ConfigVersion` · `clear` · `take` · `toList`

## חוזה · contract
- **input:** `({String note = '', String byEmail = '', int nowMs = 0, Set<String> criticalIds = const {}})`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if state.draft.isEmpty → return false
  - if cleanDraft.isEmpty → return false
- **throws:** _none_
