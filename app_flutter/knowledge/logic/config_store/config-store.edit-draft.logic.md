# ConfigStore.editDraft — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_store` · `lib/state/studio/config_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id, CfgNode Function(CfgNode) fn, {String? persona}) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_sink↝`
- **writes:** `state:_redo` · `state:_undo` · `state:state`
- **calls:** `_nodeIn` · `_withNode` · `_save`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — next = fn(_nodeIn(state.draft, id, persona))
- **compute** — d = _withNode(state.draft, id, next, persona)
- **precond** — if d == state.draft → return (void)
- **effect** — _undo.add(state.draft)
- **effect** — _redo.clear()
- **effect** — state = state.copyWith(draft: d)
- **effect** — _save()

## floor
- `add` · `clear` · `copyWith` · `fn`

## חוזה · contract
- **input:** `(String id, CfgNode Function(CfgNode) fn, {String? persona})`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if d == state.draft → return (void)
- **throws:** _none_
