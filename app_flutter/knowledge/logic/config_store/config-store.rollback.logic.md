# ConfigStore.rollback — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_store` · `lib/state/studio/config_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int versionId, {String byEmail = '', int nowMs = 0}) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kHistoryCap` · `_sink↝`
- **writes:** `state:_redo` · `state:_undo` · `state:state`
- **calls:** `_nextVersionId` · `_save`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — v = state.history.where((x) => x.id == versionId).firstOrNull
- **precond** — if v == null → return false
- **compute** — vid = _nextVersionId(nowMs)
- **compute** — version = ConfigVersion(id: vid, snapshot: v.snapshot, publishedAtMs: nowMs, note: 'שחזור גרסה ${v.…
- **effect** — state = ConfigDoc(published: v.snapshot, draft: ConfigLayer.empty, history: [version, ...…
- **effect** — _undo.clear()
- **effect** — _redo.clear()
- **effect** — _save()
- **return** — true

## floor
- `ConfigDoc` · `ConfigVersion` · `clear` · `take` · `toList` · `where`

## חוזה · contract
- **input:** `(int versionId, {String byEmail = '', int nowMs = 0})`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if v == null → return false
- **throws:** _none_
