# ConfigStore._sanitizeCritical — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_store` · `lib/state/studio/config_store.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(ConfigLayer draft, Set<String> criticalIds) -> ConfigLayer`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_violates` · `_withNode` · `_stripGuard`
- **called-by:** `ConfigStore.publish`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if criticalIds.isEmpty → return draft
- **compute** — out = draft
- **loop** — for final id in criticalIds
- **branch** — if _violates(out.global[id])
- **loop** — for final persona in out.persona.keys.toList()
- **return** — out

## floor
- `toList`

## חוזה · contract
- **input:** `(ConfigLayer draft, Set<String> criticalIds)`
- **output:** `ConfigLayer`
- **purity:** pure
- **precond:**
  - if criticalIds.isEmpty → return draft
- **throws:** _none_
