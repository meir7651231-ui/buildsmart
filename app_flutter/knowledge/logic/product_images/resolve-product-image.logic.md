# resolveProductImage — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `product_images` · `lib/data/product_images.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String assetPath) -> ImageProvider`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kImageBaseUrl` · `productImageCache`
- **writes:** _none_
- **calls:** `productImageUrl`
- **called-by:** `productImage`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if assetPath.startsWith('http://') || assetPath.startsWith('https://') → return CachedNetworkImageProvider(assetPath, cacheManager: productImageCache)
- **precond** — if kImageBaseUrl.isEmpty → return AssetImage(assetPath)
- **return** — CachedNetworkImageProvider(productImageUrl(assetPath), cacheManager: productImageCache)

## floor
- `AssetImage` · `CachedNetworkImageProvider` · `startsWith`

## חוזה · contract
- **input:** `(String assetPath)`
- **output:** `ImageProvider`
- **purity:** pure
- **precond:**
  - if assetPath.startsWith('http://') || assetPath.startsWith('https://') → return CachedNetworkImageProvider(assetPath, cacheManager: productImageCache)
  - if kImageBaseUrl.isEmpty → return AssetImage(assetPath)
- **throws:** _none_
