# scopeHe — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_intent` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_intent.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String token) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kScopeActionable` · `kScopeAll` · `kScopeEveryPrefix` · `kScopeScreenPrefix` · `kScopeSinglePrefix`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if token == kScopeAll → return 'כל האלמנטים'
- **precond** — if token == kScopeActionable → return 'כל הכפתורים'
- **precond** — if token.startsWith(kScopeEveryPrefix) → return 'כל «${token.substring(kScopeEveryPrefix.length)}»'
- **precond** — if token.startsWith(kScopeScreenPrefix) → return 'מסך «${token.substring(kScopeScreenPrefix.length)}»'
- **precond** — if token.startsWith(kScopeSinglePrefix) → return 'האלמנט «${token.substring(kScopeSinglePrefix.length)}»'
- **return** — '(טווח לא מזוהה)'

## floor
- `startsWith` · `substring`

## חוזה · contract
- **input:** `(String token)`
- **output:** `String`
- **purity:** pure
- **precond:**
  - if token == kScopeAll → return 'כל האלמנטים'
  - if token == kScopeActionable → return 'כל הכפתורים'
  - if token.startsWith(kScopeEveryPrefix) → return 'כל «${token.substring(kScopeEveryPrefix.length)}»'
  - if token.startsWith(kScopeScreenPrefix) → return 'מסך «${token.substring(kScopeScreenPrefix.length)}»'
  - if token.startsWith(kScopeSinglePrefix) → return 'האלמנט «${token.substring(kScopeSinglePrefix.length)}»'
- **throws:** _none_
