# buildScopeEdit — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_intent` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_intent.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String token, Map<String, Object?> editTemplate, RegistryView registry) -> ScopeEditResult`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kCleanFinishReasons↝` · `kStudioMaxBatch↝`
- **writes:** _none_
- **calls:** `dryCountScope` · `parseConfigEdit`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — counted = dryCountScope(token, registry)
- **precond** — if counted.rejected → return ScopeEditResult(token: token, requested: counted.requested, result: const ConfigEditResul…
- **init** — opsJson = <Map<String, Object?>>[for (final id in counted.ids) {...editTemplate, 'id' : id}]
- **return** — ScopeEditResult(token: token, requested: counted.requested, result: parseConfigEdit(jsonE…

## floor
- `ScopeEditResult` · `empty` · `jsonEncode`

## חוזה · contract
- **input:** `(String token, Map<String, Object?> editTemplate, RegistryView registry)`
- **output:** `ScopeEditResult`
- **purity:** pure
- **precond:**
  - if counted.rejected → return ScopeEditResult(token: token, requested: counted.requested, result: const ConfigEditResul…
- **throws:** _none_
