# _resolveStyle — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_intent` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_intent.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(RegistryView reg, String target, CfgStyle? style) -> ({bool ok, CfgStyle? style})`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_resolveToken`
- **called-by:** `_validateOp`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if style == null → return (ok: true, style: null)
- **compute** — color = _resolveToken(reg, target, 'color', style.colorToken)
- **precond** — if !color.ok → return (ok: false, style: null)
- **compute** — bg = _resolveToken(reg, target, 'bg', style.bgToken)
- **precond** — if !bg.ok → return (ok: false, style: null)
- **compute** — weight = _resolveToken(reg, target, 'weight', style.weightToken)
- **precond** — if !weight.ok → return (ok: false, style: null)
- **compute** — size = _resolveToken(reg, target, 'size', style.sizeToken)
- **precond** — if !size.ok → return (ok: false, style: null)
- **return** — (ok: true, style: CfgStyle(colorToken: color.value, bgToken: bg.value, weightToken: weigh…

## floor
- `CfgStyle`

## חוזה · contract
- **input:** `(RegistryView reg, String target, CfgStyle? style)`
- **output:** `({bool ok, CfgStyle? style})`
- **purity:** pure
- **precond:**
  - if style == null → return (ok: true, style: null)
  - if !color.ok → return (ok: false, style: null)
  - if !bg.ok → return (ok: false, style: null)
  - if !weight.ok → return (ok: false, style: null)
  - if !size.ok → return (ok: false, style: null)
- **throws:** _none_
