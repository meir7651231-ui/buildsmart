# dryCountScope — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_intent` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_intent.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String token, RegistryView registry) -> ScopeCount`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kScopeActionable↝` · `kScopeAll↝` · `kScopeEveryPrefix↝` · `kScopeScreenPrefix↝` · `kScopeSinglePrefix↝` · `kStudioMaxBatch`
- **writes:** _none_
- **calls:** `expandScope` · `_batchRejectHe`
- **called-by:** `buildScopeEdit`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — expanded = expandScope(token, registry)
- **precond** — if expanded.length > kStudioMaxBatch → return ScopeCount._(const <String>[], expanded.length, _batchRejectHe(expanded.length))
- **return** — ScopeCount._(expanded, expanded.length, null)

## floor
- `_`

## חוזה · contract
- **input:** `(String token, RegistryView registry)`
- **output:** `ScopeCount`
- **purity:** pure
- **precond:**
  - if expanded.length > kStudioMaxBatch → return ScopeCount._(const <String>[], expanded.length, _batchRejectHe(expanded.length))
- **throws:** _none_
