# _validateOp — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_intent` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_intent.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Object? entry, RegistryView reg) -> ConfigOp?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_axisOf` · `_freeValueOk` · `_resolveStyle` · `_actionIdOf`
- **called-by:** `parseConfigEdit`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if entry is! Map → return null
- **compute** — m = entry.map((k, v) => MapEntry(k.toString(), v))
- **compute** — shape = configOpFromJson(m)
- **precond** — if shape == null → return null
- **compute** — target = matchElementId(reg, shape.id)
- **precond** — if target == null → return null
- **precond** — if matchPropKey(reg, target, _axisOf(shape)) == null → return null
- **branch** — switch shape

## floor
- `CfgAction` · `MapEntry` · `SetAction` · `SetEmoji` · `SetHidden` · `SetOrder` · `SetStyle` · `SetText` · `configOpFromJson` · `map` · `matchActionId` · `matchCatalogActionId` · `matchElementId` · `matchPropKey` · `toString`

## חוזה · contract
- **input:** `(Object? entry, RegistryView reg)`
- **output:** `ConfigOp?`
- **purity:** pure
- **precond:**
  - if entry is! Map → return null
  - if shape == null → return null
  - if target == null → return null
  - if matchPropKey(reg, target, _axisOf(shape)) == null → return null
- **null when:** entry is! Map · shape == null · target == null · matchPropKey(reg, target, _axisOf(shape)) == null · or search exhausted
- **throws:** _none_
