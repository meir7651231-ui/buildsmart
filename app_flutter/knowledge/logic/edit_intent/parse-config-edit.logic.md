# parseConfigEdit — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_intent` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_intent.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String raw, RegistryView registry, {String? finishReason}) -> ConfigEditResult`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kCleanFinishReasons`
- **writes:** _none_
- **calls:** `_firstOpen` · `_lastClose` · `_looksTruncated` · `_rawOps` · `_validateOp`
- **called-by:** `buildScopeEdit`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — text = raw.trim()
- **precond** — if finishReason != null && !_kCleanFinishReasons.contains(finishReason) → return const ConfigEditResult.truncated()
- **compute** — start = _firstOpen(text)
- **precond** — if start < 0 → return const ConfigEditResult.empty()
- **compute** — end = _lastClose(text)
- **precond** — if end <= start → return const ConfigEditResult.truncated()
- **compute** — candidate = text.substring(start, end + 1)
- **precond** — if _looksTruncated(candidate) → return const ConfigEditResult.truncated()

## floor
- `ConfigEditResult` · `add` · `contains` · `empty` · `jsonDecode` · `substring` · `trim` · `truncated`

## חוזה · contract
- **input:** `(String raw, RegistryView registry, {String? finishReason})`
- **output:** `ConfigEditResult`
- **purity:** pure
- **precond:**
  - if finishReason != null && !_kCleanFinishReasons.contains(finishReason) → return const ConfigEditResult.truncated()
  - if start < 0 → return const ConfigEditResult.empty()
  - if end <= start → return const ConfigEditResult.truncated()
  - if _looksTruncated(candidate) → return const ConfigEditResult.truncated()
- **throws:** _none_
