# FirestoreIntelSink.flush — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `intel_sink` · `lib/state/intel/intel_sink.dart`.

## עצם · object
- **kind:** method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_flushInterval↝` · `_writer` · `of`
- **writes:** `field:_flushing` · `field:_timer` · `state:_queue`
- **calls:** `_requeue` · `_ensureTimer`
- **called-by:** `FirestoreIntelSink._ensureTimer` · `FirestoreIntelSink.enqueue` · `FirestoreIntelSink.pause`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if _flushing → return (void)
- **effect** — _timer?.cancel()
- **effect** — _timer = null
- **precond** — if _queue.isEmpty → return (void)
- **effect** — _flushing = true
- **init** — batch = List<IntelEvent>.of(_queue)
- **effect** — _queue.clear()

## floor
- `List` · `cancel` · `clear` · `debugPrint` · `map` · `toList` · `toWire` · `writeBatch`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if _flushing → return (void)
  - if _queue.isEmpty → return (void)
- **throws:** _none_
