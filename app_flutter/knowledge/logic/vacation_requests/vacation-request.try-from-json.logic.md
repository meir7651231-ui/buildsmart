# VacationRequest.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `vacation_requests` · `lib/state/vacation_requests.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> VacationRequest?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVacationPending` · `createdTs` · `decidedTs` · `declared` · `employerId` · `reason` · `role` · `signature` · `status`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — id = raw['id']
- **compute** — username = raw['username']
- **compute** — workerName = raw['workerName']
- **compute** — from = DateTime.tryParse('${raw['from']}')
- **compute** — to = DateTime.tryParse('${raw['to']}')
- **compute** — created = DateTime.tryParse('${raw['createdTs']}')
- **precond** — if id is! String || username is! String || workerName is! String || from == null || to == nu… → return null
- **return** — VacationRequest(id: id, username: username, workerName: workerName, from: from, to: to, r…

## floor
- `VacationRequest` · `tryParse`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `VacationRequest?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if id is! String || username is! String || workerName is! String || from == null || to == nu… → return null
- **null when:** raw is! Map · id is! String || username is! String || workerName is! String || from == null || to == nu… · or search exhausted
- **throws:** _none_
