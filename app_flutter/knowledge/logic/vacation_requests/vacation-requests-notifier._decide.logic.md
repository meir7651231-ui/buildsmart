# VacationRequestsNotifier._decide — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `vacation_requests` · `lib/state/vacation_requests.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id, String status) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVacationPending` · `kVacationRequestsKey↝`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** `VacationRequestsNotifier.approve` · `VacationRequestsNotifier.reject`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !state.any((r) => r.id == id && r.status == kVacationPending) → return false
- **effect** — _userTouched = true
- **effect** — state = [for (final r in state) if (r.id == id && r.status == kVacationPending) r.copyWit…
- **effect** — _persist()
- **return** — true

## floor
- `any` · `copyWith` · `now`

## חוזה · contract
- **input:** `(String id, String status)`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if !state.any((r) => r.id == id && r.status == kVacationPending) → return false
- **throws:** _none_
