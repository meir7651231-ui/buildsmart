# _formatInch — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `catalog_settings` · `lib/state/catalog_settings.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(double inch, CatalogDecimalFormat fmt) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `formatDimValue`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **branch** — if fmt == CatalogDecimalFormat.decimal
- **compute** — sixteenths = (inch * 16).round()
- **compute** — whole = sixteenths ~/ 16
- **compute** — num = sixteenths % 16
- **compute** — den = 16
- **loop** — while num != 0 && num % 2 == 0
- **effect** — num ~/= 2
- **effect** — den ~/= 2
- **precond** — if num == 0 → return '$whole"'
- **precond** — if whole == 0 → return '$num/$den"'
- **return** — '$whole $num/$den"'

## floor
- `RegExp` · `contains` · `replaceAll` · `round` · `toStringAsFixed`

## חוזה · contract
- **input:** `(double inch, CatalogDecimalFormat fmt)`
- **output:** `String`
- **purity:** pure
- **precond:**
  - if num == 0 → return '$whole"'
  - if whole == 0 → return '$num/$den"'
- **throws:** _none_
