# mergeAction — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_merge` · `lib/state/studio/config_merge.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(CfgAction? base, CfgAction? over) -> CfgAction?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_overlay`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if over == null → return base
- **precond** — if base == null → return over
- **return** — CfgAction(kind: over.kind, args: base.args.isEmpty && over.args.isEmpty ? const {} : {...…

## floor
- `CfgAction`

## חוזה · contract
- **input:** `(CfgAction? base, CfgAction? over)`
- **output:** `CfgAction?`
- **purity:** pure
- **precond:**
  - if over == null → return base
  - if base == null → return over
- **null when:** no result
- **throws:** _none_
