# dnNumber — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `dn_scale` · `../../../app_flutter/lib/features/catalog_config/dn_scale.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String label) -> int`
- **constants:** `1 << 30`

## חיבורים · connections
- **reads:** `_kFirstNum`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — m = _kFirstNum.firstMatch(label)
- **precond** — if m == null → return 1 << 30
- **return** — int.tryParse(m.group(1)!.split('.').first) ?? (1 << 30)

## floor
- `firstMatch` · `group` · `split` · `tryParse`

## חוזה · contract
- **input:** `(String label)`
- **output:** `int`
- **purity:** pure
- **precond:**
  - if m == null → return 1 << 30
- **throws:** _none_
