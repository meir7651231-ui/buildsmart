# canonicalDn — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `dn_scale` · `../../../app_flutter/lib/features/catalog_config/dn_scale.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String token) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kDnRungs↝` · `kInchToDn` · `_kFirstNum` · `num`
- **writes:** _none_
- **calls:** `snapOdToDn`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = token.trim()
- **compute** — inch = kInchToDn[t]
- **precond** — if inch != null → return 'DN$inch'
- **compute** — m = _kFirstNum.firstMatch(t)
- **precond** — if m == null → return t
- **compute** — n = num.tryParse(m.group(1)!)
- **precond** — if n == null → return t
- **return** — 'DN${snapOdToDn(n)}'

## floor
- `firstMatch` · `group` · `trim` · `tryParse`

## חוזה · contract
- **input:** `(String token)`
- **output:** `String`
- **purity:** pure
- **precond:**
  - if inch != null → return 'DN$inch'
  - if m == null → return t
  - if n == null → return t
- **throws:** _none_
