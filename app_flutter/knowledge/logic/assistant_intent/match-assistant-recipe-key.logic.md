# matchAssistantRecipeKey — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `assistant_intent` · `/home/user/buildsmart/app_flutter/lib/logic/assistant_intent.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String reply) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kSmartProducts`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `parseAssistantIntent`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — r = reply.trim()
- **precond** — if r.isEmpty → return null
- **loop** — for final p in kSmartProducts
- **branch** — if r == p.key → return p.key
- **compute** — best = 
- **loop** — for final p in kSmartProducts
- **branch** — if r.contains(p.key) && (best == null || p.key.length > best.length)
- **return** — best

## floor
- `contains` · `trim`

## חוזה · contract
- **input:** `(String reply)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if r.isEmpty → return null
- **null when:** r.isEmpty · or search exhausted
- **throws:** _none_
