# parseAssistantIntent — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `assistant_intent` · `/home/user/buildsmart/app_flutter/lib/logic/assistant_intent.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String raw) -> AssistantIntent`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kSmartProducts↝`
- **writes:** _none_
- **calls:** `_actionFromString` · `matchAssistantCategory` · `matchAssistantRecipeKey`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — text = raw.trim()
- **compute** — start = text.indexOf('{')
- **compute** — end = text.lastIndexOf('}')
- **precond** — if start < 0 || end <= start → return AssistantIntent.answer(text)

## floor
- `AssistantIntent` · `answer` · `indexOf` · `jsonDecode` · `lastIndexOf` · `substring` · `trim`

## חוזה · contract
- **input:** `(String raw)`
- **output:** `AssistantIntent`
- **purity:** pure
- **precond:**
  - if start < 0 || end <= start → return AssistantIntent.answer(text)
- **throws:** _none_
