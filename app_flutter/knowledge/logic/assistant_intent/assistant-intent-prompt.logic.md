# assistantIntentPrompt — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `assistant_intent` · `/home/user/buildsmart/app_flutter/lib/logic/assistant_intent.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<IntentTurn> history, String userText) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kIntentHistoryWindow` · `kSmartProducts` · `resolvedCatalogProducts↝`
- **writes:** _none_
- **calls:** `assistantCategories`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — recent = history.length > kIntentHistoryWindow ? history.sublist(history.length - kIntentHistoryWi…
- **compute** — cats = assistantCategories().join('\n')
- **init** — recipes = [for (final r in kSmartProducts) '${r.key}=${r.name}'].join('\n')
- **compute** — b = StringBuffer()
- **branch** — if recent.isNotEmpty
- **effect** — b.writeln('המשתמש כתב: "${promptSafeText(userText, maxLen: 600)}".')
- **effect** — b.writeln('בחר פעולה אחת מהרשימה הסגורה והחזר שורת-JSON אחת בלבד:')
- **effect** — b.writeln('- "answer": ענה ישירות. שים את התשובה ב-say, key="".')
- **effect** — b.writeln('- "findProduct": המשתמש מחפש מוצר. key = קטגוריה אחת מרשימת ' 'הקטגוריות למטה …
- **effect** — b.writeln('- "summarizeOrders": המשתמש שואל על ההזמנות/הרכש שלו.')
- **effect** — b.writeln('- "checkBudget": המשתמש שואל על התקציב / כמה נשאר.')
- **effect** — b.writeln('- "addToCart": המשתמש מבקש להוסיף ערכה לסל. key = מפתח-ערכה אחד ' 'מרשימת הערכ…
- **effect** — b.writeln()
- **effect** — b.writeln('קטגוריות זמינות ל-findProduct:')
- **effect** — b.writeln(cats)
- **effect** — b.writeln()
- **effect** — b.writeln('ערכות זמינות ל-addToCart (מפתח=שם):')
- **effect** — b.writeln(recipes)
- **effect** — b.writeln()
- **effect** — b.writeln('החזר אך ורק שורת-JSON אחת בפורמט: ' '{"action":"...","key":"...","say":"..."}')
- **effect** — b.writeln('אם אף קטגוריה/ערכה לא מתאימה — השתמש ב-answer.')
- **return** — b.toString()

## floor
- `StringBuffer` · `join` · `promptSafeText` · `sublist` · `toString` · `writeln`

## חוזה · contract
- **input:** `(List<IntentTurn> history, String userText)`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
