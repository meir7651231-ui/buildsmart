# StoreProductsNotifier.add — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `store_stock` · `lib/state/store_stock.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({required String name, required String sku, required String category, String categoryEmoji = '🆕'}) -> StoreProduct?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kStoreProductsKey↝` · `_seq`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `nameTaken` · `skuTaken` · `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — n = name.trim()
- **compute** — s = sku.trim()
- **compute** — c = category.trim()
- **precond** — if n.isEmpty || s.isEmpty || c.isEmpty → return null
- **precond** — if nameTaken(n) || skuTaken(s) → return null
- **effect** — _userTouched = true
- **effect** — _seq++
- **compute** — p = StoreProduct(id: 'sp-${DateTime.now().microsecondsSinceEpoch}-$_seq', name: n, sku: s, ca…
- **effect** — state = [...state, p]
- **effect** — unawaited(_persist())
- **return** — p

## floor
- `StoreProduct` · `now` · `trim` · `unawaited`

## חוזה · contract
- **input:** `({required String name, required String sku, required String category, String categoryEmoji = '🆕'})`
- **output:** `StoreProduct?`
- **purity:** side-effecting
- **precond:**
  - if n.isEmpty || s.isEmpty || c.isEmpty → return null
  - if nameTaken(n) || skuTaken(s) → return null
- **null when:** n.isEmpty || s.isEmpty || c.isEmpty · nameTaken(n) || skuTaken(s) · or search exhausted
- **throws:** _none_
