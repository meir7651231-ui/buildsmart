# StoreProduct.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `store_stock` · `lib/state/store_stock.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> StoreProduct?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `categoryEmoji` · `createdTs`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — id = raw['id']
- **compute** — name = raw['name']
- **compute** — sku = raw['sku']
- **compute** — category = raw['category']
- **compute** — created = DateTime.tryParse('${raw['createdTs']}')
- **precond** — if id is! String || name is! String || sku is! String || category is! String || created == n… → return null
- **return** — StoreProduct(id: id, name: name, sku: sku, category: category, categoryEmoji: raw['catego…

## floor
- `StoreProduct` · `tryParse`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `StoreProduct?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if id is! String || name is! String || sku is! String || category is! String || created == n… → return null
- **null when:** raw is! Map · id is! String || name is! String || sku is! String || category is! String || created == n… · or search exhausted
- **throws:** _none_
