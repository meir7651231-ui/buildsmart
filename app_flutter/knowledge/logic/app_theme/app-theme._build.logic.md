# AppTheme._build — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `app_theme` · `lib/theme/app_theme.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Brightness b, {bool highContrast = false, CfgTheme cfg = CfgTheme.fallback}) -> ThemeData`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `AppTheme.dark` · `AppTheme.light`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — isDark = b == Brightness.dark
- **compute** — scheme = ColorScheme.fromSeed(seedColor: cfg.brand, brightness: b, primary: cfg.brand, surface: is…
- **compute** — ink = highContrast ? (isDark ? Colors.white : Colors.black) : (isDark ? BsTokens.inkDark : Colo…
- **return** — ThemeData(useMaterial3: true, brightness: b, colorScheme: scheme, scaffoldBackgroundColor…

## floor
- `BsSemanticColors` · `FloatingActionButtonThemeData` · `TextStyle` · `TextTheme` · `ThemeData` · `fromSeed`

## חוזה · contract
- **input:** `(Brightness b, {bool highContrast = false, CfgTheme cfg = CfgTheme.fallback})`
- **output:** `ThemeData`
- **purity:** pure
- **throws:** _none_
