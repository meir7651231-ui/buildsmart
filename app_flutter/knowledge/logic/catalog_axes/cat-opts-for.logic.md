# catOptsFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `catalog_axes` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/catalog_axes.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String axis, Map<String, String> cons, [List<CatProduct>? pool, List<CatProduct>? matched]) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_sizeAxes↝` · `kRdOrder`
- **writes:** _none_
- **calls:** `catMatching` · `_sizeRank`
- **called-by:** `catFindAxes`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — base = matched ?? catMatching(cons, pool)
- **init** — counts = <String, int>{}
- **loop** — for final rp in base
- **compute** — v = rp.axes[axis]
- **branch** — if v != null
- **compute** — ord = kRdOrder[axis] ?? const <String>[]
- **return** — counts.keys.toList()..sort((a, b) {final sa = _sizeRank(axis, a); final sb = _sizeRank(ax…

## floor
- `compareTo` · `indexOf` · `sort` · `toList`

## חוזה · contract
- **input:** `(String axis, Map<String, String> cons, [List<CatProduct>? pool, List<CatProduct>? matched])`
- **output:** `List<String>`
- **purity:** pure
- **throws:** _none_
