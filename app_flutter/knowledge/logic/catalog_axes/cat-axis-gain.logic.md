# catAxisGain — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `catalog_axes` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/catalog_axes.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String axis, List<CatProduct> base) -> double`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `catFindAxes`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if base.isEmpty → return 0
- **init** — counts = <String, int>{}
- **compute** — total = 0
- **compute** — present = 0
- **loop** — for final p in base
- **compute** — vals = p.axes[axis]
- **branch** — if vals == null → continue
- **effect** — present++
- **loop** — for final v in vals
- **precond** — if total == 0 || counts.length < 2 → return 0
- **compute** — h = 0.0
- **loop** — for final c in counts.values
- **compute** — pr = c / total
- **effect** — h -= pr * (log(pr) / log(2))
- **return** — h * (present / base.length)

## floor
- `log`

## חוזה · contract
- **input:** `(String axis, List<CatProduct> base)`
- **output:** `double`
- **purity:** pure
- **precond:**
  - if base.isEmpty → return 0
  - if total == 0 || counts.length < 2 → return 0
- **throws:** _none_
