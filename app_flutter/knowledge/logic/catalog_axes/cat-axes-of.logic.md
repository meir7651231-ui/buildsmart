# catAxesOf — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `catalog_axes` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/catalog_axes.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> Map<String, Set<String>>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_methodHe`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — base = rdAxesOf(p)
- **init** — out = <String, Set<String>>{}
- **branch** — if base['dept'] != null
- **branch** — if base['cat'] != null
- **branch** — if base['type'] != null
- **branch** — if base['angle'] != null
- **branch** — if base['color'] != null
- **branch** — if base['brand'] != null
- **compute** — mat = materialOfEnriched(p)
- **branch** — if mat != null
- **effect** — put('group', <String>[groupOf(p)])
- **init** — diamInch = <String>{}
- **init** — diamDn = <String>{}
- **init** — diamMm = <String>{}
- **init** — length = <String>{}
- **init** — transition = <String>{}
- **loop** — for final t in productSizeTokens(p)
- **branch** — if t.label.contains('×')
- **branch** — if diamInch.isNotEmpty
- **branch** — if diamDn.isNotEmpty
- **branch** — if diamMm.isNotEmpty
- **branch** — if length.isNotEmpty
- **branch** — if transition.isNotEmpty
- **compute** — dims = p.dims
- **branch** — if dims != null
- **compute** — g = p.connectionGender
- **branch** — if g != null
- **compute** — m = p.connectionMethod
- **branch** — if m != null
- **return** — out

## floor
- `add` · `contains` · `groupOf` · `materialOfEnriched` · `productSizeTokens` · `put` · `rdAxesOf` · `toString` · `trim`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `Map<String, Set<String>>`
- **purity:** pure
- **throws:** _none_
