# catFindAxes — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `catalog_axes` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/catalog_axes.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Map<String, String> cons, [List<CatProduct>? pool]) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kCatAxes` · `kRdOrder↝`
- **writes:** _none_
- **calls:** `catMatching` · `catOptsFor` · `catAxisGain`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — base = catMatching(cons, pool)
- **precond** — if base.length <= 1 → return const <String>[]
- **init** — out = <String>[]
- **loop** — for final ax in kCatAxes
- **branch** — if cons.containsKey(ax.id) → continue
- **branch** — if catOptsFor(ax.id, cons, base, base).length >= 2
- **init** — gain = <String, double>{for (final ax in out) ax : catAxisGain(ax, base)}
- **effect** — out.sort((a, b) => gain[b]!.compareTo(gain[a]!))
- **return** — out

## floor
- `add` · `compareTo` · `containsKey` · `sort`

## חוזה · contract
- **input:** `(Map<String, String> cons, [List<CatProduct>? pool])`
- **output:** `List<String>`
- **purity:** pure
- **precond:**
  - if base.length <= 1 → return const <String>[]
- **throws:** _none_
