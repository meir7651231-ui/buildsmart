# SavedProjectsNotifier.rename — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `saved_projects` · `lib/state/saved_projects.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id, String newName) -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kStorageKey↝`
- **writes:** `field:_loaded↝` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **effect** — state = [for (final p in state) if (p.id == id) SavedProject(id: p.id, name: newName, anc…
- **effect** — await _persist()

## floor
- `SavedProject`

## חוזה · contract
- **input:** `(String id, String newName)`
- **output:** `Future<void>`
- **purity:** side-effecting
- **throws:** _none_
