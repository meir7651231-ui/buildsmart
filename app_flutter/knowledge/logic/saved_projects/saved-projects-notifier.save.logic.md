# SavedProjectsNotifier.save — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `saved_projects` · `lib/state/saved_projects.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({required String name, required List<String> anchorSkus, required int tempC, required Set<String> accessories, List<String> branchSkus = const []}) -> Future<SavedProject>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kStorageKey↝` · `_seq`
- **writes:** `field:_loaded↝` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — p = SavedProject(id: '${DateTime.now().microsecondsSinceEpoch}-${_seq++}', name: name, anchor…
- **effect** — state = [p, ...state]
- **effect** — await _persist()
- **return** — p

## floor
- `SavedProject` · `now` · `of`

## חוזה · contract
- **input:** `({required String name, required List<String> anchorSkus, required int tempC, required Set<String> accessories, List<String> branchSkus = const []})`
- **output:** `Future<SavedProject>`
- **purity:** side-effecting
- **throws:** _none_
