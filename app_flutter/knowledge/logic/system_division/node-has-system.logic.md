# nodeHasSystem — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `system_division` · `/home/user/buildsmart/app_flutter/lib/logic/system_division.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(CatalogNode node, WaterSystem system) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_fixtureTitles`
- **writes:** `cache:_nodeHasSystemCache`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if _fixtureTitles.contains(node.title) → return true
- **compute** — cacheKey = '${node.id}|${system.name}'
- **compute** — cached = _nodeHasSystemCache[cacheKey]
- **precond** — if cached != null → return cached
- **compute** — sup = 0
- **effect** — walk(node)
- **compute** — result = (sup != 0 || dr != 0) && (sup >= dr ? WaterSystem.supply : WaterSystem.drainage) == system
- **return** — _nodeHasSystemCache[cacheKey] = result

## floor
- `contains` · `walk`

## חוזה · contract
- **input:** `(CatalogNode node, WaterSystem system)`
- **output:** `bool`
- **purity:** deterministic-side-effecting
- **precond:**
  - if _fixtureTitles.contains(node.title) → return true
  - if cached != null → return cached
- **throws:** _none_
