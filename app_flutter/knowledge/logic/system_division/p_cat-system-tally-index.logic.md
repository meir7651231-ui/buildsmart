# _catSystemTallyIndex — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `system_division` · `/home/user/buildsmart/app_flutter/lib/logic/system_division.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> Map<String, ({int sup, int dr})>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVerifiedSpecs↝`
- **writes:** `cache:_catSystemTally`
- **calls:** `productDivisionSystems`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if _catSystemTally != null → return _catSystemTally!
- **init** — m = <String, ({int sup, int dr})>{}
- **loop** — for final p in catalogRepo().allProducts()
- **compute** — s = productDivisionSystems(p)
- **compute** — prev = m[p.categoryHe] ?? (sup: 0, dr: 0)
- **effect** — m[p.categoryHe] = (sup: prev.sup + (s.contains(WaterSystem.supply) ? 1 : 0), dr: prev.dr …
- **return** — _catSystemTally = m

## floor
- `allProducts` · `catalogRepo` · `contains`

## חוזה · contract
- **input:** `()`
- **output:** `Map<String, ({int sup, int dr})>`
- **purity:** deterministic-side-effecting
- **precond:**
  - if _catSystemTally != null → return _catSystemTally!
- **throws:** _none_
