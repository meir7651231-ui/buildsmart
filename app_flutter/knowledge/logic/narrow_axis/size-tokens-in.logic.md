# sizeTokensIn — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `narrow_axis` · `lib/features/word_finder/narrow_axis.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> ps) -> List<SizeToken>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `productSizeTokens`
- **called-by:** `narrowAxis`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — all = <SizeToken>{}
- **loop** — for final p in ps
- **effect** — all.addAll(productSizeTokens(p))
- **compute** — out = dedupLengthByMm(all.toList())
- **effect** — sortSizeTokens(out)
- **return** — out

## floor
- `addAll` · `dedupLengthByMm` · `sortSizeTokens` · `toList`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> ps)`
- **output:** `List<SizeToken>`
- **purity:** pure
- **throws:** _none_
