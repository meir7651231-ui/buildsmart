# wordOptions — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `narrow_axis` · `lib/features/word_finder/narrow_axis.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> pool) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `narrowAxis`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if pool.length <= 1 → return const []
- **init** — lists = [for (final p in pool) toks(p.nameHe)]
- **compute** — shared = lists.first.toSet()
- **loop** — for final t in lists.skip(1)
- **effect** — shared.retainAll(t.toSet())
- **init** — counts = <String, int>{}
- **loop** — for final t in lists
- **loop** — for final w in t
- **compute** — entries = counts.entries.toList()..sort((a, b) => b.value.compareTo(a.value))
- **return** — [for (final e in entries.take(12)) e.key]

## floor
- `RegExp` · `compareTo` · `contains` · `hasMatch` · `retainAll` · `skip` · `sort` · `split` · `take` · `toList` · `toSet` · `toks` · `where`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> pool)`
- **output:** `List<String>`
- **purity:** pure
- **precond:**
  - if pool.length <= 1 → return const []
- **throws:** _none_
