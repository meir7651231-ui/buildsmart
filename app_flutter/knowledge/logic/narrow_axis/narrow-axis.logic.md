# narrowAxis — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `narrow_axis` · `lib/features/word_finder/narrow_axis.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> pool, String? subtype) -> ({String label, List<String> chips})`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kFinderFacets`
- **writes:** _none_
- **calls:** `sizeTokensIn` · `angleTokensIn` · `colorOptions` · `wordOptions`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — curated = subtype == null ? null : kFinderFacets[subtype]
- **branch** — if curated != null
- **compute** — sizes = sizeTokensIn(pool)
- **precond** — if sizes.length > 1 → return (label: 'גודל', chips: sizes.map((t) => t.label).toList())
- **compute** — angles = angleTokensIn(pool)
- **precond** — if angles.length > 1 → return (label: 'זווית', chips: angles.map((t) => t.label).toList())
- **compute** — colors = colorOptions(pool)
- **precond** — if colors.length > 1 → return (label: 'צבע', chips: colors)
- **compute** — words = wordOptions(pool)
- **return** — words.length > 1 ? (label: 'דגם', chips: words) : (label: '', chips: const <String>[])

## floor
- `any` · `contains` · `map` · `toList` · `where`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> pool, String? subtype)`
- **output:** `({String label, List<String> chips})`
- **purity:** pure
- **precond:**
  - if sizes.length > 1 → return (label: 'גודל', chips: sizes.map((t) => t.label).toList())
  - if angles.length > 1 → return (label: 'זווית', chips: angles.map((t) => t.label).toList())
  - if colors.length > 1 → return (label: 'צבע', chips: colors)
- **throws:** _none_
