# colorOptions — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `narrow_axis` · `lib/features/word_finder/narrow_axis.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> pool) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `narrowAxis`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — cols = <String>{}
- **loop** — for final p in pool
- **compute** — c = p.color
- **branch** — if c != null && c.trim().isNotEmpty
- **return** — cols.length > 1 ? (cols.toList()..sort()) : const []

## floor
- `add` · `sort` · `toList` · `trim`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> pool)`
- **output:** `List<String>`
- **purity:** pure
- **throws:** _none_
