# productHasChip — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `narrow_axis` · `lib/features/word_finder/narrow_axis.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p, String chipLabel) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `productSizeTokens`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **loop** — for final t in productSizeTokens(p)
- **branch** — if t.label == chipLabel → return true
- **loop** — for final t in parseAngleTokens(p.nameHe)
- **branch** — if t.label == chipLabel → return true
- **precond** — if !RegExp(r'\d').hasMatch(chipLabel) && p.nameHe.contains(chipLabel) → return true
- **return** — p.color == chipLabel

## floor
- `RegExp` · `contains` · `hasMatch` · `parseAngleTokens`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p, String chipLabel)`
- **output:** `bool`
- **purity:** pure
- **precond:**
  - if !RegExp(r'\d').hasMatch(chipLabel) && p.nameHe.contains(chipLabel) → return true
- **throws:** _none_
