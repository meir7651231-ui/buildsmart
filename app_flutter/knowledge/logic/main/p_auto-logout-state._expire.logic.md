# _AutoLogoutState._expire — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `main` · `lib/main.dart`.

## עצם · object
- **kind:** method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `authStateProvider` · `context` · `guestBrowsingProvider` · `mounted` · `ref` · `welcomeSeenProvider`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !mounted → return (void)
- **compute** — user = ref.read(authStateProvider).user
- **precond** — if !(user?.isRealUser ?? false) → return (void)
- **effect** — await ref.read(authStateProvider.notifier).signOut()
- **precond** — if !mounted → return (void)
- **effect** — ref.read(welcomeSeenProvider.notifier).state = false
- **effect** — unawaited(clearWelcomeSeen())
- **effect** — ref.read(guestBrowsingProvider.notifier).state = false
- **effect** — unawaited(clearGuestBrowsing())
- **precond** — if !mounted → return (void)
- **effect** — showToast(context, 'נותקת עקב חוסר פעילות — יש להתחבר מחדש')

## floor
- `clearGuestBrowsing` · `clearWelcomeSeen` · `read` · `showToast` · `signOut` · `unawaited`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** pure
- **precond:**
  - if !mounted → return (void)
  - if !(user?.isRealUser ?? false) → return (void)
  - if !mounted → return (void)
  - if !mounted → return (void)
- **throws:** _none_
