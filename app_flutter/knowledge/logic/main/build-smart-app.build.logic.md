# BuildSmartApp.build — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `main` · `lib/main.dart`.

## עצם · object
- **kind:** method
- **signature:** `(BuildContext context, WidgetRef ref) -> Widget`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kAppKbOnly` · `kDebugMode` · `kKbGlobal` · `kStudioFlag` · `kUserSystem` · `appSettingsProvider` · `bsMessengerKey` · `bsNavigatorKey` · `catalogSettingsProvider` · `configThemeProvider` · `intelBusProvider` · `intelRouteObserver` · `pendingPushThreadProvider` · `pushControllerProvider` · `sessionTrackerProvider`
- **writes:** _none_
- **calls:** `debugOverlayChildren`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **effect** — ref.watch(pushControllerProvider)
- **compute** — launchThread = threadIdFromLaunchUrl(Uri.base.toString())
- **branch** — if launchThread != null && ref.read(pendingPushThreadProvider) != launchThread
- **effect** — intelRouteObserver.bind(ref.read(intelBusProvider))
- **effect** — ref.read(sessionTrackerProvider)
- **compute** — settings = ref.watch(appSettingsProvider)
- **compute** — catalogSettings = ref.watch(catalogSettingsProvider)
- **compute** — textScale = switch (catalogSettings.textSize) {CatalogTextSize.small => 0.9, CatalogTextSize.medium =…
- **compute** — highContrast = catalogSettings.highContrast
- **compute** — cfgTheme = ref.watch(configThemeProvider)
- **compute** — locale = switch (settings.lang) {BsLang.he => const Locale('he', 'IL'), BsLang.ar => const Locale(…
- **return** — MaterialApp(title: orgTerm(ref, 'brand.name', AppBrand.name), scaffoldMessengerKey: bsMes…

## floor
- `AppKeyboardInsets` · `AppKeyboardOnlyLayer` · `ConnectionIndicator` · `Directionality` · `FocusTraversalGroup` · `Locale` · `MaterialApp` · `MediaQuery` · `OnboardingGate` · `Overlay` · `OverlayEntry` · `SizedBox` · `Stack` · `StudioOverlay` · `WidgetOrderTraversalPolicy` · `_AutoLogout` · `_GlobalKeyboardOverlay` · `_UserDocSync` · `afterThisFrame` · `bind` · `combinedTextScale` · `copyWith` · `dark` · `fill` · `light` · `linear` · `of` · `orgTerm` · `read` · `scale` · `threadIdFromLaunchUrl` · `toString` · `watch`

## חוזה · contract
- **input:** `(BuildContext context, WidgetRef ref)`
- **output:** `Widget`
- **purity:** pure
- **throws:** _none_
