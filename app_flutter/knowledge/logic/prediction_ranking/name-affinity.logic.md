# nameAffinity — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `prediction_ranking` · `lib/features/global_search/prediction_ranking.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p, String query) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — name = p.nameHe.trim().toLowerCase()
- **compute** — q = query.trim().toLowerCase()
- **precond** — if q.isEmpty || name.isEmpty || !name.contains(q) → return 0
- **compute** — s = 0
- **branch** — if name == q
- **effect** — s += (q.length * 60 / name.length).round().clamp(0, 60)
- **return** — s

## floor
- `clamp` · `contains` · `round` · `startsWith` · `toLowerCase` · `trim`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p, String query)`
- **output:** `int`
- **purity:** pure
- **precond:**
  - if q.isEmpty || name.isEmpty || !name.contains(q) → return 0
- **throws:** _none_
