# productProminence — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `prediction_ranking` · `lib/features/global_search/prediction_ranking.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVerifiedSpecs` · `_diveSkus` · `_wsSplit`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — s = 0
- **branch** — if p.imageFile != null
- **branch** — if kVerifiedSpecs.containsKey(p.sku)
- **branch** — if _diveSkus.contains(p.sku)
- **compute** — toks = p.nameHe.trim().split(_wsSplit).where((t) => t.isNotEmpty).length
- **effect** — s += (12 - toks).clamp(0, 12)
- **effect** — s += ((300 - p.page) ~/ 30).clamp(0, 10)
- **return** — s

## floor
- `clamp` · `contains` · `containsKey` · `split` · `trim` · `where`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `int`
- **purity:** pure
- **throws:** _none_
