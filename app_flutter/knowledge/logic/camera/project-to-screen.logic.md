# projectToScreen — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `camera` · `lib/features/fittings/render/camera.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Mat4 proj, Mat4 view, Vec3 world, double width, double height) -> ScreenPoint`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — clip = proj.mul(view).apply(world)
- **precond** — if clip.w == 0 → return const ScreenPoint(0, 0, 0, 0)
- **compute** — ndcX = clip.x / clip.w
- **compute** — ndcY = clip.y / clip.w
- **compute** — ndcZ = clip.z / clip.w
- **compute** — sx = (ndcX * 0.5 + 0.5) * width
- **compute** — sy = (1 - (ndcY * 0.5 + 0.5)) * height
- **return** — ScreenPoint(sx, sy, ndcZ, clip.w)

## floor
- `ScreenPoint` · `apply` · `mul`

## חוזה · contract
- **input:** `(Mat4 proj, Mat4 view, Vec3 world, double width, double height)`
- **output:** `ScreenPoint`
- **purity:** pure
- **precond:**
  - if clip.w == 0 → return const ScreenPoint(0, 0, 0, 0)
- **throws:** _none_
