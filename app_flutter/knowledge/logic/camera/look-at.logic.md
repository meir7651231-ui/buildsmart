# lookAt — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `camera` · `lib/features/fittings/render/camera.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Vec3 eye, Vec3 center, Vec3 up) -> Mat4`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — z = eye - center
- **effect** — z = z.norm()
- **compute** — x = up.cross(z)
- **compute** — xl = x.length
- **effect** — x = xl == 0 ? x : x.scale(1 / xl)
- **compute** — y = z.cross(x)
- **return** — Mat4([x.x, y.x, z.x, 0, x.y, y.y, z.y, 0, x.z, y.z, z.z, 0, -x.dot(eye), -y.dot(eye), -z.…

## floor
- `Mat4` · `cross` · `dot` · `norm` · `scale`

## חוזה · contract
- **input:** `(Vec3 eye, Vec3 center, Vec3 up)`
- **output:** `Mat4`
- **purity:** pure
- **throws:** _none_
