# Mat4.apply — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `camera` · `lib/features/fittings/render/camera.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Vec3 p, {double w = 1}) -> ({double x, double y, double z, double w})`
- **constants:** _none_

## חיבורים · connections
- **reads:** `m`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — x = m[0] * p.x + m[4] * p.y + m[8] * p.z + m[12] * w
- **compute** — y = m[1] * p.x + m[5] * p.y + m[9] * p.z + m[13] * w
- **compute** — z = m[2] * p.x + m[6] * p.y + m[10] * p.z + m[14] * w
- **compute** — ww = m[3] * p.x + m[7] * p.y + m[11] * p.z + m[15] * w
- **return** — (x: x, y: y, z: z, w: ww)

## floor
- _none_

## חוזה · contract
- **input:** `(Vec3 p, {double w = 1})`
- **output:** `({double x, double y, double z, double w})`
- **purity:** pure
- **throws:** _none_
