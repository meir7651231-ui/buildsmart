# _NavSheet._launch — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `nav_launch` · `lib/services/nav_launch.dart`.

## עצם · object
- **kind:** method
- **signature:** `(BuildContext context, Uri uri) -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `urlLauncherProvider`
- **writes:** `nav:Navigator.of` · `nav:Navigator.pop`
- **calls:** _none_
- **called-by:** `_NavSheet.build`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — launcher = ProviderScope.containerOf(context, listen: false).read(urlLauncherProvider)
- **compute** — ok = await launcher(uri)
- **precond** — if !context.mounted → return (void)
- **effect** — Navigator.of(context).pop()
- **branch** — if !ok

## floor
- `containerOf` · `launcher` · `of` · `pop` · `read` · `showToast`

## חוזה · contract
- **input:** `(BuildContext context, Uri uri)`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if !context.mounted → return (void)
- **throws:** _none_
