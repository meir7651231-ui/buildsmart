# projectParts — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `mesh_projector` · `lib/features/fittings/render/mesh_projector.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<WorldPart> parts, Mat4 proj, Mat4 view, Vec3 eye, Size size) -> List<RenderTriangle>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kAmbient↝` · `kLightDir` · `math↝`
- **writes:** `mutation:out.add↝`
- **calls:** `_emit`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — light = kLightDir.norm()
- **init** — out = <RenderTriangle>[]
- **loop** — for final part in parts
- **effect** — _emit(part.mesh, part.mat, proj, view, eye, size, light, out)
- **effect** — out.sort((x, y) => y.depth.compareTo(x.depth))
- **return** — out

## floor
- `compareTo` · `norm` · `sort`

## חוזה · contract
- **input:** `(List<WorldPart> parts, Mat4 proj, Mat4 view, Vec3 eye, Size size)`
- **output:** `List<RenderTriangle>`
- **purity:** side-effecting
- **throws:** _none_
