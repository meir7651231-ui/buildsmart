# meshBounds — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `mesh_projector` · `lib/features/fittings/render/mesh_projector.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<Mesh> meshes) -> ({Vec3 center, double radius})`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — minX = double.infinity
- **compute** — minY = double.infinity
- **compute** — minZ = double.infinity
- **compute** — maxX = double.negativeInfinity
- **compute** — maxY = double.negativeInfinity
- **compute** — maxZ = double.negativeInfinity
- **compute** — any = false
- **loop** — for final m in meshes
- **loop** — for var v = 0; v < m.vertexCount; v++
- **precond** — if !any → return (center: const Vec3(0, 0, 0), radius: 1)
- **compute** — center = Vec3((minX + maxX) / 2, (minY + maxY) / 2, (minZ + maxZ) / 2)
- **compute** — dx = maxX - minX
- **compute** — dy = maxY - minY
- **compute** — dz = maxZ - minZ
- **compute** — radius = 0.5 * (dx > dy ? (dx > dz ? dx : dz) : (dy > dz ? dy : dz))
- **return** — (center: center, radius: radius <= 0 ? 1 : radius)

## floor
- `Vec3`

## חוזה · contract
- **input:** `(List<Mesh> meshes)`
- **output:** `({Vec3 center, double radius})`
- **purity:** pure
- **precond:**
  - if !any → return (center: const Vec3(0, 0, 0), radius: 1)
- **throws:** _none_
