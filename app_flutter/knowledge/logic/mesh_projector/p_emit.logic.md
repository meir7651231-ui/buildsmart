# _emit — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `mesh_projector` · `lib/features/fittings/render/mesh_projector.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Mesh m, PartMaterial mat, Mat4 proj, Mat4 view, Vec3 eye, Size size, Vec3 light, List<RenderTriangle> out) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kAmbient` · `math`
- **writes:** `mutation:out.add`
- **calls:** `_vertex` · `_normal` · `_shadeParams`
- **called-by:** `projectMeshes` · `projectParts`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **loop** — for var t = 0; t < m.indices.length; t += 3
- **compute** — i0 = m.indices[t]
- **compute** — i1 = m.indices[t + 1]
- **compute** — i2 = m.indices[t + 2]
- **compute** — w0 = _vertex(m, i0)
- **compute** — w1 = _vertex(m, i1)
- **compute** — w2 = _vertex(m, i2)
- **compute** — s0 = projectToScreen(proj, view, w0, size.width, size.height)
- **compute** — s1 = projectToScreen(proj, view, w1, size.width, size.height)
- **compute** — s2 = projectToScreen(proj, view, w2, size.width, size.height)
- **branch** — if !s0.visible || !s1.visible || !s2.visible → continue
- **compute** — n0 = _normal(m, i0)
- **compute** — n1 = _normal(m, i1)
- **compute** — n2 = _normal(m, i2)
- **compute** — normal = (n0 + n1 + n2).norm()
- **compute** — centroid = (w0 + w1 + w2).scale(1 / 3)
- **compute** — viewDir = (eye - centroid).norm()
- **branch** — if normal.dot(viewDir) < 0
- **compute** — ndotl = normal.dot(light)
- **compute** — lit = ndotl < 0 ? 0.0 : ndotl
- **compute** — shade = kAmbient + (1 - kAmbient) * lit
- **compute** — p = _shadeParams(mat)
- **compute** — h = (light + viewDir).norm()
- **compute** — ndoth = normal.dot(h)
- **compute** — ndotv = normal.dot(viewDir)
- **compute** — sh = 14 + (120 - 14) * (1 - p.rough)
- **compute** — specTerm = (ndoth <= 0 ? 0.0 : math.pow(ndoth, sh).toDouble()) * (sh + 8) / 25
- **compute** — f0 = 0.04 + (1 - 0.04) * p.metal
- **compute** — nv = ndotv < 1e-3 ? 1e-3 : ndotv
- **compute** — fresnel = f0 + (1 - f0) * math.pow(1 - nv, 5).toDouble()
- **compute** — specular = (fresnel * specTerm * 1.1).clamp(0.0, 1.0)
- **compute** — depth = (s0.depth + s1.depth + s2.depth) / 3
- **effect** — out.add(RenderTriangle(Offset(s0.x, s0.y), Offset(s1.x, s1.y), Offset(s2.x, s2.y), depth,…

## floor
- `Offset` · `RenderTriangle` · `add` · `clamp` · `dot` · `norm` · `pow` · `projectToScreen` · `scale` · `toDouble`

## חוזה · contract
- **input:** `(Mesh m, PartMaterial mat, Mat4 proj, Mat4 view, Vec3 eye, Size size, Vec3 light, List<RenderTriangle> out)`
- **output:** `void`
- **purity:** side-effecting
- **throws:** _none_
