# FeatureFlagsNotifier._load — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `feature_flags` · `lib/state/feature_flags.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_forcedOnFlags` · `_key` · `mounted`
- **writes:** `state:state`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — prefs = await SharedPreferences.getInstance()
- **precond** — if !mounted → return (void)
- **compute** — list = prefs.getStringList(_key)
- **compute** — loaded = list?.toSet() ?? <String>{}
- **init** — next = {...loaded, ..._forcedOnFlags}
- **precond** — if setEquals(next, state) → return (void)
- **effect** — state = next

## floor
- `getInstance` · `getStringList` · `setEquals` · `toSet`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if !mounted → return (void)
  - if setEquals(next, state) → return (void)
- **throws:** _none_
