# FeatureFlagsNotifier._persist — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `feature_flags` · `lib/state/feature_flags.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_key` · `state` · `mounted`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `FeatureFlagsNotifier.disable` · `FeatureFlagsNotifier.enable`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — prefs = await SharedPreferences.getInstance()
- **precond** — if !mounted → return (void)
- **effect** — await prefs.setStringList(_key, state.toList())

## floor
- `getInstance` · `setStringList` · `toList`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** pure
- **precond:**
  - if !mounted → return (void)
- **throws:** _none_
