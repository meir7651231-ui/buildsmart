# HomeContentOrderNotifier.reorder — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `home_content_order` · `lib/state/home_content_order.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int oldIndex, int newIndex) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_prefsKey↝`
- **writes:** `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** `HomeContentOrderNotifier.moveDown` · `HomeContentOrderNotifier.moveUp`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if oldIndex < 0 || oldIndex >= state.length → return (void)
- **compute** — target = newIndex
- **branch** — if target > oldIndex
- **branch** — if target < 0
- **branch** — if target > state.length - 1
- **init** — next = [...state]
- **compute** — item = next.removeAt(oldIndex)
- **effect** — next.insert(target, item)
- **effect** — state = next
- **effect** — _persist()

## floor
- `insert` · `removeAt`

## חוזה · contract
- **input:** `(int oldIndex, int newIndex)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if oldIndex < 0 || oldIndex >= state.length → return (void)
- **throws:** _none_
