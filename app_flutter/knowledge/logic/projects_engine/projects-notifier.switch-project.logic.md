# ProjectsNotifier.switchProject — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `projects_engine` · `lib/state/projects_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id, {List<SmartCartLine> outgoingCart = const []}) -> List<SmartCartLine>?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** `state:state`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if id == state.activeId → return null
- **precond** — if !state.projects.any((p) => p.id == id) → return null
- **init** — updated = [for (final p in state.projects) if (p.id == state.activeId) p.copyWith(cart: outgoingCar…
- **effect** — state = state.copyWith(projects: updated, activeId: id)
- **return** — state.active.cart

## floor
- `any` · `copyWith`

## חוזה · contract
- **input:** `(String id, {List<SmartCartLine> outgoingCart = const []})`
- **output:** `List<SmartCartLine>?`
- **purity:** side-effecting
- **precond:**
  - if id == state.activeId → return null
  - if !state.projects.any((p) => p.id == id) → return null
- **null when:** id == state.activeId · !state.projects.any((p) => p.id == id) · or search exhausted
- **throws:** _none_
