# UserProfileNotifier._mirrorToServer — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `user_profile` · `lib/state/user_profile.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_ref` · `state` · `currentUidProvider` · `useFirebaseBackend` · `usersProfileWriterProvider`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `UserProfileNotifier.update`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — ref = _ref
- **precond** — if ref == null || !useFirebaseBackend → return (void)
- **compute** — uid = ref.read(currentUidProvider)
- **compute** — writer = ref.read(usersProfileWriterProvider)
- **precond** — if uid == null || writer == null → return (void)
- **compute** — p = state
- **effect** — unawaited(writer.set(uid, {if (p.name.isNotEmpty) 'displayName' : p.name, if (validIsrael…

## floor
- `catchError` · `read` · `set` · `unawaited` · `validEmail` · `validIsraeliMobile`

## חוזה · contract
- **input:** `()`
- **output:** `void`
- **purity:** pure
- **precond:**
  - if ref == null || !useFirebaseBackend → return (void)
  - if uid == null || writer == null → return (void)
- **throws:** _none_
