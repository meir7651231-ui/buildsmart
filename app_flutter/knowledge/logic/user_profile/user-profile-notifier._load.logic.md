# UserProfileNotifier._load — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `user_profile` · `lib/state/user_profile.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kUserProfileKey` · `_userTouched`
- **writes:** `state:state`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — prefs = await SharedPreferences.getInstance()
- **compute** — raw = prefs.getString(kUserProfileKey)
- **precond** — if raw == null → return (void)
- **precond** — if _userTouched → return (void)

## floor
- `fromJson` · `getInstance` · `getString` · `jsonDecode`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if raw == null → return (void)
  - if _userTouched → return (void)
- **throws:** _none_
