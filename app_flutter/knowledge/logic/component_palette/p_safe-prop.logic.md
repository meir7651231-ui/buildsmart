# _safeProp — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `component_palette` · `/home/user/buildsmart/app_flutter/lib/logic/studio/component_palette.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String key, String value) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBodyProps` · `kLabelProps`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `validateAddComponent`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if kLabelProps.contains(key) → return promptSafeText(value, maxLen: 200, collapseWhitespace: true)
- **precond** — if kBodyProps.contains(key) → return promptSafeText(value)
- **return** — value.trim()

## floor
- `contains` · `promptSafeText` · `trim`

## חוזה · contract
- **input:** `(String key, String value)`
- **output:** `String`
- **purity:** pure
- **precond:**
  - if kLabelProps.contains(key) → return promptSafeText(value, maxLen: 200, collapseWhitespace: true)
  - if kBodyProps.contains(key) → return promptSafeText(value)
- **throws:** _none_
