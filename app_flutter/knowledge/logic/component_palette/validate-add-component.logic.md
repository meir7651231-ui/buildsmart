# validateAddComponent — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `component_palette` · `/home/user/buildsmart/app_flutter/lib/logic/studio/component_palette.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(AddComponentRequest req, {required ElementKind container, int existingOfType = 0}) -> AddComponentVerdict`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBodyProps↝` · `kComponentPalette↝` · `kLabelProps↝` · `unmodifiable`
- **writes:** _none_
- **calls:** `templateFor` · `_safeProp`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — t = templateFor(req.type)
- **precond** — if t == null → return const AddComponentVerdict.rejected('רכיב לא מוכר')
- **precond** — if !t.allowedContainers.contains(container) → return AddComponentVerdict.rejected('לא ניתן להוסיף ${t.he} לתוך מיכל מסוג «${container.name}» —…
- **init** — safe = <String, String>{for (final e in req.props.entries) e.key : _safeProp(e.key, e.value)}
- **loop** — for final key in t.requiredProps
- **compute** — value = key == 'actionId' ? (req.actionId ?? '').trim() : (safe[key] ?? '')
- **branch** — if value.isEmpty → return AddComponentVerdict.rejected('חסר שדה חובה «$key» עבור ${t.he}')
- **compute** — attached = (req.actionId ?? '').trim()
- **compute** — groundedAction = 
- **branch** — if attached.isNotEmpty
- **compute** — cap = t.maxPerContainer
- **precond** — if cap != null && existingOfType >= cap → return AddComponentVerdict.rejected('לא ניתן להוסיף עוד ${t.he} — מוגבל ל-$cap במיכל אחד')
- **return** — AddComponentVerdict.accepted(t, safeProps: Map<String, String>.unmodifiable(safe), safeAc…

## floor
- `Map` · `accepted` · `contains` · `matchCatalogActionId` · `rejected` · `trim`

## חוזה · contract
- **input:** `(AddComponentRequest req, {required ElementKind container, int existingOfType = 0})`
- **output:** `AddComponentVerdict`
- **purity:** pure
- **precond:**
  - if t == null → return const AddComponentVerdict.rejected('רכיב לא מוכר')
  - if !t.allowedContainers.contains(container) → return AddComponentVerdict.rejected('לא ניתן להוסיף ${t.he} לתוך מיכל מסוג «${container.name}» —…
  - if cap != null && existingOfType >= cap → return AddComponentVerdict.rejected('לא ניתן להוסיף עוד ${t.he} — מוגבל ל-$cap במיכל אחד')
- **throws:** _none_
