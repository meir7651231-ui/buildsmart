# buildManagerContext — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `manager_copilot` · `/home/user/buildsmart/app_flutter/lib/logic/manager_copilot.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `({required ManagerAnalytics analytics, required List<ManagerCustomer> customers, required Map<String, int> stageCounts}) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kCopilotStageLabel`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — revenue = customers.fold<int>(0, (s, c) => s + c.totalSpend)
- **compute** — totalOrders = customers.fold<int>(0, (s, c) => s + c.orderCount)
- **init** — pipe = [for (final e in kCopilotStageLabel.entries) if ((stageCounts[e.key] ?? 0) > 0) '${e.valu…
- **init** — top = [for (final c in customers.take(5)) ' - ${promptSafeText(c.name, maxLen: 40, collapseWhit…
- **compute** — creditLimitTotal = customers.fold<int>(0, (s, c) => s + c.creditLimit)
- **compute** — b = StringBuffer()..writeln('— הזמנות: סה"כ $totalOrders · פתוחות ${analytics.openOrders} · מ…
- **return** — b.toString()

## floor
- `StringBuffer` · `abs` · `clamp` · `fold` · `join` · `money` · `promptSafeText` · `round` · `take` · `toString` · `write` · `writeln`

## חוזה · contract
- **input:** `({required ManagerAnalytics analytics, required List<ManagerCustomer> customers, required Map<String, int> stageCounts})`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
