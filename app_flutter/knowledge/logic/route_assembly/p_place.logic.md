# _place — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `route_assembly` · `lib/features/fittings/geometry/route_assembly.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Mesh local, Vec3 p, Vec3 f, Vec3 axis) -> Mesh`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `assembleRoute`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — side = f.cross(axis).norm()
- **init** — wp = <double>[]
- **init** — wn = <double>[]
- **loop** — for var v = 0; v < local.vertexCount; v++
- **compute** — lx = local.positions[v * 3]
- **compute** — ly = local.positions[v * 3 + 1]
- **compute** — lz = local.positions[v * 3 + 2]
- **compute** — w = p + f.scale(lx) + axis.scale(ly) + side.scale(lz)
- **effect** — wp.addAll([w.x, w.y, w.z])
- **compute** — nx = local.normals[v * 3]
- **compute** — ny = local.normals[v * 3 + 1]
- **compute** — nz = local.normals[v * 3 + 2]
- **compute** — n = (f.scale(nx) + axis.scale(ny) + side.scale(nz)).norm()
- **effect** — wn.addAll([n.x, n.y, n.z])
- **return** — Mesh(wp, wn, local.indices)

## floor
- `Mesh` · `addAll` · `cross` · `norm` · `scale`

## חוזה · contract
- **input:** `(Mesh local, Vec3 p, Vec3 f, Vec3 axis)`
- **output:** `Mesh`
- **purity:** pure
- **throws:** _none_
