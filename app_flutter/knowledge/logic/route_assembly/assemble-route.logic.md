# assembleRoute — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `route_assembly` · `lib/features/fittings/geometry/route_assembly.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<RunElement> route, {double explode = 0}) -> List<WorldPart>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kExplodeGap` · `kPipeLen` · `kStubLen` · `math`
- **writes:** _none_
- **calls:** `_place` · `_dirVec`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — out = <WorldPart>[]
- **init** — steps = <({RunElement el, ElementMeshes em})>[]
- **loop** — for final el in route
- **compute** — em = elementMeshesFor(el.family.label, el.od, od2: el.od2)
- **branch** — if em != null
- **precond** — if steps.isEmpty → return out
- **init** — p = const Vec3(0, 0, 0)
- **init** — f = const Vec3(1, 0, 0)
- **init** — u = const Vec3(0, 1, 0)
- **compute** — firstOd = steps.first.el.od
- **effect** — emitPipe(kStubLen, firstOd, firstOd)
- **compute** — prev = firstOd
- **loop** — for var i = 0; i < steps.length; i++
- **compute** — el = steps[i].el
- **compute** — em = steps[i].em
- **branch** — if i > 0
- **branch** — if explode > 0
- **compute** — b = _dirVec(el.dir, f, u)
- **loop** — for final part in em.parts
- **compute** — bendAxis = em.turn != 0 ? b : u
- **effect** — p = p + f.scale(em.ex) + bendAxis.scale(em.ey)
- **branch** — if em.turn != 0
- **effect** — prev = el.family == Family.reducer ? (el.od2 ?? el.od) : el.od
- **branch** — if em.term → return out
- **effect** — emitPipe(kStubLen, prev, prev)
- **return** — out

## floor
- `Vec3` · `WorldPart` · `add` · `cos` · `dot` · `elementMeshesFor` · `emitPipe` · `norm` · `scale` · `sin` · `straightPipe` · `taperGeom` · `toDouble`

## חוזה · contract
- **input:** `(List<RunElement> route, {double explode = 0})`
- **output:** `List<WorldPart>`
- **purity:** pure
- **precond:**
  - if steps.isEmpty → return out
- **throws:** _none_
