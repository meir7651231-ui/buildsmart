# classifyScope — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_prompt` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_prompt.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String reply, RegistryView registry) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kScopeAll↝` · `kScopeScreenPrefix↝` · `kScopeSinglePrefix`
- **writes:** _none_
- **calls:** `studioScopeTokens`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — r = reply.trim()
- **precond** — if r.isEmpty → return null
- **compute** — tokens = studioScopeTokens(registry)
- **compute** — t = matchElementId(FakeRegistryView.of(ids: tokens), r)
- **precond** — if t != null → return t
- **branch** — if r.contains(kScopeSinglePrefix)
- **return** — null

## floor
- `contains` · `matchElementId` · `of` · `trim`

## חוזה · contract
- **input:** `(String reply, RegistryView registry)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if r.isEmpty → return null
  - if t != null → return t
- **null when:** r.isEmpty · or search exhausted
- **throws:** _none_
