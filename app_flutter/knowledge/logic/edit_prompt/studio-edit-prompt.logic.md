# studioEditPrompt — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_prompt` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_prompt.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `({required RegistryView registry, required String utterance, required String scope}) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kOpsGrammar` · `kActionCatalog` · `kComponentPalette` · `kScopeAll↝` · `kScopeScreenPrefix↝` · `kScopeSinglePrefix↝` · `kStudioMaxUtteranceChars` · `kStudioPromptCharBudget`
- **writes:** _none_
- **calls:** `scopeElementIds` · `scopeLabel` · `_elementLine` · `_fewShotExample`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — safe = promptSafeText(utterance, maxLen: kStudioMaxUtteranceChars, collapseWhitespace: true)
- **compute** — slice = scopeElementIds(scope, registry).toList()..sort()
- **compute** — b = StringBuffer()
- **effect** — b.writeln(scopeLabel(scope))
- **effect** — b.writeln()
- **effect** — b.writeln('אלמנטים בטווח (id = מה ניתן לערוך):')
- **branch** — if slice.isEmpty
- **effect** — b.writeln()
- **effect** — b.writeln('פעולות מותרות ל-setAction (id = תיאור):')
- **loop** — for final a in kActionCatalog
- **effect** — b.writeln('${a.id} = ${a.he}')
- **effect** — b.writeln()
- **effect** — b.writeln('רכיבים שניתן להוסיף (type = תיאור):')
- **loop** — for final t in kComponentPalette
- **effect** — b.writeln('${t.type.name} = ${t.he}')
- **effect** — b.writeln()
- **effect** — b.writeln('בקשת המנהל: "$safe".')
- **effect** — b.writeln(_kOpsGrammar)
- **compute** — example = _fewShotExample(slice, registry)
- **branch** — if example != null
- **compute** — prompt = b.toString()
- **return** — prompt

## floor
- `StringBuffer` · `promptSafeText` · `sort` · `toList` · `toString` · `writeln`

## חוזה · contract
- **input:** `({required RegistryView registry, required String utterance, required String scope})`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
