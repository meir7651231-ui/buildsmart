# studioScopePrompt — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_prompt` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_prompt.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(RegistryView registry, String utterance) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kScopeAll↝` · `kScopeScreenPrefix↝` · `kScopeSinglePrefix` · `kStudioMaxUtteranceChars`
- **writes:** _none_
- **calls:** `studioScopeTokens` · `_scopeTokenHe`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — safe = promptSafeText(utterance, maxLen: kStudioMaxUtteranceChars, collapseWhitespace: true)
- **compute** — tokens = studioScopeTokens(registry).toList()..sort()
- **compute** — b = StringBuffer()
- **effect** — b.writeln('טווחי-עריכה זמינים (token = תיאור):')
- **loop** — for final t in tokens
- **effect** — b.writeln('$t = ${_scopeTokenHe(t)}')
- **effect** — b.writeln('$kScopeSinglePrefix<id> = אלמנט בודד (id אמיתי מהרישום)')
- **effect** — b.writeln()
- **effect** — b.writeln('בקשת המנהל: "$safe".')
- **effect** — b.writeln('בחר token אחד בלבד מהרשימה הסגורה שמתאר את טווח-העריכה, או השב ' 'AMBIGUOUS אם…
- **return** — b.toString()

## floor
- `StringBuffer` · `promptSafeText` · `sort` · `toList` · `toString` · `writeln`

## חוזה · contract
- **input:** `(RegistryView registry, String utterance)`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
