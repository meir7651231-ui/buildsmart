# orgIdFromClaims — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `auth_state` · `lib/state/auth_state.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Map<String, dynamic>? claims) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `AuthStateNotifier._onAuthEvent` · `AuthStateNotifier.reloadRole`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if claims == null || claims.isEmpty → return null
- **compute** — value = claims['orgId']
- **precond** — if value is! String → return null
- **compute** — trimmed = value.trim()
- **return** — trimmed.isEmpty ? null : trimmed

## floor
- `trim`

## חוזה · contract
- **input:** `(Map<String, dynamic>? claims)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if claims == null || claims.isEmpty → return null
  - if value is! String → return null
- **null when:** claims == null || claims.isEmpty · value is! String · or search exhausted
- **throws:** _none_
