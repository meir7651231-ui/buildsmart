# rolesFromClaims — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `auth_state` · `lib/state/auth_state.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Map<String, dynamic>? claims) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPersonas`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `AuthStateNotifier._onAuthEvent` · `AuthStateNotifier.reloadRole`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if claims == null || claims.isEmpty → return const []
- **init** — known = {for (final p in kPersonas) p.id}
- **compute** — multi = claims['roles']
- **branch** — if multi is List
- **compute** — single = claims['role']
- **precond** — if single is String && known.contains(single) → return List.unmodifiable([single])
- **return** — const []

## floor
- `add` · `contains` · `unmodifiable`

## חוזה · contract
- **input:** `(Map<String, dynamic>? claims)`
- **output:** `List<String>`
- **purity:** pure
- **precond:**
  - if claims == null || claims.isEmpty → return const []
  - if single is String && known.contains(single) → return List.unmodifiable([single])
- **throws:** _none_
