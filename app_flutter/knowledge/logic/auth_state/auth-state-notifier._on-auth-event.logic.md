# AuthStateNotifier._onAuthEvent — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `auth_state` · `lib/state/auth_state.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(AuthUser? user) -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPersonas↝` · `_gateway` · `_gen` · `_onIdentityCleared↝` · `mounted`
- **writes:** `state:state`
- **calls:** `_clearIdentityCache` · `rolesFromClaims` · `orgIdFromClaims` · `employerIdFromClaims`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — gen = ++_gen
- **compute** — previousUid = state.user?.uid
- **branch** — if previousUid != null && previousUid != user?.uid
- **branch** — if user == null
- **effect** — state = AuthSnapshot(user: user)
- **init** — roles = const <String>[]
- **compute** — orgId = 
- **compute** — employerId = 
- **precond** — if !mounted || gen != _gen → return (void)
- **effect** — state = AuthSnapshot(user: user, roles: roles, orgId: orgId, employerId: employerId, load…

## floor
- `AuthSnapshot` · `debugPrint` · `idTokenClaims`

## חוזה · contract
- **input:** `(AuthUser? user)`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if !mounted || gen != _gen → return (void)
- **throws:** _none_
