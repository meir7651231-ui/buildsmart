# AuthStateNotifier.reloadRole — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `auth_state` · `lib/state/auth_state.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPersonas↝` · `_gateway` · `_gen` · `mounted`
- **writes:** `state:state`
- **calls:** `rolesFromClaims` · `orgIdFromClaims`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — g = _gateway
- **compute** — user = state.user
- **precond** — if g == null || user == null → return (void)
- **compute** — gen = ++_gen
- **compute** — roles = 
- **compute** — orgId = 
- **precond** — if !mounted || gen != _gen → return (void)
- **effect** — state = AuthSnapshot(user: user, roles: roles, orgId: orgId, loaded: true)

## floor
- `AuthSnapshot` · `debugPrint` · `idTokenClaims`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if g == null || user == null → return (void)
  - if !mounted || gen != _gen → return (void)
- **throws:** _none_
