# ScreenSectionsNotifier._set — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `screen_sections` · `lib/state/screen_sections.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String screen, ScreenLayout layout) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_key↝`
- **writes:** `io:prefs.remove↝` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** `ScreenSectionsNotifier.hide` · `ScreenSectionsNotifier.reorder` · `ScreenSectionsNotifier.resetScreen` · `ScreenSectionsNotifier.setLabel` · `ScreenSectionsNotifier.show`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — next = {...state}
- **branch** — if layout.isEmpty
- **effect** — state = next
- **effect** — _persist()

## floor
- `remove`

## חוזה · contract
- **input:** `(String screen, ScreenLayout layout)`
- **output:** `void`
- **purity:** side-effecting
- **throws:** _none_
