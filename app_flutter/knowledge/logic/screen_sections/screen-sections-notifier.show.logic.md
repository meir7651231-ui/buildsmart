# ScreenSectionsNotifier.show — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `screen_sections` · `lib/state/screen_sections.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String screen, String id) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** `state:state↝`
- **calls:** `_of` · `_set`
- **called-by:** `ScreenSectionsNotifier.toggle`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — l = _of(screen)
- **precond** — if !l.hidden.contains(id) → return (void)
- **effect** — _set(screen, l.copyWith(hidden: {...l.hidden}..remove(id)))

## floor
- `contains` · `copyWith` · `remove`

## חוזה · contract
- **input:** `(String screen, String id)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if !l.hidden.contains(id) → return (void)
- **throws:** _none_
