# ScreenSectionsNotifier.reorder — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `screen_sections` · `lib/state/screen_sections.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String screen, List<String> defaults, int oldIndex, int newIndex) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** `state:state↝`
- **calls:** `orderedIds` · `_set` · `_of`
- **called-by:** `ScreenSectionsNotifier.moveDown` · `ScreenSectionsNotifier.moveUp`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — ids = orderedIds(screen, defaults)
- **precond** — if oldIndex < 0 || oldIndex >= ids.length → return (void)
- **compute** — target = newIndex
- **branch** — if target > oldIndex
- **branch** — if target < 0
- **branch** — if target > ids.length - 1
- **init** — next = [...ids]
- **compute** — item = next.removeAt(oldIndex)
- **effect** — next.insert(target, item)
- **effect** — _set(screen, _of(screen).copyWith(order: next))

## floor
- `copyWith` · `insert` · `removeAt`

## חוזה · contract
- **input:** `(String screen, List<String> defaults, int oldIndex, int newIndex)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if oldIndex < 0 || oldIndex >= ids.length → return (void)
- **throws:** _none_
