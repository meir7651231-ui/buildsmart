# ScreenSectionsNotifier.orderedIds — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `screen_sections` · `lib/state/screen_sections.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String screen, List<String> defaults) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `state↝`
- **writes:** _none_
- **calls:** `_of`
- **called-by:** `ScreenSectionsNotifier.moveDown` · `ScreenSectionsNotifier.moveUp` · `ScreenSectionsNotifier.reorder` · `ScreenSectionsNotifier.visibleIds`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — stored = _of(screen).order
- **compute** — valid = defaults.toSet()
- **init** — out = <String>[]
- **loop** — for final id in stored
- **branch** — if valid.contains(id) && !out.contains(id)
- **loop** — for final id in defaults
- **branch** — if !out.contains(id)
- **return** — out

## floor
- `add` · `contains` · `toSet`

## חוזה · contract
- **input:** `(String screen, List<String> defaults)`
- **output:** `List<String>`
- **purity:** pure
- **throws:** _none_
