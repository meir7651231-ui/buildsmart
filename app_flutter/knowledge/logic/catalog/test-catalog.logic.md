# testCatalog — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `catalog` · `lib/test_harness/tests/catalog.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> List<TestResult>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kCatalogProducts` · `kCatalogTree↝` · `kLipskeyAccByCategory` · `kLipskeyAccBySku` · `kLipskeyCatalog` · `kLipskeyCompatPairOverride` · `kLipskeyConnectionSizeOverride` · `kPolyrollCatalog` · `kSmartProducts` · `_size↝` · `_stuck↝` · `fitsDN50`
- **writes:** _none_
- **calls:** `_balancedParens` · `_hasStuckDigit` · `_treeLipskeyCategories` · `_hasSize`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — results = <TestResult>[]
- **init** — hard = <TestCheck>[]
- **compute** — products = kLipskeyCatalog
- **compute** — empty = products.where((p) => p.nameHe.trim().isEmpty).toList()
- **effect** — hard.add(TestCheck(name: 'אין שמות ריקים', pass: empty.isEmpty, expected: '0', got: '${em…
- **compute** — bad = products.where((p) => garbled(p.nameHe)).toList()
- **effect** — hard.add(TestCheck(name: 'אין שמות פגומים (זבל/גרשיים/סוגריים)', pass: bad.isEmpty, expec…
- **compute** — stuck = products.where((p) => _hasStuckDigit(p.nameHe)).toList()
- **effect** — hard.add(TestCheck(name: 'אין מספר תקוע במילה', pass: stuck.isEmpty, expected: '0', got: …
- **init** — seen = <String, int>{}
- **loop** — for final p in products
- **effect** — seen[p.sku] = (seen[p.sku] ?? 0) + 1
- **compute** — dups = seen.entries.where((e) => e.value > 1).toList()
- **effect** — hard.add(TestCheck(name: 'מק"ט ייחודי (אין כפילויות)', pass: dups.isEmpty, expected: '0',…
- **compute** — missing = products.where((p) => p.sku.isEmpty || p.categoryHe.isEmpty).toList()
- **effect** — hard.add(TestCheck(name: 'כל מוצר עם sku + categoryHe', pass: missing.isEmpty, expected: …
- **compute** — treeCats = _treeLipskeyCategories()
- **compute** — prodCats = products.map((p) => p.categoryHe).toSet()
- **compute** — unmapped = prodCats.difference(treeCats)
- **effect** — hard.add(TestCheck(name: 'כל קטגוריה ממופה בעץ הצלילה', pass: unmapped.isEmpty, expected:…
- **effect** — results.add(TestResult(id: 'catalog:hard', category: TestCategory.catalog, label: 'תקינות…
- **init** — pprHard = <TestCheck>[]
- **compute** — pprEmpty = kPolyrollCatalog.where((p) => p.nameHe.trim().isEmpty).toList()
- **effect** — pprHard.add(TestCheck(name: 'PPR · אין שמות ריקים', pass: pprEmpty.isEmpty, expected: '0'…
- **compute** — pprBad = kPolyrollCatalog.where((p) => garbled(p.nameHe)).toList()
- **effect** — pprHard.add(TestCheck(name: 'PPR · אין שמות פגומים (זבל/גרשיים/סוגריים)', pass: pprBad.is…
- **compute** — pprStuck = kPolyrollCatalog.where((p) => _hasStuckDigit(p.nameHe)).toList()
- **effect** — pprHard.add(TestCheck(name: 'PPR · אין מספר תקוע במילה', pass: pprStuck.isEmpty, expected…
- **compute** — pprSkuName = kPolyrollCatalog.where((p) => p.nameHe.contains(p.sku)).toList()
- **effect** — pprHard.add(TestCheck(name: 'PPR · השם אינו מכיל את המק"ט (חילוץ-זבל)', pass: pprSkuName.…
- **init** — _toolWords = ['מקדח', 'תותב', 'מזוודת', 'מכונת', 'מברגה', 'פלטת ריתוך']
- **compute** — pprMisTool = kPolyrollCatalog.where((p) => _toolWords.any((w) => p.nameHe.contains(w)) && p.categoryHe…
- **effect** — pprHard.add(TestCheck(name: 'PPR · מוצר עם שם-כלי יושב בקטגוריית "כלי ריתוך"', pass: pprM…
- **compute** — pprMatMismatch = kPolyrollCatalog.where((p) {final mat = p.dims?['חומר'] as String?; if (mat == null) retu…
- **effect** — pprHard.add(TestCheck(name: 'PPR · חומר בשם תואם ל-dims[חומר] (PPR↔PPRCT)', pass: pprMatM…
- **effect** — results.add(TestResult(id: 'catalog:hard-ppr', category: TestCategory.catalog, label: 'תק…
- **init** — soft = <TestCheck>[]
- **compute** — noSize = products.where((p) => !_hasSize(p.nameHe)).length
- **compute** — noImg = products.where((p) => p.imageFile == null).length
- **effect** — soft.add(TestCheck(name: 'מוצרים עם גודל DN (לתאימות)', pass: true, got: '${products.leng…
- **effect** — soft.add(TestCheck(name: 'מוצרים עם תמונה', pass: true, got: '${products.length - noImg}/…
- **effect** — soft.add(TestCheck(name: 'מספר קטגוריות', pass: true, got: '${prodCats.length}'))
- **compute** — pprN = kPolyrollCatalog.length
- **compute** — pImg = kPolyrollCatalog.where((p) => p.imageAsset != null).length
- **compute** — pSpec = kPolyrollCatalog.where((p) => p.specImageAssets.first.contains('/products/spec_')).length
- **compute** — pDims = kPolyrollCatalog.where((p) => (p.dims?.length ?? 0) >= 3).length
- **effect** — soft.add(TestCheck(name: 'PPR · כיסוי-העשרה (תמונה · spec · dims)', pass: true, got: 'ima…
- **compute** — pprMisD = kPolyrollCatalog.where((p) {final d = p.dims?['d']; if (d == null) return false; if (p.na…
- **effect** — soft.add(TestCheck(name: 'PPR · dims[d] תואם לשם (יישור-עמודות)', pass: pprMisD.isEmpty, …
- **init** — pprByCat = <String, List<int>>{}
- **loop** — for final p in kPolyrollCatalog
- **effect** — pprByCat.putIfAbsent(p.categoryHe, () => []).add(p.dims?.length ?? 0)
- **compute** — richness = pprByCat.entries.map((e) => (cat: e.key, n: e.value.length, avg: e.value.fold<int>(0, (a,…
- **effect** — soft.add(TestCheck(name: 'PPR · עושר-dims לכל קו (מטריקת-קצב §15)', pass: true, got: '${k…
- **effect** — results.add(TestResult(id: 'catalog:coverage', category: TestCategory.catalog, label: 'כי…
- **init** — compat = <TestCheck>[]
- **compute** — reducer = products.firstWhere((p) => lipskeyConnectionSizes(p.nameHe).length >= 2, orElse: () => pr…
- **effect** — compat.add(TestCheck(name: 'מוצר-מעבר מזוהה עם ≥2 קצוות', pass: reducer.connectionSizes.l…
- **compute** — dn50 = products.where(fitsDN50).toList()
- **compute** — cats50 = dn50.map((p) => p.categoryHe).toSet()
- **effect** — compat.add(TestCheck(name: 'DN50 מתפרס על כמה קטגוריות (תאימות חוצה)', pass: cats50.lengt…
- **effect** — compat.add(TestCheck(name: 'כמות/אורך אינם נקראים כמידה', pass: lipskeyConnectionSizes('צ…
- **compute** — genderOk = const LipskeyCatalogProduct(sku: '_', nameHe: 'ניפל זכר 1"', nameEn: '', categoryHe: '', …
- **effect** — compat.add(TestCheck(name: 'זיהוי מין-חיבור (זכר/נקבה/דו-צדדי) — צעד 60', pass: genderOk))
- **compute** — methodOk = const LipskeyCatalogProduct(sku: '_', nameHe: 'מצמד אלקטרופוזיה DN50', nameEn: '', catego…
- **effect** — compat.add(TestCheck(name: 'זיהוי שיטת-חיבור (תבריג/דבק/אלקטרו) — צעד 61', pass: methodOk…
- **effect** — compat.add(TestCheck(name: 'override-מידה גובר על חילוץ-שם (צעד 68)', pass: () {const ove…
- **init** — allSkus = {for (final p in kLipskeyCatalog) p.sku}
- **init** — orphanOverride = [...kLipskeyConnectionSizeOverride.keys, ...kLipskeyCompatPairOverride.keys, ...kLipskeyC…
- **effect** — compat.add(TestCheck(name: 'טבלת-override ללא מק"ט יתום (צעד 68)', pass: orphanOverride.i…
- **effect** — results.add(TestResult(id: 'catalog:compat', category: TestCategory.catalog, label: 'מנוע…
- **init** — model = <TestCheck>[]
- **compute** — branded = products.where((p) => p.brandModel != null).length
- **effect** — model.add(TestCheck(name: 'זיהוי מותג-דגם מהשם (brandModel)', pass: branded > 50, got: '$…
- **compute** — colored = products.where((p) => p.colorVariant != null).length
- **effect** — model.add(TestCheck(name: 'זיהוי גוון/גימור (colorVariant)', pass: colored > 50, got: '$c…
- **compute** — idx = lipskeyWordIndex()
- **effect** — model.add(TestCheck(name: 'אינדקס-היפוך מילה→מוצרים נבנה', pass: idx.isNotEmpty && (idx['…
- **effect** — model.add(TestCheck(name: 'uid ייחודי לכל מוצר (מותג:מק"ט)', pass: products.map((p) => p.…
- **effect** — model.add(TestCheck(name: 'saleUnits כולל לפחות "בודד"', pass: products.every((p) => p.sa…
- **compute** — typed = products.where((p) => p.productType != null).length
- **effect** — model.add(TestCheck(name: 'מנתח-שם מזהה סוג ל-≥80% מהמוצרים (productType)', pass: typed >…
- **effect** — model.add(TestCheck(name: 'parsedName מחזיר רשומה עם 4 פאות', pass: () {final r = product…
- **compute** — linked = products.where((p) => smartProductForSku(p.sku) != null).length
- **effect** — model.add(TestCheck(name: 'קישור מוצר↔SmartProduct (smartProductForSku) — צעד 77', pass: …
- **init** — brandSkus = {for (final sp in kSmartProducts) for (final b in sp.brands) if (b.sku != null) b.sku!}
- **init** — catalogSkus = {for (final p in kCatalogProducts) p.sku}
- **compute** — orphanLinks = brandSkus.where((s) => !catalogSkus.contains(s)).toList()
- **effect** — model.add(TestCheck(name: 'אין קישור-SmartProduct יתום (מק"ט לא בקטלוג) — צעד 77', pass: …
- **effect** — model.add(TestCheck(name: 'lipskeyAccFor/StagesFor נופל לקטגוריה — צעד 78', pass: () {fin…
- **effect** — results.add(TestResult(id: 'catalog:model', category: TestCategory.catalog, label: 'מודל …
- **return** — results

## floor
- `LipskeyCatalogProduct` · `RegExp` · `TestCheck` · `TestResult` · `_isPprct` · `add` · `allMatches` · `any` · `compareTo` · `contains` · `containsKey` · `difference` · `every` · `expand` · `firstWhere` · `floor` · `fold` · `garbled` · `group` · `join` · `lipskeyAccFor` · `lipskeyConnectionSizes` · `lipskeyWordIndex` · `map` · `pct` · `putIfAbsent` · `round` · `smartProductForSku` · `sort` · `take` · `toList` · `toSet` · `toString` · `toStringAsFixed` · `trim` · `where`

## חוזה · contract
- **input:** `()`
- **output:** `List<TestResult>`
- **purity:** pure
- **throws:** _none_
