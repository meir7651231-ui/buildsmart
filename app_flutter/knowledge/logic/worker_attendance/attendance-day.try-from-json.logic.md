# AttendanceDay.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_attendance` · `../../../app_flutter/lib/state/worker_attendance.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> AttendanceDay?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `employerId` · `inLat` · `inLng` · `inTs` · `outLat` · `outLng` · `outTs`
- **writes:** _none_
- **calls:** `_tryDouble`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — username = raw['username']
- **compute** — date = raw['date']
- **precond** — if username is! String || date is! String → return null
- **return** — AttendanceDay(username: username, date: date, inTs: DateTime.tryParse('${raw['inTs']}'), …

## floor
- `AttendanceDay` · `tryParse`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `AttendanceDay?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if username is! String || date is! String → return null
- **null when:** raw is! Map · username is! String || date is! String · or search exhausted
- **throws:** _none_
