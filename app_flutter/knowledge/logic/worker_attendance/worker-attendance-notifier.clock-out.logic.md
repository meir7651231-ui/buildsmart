# WorkerAttendanceNotifier.clockOut — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_attendance` · `lib/state/worker_attendance.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String username, {double? lat, double? lng}) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `repo↝` · `storageKey↝`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `todayFor` · `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — today = todayFor(username)
- **precond** — if today == null || today.inTs == null || today.outTs != null → return false
- **effect** — _userTouched = true
- **effect** — state = [for (final d in state) if (d.username == username && d.date == today.date) d.cop…
- **effect** — _persist()
- **return** — true

## floor
- `copyWith` · `now`

## חוזה · contract
- **input:** `(String username, {double? lat, double? lng})`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if today == null || today.inTs == null || today.outTs != null → return false
- **throws:** _none_
