# attendanceMonth — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_attendance` · `lib/state/worker_attendance.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<AttendanceDay> all, String username, int year, int month) -> List<AttendanceDay>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — prefix = '${year.toString().padLeft(4, '0')}-' '${month.toString().padLeft(2, '0')}-'
- **return** — all.where((d) => d.username == username && d.date.startsWith(prefix)).toList()..sort((a, …

## floor
- `compareTo` · `padLeft` · `sort` · `startsWith` · `toList` · `toString` · `where`

## חוזה · contract
- **input:** `(List<AttendanceDay> all, String username, int year, int month)`
- **output:** `List<AttendanceDay>`
- **purity:** pure
- **throws:** _none_
