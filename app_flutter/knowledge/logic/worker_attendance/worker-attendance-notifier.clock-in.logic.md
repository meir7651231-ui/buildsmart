# WorkerAttendanceNotifier.clockIn — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_attendance` · `../../../app_flutter/lib/state/worker_attendance.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String username, {double? lat, double? lng, String employerId = ''}) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `storageKey↝`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `todayFor` · `attendanceDateKey` · `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — today = todayFor(username)
- **precond** — if today != null && today.inTs != null → return false
- **effect** — _userTouched = true
- **compute** — key = attendanceDateKey(DateTime.now())
- **effect** — state = [for (final d in state) if (!(d.username == username && d.date == key)) d, Attend…
- **effect** — _persist()
- **return** — true

## floor
- `AttendanceDay` · `now`

## חוזה · contract
- **input:** `(String username, {double? lat, double? lng, String employerId = ''})`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if today != null && today.inTs != null → return false
- **throws:** _none_
