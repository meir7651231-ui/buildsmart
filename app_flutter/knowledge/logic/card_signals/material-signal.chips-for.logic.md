# MaterialSignal.chipsFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_signals` · `lib/features/card_keyboard/card_signals.dart`.

## עצם · object
- **kind:** method
- **signature:** `(List<LipskeyCatalogProduct> pool) -> List<SignalChip>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kMaterialCoverageGate` · `kMaterialIsGatedCoAxis`
- **writes:** _none_
- **calls:** `seededFraction`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — materials = materialsInPoolEnriched(pool)
- **precond** — if kMaterialIsGatedCoAxis && (materials.length < 2 || seededFraction(pool) < kMaterialCovera… → return const <SignalChip>[]
- **return** — <SignalChip>[for (final m in materials) SignalChip(axisId: axisId, value: m, displayLabel…

## floor
- `SignalChip` · `materialsInPoolEnriched`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> pool)`
- **output:** `List<SignalChip>`
- **purity:** pure
- **precond:**
  - if kMaterialIsGatedCoAxis && (materials.length < 2 || seededFraction(pool) < kMaterialCovera… → return const <SignalChip>[]
- **throws:** _none_
