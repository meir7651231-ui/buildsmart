# testFinder — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `finder` · `lib/test_harness/tests/finder.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> List<TestResult>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kFinderGroups` · `kFinderSubs` · `kLipskeyCatalog`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — results = <TestResult>[]
- **init** — cats = {for (final p in kLipskeyCatalog) p.categoryHe}
- **compute** — named = kFinderGroups.where((g) => g.cats.isNotEmpty).toList()
- **init** — owner = <String, String>{}
- **init** — overlaps = <String>[]
- **loop** — for final g in named
- **loop** — for final c in g.cats
- **effect** — results.add(TestResult(id: 'finder:groups', category: TestCategory.catalog, label: 'בית —…
- **init** — orphans = <String>[]
- **init** — dupLabels = <String>[]
- **init** — notInGroup = <String>[]
- **loop** — for final e in kFinderSubs.entries
- **compute** — group = named.firstWhere((g) => g.label == e.key, orElse: () => const FinderGroup('', '', {}))
- **init** — covered = {for (final s in e.value) ...s.cats}
- **compute** — withProducts = group.cats.where(cats.contains).toSet()
- **effect** — orphans.addAll(withProducts.difference(covered).map((c) => '${e.key}:$c'))
- **init** — labels = [for (final s in e.value) s.label]
- **branch** — if labels.toSet().length != labels.length
- **loop** — for final s in e.value
- **effect** — results.add(TestResult(id: 'finder:subs', category: TestCategory.catalog, label: 'בית — ת…
- **init** — searchChecks = <TestCheck>[]
- **compute** — kitchen = findBy((p) => p.categoryHe == 'ברזי מטבח')
- **branch** — if kitchen != null
- **compute** — seat = findBy((p) => p.categoryHe == 'מושבי אסלה')
- **compute** — connector = findBy((p) => p.categoryHe == 'מסעפים וחיבורי אסלה')
- **branch** — if seat != null && connector != null
- **compute** — colored = findBy((p) => (p.color ?? '').trim().isNotEmpty)
- **branch** — if colored != null
- **effect** — results.add(TestResult(id: 'finder:search', category: TestCategory.catalog, label: 'חיפוש…
- **init** — sizeChecks = <TestCheck>[]
- **init** — mixed = [SizeToken(label: '200 מ"מ', family: SizeFamily.mm, mm: 200), SizeToken(label: '25 ס"מ', …
- **effect** — sortSizeTokens(mixed)
- **effect** — sizeChecks.add(TestCheck(name: 'מסנן גודל — מ"מ קודם ס"מ, מספרי', pass: mixed.map((t) => …
- **effect** — sizeChecks.add(TestCheck(name: 'גודל — "25 שנים אחריות" אינו chip', pass: parseSizeTokens…
- **effect** — sizeChecks.add(TestCheck(name: 'גודל — 45° אינו chip גודל (זווית)', pass: parseSizeTokens…
- **effect** — sizeChecks.add(TestCheck(name: 'גודל — ⅜" מתקפל ל-3/8" (canvaskit font)', pass: parseSize…
- **effect** — results.add(TestResult(id: 'finder:size', category: TestCategory.catalog, label: 'מסנן גו…
- **return** — results

## floor
- `FinderGroup` · `SizeToken` · `TestCheck` · `TestResult` · `add` · `addAll` · `any` · `catalogProductMatchesQuery` · `contains` · `containsKey` · `difference` · `every` · `f` · `findBy` · `firstWhere` · `join` · `map` · `parseAngleTokens` · `parseSizeTokens` · `searchRelevance` · `sortSizeTokens` · `take` · `toList` · `toSet` · `trim` · `where`

## חוזה · contract
- **input:** `()`
- **output:** `List<TestResult>`
- **purity:** pure
- **throws:** _none_
