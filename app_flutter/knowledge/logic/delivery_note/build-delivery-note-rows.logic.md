# buildDeliveryNoteRows — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `delivery_note` · `/home/user/buildsmart/app_flutter/lib/logic/delivery_note.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Order order) -> List<({String label, String value})>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_fmtDate`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — rows = <({String label, String value})>[(label: 'מספר הזמנה', value: order.id), (label: 'לקוח', …
- **branch** — if order.site.isNotEmpty
- **branch** — if order.shipTo.isNotEmpty
- **branch** — if order.createdAt != null
- **loop** — for final l in order.lines
- **compute** — head = l.emoji.isEmpty ? l.name : '${l.emoji} ${l.name}'
- **effect** — rows.add((label: head, value: '${l.qty} יח\''))
- **branch** — if order.notes.isNotEmpty
- **return** — rows

## floor
- `add`

## חוזה · contract
- **input:** `(Order order)`
- **output:** `List<({String label, String value})>`
- **purity:** pure
- **throws:** _none_
