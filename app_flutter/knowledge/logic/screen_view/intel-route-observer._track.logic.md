# IntelRouteObserver._track — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `screen_view` · `lib/state/intel/screen_view.dart`.

## עצם · object
- **kind:** method
- **signature:** `(String? screen) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_bus`
- **writes:** `field:_lastScreen`
- **calls:** _none_
- **called-by:** `IntelRouteObserver.didPop` · `IntelRouteObserver.didPush`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — bus = _bus
- **precond** — if bus == null || screen == null → return (void)
- **precond** — if screen == _lastScreen → return (void)
- **compute** — prev = _lastScreen
- **effect** — _lastScreen = screen
- **effect** — bus.track(IntelEvents.screenView, props: <String, String>{'screen' : screen, if (prev != …

## floor
- `track`

## חוזה · contract
- **input:** `(String? screen)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if bus == null || screen == null → return (void)
  - if screen == _lastScreen → return (void)
- **throws:** _none_
