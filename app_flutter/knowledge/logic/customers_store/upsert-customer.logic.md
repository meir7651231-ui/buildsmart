# upsertCustomer — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `customers_store` · `lib/state/customers_store.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<SavedCustomer> list, SavedCustomer incoming) -> List<SavedCustomer>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `CustomersStore.importAll` · `CustomersStore.upsert`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — key = incoming.dedupKey
- **compute** — idx = list.indexWhere((c) => c.id == incoming.id || c.dedupKey == key)
- **precond** — if idx < 0 → return [...list, incoming]
- **compute** — kept = list[idx]
- **compute** — merged = SavedCustomer(id: kept.id, name: incoming.name, phone: incoming.phone, email: incoming.em…
- **return** — [...list.sublist(0, idx), merged, ...list.sublist(idx + 1)]

## floor
- `SavedCustomer` · `indexWhere` · `sublist`

## חוזה · contract
- **input:** `(List<SavedCustomer> list, SavedCustomer incoming)`
- **output:** `List<SavedCustomer>`
- **purity:** pure
- **precond:**
  - if idx < 0 → return [...list, incoming]
- **throws:** _none_
