# SavedCustomer.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `customers_store` · `lib/state/customers_store.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? j) -> SavedCustomer?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `businessId` · `email` · `notes` · `phone` · `tags`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if j is! Map → return null
- **compute** — id = j['id']
- **compute** — name = j['name']
- **precond** — if id is! String || id.isEmpty || name is! String || name.isEmpty → return null
- **return** — SavedCustomer(id: id, name: name, phone: j['phone'] is String ? j['phone'] as String : ''…

## floor
- `SavedCustomer`

## חוזה · contract
- **input:** `(Object? j)`
- **output:** `SavedCustomer?`
- **purity:** pure
- **precond:**
  - if j is! Map → return null
  - if id is! String || id.isEmpty || name is! String || name.isEmpty → return null
- **null when:** j is! Map · id is! String || id.isEmpty || name is! String || name.isEmpty · or search exhausted
- **throws:** _none_
