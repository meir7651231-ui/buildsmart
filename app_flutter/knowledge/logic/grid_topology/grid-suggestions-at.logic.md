# gridSuggestionsAt — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `grid_topology` · `../../../app_flutter/lib/features/fittings/engine/grid_topology.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<GridCell> cells, (int, int, int) target, List<RunElement> candidates) -> List<RunElement>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if cells.any((c) => c.x == tx && c.y == ty && c.z == tz) → return const []
- **return** — [for (final cand in candidates) if (cells.any((c) => cellsConnect(GridCell(tx, ty, tz, ca…

## floor
- `GridCell` · `any` · `cellsConnect`

## חוזה · contract
- **input:** `(List<GridCell> cells, (int, int, int) target, List<RunElement> candidates)`
- **output:** `List<RunElement>`
- **purity:** pure
- **precond:**
  - if cells.any((c) => c.x == tx && c.y == ty && c.z == tz) → return const []
- **throws:** _none_
