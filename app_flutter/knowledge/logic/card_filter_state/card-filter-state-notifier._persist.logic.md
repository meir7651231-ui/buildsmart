# CardFilterStateNotifier._persist — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_filter_state` · `lib/state/card_filter_state.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_key` · `state`
- **writes:** `io:prefs.setString`
- **calls:** _none_
- **called-by:** `CardFilterStateNotifier._put` · `CardFilterStateNotifier.clear`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — prefs = await SharedPreferences.getInstance()
- **compute** — m = state.map((k, v) => MapEntry(k, v.toJson()))
- **effect** — await prefs.setString(_key, jsonEncode(m))

## floor
- `MapEntry` · `getInstance` · `jsonEncode` · `map` · `setString` · `toJson`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** side-effecting
- **throws:** _none_
