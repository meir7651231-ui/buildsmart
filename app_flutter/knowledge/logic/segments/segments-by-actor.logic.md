# segmentsByActor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `segments` · `/home/user/buildsmart/app_flutter/lib/logic/intel/segments.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<IntelEvent> events, {DateTime? now}) -> Map<String, ActorSegment>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kAnonymousSegmentKey↝`
- **writes:** _none_
- **calls:** `segmentKeyOf`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if events.isEmpty → return <String, ActorSegment>{}
- **init** — byActor = <String, List<IntelEvent>>{}
- **loop** — for final e in events
- **effect** — (byActor[segmentKeyOf(e)] ??= <IntelEvent>[]).add(e)
- **compute** — resolvedNow = now ?? events.map((e) => e.at).reduce((a, b) => a.isAfter(b) ? a : b)
- **init** — out = <String, ActorSegment>{}
- **effect** — byActor.forEach((key, evs) {final sessions = <String>{}; final screens = <String>{}; var …
- **return** — out

## floor
- `ActorSegment` · `add` · `detectCheckoutAbandon` · `detectDeadEndLoop` · `detectRepeatedNoResult` · `forEach` · `isAfter` · `map` · `reduce`

## חוזה · contract
- **input:** `(List<IntelEvent> events, {DateTime? now})`
- **output:** `Map<String, ActorSegment>`
- **purity:** pure
- **precond:**
  - if events.isEmpty → return <String, ActorSegment>{}
- **throws:** _none_
