# retentionCohorts — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `segments` · `/home/user/buildsmart/app_flutter/lib/logic/intel/segments.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<IntelEvent> events, {Duration localOffset = Duration.zero}) -> List<RetentionCohort>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kAnonymousSegmentKey↝`
- **writes:** `mutation:at.add↝` · `mutation:day.add↝`
- **calls:** `segmentKeyOf` · `_dayBucket` · `_buildCohort`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — daysByActor = <String, Set<DateTime>>{}
- **loop** — for final e in events
- **effect** — (daysByActor[segmentKeyOf(e)] ??= <DateTime>{}).add(_dayBucket(e.at, localOffset))
- **init** — membersByCohort = <DateTime, List<Set<DateTime>>>{}
- **effect** — daysByActor.forEach((_, days) {final first = days.reduce((a, b) => a.isBefore(b) ? a : b)…
- **init** — cohorts = <RetentionCohort>[for (final entry in membersByCohort.entries) _buildCohort(entry.key, en…
- **return** — cohorts

## floor
- `add` · `compareTo` · `forEach` · `isBefore` · `reduce` · `sort`

## חוזה · contract
- **input:** `(List<IntelEvent> events, {Duration localOffset = Duration.zero})`
- **output:** `List<RetentionCohort>`
- **purity:** side-effecting
- **throws:** _none_
