# _buildCohort — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `segments` · `/home/user/buildsmart/app_flutter/lib/logic/intel/segments.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(DateTime day, List<Set<DateTime>> members) -> RetentionCohort`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** `mutation:day.add`
- **calls:** _none_
- **called-by:** `retentionCohorts`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — maxOffset = 0
- **loop** — for final days in members
- **loop** — for final d in days
- **init** — returningByDay = <int, int>{}
- **loop** — for var n = 0; n <= maxOffset; n++
- **compute** — target = day.add(Duration(days: n))
- **compute** — count = 0
- **loop** — for final days in members
- **effect** — returningByDay[n] = count
- **return** — RetentionCohort(cohortDay: day, size: members.length, returningByDay: returningByDay)

## floor
- `Duration` · `RetentionCohort` · `add` · `contains` · `difference`

## חוזה · contract
- **input:** `(DateTime day, List<Set<DateTime>> members)`
- **output:** `RetentionCohort`
- **purity:** side-effecting
- **throws:** _none_
