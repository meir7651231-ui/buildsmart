# availabilityFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `equipment_stock_join` · `/home/user/buildsmart/app_flutter/lib/logic/equipment_stock_join.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String equipmentLabel, List<EmployerStockItem> stock) -> StockAvailability`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_normalize` · `_tokens` · `_isContiguousSubsequence` · `_locationToAvailability`
- **called-by:** `annotate`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — label = _normalize(equipmentLabel)
- **precond** — if label.isEmpty → return StockAvailability.unknown
- **compute** — labelTokens = _tokens(label)
- **loop** — for final item in stock
- **compute** — name = _normalize(item.name)
- **branch** — if name.isEmpty → continue
- **compute** — nameTokens = _tokens(name)
- **compute** — hit = name == label || _isContiguousSubsequence(nameTokens, labelTokens) || _isContiguousSubseq…
- **branch** — if hit → return _locationToAvailability(item.location)
- **return** — StockAvailability.unknown

## floor
- _none_

## חוזה · contract
- **input:** `(String equipmentLabel, List<EmployerStockItem> stock)`
- **output:** `StockAvailability`
- **purity:** pure
- **precond:**
  - if label.isEmpty → return StockAvailability.unknown
- **throws:** _none_
