# _broadcastRow — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `diff_preview` · `/home/user/buildsmart/app_flutter/lib/logic/studio/diff_preview.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<ConfigOp> ops) -> DiffLine`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_kindEmoji` · `_kindPlural`
- **called-by:** `summarizeDiff`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — counts = <ConfigOpKind, int>{}
- **compute** — styleAllColor = true
- **loop** — for final op in ops
- **compute** — kind = op.kind
- **effect** — counts[kind] = (counts[kind] ?? 0) + 1
- **branch** — if kind == ConfigOpKind.setStyle && !(op is SetStyle && op.style?.colorToken != null)
- **precond** — if counts.length == 1 → return DiffLine('${ops.length} שינויים')
- **init** — frags = <String>[for (final kind in ConfigOpKind.values) if (counts[kind] != null) '${_kindEmoji(…
- **return** — DiffLine(frags.join(' · '))

## floor
- `DiffLine` · `join`

## חוזה · contract
- **input:** `(List<ConfigOp> ops)`
- **output:** `DiffLine`
- **purity:** pure
- **precond:**
  - if counts.length == 1 → return DiffLine('${ops.length} שינויים')
- **throws:** _none_
