# _styleHe — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `diff_preview` · `/home/user/buildsmart/app_flutter/lib/logic/studio/diff_preview.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String id, CfgStyle? style, RegistryView registry) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_colorHe`
- **called-by:** `_heForOp`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — token = style?.colorToken
- **precond** — if token == null → return 'שינוי עיצוב: $id'
- **precond** — if registry.allowedValues(id, 'color').contains(token) → return 'שינוי צבע: $id ← ${_colorHe(token)}'
- **return** — 'שינוי צבע: $id'

## floor
- `allowedValues` · `contains`

## חוזה · contract
- **input:** `(String id, CfgStyle? style, RegistryView registry)`
- **output:** `String`
- **purity:** pure
- **precond:**
  - if token == null → return 'שינוי עיצוב: $id'
  - if registry.allowedValues(id, 'color').contains(token) → return 'שינוי צבע: $id ← ${_colorHe(token)}'
- **throws:** _none_
