# CardVersionsNotifier.save — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_versions` · `lib/state/card_versions.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({required String label, required String productKey, required String brandName}) -> ConfigVersion`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_key↝`
- **writes:** `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — now = DateTime.now()
- **compute** — id = '$productKey|$label|${now.microsecondsSinceEpoch}'
- **compute** — v = ConfigVersion(id: id, label: label, productKey: productKey, brandName: brandName, savedAt…
- **compute** — filtered = state.where((x) => !(x.productKey == productKey && x.label == label)).toList()
- **effect** — state = [...filtered, v]
- **effect** — _persist()
- **return** — v

## floor
- `ConfigVersion` · `now` · `toList` · `where`

## חוזה · contract
- **input:** `({required String label, required String productKey, required String brandName})`
- **output:** `ConfigVersion`
- **purity:** side-effecting
- **throws:** _none_
