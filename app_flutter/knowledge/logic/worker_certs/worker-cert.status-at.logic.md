# WorkerCert.statusAt — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_certs` · `lib/state/worker_certs.dart`.

## עצם · object
- **kind:** method
- **signature:** `(DateTime now) -> CertExpiryStatus`
- **constants:** _none_

## חיבורים · connections
- **reads:** `expiry`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — today = DateTime(now.year, now.month, now.day)
- **compute** — exp = DateTime(expiry.year, expiry.month, expiry.day)
- **precond** — if exp.isBefore(today) → return CertExpiryStatus.expired
- **precond** — if exp.difference(today).inDays <= 31 → return CertExpiryStatus.expiringSoon
- **return** — CertExpiryStatus.valid

## floor
- `DateTime` · `difference` · `isBefore`

## חוזה · contract
- **input:** `(DateTime now)`
- **output:** `CertExpiryStatus`
- **purity:** pure
- **precond:**
  - if exp.isBefore(today) → return CertExpiryStatus.expired
  - if exp.difference(today).inDays <= 31 → return CertExpiryStatus.expiringSoon
- **throws:** _none_
