# WorkerCert.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_certs` · `lib/state/worker_certs.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> WorkerCert?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `addedTs` · `employerId` · `issuer` · `photo`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — id = raw['id']
- **compute** — username = raw['username']
- **compute** — name = raw['name']
- **compute** — expiry = DateTime.tryParse('${raw['expiry']}')
- **compute** — added = DateTime.tryParse('${raw['addedTs']}')
- **precond** — if id is! String || username is! String || name is! String || expiry == null || added == null → return null
- **return** — WorkerCert(id: id, username: username, name: name, issuer: raw['issuer'] is String ? raw[…

## floor
- `WorkerCert` · `tryParse`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `WorkerCert?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if id is! String || username is! String || name is! String || expiry == null || added == null → return null
- **null when:** raw is! Map · id is! String || username is! String || name is! String || expiry == null || added == null · or search exhausted
- **throws:** _none_
