# FirestoreConfigSink.save — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_sink_firebase` · `lib/state/studio/config_sink_firebase.dart`.

## עצם · object
- **kind:** method
- **signature:** `(ConfigDoc doc) -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_cache`
- **writes:** `field:_lastPublished` · `state:_port`
- **calls:** `_sharedView`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **effect** — await _cache.save(doc)
- **compute** — prev = _lastPublished
- **precond** — if prev != null && prev == doc.published → return (void)
- **branch** — if prev == null && doc.published.isEmpty
- **effect** — _lastPublished = doc.published

## floor
- `debugPrint` · `remove` · `save` · `write`

## חוזה · contract
- **input:** `(ConfigDoc doc)`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if prev != null && prev == doc.published → return (void)
- **throws:** _none_
