# resolveAccessory — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `recipe_kit` · `lib/features/word_finder/recipe_kit.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(SmartAcc acc) -> KitLine`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kAccRankedSkus` · `kDivePool` · `kKitAutoMargin` · `kKitAutoScore` · `kKitMinScore` · `_bySku`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `assembleKit`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — curatedSku = acc.sku
- **branch** — if curatedSku != null
- **compute** — ranked = kAccRankedSkus[acc.name]
- **branch** — if ranked != null && ranked.isNotEmpty
- **compute** — best = 
- **compute** — bestScore = 0
- **compute** — runnerUp = 0
- **loop** — for final p in kDivePool
- **compute** — s = searchRelevance(p, acc.name)
- **branch** — if s > bestScore
- **precond** — if best == null || bestScore < kKitMinScore → return KitLine(acc: acc, product: null, match: KitMatch.none, score: bestScore)
- **compute** — decisiveMargin = (bestScore - runnerUp) >= kKitAutoMargin
- **precond** — if bestScore >= kKitAutoScore && decisiveMargin → return KitLine(acc: acc, product: best, match: KitMatch.auto, score: bestScore)
- **return** — KitLine(acc: acc, product: best, match: KitMatch.ambiguous, score: bestScore)

## floor
- `KitLine` · `searchRelevance` · `sublist`

## חוזה · contract
- **input:** `(SmartAcc acc)`
- **output:** `KitLine`
- **purity:** pure
- **precond:**
  - if best == null || bestScore < kKitMinScore → return KitLine(acc: acc, product: null, match: KitMatch.none, score: bestScore)
  - if bestScore >= kKitAutoScore && decisiveMargin → return KitLine(acc: acc, product: best, match: KitMatch.auto, score: bestScore)
- **throws:** _none_
