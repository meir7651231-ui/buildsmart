# _RingDiveWheelState._stepFocus — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `ring_dive_wheel` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/ring_dive_wheel.dart`.

## עצם · object
- **kind:** method
- **signature:** `(int dir) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `widget`
- **writes:** `field:_focus` · `field:_rot`
- **calls:** _none_
- **called-by:** `_RingDiveWheelState.build`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if widget.labels.isEmpty → return (void)
- **compute** — n = _n
- **compute** — next = (((_focus + dir) % n) + n) % n
- **precond** — if next == _focus → return (void)
- **branch** — if !_reduceMotion
- **effect** — setState(() {_focus = next; _rot = -next * _step;})
- **effect** — widget.onFocusChanged?.call(next)

## floor
- `call` · `selectionClick` · `setState`

## חוזה · contract
- **input:** `(int dir)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if widget.labels.isEmpty → return (void)
  - if next == _focus → return (void)
- **throws:** _none_
