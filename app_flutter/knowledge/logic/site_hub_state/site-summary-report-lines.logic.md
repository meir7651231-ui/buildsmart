# siteSummaryReportLines — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `site_hub_state` · `lib/state/site_hub_state.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<DiaryEntry> diary, List<SiteSnag> snags, List<SiteInspection> inspections) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — openSnags = snags.where((s) => s.status == 'פתוח').length
- **compute** — fixedSnags = snags.where((s) => s.status == 'טופל').length
- **compute** — plannedInsp = inspections.where((i) => i.status == 'מתוכננת').length
- **compute** — doneInsp = inspections.where((i) => i.status == 'בוצעה').length
- **return** — ['📓 רישומי-יומן: ${diary.length}', if (diary.isNotEmpty) '📅 רישום אחרון: ${diary.first.…

## floor
- `where`

## חוזה · contract
- **input:** `(List<DiaryEntry> diary, List<SiteSnag> snags, List<SiteInspection> inspections)`
- **output:** `List<String>`
- **purity:** pure
- **throws:** _none_
