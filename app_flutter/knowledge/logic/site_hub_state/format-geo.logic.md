# formatGeo — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `site_hub_state` · `lib/state/site_hub_state.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(double lat, double lng, {double? accuracyMeters}) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — latStr = '${lat.abs().toStringAsFixed(4)}°${lat >= 0 ? 'N' : 'S'}'
- **compute** — lngStr = '${lng.abs().toStringAsFixed(4)}°${lng >= 0 ? 'E' : 'W'}'
- **compute** — acc = accuracyMeters == null ? '' : ' (±${accuracyMeters.round()}מ׳)'
- **return** — '$latStr, $lngStr$acc'

## floor
- `abs` · `round` · `toStringAsFixed`

## חוזה · contract
- **input:** `(double lat, double lng, {double? accuracyMeters})`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
