# submitRoleRequest — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `role_requests` · `lib/state/role_requests.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(WidgetRef ref, String role) -> Future<bool>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kRequestableRoles` · `currentUidProvider` · `roleRequestWriterProvider` · `userProfileProvider`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — writer = ref.read(roleRequestWriterProvider)
- **compute** — uid = ref.read(currentUidProvider)
- **precond** — if writer == null || uid == null || !kRequestableRoles.contains(role) → return false
- **compute** — p = ref.read(userProfileProvider)

## floor
- `contains` · `debugPrint` · `delete` · `now` · `read` · `set` · `toIso8601String`

## חוזה · contract
- **input:** `(WidgetRef ref, String role)`
- **output:** `Future<bool>`
- **purity:** pure
- **precond:**
  - if writer == null || uid == null || !kRequestableRoles.contains(role) → return false
- **throws:** _none_
