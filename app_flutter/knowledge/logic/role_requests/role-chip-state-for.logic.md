# roleChipStateFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `role_requests` · `lib/state/role_requests.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `({required bool isRegistered, required bool isActive, required bool hasServerRole, required String? requestStatus}) -> RoleChipState`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !isRegistered → return RoleChipState.needsRegistration
- **precond** — if isActive || hasServerRole || requestStatus == 'approved' → return RoleChipState.approved
- **precond** — if requestStatus == 'denied' → return RoleChipState.rejected
- **precond** — if requestStatus == 'pending' → return RoleChipState.inProcess
- **return** — RoleChipState.needsRegistration

## floor
- _none_

## חוזה · contract
- **input:** `({required bool isRegistered, required bool isActive, required bool hasServerRole, required String? requestStatus})`
- **output:** `RoleChipState`
- **purity:** pure
- **precond:**
  - if !isRegistered → return RoleChipState.needsRegistration
  - if isActive || hasServerRole || requestStatus == 'approved' → return RoleChipState.approved
  - if requestStatus == 'denied' → return RoleChipState.rejected
  - if requestStatus == 'pending' → return RoleChipState.inProcess
- **throws:** _none_
