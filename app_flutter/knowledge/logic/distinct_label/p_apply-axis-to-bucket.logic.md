# _applyAxisToBucket — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `distinct_label` · `lib/features/word_finder/distinct_label.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String axis, List<_Row> bucket, String baseLabel) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kLabelAxisSeparator`
- **writes:** _none_
- **calls:** `_sharedWordTokens` · `_axisValue`
- **called-by:** `distinctSelectionLabels`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — sharedTokens = axis == 'word' ? _sharedWordTokens(bucket) : const <String>{}
- **init** — values = <_Row, String>{for (final r in bucket) r : _axisValue(axis, r, baseLabel, sharedTokens)}
- **precond** — if values.values.toSet().length < 2 → return (void)
- **loop** — for final r in bucket
- **compute** — v = values[r]!
- **branch** — if v.isEmpty → continue
- **effect** — r.label = '${r.label}$kLabelAxisSeparator$v'

## floor
- `toSet`

## חוזה · contract
- **input:** `(String axis, List<_Row> bucket, String baseLabel)`
- **output:** `void`
- **purity:** pure
- **precond:**
  - if values.values.toSet().length < 2 → return (void)
- **throws:** _none_
