# distinctSelectionLabels — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `distinct_label` · `lib/features/word_finder/distinct_label.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> products) -> Map<String, String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kDistinctAxisOrder` · `kLabelAxisSeparator↝`
- **writes:** _none_
- **calls:** `_baseLabel` · `_groupByLabel` · `_applyAxisToBucket` · `_numberSuffix`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if products.isEmpty → return const <String, String>{}
- **init** — rows = <_Row>[for (var i = 0; i < products.length; i++) _Row(products[i], i, _baseLabel(products…
- **loop** — for final entry in _groupByLabel(rows).entries
- **compute** — baseLabel = entry.key
- **compute** — group = entry.value
- **branch** — if group.length < 2 → continue
- **loop** — for final axis in kDistinctAxisOrder
- **loop** — for final bucket in _groupByLabel(rows).values
- **branch** — if bucket.length < 2 → continue
- **effect** — bucket.sort((a, b) => a.index.compareTo(b.index))
- **loop** — for var k = 1; k < bucket.length; k++
- **return** — <String, String>{for (final r in rows) r.product.sku : r.label}

## floor
- `_Row` · `compareTo` · `sort` · `toList` · `where`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> products)`
- **output:** `Map<String, String>`
- **purity:** pure
- **precond:**
  - if products.isEmpty → return const <String, String>{}
- **throws:** _none_
