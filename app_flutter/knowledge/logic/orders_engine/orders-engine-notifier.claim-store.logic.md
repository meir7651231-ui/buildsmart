# OrdersEngineNotifier.claimStore — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `orders_engine` · `/home/user/buildsmart/app_flutter/lib/state/orders_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String orderId, String uid) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_remote`
- **writes:** `state:state`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if uid.isEmpty → return (void)
- **compute** — r = _remote
- **branch** — if r != null
- **compute** — idx = state.indexWhere((o) => o.id == orderId)
- **precond** — if idx < 0 → return (void)
- **compute** — cur = state[idx].storeUid
- **precond** — if cur.isNotEmpty → return (void)
- **effect** — state = [for (var i = 0; i < state.length; i++) if (i == idx) state[i].copyWith(storeUid:…

## floor
- `claimStore` · `copyWith` · `indexWhere`

## חוזה · contract
- **input:** `(String orderId, String uid)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if uid.isEmpty → return (void)
  - if idx < 0 → return (void)
  - if cur.isNotEmpty → return (void)
- **throws:** _none_
