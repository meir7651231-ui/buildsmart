# OrdersEngineNotifier.setStage — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `orders_engine` · `lib/state/orders_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String orderId, String stage) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kManagerOrderFlow` · `kOrdersEngineKey↝` · `_remote` · `persist↝`
- **writes:** `field:_loaded↝` · `io:prefs.setString↝` · `state:state`
- **calls:** _none_
- **called-by:** `OrdersEngineNotifier.advance`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — r = _remote
- **branch** — if r != null
- **precond** — if !kManagerOrderFlow.contains(stage) → return (void)
- **compute** — idx = state.indexWhere((o) => o.id == orderId)
- **precond** — if idx < 0 → return (void)
- **precond** — if state[idx].stage == stage → return (void)
- **effect** — state = [for (var i = 0; i < state.length; i++) if (i == idx) state[i].copyWith(stage: st…

## floor
- `contains` · `copyWith` · `indexWhere` · `setStage`

## חוזה · contract
- **input:** `(String orderId, String stage)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if !kManagerOrderFlow.contains(stage) → return (void)
  - if idx < 0 → return (void)
  - if state[idx].stage == stage → return (void)
- **throws:** _none_
