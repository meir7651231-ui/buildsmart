# OrdersEngineNotifier.advance — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `orders_engine` · `/home/user/buildsmart/app_flutter/lib/state/orders_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String orderId) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kManagerOrderFlow` · `_functions` · `_remote` · `serverCallables` · `mounted↝`
- **writes:** `state:state↝`
- **calls:** `_advanceViaCallable` · `setStage`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — r = _remote
- **branch** — if r != null
- **compute** — idx = state.indexWhere((o) => o.id == orderId)
- **precond** — if idx < 0 → return (void)
- **compute** — cur = kManagerOrderFlow.indexOf(state[idx].stage)
- **precond** — if cur < 0 || cur >= kManagerOrderFlow.length - 1 → return (void)
- **effect** — setStage(orderId, kManagerOrderFlow[cur + 1])

## floor
- `advance` · `indexOf` · `indexWhere` · `unawaited`

## חוזה · contract
- **input:** `(String orderId)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if idx < 0 → return (void)
  - if cur < 0 || cur >= kManagerOrderFlow.length - 1 → return (void)
- **throws:** _none_
