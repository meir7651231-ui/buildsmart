# OrdersEngineNotifier._nextId — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `orders_engine` · `/home/user/buildsmart/app_flutter/lib/state/orders_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `state`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `OrdersEngineNotifier.placeOrder`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — re = RegExp(r'^BS-(\d+)$')
- **compute** — maxN = 0
- **loop** — for final o in state
- **compute** — m = re.firstMatch(o.id)
- **branch** — if m != null
- **return** — maxN > 0 ? 'BS-${maxN + 1}' : 'BS-${DateTime.now().millisecondsSinceEpoch}'

## floor
- `RegExp` · `firstMatch` · `group` · `now` · `tryParse`

## חוזה · contract
- **input:** `()`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
