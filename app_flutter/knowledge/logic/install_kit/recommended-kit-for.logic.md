# recommendedKitFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_kit` · `/home/user/buildsmart/app_flutter/lib/logic/install_kit.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> chain) -> List<KitItem>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVerifiedSpecs`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if chain.length < 2 → return const []
- **init** — out = <String, KitItem>{}
- **loop** — for var i = 0; i < chain.length - 1; i++
- **compute** — a = chain[i]
- **compute** — sa = kVerifiedSpecs[a.sku]
- **compute** — sb = kVerifiedSpecs[b.sku]
- **branch** — if sa == null || sb == null → continue
- **compute** — jointA = 
- **compute** — isDirect = false
- **loop** — for final eA in sa.ends
- **branch** — if jointA == null → continue
- **branch** — if jointA.type == EndType.bspMale || jointA.type == EndType.bspFemale
- **branch** — if sa.material.startsWith('PPR') && sb.material.startsWith('PPR')
- **branch** — if jointA.type == EndType.pexPress
- **branch** — if jointA.type == EndType.copperPress
- **compute** — ma = sa.material
- **branch** — if ma != mb
- **return** — out.values.toList()

## floor
- `KitItem` · `addItem` · `contains` · `directMatesWith` · `pipeSharedWith` · `putIfAbsent` · `startsWith` · `toList`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> chain)`
- **output:** `List<KitItem>`
- **purity:** pure
- **precond:**
  - if chain.length < 2 → return const []
- **throws:** _none_
