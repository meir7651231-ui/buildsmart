# recommendedKitForProduct — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_kit` · `/home/user/buildsmart/app_flutter/lib/logic/install_kit.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> List<KitItem>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVerifiedSpecs`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — spec = kVerifiedSpecs[p.sku]
- **branch** — if p.brand == 'פולירול' || (spec?.material.startsWith('PPR') ?? false)
- **branch** — if p.brand == 'חוליות'
- **precond** — if spec == null → return const []
- **init** — out = <String, KitItem>{}
- **loop** — for final e in spec.ends
- **branch** — if e.type == EndType.bspMale || e.type == EndType.bspFemale
- **return** — out.values.toList()

## floor
- `KitItem` · `add` · `contains` · `putIfAbsent` · `startsWith` · `toList` · `toString` · `tryParse`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `List<KitItem>`
- **purity:** pure
- **precond:**
  - if spec == null → return const []
- **throws:** _none_
