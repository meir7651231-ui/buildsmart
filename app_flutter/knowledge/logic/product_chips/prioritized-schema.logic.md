# prioritizedSchema — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `product_chips` · `../../../app_flutter/lib/features/catalog_config/product_chips.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p, {List<LipskeyCatalogProduct>? universe}) -> ProductConfigSchema`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kAxisToChip↝` · `_kSizeChips↝` · `_kUnknownPriority↝` · `kChipPriority↝` · `resolvedCatalogProducts`
- **writes:** _none_
- **calls:** `axisChips` · `chipPriority` · `_angleAttribute` · `_dnAttribute`
- **called-by:** `chipRoster`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — products = universe ?? resolvedCatalogProducts
- **compute** — base = configSchemaFor(p, universe: products)
- **compute** — engine = base.familyId == p.categoryHe ? const <AttributeDef>[] : base.attributes
- **init** — engineIds = {for (final a in engine) a.id}
- **compute** — hasDiameter = engineIds.any((id) => id.startsWith('diameter'))
- **init** — extra = [for (final c in axisChips(p, products)) if (!engineIds.contains(c.id) && !(c.id == 'diam…
- **init** — ordered = [...engine, ...extra]..sort((a, b) {final byPriority = chipPriority(a.id).compareTo(chipP…
- **return** — ProductConfigSchema(sku: base.sku, nameHe: base.nameHe, familyId: base.familyId, emoji: b…

## floor
- `ProductConfigSchema` · `any` · `compareTo` · `configSchemaFor` · `contains` · `sort` · `startsWith`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p, {List<LipskeyCatalogProduct>? universe})`
- **output:** `ProductConfigSchema`
- **purity:** pure
- **throws:** _none_
