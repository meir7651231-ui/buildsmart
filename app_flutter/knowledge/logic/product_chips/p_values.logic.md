# _values — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `product_chips` · `../../../app_flutter/lib/features/catalog_config/product_chips.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Iterable<String> raw) -> List<AttributeValue>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `axisChips`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — seen = <String>[]
- **loop** — for final r in raw
- **compute** — t = r.trim()
- **branch** — if t.isNotEmpty && !seen.contains(t)
- **return** — [for (var i = 0; i < seen.length; i++) AttributeValue(id: 'v$i', labelHe: seen[i], canoni…

## floor
- `AttributeValue` · `add` · `contains` · `trim`

## חוזה · contract
- **input:** `(Iterable<String> raw)`
- **output:** `List<AttributeValue>`
- **purity:** pure
- **throws:** _none_
