# _dnAttribute — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `product_chips` · `../../../app_flutter/lib/features/catalog_config/product_chips.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(AttributeDef a) -> AttributeDef`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `prioritizedSchema`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if a.id != 'diameter' → return a
- **init** — order = <String>[]
- **init** — inchHint = <String, String>{}
- **loop** — for final v in a.values
- **compute** — raw = v.labelHe
- **compute** — dn = canonicalDn(raw)
- **branch** — if !order.contains(dn)
- **branch** — if raw.contains('"')
- **effect** — order.sort((x, y) => dnNumber(x).compareTo(dnNumber(y)))
- **return** — AttributeDef(id: a.id, tradeId: a.tradeId, nameHe: a.nameHe, emoji: a.emoji, kind: a.kind…

## floor
- `AttributeDef` · `AttributeValue` · `add` · `canonicalDn` · `compareTo` · `contains` · `dnNumber` · `putIfAbsent` · `sort`

## חוזה · contract
- **input:** `(AttributeDef a)`
- **output:** `AttributeDef`
- **purity:** pure
- **precond:**
  - if a.id != 'diameter' → return a
- **throws:** _none_
