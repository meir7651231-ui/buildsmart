# axisChips — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `product_chips` · `../../../app_flutter/lib/features/catalog_config/product_chips.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p, List<LipskeyCatalogProduct> universe) -> List<AttributeDef>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kAxisToChip` · `_kChipLabel↝` · `_kSizeChips` · `_kTradeId↝`
- **writes:** _none_
- **calls:** `_typeGroup` · `_def` · `_values`
- **called-by:** `prioritizedSchema`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — group = _typeGroup(p, universe)
- **init** — agg = <String, List<String>>{}
- **loop** — for final m in group
- **compute** — memberHasDiameter = false
- **effect** — catAxesOf(m).forEach((axis, values) {final chipId = _kAxisToChip[axis]; if (chipId == nul…
- **branch** — if !memberHasDiameter
- **return** — [for (final e in agg.entries) if (e.value.length > 1 || _kSizeChips.contains(e.key)) _def…

## floor
- `add` · `canonicalMaterial` · `catAxesOf` · `contains` · `forEach` · `isTrueColor` · `odOf` · `putIfAbsent`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p, List<LipskeyCatalogProduct> universe)`
- **output:** `List<AttributeDef>`
- **purity:** pure
- **throws:** _none_
