# revolve — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `primitives` · `lib/features/fittings/geometry/primitives.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<List<double>> profile, int seg) -> Mesh`
- **constants:** _none_

## חיבורים · connections
- **reads:** `math`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `tube`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — p = <double>[]
- **init** — n = <double>[]
- **init** — idx = <int>[]
- **loop** — for var i = 0; i < profile.length; i++
- **compute** — p0 = profile[i]
- **compute** — p1 = profile[(i + 1) % profile.length]
- **compute** — dx = p1[0] - p0[0]
- **compute** — dr = p1[1] - p0[1]
- **compute** — nx = -dr
- **compute** — nr = dx
- **compute** — nl = math.sqrt(nx * nx + nr * nr)
- **branch** — if nl != 0
- **loop** — for var s = 0; s < seg; s++
- **return** — Mesh(p, n, idx)

## floor
- `Mesh` · `addAll` · `cos` · `sin` · `sqrt`

## חוזה · contract
- **input:** `(List<List<double>> profile, int seg)`
- **output:** `Mesh`
- **purity:** pure
- **throws:** _none_
