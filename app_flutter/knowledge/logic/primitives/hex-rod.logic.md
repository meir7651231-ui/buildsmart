# hexRod — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `primitives` · `lib/features/fittings/geometry/primitives.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(int sides, double r, double len, double cx) -> Mesh`
- **constants:** _none_

## חיבורים · connections
- **reads:** `math`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — p = <double>[]
- **init** — n = <double>[]
- **init** — idx = <int>[]
- **compute** — hh = len / 2
- **loop** — for var i = 0; i < sides; i++
- **compute** — a0 = i / sides * 2 * math.pi
- **compute** — a1 = (i + 1) / sides * 2 * math.pi
- **compute** — am = (a0 + a1) / 2
- **compute** — nc = math.cos(am)
- **compute** — ns = math.sin(am)
- **compute** — b = p.length ~/ 3
- **effect** — p.addAll([cx - hh, r * math.cos(a0), r * math.sin(a0)])
- **effect** — n.addAll([0, nc, ns])
- **effect** — p.addAll([cx + hh, r * math.cos(a0), r * math.sin(a0)])
- **effect** — n.addAll([0, nc, ns])
- **effect** — p.addAll([cx - hh, r * math.cos(a1), r * math.sin(a1)])
- **effect** — n.addAll([0, nc, ns])
- **effect** — p.addAll([cx + hh, r * math.cos(a1), r * math.sin(a1)])
- **effect** — n.addAll([0, nc, ns])
- **effect** — idx.addAll([b, b + 1, b + 2, b + 2, b + 1, b + 3])
- **return** — Mesh(p, n, idx)

## floor
- `Mesh` · `addAll` · `cos` · `sin`

## חוזה · contract
- **input:** `(int sides, double r, double len, double cx)`
- **output:** `Mesh`
- **purity:** pure
- **throws:** _none_
