# CardAccStateNotifier._update — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_acc_state` · `lib/state/card_acc_state.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String productKey, String accName, AccState Function(AccState? prev) f) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_key↝` · `from`
- **writes:** `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** `CardAccStateNotifier.setQty` · `CardAccStateNotifier.setSelected`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — outer = Map<String, Map<String, AccState>>.from(state)
- **init** — inner = Map<String, AccState>.from(outer[productKey] ?? const {})
- **effect** — inner[accName] = f(inner[accName])
- **effect** — outer[productKey] = inner
- **effect** — state = outer
- **effect** — _persist()

## floor
- `Map` · `f`

## חוזה · contract
- **input:** `(String productKey, String accName, AccState Function(AccState? prev) f)`
- **output:** `void`
- **purity:** side-effecting
- **throws:** _none_
