# projectQuoteText — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_projects` · `lib/state/card_projects.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String project, List<ProjectItem> items) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — lines = <String>['הצעת מחיר — פרויקט "$project"']
- **compute** — total = 0
- **loop** — for final it in items
- **compute** — prod = catalogProductForSku(it.sku)
- **compute** — unit = prod == null ? 0 : (priceFor(prod) ?? 0)
- **compute** — sub = unit * it.qty
- **effect** — total += sub
- **effect** — lines.add('• ${it.location}: ${it.brandName} ×${it.qty} — ~₪$sub')
- **effect** — lines.add('סה"כ משוער: ~₪$total')
- **effect** — lines.add('— נוצר ב-${AppBrand.name}')
- **return** — lines.join('\n')

## floor
- `add` · `catalogProductForSku` · `join` · `priceFor`

## חוזה · contract
- **input:** `(String project, List<ProjectItem> items)`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
