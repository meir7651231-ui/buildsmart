# _xf — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `element_meshes` · `lib/features/fittings/geometry/element_meshes.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Mesh m, {double rotZ = 0, double tx = 0, double ty = 0, double tz = 0}) -> Mesh`
- **constants:** _none_

## חיבורים · connections
- **reads:** `math`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `elementMeshesFor`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — c = math.cos(rotZ)
- **compute** — s = math.sin(rotZ)
- **init** — p = <double>[]
- **init** — n = <double>[]
- **loop** — for var i = 0; i < m.vertexCount; i++
- **compute** — x = m.positions[i * 3]
- **compute** — y = m.positions[i * 3 + 1]
- **compute** — z = m.positions[i * 3 + 2]
- **effect** — p.addAll([c * x - s * y + tx, s * x + c * y + ty, z + tz])
- **compute** — nx = m.normals[i * 3]
- **compute** — ny = m.normals[i * 3 + 1]
- **compute** — nz = m.normals[i * 3 + 2]
- **effect** — n.addAll([c * nx - s * ny, s * nx + c * ny, nz])
- **return** — Mesh(p, n, m.indices)

## floor
- `Mesh` · `addAll` · `cos` · `sin`

## חוזה · contract
- **input:** `(Mesh m, {double rotZ = 0, double tx = 0, double ty = 0, double tz = 0})`
- **output:** `Mesh`
- **purity:** pure
- **throws:** _none_
