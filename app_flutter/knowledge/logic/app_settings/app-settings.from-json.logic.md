# AppSettings.fromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `app_settings` · `lib/state/app_settings.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Map<String, dynamic> j) -> AppSettings`
- **constants:** _none_

## חיבורים · connections
- **reads:** `biometric` · `consentedPolicyVersion` · `currency` · `express` · `haul` · `highContrast` · `lang` · `locationPerm` · `notifBudget` · `notifDeals` · `notifOrders` · `notifShipments` · `privAnalytics` · `privCrashReports` · `privLocation` · `privMarketing` · `privPresence` · `reduceMotion` · `sessionTimeout` · `textSize` · `theme` · `twoFA` · `units`
- **writes:** _none_
- **calls:** `_enum` · `_intToTimeout`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — display = m('display')
- **compute** — notif = m('notif')
- **compute** — region = m('region')
- **compute** — delivery = m('delivery')
- **compute** — acc = m('accessibility')
- **compute** — sec = m('security')
- **compute** — priv = (sec['privacy'] as Map<String, dynamic>?) ?? const {}
- **return** — AppSettings(theme: _enum(display['theme'], BsTheme.values, BsTheme.light), textSize: _enu…

## floor
- `AppSettings` · `m` · `toInt`

## חוזה · contract
- **input:** `(Map<String, dynamic> j)`
- **output:** `AppSettings`
- **purity:** pure
- **throws:** _none_
