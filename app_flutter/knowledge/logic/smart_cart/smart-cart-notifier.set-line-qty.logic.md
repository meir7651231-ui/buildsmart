# SmartCartNotifier.setLineQty — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `smart_cart` · `/home/user/buildsmart/app_flutter/lib/state/smart_cart.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int index, int qty) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** `state:state`
- **calls:** `remove`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if index < 0 || index >= state.length → return (void)
- **branch** — if qty <= 0
- **compute** — l = state[index]
- **effect** — state = [for (var i = 0; i < state.length; i++) if (i == index) SmartCartLine(productKey:…

## floor
- `SmartCartLine`

## חוזה · contract
- **input:** `(int index, int qty)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if index < 0 || index >= state.length → return (void)
- **throws:** _none_
