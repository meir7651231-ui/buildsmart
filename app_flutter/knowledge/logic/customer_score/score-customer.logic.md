# scoreCustomer — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `customer_score` · `/home/user/buildsmart/app_flutter/lib/logic/customer_score.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(RfmInput input) -> CustomerScore`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kRfmFreqHigh` · `kRfmFreqMid` · `kRfmMoneyHigh` · `kRfmMoneyMid` · `kRfmRecentDays` · `kRfmStaleDays`
- **writes:** _none_
- **calls:** `_band`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — f = _band(input.orderCount, kRfmFreqHigh, kRfmFreqMid)
- **compute** — m = _band(input.totalSpend, kRfmMoneyHigh, kRfmMoneyMid)
- **compute** — rd = input.recencyDays
- **compute** — r = 
- **branch** — if rd == null
- **compute** — hasR = r >= 0
- **compute** — points = f + m + (hasR ? r : 0)
- **compute** — maxPoints = hasR ? 6 : 4
- **compute** — ratio = maxPoints == 0 ? 0.0 : points / maxPoints
- **compute** — tier = 
- **branch** — if ratio >= 0.75
- **compute** — atRisk = hasR && r == 0 && (f + m) >= 3
- **return** — CustomerScore(r: r, f: f, m: m, points: points, maxPoints: maxPoints, tier: tier, atRisk:…

## floor
- `CustomerScore`

## חוזה · contract
- **input:** `(RfmInput input)`
- **output:** `CustomerScore`
- **purity:** pure
- **throws:** _none_
