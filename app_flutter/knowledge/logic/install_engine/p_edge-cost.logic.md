# _edgeCost — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct a, LipskeyCatalogProduct b) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_drainageFamily` · `_fittingCats↝` · `_fittingTypes↝` · `kBspInchToMm↝` · `kVerifiedSpecs` · `companyCatalogActive↝`
- **writes:** _none_
- **calls:** `_minBoreMmOf` · `isFitting`
- **called-by:** `_findShortestPathExcluding` · `_pathCost` · `findShortestPath`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — sa = kVerifiedSpecs[a.sku]
- **compute** — sb = kVerifiedSpecs[b.sku]
- **compute** — ma = sa?.material
- **compute** — mb = sb?.material
- **compute** — transition = 
- **branch** — if ma == null || mb == null || ma == mb
- **compute** — pipeBridge = 2
- **branch** — if sa != null && sb != null
- **compute** — bore = _minBoreMmOf(b)
- **compute** — boreCost = bore == null || bore >= 15 ? 0 : (15 - bore).round().clamp(0, 10)
- **compute** — deviceFiller = isFitting(b) ? 0 : 50
- **return** — 10 + deviceFiller + transition + pipeBridge + boreCost

## floor
- `clamp` · `contains` · `directMatesWith` · `round`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct a, LipskeyCatalogProduct b)`
- **output:** `int`
- **purity:** pure
- **throws:** _none_
