# buildInstallation — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> anchors, {int maxDepthPerSegment = 6, int tempC = 20, Set<String> accessories = const {}, bool loop = false, bool autoCompliance = false}) -> InstallationPlan`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_accessorySkus↝` · `_fixtureCats↝` · `_kHotThresholdC↝` · `_kIsolationValveSkus↝` · `_structuralCats↝` · `_terminalCats↝` · `kHotWaterAccessorySkus↝` · `kVerifiedSpecs↝` · `chainUniverse` · `isShutoff↝`
- **writes:** `mutation:accessories.add↝` · `mutation:items.insert↝`
- **calls:** `findShortestPath` · `_findBridge` · `materializeChain` · `_autoAddCompliance` · `flowRole`
- **called-by:** `buildTreeInstallation`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if anchors.isEmpty → return const InstallationPlan([], [], {})
- **init** — items = <LipskeyCatalogProduct>[]
- **init** — qty = <String, int>{}
- **init** — gaps = <InstallationGap>[]
- **effect** — add(anchors.first)
- **loop** — for var i = 0; i < anchors.length - 1; i++
- **compute** — a = anchors[i]
- **compute** — seg = findShortestPath(a, b, maxDepth: maxDepthPerSegment, tempC: tempC)
- **branch** — if seg == null
- **loop** — for final p in seg.skip(1)
- **branch** — if loop && anchors.length >= 2
- **branch** — if items.length >= 2
- **branch** — if accessories.isNotEmpty && items.isNotEmpty
- **branch** — if autoCompliance && items.isNotEmpty
- **compute** — terminals = items.where((p) => flowRole(p) == FlowRole.fixture).toList()
- **branch** — if terminals.length > 1
- **compute** — zones = items.isEmpty ? const <String, List<String>>{} : {'קו ראשי' : items.map((p) => p.sku).toL…
- **return** — InstallationPlan(items, gaps, qty, zones: zones)

## floor
- `InstallationGap` · `InstallationPlan` · `add` · `addAll` · `clear` · `containsKey` · `fold` · `forEach` · `map` · `skip` · `sublist` · `toList` · `where`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> anchors, {int maxDepthPerSegment = 6, int tempC = 20, Set<String> accessories = const {}, bool loop = false, bool autoCompliance = false})`
- **output:** `InstallationPlan`
- **purity:** side-effecting
- **precond:**
  - if anchors.isEmpty → return const InstallationPlan([], [], {})
- **throws:** _none_
