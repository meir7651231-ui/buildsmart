# _couplingFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String dn, Set<String> mats) -> LipskeyCatalogProduct?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kDrainageFamily` · `kVerifiedSpecs` · `chainUniverse`
- **writes:** _none_
- **calls:** `_isPipeProductE`
- **called-by:** `_pipeBetween`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — fallback = 
- **loop** — for final p in chainUniverse
- **branch** — if _isPipeProductE(p) → continue
- **compute** — s = kVerifiedSpecs[p.sku]
- **branch** — if s == null → continue
- **compute** — m = s.material
- **compute** — compat = mats.contains(m) || (_kDrainageFamily.contains(m) && mats.any(_kDrainageFamily.contains))
- **branch** — if !compat → continue
- **compute** — dnEnds = s.ends.where((e) => e.type == EndType.hdpeCompression && e.size == dn).length
- **branch** — if dnEnds >= 2 → return p
- **branch** — if dnEnds >= 1
- **return** — fallback

## floor
- `any` · `contains` · `where`

## חוזה · contract
- **input:** `(String dn, Set<String> mats)`
- **output:** `LipskeyCatalogProduct?`
- **purity:** pure
- **null when:** no result
- **throws:** _none_
