# flowRole — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> FlowRole`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_accessorySkus` · `_fixtureCats` · `_structuralCats` · `_terminalCats` · `kHotWaterAccessorySkus`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_findBridge` · `_findShortestPathExcluding` · `_usableConnector` · `buildInstallation` · `findShortestPath`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if _accessorySkus.contains(p.sku) || kHotWaterAccessorySkus.contains(p.sku) → return FlowRole.accessory
- **compute** — c = p.categoryHe
- **precond** — if _structuralCats.contains(c) → return FlowRole.accessory
- **precond** — if _fixtureCats.contains(c) || _terminalCats.contains(c) → return FlowRole.fixture
- **return** — FlowRole.connector

## floor
- `contains`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `FlowRole`
- **purity:** pure
- **precond:**
  - if _accessorySkus.contains(p.sku) || kHotWaterAccessorySkus.contains(p.sku) → return FlowRole.accessory
  - if _structuralCats.contains(c) → return FlowRole.accessory
  - if _fixtureCats.contains(c) || _terminalCats.contains(c) → return FlowRole.fixture
- **throws:** _none_
