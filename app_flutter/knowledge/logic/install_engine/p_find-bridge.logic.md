# _findBridge — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct from, LipskeyCatalogProduct to, int tempC) -> LipskeyCatalogProduct?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_accessorySkus↝` · `_allSystems↝` · `_drainCats↝` · `_fittingCats↝` · `_fittingTypes↝` · `_fixtureCats↝` · `_structuralCats↝` · `_supplyCats↝` · `_terminalCats↝` · `kHotWaterAccessorySkus↝` · `kVerifiedSpecs` · `chainUniverse` · `companyCatalogActive↝`
- **writes:** _none_
- **calls:** `flowRole` · `productSystems` · `isFitting` · `productSuitableForTemp` · `canConnect`
- **called-by:** `buildInstallation` · `buildTreeInstallation`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if flowRole(from) == FlowRole.fixture && flowRole(to) == FlowRole.fixture → return null
- **compute** — shared = productSystems(from).intersection(productSystems(to))
- **precond** — if shared.isEmpty → return null
- **compute** — best = 
- **compute** — bestVerified = false
- **loop** — for final p in chainUniverse
- **branch** — if !isFitting(p) → continue
- **branch** — if !productSuitableForTemp(p, tempC) → continue
- **branch** — if !canConnect(from, p) || !canConnect(p, to) → continue
- **compute** — isVerified = kVerifiedSpecs[p.sku] != null
- **branch** — if best == null || (!bestVerified && isVerified)
- **return** — best

## floor
- `intersection`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct from, LipskeyCatalogProduct to, int tempC)`
- **output:** `LipskeyCatalogProduct?`
- **purity:** pure
- **precond:**
  - if flowRole(from) == FlowRole.fixture && flowRole(to) == FlowRole.fixture → return null
  - if shared.isEmpty → return null
- **null when:** flowRole(from) == FlowRole.fixture && flowRole(to) == FlowRole.fixture · shared.isEmpty · or search exhausted
- **throws:** _none_
