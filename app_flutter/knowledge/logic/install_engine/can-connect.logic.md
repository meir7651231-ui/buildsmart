# canConnect — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct a, LipskeyCatalogProduct b) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVerifiedSpecs`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_findBridge` · `_findShortestPathExcluding` · `compatibleWith` · `findShortestPath`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if a.sku == b.sku → return false
- **compute** — vA = kVerifiedSpecs[a.sku]
- **precond** — if vA != null && vB != null → return vA.compatibleWith(vB)
- **compute** — sA = a.connectionSizes.toSet()
- **compute** — sB = b.connectionSizes.toSet()
- **precond** — if sA.isEmpty || sB.isEmpty || sA.intersection(sB).isEmpty → return false
- **compute** — gA = a.connectionGender
- **precond** — if gA != null && gB != null && gA == gB → return false
- **compute** — mA = a.connectionMethod
- **precond** — if mA != null && mB != null && mA != mB → return false
- **return** — true

## floor
- `compatibleWith` · `intersection` · `toSet`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct a, LipskeyCatalogProduct b)`
- **output:** `bool`
- **purity:** pure
- **precond:**
  - if a.sku == b.sku → return false
  - if vA != null && vB != null → return vA.compatibleWith(vB)
  - if sA.isEmpty || sB.isEmpty || sA.intersection(sB).isEmpty → return false
  - if gA != null && gB != null && gA == gB → return false
  - if mA != null && mB != null && mA != mB → return false
- **throws:** _none_
