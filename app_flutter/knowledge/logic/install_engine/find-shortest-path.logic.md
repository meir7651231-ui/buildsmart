# findShortestPath — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct from, LipskeyCatalogProduct to, {int maxDepth = 6, int tempC = 20}) -> List<LipskeyCatalogProduct>?`
- **constants:** `1 << 30`

## חיבורים · connections
- **reads:** `_accessorySkus↝` · `_allSystems↝` · `_drainCats↝` · `_drainageFamily↝` · `_fixtureCats↝` · `_structuralCats↝` · `_supplyCats↝` · `_terminalCats↝` · `kHotWaterAccessorySkus↝` · `kVerifiedSpecs↝` · `chainUniverse↝`
- **writes:** `cache:_compatCache↝`
- **calls:** `flowRole` · `productSystems` · `canConnect` · `compatibleWith` · `_usableConnector` · `_edgeCost`
- **called-by:** `buildInstallation` · `buildTreeInstallation` · `findAlternativePaths`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if from.sku == to.sku → return [from]
- **precond** — if flowRole(from) == FlowRole.fixture && flowRole(to) == FlowRole.fixture → return null
- **compute** — sysFrom = productSystems(from)
- **compute** — sysTo = productSystems(to)
- **precond** — if sysFrom.intersection(sysTo).isEmpty → return null
- **precond** — if canConnect(from, to) → return [from, to]
- **init** — buckets = SplayTreeMap<int, List<(List<LipskeyCatalogProduct>, Set<WaterSystem>)>>()
- **effect** — buckets[0] = [([from], sysFrom)]
- **init** — bestCost = <String, int>{from.sku : 0}
- **loop** — while buckets.isNotEmpty
- **compute** — cost = buckets.firstKey()!
- **compute** — bucket = buckets[cost]!
- **branch** — if bucket.isEmpty
- **compute** — tail = path.last
- **branch** — if tail.sku == to.sku → return path
- **branch** — if cost > (bestCost[tail.sku] ?? 1 << 30) → continue
- **branch** — if path.length > maxDepth → continue
- **loop** — for final next in compatibleWith(tail, tempC: tempC)
- **return** — null

## floor
- `SplayTreeMap` · `add` · `firstKey` · `intersection` · `putIfAbsent` · `remove` · `removeLast`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct from, LipskeyCatalogProduct to, {int maxDepth = 6, int tempC = 20})`
- **output:** `List<LipskeyCatalogProduct>?`
- **purity:** deterministic-side-effecting
- **precond:**
  - if from.sku == to.sku → return [from]
  - if flowRole(from) == FlowRole.fixture && flowRole(to) == FlowRole.fixture → return null
  - if sysFrom.intersection(sysTo).isEmpty → return null
  - if canConnect(from, to) → return [from, to]
- **null when:** flowRole(from) == FlowRole.fixture && flowRole(to) == FlowRole.fixture · sysFrom.intersection(sysTo).isEmpty · or search exhausted
- **throws:** _none_
