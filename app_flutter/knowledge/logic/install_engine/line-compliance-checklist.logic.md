# lineComplianceChecklist — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> chain, int tempC, Set<String> accessories, {TradeResolution? trade}) -> List<LineCheck>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kHotThresholdC` · `_kIsolationValveSkus` · `kVerifiedSpecs↝`
- **writes:** _none_
- **calls:** `_galvanicallyDissimilar` · `lineIsSupply` · `_isDirectionalDevice` · `_directionalContext`
- **called-by:** `InstallationPlan.compliance`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **branch** — if trade != null && trade.tradeId != 'plumbing'
- **compute** — skus = chain.map((p) => p.sku).toSet()
- **compute** — mats = chain.map(productMaterial).whereType<String>().toSet()
- **compute** — hot = tempC >= _kHotThresholdC
- **compute** — hasPex = mats.contains('PEX')
- **compute** — recirc = skus.contains('HW-PUMP-25') || skus.contains('HW-TEE-RECIRC')
- **compute** — dissimilar = _galvanicallyDissimilar(mats)
- **compute** — isolationCount = chain.where((p) => _kIsolationValveSkus.contains(p.sku) || ((p.productType == 'ברז' || p.…
- **compute** — hasCommercialPump = skus.contains('HW-PUMP-40')
- **compute** — hasManifoldOrShower = has({'HW-MANIFOLD-3', 'HW-MANIFOLD-4', 'HW-MANIFOLD-6', 'HW-SHOWER-HEAD', 'HW-TMTV-32', '…
- **compute** — isSupply = lineIsSupply(chain)
- **compute** — hasGardenOutlet = chain.any((p) => p.categoryHe == 'ברזי גן' || p.categoryHe == 'ציוד גן')
- **return** — [if (isSupply) LineCheck(recirc ? 'ברז ניתוק ×3 (כניסת דוד + אחרי משאבה + מניפולד)' : 'בר…

## floor
- `LineCheck` · `acc` · `any` · `completion` · `contains` · `has` · `map` · `specOf` · `systemCoherence` · `toSet` · `where` · `whereType`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> chain, int tempC, Set<String> accessories, {TradeResolution? trade})`
- **output:** `List<LineCheck>`
- **purity:** pure
- **throws:** _none_
