# _autoAddCompliance — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> items, Map<String, int> qty, int tempC, {bool loop = false, Set<String>? accessories}) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kHotThresholdC` · `_kIsolationValveSkus` · `kVerifiedSpecs` · `chainUniverse↝` · `isShutoff`
- **writes:** `cache:_skuCache↝` · `mutation:accessories.add` · `mutation:items.insert`
- **calls:** `_skuOf` · `lineIsSupply` · `_galvanicallyDissimilar` · `productMaterial`
- **called-by:** `buildInstallation` · `buildTreeInstallation`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — skus = qty.keys.toSet()
- **compute** — hot = tempC >= _kHotThresholdC
- **compute** — hasCommercialPump = skus.contains('HW-PUMP-40')
- **compute** — hasManifoldOrShower = skus.intersection({'HW-MANIFOLD-3', 'HW-MANIFOLD-4', 'HW-MANIFOLD-6', 'HW-SHOWER-HEAD', '…
- **compute** — isSupply = lineIsSupply(items)
- **branch** — if isSupply && isolations() == 0
- **branch** — if isSupply && hot
- **branch** — if loop
- **compute** — hasPex = items.any((p) => kVerifiedSpecs[p.sku]?.material == 'PEX' || (p.categoryHe == 'מחברי NTM'…
- **branch** — if hot && hasPex
- **branch** — if hasCommercialPump
- **compute** — matsFinal = items.map(productMaterial).whereType<String>().toSet()
- **branch** — if _galvanicallyDissimilar(matsFinal)
- **branch** — if accessories != null

## floor
- `add` · `any` · `clamp` · `contains` · `indexWhere` · `insert` · `insertAt` · `intersection` · `isolations` · `map` · `toInt` · `toSet` · `where` · `whereType`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> items, Map<String, int> qty, int tempC, {bool loop = false, Set<String>? accessories})`
- **output:** `void`
- **purity:** side-effecting
- **throws:** _none_
