# _directionalContext — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> chain, int i) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `lineComplianceChecklist`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — up = i > 0 ? chain[i - 1].nameHe : null
- **compute** — down = i < chain.length - 1 ? chain[i + 1].nameHe : null
- **precond** — if up != null && down != null → return 'בין "$up" ל-"$down"'
- **precond** — if down != null → return 'בכניסת הקו (לפני "$down")'
- **precond** — if up != null → return 'ביציאת הקו (אחרי "$up")'
- **return** — 'בקו'

## floor
- _none_

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> chain, int i)`
- **output:** `String`
- **purity:** pure
- **precond:**
  - if up != null && down != null → return 'בין "$up" ל-"$down"'
  - if down != null → return 'בכניסת הקו (לפני "$down")'
  - if up != null → return 'ביציאת הקו (אחרי "$up")'
- **throws:** _none_
