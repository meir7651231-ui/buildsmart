# findAlternativePaths — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct from, LipskeyCatalogProduct to, {int k = 3, int maxDepth = 6, int tempC = 20}) -> List<List<LipskeyCatalogProduct>>`
- **constants:** `1 << 30`

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `findShortestPath` · `_findShortestPathExcluding` · `_pathCost`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if k <= 0 → return const []
- **init** — results = <List<LipskeyCatalogProduct>>[]
- **compute** — first = findShortestPath(from, to, maxDepth: maxDepth, tempC: tempC)
- **precond** — if first == null → return const []
- **effect** — results.add(first)
- **init** — blocked = <(String, String)>{}
- **loop** — while results.length < k
- **init** — bestCandidate = <LipskeyCatalogProduct>[]
- **compute** — bestCost = 1 << 30
- **compute** — lastPath = results.last
- **loop** — for var i = 0; i < lastPath.length - 1; i++
- **branch** — if bestCandidate.isEmpty → break
- **effect** — results.add(bestCandidate)
- **return** — results

## floor
- `add` · `any` · `contains` · `every` · `generate` · `remove`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct from, LipskeyCatalogProduct to, {int k = 3, int maxDepth = 6, int tempC = 20})`
- **output:** `List<List<LipskeyCatalogProduct>>`
- **purity:** pure
- **precond:**
  - if k <= 0 → return const []
  - if first == null → return const []
- **throws:** _none_
