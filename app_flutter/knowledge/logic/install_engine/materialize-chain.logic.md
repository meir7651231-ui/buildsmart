# materializeChain — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> chain) -> List<LipskeyCatalogProduct>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVerifiedSpecs↝`
- **writes:** _none_
- **calls:** `_pipeBetween`
- **called-by:** `buildInstallation`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if chain.length < 2 → return List.of(chain)
- **init** — out = <LipskeyCatalogProduct>[chain.first]
- **loop** — for var i = 0; i < chain.length - 1; i++
- **compute** — pipe = _pipeBetween(chain[i], chain[i + 1])
- **branch** — if pipe != null
- **effect** — out.add(chain[i + 1])
- **return** — out

## floor
- `add` · `of`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> chain)`
- **output:** `List<LipskeyCatalogProduct>`
- **purity:** pure
- **precond:**
  - if chain.length < 2 → return List.of(chain)
- **throws:** _none_
