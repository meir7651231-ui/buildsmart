# buildTreeInstallation — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> trunk, List<LipskeyCatalogProduct> branchTargets, {int maxDepthPerSegment = 6, int tempC = 20, Set<String> accessories = const {}, bool autoCompliance = false}) -> InstallationPlan`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_branchLetters↝` · `_kHotThresholdC` · `_kIsolationValveSkus↝` · `kVerifiedSpecs↝` · `chainUniverse↝` · `isShutoff↝`
- **writes:** `cache:_skuCache↝` · `mutation:accessories.add↝` · `mutation:items.insert↝`
- **calls:** `buildInstallation` · `manifoldOutlets` · `_branchLabel` · `findShortestPath` · `_findBridge` · `_skuOf` · `_autoAddCompliance`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — items = <LipskeyCatalogProduct>[]
- **init** — qty = <String, int>{}
- **init** — gaps = <InstallationGap>[]
- **init** — zones = <String, List<String>>{}
- **init** — engineWarnings = <String>[]
- **compute** — manifold = 
- **branch** — if trunk.isNotEmpty
- **compute** — root = manifold ?? (branchTargets.isNotEmpty ? branchTargets.first : null)
- **init** — builtZones = <String>[]
- **compute** — realTargets = root == null ? const <LipskeyCatalogProduct>[] : branchTargets.where((t) => t.sku != root…
- **compute** — outlets = manifold != null ? manifoldOutlets(manifold) : 0
- **compute** — cap = (outlets > 0 && outlets < realTargets.length) ? outlets : realTargets.length
- **branch** — if manifold != null && outlets > 0 && realTargets.length > outlets
- **compute** — routed = 0
- **loop** — for var bi = 0; bi < cap; bi++
- **compute** — t = realTargets[bi]
- **compute** — zl = _branchLabel(routed++)
- **effect** — builtZones.add(zl)
- **compute** — seg = findShortestPath(root!, t, maxDepth: maxDepthPerSegment, tempC: tempC)
- **branch** — if seg == null
- **loop** — for final p in seg.skip(1)
- **branch** — if manifold != null && tempC >= _kHotThresholdC && builtZones.isNotEmpty
- **compute** — trunkSkus = items.map((p) => p.sku).toSet()
- **branch** — if trunkSkus.contains('HW-PUMP-40') && builtZones.isNotEmpty
- **branch** — if accessories.isNotEmpty && items.isNotEmpty
- **branch** — if autoCompliance && items.isNotEmpty
- **return** — InstallationPlan(items, gaps, qty, zones: zones, warnings: engineWarnings)

## floor
- `InstallationGap` · `InstallationPlan` · `add` · `addAll` · `contains` · `containsKey` · `difference` · `fold` · `map` · `putIfAbsent` · `qtyOf` · `skip` · `toList` · `toSet` · `where`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> trunk, List<LipskeyCatalogProduct> branchTargets, {int maxDepthPerSegment = 6, int tempC = 20, Set<String> accessories = const {}, bool autoCompliance = false})`
- **output:** `InstallationPlan`
- **purity:** side-effecting
- **throws:** _none_
