# connectionFailReason — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct a, LipskeyCatalogProduct b) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVerifiedSpecs`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — vA = kVerifiedSpecs[a.sku]
- **branch** — if vA != null && vB != null
- **compute** — sA = a.connectionSizes.toSet()
- **compute** — sB = b.connectionSizes.toSet()
- **precond** — if sA.isEmpty || sB.isEmpty → return 'גודל חיבור לא ידוע'
- **precond** — if sA.intersection(sB).isEmpty → return 'גודל שונה: ${sA.first} ↔ ${sB.first}'
- **compute** — gA = a.connectionGender
- **precond** — if gA == null || gB == null → return 'מין חיבור לא ידוע'
- **branch** — if gA == gB
- **compute** — mA = a.connectionMethod
- **branch** — if mA != null && mB != null && mA != mB
- **return** — 'אין נקודת חיבור משותפת'

## floor
- `intersection` · `map` · `sizes` · `toSet` · `where`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct a, LipskeyCatalogProduct b)`
- **output:** `String`
- **purity:** pure
- **precond:**
  - if sA.isEmpty || sB.isEmpty → return 'גודל חיבור לא ידוע'
  - if sA.intersection(sB).isEmpty → return 'גודל שונה: ${sA.first} ↔ ${sB.first}'
  - if gA == null || gB == null → return 'מין חיבור לא ידוע'
- **throws:** _none_
