# manifoldOutlets — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVerifiedSpecs`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `buildTreeInstallation`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if p.productType != 'מחלק' && p.categoryHe != 'מחלקים' → return 0
- **compute** — spec = kVerifiedSpecs[p.sku]
- **precond** — if spec == null || spec.ends.length < 3 → return 0
- **init** — counts = <String, int>{}
- **loop** — for final e in spec.ends
- **effect** — counts[e.size] = (counts[e.size] ?? 0) + 1
- **compute** — maxc = counts.values.fold(0, (a, b) => a > b ? a : b)
- **return** — maxc >= 2 ? maxc : 0

## floor
- `fold`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `int`
- **purity:** pure
- **precond:**
  - if p.productType != 'מחלק' && p.categoryHe != 'מחלקים' → return 0
  - if spec == null || spec.ends.length < 3 → return 0
- **throws:** _none_
