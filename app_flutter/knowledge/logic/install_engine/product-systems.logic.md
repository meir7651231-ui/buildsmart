# productSystems — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `install_engine` · `/home/user/buildsmart/app_flutter/lib/logic/install_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> Set<WaterSystem>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_allSystems` · `_drainCats` · `_fixtureCats` · `_structuralCats` · `_supplyCats` · `kVerifiedSpecs`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_findBridge` · `_findShortestPathExcluding` · `findShortestPath`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — c = p.categoryHe
- **precond** — if _supplyCats.contains(c) → return {WaterSystem.supply}
- **precond** — if _drainCats.contains(c) → return {WaterSystem.drainage}
- **precond** — if _fixtureCats.contains(c) || _structuralCats.contains(c) → return _allSystems
- **compute** — ends = kVerifiedSpecs[p.sku]?.endSystems
- **return** — (ends == null || ends.isEmpty) ? _allSystems : ends

## floor
- `contains`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `Set<WaterSystem>`
- **purity:** pure
- **precond:**
  - if _supplyCats.contains(c) → return {WaterSystem.supply}
  - if _drainCats.contains(c) → return {WaterSystem.drainage}
  - if _fixtureCats.contains(c) || _structuralCats.contains(c) → return _allSystems
- **throws:** _none_
