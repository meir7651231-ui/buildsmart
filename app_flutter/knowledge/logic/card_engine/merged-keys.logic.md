# mergedKeys — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_engine` · `lib/features/card_keyboard/card_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> pool, List<NewbieStep> stack, WordLexicon lexicon, String? subtype, {Set<String> historySkus = const <String>{}}) -> CardVerdict`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kFirstQuestion` · `kFirstWordCount` · `kMaxDiveTurns` · `kMergedAxisFloor↝` · `kMergedAxisMaxPerAxis↝` · `kMergedKeyCap↝` · `kShowProductsThreshold` · `kSmartProducts↝` · `divePoolBySku↝`
- **writes:** _none_
- **calls:** `_mergedChips`
- **called-by:** `mergedKeysMemoized`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if stack.isEmpty → return CardAskWords(kFirstQuestion, wordsByFrequency(lexicon).take(kFirstWordCount).toList())
- **precond** — if pool.isNotEmpty && distinctCardCount(pool) <= 1 → return CardResolve(pool.first, pool)
- **precond** — if distinctCardCount(pool) <= kShowProductsThreshold → return CardShowProducts(distinctProducts(pool, cap: pool.length))
- **precond** — if stack.length >= kMaxDiveTurns - 1 → return CardShowProducts(distinctProducts(pool, cap: pool.length))
- **compute** — chips = _mergedChips(pool, stack, subtype, historySkus)
- **precond** — if chips.isEmpty → return CardShowProducts(distinctProducts(pool, cap: pool.length))
- **return** — MergedKeys(chips)

## floor
- `CardAskWords` · `CardResolve` · `CardShowProducts` · `MergedKeys` · `distinctCardCount` · `distinctProducts` · `take` · `toList` · `wordsByFrequency`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> pool, List<NewbieStep> stack, WordLexicon lexicon, String? subtype, {Set<String> historySkus = const <String>{}})`
- **output:** `CardVerdict`
- **purity:** pure
- **precond:**
  - if stack.isEmpty → return CardAskWords(kFirstQuestion, wordsByFrequency(lexicon).take(kFirstWordCount).toList())
  - if pool.isNotEmpty && distinctCardCount(pool) <= 1 → return CardResolve(pool.first, pool)
  - if distinctCardCount(pool) <= kShowProductsThreshold → return CardShowProducts(distinctProducts(pool, cap: pool.length))
  - if stack.length >= kMaxDiveTurns - 1 → return CardShowProducts(distinctProducts(pool, cap: pool.length))
  - if chips.isEmpty → return CardShowProducts(distinctProducts(pool, cap: pool.length))
- **throws:** _none_
