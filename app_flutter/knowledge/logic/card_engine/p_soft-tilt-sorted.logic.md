# _softTiltSorted — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_engine` · `lib/features/card_keyboard/card_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<SignalChip> chips, SignalSource src, List<LipskeyCatalogProduct> pool, Set<String> connMates, Set<String> recipeMates, Set<String> historySkus) -> List<SignalChip>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_mergedChips`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — ranked = [for (var i = 0; i < chips.length; i++) (index: i, chip: chips[i], tilt: tiltOf(chips[i])…
- **return** — [for (final e in ranked) e.chip]

## floor
- `any` · `compareTo` · `contains` · `matches` · `softTilt` · `sort` · `tiltOf` · `where`

## חוזה · contract
- **input:** `(List<SignalChip> chips, SignalSource src, List<LipskeyCatalogProduct> pool, Set<String> connMates, Set<String> recipeMates, Set<String> historySkus)`
- **output:** `List<SignalChip>`
- **purity:** pure
- **throws:** _none_
