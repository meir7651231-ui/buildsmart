# _mergedChips — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_engine` · `lib/features/card_keyboard/card_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> pool, List<NewbieStep> stack, String? subtype, Set<String> historySkus) -> List<SignalChip>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kMergedAxisFloor` · `kMergedAxisMaxPerAxis` · `kMergedKeyCap` · `kSmartProducts` · `divePoolBySku`
- **writes:** _none_
- **calls:** `_scoreAxis` · `_softTiltSorted` · `representativeTake`
- **called-by:** `mergedKeys`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — answered = <String>{for (final s in stack) s.axisLabel}
- **init** — scored = <_AxisScore>[]
- **compute** — sources = sourcesFor(subtype)
- **compute** — anchor = softAnchor(pool)
- **init** — connMates = <String>{}
- **init** — recipeMates = <String>{}
- **branch** — if anchor != null
- **loop** — for var rank = 0; rank < sources.length; rank++
- **compute** — src = sources[rank]
- **branch** — if answered.contains(src.axisName) → continue
- **compute** — score = _scoreAxis(rank, src, pool)
- **branch** — if score != null
- **effect** — scored.sort((a, b) {final lhs = a.sumSq * b.n; final rhs = b.sumSq * a.n; if (lhs != rhs)…
- **init** — out = <SignalChip>[]
- **loop** — for final a in scored
- **branch** — if out.length >= kMergedKeyCap → break
- **compute** — room = kMergedKeyCap - out.length
- **branch** — if out.isNotEmpty && room < kMergedAxisFloor → break
- **compute** — take = room < kMergedAxisMaxPerAxis ? room : kMergedAxisMaxPerAxis
- **compute** — src = sources[a.rank]
- **compute** — isMagnitude = const {'size', 'angle'}.contains(a.chips.first.axisId)
- **compute** — ordered = (anchor == null || isMagnitude) ? a.chips : _softTiltSorted(a.chips, src, pool, connMates…
- **compute** — taken = isMagnitude ? representativeTake(ordered, take) : ordered.take(take).toList()
- **loop** — for final c in taken
- **return** — out

## floor
- `add` · `addAll` · `any` · `compareTo` · `compatibleProductsFor` · `contains` · `distinctCardCount` · `kitSkusFor` · `matches` · `softAnchor` · `sort` · `sourcesFor` · `take` · `toDouble` · `toList` · `where` · `withInfoGain`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> pool, List<NewbieStep> stack, String? subtype, Set<String> historySkus)`
- **output:** `List<SignalChip>`
- **purity:** pure
- **throws:** _none_
