# _scoreAxis — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_engine` · `lib/features/card_keyboard/card_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(int rank, SignalSource src, List<LipskeyCatalogProduct> pool) -> _AxisScore?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kMergedAxisFloor`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_mergedChips`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — chips = src.chipsFor(pool)
- **precond** — if chips.length < kMergedAxisFloor → return null
- **compute** — n = distinctCardCount(pool)
- **precond** — if n == 0 → return null
- **compute** — sumSq = 0
- **compute** — anySplit = false
- **loop** — for final chip in chips
- **compute** — nc = distinctCardCount(pool.where((p) => src.matches(p, chip)).toList())
- **branch** — if nc < n
- **effect** — sumSq += nc * nc
- **precond** — if !anySplit → return null
- **return** — _AxisScore(rank, chips, sumSq, n)

## floor
- `_AxisScore` · `chipsFor` · `distinctCardCount` · `matches` · `toList` · `where`

## חוזה · contract
- **input:** `(int rank, SignalSource src, List<LipskeyCatalogProduct> pool)`
- **output:** `_AxisScore?`
- **purity:** pure
- **precond:**
  - if chips.length < kMergedAxisFloor → return null
  - if n == 0 → return null
  - if !anySplit → return null
- **null when:** chips.length < kMergedAxisFloor · n == 0 · !anySplit · or search exhausted
- **throws:** _none_
