# mergedKeysMemoized — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_engine` · `lib/features/card_keyboard/card_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> pool, List<NewbieStep> stack, WordLexicon lexicon, String? subtype, {bool enabled = kCensusMemoEnabled}) -> CardVerdict`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kFirstQuestion↝` · `kFirstWordCount↝` · `kMaxDiveTurns↝` · `kShowProductsThreshold↝` · `collapseKeyOf`
- **writes:** `cache:_mergedKeysMemo`
- **calls:** `mergedKeys`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !enabled → return mergedKeys(pool, stack, lexicon, subtype)
- **compute** — poolSig = (pool.map(collapseKeyOf).toSet().toList()..sort()).join('|')
- **compute** — axesSig = (stack.map((s) => s.axisLabel).toSet().toList()..sort()).join(',')
- **compute** — key = '$poolSig##$axesSig##$subtype'
- **return** — _mergedKeysMemo.putIfAbsent(key, () => mergedKeys(pool, stack, lexicon, subtype))

## floor
- `join` · `map` · `putIfAbsent` · `sort` · `toList` · `toSet`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> pool, List<NewbieStep> stack, WordLexicon lexicon, String? subtype, {bool enabled = kCensusMemoEnabled})`
- **output:** `CardVerdict`
- **purity:** deterministic-side-effecting
- **precond:**
  - if !enabled → return mergedKeys(pool, stack, lexicon, subtype)
- **throws:** _none_
