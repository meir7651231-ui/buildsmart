# representativeTake — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `card_engine` · `lib/features/card_keyboard/card_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<SignalChip> chips, int count) -> List<SignalChip>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_mergedChips`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if count <= 1 || chips.length <= count → return chips.take(count).toList()
- **init** — picked = <int>{}
- **init** — out = <SignalChip>[]
- **loop** — for var i = 0; i < count; i++
- **compute** — idx = (i * (chips.length - 1) / (count - 1)).round()
- **branch** — if picked.add(idx)
- **return** — out

## floor
- `add` · `round` · `take` · `toList`

## חוזה · contract
- **input:** `(List<SignalChip> chips, int count)`
- **output:** `List<SignalChip>`
- **purity:** pure
- **precond:**
  - if count <= 1 || chips.length <= count → return chips.take(count).toList()
- **throws:** _none_
