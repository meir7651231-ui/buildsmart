# detectCheckoutAbandon — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `funnels` · `/home/user/buildsmart/app_flutter/lib/logic/intel/funnels.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<IntelEvent> events, {required DateTime now, Duration idleEnd = kIdleEnd}) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `analyzeIntel`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — bySession = <String, List<IntelEvent>>{}
- **loop** — for final e in events
- **effect** — (bySession[e.sessionId ?? ''] ??= <IntelEvent>[]).add(e)
- **init** — flagged = <String>[]
- **loop** — for final entry in bySession.entries
- **compute** — evs = entry.value
- **compute** — hasStart = evs.any((e) => e.name == IntelEvents.checkoutStart)
- **branch** — if !hasStart → continue
- **compute** — converted = evs.any((e) => e.name == IntelEvents.orderPlaced)
- **branch** — if converted → continue
- **compute** — last = evs.map((e) => e.at).reduce((a, b) => a.isAfter(b) ? a : b)
- **branch** — if now.difference(last) >= idleEnd
- **effect** — flagged.sort()
- **return** — flagged

## floor
- `add` · `any` · `difference` · `isAfter` · `map` · `reduce` · `sort`

## חוזה · contract
- **input:** `(List<IntelEvent> events, {required DateTime now, Duration idleEnd = kIdleEnd})`
- **output:** `List<String>`
- **purity:** pure
- **throws:** _none_
