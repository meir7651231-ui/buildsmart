# detectRepeatedNoResult — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `funnels` · `/home/user/buildsmart/app_flutter/lib/logic/intel/funnels.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<IntelEvent> events, {int threshold = kRepeatNoResult}) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `analyzeIntel`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — counts = <String, int>{}
- **loop** — for final e in events
- **branch** — if e.name != IntelEvents.searchNoResult → continue
- **compute** — k = e.sessionId ?? ''
- **effect** — counts[k] = (counts[k] ?? 0) + 1
- **init** — flagged = <String>[for (final entry in counts.entries) if (entry.value >= threshold) entry.key]..so…
- **return** — flagged

## floor
- `sort`

## חוזה · contract
- **input:** `(List<IntelEvent> events, {int threshold = kRepeatNoResult})`
- **output:** `List<String>`
- **purity:** pure
- **throws:** _none_
