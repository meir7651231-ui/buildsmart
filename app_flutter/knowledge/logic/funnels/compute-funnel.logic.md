# computeFunnel — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `funnels` · `/home/user/buildsmart/app_flutter/lib/logic/intel/funnels.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<IntelEvent> events, {List<String> flow = kIntelFunnelFlow}) -> FunnelReport`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kPercentScale` · `filled`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `analyzeIntel`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — flowSet = flow.toSet()
- **init** — bySession = <String, Set<String>>{}
- **loop** — for final e in events
- **branch** — if !flowSet.contains(e.name) → continue
- **effect** — (bySession[e.sessionId ?? ''] ??= <String>{}).add(e.name)
- **init** — reached = List<int>.filled(flow.length, 0)
- **loop** — for final stages in bySession.values
- **loop** — for var i = 0; i < flow.length; i++
- **compute** — base = flow.isEmpty ? 0 : reached[0]
- **init** — stages = <FunnelStage>[for (var i = 0; i < flow.length; i++) FunnelStage(name: flow[i], reached: r…
- **compute** — biggest = 
- **branch** — if base > 0
- **return** — FunnelReport(stages: stages, biggestDropOff: biggest)

## floor
- `FunnelDropOff` · `FunnelReport` · `FunnelStage` · `List` · `add` · `contains` · `toSet`

## חוזה · contract
- **input:** `(List<IntelEvent> events, {List<String> flow = kIntelFunnelFlow})`
- **output:** `FunnelReport`
- **purity:** pure
- **throws:** _none_
