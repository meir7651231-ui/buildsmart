# detectDeadEndLoop — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `funnels` · `/home/user/buildsmart/app_flutter/lib/logic/intel/funnels.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<IntelEvent> events, {int threshold = kBackLoop}) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_maxOscillation`
- **called-by:** `analyzeIntel`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — bySession = <String, List<IntelEvent>>{}
- **loop** — for final e in events
- **branch** — if e.name != IntelEvents.screenView → continue
- **effect** — (bySession[e.sessionId ?? ''] ??= <IntelEvent>[]).add(e)
- **init** — flagged = <String>[]
- **loop** — for final entry in bySession.entries
- **init** — indexed = <(int, IntelEvent)>[for (var i = 0; i < entry.value.length; i++) (i, entry.value[i])]..so…
- **init** — path = <String>[]
- **loop** — for final p in indexed
- **branch** — if _maxOscillation(path) >= threshold
- **effect** — flagged.sort()
- **return** — flagged

## floor
- `add` · `compareTo` · `sort`

## חוזה · contract
- **input:** `(List<IntelEvent> events, {int threshold = kBackLoop})`
- **output:** `List<String>`
- **purity:** pure
- **throws:** _none_
