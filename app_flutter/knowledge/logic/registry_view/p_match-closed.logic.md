# _matchClosed — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `registry_view` · `/home/user/buildsmart/app_flutter/lib/logic/studio/registry_view.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Set<String> closed, String reply) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `matchActionId` · `matchComponentType` · `matchElementId` · `matchPropKey` · `matchValue`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — r = reply.trim()
- **precond** — if r.isEmpty → return null
- **loop** — for final k in closed
- **branch** — if r == k → return k
- **compute** — best = 
- **loop** — for final k in closed
- **branch** — if k.isNotEmpty && r.contains(k) && (best == null || k.length > best.length)
- **return** — best

## floor
- `contains` · `trim`

## חוזה · contract
- **input:** `(Set<String> closed, String reply)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if r.isEmpty → return null
- **null when:** r.isEmpty · or search exhausted
- **throws:** _none_
