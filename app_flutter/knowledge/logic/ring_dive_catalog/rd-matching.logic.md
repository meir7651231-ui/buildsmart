# rdMatching — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `ring_dive_catalog` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/ring_dive_catalog.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(RdCons cons, [List<RdProduct>? pool]) -> List<RdProduct>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `rdFindAxes` · `rdOptsFor`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — ps = pool ?? rdPool
- **precond** — if cons.isEmpty → return ps
- **return** — ps.where((rp) => cons.entries.every((e) => rp.has(e.key, e.value))).toList()

## floor
- `every` · `has` · `toList` · `where`

## חוזה · contract
- **input:** `(RdCons cons, [List<RdProduct>? pool])`
- **output:** `List<RdProduct>`
- **purity:** pure
- **precond:**
  - if cons.isEmpty → return ps
- **throws:** _none_
