# rdOptsFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `ring_dive_catalog` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/ring_dive_catalog.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String field, RdCons cons, [List<RdProduct>? pool, List<RdProduct>? matched]) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kRdOrder↝`
- **writes:** _none_
- **calls:** `rdMatching` · `_rank`
- **called-by:** `rdFindAxes`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — base = matched ?? rdMatching(cons, pool)
- **init** — set = <String>{}
- **loop** — for final rp in base
- **compute** — v = rp.axes[field]
- **branch** — if v != null
- **compute** — out = set.toList()..sort((a, b) {final r = _rank(field, a).compareTo(_rank(field, b)); return r…
- **return** — out

## floor
- `addAll` · `compareTo` · `sort` · `toList`

## חוזה · contract
- **input:** `(String field, RdCons cons, [List<RdProduct>? pool, List<RdProduct>? matched])`
- **output:** `List<String>`
- **purity:** pure
- **throws:** _none_
