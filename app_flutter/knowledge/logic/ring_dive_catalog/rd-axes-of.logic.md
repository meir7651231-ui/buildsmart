# rdAxesOf — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `ring_dive_catalog` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/ring_dive_catalog.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> Map<String, Set<String>>`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — out = <String, Set<String>>{}
- **compute** — taxo = _taxo[p.categoryHe]
- **compute** — dept = _dept[p.categoryHe]
- **branch** — if dept != null
- **effect** — put('cat', <String>[taxo?.cat ?? p.categoryHe])
- **effect** — put('type', <String>[taxo?.type ?? p.categoryHe])
- **effect** — put('size', productSizeTokens(p).map((t) => t.label))
- **effect** — put('angle', parseAngleTokens(p.nameHe).map((t) => t.label))
- **compute** — material = materialOf(p)
- **branch** — if material != null
- **compute** — color = p.color
- **branch** — if color != null
- **effect** — put('brand', <String>[p.brand])
- **return** — out

## floor
- `map` · `materialOf` · `parseAngleTokens` · `productSizeTokens` · `put` · `trim`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `Map<String, Set<String>>`
- **purity:** pure
- **throws:** _none_
