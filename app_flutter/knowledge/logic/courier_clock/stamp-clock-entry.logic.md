# stampClockEntry — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `courier_clock` · `/home/user/buildsmart/app_flutter/lib/state/courier_clock.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Map<String, dynamic> map, String orderId, {required bool pickedUp, required bool delivered}) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `from`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `stampCourierClock`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — entry = map[orderId] is Map ? Map<String, dynamic>.from(map[orderId] as Map) : <String, dynamic>{}
- **compute** — now = DateTime.now().toIso8601String()
- **branch** — if pickedUp && DateTime.tryParse('${entry['pickedUpAt']}') == null
- **branch** — if delivered && DateTime.tryParse('${entry['deliveredAt']}') == null
- **effect** — map[orderId] = entry

## floor
- `Map` · `now` · `toIso8601String` · `tryParse`

## חוזה · contract
- **input:** `(Map<String, dynamic> map, String orderId, {required bool pickedUp, required bool delivered})`
- **output:** `void`
- **purity:** pure
- **throws:** _none_
