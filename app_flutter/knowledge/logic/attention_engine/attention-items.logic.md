# attentionItems — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `attention_engine` · `/home/user/buildsmart/app_flutter/lib/logic/attention_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(AttentionInput inp, {required OrgConfig cfg}) -> List<AttentionItem>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kAttnApprovalsCritCount` · `kAttnOrderCritDays`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `digestLines`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — out = <AttentionItem>[]
- **init** — aging = [...inp.agingOrders]..sort((a, b) => b.ageDays - a.ageDays)
- **compute** — tagOrder = termOf(cfg, 'attn.tag.order', 'הזמנה')
- **loop** — for final o in aging.take(3)
- **effect** — out.add(AttentionItem(key: 'order:${o.id}', tag: tagOrder, title: 'הזמנה ${o.id} ממתינה $…
- **branch** — if aging.length > 3
- **branch** — if inp.pendingApprovals > 0
- **branch** — if inp.pendingVacations > 0
- **branch** — if inp.pendingAccountReqs > 0
- **return** — [...out.where((a) => a.sev == AttentionSev.crit), ...out.where((a) => a.sev == AttentionS…

## floor
- `AttentionItem` · `add` · `sort` · `take` · `termOf` · `where`

## חוזה · contract
- **input:** `(AttentionInput inp, {required OrgConfig cfg})`
- **output:** `List<AttentionItem>`
- **purity:** pure
- **throws:** _none_
