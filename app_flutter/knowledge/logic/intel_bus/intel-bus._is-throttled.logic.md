# IntelBus._isThrottled — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `intel_bus` · `lib/state/intel/intel_bus.dart`.

## עצם · object
- **kind:** method
- **signature:** `(String name) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_minInterval` · `_throttled`
- **writes:** `field:_lastEmittedAt`
- **calls:** _none_
- **called-by:** `IntelBus.track`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !_throttled.contains(name) → return false
- **compute** — now = DateTime.now()
- **compute** — last = _lastEmittedAt[name]
- **precond** — if last != null && now.difference(last) < _minInterval → return true
- **effect** — _lastEmittedAt[name] = now
- **return** — false

## floor
- `contains` · `difference` · `now`

## חוזה · contract
- **input:** `(String name)`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if !_throttled.contains(name) → return false
  - if last != null && now.difference(last) < _minInterval → return true
- **throws:** _none_
