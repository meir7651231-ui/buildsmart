# PenaltyLedgerNotifier.add — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `finance_hub_state` · `lib/state/finance_hub_state.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(int days, {DateTime? now}) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPenaltyPerDay`
- **writes:** `state:state`
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — d = days < 1 ? 1 : days
- **compute** — row = Penalty(id: 'PEN-${300 + state.length + 1}', days: d, perDay: kPenaltyPerDay, amount: d *…
- **effect** — state = [row, ...state]
- **return** — row.amount

## floor
- `Penalty` · `caToday`

## חוזה · contract
- **input:** `(int days, {DateTime? now})`
- **output:** `int`
- **purity:** side-effecting
- **throws:** _none_
