# VoiceService.listen — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `voice` · `lib/services/voice.dart`.

## עצם · object
- **kind:** method
- **signature:** `({required void Function(String text) onFinal, void Function(String reason)? onError, String localeId = 'he-IL'}) -> Future<bool>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_engine` · `stt`
- **writes:** `field:_initialized↝` · `field:_sessionActive` · `field:_sessionGotResult` · `field:_sessionOnError`
- **calls:** `_ensureInitialized`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — ok = await _ensureInitialized()
- **precond** — if !ok → return false
- **effect** — _sessionActive = true
- **effect** — _sessionGotResult = false
- **effect** — _sessionOnError = onError
- **return** — true

## floor
- `SpeechListenOptions` · `listen` · `onFinal`

## חוזה · contract
- **input:** `({required void Function(String text) onFinal, void Function(String reason)? onError, String localeId = 'he-IL'})`
- **output:** `Future<bool>`
- **purity:** side-effecting
- **precond:**
  - if !ok → return false
- **throws:** _none_
