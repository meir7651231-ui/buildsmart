# _criticalBusinessKind — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_safety` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_safety.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(ElementDescriptor d) -> _CriticalKind?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_reasonToBlock`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — id = d.id.toLowerCase()
- **compute** — label = d.labelHe
- **precond** — if label.contains('אשר הזמנה') || id.contains('confirmorder') || id.contains('approveorder') → return _CriticalKind.confirmOrder
- **precond** — if id.contains('price') || label.contains('מחיר') → return _CriticalKind.price
- **return** — null

## floor
- `contains` · `toLowerCase`

## חוזה · contract
- **input:** `(ElementDescriptor d)`
- **output:** `_CriticalKind?`
- **purity:** pure
- **precond:**
  - if label.contains('אשר הזמנה') || id.contains('confirmorder') || id.contains('approveorder') → return _CriticalKind.confirmOrder
  - if id.contains('price') || label.contains('מחיר') → return _CriticalKind.price
- **null when:** no result
- **throws:** _none_
