# _batchCeilingReason — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_safety` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_safety.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<ConfigOp> ops, RegistryView grounding, int priorSessionOps) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kStudioMaxBatch` · `kStudioMaxRegistryFraction` · `kStudioSessionBudget`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `validateSafe`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if ops.isEmpty → return null
- **precond** — if ops.length > kStudioMaxBatch → return 'השינוי נרחב מדי — ${ops.length} פעולות (מעל התקרה ' '$kStudioMaxBatch). צמצם את הטווח.'
- **compute** — cumulative = priorSessionOps + ops.length
- **precond** — if cumulative > kStudioSessionBudget → return 'השינוי נרחב מדי לסשן — $cumulative פעולות מצטברות ' '(מעל $kStudioSessionBudget). צמצם.'
- **compute** — size = grounding.elementIds().length
- **init** — distinct = <String>{for (final op in ops) op.id}.length
- **branch** — if size > 0 && distinct > size * kStudioMaxRegistryFraction
- **return** — null

## floor
- `elementIds` · `round`

## חוזה · contract
- **input:** `(List<ConfigOp> ops, RegistryView grounding, int priorSessionOps)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if ops.isEmpty → return null
  - if ops.length > kStudioMaxBatch → return 'השינוי נרחב מדי — ${ops.length} פעולות (מעל התקרה ' '$kStudioMaxBatch). צמצם את הטווח.'
  - if cumulative > kStudioSessionBudget → return 'השינוי נרחב מדי לסשן — $cumulative פעולות מצטברות ' '(מעל $kStudioSessionBudget). צמצם.'
- **null when:** ops.isEmpty · or search exhausted
- **throws:** _none_
