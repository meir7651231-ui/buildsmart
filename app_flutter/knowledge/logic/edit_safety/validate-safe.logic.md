# validateSafe — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_safety` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_safety.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<ConfigOp> ops, {List<ElementDescriptor> registry = kElementRegistry, RegistryView? view, String? persona, int priorSessionOps = 0}) -> SafetyVerdict`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kRefSurface↝` · `kStudioMaxBatch↝` · `kStudioMaxRegistryFraction↝` · `kStudioMinContrast↝` · `kStudioSessionBudget↝`
- **writes:** _none_
- **calls:** `_batchCeilingReason` · `_reasonToBlock`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — grounding = view ?? ElementRegistryView(registry)
- **compute** — ceiling = _batchCeilingReason(ops, grounding, priorSessionOps)
- **precond** — if ceiling != null → return SafetyVerdict(applied: const [], blocked: [for (final op in ops) BlockedEntry(op, ceiling…
- **init** — applied = <ConfigOp>[]
- **init** — blocked = <BlockedEntry>[]
- **loop** — for final op in ops
- **compute** — reason = _reasonToBlock(op, registry, grounding, persona)
- **branch** — if reason == null
- **return** — SafetyVerdict(applied: applied, blocked: blocked)

## floor
- `BlockedEntry` · `ElementRegistryView` · `SafetyVerdict` · `add`

## חוזה · contract
- **input:** `(List<ConfigOp> ops, {List<ElementDescriptor> registry = kElementRegistry, RegistryView? view, String? persona, int priorSessionOps = 0})`
- **output:** `SafetyVerdict`
- **purity:** pure
- **precond:**
  - if ceiling != null → return SafetyVerdict(applied: const [], blocked: [for (final op in ops) BlockedEntry(op, ceiling…
- **throws:** _none_
