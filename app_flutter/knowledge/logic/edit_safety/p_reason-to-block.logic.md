# _reasonToBlock — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_safety` · `/home/user/buildsmart/app_flutter/lib/logic/studio/edit_safety.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(ConfigOp op, List<ElementDescriptor> registry, RegistryView grounding, String? persona) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kRefSurface` · `_kRoleContractor↝` · `kStudioMinContrast`
- **writes:** _none_
- **calls:** `_criticalBusinessKind` · `_roleFloorBlock` · `_colorForToken` · `_contrastRatio`
- **called-by:** `validateSafe`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — d = findDescriptor(registry, op.id)
- **precond** — if d == null → return 'רכיב לא מוכר במרשם — חסום מטעמי בטיחות (fail-closed)'
- **precond** — if d.kImmutable → return 'רכיב נעול (ניווט/הזדהות) — «${d.labelHe}» אינו ניתן לעריכה'
- **compute** — crit = _criticalBusinessKind(d)
- **branch** — switch op

## floor
- `actionIdsFor` · `allowedValues` · `contains` · `findDescriptor` · `toStringAsFixed`

## חוזה · contract
- **input:** `(ConfigOp op, List<ElementDescriptor> registry, RegistryView grounding, String? persona)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if d == null → return 'רכיב לא מוכר במרשם — חסום מטעמי בטיחות (fail-closed)'
  - if d.kImmutable → return 'רכיב נעול (ניווט/הזדהות) — «${d.labelHe}» אינו ניתן לעריכה'
- **null when:** no result
- **throws:** _none_
