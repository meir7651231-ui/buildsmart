# MaterialRequest.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `material_requests_engine` · `lib/state/material_requests_engine.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> MaterialRequest?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kMatReqRequested` · `kMatReqStatuses` · `note` · `username`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — id = raw['id']
- **compute** — employerId = raw['employerId']
- **compute** — workerUid = raw['workerUid']
- **compute** — workerName = raw['workerName']
- **compute** — createdTs = raw['createdTs']
- **precond** — if id is! String || employerId is! String || workerUid is! String || workerName is! String |… → return null
- **compute** — rawItems = raw['items']
- **compute** — items = rawItems is List ? [for (final e in rawItems) if (e is String) e] : const <String>[]
- **compute** — status = raw['status']
- **return** — MaterialRequest(id: id, employerId: employerId, workerUid: workerUid, username: raw['user…

## floor
- `MaterialRequest` · `contains`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `MaterialRequest?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if id is! String || employerId is! String || workerUid is! String || workerName is! String |… → return null
- **null when:** raw is! Map · id is! String || employerId is! String || workerUid is! String || workerName is! String |… · or search exhausted
- **throws:** _none_
