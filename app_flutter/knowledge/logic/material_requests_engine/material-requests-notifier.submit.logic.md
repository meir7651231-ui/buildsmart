# MaterialRequestsNotifier.submit — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `material_requests_engine` · `lib/state/material_requests_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({required String employerId, required String workerUid, required String username, required String workerName, required List<String> items, String note = ''}) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kMatReqRequested` · `kMaterialRequestsKey↝` · `_remote` · `_seq`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — remote = _remote
- **branch** — if remote != null
- **effect** — _userTouched = true
- **effect** — _seq++
- **init** — cleaned = [for (final raw in items) if (raw.trim().isNotEmpty) raw.trim()]
- **compute** — id = 'mat-${DateTime.now().microsecondsSinceEpoch}-$_seq'
- **compute** — r = MaterialRequest(id: id, employerId: employerId, workerUid: workerUid, username: username,…
- **effect** — state = [...state, r]
- **effect** — _persist()
- **return** — id

## floor
- `MaterialRequest` · `now` · `submit` · `trim`

## חוזה · contract
- **input:** `({required String employerId, required String workerUid, required String username, required String workerName, required List<String> items, String note = ''})`
- **output:** `String`
- **purity:** side-effecting
- **throws:** _none_
