# MaterialRequestsNotifier.setStatus — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `material_requests_engine` · `lib/state/material_requests_engine.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id, String status) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kMatReqDeclined↝` · `kMatReqStatuses` · `kMatReqSupplied↝` · `kMaterialRequestsKey↝` · `_remote`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `_isTerminal` · `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !kMatReqStatuses.contains(status) → return (void)
- **compute** — remote = _remote
- **branch** — if remote != null
- **effect** — _userTouched = true
- **effect** — state = [for (final r in state) if (r.id == id && !_isTerminal(r.status)) r.copyWith(stat…
- **effect** — _persist()

## floor
- `contains` · `copyWith` · `setStatus`

## חוזה · contract
- **input:** `(String id, String status)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if !kMatReqStatuses.contains(status) → return (void)
- **throws:** _none_
