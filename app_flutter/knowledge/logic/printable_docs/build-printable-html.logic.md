# buildPrintableHtml — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `printable_docs` · `/home/user/buildsmart/app_flutter/lib/logic/printable_docs.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `({required String title, required List<({String label, String value})> rows, String? signatureDataUrl, String? declaration}) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_esc`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — b = StringBuffer()
- **compute** — safeTitle = _esc(title)
- **compute** — rowsHtml = StringBuffer()
- **loop** — for final r in rows
- **branch** — if r.value.isEmpty → continue
- **effect** — rowsHtml..write('<tr><th>')..write(_esc(r.label))..write('</th><td>')..write(_esc(r.value…
- **effect** — b.write('<!doctype html>')
- **effect** — b.write('<html dir="rtl" lang="he"><head><meta charset="utf-8">')
- **effect** — b.write('<meta name="viewport" content="width=device-width, ' 'initial-scale=1">')
- **effect** — b.write('<title>')
- **effect** — b.write(safeTitle)
- **effect** — b.write('</title><style>')
- **effect** — b.write('@page{size:A4;margin:18mm}')
- **effect** — b.write('*{box-sizing:border-box}')
- **effect** — b.write('body{direction:rtl;text-align:right;' 'font-family:Arial,"Segoe UI",sans-serif;c…
- **effect** — b.write('h1{font-size:22px;margin:0 0 16px;' 'border-bottom:2px solid #1a1a1a;padding-bot…
- **effect** — b.write('table{width:100%;border-collapse:collapse;margin:0 0 16px}')
- **effect** — b.write('th,td{border:1px solid #ccc;padding:8px 10px;' 'text-align:right;vertical-align:…
- **effect** — b.write('th{background:#f3f3f3;width:38%;font-weight:600}')
- **effect** — b.write('.declaration{margin:16px 0;padding:10px 12px;' 'background:#f7f7f7;border-radius…
- **effect** — b.write('.signature{margin:16px 0}')
- **effect** — b.write('.signature .label{font-weight:600;margin-bottom:6px}')
- **effect** — b.write('.signature img{max-width:280px;height:auto;' 'border:1px solid #ccc;border-radiu…
- **effect** — b.write('footer{margin-top:24px;padding-top:8px;' 'border-top:1px solid #ccc;color:#777;f…
- **effect** — b.write('</style></head><body>')
- **effect** — b.write('<h1>')
- **effect** — b.write(safeTitle)
- **effect** — b.write('</h1>')
- **effect** — b.write('<table>')
- **effect** — b.write(rowsHtml.toString())
- **effect** — b.write('</table>')
- **branch** — if declaration != null && declaration.isNotEmpty
- **branch** — if signatureDataUrl != null
- **effect** — b.write('<footer>מסמך דיגיטלי — ${AppBrand.name} · ')
- **effect** — b.write(safeTitle)
- **effect** — b.write('</footer>')
- **effect** — b.write('</body></html>')
- **return** — b.toString()

## floor
- `StringBuffer` · `toString` · `write`

## חוזה · contract
- **input:** `({required String title, required List<({String label, String value})> rows, String? signatureDataUrl, String? declaration})`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
