# AbExperimentsNotifier.ensure — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `ab_experiments` · `lib/state/ab_experiments.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String experiment, List<String> variants) -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_key↝`
- **writes:** `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **branch** — if variants.isEmpty
- **compute** — existing = state[experiment]
- **precond** — if existing != null → return existing
- **compute** — idx = experiment.hashCode.abs() % variants.length
- **compute** — pick = variants[idx]
- **effect** — state = {...state, experiment : pick}
- **effect** — _persist()
- **return** — pick

## floor
- `abs` · `value`

## חוזה · contract
- **input:** `(String experiment, List<String> variants)`
- **output:** `String`
- **purity:** side-effecting
- **precond:**
  - if existing != null → return existing
- **throws:** `ArgumentError.value(variants, 'variants', 'must contain at least one variant')`
