# fuzzyMatch — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `fuzzy_match` · `/home/user/buildsmart/app_flutter/lib/logic/fuzzy_match.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String query, String candidate) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `filled↝`
- **writes:** _none_
- **calls:** `damerauLevenshtein` · `fuzzyTolerance`
- **called-by:** `fuzzyNameMatch`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — q = normSearch(query)
- **compute** — c = normSearch(candidate)
- **precond** — if q.isEmpty || c.isEmpty → return false
- **precond** — if c.contains(q) → return true
- **return** — damerauLevenshtein(q, c) <= fuzzyTolerance(q.length)

## floor
- `contains` · `normSearch`

## חוזה · contract
- **input:** `(String query, String candidate)`
- **output:** `bool`
- **purity:** pure
- **precond:**
  - if q.isEmpty || c.isEmpty → return false
  - if c.contains(q) → return true
- **throws:** _none_
