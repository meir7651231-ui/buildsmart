# damerauLevenshtein — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `fuzzy_match` · `/home/user/buildsmart/app_flutter/lib/logic/fuzzy_match.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String a, String b) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** `filled`
- **writes:** _none_
- **calls:** `_min3`
- **called-by:** `fuzzyMatch` · `fuzzyScore`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if a == b → return 0
- **precond** — if a.isEmpty → return b.length
- **precond** — if b.isEmpty → return a.length
- **compute** — n = a.length
- **compute** — m = b.length
- **init** — prev2 = List<int>.filled(m + 1, 0)
- **init** — prev = List<int>.generate(m + 1, (j) => j)
- **init** — cur = List<int>.filled(m + 1, 0)
- **loop** — for var i = 1; i <= n; i++
- **effect** — cur[0] = i
- **loop** — for var j = 1; j <= m; j++
- **compute** — tmp = prev2
- **effect** — prev2 = prev
- **effect** — prev = cur
- **effect** — cur = tmp
- **return** — prev[m]

## floor
- `List` · `codeUnitAt`

## חוזה · contract
- **input:** `(String a, String b)`
- **output:** `int`
- **purity:** pure
- **precond:**
  - if a == b → return 0
  - if a.isEmpty → return b.length
  - if b.isEmpty → return a.length
- **throws:** _none_
