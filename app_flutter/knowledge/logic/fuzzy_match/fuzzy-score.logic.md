# fuzzyScore — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `fuzzy_match` · `/home/user/buildsmart/app_flutter/lib/logic/fuzzy_match.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String query, String candidate) -> int`
- **constants:** _none_

## חיבורים · connections
- **reads:** `filled↝`
- **writes:** _none_
- **calls:** `damerauLevenshtein` · `fuzzyTolerance`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — q = normSearch(query)
- **compute** — c = normSearch(candidate)
- **precond** — if q.isEmpty || c.isEmpty → return -1
- **precond** — if c.contains(q) → return 0
- **compute** — d = damerauLevenshtein(q, c)
- **return** — d <= fuzzyTolerance(q.length) ? d : -1

## floor
- `contains` · `normSearch`

## חוזה · contract
- **input:** `(String query, String candidate)`
- **output:** `int`
- **purity:** pure
- **precond:**
  - if q.isEmpty || c.isEmpty → return -1
  - if c.contains(q) → return 0
- **throws:** _none_
