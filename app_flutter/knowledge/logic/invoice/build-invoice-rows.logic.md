# buildInvoiceRows — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `invoice` · `/home/user/buildsmart/app_flutter/lib/logic/invoice.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Order order) -> List<({String label, String value})>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVatRate`
- **writes:** _none_
- **calls:** `invoiceVatOf`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — rows = <({String label, String value})>[(label: 'מספר הזמנה', value: order.id), (label: 'לקוח', …
- **branch** — if order.site.isNotEmpty
- **loop** — for final l in order.lines
- **compute** — head = l.emoji.isEmpty ? l.name : '${l.emoji} ${l.name}'
- **effect** — rows.add((label: '$head × ${l.qty}', value: formatNis(l.price)))
- **effect** — rows.add((label: 'סה"כ לתשלום (כולל מע"מ)', value: formatNis(order.sum)))
- **effect** — rows.add((label: 'מזה מע"מ ${(kVatRate * 100).round()}%', value: formatNis(invoiceVatOf(o…
- **return** — rows

## floor
- `add` · `formatNis` · `round`

## חוזה · contract
- **input:** `(Order order)`
- **output:** `List<({String label, String value})>`
- **purity:** pure
- **throws:** _none_
