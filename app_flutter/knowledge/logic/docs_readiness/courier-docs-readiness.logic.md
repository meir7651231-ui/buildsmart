# courierDocsReadiness — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `docs_readiness` · `../../../app_flutter/lib/state/docs_readiness.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `({required String username, required List<WorkerCert> certs, required DateTime now}) -> DocsReadiness`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kCourierCertPresets`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — mine = certs.where((c) => c.username == username).toList()
- **init** — missing = <String>[]
- **init** — expiring = <String>[]
- **compute** — presentNames = mine.map((c) => c.name).toSet()
- **loop** — for final required in kCourierCertPresets
- **branch** — if !presentNames.contains(required)
- **loop** — for final c in mine
- **branch** — switch c.statusAt(now)
- **return** — DocsReadiness(ready: missing.isEmpty, missing: missing, expiring: expiring)

## floor
- `DocsReadiness` · `add` · `contains` · `map` · `statusAt` · `toList` · `toSet` · `where`

## חוזה · contract
- **input:** `({required String username, required List<WorkerCert> certs, required DateTime now})`
- **output:** `DocsReadiness`
- **purity:** pure
- **throws:** _none_
