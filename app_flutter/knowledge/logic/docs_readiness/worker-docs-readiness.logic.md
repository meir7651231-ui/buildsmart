# workerDocsReadiness — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `docs_readiness` · `../../../app_flutter/lib/state/docs_readiness.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `({required String username, required WorkerFormsState forms, required List<WorkerCert> certs, required DateTime now, List<String> requiredCertNames = const []}) -> DocsReadiness`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — missing = <String>[]
- **init** — expiring = <String>[]
- **compute** — form = forms.form101For(username, now.year)
- **compute** — hasSigned101 = form != null && form.signature.isNotEmpty && form.declared
- **branch** — if !hasSigned101
- **loop** — for final c in certs
- **branch** — if c.username != username → continue
- **branch** — switch c.statusAt(now)
- **loop** — for final req in requiredCertNames
- **compute** — key = normalizeDocName(req)
- **compute** — satisfied = certs.any((c) => c.username == username && normalizeDocName(c.name) == key && c.statusAt(…
- **branch** — if !satisfied
- **return** — DocsReadiness(ready: missing.isEmpty, missing: missing, expiring: expiring)

## floor
- `DocsReadiness` · `add` · `any` · `form101For` · `normalizeDocName` · `statusAt`

## חוזה · contract
- **input:** `({required String username, required WorkerFormsState forms, required List<WorkerCert> certs, required DateTime now, List<String> requiredCertNames = const []})`
- **output:** `DocsReadiness`
- **purity:** pure
- **throws:** _none_
