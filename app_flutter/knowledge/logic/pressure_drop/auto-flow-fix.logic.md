# autoFlowFix — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `pressure_drop` · `/home/user/buildsmart/app_flutter/lib/logic/pressure_drop.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> chain, {double pipeLengthMeters = 5.0, double flowRateLPS = 0.3, double verticalRiseMeters = 0.0}) -> ({List<LipskeyCatalogProduct> chain, List<String> changes})`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kOffLineSkus↝` · `kVerifiedSpecs↝` · `chainUniverse`
- **writes:** _none_
- **calls:** `estimatePressureDrop` · `widerSiblingOf` · `_swapMatesWithNeighbours`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if chain.length < 2 → return (chain: chain, changes: const [])
- **init** — changes = <String>[]
- **init** — working = [...chain]
- **loop** — for var safety = 0; safety < 5; safety++
- **compute** — pd = estimatePressureDrop(working, pipeLengthMeters: pipeLengthMeters, flowRateLPS: flowRateLP…
- **branch** — if pd.bottleneck == null → break
- **branch** — if pd.minBoreMm >= 13 && (pd.dropBar <= 1.0) → break
- **compute** — bottleneck = pd.bottleneck!
- **compute** — idx = working.indexWhere((p) => p.sku == bottleneck.sku)
- **branch** — if idx < 0 → break
- **compute** — wider = widerSiblingOf(bottleneck)
- **branch** — if wider == null → break
- **branch** — if !_swapMatesWithNeighbours(working, idx, wider) → break
- **effect** — working[idx] = wider
- **effect** — changes.add('🔄 הוחלף "${bottleneck.nameHe}" ב-"${wider.nameHe}" לפתיחת צוואר-בקבוק')
- **compute** — pd2 = estimatePressureDrop(working, pipeLengthMeters: pipeLengthMeters, flowRateLPS: flowRateLP…
- **branch** — if pd2.dropBar > 1.0
- **return** — (chain: working, changes: changes)

## floor
- `add` · `any` · `indexWhere` · `toList` · `toStringAsFixed` · `where`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> chain, {double pipeLengthMeters = 5.0, double flowRateLPS = 0.3, double verticalRiseMeters = 0.0})`
- **output:** `({List<LipskeyCatalogProduct> chain, List<String> changes})`
- **purity:** pure
- **precond:**
  - if chain.length < 2 → return (chain: chain, changes: const [])
- **throws:** _none_
