# estimatePressureDrop — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `pressure_drop` · `/home/user/buildsmart/app_flutter/lib/logic/pressure_drop.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> chain, {double pipeLengthMeters = 5.0, double flowRateLPS = 0.3, double verticalRiseMeters = 0.0}) -> PressureDropResult`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kOffLineSkus` · `kVerifiedSpecs↝`
- **writes:** _none_
- **calls:** `_kForType` · `_minBoreOf` · `_frictionFactor` · `widerSiblingOf`
- **called-by:** `autoFlowFix`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — totalK = 0.0
- **loop** — for final p in chain
- **branch** — if _kOffLineSkus.contains(p.sku) → continue
- **effect** — totalK += _kForType(p.productType)
- **compute** — minBore = 
- **compute** — bottleneck = 
- **loop** — for final p in chain
- **branch** — if _kOffLineSkus.contains(p.sku) → continue
- **compute** — b = _minBoreOf(p)
- **branch** — if b == null → continue
- **branch** — if minBore == null || b < minBore
- **effect** — minBore ??= 0.020
- **compute** — rho = 1000.0
- **compute** — mu = 0.001
- **compute** — g = 9.81
- **compute** — area = 3.14159265 * minBore * minBore / 4.0
- **compute** — q = flowRateLPS / 1000.0
- **compute** — v = q / area
- **compute** — reynolds = rho * v * minBore / mu
- **compute** — f = _frictionFactor(reynolds)
- **compute** — frictionTerm = f * pipeLengthMeters / minBore
- **compute** — dynamicPa = (totalK + frictionTerm) * (rho * v * v / 2.0)
- **compute** — staticPa = rho * g * verticalRiseMeters
- **compute** — dropPa = dynamicPa + staticPa
- **compute** — dropBar = dropPa / 1e5
- **init** — suggestions = <FlowSuggestion>[]
- **compute** — wider = bottleneck == null ? null : widerSiblingOf(bottleneck)
- **branch** — if minBore * 1000 < 13 && flowRateLPS >= 0.3
- **branch** — if dropBar > 1.0
- **branch** — if verticalRiseMeters >= 10
- **branch** — if reynolds < 2300 && flowRateLPS >= 0.2 && bottleneck != null
- **branch** — if suggestions.isEmpty
- **return** — PressureDropResult(dropBar: dropBar, totalK: totalK, frictionMetres: pipeLengthMeters, mi…

## floor
- `FlowSuggestion` · `PressureDropResult` · `add` · `contains` · `toStringAsFixed`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> chain, {double pipeLengthMeters = 5.0, double flowRateLPS = 0.3, double verticalRiseMeters = 0.0})`
- **output:** `PressureDropResult`
- **purity:** pure
- **throws:** _none_
