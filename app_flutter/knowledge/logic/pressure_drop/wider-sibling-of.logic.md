# widerSiblingOf — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `pressure_drop` · `/home/user/buildsmart/app_flutter/lib/logic/pressure_drop.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(LipskeyCatalogProduct p) -> LipskeyCatalogProduct?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kBspInchToMm↝` · `kVerifiedSpecs`
- **writes:** _none_
- **calls:** `_boreMeters`
- **called-by:** `autoFlowFix` · `estimatePressureDrop`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — spec = kVerifiedSpecs[p.sku]
- **precond** — if spec == null → return null
- **compute** — myMin = 
- **loop** — for final e in spec.ends
- **compute** — b = _boreMeters(e)
- **branch** — if b == null → continue
- **branch** — if myMin == null || b < myMin
- **precond** — if myMin == null → return null
- **compute** — best = 
- **compute** — bestBore = 
- **loop** — for final q in catalogRepo().allProducts()
- **branch** — if q.sku == p.sku → continue
- **branch** — if q.productType != p.productType → continue
- **branch** — if q.brand != p.brand → continue
- **branch** — if q.categoryHe != p.categoryHe → continue
- **compute** — qSpec = kVerifiedSpecs[q.sku]
- **branch** — if qSpec == null → continue
- **compute** — qMin = 
- **loop** — for final e in qSpec.ends
- **branch** — if qMin == null → continue
- **branch** — if qMin <= myMin → continue
- **branch** — if bestBore == null || qMin < bestBore
- **return** — best

## floor
- `allProducts` · `catalogRepo`

## חוזה · contract
- **input:** `(LipskeyCatalogProduct p)`
- **output:** `LipskeyCatalogProduct?`
- **purity:** pure
- **precond:**
  - if spec == null → return null
  - if myMin == null → return null
- **null when:** spec == null · myMin == null · or search exhausted
- **throws:** _none_
