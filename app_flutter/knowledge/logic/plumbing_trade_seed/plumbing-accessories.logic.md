# plumbingAccessories — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `plumbing_trade_seed` · `lib/domain/seeds/plumbing_trade_seed.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> List<AccessoryRule>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPlumbingTradeId` · `kSmartProducts` · `kUncategorizedCategoryId` · `_smartKeyToIdCache↝`
- **writes:** _none_
- **calls:** `_smartKeyToId`
- **called-by:** `buildPlumbingSeed`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — smartKeyMap = _smartKeyToId()
- **init** — out = <AccessoryRule>[]
- **loop** — for final sp in kSmartProducts
- **loop** — for var i = 0; i < sp.acc.length; i++
- **effect** — out.sort((a, b) => a.id.compareTo(b.id))
- **return** — out

## floor
- `AccessoryRule` · `add` · `compareTo` · `sort`

## חוזה · contract
- **input:** `()`
- **output:** `List<AccessoryRule>`
- **purity:** pure
- **throws:** _none_
