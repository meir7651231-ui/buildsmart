# plumbingConnectorTypes — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `plumbing_trade_seed` · `lib/domain/seeds/plumbing_trade_seed.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> List<ConnectorType>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPlumbingTradeId` · `kVerifiedSpecs`
- **writes:** _none_
- **calls:** `_connTypeId` · `_systemId` · `_systemOfEndType`
- **called-by:** `buildPlumbingSeed`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — nameHe = <EndType, String>{EndType.hdpeCompression : 'הידוק HDPE', EndType.pexPress : 'PEX פרס', E…
- **init** — sizesByType = <EndType, Set<String>>{for (final e in EndType.values) e : <String>{}}
- **loop** — for final spec in kVerifiedSpecs.values
- **loop** — for final end in spec.ends
- **return** — <ConnectorType>[for (final e in EndType.values) ConnectorType(id: _connTypeId(e), tradeId…

## floor
- `ConnectorType` · `add` · `compareTo` · `sort` · `toList`

## חוזה · contract
- **input:** `()`
- **output:** `List<ConnectorType>`
- **purity:** pure
- **throws:** _none_
