# plumbingCategories — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `plumbing_trade_seed` · `lib/domain/seeds/plumbing_trade_seed.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> List<TradeCategory>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kCatalogTree` · `kPlumbingTradeId` · `kUncategorizedCategoryId`
- **writes:** _none_
- **calls:** `_categoryId`
- **called-by:** `buildPlumbingSeed`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — out = <TradeCategory>[]
- **loop** — for var i = 0; i < kCatalogTree.length; i++
- **effect** — walk(kCatalogTree[i], null, i)
- **effect** — out.add(TradeCategory(id: kUncategorizedCategoryId, tradeId: kPlumbingTradeId, titleHe: '…
- **effect** — out.sort((a, b) => a.id.compareTo(b.id))
- **return** — out

## floor
- `TradeCategory` · `add` · `compareTo` · `sort` · `walk`

## חוזה · contract
- **input:** `()`
- **output:** `List<TradeCategory>`
- **purity:** pure
- **throws:** _none_
