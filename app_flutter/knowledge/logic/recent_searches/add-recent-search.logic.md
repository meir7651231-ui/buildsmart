# addRecentSearch — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `recent_searches` · `lib/state/recent_searches.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<String> current, String query, {int max = kMaxRecentSearches}) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `from`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `RecentSearchesNotifier.add`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — q = query.trim()
- **precond** — if q.isEmpty → return List<String>.from(current)
- **compute** — list = List<String>.from(current)..remove(q)..insert(0, q)
- **branch** — if list.length > max
- **return** — list

## floor
- `List` · `insert` · `remove` · `removeRange` · `trim`

## חוזה · contract
- **input:** `(List<String> current, String query, {int max = kMaxRecentSearches})`
- **output:** `List<String>`
- **purity:** pure
- **precond:**
  - if q.isEmpty → return List<String>.from(current)
- **throws:** _none_
