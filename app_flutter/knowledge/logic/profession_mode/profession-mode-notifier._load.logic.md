# ProfessionModeNotifier._load — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `profession_mode` · `/home/user/buildsmart/app_flutter/lib/state/profession_mode.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> Future<void>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kUserProfileKey` · `_key`
- **writes:** `io:prefs.setString` · `state:state`
- **calls:** `professionModeFromTrade` · `defaultDetailFor`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — prefs = await SharedPreferences.getInstance()
- **compute** — s = prefs.getString(_key)
- **branch** — if s != null
- **compute** — profileRaw = prefs.getString(kUserProfileKey)
- **precond** — if profileRaw == null → return (void)

## floor
- `fromJson` · `getInstance` · `getString` · `jsonDecode` · `setString`

## חוזה · contract
- **input:** `()`
- **output:** `Future<void>`
- **purity:** side-effecting
- **precond:**
  - if profileRaw == null → return (void)
- **throws:** _none_
