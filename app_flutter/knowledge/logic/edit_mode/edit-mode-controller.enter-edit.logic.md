# EditModeController.enterEdit — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `edit_mode` · `lib/state/studio/edit_mode.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_ref↝` · `studioOwnerEmailProvider↝`
- **writes:** `state:_audit↝` · `state:state`
- **calls:** `_log`
- **called-by:** `EditModeController.toggleEdit`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !_gateOpen → return (void)
- **precond** — if state.isEditing → return (void)
- **effect** — state = state.copyWith(isEditing: true, previewDraft: true)
- **effect** — _log('enter')

## floor
- `copyWith`

## חוזה · contract
- **input:** `()`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if !_gateOpen → return (void)
  - if state.isEditing → return (void)
- **throws:** _none_
