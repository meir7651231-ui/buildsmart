# crossesSystem — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `hop_graph` · `lib/features/card_keyboard/hop_graph.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String aSku, String bSku) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kVerifiedSpecs`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_addCategoryEdges` · `_addCompatEdges` · `_addKitEdges`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — a = kVerifiedSpecs[aSku]?.endSystems
- **compute** — b = kVerifiedSpecs[bSku]?.endSystems
- **precond** — if a == null || b == null → return false
- **return** — (only(a, WaterSystem.supply) && only(b, WaterSystem.drainage)) || (only(a, WaterSystem.dr…

## floor
- `contains` · `only`

## חוזה · contract
- **input:** `(String aSku, String bSku)`
- **output:** `bool`
- **purity:** pure
- **precond:**
  - if a == null || b == null → return false
- **throws:** _none_
