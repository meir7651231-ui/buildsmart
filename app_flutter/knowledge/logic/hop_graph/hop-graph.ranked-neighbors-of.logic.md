# HopGraph.rankedNeighborsOf — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `hop_graph` · `lib/features/card_keyboard/hop_graph.dart`.

## עצם · object
- **kind:** method
- **signature:** `(String sku) -> List<String>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kEdgeKindRank` · `_adj`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — neighbours = _adj[sku]
- **precond** — if neighbours == null → return const <String>[]
- **compute** — entries = neighbours.entries.toList()..sort((a, b) {final ra = best(a.value); final rb = best(b.val…
- **return** — [for (final e in entries) e.key]

## floor
- `best` · `compareTo` · `map` · `reduce` · `sort` · `toList`

## חוזה · contract
- **input:** `(String sku)`
- **output:** `List<String>`
- **purity:** pure
- **precond:**
  - if neighbours == null → return const <String>[]
- **throws:** _none_
