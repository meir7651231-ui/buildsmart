# _addHubEdges — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `hop_graph` · `lib/features/card_keyboard/hop_graph.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Map<String, Map<String, Set<EdgeKind>>> adj) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `divePoolBySku`
- **writes:** _none_
- **calls:** _none_
- **called-by:** `_buildPopulated`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — repOf = <String, String>{}
- **loop** — for final sku in adj.keys
- **effect** — repOf.putIfAbsent(groupOf(divePoolBySku[sku]!), () => sku)
- **loop** — for final sku in adj.keys
- **effect** — biLink(sku, repOf[groupOf(divePoolBySku[sku]!)]!)
- **compute** — reps = repOf.values.toList()
- **loop** — for var i = 0; i < reps.length; i++
- **loop** — for var j = i + 1; j < reps.length; j++

## floor
- `add` · `biLink` · `groupOf` · `putIfAbsent` · `toList`

## חוזה · contract
- **input:** `(Map<String, Map<String, Set<EdgeKind>>> adj)`
- **output:** `void`
- **purity:** pure
- **throws:** _none_
