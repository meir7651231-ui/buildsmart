# _huliotSpecFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `huliot_smartlock_catalog` · `lib/data/huliot_smartlock_catalog.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(int page, String nameHe, String categoryHe) -> String?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_huliotImageForCrop`
- **called-by:** `_sl`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — _specCropDisabled = false
- **precond** — if _specCropDisabled → return null
- **compute** — img = _huliotImageForCrop(page, nameHe, categoryHe)
- **precond** — if img == null || img.startsWith('page_') → return null
- **precond** — if img.startsWith('sml_p27_') → return null
- **precond** — if img.startsWith('sml_p24_') → return null
- **precond** — if img == 'sml_p40_c.jpg' || img == 'sml_p41_c.jpg' → return null
- **init** — _missingSpecs = {'sml_p11_b.jpg', 'sml_p30_d.jpg', 'sml_p32_a.jpg', 'sml_p36_b.jpg', 'sml_p38_a.jpg', 'sm…
- **precond** — if _missingSpecs.contains(img) → return null
- **return** — 'spec_$img'

## floor
- `contains` · `startsWith`

## חוזה · contract
- **input:** `(int page, String nameHe, String categoryHe)`
- **output:** `String?`
- **purity:** pure
- **precond:**
  - if _specCropDisabled → return null
  - if img == null || img.startsWith('page_') → return null
  - if img.startsWith('sml_p27_') → return null
  - if img.startsWith('sml_p24_') → return null
  - if img == 'sml_p40_c.jpg' || img == 'sml_p41_c.jpg' → return null
  - if _missingSpecs.contains(img) → return null
- **null when:** _specCropDisabled · img == null || img.startsWith('page_') · img.startsWith('sml_p27_') · img.startsWith('sml_p24_') · img == 'sml_p40_c.jpg' || img == 'sml_p41_c.jpg' · _missingSpecs.contains(img) · or search exhausted
- **throws:** _none_
