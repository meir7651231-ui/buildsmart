# validBusinessId — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `input_validators` · `/home/user/buildsmart/app_flutter/lib/logic/input_validators.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String input) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — digits = input.replaceAll(RegExp(r'[\s-]'), '')
- **precond** — if !RegExp(r'^\d{9}$').hasMatch(digits) → return false
- **compute** — sum = 0
- **loop** — for var i = 0; i < 9; i++
- **compute** — product = (digits.codeUnitAt(i) - 0x30) * (i.isEven ? 1 : 2)
- **effect** — sum += product > 9 ? product - 9 : product
- **return** — sum % 10 == 0

## floor
- `RegExp` · `codeUnitAt` · `hasMatch` · `replaceAll`

## חוזה · contract
- **input:** `(String input)`
- **output:** `bool`
- **purity:** pure
- **precond:**
  - if !RegExp(r'^\d{9}$').hasMatch(digits) → return false
- **throws:** _none_
