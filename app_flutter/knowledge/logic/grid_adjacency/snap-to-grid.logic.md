# snapToGrid — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `grid_adjacency` · `../../../app_flutter/lib/features/fittings/engine/grid_adjacency.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Vec3 dir, {double tol = 1e-6}) -> GridStep?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `gridPortsOf` · `portsFace`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **loop** — for final s in GridStep.values
- **branch** — if (dir.x - s.dx).abs() < tol && (dir.y - s.dy).abs() < tol && (dir.z - s.dz).abs() < tol → return s
- **return** — null

## floor
- `abs`

## חוזה · contract
- **input:** `(Vec3 dir, {double tol = 1e-6})`
- **output:** `GridStep?`
- **purity:** pure
- **null when:** no result
- **throws:** _none_
