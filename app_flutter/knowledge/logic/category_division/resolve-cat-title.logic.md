# resolveCatTitle — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `category_division` · `/home/user/buildsmart/app_flutter/lib/logic/category_division.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String title) -> CatalogNode?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kCatalogTree`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — hit = 
- **loop** — for final t in kCatalogTree
- **effect** — walk(t)
- **precond** — if hit != null → return hit
- **precond** — if catalogRepo().allProducts().any((p) => p.categoryHe == title) → return CatalogNode(id: 'catdept.$title', title: title, emoji: '📦', lipskeyCategory: title)
- **return** — null

## floor
- `CatalogNode` · `allProducts` · `any` · `catalogRepo` · `walk`

## חוזה · contract
- **input:** `(String title)`
- **output:** `CatalogNode?`
- **purity:** pure
- **precond:**
  - if hit != null → return hit
  - if catalogRepo().allProducts().any((p) => p.categoryHe == title) → return CatalogNode(id: 'catdept.$title', title: title, emoji: '📦', lipskeyCategory: title)
- **null when:** no result
- **throws:** _none_
