# SessionTracker._endSession — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `session_tracker` · `lib/state/intel/session_tracker.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String reason) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_clock` · `_ref` · `_startedAt` · `intelBusProvider`
- **writes:** `field:_idleTimer` · `field:_presence` · `state:state`
- **calls:** _none_
- **called-by:** `SessionTracker._armIdleTimer` · `SessionTracker._onUid` · `SessionTracker.didChangeAppLifecycleState`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if state != SessionPhase.active → return (void)
- **compute** — startedAt = _startedAt
- **compute** — durS = startedAt == null ? 0 : _clock().difference(startedAt).inSeconds
- **effect** — _ref.read(intelBusProvider).track(IntelEvents.sessionEnd, props: <String, String>{'reason…
- **effect** — state = SessionPhase.ended
- **effect** — _idleTimer?.cancel()
- **effect** — _idleTimer = null
- **compute** — presence = _presence
- **effect** — _presence = null
- **effect** — unawaited(presence?.stop())

## floor
- `_clock` · `cancel` · `difference` · `read` · `stop` · `track` · `unawaited`

## חוזה · contract
- **input:** `(String reason)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if state != SessionPhase.active → return (void)
- **throws:** _none_
