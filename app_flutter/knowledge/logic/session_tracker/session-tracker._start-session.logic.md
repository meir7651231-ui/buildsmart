# SessionTracker._startSession — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `session_tracker` · `lib/state/intel/session_tracker.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `() -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kIdleEnd↝` · `_armIdle↝` · `_clock` · `_ref` · `actorKeyProvider↝` · `appSettingsProvider↝` · `currentUidProvider↝` · `intelBusProvider` · `mainTabProvider↝` · `presenceWriterProvider↝` · `roleProvider` · `sessionIdProvider`
- **writes:** `field:_idleTimer↝` · `field:_presence↝` · `field:_sessionId` · `field:_startedAt` · `state:state`
- **calls:** `_mintSessionId` · `_armIdleTimer` · `_maybeStartPresence`
- **called-by:** `SessionTracker.didChangeAppLifecycleState`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if state == SessionPhase.active → return (void)
- **effect** — _sessionId = _mintSessionId()
- **effect** — _startedAt = _clock()
- **effect** — _ref.read(sessionIdProvider.notifier).state = _sessionId
- **effect** — state = SessionPhase.active
- **compute** — role = _ref.read(roleProvider)
- **effect** — _ref.read(intelBusProvider).track(IntelEvents.sessionStart, props: <String, String>{'acto…
- **effect** — _armIdleTimer()
- **effect** — _maybeStartPresence()

## floor
- `_clock` · `read` · `track`

## חוזה · contract
- **input:** `()`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if state == SessionPhase.active → return (void)
- **throws:** _none_
