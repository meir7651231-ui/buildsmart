# RecentlyViewedNotifier.touch — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `recently_viewed` · `lib/state/recently_viewed.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String? sku) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** `cap`
- **writes:** `field:_userTouched` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if sku == null || sku.isEmpty → return (void)
- **effect** — _userTouched = true
- **init** — next = [sku, ...state.where((s) => s != sku)]
- **effect** — state = List.unmodifiable(next.length > cap ? next.sublist(0, cap) : next)
- **effect** — _persist()

## floor
- `sublist` · `unmodifiable` · `where`

## חוזה · contract
- **input:** `(String? sku)`
- **output:** `void`
- **purity:** side-effecting
- **precond:**
  - if sku == null || sku.isEmpty → return (void)
- **throws:** _none_
