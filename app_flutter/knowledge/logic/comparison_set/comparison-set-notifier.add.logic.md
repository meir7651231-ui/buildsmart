# ComparisonSetNotifier.add — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `comparison_set` · `lib/state/comparison_set.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String productKey) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_key↝` · `maxItems`
- **writes:** `state:state`
- **calls:** `_persist`
- **called-by:** `ComparisonSetNotifier.toggle`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if state.contains(productKey) → return true
- **precond** — if state.length >= maxItems → return false
- **effect** — state = {...state, productKey}
- **effect** — _persist()
- **return** — true

## floor
- `contains`

## חוזה · contract
- **input:** `(String productKey)`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if state.contains(productKey) → return true
  - if state.length >= maxItems → return false
- **throws:** _none_
