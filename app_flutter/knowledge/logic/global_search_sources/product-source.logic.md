# productSource — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `global_search_sources` · `lib/features/global_search/global_search_sources.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(String query, int max) -> List<SearchResult>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kDivePool`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **init** — hits = <SearchResult>[]
- **loop** — for final p in kDivePool
- **compute** — s = scoreMatch(query, p.nameHe)
- **branch** — if s <= 0 → continue
- **effect** — hits.add(SearchResult(kind: SearchResultKind.product, title: p.nameHe, subtitle: p.catego…
- **effect** — hits.sort((a, b) => b.score.compareTo(a.score))
- **return** — hits.length <= max ? hits : hits.sublist(0, max)

## floor
- `SearchResult` · `add` · `compareTo` · `scoreMatch` · `showLipskeyProductSheet` · `sort` · `sublist` · `toList` · `where`

## חוזה · contract
- **input:** `(String query, int max)`
- **output:** `List<SearchResult>`
- **purity:** pure
- **throws:** _none_
