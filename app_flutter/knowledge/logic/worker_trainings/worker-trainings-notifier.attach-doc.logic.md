# WorkerTrainingsNotifier.attachDoc — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_trainings` · `lib/state/worker_trainings.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id, String dataUrl) -> Future<bool>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `debugPersistOverride↝` · `persist↝` · `repo↝` · `storageKey↝` · `mounted`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — before = state
- **precond** — if !before.any((t) => t.id == id) → return false
- **effect** — _userTouched = true
- **effect** — state = [for (final t in before) if (t.id == id) t.copyWith(doc: dataUrl) else t]
- **compute** — ok = await _persist()
- **branch** — if !ok
- **return** — true

## floor
- `any` · `copyWith`

## חוזה · contract
- **input:** `(String id, String dataUrl)`
- **output:** `Future<bool>`
- **purity:** side-effecting
- **precond:**
  - if !before.any((t) => t.id == id) → return false
- **throws:** _none_
