# WorkerTraining.tryFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_trainings` · `lib/state/worker_trainings.dart`.

## עצם · object
- **kind:** method
- **signature:** `(Object? raw) -> WorkerTraining?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kTrainingRecorded` · `by` · `doc` · `employerId` · `status`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — id = raw['id']
- **compute** — username = raw['username']
- **compute** — title = raw['title']
- **compute** — date = DateTime.tryParse('${raw['date']}')
- **precond** — if id is! String || username is! String || title is! String || date == null → return null
- **return** — WorkerTraining(id: id, username: username, title: title, by: raw['by'] is String ? raw['b…

## floor
- `WorkerTraining` · `tryParse`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `WorkerTraining?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if id is! String || username is! String || title is! String || date == null → return null
- **null when:** raw is! Map · id is! String || username is! String || title is! String || date == null · or search exhausted
- **throws:** _none_
