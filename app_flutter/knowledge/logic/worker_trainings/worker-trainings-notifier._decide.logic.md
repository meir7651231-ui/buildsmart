# WorkerTrainingsNotifier._decide — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `worker_trainings` · `lib/state/worker_trainings.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `(String id, String status) -> bool`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kTrainingPending` · `debugPersistOverride↝` · `persist↝` · `storageKey↝`
- **writes:** `field:_userTouched` · `io:prefs.setString↝` · `state:state`
- **calls:** `_persist`
- **called-by:** `WorkerTrainingsNotifier.approve` · `WorkerTrainingsNotifier.reject`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if !state.any((t) => t.id == id && t.status == kTrainingPending) → return false
- **effect** — _userTouched = true
- **effect** — state = [for (final t in state) if (t.id == id && t.status == kTrainingPending) t.copyWit…
- **effect** — _persist()
- **return** — true

## floor
- `any` · `copyWith`

## חוזה · contract
- **input:** `(String id, String status)`
- **output:** `bool`
- **purity:** side-effecting
- **precond:**
  - if !state.any((t) => t.id == id && t.status == kTrainingPending) → return false
- **throws:** _none_
