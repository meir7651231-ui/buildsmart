# plainProductsFor — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `plain_dive` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/plain_dive.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(PlainNode n) -> List<LipskeyCatalogProduct>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_pool`
- **writes:** `cache:_cache`
- **calls:** _none_
- **called-by:** `_allNodes` · `_reaches`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — key = '${n.superCat}|${n.classification}|${n.technical}|${n.categoriesExact?.join(',') ?? ''}'
- **compute** — cached = _cache[key]
- **precond** — if cached != null → return cached
- **branch** — if n.categoriesExact != null
- **compute** — ps = _pool.where((p) => catalogProductMatchesQuery(p, n.technical)).toList()
- **branch** — if RegExp(r'^\d+$').hasMatch(n.technical)
- **branch** — if ps.isEmpty
- **return** — _cache[key] = ps

## floor
- `RegExp` · `catalogProductMatchesQuery` · `contains` · `hasMatch` · `join` · `toList` · `toSet` · `where`

## חוזה · contract
- **input:** `(PlainNode n)`
- **output:** `List<LipskeyCatalogProduct>`
- **purity:** deterministic-side-effecting
- **precond:**
  - if cached != null → return cached
- **throws:** _none_
