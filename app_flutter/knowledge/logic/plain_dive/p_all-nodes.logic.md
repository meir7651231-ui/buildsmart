# _allNodes — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `plain_dive` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/plain_dive.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `() -> List<PlainNode>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `kPlainCoverage` · `kPlainDict` · `_pool`
- **writes:** `cache:_cache↝` · `cache:_treeCache`
- **calls:** `plainProductsFor`
- **called-by:** `plainClassifications` · `plainNodes` · `plainSuperCats`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — cached = _treeCache
- **precond** — if cached != null → return cached
- **init** — curated = <PlainNode>[...kPlainDict, ...kPlainCoverage]
- **init** — reached = <String>{}
- **loop** — for final n in curated
- **loop** — for final p in plainProductsFor(n)
- **init** — leftover = <String>{}
- **loop** — for final p in _pool
- **branch** — if !reached.contains(p.sku)
- **init** — auto = <PlainNode>[for (final cat in leftover.toList()..sort()) PlainNode(superCat: 'עוד', class…
- **return** — _treeCache = <PlainNode>[...curated, ...auto]

## floor
- `PlainNode` · `add` · `contains` · `sort` · `toList`

## חוזה · contract
- **input:** `()`
- **output:** `List<PlainNode>`
- **purity:** deterministic-side-effecting
- **precond:**
  - if cached != null → return cached
- **throws:** _none_
