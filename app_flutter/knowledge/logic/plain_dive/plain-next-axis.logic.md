# plainNextAxis — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `plain_dive` · `/home/user/buildsmart/app_flutter/lib/features/ring_dive/plain_dive.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(List<LipskeyCatalogProduct> products, {Set<String> usedAxes = const <String>{}}) -> ({String axis, List<String> values})?`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_narrowAxes` · `kRdOrder`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if products.length <= 1 → return null
- **loop** — for final axis in _narrowAxes
- **branch** — if usedAxes.contains(axis) → continue
- **branch** — if axis == 'size'
- **init** — vals = <String>{}
- **compute** — everyoneHasIt = true
- **loop** — for final p in products
- **branch** — if vals.length >= 2 && everyoneHasIt
- **return** — null

## floor
- `addAll` · `any` · `compareTo` · `contains` · `every` · `indexOf` · `productHasChip` · `rdAxesOf` · `sizeTokensIn` · `sort` · `toList`

## חוזה · contract
- **input:** `(List<LipskeyCatalogProduct> products, {Set<String> usedAxes = const <String>{}})`
- **output:** `({String axis, List<String> values})?`
- **purity:** pure
- **precond:**
  - if products.length <= 1 → return null
- **null when:** products.length <= 1 · or search exhausted
- **throws:** _none_
