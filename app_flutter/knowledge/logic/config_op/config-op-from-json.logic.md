# configOpFromJson — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `config_op` · `/home/user/buildsmart/app_flutter/lib/logic/studio/config_op.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(Object? raw) -> ConfigOp?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `_strMap`
- **called-by:** `configOpsFromJson`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if raw is! Map → return null
- **compute** — j = raw.map((k, v) => MapEntry(k.toString(), v))
- **compute** — rawId = j['id']
- **precond** — if rawId is! String || rawId.isEmpty → return null
- **compute** — id = rawId
- **branch** — switch j['op']

## floor
- `MapEntry` · `SetAction` · `SetEmoji` · `SetHidden` · `SetOrder` · `SetStyle` · `SetText` · `fromJson` · `map` · `toInt` · `toString`

## חוזה · contract
- **input:** `(Object? raw)`
- **output:** `ConfigOp?`
- **purity:** pure
- **precond:**
  - if raw is! Map → return null
  - if rawId is! String || rawId.isEmpty → return null
- **null when:** raw is! Map · rawId is! String || rawId.isEmpty · or search exhausted
- **throws:** _none_
