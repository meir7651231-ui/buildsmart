# planAddName — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `workflow_engine` · `/home/user/buildsmart/app_flutter/lib/logic/workflow_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(WfCase a, String rawName, int? units, String id, {required String todayIso}) -> ({List<WfName> names, List<WfLog>? log})?`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `wfNormName`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — nm = rawName.trim()
- **precond** — if nm.isEmpty → return null
- **compute** — key = wfNormName(nm)
- **precond** — if a.names.any((x) => wfNormName(x.name) == key) → return null
- **init** — names = [...a.names, WfName(id: id, name: nm, units: units)]
- **precond** — if units != null → return (names: names, log: [WfLog(date: todayIso, units: units, name: nm), ...a.log])
- **return** — (names: names, log: null)

## floor
- `WfLog` · `WfName` · `any` · `trim`

## חוזה · contract
- **input:** `(WfCase a, String rawName, int? units, String id, {required String todayIso})`
- **output:** `({List<WfName> names, List<WfLog>? log})?`
- **purity:** pure
- **precond:**
  - if nm.isEmpty → return null
  - if a.names.any((x) => wfNormName(x.name) == key) → return null
  - if units != null → return (names: names, log: [WfLog(date: todayIso, units: units, name: nm), ...a.log])
- **null when:** nm.isEmpty · a.names.any((x) => wfNormName(x.name) == key) · or search exhausted
- **throws:** _none_
