# wfDailyRows — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `workflow_engine` · `/home/user/buildsmart/app_flutter/lib/logic/workflow_engine.dart`.

## עצם · object
- **kind:** top-level-function
- **signature:** `(OrgConfig cfg, List<({String name, String phone, WfCase? wf})> entities, String todayIso) -> List<List<Object>>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `_kStageFallback↝`
- **writes:** _none_
- **calls:** `wfUnitLabel` · `wfItemLabel` · `wfUnitsTotal` · `wfStageLabel`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — unit = wfUnitLabel(cfg)
- **compute** — item = wfItemLabel(cfg)
- **init** — rows = <List<Object>>[['שם', 'טלפון', '$unit היום', 'שלב', item, 'מתי לדבר שוב', 'הערה']]
- **loop** — for final e in entities
- **compute** — a = e.wf
- **branch** — if a == null → continue
- **compute** — touched = a.lastTouch == todayIso || a.log.any((l) => l.date == todayIso)
- **branch** — if !touched → continue
- **compute** — logToday = a.log.where((l) => l.date == todayIso).toList()
- **compute** — unitsToday = logToday.isNotEmpty ? logToday.fold<int>(0, (t, l) => t + l.units) : (wfUnitsTotal(a) == …
- **compute** — namesLine = a.names.map((n) => n.name + (n.units != null ? ' ·${n.units}' : '') + (n.done ? ' ✓' : ''…
- **effect** — rows.add([e.name, e.phone, unitsToday, wfStageLabel(cfg, a.stage), namesLine, a.nextTouch…
- **return** — rows

## floor
- `add` · `any` · `fold` · `join` · `map` · `toList` · `where`

## חוזה · contract
- **input:** `(OrgConfig cfg, List<({String name, String phone, WfCase? wf})> entities, String todayIso)`
- **output:** `List<List<Object>>`
- **purity:** pure
- **throws:** _none_
