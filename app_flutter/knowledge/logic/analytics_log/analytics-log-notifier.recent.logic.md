# AnalyticsLogNotifier.recent — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `analytics_log` · `lib/state/analytics_log.dart`.

## עצם · object
- **kind:** notifier-method
- **signature:** `({String? name, int limit = 50}) -> List<AnalyticsEvent>`
- **constants:** _none_

## חיבורים · connections
- **reads:** `state` · `unmodifiable`
- **writes:** _none_
- **calls:** _none_
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — filtered = name == null ? state : state.where((e) => e.name == name).toList(growable: false)
- **precond** — if filtered.length <= limit → return List<AnalyticsEvent>.unmodifiable(filtered)
- **return** — List<AnalyticsEvent>.unmodifiable(filtered.sublist(0, limit))

## floor
- `List` · `sublist` · `toList` · `where`

## חוזה · contract
- **input:** `({String? name, int limit = 50})`
- **output:** `List<AnalyticsEvent>`
- **purity:** pure
- **precond:**
  - if filtered.length <= limit → return List<AnalyticsEvent>.unmodifiable(filtered)
- **throws:** _none_
