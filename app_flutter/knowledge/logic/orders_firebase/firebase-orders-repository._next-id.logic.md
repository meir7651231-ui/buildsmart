# FirebaseOrdersRepository._nextId — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `orders_firebase` · `lib/data/repositories/orders_firebase.dart`.

## עצם · object
- **kind:** method
- **signature:** `() -> String`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** _none_
- **called-by:** `FirebaseOrdersRepository.placeOrder`
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **compute** — re = RegExp(r'^BS-(\d+)$')
- **compute** — maxN = 0
- **loop** — for final o in cached()
- **compute** — m = re.firstMatch(o.id)
- **branch** — if m != null
- **return** — maxN > 0 ? 'BS-${maxN + 1}' : 'BS-${DateTime.now().millisecondsSinceEpoch}'

## floor
- `RegExp` · `cached` · `firstMatch` · `group` · `now` · `tryParse`

## חוזה · contract
- **input:** `()`
- **output:** `String`
- **purity:** pure
- **throws:** _none_
