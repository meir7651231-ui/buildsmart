# FirebaseOrdersRepository.claimStore — logic decomposition

> Auto-generated by `tools/atom/decompose` (logic). Read-only —
> re-run the decomposer. Module: `orders_firebase` · `lib/data/repositories/orders_firebase.dart`.

## עצם · object
- **kind:** method
- **signature:** `(String orderId, String uid) -> void`
- **constants:** _none_

## חיבורים · connections
- **reads:** _none_
- **writes:** _none_
- **calls:** `byId`
- **called-by:** _none_
- **gated-by:** _none_
  <br/>(`↝` = reached transitively, through a call)

## התנהגות · behaviour (algorithm)
- **precond** — if uid.isEmpty → return (void)
- **compute** — o = byId(orderId)
- **precond** — if o == null → return (void)
- **precond** — if o.storeUid.isNotEmpty → return (void)
- **effect** — upsert(o.copyWith(storeUid: uid))

## floor
- `copyWith` · `upsert`

## חוזה · contract
- **input:** `(String orderId, String uid)`
- **output:** `void`
- **purity:** pure
- **precond:**
  - if uid.isEmpty → return (void)
  - if o == null → return (void)
  - if o.storeUid.isNotEmpty → return (void)
- **throws:** _none_
