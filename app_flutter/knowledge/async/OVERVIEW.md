# async — the loading state machine, and the split

> Auto-generated by `tools/atom/decompose` (async). Read-only.

- **providers:** 310 — **10 async** (Stream/Future) · **300 sync** (State/plain, cannot fail)
- **async backing:** 7 local · 3 firestore
- **`.when` consumers:** 2 (2 fold error→empty · HARD RULE #3)
- **typed exceptions:** 4

## The split (step 85)

The catalog / cart are **synchronous const data** — no `AsyncValue`, no loading, empty is ad-hoc. Only **firestore / functions** flow through `AsyncValue` + `.when`. There is **no uniform async contract**; a sync provider has **no error path because it cannot fail** — a documented gap, not a missing feature.

## Typed exceptions (the repo failure surface)

- `ClaudeException` · 2 throw-site(s)
- `OrderFunctionsException` · 2 throw-site(s)
- `UploadFunctionsException` · 2 throw-site(s)
- `AuthGatewayException` · 2 throw-site(s)

## `.when` consumers — the modeled states

| source | states | error→empty (#3) | empty-in-data |
|---|---|---|---|
| `dir` | loading·error·data | ✅ | ✅ |
| `async` | loading·error·data | ✅ | ✅ |
