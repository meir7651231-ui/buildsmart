# knowledge/backend — the Cloud Functions surface, opened

> DECOMP-DEPTH phase B output (steps 97-98). Auto-generated by the **read-only**
> backend decomposer (`tools/atom/decompose --backend <file.ts>` /
> `--backend-batch functions/src --out app_flutter/knowledge/backend`). Do not
> hand-edit — re-run the tool.

The server side is **TypeScript Cloud Functions**, so this is a source-scan (not
the Dart analyzer the other layers use), opened at the **function tier** with the
same spirit: **object** (name · trigger · region) · **connections** (which
Firestore collections it reads/writes · external calls — FCM/R2/Claude/email ·
auth-gated?) · **contract** (the `HttpsError` codes it throws).

- **[OVERVIEW.md](OVERVIEW.md)** — all **20 functions**: **10 callable**
  (`onCall`) · **8 firestore triggers** (`onDocument*`) · **2 scheduled**
  (`onSchedule`).
- per-module dirs (`orders/`, `r2/`, `claude/`, `push/`).

## The loop with the client (the async mirror)

This closes DECOMP-DEPTH's client/server loop. The client's typed exceptions
(`knowledge/async`) are the **mirror** of these server throws:

```
server: advanceOrderStage → throw new HttpsError("failed-precondition", …)
client: order_functions.dart → OrderFunctionsException("failed-precondition", …)
```

The typed error surface: `unauthenticated` · `invalid-argument` · `not-found` ·
`failed-precondition` · `permission-denied` · `internal`.

## What the map shows

- **Auth is at the gateway** — every `onCall` checks `request.auth` before work.
- **The triggers guard integrity** — `revertIllegalOrderStageWrite` /
  `revertIllegalConfigWrite` are `onDocumentUpdated` reactions that undo an
  illegal client write (defense-in-depth behind the rules).
- **External reach is narrow** — only `getUploadUrl` touches R2, only `askClaude`
  reaches Claude, the push/email fns fan out FCM/mail.

**Guards:** read-only · zero server-code change · the contract is documented,
never edited. The body scan stops at the next top-level `export` so a trailing
re-export never bleeds into a function's call set.
