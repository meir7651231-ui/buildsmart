# חוזה · `damerauLevenshtein` (Dart)

מקור-אמת (קדוש, חוק-4): `buildsmart/app_flutter/lib/logic/fuzzy_match.dart:16-57`.

**עוזר מוסק:** `_min3` נקרא בטיוטה (:32) אך **גופו לא הופיע** בקובץ-המחצב. הוסק
verbatim מהשם + השימוש (מינימום שלושת מועמדי-DL: הוספה/מחיקה/החלפה) והוטבע inline.
זו הפרשנות היחידה העקבית עם רקורסיית-Damerau-Levenshtein. אין קריאה-לשכן חיצונית ⇒ אין שקע.

## חתימה
```dart
int damerauLevenshtein(String a, String b)
```

## קלט
- `a`, `b` — שתי מחרוזות. ההשוואה היא **פר-codeUnit** (`codeUnitAt`), לא פר-גרפמה
  (נאמנות-מקור: תווי-BMP עבריים/לטיניים = codeUnit יחיד; סוגטים/אמוג'י = שני codeUnits).

## פלט / התנהגות (עוגני-שורה)
- `:17` — `a == b` ⇒ `0` (קיצור-דרך).
- `:18-19` — צד ריק ⇒ אורך-הצד-השני (רק הוספות/מחיקות).
- `:23-46` — DP שלוש-שורות: `cost=0/1` להחלפה, `+1` להוספה/מחיקה, `prev2[j-2]+1`
  לחילוף-שכנים (‏`:37-43`, מותנה `i>1 && j>1` והצלבת-התווים).
- הפלט תמיד `≥ 0` וסימטרי בפועל (‏`d(a,b)==d(b,a)`).

## דוגמאות מספריות
| # | a | b | ⇒ | הערה |
|---|---|---|---|------|
| 1 | `''` | `''` | `0` | שווים |
| 2 | `'abc'` | `'abc'` | `0` | זהים |
| 3 | `''` | `'abc'` | `3` | ריק ⇒ אורך |
| 4 | `'abc'` | `''` | `3` | ריק ⇒ אורך |
| 5 | `'ab'` | `'ba'` | `1` | חילוף-שכנים אחד |
| 6 | `'abcd'` | `'acbd'` | `1` | חילוף bc↔cb |
| 7 | `'kitten'` | `'sitting'` | `3` | קלאסי |
| 8 | `'flaw'` | `'lawn'` | `2` |  |
| 9 | `'שלום'` | `'שלמו'` | `2` | עברית BMP |
| 10 | `'ca'` | `'abc'` | `3` |  |

## שקעים
- אין. `_min3` פרטי-מוטבע; `codeUnitAt`/`List.filled`/`List.generate` — שפה/סטנדרט.

## DoD
```
dart run --enable-asserts new/dart/damerau_levenshtein_test.dart  ⇒ exit 0 + "OK damerauLevenshtein: N asserts passed"
```
