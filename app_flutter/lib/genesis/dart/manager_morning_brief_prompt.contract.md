# חוזה · `managerMorningBriefPrompt` (Dart)

מקור-אמת (קדוש, חוק-4): `buildsmart/app_flutter/lib/logic/manager_copilot.dart:109-116`
(‏`managerMorningBriefPrompt`).

## חתימה
```dart
String managerMorningBriefPrompt(String context)
```

## קלט
- `context` — מצב-העסק (נתוני-אמת) כטקסט, מוטבע verbatim.

## פלט / התנהגות (עוגני-שורה)
- `:110-115` — התבנית הקבועה:
  ```
  מצב-העסק כעת (נתוני-אמת):
  <context>

  כתוב תדריך-בוקר קצר לבעלים: 3-4 נקודות-תבליט על מה שדורש תשומת-לב היום (הזמנות תקועות/פתוחות, ניצול-אשראי גבוה, לקוח בולט). אך ורק לפי הנתונים שלמעלה — בלי להמציא (אין נתוני-עבר/מגמה). פתח ב-"☀️ תדריך-בוקר:".
  ```
- ‏3 שורות-הסיום מחוברות ללא-רווח-פותח (המקור מרווח בסוף כל fragment: `…היום ` · `…בולט). ` · `…מגמה). `).
- טהור: הפלט נקבע בלעדית מ-`context`.

## דוגמאות
1. `context='2 הזמנות תקועות'` ⇒ מתחיל ב-`'מצב-העסק כעת (נתוני-אמת):\n2 הזמנות תקועות\n\nכתוב תדריך-בוקר…'` ומסתיים ב-`'פתח ב-"☀️ תדריך-בוקר:".'`.
2. `context=''` ⇒ `'מצב-העסק כעת (נתוני-אמת):\n\n\nכתוב תדריך-בוקר…'`.
3. הפלט תמיד מכיל את הרצף `'☀️ תדריך-בוקר:'` ואת `'בלי להמציא (אין נתוני-עבר/מגמה).'`.

## שקעים
- אין.

## DoD (דיבר 12)
```
dart run --enable-asserts new/dart/manager_morning_brief_prompt_test.dart  ⇒ exit 0 + "OK managerMorningBriefPrompt: N asserts passed"
```
