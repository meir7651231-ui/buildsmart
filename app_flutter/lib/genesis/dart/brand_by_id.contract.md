# חוזה · brand_by_id

> אטום-Dart · נחצב ע"י סוכן-בינה (חוק-4 verbatim; רשימת-המותגים הועתקה byte-verbatim).

## מקור
buildsmart/app_flutter/lib/data/brands.dart:88-94

## טיפוסים/קבועים מוטבעים
- `Brand` (class, 6 שדות + const ctor) — verbatim.
- `kBrands` (const List<Brand>) — verbatim.

## אימות
`brand_by_id_test.dart` — assertions מול הרשימה. הרצה: `dart run --enable-asserts new/dart/brand_by_id_test.dart`.
