# חוזה · `portCountFor`

מוצא: `buildsmart/app_flutter/lib/data/polyroll_specs.dart:41-51` (`_portCountFor`).

`int portCountFor(LipskeyCatalogProduct p)` — מספר פורטי-החיבור לפי `productType`:
צינור→2, פקק→1, מסעף/רוכב→3, אומגה→2, ברירת-מחדל→2.

- טיפוס-שכן מוטבע: `LipskeyCatalogProduct` (productType).
- טהור, אפס import.
