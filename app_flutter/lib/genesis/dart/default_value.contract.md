# חוזה · `defaultValue`

מוצא: `buildsmart/app_flutter/lib/features/catalog_config/config_card.dart:189-199` (`_defaultValue`).

`AttributeValue defaultValue(AttributeDef attr)` — הערך עם `sortIndex == 0`, אחרת `values.first`.

- קורא-מובטח: `values` לא-ריק.
- טיפוסי-שכן מוטבעים: `AttributeValue` (labelHe, sortIndex), `AttributeDef` (values).
- טהור, אפס import.
