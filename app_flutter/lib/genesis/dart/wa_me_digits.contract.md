# חוזה · `waMeDigits` (Dart)

מקור-אמת (קדוש, חוק-4): `buildsmart/app_flutter/lib/logic/input_validators.dart:74-90`
(‏`waMeDigits`). כבר top-level ב-מקור — אין קריאה-לשכן, אין שדה-מחלקה, אפס-שקע.

## חתימה
```dart
String waMeDigits(String input)
```

## קלט
- `input` — מחרוזת-טלפון חופשית: ספרות + כל תו-לא-ספרה (רווח, `-`, `(`, `)`, `+`, `00`).

## פלט / התנהגות (עוגני-שורה)
- `input_validators.dart:75` — `digits = input.replaceAll(RegExp(r'\D'), '')` —
  מסירים **כל** תו שאינו ספרה (כולל `+`).
- `input_validators.dart:76` — `digits.isEmpty` ⇒ מחזירים **`''`**.
- `input_validators.dart:78-80` — אם `digits.startsWith('00')` ⇒ `digits = digits.substring(2)`
  (מסירים את קידומת-`00` הבינלאומית).
- `input_validators.dart:83-85` — אם `digits.startsWith('0')` ⇒ `digits = '972' + digits.substring(1)`
  (טרנק-`0` ישראלי ⇒ קוד-מדינה 972).
- `input_validators.dart:86` — מחזירים `digits`.
- ⚠️ שני ה-`if` **עצמאיים** (לא else-if): קלט שאחרי הסרת-`00` עדיין מתחיל ב-`0`
  עובר **שני** הטרנספורמים בזה-אחר-זה (ראה דוגמה 9).

## דוגמאות מספריות (מקריאת-הקוד)
| # | input | ⇒ | סיבה |
|---|-------|---|------|
| 1 | `"050-123 4567"` | `"972501234567"` | טרנק-0 ישראלי → 972 |
| 2 | `"+972 50 123 4567"` | `"972501234567"` | `+`/רווחים מוסרים; כבר-בינלאומי |
| 3 | `"00972501234567"` | `"972501234567"` | קידומת-00 מוסרת; אין 0 מוביל |
| 4 | `"972501234567"` | `"972501234567"` | ללא 0/00 — ללא-שינוי |
| 5 | `"abc"` | `""` | אפס ספרות |
| 6 | `""` | `""` | ריק |
| 7 | `"00"` | `""` | 00 מוסר ⇒ ריק ⇒ אין ענף-0 |
| 8 | `"0"` | `"972"` | 0 מוביל ⇒ 972 + ריק |
| 9 | `"000123"` | `"972123"` | קסקדה: 00 מוסר ⇒ `"0123"` ⇒ 0 מוביל ⇒ `"972123"` |
| 10 | `"(050) 123-4567"` | `"972501234567"` | סוגריים/רווח/מקף מוסרים; טרנק-0 → 972 |

## שקעים
אין. `String.replaceAll` · `String.startsWith` · `String.substring` · `RegExp` —
שפה/סטנדרט בלבד (לא-שקע).

## DoD (פקודה+פלט-צפוי, לפני הקוד — דיבר 12)
```
dart run --enable-asserts new/dart/wa_me_digits_test.dart  ⇒ exit 0 + "OK waMeDigits: N asserts passed"
```
