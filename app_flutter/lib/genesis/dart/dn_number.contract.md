# חוזה · `dnNumber`

מוצא: `buildsmart/app_flutter/lib/features/catalog_config/dn_scale.dart:70-75`.

## חתימה
```dart
int dnNumber(String label)
```

## התנהגות
מחלץ את המספר-השלם של תווית `DN<n>` למיון-עולה של גלגל-הבורר (‏DN15 לפני DN110,
לא ההפך הלקסיקלי). לוקח את **המספר-הראשון** בטקסט (‏`_kFirstNum`), מפריד ב-`.`
ולוקח את החלק-השלם. תווית ללא-מספר ⇒ ‏`1 << 30` (‏1073741824) — ממיינת אחרון,
דטרמיניסטית.

## מפל-מינימום
- ה-RegExp `_kFirstNum = RegExp(r'(\d+(?:\.\d+)?)')` הוטבע verbatim.

## שוליים
- אין-מספר ⇒ `1 << 30`.
- עשרוני "110.5" ⇒ 110 (‏split('.').first).
