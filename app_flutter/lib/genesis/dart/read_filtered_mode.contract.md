# חוזה · read_filtered_mode

> אטום-Dart · נחצב ע"י סוכן-בינה (חוק-4 verbatim). קלט-דומיין מסונתז בשיפוט.

## מקור
buildsmart/app_flutter/lib/data/edge/filtered_mode.dart:16-18

## טיפוסים מוטבעים
- `EdgeKvStore` (abstract interface) — verbatim; שקע-אחסון, מסונתז בבדיקה כ-`_FakeStore`.
- `kFilteredModeKey` — const string, verbatim.

## אימות
בדיקת-Golden `read_filtered_mode_test.dart` — assertions מחושבים-ידנית. הרצה: `dart run --enable-asserts new/dart/read_filtered_mode_test.dart`.
