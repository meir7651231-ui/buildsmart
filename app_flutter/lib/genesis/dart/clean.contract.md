# חוזה · `clean`

מוצא: `buildsmart/app_flutter/lib/features/word_finder/keyboard_predictions.dart:57-69` (`_clean`).

`List<String> clean(Iterable<String> raw, int max)` — מקצץ, זורק ריקות, מכווץ כפילויות (הראשון נשמר), חותך ל-`max`. שומר-סדר.

- החיתוך אחרי dedup/זריקת-ריקות.
- טהור, אפס import.
