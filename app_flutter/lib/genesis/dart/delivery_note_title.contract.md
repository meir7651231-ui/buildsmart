# חוזה · deliveryNoteTitle

**מוצא:** `buildsmart/app_flutter/lib/logic/delivery_note.dart:41` (עוגן-שורה).

## חתימה
```dart
String deliveryNoteTitle(NoteOrder order)
```
- **קלט:** `NoteOrder{ id }` — מחזיק-קלט טהור, השדה היחיד שהכותרת קוראת (`order.id`).
- **פלט:** `String` — הכותרת `'תעודת משלוח — <id>'`.

## התנהגות
verbatim: מחזיר את המחרוזת `'תעודת משלוח — '` משורשרת ל-`order.id`.
המפריד הוא מקף-אם (`—`, U+2014) עטוף רווחים. אין תלות בזמן/זהות/שכן.

## דוגמאות (עוגן delivery_note.dart:41)
| # | order.id | פלט |
|---|----------|-----|
| 1 | `"ORD-1042"` | `"תעודת משלוח — ORD-1042"` |
| 2 | `"7"` | `"תעודת משלוח — 7"` |
| 3 | `""` (ריק) | `"תעודת משלוח — "` |
| 4 | `"הזמנה #12"` | `"תעודת משלוח — הזמנה #12"` |
