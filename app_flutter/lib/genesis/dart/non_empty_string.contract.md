# חוזה · `nonEmptyString`

מוצא: `buildsmart/app_flutter/lib/data/bs_user.dart:217-226` (`_nonEmptyString`).

`String? nonEmptyString(Object? value)` — מחרוזת מקוצצת לא-ריקה או null.

- לא-String ⇒ null.
- מקוצץ-ריק ⇒ null; אחרת המקוצץ.
- טהור, אפס import.
