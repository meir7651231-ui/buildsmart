# חוזה · directionalContext

**מוצא (קדוש, L4):** `install_engine.dart:181-188` (origin/main, `_directionalContext`, verbatim).

## חתימה
```dart
String directionalContext(List<String> names, int i);
```

## קלט
- `names` — רשימת-שמות (במקור `chain[k].nameHe`, לפי-סדר). קורס מ-`List<LipskeyCatalogProduct>` — הגוף קורא אך-ורק nameHe.
- `i` — אינדקס ההתקן החד-כיווני.

## פלט
`String` — ניסוח-המיקום לפי-השכנים.

## התנהגות (עוגני-שורה למקור)
1. `up = i>0 ? names[i-1] : null` · `down = i<names.length-1 ? names[i+1] : null` (install_engine.dart:182-183).
2. up וגם down ⇒ `'בין "$up" ל-"$down"'` (:184).
3. רק down (i==0) ⇒ `'בכניסת הקו (לפני "$down")'` (:185).
4. רק up (i==אחרון) ⇒ `'ביציאת הקו (אחרי "$up")'` (:186).
5. אין שכנים (רשימה בת-1) ⇒ `'בקו'` (:187).

## דוגמאות מספריות (מוכחות ב-directional_context_test.dart)
| # | names | i | פלט | עוגן |
|---|-------|---|-----|------|
| 1 | [ברז, שסתום, משאבה] | 1 | בין "ברז" ל-"משאבה" | :184 |
| 2 | [שסתום, משאבה] | 0 | בכניסת הקו (לפני "משאבה") | :185 |
| 3 | [ברז, שסתום] | 1 | ביציאת הקו (אחרי "ברז") | :186 |
| 4 | [שסתום] | 0 | בקו | :187 |

## עדשה-עוינת (קלטי-קצה — CURRICULUM #6)
- קצוות: i==0 ⇒ אין up ⇒ ענף-הכניסה (#2); i==last ⇒ אין down ⇒ ענף-היציאה (#3).
- יחיד (length==1) ⇒ שני השכנים null ⇒ 'בקו' (#4).
- מירכאות-כפולות בתוך הפלט הן חלק מהניסוח המקורי (verbatim).
