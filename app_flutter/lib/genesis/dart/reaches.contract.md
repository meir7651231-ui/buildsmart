# חוזה · reaches
`bool reaches(PlainNode n, {required List<Object?> Function(PlainNode) plainProductsFor})`
האם לצומת יש מוצר-כלשהו: plainProductsFor(n).isNotEmpty.
מוטבע: PlainNode. טיפוס-האיבר לא-נגוע ⇒ Object?.
