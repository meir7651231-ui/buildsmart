# חוזה · stockMerkaz
`int stockMerkaz(String sku, {required int Function(String,int) tail})`
מלאי מרכז ל-SKU: הכותרת (118220) ⇒ 3; אחרת 1 + (tail(sku,2) % 7).
שקע: `tail` — n הספרות האחרונות של SKU נומרי. מוטבע: kC1HeadlineSku='118220'.
