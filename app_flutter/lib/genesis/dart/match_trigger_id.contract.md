# חוזה · matchTriggerId
`String? matchTriggerId(String reply, {required matchClosed})`
עיגון תשובת-מודל למזהה-טריגר-אמת (order.stuck/new/open/delivered), או null.
שקע: `matchClosed(Set<String>,String)`. מוטבע: kRuleTriggerIds.
