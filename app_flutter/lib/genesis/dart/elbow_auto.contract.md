# חוזה · elbowAuto
`Map<String,double> elbowAuto(int d, {int angle=90, required miteredElbow, required elbow})`
בורר-דיאגרמה: d≥160 ⇒ miteredElbow(d); אחרת elbow(d, angle).
שקעים: `miteredElbow(int)` · `elbow(int,{int angle})`.
