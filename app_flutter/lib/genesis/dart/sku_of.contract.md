# חוזה · skuOf

**מוצא:** buildsmart/app_flutter/lib/logic/install_engine.dart:11-17 (‏`_skuCache` שורה 13 + `_skuOf` שורות 14-17).

## קלט
| שקע | טיפוס | במקור | הערה |
|---|---|---|---|
| `sku` | `String` | פרמטר `_skuOf(String sku)` (שורה 14) | מפתח-החיפוש |
| `catalog` | `List<T>` | השכן `kCompatCatalog` (שורה 15) | הכרעה 1 — דאטה-מוזרקת |
| `skuKey` | `String Function(T)` | `p.sku` (שורה 15) | שקע-ריאדר; השדה היחיד שנגוע |
| `cache` | `SkuCache<T>` | משתנה-המודול `_skuCache` (שורה 13) | מחזיק שהקופסה בעלת-חייו |

## פלט
`T?` — המוצר שמפתחו `sku`, או `null` כשאינו בקטלוג.

## התנהגות (עוגני-שורה)
1. **בנייה-פעם-אחת** — `cache.map ??= {for (p in catalog) skuKey(p): p}` (verbatim שורה 15-16): המפה נבנית בקריאה הראשונה בלבד; שינוי `catalog` אחרי-כן **אינו נראה** עד שהקופסה מחליפה מחזיק.
2. **כפל-SKU** — האחרון-בקטלוג מנצח (map-comprehension דורסת מפתח קיים).
3. **החטאה** — `cache.map![sku]` (שורה 16) ⇒ `null` כשהמפתח חסר, כולל קטלוג ריק.
4. מחזיקים נפרדים = מטמונים בלתי-תלויים (במקור: משתנה-מודול יחיד; ההזרקה מכלילה, זהה-ביט פר-מחזיק).

## דוגמאות מספריות (מוכחות ב-sku_of_test.dart)
- קטלוג `[('A',1),('B',2)]`, ‏`skuOf('B')` ⇒ הרשומה עם 2.
- ‏`skuOf('C')` באותו קטלוג ⇒ `null`.
- קטלוג `[('X',1),('X',9)]` ⇒ `skuOf('X')` = הרשומה עם 9 (אחרון מנצח).
- אחרי קריאה ראשונה, הוספת `('NEW',7)` לרשימה ⇒ `skuOf('NEW')` עדיין `null` (מטמון קפוא).
- מחזיק חדש על הקטלוג המורחב ⇒ `skuOf('NEW')` = 7.

## DoD
`dart run --enable-asserts new/dart/sku_of_test.dart` ⇒ exit 0.
