# חוזה · matchRuleOp
`String? matchRuleOp(String reply, {required matchClosed})`
עיגון תשובת-מודל לאופרטור-אמת (>,>=,<,<=,=), או null.
שקע: `matchClosed(Set<String>,String)` — exact→longest-contained→null. מוטבע: kRuleOps.
