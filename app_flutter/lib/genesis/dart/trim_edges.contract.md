# חוזה · `trimEdges`

מוצא: `buildsmart/app_flutter/lib/features/global_search/narrowers.dart:36-46` (הפרטי `_trimEdges`).

## חתימה
```dart
String trimEdges(String t)
```

## התנהגות
גוזם פיסוק-קצה **משני-הקצוות בלבד** של [t] — פיסוק-פנימי נשמר. תווי-הקצה
(`_edge` = ‏`. * # " ' ׳ ״`) מקצצים "מס."→"מס" · "*עם"→"עם", אבל "ח.פ" ו-"ש״ת"
שורדים כי הפיסוק בתוכם. כשאין-שינוי (אין פיסוק-קצה) מחזיר את אותו-המופע (בלי
substring מיותר).

## מפל-מינימום
- הקבוע `_edge = '.*#"\'׳״'` הוטבע verbatim.

## שוליים
- מחרוזת ריקה ⇒ ריקה.
- מחרוזת כולה-פיסוק-קצה ⇒ ריקה.
- פיסוק פנימי בלבד ⇒ ללא-שינוי.
