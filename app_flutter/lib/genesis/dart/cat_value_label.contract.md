# חוזה · catValueLabel

> אטום-Dart · נחצב אוטומטית ע"י חצב-AST (חוק-4 — verbatim מהמקור).

## מקור
buildsmart/app_flutter/lib/features/ring_dive/catalog_slang.dart:114-116

## התנהגות
The layman label for [value] on [axis] — its slang if the owner mapped one,
else the value verbatim (sizes, colours, angles already read plainly).

## אימות
בדיקת-Golden (`cat_value_label_test.dart`): אפיון דטרמיניסטי על סל-קלטים — הוקלט מהרצת הקוד-החלוץ. הרצה: `dart run --enable-asserts new/dart/cat_value_label_test.dart`.
