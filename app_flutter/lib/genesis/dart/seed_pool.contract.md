# חוזה · `seedPool`

מוצא: `buildsmart/app_flutter/lib/features/card_keyboard/pool_seed.dart:28-33`.

`List<LipskeyCatalogProduct> seedPool(CardSeed seed, List<LipskeyCatalogProduct> universe)` —
היקום מסונן בפרדיקט-הזרע (`seed.seedPredicate`).

- כל תוצאה מוצר-אמת מהיקום; אינו ממציא.
- טיפוסי-שכן מוטבעים: `LipskeyCatalogProduct` (sku), `CardSeed` (seedPredicate).
- טהור, אפס import.
