# חוזה · `maxOscillation` (Dart)

מקור-אמת (קדוש, חוק-4): `buildsmart/app_flutter/lib/logic/intel/funnels.dart:269-283`
(‏`_maxOscillation`, פרטי-במקור). מקודם ל-public לפי כלל-הגלגול. גלאי-דישדוש
לניתוח-המשפך (דשבורד-המנהל): רצף A-B-A-B ⇒ המשתמש תקוע מנווט הלוך-ושוב.

## חתימה
```dart
int maxOscillation(List<String> path)
```

## קלט
- `path` — נתיב-מזהים (רצף מסכים/פעולות בסשן), `List<String>`.

## התנהגות (עוגני-שורה)
- `funnels.dart:272` — הלולאה מ-`i = 2` (צריך שני קודמים).
- `funnels.dart:273` — התנאי: `path[i] == path[i-2] && path[i] != path[i-1]`
  (חזרה למיקום-לפני-הקודם, כשהקודם שונה = צעד "אחורה").
- `:274-275` — עמד בתנאי ⇒ `run++`; מעדכן `best` אם `run > best`.
- `:277` — נכשל ⇒ `run = 0` (אפס-ספירה; ריצה נשברת).
- `:280` — מחזיר `best` (0 כשאין תנודה כלל).

## דוגמאות מספריות
| # | path | ⇒ | נימוק |
|---|------|---|-------|
| 1 | `[]` | `0` | אין איברים |
| 2 | `['a','b']` | `0` | הלולאה (i≥2) לא רצה |
| 3 | `['a','b','a']` | `1` | i=2: a==a, a≠b ⇒ run=1 |
| 4 | `['a','b','a','b','a']` | `3` | תנודה מלאה: run 1→2→3 |
| 5 | `['a','a','a']` | `0` | path[i]==path[i-1] ⇒ else, run=0 |
| 6 | `['a','b','c','b','a']` | `1` | רק i=3 עומד (b==b, b≠c) |
| 7 | `['a','b','a','b','c','d','c','d']` | `2` | שתי תנודות נפרדות ‏(best=2), הריצה מתאפסת באמצע |

## שקעים
אין. `List.length`/אינדוקס/שוויון-מחרוזת — שפה בלבד.

## DoD (דיבר 12)
```
dart run --enable-asserts new/dart/max_oscillation_test.dart  ⇒ exit 0 + "OK maxOscillation: N asserts passed"
```
