# חוזה · `money` (Dart)

מקור-אמת (קדוש, חוק-4): `buildsmart/app_flutter/lib/logic/manager_copilot.dart:59-70`
(‏`money`, פונקציה מקוננת בבונה-הפרומפט). מקודמת ל-top-level (כלל-הגלגול).

## חתימה
```dart
String money(int n)
```

## קלט
- `n` — סכום שלם (שקלים), `int`; שלילי מותר.

## התנהגות (עוגני-שורה)
- `manager_copilot.dart:61` — `neg = n < 0`.
- `:62` — מקבצים מעל `n.abs()`, מוסיפים סימן בסוף (הערת-המקור: "no ₪-,100").
- `:64-66` — פסיק לפני כל שלישייה: `i > 0 && (s.length - i) % 3 == 0`.
- `:68` — מחזיר `${neg ? '-' : ''}₪$b` (מינוס לפני ה-₪).

## דוגמאות מספריות
| # | n | ⇒ |
|---|---|---|
| 1 | `0` | `'₪0'` |
| 2 | `100` | `'₪100'` |
| 3 | `1000` | `'₪1,000'` |
| 4 | `1234567` | `'₪1,234,567'` |
| 5 | `-100` | `'-₪100'` |
| 6 | `-1000` | `'-₪1,000'` |
| 7 | `12` | `'₪12'` |

## שקעים
אין. `int.abs`/`toString`/`StringBuffer` — שפה בלבד.

## DoD (דיבר 12)
```
dart run --enable-asserts new/dart/money_test.dart  ⇒ exit 0 + "OK money: N asserts passed"
```
