# חוזה · `opTag`

מוצא: `buildsmart/app_flutter/lib/logic/studio/edit_safety.dart:474-483` (`_opTag`).

`String opTag(ConfigOp op)` — תג-ה-op לפי תת-הטיפוס של `ConfigOp`:
SetText→'setText', SetEmoji→'setEmoji', SetHidden→'setHidden', SetOrder→'setOrder', SetStyle→'setStyle', SetAction→'setAction'.

- sealed `ConfigOp` + 6 שלדים מוטבעים (התאמת-תבנית לפי טיפוס; אין קריאת שדה).
- טהור, אפס import.
