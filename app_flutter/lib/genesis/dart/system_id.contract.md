# חוזה · system_id

> אטום-Dart · נחצב ע"י סוכן-בינה (חוק-4 verbatim). קלט-דומיין מסונתז בשיפוט.

## מקור
buildsmart/app_flutter/lib/domain/seeds/plumbing_trade_seed.dart:38-39

## טיפוסים מוטבעים
- `WaterSystem` (enum, 2 ערכים) — מוטבע-מינימום; הפונקציה נוגעת רק ב-`.name`.
- `kPlumbingTradeId` — const string 'plumbing', verbatim.

## אימות
בדיקת-Golden `system_id_test.dart` — assertions מחושבים-ידנית. הרצה: `dart run --enable-asserts new/dart/system_id_test.dart`.
