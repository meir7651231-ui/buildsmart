# חוזה · basename

> אטום-Dart · נחצב אוטומטית ע"י חצב-AST (חוק-4 — verbatim מהמקור).

## מקור
buildsmart/app_flutter/lib/features/catalog_config/image_quality.dart:66-74

## התנהגות
The basename of [assetPath] (after the last '/'), or null.

## אימות
בדיקת-Golden (`basename_test.dart`): אפיון דטרמיניסטי על סל-קלטים — הוקלט מהרצת הקוד-החלוץ. הרצה: `dart run --enable-asserts new/dart/basename_test.dart`.
