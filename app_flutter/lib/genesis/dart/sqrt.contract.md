# חוזה · `sqrt` (Dart)

מקור-אמת (קדוש, חוק-4): `buildsmart/app_flutter/lib/logic/pressure_drop.dart:334-342`
(‏`_sqrt`). עוזר פרטי-במקור, נחשף כ-top-level. שאר-הטיוטה (‏`estimatePressureDrop`,
`checkDrainageSlope`) אינם היעד — לא הוטבעו.

## חתימה
```dart
double sqrt(double x)
```

## קלט
- `x` — הרדיקנד, `double`.

## פלט / התנהגות (עוגני-שורה)
- `pressure_drop.dart:336` — `var r = x / 2;` (ניחוש-פתיחה).
- `pressure_drop.dart:337-339` — לולאה **קבועה** של 5 איטרציות: `r = 0.5 * (r + x / r)`.
- `pressure_drop.dart:341` — מחזיר `r` (בלי בדיקת-התכנסות; תמיד 5 צעדים).
- **נאמנות-מקור (לא-שיפור):** אין מגן על `x == 0` — בצעד הראשון `x / r == 0 / 0 == NaN`
  ⇒ הפלט `NaN`. אין מגן על `x < 0`.

## דוגמאות מספריות
| # | x | ⇒ (בקירוב) | הערה |
|---|---|-----------|------|
| 1 | 4.0 | `2.0` **מדויק** | ‏r₀=2, נקודת-שבת כבר באיטרציה 1 |
| 2 | 2.0 | ‏≈ 1.4142135623746899 | תוך `1e-6` מ-`dart:math.sqrt` |
| 3 | 9.0 | ‏≈ 3.0000000000455 | תוך `1e-6` |
| 4 | 1.0 | ‏≈ 1.0000000000000212 | **לא** מדויק (5 צעדים מ-r₀=0.5) |
| 5 | 0.0 | `NaN` | נאמנות-מקור: `0/0` באיטרציה הראשונה |

## שקעים
- אין. עצמאי לחלוטין (חוק-1). `double` op-ים = שפה/סטנדרט.

## DoD (פקודה+פלט-צפוי, לפני הקוד — דיבר 12)
```
dart analyze new/dart/sqrt.dart                         ⇒ No issues found!
dart run --enable-asserts new/dart/sqrt_test.dart       ⇒ exit 0 + "OK sqrt: N asserts passed"
```
