# חוזה · `pow025` (Dart)

מקור-אמת (קדוש, חוק-4): `buildsmart/app_flutter/lib/logic/pressure_drop.dart:327-333`
(‏`_pow025`, פרטי-במקור ⇒ פורסם `pow025`, כלל-הגלגול). השכן `_sqrt` (שקע-מועמד `sqrt`,
כותרת-הטיוטה) = `sqrt` מ-`dart:math` (dart:* מותר, חוק-1 — אינו אטום, אינו שקע).

## חתימה
```dart
double pow025(double x)
```

## קלט
- `x` — `double`. כל ערך. `pressure_drop.dart:329` — `final s = x > 0 ? x : 1e-9`.

## פלט / התנהגות (עוגני-שורה)
- `pressure_drop.dart:329` — `s = x > 0 ? x : 1e-9` (מגן: אי-חיובי ⇒ 1e-9).
- `pressure_drop.dart:330-331` — `r1 = sqrt(s); return sqrt(r1)` ⇒ `sqrt(sqrt(s))` = `s^0.25`.
- לכן עבור `x > 0` ⇒ `x^0.25`; עבור `x <= 0` ⇒ `(1e-9)^0.25 = 10^-2.25 ≈ 0.00562341...`.

## דוגמאות מספריות
| # | x | ⇒ | הסבר |
|---|---|---|------|
| 1 | 16.0 | `2.0` | sqrt(sqrt(16))=sqrt(4)=2 |
| 2 | 81.0 | `3.0` | sqrt(sqrt(81))=sqrt(9)=3 |
| 3 | 1.0 | `1.0` | sqrt(sqrt(1))=1 |
| 4 | 256.0 | `4.0` | sqrt(16)=4 |
| 5 | 10000.0 | `10.0` | sqrt(sqrt(1e4))=sqrt(100)=10 |
| 6 | 0.0 | `≈0.0056234` | s=1e-9 ⇒ (1e-9)^0.25 |
| 7 | -4.0 | `≈0.0056234` | אי-חיובי ⇒ מוצמד ל-1e-9 (זהה ל-x=0) |

## עדשה-עוינת
| # | x | ⇒ |
|---|---|---|
| 8 | 1e-9 | `≈0.0056234` (x>0 ⇒ עצמו, שווה למגן) |
| 9 | 0.0001 | `≈0.1` (sqrt(sqrt(1e-4))=sqrt(0.01)=0.1) |

## שקעים
- אין שקע-אטום. `math.sqrt` — ספריית-שפה (`dart:math`), מותר בחוק-1.

## DoD (פקודה+פלט-צפוי, לפני הקוד — דיבר 12)
```
dart analyze new/dart/pow025.dart                          ⇒ "No issues found!"
dart run --enable-asserts new/dart/pow025_test.dart        ⇒ exit 0 + "OK pow025: N asserts passed"
```
