# חוזה · conn_type_id

> אטום-Dart · נחצב ע"י סוכן-בינה (חוק-4 verbatim). קלט-דומיין מסונתז בשיפוט.

## מקור
buildsmart/app_flutter/lib/domain/seeds/plumbing_trade_seed.dart:40-45

## טיפוסים מוטבעים
- `EndType` (enum, 6 ערכים) — מוטבע-מינימום; הפונקציה נוגעת רק ב-`.name`.
- `kPlumbingTradeId` — const string 'plumbing', verbatim.

## אימות
בדיקת-Golden `conn_type_id_test.dart` — assertions מחושבים-ידנית. הרצה: `dart run --enable-asserts new/dart/conn_type_id_test.dart`.
