// menu_state.dart — intentionally empty (kept, not deleted: protocol Gate 90
// forbids removing files under lib/state/).
//
// This file used to hold the menu-dial drill providers (homeDrillProvider /
// projectsDrillProvider / settingsGroupProvider / settingsDrillProvider). The
// menu-dial was removed in the dial-distribution refactor (its content is now
// native: catalog ⋮ · ProfileScreen via the name-chip · the extended
// CatalogSettingsScreen · the store project-picker · per-persona dashboards),
// so those providers are gone. No live provider remains here.
