# Generated test suite — triage

Auto-generated by `tools/atom/testgen` from the atom-decomposition graphs.
**Opt-in + non-blocking** (self-skip unless `--dart-define=atomgen=true`; the main
`flutter test` compiles all files and runs 0). Regenerate:
`dart run tools/atom/testgen/bin/testgen.dart --graphs app_flutter/knowledge/screens --out app_flutter/test/generated`.

## The rule (graph → tests)

- `registry-ID` → **wired** (renders) · **hide** (gone when `resolvedNodeProvider`
  is overridden hidden).
- `verb` + toast → **verb** (tap the label → toast fires).
- `formula` (money-ternary) → **formula** (null → constant · 1234 → ₪1,234).
- Skipped: preview / const-gated atoms, required-ctor-arg composers, trivial reads.

## Harness (what it seeds)

Fonts · RTL · tall surface · mock prefs · **overflow-tolerant** (layout overflow
is drained — a real non-overflow exception is re-thrown, never swallowed) · a
**board session by screen-role** (`manager_*`/`store_*`/`worker_*`/`courier_*`)
so role screens render their body instead of the login gate.

## Latest triage (slice-3)

- **Screens:** 62 · **Tests:** 1001 · **0 compile-fail** (main-suite safe).
- **509 pass · 492 fail.** Anchor `contractor-home` **14/14** (ST-3 complete).

### Slice log (honest)

- **slice-1:** wired · hide · verb-toast — 479 pass.
- **slice-2:** + formula — 480 pass. contractor-home 14/14.
- **slice-3:** overflow-tolerant harness + board-session role-seed → **509 pass
  (+29).** The overflow hypothesis alone converted **0** (verified — those were
  multi-line error dumps, not distinct failures); the real lever was the
  role-session seed (e.g. `manager_profile` 2→11/12).

### Why the remaining ~492 are NOT one "harness gap"

The failures are **per-screen state**, not a single fix — the tool honestly flags
every registry element that doesn't render in the seeded default state:

1. **Tabbed content** — the element lives on a non-default tab (needs a tap the
   generic pump doesn't do). The dominant bucket.
2. **Data-dependent sections** — renders only with seeded orders/cart/catalog data.
3. **Intentionally hidden** — e.g. `manager_profile_screen.t02` ("מצב הדגמה") is
   behind `if (session.demo && !kHideUnderConstruction)` — wired in the registry
   but hidden for App-Store review. **A real discovery, not a harness bug.**

`pass = verified · fail = reported`. The fails are a triage worklist (which
elements are conditional / hidden / possibly broken), never swallowed. Further
conversion needs per-screen levers (tab-walk, data seeds) with diminishing
returns — a long tail, tracked here rather than force-passed.
