<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Product/page images load from the cloud (Cloudflare R2) at runtime, so the
         RELEASE build needs INTERNET. Flutter auto-adds this only to the debug/profile
         manifests, so without it here release builds show blank product images. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- mobile_scanner + image_picker(camera) — barcode scanning & proof/profile
         photo capture (camera not required to install; those features simply
         won't run on a camera-less device — required="false" keeps the app
         installable for Play compliance). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <!-- speech_to_text — voice search (microphone). Hardware feature marked
         required="false" so the app installs on mic-less devices. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <!-- image_picker (gallery pick) — attach existing photos for proof-of-delivery,
         profile picture and certificates. Modern Android (13+, API 33+) uses the
         scoped READ_MEDIA_IMAGES; older devices (<=API 32) fall back to
         READ_EXTERNAL_STORAGE, capped with maxSdkVersion so it isn't requested
         on API 33+ where it has no effect. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <application
        android:label="buildsmart"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
