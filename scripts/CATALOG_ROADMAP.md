# BuildSmart · Catalog Engine — 100 צעדים מלאים

מ-מסמך+סקריפט ל**מערכת אכיפה אוטומטית** שמבטיחה אפס שגיאות גם ב-80,000 מוצרים.
כל צעד = משימה שלמה ומבוצעת לבדה. סטטוס: ✅ הושלם · 🔄 בעבודה · ⬜ פתוח.

1. ✅ להגדיר מודל חומרה תלת-שלבי (ERROR חוסם / WARN לבדיקה / INFO לדיווח) שכל חוק ולידציה משויך אליו, כדי שאפשר לחסום merge רק על מה שבאמת קריטי.
2. ✅ לכתוב parser ל-`lipskey_catalog.dart` שמחלץ כל `LipskeyCatalogProduct` לשדות (sku/name/category/page/image/brand), כולל תמיכה במחרוזות single ו-double quote (שמות עם גרש).
3. ✅ חוק ERROR — שם ריק: לזהות כל מוצר ש-`nameHe` שלו ריק או רווחים בלבד.
4. ✅ חוק ERROR — שם פגום: לזהות טקסט-אריזה שדלף ("כמות באריזה/במשטח"), שדות מאוחדים ב-`|`, וגרשיים שגויים (״ “).
5. ✅ חוק ERROR — מספר תקוע במילה: לזהות ספרה צמודה לאות עברית ("מחסום2", "1.5מחסום") כסימן ל-RTL שבור.
6. ✅ חוק ERROR — סוגריים לא מאוזנים: לזהות שם שבו מספר '(' ≠ ')'.
7. ✅ חוק ERROR — מק"ט חסר: לזהות מוצר בלי `sku`.
8. ✅ חוק ERROR — קטגוריה חסרה: לזהות מוצר בלי `categoryHe`.
9. ✅ חוק ERROR — קטגוריה לא-ממופה: לזהות `categoryHe` שאין לו `lipskeyCategory` תואם בעץ הצלילה (מוצר שלא ניתן להגיע אליו).
10. ✅ חוק ERROR — מק"ט כפול: לזהות אותו `sku` המשמש שני מוצרים שונים.
11. ✅ חוק WARN — brand לא-חוקי: לזהות `brand` שאינו ברשימת המותגים המאושרת.
12. ✅ חוק WARN — שם כפול בקטגוריה: לזהות שני מוצרים עם אותו שם מדויק באותה קטגוריה.
13. ✅ חוק WARN — אי-התאמת דומיין: לזהות מילת-מפתח חוצה-תחום (ברז בקטגוריית ניקוז, אסלה בקטגוריית ברז) בצורה שמרנית למניעת רעש.
14. ✅ לכתוב `selftest` עם fixtures "מורעלים" — מוצר פגום לכל חוק — שמוכיח שכל חוק ERROR אכן יורה, ובקרת-שלילה שמוצר תקין אינו מפעיל אף ERROR.
15. ✅ להוסיף פלט `audit --json` עם exit-code (1 על ERROR) לצריכה ע"י CI וכלים.
16. ✅ לכתוב `fix` שמבצע נרמול בטוח: גרשיים→", הסרת טקסט-אריזה, רווח בגבול ספרה↔אות בלי שינוי סדר המילים.
17. ✅ להוסיף ל-`fix` הגנת גרש-Dart: לעולם לא להכניס `'` בודד למחרוזת single-quote ששוברת קומפילציה.
18. ✅ להגדיר ש-`fix` רץ ב-dry-run כברירת מחדל ודורש `--apply` מפורש לכתיבה.
19. ✅ להוסיף ל-`fix` הדפסת diff מלא לכל מק"ט (לפני→אחרי) כך שכל שינוי גלוי לעין לפני אישור.
20. ✅ לכתוב לוג שינויים מצטבר (`scripts/fix_log.csv`) שמתעד כל תיקון אוטומטי עם תאריך, מק"ט, שדה, ערך-קודם וערך-חדש.
21. ✅ להוסיף נרמול ל-`nameEn`: הסרת `|` וכפילויות שנותרו מחילוץ ה-PDF, כדי שהשם האנגלי יהיה נקי כמו העברי.
22. ⬜ לאחד סימון אינץ' עקבי בכל הקטלוג (`1.25"` במקום `11/4`/`1¼` מעורבים) כדי שזיהוי המידה והתאימות יעבדו אחיד.
23. ⬜ לאחד סימון DN עקבי: היכן שברור שמספר בודד הוא קוטר (40/50/110), לנרמל ל-`DN##` כדי לחזק את מנוע התאימות.
24. ✅ להוסיף trim גלובלי לרווחים כפולים ולתווי-בקרה נסתרים בכל השמות.
25. ⬜ לזהות זוגות שם-כפול ולהוסיף מבדיל-גודל אוטומטי מהמקור (כמו שעשינו לברכי 45° → DN32/40/50).
26. ✅ להוסיף גיבוי אוטומטי (rollback) של `lipskey_catalog.dart` לפני כל `--apply`, עם פקודת שחזור.
27. ✅ להוסיף `fix --rule <name>` להרצת תיקון ממוקד של חוק יחיד בלבד.
28. ✅ לאמת אידמפוטנטיות: הרצת `fix --apply` פעמיים חייבת לתת אותו פלט (אין שינוי בריצה השנייה).
29. ✅ להגדיר רשמית את סכמת `source_truth.json` (sku, nameHe, nameEn, categoryHe, brand, sizes[], page, pdf, imageFile).
30. ✅ לכתוב ולידטור-סכמה שבודק שכל רשומה ב-source_truth מכילה את שדות-החובה בטיפוסים הנכונים.
31. ✅ להוסיף ל-`truthcheck` השוואת שם (nameHe) מוצר-מול-מקור ודיווח כל פער.
32. ✅ להוסיף ל-`truthcheck` השוואת קטגוריה מוצר-מול-מקור.
33. ✅ להוסיף ל-`truthcheck` השוואת מידות (sizes) מוצר-מול-מקור — קריטי לתאימות.
34. ✅ להוסיף ל-`truthcheck` השוואת brand מוצר-מול-מקור.
35. ✅ להוסיף ל-`truthcheck` זיהוי מק"טים עודפים (באפליקציה ולא במקור) וחסרים (במקור ולא באפליקציה).
36. ✅ לכתוב `truthapply` שמתקן אוטומטית שמות/קטגוריות/מידות מתוך source_truth, עם dry-run ו-`--apply`.
37. ✅ לאמת שלכל `imageFile` בקטלוג קיים קובץ תמונה בפועל בתיקיית ה-assets.
38. ✅ לאמת התאמת תמונה↔מק"ט (שם הקובץ = ה-sku, או hash מול מקור) למניעת שיוך-תמונה שגוי.
39. ✅ להפיק דוח כיסוי-אימות: אחוז המוצרים שאומתו מול מקור-אמת, מפולח לפי מותג/קטגוריה.
40. ✅ לשדרג את `crosscheck` לעבוד על מספר PDFs יחד עם ניקוד-דמיון מדורג (לא רק סף 50%).
41. ✅ לבנות "תור-בדיקה" של שמות חשודים (חפיפה נמוכה למקור) לסקירה ידנית ממוקדת.
42. ✅ להוסיף `verify` במצב-אצווה שמרנדר את כל עמודי המקור של החשודים בבת-אחת ל-PNG.
43. ⬜ להוסיף OCR-fallback (Tesseract עברית) לעמודי PDF שאין בהם שכבת-טקסט.
44. ✅ לקבע snapshot של source_truth כ-artifact ב-CI כדי שאימות יהיה דטרמיניסטי.
45. ✅ ליצור workflow `catalog-qa.yml` ב-GitHub Actions שמריץ selftest + audit.
46. ✅ להגדיר שה-CI נכשל (חוסם) על כל שגיאת ERROR בקטלוג.
47. ✅ להגביל את ה-trigger רק לשינויים בקבצי הנתונים/הכלי (paths-filter) לחיסכון בזמן ריצה.
48. ✅ להתקין pre-commit hook מקומי שמריץ `audit` לפני כל commit שנוגע בנתונים.
49. ✅ להעלות את דוח ה-QA (JSON/CSV) כ-artifact ניתן-להורדה בכל ריצת CI.
50. ✅ להוסיף badge סטטוס QA ל-README כדי שמצב האיכות גלוי מיד.
51. ✅ להגדיר תקרת-WARN מקסימלית שמעליה ה-CI נכשל (מניעת הצטברות חוב-איכות).
52. ⬜ להוסיף הערת-PR אוטומטית שמסכמת את כל ה-findings של השינוי.
53. ✅ להריץ `flutter analyze` אוטומטית ב-CI על כל שינוי בנתונים.
54. ⬜ להריץ את רגרסיית "קטלוג" באופן headless (flutter test) בתוך ה-CI.
55. ⬜ לחסום merge ל-main ללא ריצת-CI ירוקה (branch protection).
56. ✅ לבנות `_sizeSet` שמחלץ מידות בהקשר ממדי בלבד ומתעלם מכמות-אריזה ואורך.
57. ✅ לפצל את התאימות בכרטיס לפי צד/קצה (מצמד 75/50 → צד DN75 + צד DN50 בנפרד).
58. ✅ להוסיף שדה `sizes` מובנה ל-`LipskeyCatalogProduct` (רשימת מידות) במקום ניתוח-שם שביר.
59. ⬜ למגר את כל 788 המוצרים: לחלץ מידות מהשם פעם-אחת ולמלא את שדה `sizes`.
60. ⬜ להוסיף סינון תאימות לפי סוג-חיבור (זכר/נקבה) כדי שלא יוצעו שני זכרים.
61. ⬜ להוסיף סינון תאימות לפי שיטת-חיבור (תבריג / דבק / אלקטרופוזיה).
62. ✅ להוסיף סינון תאימות לפי חומר (PVC / HDPE / נחושת) — לא לחבר חומרים לא-תואמים.
63. ✅ להוסיף דירוג-רלוונטיות: חלקים מאותה קטגוריית-אב מוצגים ראשונים בתאימות.
64. ⬜ לבנות "ערכת התקנה" — הצעה אוטומטית של כל החלקים הדרושים לחיבור מלא של מוצר.
65. ✅ להוסיף רגרסיה: מצמד 75/50 חייב להחזיר בדיוק 2 צדדים עם החלקים הנכונים.
66. ✅ להוסיף רגרסיה: צינור DN50 חייב להחזיר לפחות N חלקים תואמים מקטגוריות אחרות.
67. ⬜ למטמן (precompute) את מפת-התאימות בזמן build לביצועים בקטלוג ענק.
68. ⬜ לבנות טבלת-תאימות ידנית ל-override של חריגים שהאלגוריתם לא תופס.
69. ⬜ לבנות מנתח שמפרק שם מוצר ל-{type, subtype, brand, variant} לפי דפוסים+מילון.
70. ⬜ להוסיף את השדות המובנים ל-model ולמלא אותם בכל המוצרים.
71. ⬜ לעדכן את הכרטיס להציג מבנה מובנה (סוג/דגם/גוון) במקום פיצול-מילים גנרי.
72. ✅ לבנות אינדקס-היפוך מילה→מוצרים לחיפוש מהיר (O(1)) במקום סריקה לינארית.
73. ✅ לנרמל שמות-מותג למחרוזת קנונית אחת (קיסר/דיור/פיטרה/AQUATEC) למניעת כפילויות-איות.
74. ⬜ להגדיר טקסונומיית-קטגוריות מפורשת (אב→תת→עלה) כמבנה-נתונים ולא רק כעץ-UI.
75. ✅ להפוך צבע/גימור לשדה מובנה (ניקל/שחור/פרגמון/לבן) לסינון וחיפוש.
76. ✅ להפוך יחידת-מכירה למובנית (בודד/ארגז/משטח + כמויות) במקום נגזרת מ-qtyPack.
77. ⬜ ליצור קישור מפורש מוצר↔SmartProduct (במקום smartKey על העלה בלבד).
78. ⬜ ליצור קישור מפורש מוצר↔אביזרים↔שלבי-התקנה ברמת המוצר.
79. ✅ להוסיף מזהה יציב (UUID) לכל מוצר מעבר ל-SKU (ל-SKU שמשתנה בין מהדורות).
80. ✅ לפרסם JSON-Schema רשמית למבנה המוצר לשימוש כלים חיצוניים.
81. ⬜ לתמוך ב-N קטלוגים מקבילים עם sharding לפי מותג/ספק.
82. ⬜ לזהות דדופ חוצה-קטלוג (אותו מוצר פיזי עם מק"טים שונים אצל ספקים שונים).
83. ⬜ למזג מותגים מרובים (ליפסקי+AQUATEC+פלסון+...) למודל-מוצר אחיד.
84. ⬜ להריץ benchmark על 50,000 רשומות סינתטיות ולוודא שהכלי+האפליקציה עומדים.
85. ⬜ לפצל את `lipskey_catalog.dart` לקבצים-לפי-קטגוריה לקריאות ול-build מהיר.
86. ⬜ להוסיף טעינה-עצלה (lazy) של קטגוריות גדולות באפליקציה.
87. ⬜ לבנות אינדקס-חיפוש מובנה (trie/map) במקום `where` לינארי על כל הקטלוג.
88. ⬜ להוסיף pagination ברשימות-מוצרים ארוכות (מאות פריטים בקטגוריה).
89. ⬜ לבנות צינור-ייבוא-אצווה (CSV/JSON → קוד Dart) אוטומטי לקליטת קטלוג חדש.
90. ⬜ לתמוך בגרסאות-קטלוג (2024 מול 2025) עם diff בין מהדורות ושמירת היסטוריה.
91. ⬜ לבנות דשבורד-כיסוי באפליקציה: אחוז מוצרים עם תמונה / גודל / מאומת-מקור.
92. ⬜ לשמור snapshot היסטורי של מדדי-איכות לצפייה במגמה לאורך זמן.
93. ✅ להפיק export אוטומטי (CSV/JSON) ל-stakeholders בכל מהדורה.
94. ✅ לבנות דוח "מה השתנה" בין שני commits (מוצרים נוספו/הוסרו/שונו).
95. ⬜ להוסיף התראה אוטומטית על ירידת-איכות (regression בכיסוי/באחוז-מאומת).
96. ✅ להגדיר KPI-בריאות (אחוז שמות-מאומתים, אחוז-תמונות, אחוז-גודל) ולעקוב אחריהם.
97. ✅ לתעד API מלא לכל פקודות הכלי (`--help` מפורט לכל פקודה).
98. ✅ לכתוב בדיקות-יחידה (pytest) ל-`catalog_qa.py` עצמו מעבר ל-selftest.
99. ⬜ לחבר את source_truth של כל 4 ה-PDFs ולהריץ `truthcheck` על המאגר המלא.
100. ⬜ להגיע ל"אפס שגיאות מאומת" — 100% crosscheck ירוק על כל מוצר בקטלוג, מאומת מול המקור.

---
**בוצע: 1–18, 29, 31–35, 37, 45–46, 56–57, 65–66, 97 (≈34/100).** המשך: גל 5 (58–64) → גל 3 (truthapply 36) → גל 6.
