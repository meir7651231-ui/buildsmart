# Inspection #0010
Stage: frame + operations
Date: 2026-05-21 07:24 UTC
Diff scope: 2 files (+185 / -23)
- app/src/store/app-settings.ts (~145 lines; היה ~85)
- app/src/components/menu/submenu-settings.tsx (+82 — LEAF_BINDINGS עם 22 כניסות + reset)

## Counts
CRITICAL: 0
MAJOR:    0
MINOR:    1

## Findings

### CRITICAL
- (none)

### MAJOR
- (none)

### MINOR
- **accessibility-not-in-legacy-app-settings-object**
  קובץ: `app/src/store/app-settings.ts:38`
  ממצא: בלגאסי `resetSettings()` ב-`index.html:6962-6971` אין `accessibility` ב-appSettings; ה-toggleHighContrast המקורי קורא/כותב רק ל-`document.documentElement.getAttribute('data-contrast')` (DOM-only). כאן הוספתי `accessibility.highContrast: boolean` ל-state ול-localStorage.
  כלל: R6 (legacy-as-spec) / R8 (no invention)
  פעולה: permitted adaptation. הלגאסי לא שמר את ההעדפה בריענון — זה bug בלגאסי. אנחנו משפרים בכך שאנחנו שומרים. ה-Inspector סימן כ"permitted adaptation". לתעד כאן, לא חוסם.

## תיאור הדיף (בעברית)
המשך חיבור ענפים. אחרי תצוגה (6 עלים, INSP-0009), עכשיו עוד 16 עלים מחוברים — סך הכל 22 מתוך ~84.

**הענפים שהתחברו בדיף הזה:**
1. **🔔 התראות** (4 toggles) — `notif.{shipments, deals, budget, orders}`. כל אחד מהפך bool בנפרד; persisted ל-localStorage.
2. **♿ נגישות** (1 toggle) — `accessibility.highContrast`. כותב `data-contrast='high'|'normal'` ל-`<html>` (תואם לפי המקור `index.html:6843`).
3. **🌍 אזור ושפה** (3 selects, 7 עלים): שפה (he/ar/en), יחידות (metric/imperial), מטבע (ils/usd). כל בחירה כותבת attribute מתאים ל-`<html>` ומאוחסנת.
4. **🚚 משלוח ותשלום** (4 עלים): סוג הובלה (small/van/truck) + ברירת מחדל אקספרס (toggle). פריט "אמצעי תשלום" נשאר ללא binding (דורש מסך טופס, יחובר בנפרד).
5. **↺ איפוס** — חוט ל-`resetSettings()` שמחזיר הכל ל-DEFAULTS וסוגר את הדיאל.

**ארכיטקטורת ה-store:**
- ה-`AppSettings` שמרחב מ-`{display}` ל-`{display, notif, region, delivery, accessibility}`.
- כל סטרים `appSettings.value = {...s, ...}` — shallow clone, אין mutation.
- `effect()` יחיד כותב את כל ערכי ה-signal ל-`<html>` (data-theme/data-text-size/data-reduce-motion/data-lang/data-units/data-currency/data-haul/data-express/data-contrast) ו-localStorage. חד-כיווני — אין feedback loop.
- `load()` עם `pick()` helper מסנן ערכי localStorage לערכי enum מותרים בלבד (הגנה מ-corruption).

## אימות התנהגות (playwright)
ריצה headless עם chromium 1194:
- מעבר לעלה "$ דולר" → `data-currency='usd'`, `stored.region.currency='usd'`, leaf is-on על "$ דולר".
- toggle על "עדכוני משלוחים" → `stored.notif.shipments=false`, leaf לא-on.
- לחיצה על "איפוס לברירת מחדל" → `stored` חוזר ל-DEFAULTS (currency='ils', notif.shipments=true, וכו'). הדיאל נסגר.
- כל ה-attributes על `<html>` עודכנו עקבית.

## אימות טקסטי קצה (R6/R8)
כל המפתחות ב-LEAF_BINDINGS אומתו verbatim מול הלגאסי:
- התראות: עדכוני משלוחים · מבצעים והטבות · התראות תקציב · עדכוני הזמנות (`index.html:6824-6829`)
- שפה: עברית · العربية · English (`SETTINGS_LABELS.lang` ב-`6754`)
- יחידות: מטרי (מ׳, ק״ג) · אימפריאלי (`6755`)
- מטבע: ₪ שקל · $ דולר (`6756`)
- הובלה: משלוח קטן · טנדר · משאית (`SETTINGS_LABELS.defaultHaul` ב-`6753`)
- אקספרס: ברירת מחדל — משלוח אקספרס (`6860`)
- נגישות: מצב ניגודיות גבוהה (לשמש) (`6842`)
- DEFAULTS: כל 9 השדות זהים ל-`resetSettings()` הלגאסי ב-`6963-6968`. רק `accessibility.highContrast: false` הוסף — ראה MINOR לעיל.

## Frame checks
- FRM-01 (R1): PASS — 5 ה-FABs ללא שינוי מיקום.
- FRM-02 (R2): PASS — אין overlay חדש.
- FRM-03 (R3): PASS — `<ul class="dial">` נשמר.
- FRM-04 (R4): PASS — leaves with `--on` שומרים `dial__circle` + `dial__label` כשני elements; ה-`--on` משנה רק את העיגול. עוגנים `--active` צובעים שניהם — הבדל ויזואלי מכוון: leaf-on = "הערך הנוכחי", anchor-active = "ירדנו לכאן".
- FRM-05 (R5): PASS — תבנית tap מורחבת: binding → action ותפריט פתוח; ללא binding → close; branch → drill; active anchor → pop; backdrop → full close.
- FRM-06: PASS — אין backdrop חדש.
- FRM-07 (R7): PASS — `MenuSpeedDial` מיובא ב-`app.tsx`; `menuOpen.value` gate בתוקף; `testTabs` ב-`src/test/tests/tabs.tsx` לא תלוי ב-`app-settings`.

## Operations checks
- OPS-01 typecheck: PASS — `tsc -b --noEmit`, אין שגיאות.
- OPS-02 build: PASS — vite + PWA, ~582ms, 38 precache entries, 579.08 KiB.
- OPS-03 regression runner: PASS — `runner.ts`, `regression-store.ts`, `tabs.tsx`, `buttons.ts`, `regression-panel.tsx` כולם קיימים; ה-panel מיובא ב-`manager.tsx`.
- Code-loop scan (L-01..L-08): PASS — ה-`effect()` ב-`app-settings.ts` קורא `appSettings.value` וכותב ל-`document` + `localStorage`; **לא** מכתיב חזרה ל-signal. כל ה-setters משתמשים ב-shallow clone וקוראים אך ורק ב-`onClick` handlers.
- CSS sanity: PASS — אין שינוי CSS בדיף הזה. ה-attributes החדשים (`data-lang`/`data-units`/`data-currency`/`data-haul`/`data-express`/`data-contrast`) זמינים לכללי CSS עתידיים.

## Stuck-loop check
9 דוחות קודמים נבדקו (INSP-0001 — INSP-0009).
- INSP-0001..0005, 0007..0009: אין ממצאים.
- INSP-0002: `spec-fabs-missing-adrs-field`, `spec-invented-features-disclosed` — spec.json, לא בדיף.
- INSP-0004: `tab-id-mismatch-profile-vs-settings` (MINOR) — לא בדיף.
- INSP-0006: `subsub-text-mismatch-delivery` — תוקן בסבב.
ה-MINOR החדש `accessibility-not-in-legacy-app-settings-object` לא חוזר באף דוח קודם.
**No stuck loop.**

## VERDICT: GO

## סטטוס כיסוי עלים
22 / ~84 עלים מחוברים לפעולות אמיתיות.
- ✅ display (6): theme · textSize · reduceMotion
- ✅ notifications (4): כל ה-toggles
- ✅ accessibility (1): highContrast
- ✅ region (7): lang · units · currency
- ✅ delivery (4 מתוך 5): defaultHaul · express (חסר: אמצעי תשלום)
- ✅ reset (1): action
- ⏳ account (4 — דורש מסך טופס)
- ⏳ about (4 — דורש toasts/screens)
- ⏳ delivery.אמצעי תשלום (1 — דורש מסך טופס)
- ⏳ security (10 + 17 sub — דורש מסכי-hub)
- ⏳ support (8 + 9 sub — דורש מסכי-hub)
