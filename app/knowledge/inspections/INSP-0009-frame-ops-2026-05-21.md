# Inspection #0009
Stage: frame + operations
Date: 2026-05-21 07:15 UTC
Diff scope: 6 files (~200 ins / ~25 del)
- app/src/store/app-settings.ts (NEW, ~85 lines)
- app/src/components/menu/submenu-settings.tsx
- app/src/components/menu-speed-dial.tsx
- app/src/styles/tokens.css
- app/src/styles/global.css
- app/src/app.tsx

## Counts
CRITICAL: 0
MAJOR:    0
MINOR:    0

## Findings

### CRITICAL
- (none)

### MAJOR
- (none)

### MINOR
- (none)

## תיאור הדיף (בעברית)
חיבור ענף ה-תצוגה end-to-end — מהעלה הקצה (`בהיר/כהה`, `קטן/בינוני/גדול`, `הפחתת אנימציות`) דרך store + persist עד התצוגה של המסך הראשי.

1. **Store חדש (`app/src/store/app-settings.ts`)**: signal `appSettings: {display:{theme, textSize, reduceMotion}}` עם defaults מ-`resetSettings()` ב-`index.html:6962-6968`. עוטרים: `setTheme`, `setTextSize`, `setReduceMotion`, `toggleReduceMotion`. אפקט יחיד שכותב את ערכי ה-signal ל-`document.documentElement` (data-theme/data-text-size/data-reduce-motion) ול-localStorage תחת `bs.settings.v1`. אין כתיבה חזרה ל-signal — אין feedback loop.

2. **app.tsx** מוסיף `import './store/app-settings'` כדי שה-`effect()` יתבצע באתחול.

3. **tokens.css** מוסיף שני בלוקי כללים: `[data-text-size='small'|'large']` שמכפיל `body { zoom }`, ו-`[data-reduce-motion='true'] *` שמוריד animation/transition durations לכמעט-אפס. הפלטה ל-`[data-theme='dark']` הייתה כבר מוגדרת — לא נגעתי בה.

4. **submenu-settings.tsx**: הוספתי מפת `LEAF_BINDINGS: Record<string, {action, isActive?}>` עם 6 כניסות לענף display (path-key `'group>label>label'`). הוספתי helper `leafKey(group, path, label)`. `SettingsTreeSubmenu` קיבל `pathPrefix: string[]`. בלחיצה על leaf: אם יש binding — מריצים `action()` והתפריט נשאר פתוח כדי שהמשתמש יראה את ההשפעה; אם אין — חוזרים להתנהגות הלגאסי (`closeMenu()`). כשה-binding `isActive()` מחזיר true, ה-leaf מקבל `dial__item--leaf-on` ו-`dial__circle--on` (עיגול בצבע מותג בלבד; ה-pill נשאר לבן — זה ההבדל מ-anchor שצובע גם את ה-pill).

5. **menu-speed-dial.tsx**: שורה אחת — מעביר את ה-`path` הקיים כ-`pathPrefix` ל-`SettingsTreeSubmenu`.

6. **global.css**: `.dial__circle--on { background: var(--brand); color: #fff; }`.

## אימות התנהגות (playwright)
ריצה headless עם chromium 1194 + viewport 414×896:
- start state: `data-theme=light`, `data-text-size=medium`, `data-reduce-motion=false`.
- לאחר לחיצה על "כהה": `data-theme=dark`, `body backgroundColor=rgb(20,23,26)`, leaf "כהה" מקבל `dial__circle--on` עם backgroundColor=rgb(58,158,153) (ה-`--brand` של הפלטה הכהה).
- לאחר לחיצה על "גדול": `data-text-size=large`, `body zoom=1.1`. הקטלוג מציג text מוגדל בצילום.
- לאחר Toggle "הפחתת אנימציות": `data-reduce-motion=true` נשמר ל-localStorage.
- כל המצב מוסר ל-localStorage כ-`{"display":{"theme":"dark","textSize":"large","reduceMotion":false}}` ונטען בריענון.

## Frame checks
- FRM-01 (R1): PASS — 5 ה-FABs ללא שינוי.
- FRM-02 (R2): PASS — אין overlay חדש; ה-zoom פועל על body ולא יוצר window/overlay.
- FRM-03 (R3): PASS — `<ul class="dial">` נשמר.
- FRM-04 (R4): PASS — leaves with `--on` שומרים `dial__circle` + `dial__label` כשני elements נפרדים; ה-`--on` משנה רק את העיגול. ה-`--active` (אנקור breadcrumb) צובע את שניהם, וזה הבדל ויזואלי מכוון: leaf-on = "הערך הנוכחי", anchor-active = "ירדנו לכאן".
- FRM-05 (R5): PASS — leaf with binding: action + מנו פתוח; leaf without: legacy close; branch: drill; active anchor: pop; backdrop: full close. הרחבה מורשית של דפוס ה-tap.
- FRM-06: PASS — אין backdrop חדש.
- FRM-07 (R7): PASS — `MenuSpeedDial` עדיין מיובא ב-`app.tsx`; `menuOpen.value` בתוקף; `testTabs` ב-`src/test/tests/tabs.tsx` לא תלוי ב-`app-settings`.
- R6/R8: PASS — `appSettings` schema תואם את הלגאסי ב-`index.html:6749` + `resetSettings()` ב-`6962-6968`. כל ה-labels (בהיר/כהה/קטן/בינוני/גדול/הפחתת אנימציות) אומתו ב-INSP-0007/0008.

## Operations checks
- OPS-01 typecheck: PASS — `tsc -b --noEmit`, אין שגיאות.
- OPS-02 build: PASS — vite + PWA, ~637ms, 38 precache entries, 575.40 KiB.
- OPS-03 regression runner: PASS — כל הקבצים קיימים; `RegressionPanel` מיובא ב-`manager.tsx`.
- Code-loop scan (L-01..L-08): PASS — ה-`effect()` ב-`app-settings.ts` קורא `appSettings.value` וכותב ל-document + localStorage; **לא** מכתיב חזרה ל-signal. אין feedback loop. כל ה-setters נקראים אך ורק בתוך `onClick` handlers.
- CSS sanity: PASS — `zoom` נתמך ב-WebKit/Blink; ה-`!important` ב-reduce-motion חיוני כי כללים ספציפיים יותר מנצחים אחרת.

## Stuck-loop check
8 דוחות קודמים נבדקו (INSP-0001 — INSP-0008).
- INSP-0001: אין ממצאים.
- INSP-0002: `spec-fabs-missing-adrs-field`, `spec-invented-features-disclosed` — spec.json-related.
- INSP-0003: אין ממצאים.
- INSP-0004: `tab-id-mismatch-profile-vs-settings` (MINOR) — לא רלוונטי.
- INSP-0005: אין ממצאים.
- INSP-0006: `subsub-text-mismatch-delivery` — תוקן בסבב.
- INSP-0007: אין ממצאים.
- INSP-0008: אין ממצאים.
אין finding-id שחוזר ב-2+ דוחות.
**No stuck loop.**

## VERDICT: GO
