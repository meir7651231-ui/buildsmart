# Inspection #0007
Stage: frame + operations
Date: 2026-05-21 02:44 UTC
Diff scope: 4 files
- app/src/components/menu-speed-dial.tsx (+45 / -10)
- app/src/components/menu/submenu-settings.tsx (+165 / -26, refactored data model)
- app/src/store/app-store.ts (+16 / -2)
- app/src/styles/global.css (+5 / 0 — max-height + overflow on `.dial`)

## Counts
CRITICAL: 0
MAJOR:    0
MINOR:    0

## Findings

### CRITICAL
- (none)

### MAJOR
- (none)

### MINOR
- (none)

## תיאור הדיף (בעברית)
דרגה רביעית של drill-down — צלילה מלאה עד שמות העלים האחרונים.
- מבנה הנתונים הוחלף מ-`Record<id, string[]>` (שמות שטוחים) ל-`Record<id, SubRow[]>` כש-`SubRow = {label, children?}`.
- 8 שורות תת-תפריט (level 2) קיבלו `children` — האפשרויות/הנכדים שלהן.
- שמות העלים נשאבו משלושה מקומות בקוד המקורי:
  - `SETTINGS_LABELS` ב-`index.html:6750-6757` — לאופציות של select (ערכת נושא / גודל טקסט / סוג הובלה / שפה / יחידות / מטבע)
  - `openSecurityHub()` ב-`index.html:21752-21762` — 10 אריחי "מרכז האבטחה"
  - `openServiceHub()` ב-`index.html:22081-22090` — 8 אריחי "מרכז השירות"
- סך הכל נוספו **33 עלים** ב-level 3, פרושים על 8 ענפים.
- בלחיצה על שורת תת-תפריט: אם יש לה `children` — נכנס לרמה 3 (`setSubRow(label)`); אחרת — סוגר את הדיאל (עלה).
- ברמה 3, הדיאל מציג 3 עוגנים פעילים בתחתית (הגדרות + קבוצה + שורת התת-תפריט) ומעליהם עולים העלים.
- ניווט: עוגן עליון = חזרה רמה אחת; עוגן "הגדרות" = קפיצה לרמה 0; backdrop = סגירה מלאה.
- CSS: הוספתי `max-height` + `overflow-y:auto` ל-`.dial` כדי שהמחסנית הכבדה ביותר (אבטחה: 3 עוגנים + 10 עלים = 13 פריטים) תסקרול במכשירים קטנים. הסקרולבר מוסתר.

## בדיקת טקסטים (R6/R8) — אומת ב-Inspector
- display.theme: `בהיר`, `כהה` ✓
- display.textSize: `קטן`, `בינוני`, `גדול` ✓
- delivery.defaultHaul: `משלוח קטן`, `טנדר`, `משאית` ✓
- region.lang: `עברית`, `العربية`, `English` ✓
- region.units: `מטרי (מ׳, ק״ג)`, `אימפריאלי` ✓
- region.currency: `₪ שקל`, `$ דולר` ✓
- security.10: `אימות דו-שלבי`, `הרשאות גישה`, `כניסה ביומטרית`, `יומן ביקורת`, `הרשאת מיקום`, `נעילת הפעלה`, `הצפנת נתונים`, `היסטוריית כניסות`, `ניהול מכשירים`, `בקרת פרטיות` ✓
- support.8: `מוקד תמיכה`, `צ׳אטבוט`, `דיווח על באג`, `המרת מידות`, `מחשבון כמויות`, `סנכרון יומן`, `לוח דרושים`, `סיור היכרות` ✓
- עלי-עלים שנותרו עלים (אין `children`): account (4), notifications (4), display.reduceMotion, accessibility (1), delivery [express + payment] (2), about (4) — אישרו ✓

## Frame checks
- FRM-01 (R1): PASS — 5 ה-FABs ללא שינוי מיקום.
- FRM-02 (R2): PASS — אין overlay חדש; `dial__backdrop` בשימוש חוזר.
- FRM-03 (R3): PASS — `<ul class="dial">` עם `dial__item`; אין drawer/modal/sheet.
- FRM-04 (R4): PASS — שלושת עוגני הברד-קרמב (settings+group+subRow) ועלי-העלים שומרים `dial__circle` + `dial__label` כשני elements נפרדים. ה-modifiers (`--active`, `--sub`) רק משנים סטייל.
- FRM-05 (R5): PASS — דפוס "Tap-active-to-back": L3 → L2 (subRow anchor), L2 → L1 (group anchor), L1 → L0 (settings anchor); backdrop = full close.
- FRM-06: PASS — אין backdrop חדש.
- FRM-07 (R7): PASS — `MenuSpeedDial` עדיין מיובא ב-`app.tsx`; `menuOpen.value` gate בתוקף; `testTabs` לא תלוי ב-internal ids.

## Operations checks
- OPS-01 typecheck: PASS — `tsc -b --noEmit`, אין שגיאות.
- OPS-02 build: PASS — vite + PWA, ~574ms, 38 precache entries, 572 KiB.
- OPS-03 regression runner: PASS — `runner.ts`, `regression-store.ts`, `tabs.tsx`, `buttons.ts` קיימים; `regression-panel` מיובא ב-`manager.tsx`.
- Code-loop scan (L-01..L-08): PASS — אין `useEffect`/`effect`/setter בגוף ה-render/`while(true)`. כל ה-setters (`setMenuTab`, `setSettingsGroup`, `setSubRow`, `closeMenu`) נקראים אך ורק בתוך `onClick` handlers.
- CSS sanity: PASS — `.dial` `max-height` ו-`overflow-y:auto` תקפים; `scrollbar-width: none` + `::-webkit-scrollbar { display: none }` מסתירים את הסקרולבר; סלקטורי `.dial*` הקיימים לא הופרו.

## Stuck-loop check
6 דוחות קודמים נבדקו (INSP-0001 — INSP-0006).
- INSP-0001: אין ממצאים.
- INSP-0002: `spec-fabs-missing-adrs-field`, `spec-invented-features-disclosed` — spec.json-related, לא בדיף זה.
- INSP-0003: אין ממצאים.
- INSP-0004: `tab-id-mismatch-profile-vs-settings` (MINOR) — לא רלוונטי.
- INSP-0005: אין ממצאים.
- INSP-0006: `subsub-text-mismatch-delivery` — נמצא ותוקן בסבב אחד.
אין finding-id שחוזר ב-2+ דוחות.
**No stuck loop.**

## VERDICT: GO
