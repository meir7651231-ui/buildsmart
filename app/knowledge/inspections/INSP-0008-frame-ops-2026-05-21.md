# Inspection #0008
Stage: frame + operations
Date: 2026-05-21 02:59 UTC
Diff scope: 3 files (~200 ins / 132 del)
- app/src/components/menu-speed-dial.tsx
- app/src/components/menu/submenu-settings.tsx (data model refactored)
- app/src/store/app-store.ts

## Counts
CRITICAL: 0
MAJOR:    0
MINOR:    0

## Findings

### CRITICAL
- (none)

### MAJOR
- (none)

### MINOR
- (none)

## תיאור הדיף (בעברית)
דרגה חמישית של עומק עץ ה-drill + ריפקטור של מודל ה-state.
- **מודל הנתונים**: `SubRow = {label, children?: string[]}` הוחלף ב-`Node = {label, children?: Node[]}` — רקורסיבי, תומך בכל עומק.
- **6 ענפי Level 4 חדשים** (26 שמות-עלים נוספים):
  - אבטחה → מרכז האבטחה → **הרשאות גישה** → 5 תפקידי RBAC (קבלן/מנהל מערכת/ספק·חנות/שליח/עובד) · `index.html:21812-21813`
  - אבטחה → מרכז האבטחה → **נעילת הפעלה** → 4 timeouts (5/15/30/60 דק׳) · `index.html:21922`
  - אבטחה → מרכז האבטחה → **הצפנת נתונים** → 4 שורות סטטוס (TLS/local/hash/cloud) · `index.html:21952-21957`
  - אבטחה → מרכז האבטחה → **בקרת פרטיות** → 4 toggles (analytics/location/marketing/crashReports) · `index.html:22018-22023`
  - שירות → מרכז השירות → **מחשבון כמויות** → 3 מצבים (אריחים/צבע/בטון) · `index.html:22289-22293`
  - שירות → מרכז השירות → **סיור היכרות** → 6 שלבי tour (מסך הבית→מוכנים!) · `index.html:22374-22381`
- **state חדש**: `menuActiveSubRow: string | null` נמחק; הוחלף ב-`menuActiveSettingsPath: string[]` (path של labels לכל עומק).
  - `pushSettingsPath(label)`: צלילה צעד נוסף.
  - `popSettingsPathTo(depth)`: חזרה לרמה ספציפית.
  - `closeMenu` / `toggleMenu` / `setMenuTab(null)` / `setSettingsGroup` כולם מנקים את ה-path.
- **רנדור**: שני הקומפוננטים הישנים (`SettingsSubSubmenu`, `SettingsLeafSubmenu`) הוחלפו ב-`SettingsTreeSubmenu({group, nodes})` יחיד. ה-helper `walkSettings(group, path)` מחזיר `{anchors, current}` — רשימת העוגנים שצללו אליהם ורשימת ה-nodes שצריך לצייר מעל.
- **רנדור ה-SettingsLevel ב-menu-speed-dial.tsx**: צועד לפי `path`, פולט עוגן לכל צעד-צלילה (כל אחד עם `popSettingsPathTo(i)` ב-onClick שלו), ואז `<SettingsTreeSubmenu>` מעל.

## אימות טקסטי הקצה (R6/R8) — אישר Inspector
- RBAC roles: קבלן · מנהל מערכת · ספק / חנות · שליח · עובד ✓
- Session timeouts: 5 דק׳ · 15 דק׳ · 30 דק׳ · 60 דק׳ ✓
- Encryption rows: תקשורת מוצפנת (HTTPS/TLS) · נתונים מקומיים מוגנים · סיסמאות מאוחסנות כ-Hash · גיבוי מוצפן בענן ✓
- Privacy toggles: שיתוף נתוני שימוש · שירותי מיקום · התאמת תוכן שיווקי · שליחת דוחות תקלה ✓
- QtyCalc modes: אריחים · צבע · בטון ✓
- Onboarding tour: מסך הבית · הזמנה · תקציב · משימות ואתר · מועדון BuildSmart · מוכנים! ✓
דגלי emoji בקוד המקורי (📊📍📣🐛🔒🗄️🔑☁️) הושמטו עקבית כי ה-SVG בעיגול הדיאל ממלא את אותו תפקיד.

## Frame checks
- FRM-01 (R1): PASS — 5 ה-FABs ללא שינוי מיקום.
- FRM-02 (R2): PASS — אין overlay חדש; `dial__backdrop` בשימוש חוזר.
- FRM-03 (R3): PASS — `<ul class="dial">` עם `dial__item`; אין drawer/modal/sheet.
- FRM-04 (R4): PASS — כל העוגנים (group + N path anchors) ופריטי `SettingsTreeSubmenu` שומרים `dial__circle` + `dial__label` כשני elements נפרדים.
- FRM-05 (R5): PASS — דפוס "Tap-active-to-back": כל עוגן `anchors[i]` קורא `popSettingsPathTo(i)` → חוזר לעומק i; עוגן הקבוצה → `setSettingsGroup(null)` → רמה 1; backdrop → סגירה מלאה.
- FRM-06: PASS — אין backdrop חדש.
- FRM-07 (R7): PASS — `MenuSpeedDial` עדיין מיובא ב-`app.tsx`; `menuOpen.value` gate בתוקף; ה-registry לא תלוי במבנה ה-Node הפנימי.

## Operations checks
- OPS-01 typecheck: PASS — `tsc -b --noEmit`, אין שגיאות.
- OPS-02 build: PASS — vite + PWA, ~622ms, 38 precache entries, 573 KiB.
- OPS-03 regression runner: PASS — `runner.ts`, `regression-store.ts`, `tabs.tsx`, `buttons.ts`, `regression-panel.tsx` כולם קיימים; ה-panel מיובא ב-`manager.tsx`.
- Code-loop scan (L-01..L-08): PASS — אין `useEffect`/`effect`/setter בגוף ה-render. `walkSettings` היא פונקציה טהורה הקוראת signals במהלך הרנדור — לא setter. כל ה-setters (`pushSettingsPath`/`popSettingsPathTo`/`setSettingsGroup`/`closeMenu`) נקראים אך ורק בתוך `onClick` handlers.
- CSS sanity: N/A — אין שינויי CSS בדיף הזה.

## אימות ויזואלי (playwright)
6 צילומי מסך נוצרו עם Chromium 1194 על iPhone XR viewport (414×896, 2x DPR):
1. רמה 0 — FAB תפריט + חיפוש בלבד.
2. רמה 1 — אחרי הקשה על FAB תפריט: 5 הטאבים.
3. רמה 2 — אחרי הקשה על הגדרות: 10 שורות הגדרות + עוגן הגדרות פעיל.
4. רמה 3 — אחרי הקשה על אבטחה והרשאות: מרכז האבטחה + 2 עוגנים פעילים.
5. רמה 4 — אחרי הקשה על מרכז האבטחה: 10 פריטי hub + 3 עוגנים פעילים.
6. רמה 5 — אחרי הקשה על בקרת פרטיות: 4 שמות toggle + 4 עוגנים פעילים.
המבנה תקין: עוגנים בצבע מותג בתחתית, עלים לבנים מעל, כל הטקסט תואם למקור.

## Stuck-loop check
7 דוחות קודמים נבדקו (INSP-0001 — INSP-0007).
- INSP-0001: אין ממצאים.
- INSP-0002: `spec-fabs-missing-adrs-field`, `spec-invented-features-disclosed` — spec.json-related, לא בדיף.
- INSP-0003: אין ממצאים.
- INSP-0004: `tab-id-mismatch-profile-vs-settings` (MINOR) — לא רלוונטי.
- INSP-0005: אין ממצאים.
- INSP-0006: `subsub-text-mismatch-delivery` — תוקן בסבב אחד.
- INSP-0007: אין ממצאים.
אין finding-id שחוזר ב-2+ דוחות.
**No stuck loop.**

## VERDICT: GO
