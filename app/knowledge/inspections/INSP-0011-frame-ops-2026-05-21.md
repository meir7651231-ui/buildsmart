# Inspection #0011
Stage: frame + operations
Date: 2026-05-21 07:49 UTC
Diff scope: 5 files (+40 / -0)
- app/src/store/toast-store.ts (NEW, 14 lines)
- app/src/components/toast.tsx (NEW, 10 lines)
- app/src/app.tsx (+1 import, +1 JSX mount)
- app/src/components/menu/submenu-settings.tsx (+4 LEAF_BINDINGS, +1 import)
- app/src/styles/global.css (+26 lines: .toast + @keyframes toast-in)

## Counts
CRITICAL: 0
MAJOR:    0
MINOR:    0

## Findings

### CRITICAL — (אין)
### MAJOR — (אין)
### MINOR — (אין)

## תיאור הדיף (בעברית)
חיבור 4 עלים מסעיף "מידע" (about) — מ-22 ל-26 עלים מחוברים מתוך ~84.

הדיף מוסיף מנגנון toast: notification קצרה שמופיעה 3.2 שניות ונעלמת אוטומטית, pointer-events:none, centered above FABs, z-index 200.

**toast-store.ts**: `toastMsg: Signal<string | null>`. `showToast()` מאפס timer קיים, מציב ערך, ומתזמן null עם setTimeout. אין effect(). אין feedback loop.

**toast.tsx**: קורא `toastMsg.value`, מחזיר null כשאין הודעה, מרנדר `<div class="toast">` עם aria-live="polite", aria-atomic="true" כשיש.

**app.tsx**: מוסיף `<Toast />` בתוך `.screen` div אחרי `<ProductSheet />`.

**LEAF_BINDINGS** (submenu-settings.tsx) — 4 כניסות חדשות, verbatim מ-legacy index.html:6870-6876:
- `'about>גרסה'` → showToast('BuildSmart 1.0 · אב-טיפוס')
- `'about>תנאי שימוש'` → showToast('תנאי השימוש — יוצגו בגרסה המלאה')
- `'about>מדיניות פרטיות'` → showToast('מדיניות הפרטיות — תוצג בגרסה המלאה')
- `'about>יצירת קשר'` → showToast('תמיכה — support@buildsmart.demo')

אין isActive על none מהם (הם informational, לא selects/toggles). תפריט נשאר פתוח (pattern עקבי).

**CSS**: `.toast` — position fixed, bottom calc(safe + 96px), left 50%, translateX(-50%), max-width 280px, pointer-events none, @keyframes toast-in (opacity+translateY).

## אימות R6/R8
- 'BuildSmart 1.0 · אב-טיפוס' ← setLink('גרסה','BuildSmart 1.0 · אב-טיפוס',null) ✓
- 'תנאי השימוש — יוצגו בגרסה המלאה' ← toast() at index.html:6873 ✓
- 'מדיניות הפרטיות — תוצג בגרסה המלאה' ← toast() at index.html:6874 ✓
- 'תמיכה — support@buildsmart.demo' ← toast() at index.html:6875 ✓
4/4 verbatim.

## Frame checks
- FRM-01 (R1): PASS — Toast pointer-events:none, z-index 200. FABs עם z-index נמוך יותר; לא נדחקים.
- FRM-02 (R2): PASS — Toast לא overlay מלאה; non-blocking, auto-dismiss.
- FRM-03 (R3): PASS — Toast לא drawer/sheet/modal; היא notification מחוץ לדיאל.
- FRM-04 (R4): PASS — About leaves ב-dial שומרים circle+label נפרדים; אין is-on (informational).
- FRM-05 (R5): PASS — About leaves מריצות showToast ותפריט נשאר פתוח; עקבי עם pattern.
- FRM-06: PASS — אין backdrop חדש.
- FRM-07 (R7): PASS — Toast מחזיר null כשאין הודעה; לא משפיע על persona views.

## Operations checks
- OPS-01 typecheck: PASS — `tsc -b --noEmit`, אין שגיאות.
- OPS-02 build: PASS — vite + PWA, ~655ms, 38 precache entries, 580.20 KiB.
- OPS-03 regression: PASS — כל הקבצים קיימים; panel מיובא ב-manager.tsx.
- Code-loop scan (L-01..L-08): PASS — toast-store.ts: אין effect(); setTimeout callback כותב `toastMsg.value = null` — לא מפעיל את toast() שוב, אין loop. toast.tsx: קורא בלבד, אין setter.

## אימות התנהגות (playwright)
- 'גרסה' → toast.textContent = 'BuildSmart 1.0 · אב-טיפוס' ✓
- 'תנאי שימוש' → toast.textContent = 'תנאי השימוש — יוצגו בגרסה המלאה' ✓
- 'יצירת קשר' → toast.textContent = 'תמיכה — support@buildsmart.demo' ✓
- menuOpen בעקבות tap = true (role="menu" עדיין ב-DOM) ✓

## Stuck-loop check
10 דוחות קודמים (INSP-0001..0010). אין finding-id שחוזר ב-2+ דוחות.
**No stuck loop.**

## VERDICT: GO

## סטטוס כיסוי עלים
26 / ~84 עלים מחוברים:
✅ display (6) · notifications (4) · accessibility (1) · region (7) · delivery (4) · about (4) · reset (1)
⏳ account (4) · delivery.payment (1) · security hub (27) · support hub (17)
