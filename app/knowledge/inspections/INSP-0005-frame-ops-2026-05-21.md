# Inspection #0005
Stage: frame + operations
Date: 2026-05-21 02:24 UTC
Diff scope: 4 files
- app/src/components/menu-speed-dial.tsx (+86 / -22)
- app/src/store/app-store.ts (+10 / 0)
- app/src/styles/global.css (+30 / 0)
- app/src/components/menu/submenu-settings.tsx (NEW, 157 lines)

## Counts
CRITICAL: 0
MAJOR:    0
MINOR:    0

## Findings

### CRITICAL
- (none)

### MAJOR
- (none)

### MINOR
- (none)

## תיאור הדיף (בעברית)
הוספת drill-down בתפריט התחתון:
- כשהדיאל פתוח ומקישים על "הגדרות" — `menuActiveTab` מתעדכן ל-`'settings'` במקום סגירה.
- הדיאל מצייר מחדש: רק הטאב הפעיל "מעוגן" בתחתית (עיגול וכיתוב בצבע מותג), ומעליו 10 פריטי תת-תפריט עולים בסדר הקריאה של הקוד המקורי (`renderSettings` ב-`index.html:6806`): חשבון → התראות → תצוגה → נגישות → אבטחה והרשאות → שירות ותמיכה → משלוח ותשלום → אזור ושפה → מידע → איפוס.
- הקשה שנייה על הטאב הפעיל מחזירה ל-5 הטאבים.
- 4 הטאבים האחרים (בית/קטלוג/הפרויקטים/רכש) עדיין סוגרים את הדיאל — תתי-תפריט שלהם יבנו בנפרד.
- לחיצה על כל אחד מ-10 פריטי ההגדרות כרגע רק קוראת `closeMenu()`. שמות בלבד, לפי בקשת המשתמש.

## Frame checks
- FRM-01 (R1): PASS — אין שינוי מיקום של חמשת ה-FABs; ה-CSS של `.dial` לא נגעה במיקום (`bottom`, `inset-inline-end` ללא שינוי).
- FRM-02 (R2): PASS — אין overlay חדש; `dial__backdrop` קיים בשימוש חוזר (35% שחור, blur 2px — מתחת לסף 45%/3px).
- FRM-03 (R3): PASS — `<ul class="dial">` עם `dial__item` נשמר; אין drawer/modal/sheet.
- FRM-04 (R4): PASS — כל פריט (כולל הסאב והפעיל) שומר `dial__circle` + `dial__label` כשני elements נפרדים. ה-modifiers `--active`/`--sub`/`--danger` רק משנים סטייל, לא מאחדים אלמנטים.
- FRM-05 (R5): PASS — דפוס FAB→דיאל→תת-דיאל; הקשה על טאב פעיל = חזרה, הקשה על תת-פריט = פעולה (כרגע סגירה).
- FRM-06: PASS — אין backdrop חדש בכלל.
- FRM-07 (R7): PASS — `MenuSpeedDial` עדיין מיובא ב-`app.tsx`; `menuOpen.value` gate בתוקף; `testTabs` ב-`src/test/tests/tabs.tsx` לא נשען על ה-ids הפנימיים של פריטי הדיאל.
- R6 (legacy-as-spec): PASS — 10 השמות בסדר המקור (renderSettings index.html:6806). אין המצאות.
- R8 (no invention): PASS — לא נוספו פריטים שאינם בקוד המקורי.

## Operations checks
- OPS-01 typecheck: PASS — `tsc -b --noEmit`, אין שגיאות.
- OPS-02 build: PASS — vite + PWA, 562–574ms, 38 precache entries, ~568 KiB.
- OPS-03 regression runner: PASS — `src/test/runner.ts`, `src/store/regression-store.ts`, `src/test/tests/tabs.tsx`, `src/test/tests/buttons.ts`, `src/components/regression/regression-panel.tsx` כולם קיימים; הפאנל עדיין מיובא ב-`src/views/manager.tsx`. ריצה בדפדפן לא בוצעה (לפי ההנחיה).
- Code-loop scan (L-01..L-08): PASS — אין `useEffect`/`effect`/setter בגוף ה-render/`while(true)` בקבצים שהשתנו. הסטייטים `setMenuTab`/`closeMenu` נקראים רק ב-`onClick`.
- CSS sanity: PASS — סלקטורים חדשים well-formed (`.dial__item--sub`, `.dial__item--danger`, `.dial__circle--active`, `.dial__label--active`); סלקטורי `.dial` הקיימים לא הופרו.

## Stuck-loop check
4 דוחות קודמים נבדקו (INSP-0001 — INSP-0004).
- INSP-0001: אין ממצאים.
- INSP-0002: `spec-fabs-missing-adrs-field`, `spec-invented-features-disclosed` — שניהם spec.json-related, לא רלוונטיים לדיף הזה (אין שינוי ב-spec.json).
- INSP-0003: אין ממצאים.
- INSP-0004: `tab-id-mismatch-profile-vs-settings` MINOR — id פנימי `'settings'` מול `data-tab="profile"` במקור. בדיף הנוכחי השארנו `'settings'` (סדר רענון שמות אינו חלק מהמשימה הנוכחית); אין עליה לרמת חומרה, וזה לא חוזר תחת ID שונה.
אין finding ID שחוזר ב-2+ דוחות.
**No stuck loop.**

## VERDICT: GO
