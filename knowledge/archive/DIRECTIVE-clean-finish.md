# הנחיה: לסגור את Clean — פיננסים-4 + שלב-5 (הוכחת "שני-לינקים · קוד-אחד")

> **ביצוע:** `claude/whats-happening-LyY9G` · **דחיפה: רק על "תדחוף".**
> **עקרונות:** additive · מאחורי דגל/פרופיל · **האתר-החי לא-נגע** · שער-ירוק · **בייטים-לא-פרוזה**.
> זו ההנחיה שסוגרת את הצד-הצי של תוכנית-Clean. בסוף: הבעלים פותח **2 לינקים חיים** מאותו קוד.

---

## משימה 1 · לסגור **פיננסים-4** (הלוז שדולג 4 פעמים — אל תדלג שוב!)
ב-`app_flutter/lib/screens/finance_hub_sheets.dart` — 4 ערכים עדיין **מזויפים-כאמת** (בלי חיווט, בלי תווית):
- **ROI** `:1095` (`roiPct` = `total*1.42` → **תמיד 42%**)
- **קבלני-משנה** `:632`/`:669` (`kSubcontractors` const)
- **מדד-בנייה** `:488`/`:490` (`kBuildIndex` const)
- **חשבונית** (`kInvoiceTotal`)

**תקן כמו שאר הסריקה:** חווט ל-`financeRepositoryProvider`, **או** תן תווית-"(דמו)" בדיוק בסגנון שכבר קיים (FX `:1622` / תגמולים) — `_ServerNote('⚙️ בפרודקשן: … מהשרת — כאן דמו')`.
**DoD:** אפס ערך-פיננסי שמתחזה-לחי בלי חיווט/תווית.

---

## משימה 2 · שלב-5 — הוכחת "שני-לינקים · קוד-אחד" (הרגע שהבעלים רואה)

### 2א · להפוך את ה-Clean ל**חי** (לינק אמיתי, לא רק build-proof)
לאטמט את הפריסה: **workflow/CI** (או הרצה מתועדת) שבונה `APP_PROFILE=clean` ופורס ל**ערוץ-Firebase-Hosting נפרד** (`firebase hosting:channel:deploy clean`) — **האתר-הראשי-החי לא-נגע** — ומוציא את **הלינק בפלט-ה-run**. הבעלים פותח → רואה "בנייה חכמה Clean".

### 2ב · חברת-דמו #2
פרופיל/config שני: **AppBrand שם+מיתוג שונה + קטלוג-מוחלף** (דרך seam 3.1c). פרוס ל**ערוץ שני נפרד** → **לינק שני**.

### 2ג · ההוכחה
**שני לינקים חיים בו-זמנית, מאותו commit** — ה-`MaorClean → MaorHachesed` שלנו. תעד את 2 הלינקים ב-`LAUNCH_READINESS`.

---

## ⚠️ גבול-יושר (קריטי לשלב-5)
- Clean-v1 יכול לשאת קטלוג-גנרי אם החלפת-הקטלוג-המלאה (3.1c) עוד לא בשלה —
  **אבל חברה #2 *חייבת* קטלוג + מיתוג שונים גלויים.** אם שני הלינקים נראים זהים → **לא הוכחנו כלום.** ההבדל הנראה-לעין הוא כל הפואנטה.

## הגנות (לא-שובר-קיים)
הכל מאחורי דגל/פרופיל · **האתר-הראשי לא-נגע לרגע** · שער-ירוק · **mutation-verify** לכל טסט-חדש · הישן ל-rollback.

## DoD (הוכחה בבייטים)
פיננסים-4 סגור (אפס-מתחזה) · **2 לינקים חיים** (Clean + חברה#2) מאותו קוד, עם **הבדל נראה-לעין** (שם+קטלוג) · מתועד ב-LAUNCH_READINESS · הקיים זהה-בייטים · שער ירוק.
