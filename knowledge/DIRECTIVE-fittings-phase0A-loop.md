# 🔁 הנחיה — מנוע-הקטלוג-3D: פאזה 0+A + **מצב-לולאה-רציף**

> **הקשר:** slice-1 נחת ואומת ירוק (Protocol Enforcement run 783 + כל הצינורות). עכשיו בונים את מנוע-הקטלוג-3D לפי `knowledge/CATALOG-3D-100-STEPS.md` (ענף `nice-volta`), **החל מפאזה 0+A**, ב**לולאה רציפה** — לא עוצרים לאישור אחרי כל פרוסה.

---

## מה בונים עכשיו (פאזה 0 → A · PP-R קצה-לקצה)

**פאזה 0 (1–12) — פורט + מיפוי, בלי נגיעה בחי:**
- מודול `lib/features/fittings/` + דגלי `kFittingEngine*` **default-OFF** + רישום שער #124.
- פורט `generate(family,od)→dims` מ-`pure_engine.py` → Dart טהור (בסיס אוניברסלי + חוקי-צורה ל-10 המשפחות).
- `familyOf(product)` + `odOf(product)` (זיהוי >95% על פולירול+חוליות).
- **🔑 golden-test 1:1 מול `pure_engine.py`** (627 מוצרים · רצפת ~1.3 מ״מ).

**פאזה A (13–26) — המזריע (הוואו · סיכון-אפס):**
- `familySpecFor(product)→VerifiedSpec?` (golden ⊇ `polyrollSpecFor`).
- `registerFamilySpecs()` דרך **`putIfAbsent` בלבד**, חיווט ב-`main.dart:260` אחרי פולירול.
- **🔑 חוליות: 789 SKU · 0%→~100% מחברים.** כיסוי מותג-כללי (כל מותג-עתידי מקבל specs מיד).
- מבחן-keystone: אינסטלציה **byte-identical** כשהדגל כבוי.

**משפחה ראשונה: PP-R** (כבר יש `plumbing_trade_seed`).

## דרישת-הקטלוג (מודל "מעלים קובץ → מתחבר לבד")
כל שורת-מוצר חייבת לשאת: **{משפחה, מידת-מפתח (OD/DN)}**.
- יש → נגזר בחינם. חסר → **ממלאים דאטה** (זול), לא ציור-3D.
- ~1,623 מתוך 1,867 כבר עם `verified_specs`. **דוח-כיסוי** בתחילת פאזה A: כמה מוצרים מכוסים, מה חסר.

---

## 🔁 פרוטוקול-הלולאה (החדש — זה העיקר)

```
לולאה:
  1. קח את הפרוסה הבאה שלא-נעשתה מ-CATALOG-3D-100-STEPS.md (התחלה: שלב 1)
  2. בנה: מגודר-דגל · keystone byte-identical · putIfAbsent · golden-מול-Python
  3. כל השערים המקומיים עוברים
  4. נחת → CI חייב להיות ירוק (Protocol Enforcement + deploys) — אותו רף כמו slice-1
  5. ירוק? → סמן "נעשה", חזור ל-1 עם הפרוסה הבאה. **בלי לחכות לאישור.**
  6. אדום / חסום / החלטת-בעלים / אותו-כשל-פעמיים? → עצור, דווח, המתן
```

**ממשיך לבד (בלי לשאול):** כל פרוסה טכנית/מנוע שעוברת ירוק → מיד הבאה.

**עוצר-ושואל רק ב:**
- חסם-טכני אמיתי (אי-אפשר להתקדם).
- **החלטת-בעלים:** טקסט משפטי · כסף/עלות · חשבונות חיצוניים · כל דבר בלתי-הפיך פונה-למשתמש · סיכון-keystone.
- **אותו כשל-שורש פעמיים (P-01):** אל תנסה שלישית — עצור ושאל.

**מדווח (בלי להציף):** שורת-התקדמות אחת בכל **אבן-דרך 🔑/✅** בלבד (לא כל שלב), מאומתת — כמו דוח-ה-CI-ירוק. לא commit-by-commit.

**נקודות-עצירה טבעיות (החלטת-בעלים צפויה):**
- סוף פאזה 0 (שלב 12) — המנוע מוכיח ליבה. *(דיווח, ממשיך)*
- **סוף פאזה A (שלב 26) — חוליות מתחברת. ← אבן-הדרך הגדולה הראשונה.** *(דיווח)*
- פאזה C (שלב 43/49) — הטמעת-3D בכרטיס-החי + בחירת-package-3D = **החלטת-מוצר → עצור ושאל.**

---

## שומרים קבועים (חלים בכל פרוסה בלולאה)
- **keystone:** אינסטלציה byte-identical — רק `putIfAbsent`, לעולם לא דורסים. `_authoredConfigOf:212` נשאר null עד פאזה E.
- `kFittingEngine*` **default-OFF** · רישום שער ב-`GATE_REGISTRY` באותו commit.
- golden-מול-`pure_engine.py` ירוק **לפני** כל חיווט · answer-equivalent (לא byte) לנתון-נגזר.
- ריתוך/בטיחות → **caveat-יצרן חובה**, אין ערך-מחייב בלי אימות.
- ממשל-זונות: דאטה=קטלגן · מסך+3D=מקבץ · ידע=פרוטוקוליסט. אין drop חוצה-זונה בלי תיאום.

## אימות (התפקיד שלי)
כל אבן-דרך — אני מאמת CI-ירוק אמיתי בעצמי (כמו slice-1) לפני שהיא נחשבת "נעשתה". דיווח לא-מאומת ≠ ✅.

---

## ✂️ בלוק להעתקה לנחיל

━━━━━━━━━━━━━━━━━━
משימה: בנה את מנוע-הקטלוג-3D לפי `knowledge/CATALOG-3D-100-STEPS.md` (ענף nice-volta), החל מפאזה 0, ב-**מצב-לולאה-רציף**.

הלולאה: קח פרוסה → בנה (מגודר `kFittingEngine*` OFF · keystone אינסטלציה byte-identical · `putIfAbsent` בלבד · golden 1:1 מול `pure_engine.py`) → כל השערים + CI ירוק (Protocol Enforcement + deploys) → **ירוק? עבור מיד לפרוסה הבאה בלי לחכות לאישור** → חזור.

עצור-ושאל רק ב: חסם-טכני אמיתי · החלטת-בעלים (משפטי/כסף/חשבונות/בלתי-הפיך/keystone) · אותו כשל-שורש פעמיים (P-01, אל תנסה שלישית).

דווח: שורה אחת מאומתת בכל אבן-דרך 🔑/✅ בלבד — לא commit-by-commit.

התחלה: פאזה 0+A, משפחת **PP-R**. פאזה 0 = פורט generate/familyOf/odOf + golden. פאזה A = familySpecFor → registerFamilySpecs (putIfAbsent, main.dart:260) → **חוליות 789 SKU 0→100% מחברים**. דרוש: דוח-כיסוי-קטלוג ({משפחה+OD} פר-מוצר; ~1,623/1,867 כבר עם specs).

עצור טבעי אחרי פאזה A (שלב 26, חוליות מתחברת) לדיווח; פאזה C (הטמעת-3D בכרטיס-החי) = החלטת-מוצר, עצור ושאל.
━━━━━━━━━━━━━━━━━━
