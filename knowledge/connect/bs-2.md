# bs-2 — 49 מנועי-buildsmart מול המחולל

> הרשימה נגזרה מהאינדקס המחויב, לא מהפרומפט:
> `cd -ai-chat-server && node -e 'const j=require("./machtzev/generator/engine-index.json"); const my=j.engines.filter(e=>e.file.startsWith("buildsmart/") && !/^buildsmart\/(functions\/|rules_test\/|app\/|edge-proxy\/|service-worker\.js|scripts\/seed\/)/.test(e.file)); console.log(my.length)'` ⇒ **49**
> כל שורה נכתבה אחרי פתיחת-הקובץ וקריאתו. הפירוט המלא (‏does / doesNot / callers / evidence) — `bs-2.json`.

**מופו עד כה: 40/49**

| # | קובץ | שורות | מה עושה (תמצית) | מה **לא** | קוראים היום | איפה לחבר | §22 |
|---|------|------:|-----------------|-----------|-------------|-----------|:---:|
| 1 | `.claude/hooks/pre-tool.sh` | 186 | PreToolUse hook: קורא JSON מ-stdin ומחלץ tool_name/command/file_path דרך jq · חוסם (exit 2) עריכת 11 קבצי-הגנה ברשימת PROTECTED_PATHS ל-Edit/Write/NotebookEdit, אלא אם קיים .allow_protocol_edit תקף (≤24h, ≥30 תווים לא-רווח) · חוסם ב-Bash: --no-verify · core.hooksPath inline/config · force-push (כולל refspec +branch) ·  | לא fail-closed בהיעדר jq — אם jq חסר, $tool ריק ⇒ exit 0 בשורה 91 (הכל עובר) · לא מקנן נתיבים (אין realpath) — התאמת-suffix בלבד, ולא מסיר מרכאות; `sed -i '.git''hooks/pre-commit'` לא נבדק · לא מעריך פר-מקטע חוץ מ-cp; שאר הבדיקות רצות על כל | buildsmart/.claude/settings.json:19 · buildsmart/.githooks/pre-commit:163 · buildsmart/.github/workflows/protocol-enforce.yml:99 · -ai-chat-server | ∅ | **0** |
| 2 | `.claude/hooks/session-start.sh` | 70 | SessionStart hook: מפעיל את שערי-הפרוטוקול בכל סביבה — `git config core.hooksPath .githooks` + chmod +x · ב-remote בלבד (CLAUDE_CODE_REMOTE=true): מוסיף /home/user/flutter/bin ל-PATH ומריץ `flutter pub get --no-example` ב-app_flutter · מריץ `bash $REPO/scripts/gen_version.sh` ליצירת lib/version.g.dart לפני analyze/buil | לא מתקין Dart ולא npm — מסתמך על /home/user/flutter קיים · לא כותב CLAUDE_ENV_FILE — ה-PATH נשאר בתת-התהליך של ה-hook בלבד · לא מגדיר merge.regen.driver, למרות ש-.gitattributes של -ai-chat-server מצפה לכך · לא מריץ שום שער — רק מפעיל hooksP | buildsmart/.claude/settings.json:8 · buildsmart/.claude/hooks/pre-tool.sh:35 · buildsmart/.githooks/pre-commit:670 · -ai-chat-server | -ai-chat-server/.claude/hooks/session-start.sh:41 (בסוף מקטע «3 · typescript vendored», לפני כרטיס-המצב) | **2** |
| 3 | `app_flutter/knowledge/catalog-3d/master_ratios.py` | 77 | סקריפט חד-פעמי: קורא zipdump/uploads/catalog-dump.json, מסנן ל-9 משפחות PPR של פולירול, ומחלץ אותיות-מידה ב-regex `([A-Za-z][0-9]?)\s*:\s*([\d.]+)` (למעט PN/SDR) · מחשב לכל (תת-משפחה, אות) את החציון של v/d ואת פיזור-החציון, ומסמן clean אם spread<0.04 — עם סף-מינימום n≥4 · כותב construction_ratios.json ומדפיס טבלת-אב ·  | לא ניתן להרצה בריפו הזה — קובץ-הקלט חסר · לא מקבל ארגומנטים ולא ניתן להפנות לקובץ אחר — הנתיב וגם נתיב-הפלט קשיחים ויחסיים ל-cwd · לא מחשב מידות למוצר — פולט יחסים בלבד (ratio/spread/n/clean) · לא משמש שום קוד Dart — construction_ratios.jso | buildsmart · -ai-chat-server | ∅ (לא ידוע אם קיים מקבילה מחוברת) | **1** |
| 4 | `app_flutter/knowledge/catalog-3d/polyroll_dim_engine.py` | 222 | מנוע דאטה-דריבן: PolyrollDimEngine בונה טבלת (משפחה,סדרה)→{גודל:מידות} מ-catalog-dump.json ומייצר מידות לכל שאילתת (משפחה,גודל) · שלוש דרגות-אמת מפורשות: exact (הגודל קיים בקטלוג) · interp (אינטרפולציה לינארית בין שני גדלים סמוכים) · None עם סיבה 'out-of-range'/'none' — לעולם לא מנחש · PipeBase — שכבת-בסיס (Layer 0) שמ | לא ניתן להרצה בריפו הזה — catalog-dump.json חסר · לא גיאומטריה — הוא לומד מדאטה, ולכן אינו יכול לתת מידה למשפחה/סדרה שלא ראה · לא פורט ל-Dart — בניגוד ל-pure_engine.py · לא כותב קובץ-פלט — מדפיס בלבד · אינטרפולציה לינארית בלבד — ללא הערכת-ש | buildsmart · -ai-chat-server | ∅ (לא ידוע אם קיים מקבילה מחוברת) | **1** |
| 5 | `app_flutter/knowledge/catalog-3d/pure_engine.py` | 167 | מנוע-אביזרים PP-R טהור: base(od,pn) גוזר OD/wall/ID/B/C/F מ-SDR ומטבלת-ריתוך DIN 8077 — אפס נתוני-קטלוג · 9 משפחות חד-קוטריות ב-ENGINE (מצמד · ברך 90°/45° · טי · מתאם-תבריג · ברז-כדורי · פקק · רוכב · צווארון) + reducer(d1,d2) הדו-קוטרי + mitered_elbow ל-d≥160 דרך elbow_auto · כל אות מגיאומטריה מפורשת: z=R·tan(θ/2) עם R | לא קורא דאטה ולא כותב קובץ — פונקציות טהורות; ה-__main__ קורא קטלוג רק לאימות-דיוק, ורק אם ניתן argv[1] · לא מטפל באומגה ולא ב-EF (ריתוך חשמלי) — שתי משפחות שקיימות בקטלוג ואינן ב-ENGINE · DEPTH מכסה 10 קטרים (20-125) בלבד — base() מחזיר F= | buildsmart/app_flutter/lib/features/fittings/engine/fitting_dims.dart:1 · buildsmart/app_flutter/test/fittings/fitting_engine_golden_test.dart · buildsmart/app_flutter/lib/features/fittings/engine/{mo | -ai-chat-server/new/dart/plug.contract.md:3 + coupler.contract.md:3 (קיים כבר כמקור-חוזה) → ההרחבה: machtzev/generator/gen-verify.mjs:67 / golden-harness.mjs:38, שם רצה `flutter test` בתוך buildsmart | **3** |
| 6 | `app_flutter/scripts/apply_lipskey_enrich.py` | 123 | מחיל-יחיד דטרמיניסטי: קורא knowledge/_enrich_validated.json ומזריק dims/color/qtyPack/qtyPallet לתוך lib/data/lipskey_catalog.dart, לפי התאמת sku · אידמפוטנטי — שדה שכבר קיים על המוצר נספר כ-skip ולא נדרס, בשני הייצוגים · מטפל בשני ייצוגי-מוצר שונים ב-Dart: constructor בשורה-אחת (מוצרי AQUATEC/Qondus) — הזרקה לפני ה-`) | לא ניתן להרצה כרגע — קובץ-הקלט חסר · לא מאמת דבר — אין בו שער-R8; הוא סומך על כך שהקלט כבר עבר validate (בניגוד ל-fill_attrs_from_fleet.py:254 שבודק שוב) · לא מריץ analyze/test אחרי הכתיבה — כותב Dart ויוצא · לא גיבוי ולא rollback — כתיבה י | buildsmart · -ai-chat-server | ∅ בצינור הנוכחי | **1** |
| 7 | `app_flutter/scripts/audit_gates.sh` | 148 | מריץ 18 קריאות test_gate ומדפיס PASS/FAIL, exit 1 אם FAIL>0 · בודק בפועל דבר אחד: ששני ה-hooks מסונכרנים — `diff -q .githooks/pre-commit .git/hooks/pre-commit` | **לא בודק את השערים.** הכותרת אומרת «כל שער מקבל באג מיקרוסקופי ובודק שהוא תופס אותו» — בפועל 17 מתוך 18 המקרים מעתיקים את תנאי-השער אל תוך הסקריפט ובודקים את ההעתק. `.githooks/pre-commit` לא מורץ אף פעם · לכן שינוי ב-.githooks/pre-commit א | buildsmart · -ai-chat-server | ∅ | **0** |
| 8 | `app_flutter/scripts/crop_huliot.py` | 470 | חיתוך אדפטיבי של 44 עמודי קטלוג Huliot SmartLock לתמונות-מוצר: מזהה את הקו הירוק האופקי שמסמן ראש-בנד (‏≥15% מרוחב-העמוד, g≥130) · מסיר קווי-כותרת-עמוד עודפים לפי ספירת SECTIONS הצפויה, וכל קו נותר = ראש-בנד; תחתית = הקו הבא או תחתית-העמוד · בתוך בנד: מוצא בלוקי-תוכן לפי דיו בשורות, מפצל תצלום מדיאגרמה כשהם מודבקים (בל | לא OCR ולא קורא טקסט — פיקסלים בלבד (PIL/ImageStat). שיוך תמונה↔מוצר נעשה בצד-Dart לפי (page, tag) · לא גנרי — SECTIONS הוא טבלה ידנית של 31 עמודים, ו-CROPS_27 הוא קואורדינטות קשיחות. קטלוג אחר לא יעבוד · לא מאמת ויזואלית את מה שחתך — אין c | buildsmart · -ai-chat-server | ∅ בצינור הנוכחי (לא ידוע אם קיים מקבילה מחוברת) | **2** |
| 9 | `app_flutter/scripts/enrich_from_name.py` | 85 | מפיק העשרה משם-המוצר עצמו לתוך knowledge/_enrich_validated.json — שני זרמים: dims['מידה'] ו-color · 6 תבניות-גודל מסודרות (DN.. · 20"×1/2" · 1/2" · 16×16 · 200 מ"מ · 3") — הראשונה שמתאימה מנצחת · 14 מילות-גימור, ממוינות ארוך-קודם כדי ש-«זהב מוברש» ינצח את «זהב» · **מודעות-קטגוריה כשער-אמת**: גימור נקלט רק ב-22 קטגוריות | לא כותב לקטלוג — רק ל-JSON שמוזן ל-apply_lipskey_enrich.py · לא ממציא — כל ערך הוא תת-מחרוזת מילולית של nameHe (regex על nm בלבד) · חותך כל מוצר ל-1700 התווים הראשונים — שדה שיושב אחרי זה לא ייבדק (למשל has_dims עלול לחזור False בטעות) · מז | buildsmart · -ai-chat-server | -ai-chat-server/machtzev/generator/entity-terms.mjs:5 (נקודת-ה-`--gate`, שם הוא מדלג כשאין maor-system) | **2** |
| 10 | `app_flutter/scripts/fill_attrs_from_fleet.py` | 64 | מחיל אטריביוטים שנחל-הסוכנים חילץ (זווית · יציאות · תצורה · מאפיין · ייעוד) מ-knowledge/_fleet_attrs.json אל dims שב-lib/data/lipskey_catalog.dart · **שער-R8 קשיח בהחלה (defence-in-depth)**: כל ערך נבדק שוב שהוא טוקן מילולי של nameHe של אותו מוצר — כולל נרמול ° — לפני ההזרקה; כלומר גם פלט-סוכן שגוי לא יכול לעבור · אידמ | לא מריץ את בדיקת-ה-parity שהוא מסתמך עליה — ההנמקה בשורות 232-234 מתועדת כ«verified by running the parity suite», אבל הסקריפט עצמו לא מריץ כלום · לא מחלץ — רק מחיל. החילוץ נעשה ע"י נחיל-סוכנים שאינו בריפו · חותך ל-1700 תווים ראשונים של המוצ | buildsmart · -ai-chat-server | -ai-chat-server/machtzev/generator/entity-terms.mjs:5 · ולצדו machtzev/no-fakers-check.mjs:2 (שער `no-fakers`, police.mjs:134) | **2** |
| 11 | `app_flutter/scripts/fill_material_from_name.py` | 62 | מוסיף dims['חומר'] כשחומר-מבני כתוב מילולית בשם-המוצר — 4 חומרים בלבד: נירוסטה · נחושת · פליז · פלסטיק · **מבחין בין חומר לגימור** — כרום/ברונזה/ניקל נדחים במפורש כי הם color ולא חומר · **מבחין בין הגוף לתת-רכיב** — «ציר פלסטיק» (ציר על מושב-אסלה) מדולג, כדי לא לייחס חומר של רכיב-משנה למוצר כולו · מחריג SKUs מוצמדי-gat | לא מזהה חומרים אחרים — 4 מילים קשיחות. PVC/HDPE/PPR/אלומיניום/קרמיקה לא נתפסים למרות שהם בקטלוג · gate117 נקרא ב-regex גס `'(\d{6,})'` על **כל** קובץ-הבדיקה — כל מספר בן 6+ ספרות במרכאות נחשב SKU-מוצמד, גם אם אינו · לא --dry, לא גיבוי, לא מ | buildsmart · -ai-chat-server | ∅ בצינור הנוכחי | **1** |
| 12 | `app_flutter/scripts/fill_modes_from_name.py` | 70 | ממלא מפרט שנשא בשם-המוצר למוצרי-נוי שהקטלוג התמונתי לא נתן להם מידות: dims['מצבים'] (‏`(\d+)\s*מצבים`) · dims['אורך'] (קצר/ארוך) · dims['סוג'] (6 טוקנים) · **מגביל לעצמו את קהל-היעד**: רק מוצר שאין לו אף מפתח-מפרט אמיתי (‏`real = keys − {תיאור}` ריק) — כלומר לא נוגע במוצר שכבר יש לו דאטה · מחריג gate-117 ואידמפוטנטי (מ | לא מוודא שהטוקן שנמצא שייך למוצר ולא לאביזר נלווה — «ארוך»/«קצר» יכולים לתאר את הצינור ולא את המוצר; אין בדיקת-הקשר כמו ב-fill_material_from_name.py:24 · «יחיד»/«כפול» כטוקני-סוג מתאימים גם למילים בתוך תיאור-פונקציונלי אחר; אין anchor · לא  | buildsmart · -ai-chat-server | ∅ בצינור הנוכחי (לא ידוע אם קיים מקבילה מחוברת) | **1** |
| 13 | `app_flutter/scripts/fill_residual_rt.py` | 80 | מעבר-שאריות: color ממילת-צבע-טהור בשם (8 מילים) + dims['מידה'] מ-6 תבניות-גודל שמעברים קודמים החמיצו (‏1"-3/4" · 3/4" · DN · 1.5" · ס"מ · מ"מ) · **מנמק את בטיחות-ה-R8 שלו**: «צבע-טהור לעולם אינו חומר-גוף», ולכן מותר בכל קטגוריה — בניגוד ל-enrich_from_name.py שנאלץ לגדר ל-22 קטגוריות דקורטיביות · מידה נכנסת רק למוצר בלי | לא מבדיל בין «לבן» כצבע-המוצר ל«לבן» כחלק משם-דגם או מתיאור-אריזה — אין anchor, תת-מחרוזת בלבד · סדר-התבניות קובע ושקט: `1"` יתפס ע"י התבנית הרביעית לפני שהחמישית (`ס"מ`) תיבדק, גם אם בשם יש שניהם · לא --dry, אין גיבוי, אין בדיקות · gate117 | buildsmart · -ai-chat-server | ∅ בצינור הנוכחי (לא ידוע אם קיים מקבילה מחוברת) | **1** |
| 14 | `app_flutter/scripts/fix_audit_all.py` | 69 | מחיל 4 טבלאות-תיקון קשיחות פר-SKU על lib/data/lipskey_catalog.dart: 23 טיפו · 3 החלפות-dims · 4 imageFile→null · מילוי-שם אחד · **כל עריכה מותנית בכך שהערך-הישן קיים** — אחרת no-op שקט; כלומר הוא לא יכול «לתקן» משהו שכבר שונה · scoped פר-בלוק-מוצר (split על `LipskeyCatalogProduct(`) — טיפו מוחלף רק בתוך המוצר שלו · מדפ | **אינו בטוח להרצה-חוזרת היום — הוא יסיג את הקטלוג.** IMAGE_NULL מכיל 4 SKUs, אבל רק 2 מהם עדיין null; ל-120011 ול-686366 הוצמדו מאז תמונות, וריצה נוספת תאפס אותן · ההערה בקוד סותרת את הקוד: «Only these 5 remain nulled» — אבל ב-IMAGE_NULL יש | buildsmart · -ai-chat-server | ∅ בצינור הנוכחי | **1** |
| 15 | `app_flutter/scripts/fix_audit_dims.py` | 49 | שני תיקונים בלבד, שניהם R8: 2 שגיאות-יחידה (dims['מידה'] ב-ס"מ כשהשם אומר מ"מ ⇒ מ"מ) ו-6 הסרות של 'מידה' משובשת · **כשהערך שנגזר הוא זבל — מוחק את המפתח ולא מנחש.** הנימוק מפורש: הגודל האמיתי עדיין מופיע בשם · תיקון-היחידה מותנה בכך שהמחרוזת הישנה קיימת ב-1700 התווים הראשונים של המוצר — אחרת no-op · ההסרה מטפלת בשתי עמ | אין גידור gate-117 בקוד כלל (שלא כמו fill_material/fill_modes/fill_residual) · ההסרה לא מוודאת שהערך שהוסר הוא אכן המשובש — REMOVE_MIDA הוא רשימת-SKUs, וה-regex מוחק את 'מידה' יהיה אשר יהיה; הרצה-חוזרת אחרי מילוי-מחדש תמחק ערך תקין · לא --d | buildsmart · -ai-chat-server | ∅ בצינור הנוכחי (לא ידוע אם קיים מקבילה מחוברת) | **1** |
| 16 | `app_flutter/scripts/generate_stuck_regression.sh` | 129 | מייצר את app_flutter/test/stuck_regression_test.dart מתוך knowledge/stuck_log.md — כל שורת `ANTIPATTERN:` הופכת ל-test שרץ לנצח · **שני יעדי-סריקה**: `ANTIPATTERN:` סורק את lib/ (Dart) · `ANTIPATTERN[hook]:` סורק את ../.githooks/pre-commit (bash) — כי 17 מתוך 31 האנטי-פטרנים היו hook-bash וסרקו lib/ לחינם · מנקה escape | לא מאמת שה-regex תקין — תבנית שגויה תיווצר כ-RegExp שנכשל בזמן-ריצה, לא בזמן-יצירה · לא בודק כפילות-תוכן — המספור (#N) הוא סידורי לפי סדר-השורות, ולכן הוספת ANTIPATTERN באמצע הקובץ מזיזה את כל המספרים שאחריו · סורק שורה-שורה — אנטי-פטרן שמש | buildsmart/.githooks/pre-commit:758 · buildsmart/.githooks/pre-commit:773-774 · -ai-chat-server | -ai-chat-server/machtzev/police.mjs — כשער חדש לצד `learn` (שורה 168), הניזון מ-machtzev/LEARNINGS.md · ולצדו machtzev/learn-check.mjs | **3** |
| 17 | `app_flutter/scripts/harvest_lipski_site.py` | 56 | קוצר את אתר-היצרן הרשמי lipski.co.il: קורא את product-sitemap.xml, נכנס לכל עמוד-מוצר, ומחלץ sku · שם · תמונה ראשית · שלוש אסטרטגיות-SKU בסדר יורד של אמינות (schema JSON · span.sku · og retailer_item_id), והראשונה שמחזירה ערך לא-ריק ולא-'N/A' מנצחת · משתמש ב-curl עם User-Agent של דפדפן — כי Cloudflare חוסם את ה-UA של u | לא ניתן לאמת כאן — קובץ-הפלט אינו בריפו · לא מטפל בכשל-רשת — `curl` בולע כל חריגה ומחזיר מחרוזת ריקה, והלולאה פשוט `continue`. sitemap ריק ⇒ «product URLs: 0» ופלט ריק, בלי קוד-יציאה שגוי · לא מוריד את התמונות — רק את ה-URL · לא מצליב מול ה | buildsmart · -ai-chat-server | ∅ (לא ידוע אם קיים מקבילה מחוברת) | **1** |
| 18 | `app_flutter/scripts/match_lipskey_pdf.py` | 115 | מצליב את חילוץ-ה-PDF המאומת של הנחיל (knowledge/_pdf_extracted.json) מול 924 ה-SKUs שבקטלוג החי, ופולט רק מה שמותר להחיל · **דוחה קריאות לא-בטוחות**: כל מוצר עם `confidence == 'low'` מדולג ונספר בנפרד · **לא ממציא ולא מוסיף מוצרים**: SKU שקיים ב-PDF ואינו בקטלוג נאסף ל-new_skus ו**מודפס להכרעת-משתמש** — לא נכתב · **לא  | לא ניתן להרצה כאן — קובץ-הקלט חסר · לא כותב לקטלוג — רק ל-JSON שמוזן ל-apply_lipskey_enrich.py · לא מחלץ מה-PDF — החילוץ נעשה ע"י נחיל שאינו בריפו · gate117 כאן נקרא ב-regex `'(\d{6})'` — בדיוק 6 ספרות, בעוד fill_material_from_name.py:32 מש | buildsmart · -ai-chat-server | -ai-chat-server/machtzev/generator/gen-verify.mjs:88-92 ו-golden-harness.mjs:54-58 (בלוקי-ה-`if (gate)`), כדפוס שחסר שם | **2** |
| 19 | `app_flutter/scripts/mutation_verify.sh` | 72 | אימות-מוטציה בששה שלבים: גיבוי byte-exact ⇒ הזרקת sed ⇒ test חייב להיות אדום ⇒ שחזור מהגיבוי ⇒ test חייב להיות ירוק ⇒ רישום אוטומטי ל-knowledge/mutation_log.md · **משחזר מ-backup ולא מ-`git checkout`** — כדי שעריכות לא-מקומיטות באותו קובץ ישרדו. זה הבאג שהסקריפט נולד לתקן · trap על EXIT/INT/TERM — גם קריסה או Ctrl-C מש | **עדיין נוגע בעץ-העבודה החי.** המוטציה מוזרקת בקובץ המקורי (‏`sed -i`), לא בעותק-sandbox; אם התהליך נהרג ב-SIGKILL, ה-trap לא רץ והקובץ נשאר מוטנטי · מריץ מוטציה אחת שהמשתמש מספק — אינו סורק ואינו דוגם; אין baseline ואין ראצ׳ט · **רושם רק ה | buildsmart · -ai-chat-server | -ai-chat-server/machtzev/generator/golden-harness.mjs:34 + 44 — הכתיבה על המראה והשחזור שאחריה | **2** |
| 20 | `app_flutter/scripts/polish_shot.js` | 26 | צילום-מסך headless של ה-build ב-Playwright chromium: goto(url) ⇒ המתנה קבועה ⇒ screenshot ⇒ סגירה · viewport של טלפון — 412×915 ב-deviceScaleFactor 2 · עוקף את ה-proxy עם תעודה לא-מהימנה של הסנדבוקס — `ignoreHTTPSErrors` + `--ignore-certificate-errors` · מדפיס שגיאות-עמוד ל-stdout ויוצא 1 בכשל | **לא מוודא שהאפליקציה בכלל עלתה** — ממתין זמן קבוע (‏13000ms ברירת-מחדל) ומצלם, גם אם המסך לבן · לא סופר widgets ולא בודק flt-glass-pane; אין קריטריון-הצלחה מלבד «לא נזרקה חריגה» · לא אוסף requestfailed/console-error — רק pageerror · לא מגי | buildsmart/app_flutter/scripts/polish_shot.sh:35-36 · -ai-chat-server | ∅ | **0** |
| 21 | `app_flutter/scripts/polish_shot.sh` | 36 | צינור-צילום מלא: בונה web עם canvaskit מקומי אם חסר ⇒ מגיש את build/web ב-python3 http.server על 8099 ⇒ מריץ polish_shot.js ⇒ הורג את השרת ב-trap · **מנמק את הצורה**: gstatic.com (canvaskit + גופנים) חסום במדיניות-הרשת של הסנדבוקס, ולכן חייבים `--no-web-resources-cdn` כדי לצרוף את canvaskit מקומית · בונה רק אם חסר — `[ | לא ניתן להרצה כאן — מגדיר PATH ל-flutter שאינו קיים בקונטיינר · **לא בודק שהצילום מוצלח** — כל הוודאות מואצלת ל-polish_shot.js, שרק ממתין זמן קבוע · פורט 8099 קשיח — שתי ריצות מקבילות מתנגשות · `sleep 2` קשיח אחרי הפעלת-השרת, בלי בדיקת-מוכנ | buildsmart · -ai-chat-server | ∅ | **0** |
| 22 | `app_flutter/scripts/post_build.sh` | 63 | מתקן את build/web/flutter_bootstrap.js אחרי כל `flutter build web --release` כך ש-canvaskit ייטען מהתיקייה המצורפת ולא מ-gstatic CDN · מזריק `canvasKitBaseUrl: "canvaskit/"` בשני מקומות — גם ב-config העליון וגם ב-initializeEngine · אידמפוטנטי — אם התיקון כבר קיים, יוצא 0 בלי לגעת · **fail-loud על שינוי-צורה**: אם בלוק  | **התאמת-מחרוזת מדויקת, לא parse** — ההחלפה היא `s.replace(old,new)` על 8 שורות מדויקות כולל רווחים. כל שינוי ב-Flutter SDK (למשל `useColorEmoji` שיוצא משימוש) מפיל אותו ל-exit 2 · לא בונה ולא מצלם — רק מתקן קובץ אחד אחרי בנייה שכבר קרתה · * | buildsmart · -ai-chat-server | ∅ | **0** |
| 23 | `app_flutter/scripts/protocol_check.sh` | 115 | 8 שערים ידניים לפני commit/push: analyze · test · build web · סנכרון-גרסאות home_shell↔STATUS · קיום WIRING.md · אין משטח כהה ב-lib/screens · ענף נכון · אין שינויים לא-שמורים · שלושת הראשונים מריצים flutter בפועל ומדווחים ספירת-בדיקות · מסכם PASS/FAIL ויוצא 1 אם יש כשל · שער-8 הוא warn ולא fail — שינויים לא-שמורים לא ח | **לא נייד** — `cd /home/user/buildsmart/app_flutter` קשיח בשורה 7. worktree, קלון אחר או הנתיב שב-CLAUDE.md של המחולל (‏/home/user/meir7651231-ui/buildsmart) — כולם יריצו את השערים על העותק הלא-נכון או ייכשלו · **נכשל על כל ענף שאינו claude | buildsmart · -ai-chat-server | ∅ | **0** |
| 24 | `orchestrator/scripts/assert-manifest.sh` | 24 | מריץ manifest של הצהרות-בייטים: כל שורה `<relpath>:::<regex>` = הקובץ **חייב** להכיל · `<relpath>:::!<regex>` = **אסור** שיכיל · **manifest ריק נכשל** — «אין הצהרות» לעולם אינו מעבר שקט · מאחד conformance (מחרוזת-חובה מהמקור) ו-regression (מחרוזת-אסורה שהבאג החזיר) לאותו מנגנון · **מאציל את בדיקת-הבייטים ל-grep-verify. | לא מגדיר מה להצהיר — ה-manifest חיצוני לו. (‏שני manifests מחויבים **כן** קיימים: orchestrator/manifests/buildsmart.conformance.txt ⇒ 269 חוקים, מתוכם 11 מסוג `:::!` · buildsmart.required-tests.txt ⇒ 44 · ועוד conformance.example.txt ריק-מח | buildsmart/orchestrator/scripts/central-verify.sh:80 · buildsmart/orchestrator/scripts/selftest.sh · -ai-chat-server | -ai-chat-server/machtzev/generator/gen-verify.mjs:86-93 ו-golden-harness.mjs:54-59 (בלוקי-ה-`if (gate)`) | **3** |
| 25 | `orchestrator/scripts/central-verify.sh` | 68 | השער: מחזיר 0 רק אם analyze נקי · test עובר · build מצליח — במפורש כמתאם-פר-סטאק (‏Flutter היום), עם הוראה להחליף את שלושת הבלוקים לסטאק אחר · **מדפיס טביעת-אצבע של היעד** — נתיב · HEAD קצר · שם-החבילה מ-pubspec — כדי שהאורקסטרייטור יוכל לוודא שהשער כוון ל-worktree שבו התיקונים הוחלו (מלכודת scope-targeting של ה-red-te | **אינו מוכיח נכונות** — ההצהרה המפורשת של הפרוטוקול: «ירוק = עקביות, לא הוכחת-נכונות» · לא מריץ `dart format` — ולכן L0 בסולם-האימות לא מכוסה ע"י אף שער · `--no-fatal-warnings` — אזהרות לא מפילות. עקבי עם שער 31, אך זו החלטה, לא ברירת-מחדל  | buildsmart/CLAUDE.md (חוק-על #0) · buildsmart/orchestrator/scripts/selftest.sh · -ai-chat-server | -ai-chat-server/machtzev/generator/gen-verify.mjs:66-67 · golden-harness.mjs:37-38 — ממש לפני ה-`spawnSync(FLUTTER,['test',…],{cwd:BS})` | **3** |
| 26 | `orchestrator/scripts/ckpt.sh` | 24 | checkpoint עמיד לריצת-נחיל עם 6 פקודות: init · set · get · phase · guard · show · **המצב חי ב-git-admin dir של ה-worktree** (`$(git rev-parse --absolute-git-dir)/orchestrator-state/checkpoint.json`) — שורד את ה-worktree ולעולם לא מופיע ב-`git diff` · **כתיבה אטומית** — mktemp באותה תיקייה ואז `mv -f`, כך שקריסה באמצע ל | לא מריץ ולא מחליט — רק זוכר. אין בו לוגיקה שמפעילה את השלב הבא · **לא מונע דילוג קדימה** — `phase` מקבל כל שם חוקי ללא בדיקת-רצף; רק `guard` בודק, והוא צריך להיקרא במפורש · אין נעילה — שני תהליכים במקביל יכולים לדרוס זה את ה-checkpoint של ז | buildsmart/orchestrator/scripts/selftest.sh · buildsmart/orchestrator/PLAYBOOK.md · -ai-chat-server | -ai-chat-server/machtzev/generator/ship.mjs — סביב שלבי 1-6 (השורות 61-140), ו-machtzev/generator/regen.mjs | **2** |
| 27 | `orchestrator/scripts/diff-coverage.sh` | 48 | שער-כיסוי-לפי-דיף: כל קובץ-מקור **ששונה** חייב להופיע בדוח ה-lcov · **הורג את ה-false-green של T2**: חפיפת-אפס בין נתיבי-lcov לנתיבי-git (שניהם לא-ריקים, ואף נתיב לא משותף) היא BLOCK קשיח — כי זו אי-התאמת-קידומת ולא כיסוי אמיתי · מנרמל שני הצדדים לאותה צורה: מסיר `./` ו-`/` מובילים, מסיר strip-prefix (עם escape ל-regex | **בודק נוכחות בדוח, לא אחוז-כיסוי** — קובץ עם 0% שורות-מכוסות אבל עם רשומת SF: עובר · לא מייצר את ה-lcov ולא מריץ בדיקות — מקבל קובץ מוכן · ספציפי ל-Dart — `grep -E '\.dart$'` קשיח בנרמול · לא מחשב את רשימת-ה-changed בעצמו — הקורא חייב לספק | buildsmart/orchestrator/scripts/selftest.sh · -ai-chat-server | -ai-chat-server/machtzev/police.mjs:132 — לצד `gate('coverage','coverage-gate.mjs')`, כשער נפרד לפי-דיף | **2** |
| 28 | `orchestrator/scripts/ff-push.sh` | 39 | דוחף fast-forward בלבד, עם ארבעה ארגומנטים מפורשים: worktree · repo · branch · **ה-sha שהקורא מצפה שיהיה ב-origin** · **מסרב לענף המוגן/ברירת-המחדל בלי שום override** — main/master וגם ה-DEFAULT שנקרא דינמית מ-refs/remotes/origin/HEAD · **מנמק למה אין override**: ה-red-team הראה שסוכן עם shell פשוט יגדיר `ALLOW_PROTECT | לא מריץ שום שער לפני הדחיפה — הוא מניח ש-central-verify כבר עבר · לא בודק את תוכן ה-commit (trailer, חתימה, הודעה) · **אינו מגן בפועל אם הסוכן פשוט לא קורא לו** — זהו סקריפט, לא hook; לעומת זאת ‏.githooks/pre-push בשני הריפואים כן רץ על כל  | buildsmart/orchestrator/PLAYBOOK.md · -ai-chat-server | -ai-chat-server/machtzev/generator/ship.mjs:140 — `pushWith(BS, branchOf(BS))`, הדחיפה לתוך buildsmart | **1** |
| 29 | `orchestrator/scripts/grep-verify.sh` | 26 | מאמת **בייטים ולא את הפרוזה של הסוכן**: כל ארגומנט הוא `file:::regex` (חייב להופיע ≥1) או `file:::!regex` (חייב להופיע 0), ויציאה ≠0 אם משהו נכשל · מיועד במפורש לרגע שבו צי-תיקון חוזר ומצהיר «כבר תוקן» / «אין צורך בשינוי» · **קובץ חסר = FAIL, לא מעבר שקט** — ובדיוק מסומן מאיפה זה נלמד: ה-A/B של ה-bootstrap גילה שבדיקת- | לא מוצא את התיקונים ולא יודע מה לבדוק — הדפוסים מגיעים מהקורא (או מ-manifest דרך assert-manifest.sh) · **`grep` בסיסי (BRE), לא `-E` ולא `-F` — וזה מייצר false-negative אמיתי בייצור.** בהרצה שלי על ה-manifest המחויב, **10 מתוך 16 הכשלים** ה | buildsmart/orchestrator/scripts/assert-manifest.sh:24 · buildsmart/orchestrator/scripts/selftest.sh · buildsmart/orchestrator/PLAYBOOK.md · -ai-chat-server | -ai-chat-server/machtzev/generator/gen-verify.mjs:77-85 (בניית ה-report) — כשלב שני אחרי ספירת-האטומים | **2** |
| 30 | `orchestrator/scripts/lens-coverage.sh` | 26 | שער-כיסוי-עדשות: כל עדשת-ביקורת **נדרשת** חייבת לחזור מהצי, אחרת BLOCK עם רשימת-החסרות · מנסח את החור שהוא סוגר: «הרצנו 9 מתוך 10 עדשות ואף אחד לא שם לב שה-10 חסרה» — נקודת-מבט חסרה היא חור שקט, לא כשל נראה · שני ייצוגים לרשימת-המוחזרות: תיקייה (מזהה-העדשה = שם-הקובץ בלי הסיומת, *.md/*.json) או קובץ-רשימה (מזהה לשורה)  | **בודק שהעדשה חזרה, לא שהיא נסרקה** — קובץ ריק בשם הנכון עובר את השער · לא מגדיר מהן העדשות הנדרשות — הרשימה חיצונית: `orchestrator/lenses/registry.txt` (‏10 מזהים), והקורא רשאי להעביר תת-קבוצה ממנה · לא מריץ את הצי ולא מוודא שהוא שוגר — רק | buildsmart/orchestrator/scripts/selftest.sh · buildsmart/CLAUDE.md + orchestrator/PLAYBOOK.md · -ai-chat-server | -ai-chat-server/machtzev/police.mjs:176-180 — בלוק «פריטי מרשם⇄ריצה» שבסוף | **2** |
| 31 | `orchestrator/scripts/registry.sh` | 41 | מרשם תת-סוכנים: `open <id> <role> <scope>` · `close <id> <status>` · `assert-none-open` · `list` · `reset` · **סוכן שדלף = BLOCK קשיח**: `assert-none-open` יוצא 1 ומדפיס כל שורה פתוחה עם id/role/scope — כי נתיב-ביקורת/תיקון שנעלם בשקט הוא חור, לא כשל נראה · **דוחה התנגשות-id**: `open` על id שכבר OPEN יוצא 2 — שני סוכני | לא משגר ולא קוטל סוכנים — רק רושם. סוכן שרץ ולא נרשם כלל אינו נראה למרשם · **`reset` מוחק את כל ההיסטוריה בלי אזהרה** — כולל שורות OPEN, כלומר הוא עוקף את assert-none-open · לא בודק ש-`role` ו-`scope` חוקיים (למשל מול registry.txt של העדשות | buildsmart/orchestrator/scripts/selftest.sh:9,37-44 · buildsmart/orchestrator/PLAYBOOK.md · -ai-chat-server | -ai-chat-server/machtzev/police.mjs:176-180 — אותו בלוק «פריטי מרשם⇄ריצה» שכבר אוכף זאת על שערים | **2** |
| 32 | `orchestrator/scripts/report-lint.sh` | 36 | מאמת דוח-ריצה JSON מול חוזה, ב-jq טהור (אין jsonschema בסביבה) · **האינווריאנט המרכזי — «נראה גמור, אינו»**: `status: CLEAN` עם ולו פריט `deferred` אחד = כשל, ו-CLEAN עם discrepancy שאינה `resolved:true` = כשל · `status: FAILED` מחייב `reason` לא-ריק · כל פריט `deferred` מחייב `reason` לא-ריק — דחייה בלי נימוק אינה דחי | **לא בודק שהדוח נכון** — רק שהוא עקבי-עם-עצמו. דוח שמצהיר CLEAN עם `fixed:[]` ריק ובלי deferred עובר, גם אם שום דבר לא נבדק · לא מצליב את הדוח לעולם — אין אימות ש-`fixed` באמת בקבצים (זה תפקיד grep-verify.sh) · לא אוכף שדות נוספים שה-schema | buildsmart/orchestrator/scripts/selftest.sh:9,46-63 · buildsmart/orchestrator/schemas/report.schema.json · -ai-chat-server | -ai-chat-server/machtzev/police.mjs:181-183 — שורת-הסיכום שמחשבת `ran/skipped/yellow/failed` ומחליטה ירוק/צהוב/אדום | **2** |
| 33 | `orchestrator/scripts/required-tests.sh` | 28 | אוכף ש**זרימות-בדיקה קריטיות קיימות**: שורת-manifest = נתיב-קובץ-בדיקה, או `נתיב:::תת-מחרוזת-משם-הבדיקה` · **מנמק למה נוכחות מספיקה**: מכיוון ש-central-verify מריץ `flutter test` והסוויטה נכשלת אם בדיקה נופלת, «קיים + סוויטה-ירוקה» ⇒ הזרימה גם רצה וגם עברה · סוגר את החור «השער מריץ את מה שבמקרה קיים» — שינוי שלא נוגע ב | לא מריץ בדיקות — בודק קיום בלבד; ההנמקה תקפה רק אם מישהו באמת מריץ את הסוויטה · **«קיים» אינו «בודק»** — קובץ-בדיקה עם `expect(true, isTrue)` יעבור, ותת-מחרוזת-השם יכולה להימצא בהערה · לא מגדיר מהן הזרימות הקריטיות — ה-manifest חיצוני · ה-m | buildsmart/orchestrator/scripts/central-verify.sh:87 · buildsmart/orchestrator/scripts/selftest.sh:11,111-123 · -ai-chat-server | -ai-chat-server/machtzev/generator/gen-verify.mjs:25-29 — הבחירה של קובצי-הבדיקה · ו-golden-harness.mjs:17-19 (מפת TESTS) | **2** |
| 34 | `orchestrator/scripts/selftest.sh` | 127 | בדיקה-עצמית לשכבת-הכלים: מפעיל כל סקריפט על נתונים סינתטיים ומשווה **קוד-יציאה צפוי**, לא פלט · **הרמטי** — `git init` בריפו-חד-פעמי ו-tmpdir, שניהם נמחקים ב-trap EXIT; לא נוגע ב-worktree האמיתי · מכסה 8 סקריפטים ב-49 בדיקות: ckpt (9) · registry (7) · report-lint (8) · lens-coverage (3) · diff-coverage (6) · grep-verif | **לא מכסה 4 מתוך 12 סקריפטי-ה-orchestrator**: central-verify.sh · ff-push.sh · wt-setup.sh · selftest.sh עצמו — כלומר השער (‏central-verify) והדחיפה (‏ff-push), שני המסוכנים ביותר, אינם נבדקים · משווה קוד-יציאה בלבד, לא פלט — שער שחוסם מהסי | buildsmart · -ai-chat-server | -ai-chat-server/machtzev/police.mjs:169-170 — לצד `gate('selftest','police-selftest.mjs')` ו-`gate('audit-gates','audit-gates.mjs')` | **2** |
| 35 | `orchestrator/scripts/wt-setup.sh` | 9 | יוצר worktree **detached** טרי על commit/branch לריצת-צינור אחת · מביא את ה-ref מ-origin לפני היצירה, בלי תגיות · מדפיס את ה-sha הקצר שנוצר בפועל — לא את ה-ref שביקשת; זו הראיה שהצינור כוון לנקודה הנכונה · מדפיס את פקודת-הניקוי המדויקת · `set -euo pipefail` — ה-`worktree add` נכשל בקול | **בולע כשל-fetch** (‏`\|\| true`) — כלומר ייתכן worktree על ref ישן מהמטמון המקומי, בלי אזהרה. זה בדיוק ההיפך מ-ff-push.sh:68-72, שבמפורש **אינו** בולע כשל-fetch «כדי שרפרנס-מעקב ישן לא יעבור את הבדיקה» — שני סקריפטים באותה תיקייה, שתי החלט | buildsmart/orchestrator/PLAYBOOK.md · -ai-chat-server | -ai-chat-server/machtzev/generator/golden-harness.mjs:28-34 — שם נכתב המראה **לתוך עץ-העבודה החי** של buildsmart | **2** |
| 36 | `scripts/bootstrap-studio-pointer.mjs` | 64 | זורע פעם-אחת את מצביע-הפרסום `studioConfig/published` ב-Firestore עם baseline ריק `{version:0, schema:1, ref:null, checksum:null, publishedAt, publishedBy:'bootstrap', publishGuard}` · **אטומי ובלתי-הרסני** — `ref.create()` הוא create-if-absent; ALREADY_EXISTS נתפס והמצביע החי נשאר **ללא נגיעה**. הסקריפט אינו יכול לדרו | לא ניתן להרצה כאן — דורש firebase-admin ו-GOOGLE_APPLICATION_CREDENTIALS · לא מאמת את ה-rules ולא את סדר-הפריסה בפועל — הסדר נאכף בקובץ-ה-workflow, לא כאן · לא מפרסם תצורה ולא קורא מ-Studio — רק זורע מצביע-ריק של ~200B · ה-projectId ברירת-מ | buildsmart/.github/workflows/firebase-deploy.yml:97 · buildsmart/functions/src/selftest.ts:198 · buildsmart/app_flutter/WIRING.md · knowledge/firestore-schema.md · knowledge/studio-plan/detail/051-068 | -ai-chat-server/machtzev/generator/server.mjs:54-59 (‏`rulesFor` שמחולל את firestore.rules) ו-machtzev/generator/server-gate.mjs (שער `server`, police.mjs:145) | **2** |
| 37 | `scripts/build_clean.sh` | 30 | בונה את פרופיל-ה-CLEAN — הבסיס הגנרי ללבן-תווית — בפקודה אחת: `flutter build web --release --dart-define=APP_PROFILE=clean` · **מתעד במדויק מה הפרופיל אומר**: כל יכולת-UX מסופקת דלוקה · אין שרת-חברה (`CATALOG_BASE_URL=''`) ואין CDN (`IMAGE_BASE_URL=''`) — נכסים מצורפים בלבד · מראה-הפרסום של Studio כבוי · שכבת-הדגלים לא | לא פורס — הבנייה בלבד; הפריסה היא פעולת-בעלים ידנית · לא מריץ analyze/test/`app_profile_flags_test` לפני הבנייה, למרות שהוא מסתמך עליהם להגדרת-הפרופיל · **PATH שונה מכל שאר הסקריפטים**: `$HOME/flutter/bin` ולא `/home/user/flutter/bin`. שאר  | buildsmart · -ai-chat-server | -ai-chat-server/machtzev/generator/ship.mjs:86-93 — הלולאה `for (const [name, entry] of SITES)` שבונה אתר פר-כניסה | **1** |
| 38 | `scripts/catalog_import.py` | 152 | צינור-ייבוא-אצווה: CSV (עם כותרות) או JSON (מערך או `{products:[…]}`) ⇒ רשומות `LipskeyCatalogProduct(...)` מוכנות ל-lipskey_catalog.dart, ב-12 שדות נתמכים · **מחיל נרמול-שם בטוח**: מאחד גרשיים, מסיר מקטעי-אריזה («כמות באריזה/במשטח», «12 באריזה»), מסיר כפילויות-מקטע דרך `dict.fromkeys`, ומוסיף רווח בגבול ספרה↔אות **בלי | **לא כותב לקטלוג** — פולט ל-stdout, וההדבקה ידנית · **אימות-כפילות לא מדויק**: `seen.add(sku)` מתבצע גם כששדה-ה-sku ריק, ולכן שורה שנייה עם sku ריק תיחשב «מק"ט כפול» מלבד היותה «שדה חובה חסר» · לא בודק מול הקטלוג הקיים — מק"ט שכבר קיים ב-li | buildsmart/scripts/test_catalog_qa.py · buildsmart (תיעוד) · -ai-chat-server | -ai-chat-server/machtzev/generator/app-ds.mjs / render-module.mjs — שלב פליטת-ה-Dart · ובפרט הכלל ב-catalog_import.py:47-51 | **2** |
| 39 | `scripts/catalog_qa.py` | 623 | מנוע-QA לקטלוג עם 21 פקודות (audit · selftest · fix · export · truthcheck · crosscheck · suspects · pdfmap · verify · images · imgmatch · coverage · schema · truthapply · report · diff · crossdedup …) · **מודל-חומרה מפורש** ERROR/WARN/INFO, ורק ERROR חוסם · **פרסור מאוזן-סוגריים ולא regex** — עם הסיבה כתובה בקוד: looka | **כיסוי-האימות מול המקור הוא 29% בלבד** — 199 שמות שונים מהמקור · 89 קטגוריות שונות · 647 מוצרים שאין להם רשומה ב-source_truth · 0 חסרים. כלומר «הכלל הזהוב» נבדק על פחות משליש מהקטלוג · **ה-truthcheck לא חוסם** — הוא יצא 0 למרות 199+89 אי-ה | buildsmart/.github/workflows/catalog-qa.yml:19,22,26,30,35 · buildsmart/scripts/hooks/pre-commit:5 · buildsmart/app_flutter/lib/test_harness/tests/catalog.dart:8 · buildsmart (תיעוד) · -ai-chat-server | -ai-chat-server/machtzev/generator/hamtzaa.mjs (שער `hamtzaa --ratchet`, police.mjs:140) ו-machtzev/purity/purity-data.mjs (שער `puredata`, police.mjs:125) | **2** |
| 40 | `scripts/compare_local_vs_flip.sh` | 70 | בונה פעמיים — LOCAL (דגלים כבויים, ברירת-המחדל המסופקת) ו-FLIP (‏USE_FIREBASE_BACKEND + STUDIO_LIVE + CATALOG_SERVER_SEARCH + CATALOG_BASE_URL) — ומשווה · **מתחיל בהוכחת-אי-הנסיגה** — מריץ `flutter test test/backend_flag_test.dart` לפני כל בנייה; זו הערובה שהבנייה המקומית ביט-זהה לקודמת למרות קוד-Pillar-5 שמאחורי הדגלי | **לא חוסם** — הפרש-מניפסט מדפיס «⚠️» ו-`\|\| true`, והסקריפט ממשיך ליציאה 0 · לא משווה תוכן — רק שמות וגדלים של קבצים בעומק ≤2; main.dart.js נמדד בגודל בלבד · לא משווה התנהגות — אין הרצה, אין צילום, אין diff סמנטי. זה מוצהר כנקודת-הרחבה עתי | buildsmart · -ai-chat-server | -ai-chat-server/machtzev/generator/ship.mjs:85-93 — מיד אחרי הבניות · והשוואת-הפלט ליד machtzev/generator/gen-verify.mjs:31 (בדיקת סחף-מראה) | **1** |

## ציוני §22 — הנימוק המלא

### `.claude/hooks/pre-tool.sh` — 0

מקבילה מחוברת וטובה יותר: `-ai-chat-server/.claude/hooks/pre-tool.sh` (116 שורות), מחוברת כשער `pretool` ב-machtzev/police.mjs:169 (`pretool-selftest.mjs`) ונעולה ב-machtzev/pins.sha256:1. היא superset מדוד: fail-closed על jq (שורה 8) · realpath -m (42) · הסרת-מרכאות (54) · הערכה פר-מקטע ;/&&/||/| (67-108) · PROTECTED כ-regex-תיקיות (26-27) · חסימת commit-tree/update-ref/fast-import/replace (79) · cherry-pick (80) · push ל-main/delete-ref (83-90) · GIT_CONFIG_PARAMETERS (61) · find -exec (102) · dd/awk/ex/tee/install/ln (96) · כתיבה דרך node/python -e (97) · sleep/timeout/read -t (105-107) · PRETOOL_SELFTEST (30,64). כל חסימה שב-buildsmart קיימת גם שם. ההבדל היחיד לטובת buildsmart הוא BUILDSMART_EMERGENCY_DISABLE — פתח-מילוט, לא חוזק

### `.claude/hooks/session-start.sh` — 2

שני שערי-§22 שמריצים אפליקציה-מחוללת בפועל (genverify · goldenharness) תלויים ב-buildsmart/app_flutter מוכן-להרצה, ואף אחד מצינורות -ai-chat-server לא מכין אותו: `grep -n 'pub get\|gen_version' machtzev/generator/ship.mjs` ⇒ ∅. הקובץ הזה הוא הרצף היחיד שמתעד ומבצע את ההכנה. 3 לא, כי ההפעלה (core.hooksPath) כבר קיימת ומחוברת ב--ai-chat-server/.claude/hooks/session-start.sh:11-14

**איפה לחבר:** `-ai-chat-server/.claude/hooks/session-start.sh:41 (בסוף מקטע «3 · typescript vendored», לפני כרטיס-המצב)` — הכנת-buildsmart שחסרה שם לגמרי: `BS=${BUILDSMART:-$(realpath ../buildsmart/app_flutter)}; [ -f "$BS/pubspec.yaml" ] && (cd "$BS" && flutter pub get --no-example) && bash "$BS/../scripts/gen_version.sh"`. בלי זה שני השערים שמריצים `flutter test` בתוך buildsmart — gen-verify.mjs:67 ו-golden-harness.mjs:38 — רצים על עץ שלא עבר pub get ובלי lib/version.g.dart (gitignored, נוצר רק מ-gen_version.sh)

### `app_flutter/knowledge/catalog-3d/master_ratios.py` — 1

הוא **ולידטור** של pure_engine.py — היחסים שהוא מדד (D=1.333d · z=R·tan(θ/2)) הם המקור לקבועים שמחושבים ב-fitting_dims.dart הדארטי. כשלעצמו אינו מקרב משפט-בעברית⇒אפליקציה, ואינו רץ. לא 0: אין מקבילה מחוברת שלומדת יחסים-הנדסיים מקטלוג, והפלט שלו (construction_ratios.json) מצוטט כמקור-אמת ב-app_flutter/WIRING.md:190. לא ידוע אם קיים מקבילה מחוברת

**איפה לחבר:** `∅ (לא ידוע אם קיים מקבילה מחוברת)` — מנוע-לימוד-יחסים מדאטה אינו חלק מצינור-המחולל של -ai-chat-server (שעובד על משפט⇒Dart, לא על קטלוג-מוצרים). חיבור אפשרי רק אם יוגדר מקור-קטלוג; אין כזה ב--ai-chat-server (‏`grep -rn --exclude-dir=.git -F catalog-dump .` ⇒ 2 התאמות, שתיהן שדה `reads` של שני המנועים האלה בתוך machtzev/generator/engine-index.json:918,951 — כלומר ∅ מחוץ לאינדקס)

### `app_flutter/knowledge/catalog-3d/polyroll_dim_engine.py` — 1

מנוע-אמת אמיתי עם דירוג-ודאות מפורש, אבל מנותק כפליים: אין לו קלט בריפו ואין לו פורט-Dart. אינו על מסלול משפט⇒אפליקציה. לא 0: אין מקבילה מחוברת שמייצרת מידות-מוצר עם דירוג-ודאות, והוא לא כפול ל-pure_engine.py (זה גיאומטריה טהורה, זה דאטה — שני מסלולים שונים במכוון, INTEGRATION-SPEC.md:189,194). לא ידוע אם קיים מקבילה מחוברת

**איפה לחבר:** `∅ (לא ידוע אם קיים מקבילה מחוברת)` — הדפוס «exact/interp/None-עם-סיבה» הוא בדיוק §20 «לעולם לא לזייף דאטה» + הכרעה-27 «מה שאין לו מקור ⇒ ∅ מדווח». אין ב--ai-chat-server מנוע-מידות-ממדידה; הוא מחולל UI ממשפט. חיבור אפשרי רק כשיוגדר צינור-קטלוג — כרגע אין

### `app_flutter/knowledge/catalog-3d/pure_engine.py` — 3

היחיד מבין 49 שכבר עשה את המסע במלואו: ידע-דומייני ⇒ קוד-טהור ⇒ פורט-Dart חי באפליקציה ⇒ golden ביט-זהה ⇒ מצוטט בחוזי-אטום של הגנסיס עצמו (new/dart/*.contract.md:3). בדיוק הצורה ש-§22 דורש — «אפליקציה עובדת 100%» עם מספר שאפשר להגן עליו. אין לו מקבילה מחוברת: `grep -rn 'SDR\|DIN 8077\|socket_depth' machtzev/` ⇒ ∅

**איפה לחבר:** `-ai-chat-server/new/dart/plug.contract.md:3 + coupler.contract.md:3 (קיים כבר כמקור-חוזה) → ההרחבה: machtzev/generator/gen-verify.mjs:67 / golden-harness.mjs:38, שם רצה `flutter test` בתוך buildsmart` — המנוע הוא ה-oracle היחיד שיש לחוזי-ה-plug/coupler בגנסיס. אם מוסיפים ל-TESTS/ל-live גם את test/fittings/fitting_engine_golden_test.dart, כל הרכבה-מחדש של אטום-אביזר נמדדת מול מספרים שנגזרו מגיאומטריה — לא מול fixture שהמחולל עצמו כתב. זה בדיוק «אפס מילון-דומייני» של §20: הידע ההנדסי בקוד, לא בטבלה

### `app_flutter/scripts/apply_lipskey_enrich.py` — 1

הדפוס נכון ומדיד — מחיל-יחיד אידמפוטנטי שסופר applied/skipped ומודפס — אבל הוא לא-רץ (הקלט חסר), לא-מאומת (אין שער-R8 בתוכו) וכפוף לצינור-קטלוג שאין ב--ai-chat-server. לא 0: לא מצאתי מנוע מחובר שמזריק שדות לקובץ-Dart קיים בלי לדרוס; `grep -rn 'idempotent' machtzev/generator/*.mjs` לא נבדק כשווה-ערך, ולכן — לא ידוע אם קיים מקבילה מחוברת

**איפה לחבר:** `∅ בצינור הנוכחי` — המחולל של -ai-chat-server פולט Dart מ-`new/dart-gen-bs` דרך `render-module.mjs`/`app-ds.mjs`; אין לו שלב «הזרקת-שדות-לקובץ-Dart-קיים». חיבור היה מחייב שכבת-דאטה שאין. לא ידוע אם קיים מקבילה מחוברת

### `app_flutter/scripts/audit_gates.sh` — 0

מקבילה מחוברת וטובה יותר בשם: `machtzev/audit-gates.mjs` (57 שורות), שער `audit-gates` ב-machtzev/police.mjs:170. ההבדל אינו סגנוני אלא קטגורי — audit-gates.mjs פותח worktree זמני (שורה 17), מגדיר בו `core.hooksPath=.githooks` (שורה 21), **מזריע הפרה אמיתית ומריץ `git commit` אמיתי**, והראיה היא הכישלון עם שם-השער בפלט (שורה 2-3). audit_gates.sh בודק העתקים של התנאים, ולכן לא יכול לזהות שהעתק נשאר מאחור — וזה כבר קרה (audit_gates.sh:129 מול pre-tool.sh:111). לפי כלל-האמת שבמשימה, כותרת-קובץ אינה ראיה: כאן היא סותרת את הקוד

### `app_flutter/scripts/crop_huliot.py` — 2

המנוע הזה הוא הסיבה שיש 159 תמונות-מוצר אמיתיות באפליקציה במקום placeholders — וזה ישירות «אפליקציה עובדת 100%» של §22, כי מוצר בלי תמונה הוא כרטיס לבן. הוא גם רץ-ומדיד: 44 קלטים ⇒ 159 פלטים, כולם מחווטים דרך huliot_smartlock_catalog.dart:49. לא 3, כי אין לו שום אימות-עצמי (השער היחיד שנולד ממנו הוא warn) ואינו גנרי מעבר לקטלוג אחד. אין מקבילה מחוברת: `grep -rln 'sharp\|jimp\|ImageStat\|from PIL' machtzev/` ⇒ התאמה **אחת**, והיא מחלקת-CSS `.btn.sharp` ב-machtzev/pure/action-family.html:41 — כלומר אפס ספריות-תמונה. היחיד שנוגע בתמונות הוא machtzev/tools/site-shot.mjs (צילום-מסך של אתר בנוי)

**איפה לחבר:** `∅ בצינור הנוכחי (לא ידוע אם קיים מקבילה מחוברת)` — המחולל מייצר UI ממשפט; חילוץ-assets מ-PDF-תמונתי אינו בצינור. הקישור הקרוב ביותר שקיים הוא `machtzev/tools/site-shot.mjs` — אבל הוא מצלם אתר, לא חותך קטלוג. אין ב--ai-chat-server ספריית-תמונה כלשהי

### `app_flutter/scripts/enrich_from_name.py` — 2

מדגים את כלל-האמת של §20 בצורה שניתן להעתיק: ערך תקף רק אם הוא מילולית במקור, ועוד מגודר לפי הקשר. §22 דורש «אפס-באגים» על משפט-חופשי בעברית, וזיהוי-מונחים-עבריים-ממקור-אמיתי הוא בדיוק החוליה ש-entityterms מדלג עליה היום. לא 3 כי הוא עצמו לא רץ, לא מחובר, וחוסר-הסימטריה שלו (תיאור נכתב גם בלי מידה) הוא פגם ממשי

**איפה לחבר:** `-ai-chat-server/machtzev/generator/entity-terms.mjs:5 (נקודת-ה-`--gate`, שם הוא מדלג כשאין maor-system)` — `entityterms` הוא אחד משני השערים שלפי knowledge/HANDOFF-2026-09-16.md מדלגים **תמיד** («⚪ אין maor-system»). מקור-המונחים שלו נעלם. enrich_from_name.py מדגים מקור חלופי שכן קיים: 924 שמות-מוצר עבריים ב-lib/data/lipskey_catalog.dart, עם כלל-חילוץ שאינו יכול להמציא (תת-מחרוזת מילולית) ועם **שער-קטגוריה** שמונע את טעות-ההקשר (נחושת=חומר מול נחושת=גימור). זו בדיוק דרישת §19-ד «אוצר-מילים = דאטה, לא מילון-במנוע» שב-entity-terms.mjs:2

### `app_flutter/scripts/fill_attrs_from_fleet.py` — 2

הלב כאן הוא הדפוס, לא הקובץ: שכבת-אימות שנייה שמניחה שהסוכן שלפניה עלול להזות, ולכן מצליבה כל ערך מול המקור לפני כתיבה. §22 דורש «אפס-באגים» מצינור שרובו סוכנים — וזה המנגנון שהופך את זה לאפשרי בלי לסמוך על אף סוכן. לא 3: המנוע עצמו חד-פעמי, לא רץ אוטומטית, ולא מריץ את בדיקת-ה-parity שההנמקה שלו נשענת עליה

**איפה לחבר:** `-ai-chat-server/machtzev/generator/entity-terms.mjs:5 · ולצדו machtzev/no-fakers-check.mjs:2 (שער `no-fakers`, police.mjs:134)` — השורות 252-255 הן הביטוי הקומפקטי ביותר שמצאתי לחוק «לעולם לא לזייף דאטה» (§20): **הסוכן מציע, השער בודק מול המקור, והמחלק דוחה בשקט מה שלא עומד**. `no-fakers-check.mjs` עושה את ההפך — הוא אוסר רשימת-שמות (FAKERS) בפלט; הוא לא יודע לוודא שערך *הוא* מהמקור. הכנסת בדיקת-«הערך מופיע מילולית במקור» לצינור-ההחלה של המחולל היא ההרחבה הישירה

### `app_flutter/scripts/fill_material_from_name.py` — 1

חד-פעמי, כבר הוחל (64 ⇒ מאומת בקטלוג), לא-מחובר, ורשימת-החומרים שלו מכסה 4 מתוך הרבה יותר. התרומה שנשארת היא כלל-הדיסאמביגואציה (חומר≠גימור≠תת-רכיב) — נכון וחשוב, אבל הוא כבר מיוצג בערך של enrich_from_name.py שנשא את נקודת-החיבור. לא 0: אין מקבילה מחוברת שמסווגת חומר-מוצר מטקסט עברי — `grep -rln 'חומר' machtzev/generator/*.mjs` לא הניב מסווג כזה

**איפה לחבר:** `∅ בצינור הנוכחי` — הכלל «מילת-חומר בשם ⇒ חומר, אלא אם היא גימור או תת-רכיב» הוא פריט-אוצר-מילים, ושייך לאותה נקודה שנתתי ל-enrich_from_name.py (‏entity-terms.mjs:5). כשלעצמו הוא ספציפי מדי לחבר בנפרד. לא ידוע אם קיים מקבילה מחוברת

### `app_flutter/scripts/fill_modes_from_name.py` — 1

חד-פעמי, הוחל, לא-מחובר, וכלל-החילוץ שלו הוא החלש מבין ארבעת המְמַלאים — התאמת-תת-מחרוזת בלי anchor ובלי בדיקת-הקשר. מה שבאמת שווה כאן הוא המדד (מפרט-אמיתי 87%), שאין לו שער. לא 0: אין מקבילה מחוברת שממלאת מפרט משם-מוצר

**איפה לחבר:** `∅ בצינור הנוכחי (לא ידוע אם קיים מקבילה מחוברת)` — הדבר הנכון לחבר כאן אינו הסקריפט אלא **המדד** שהוא הזיז: «מפרט-אמיתי 802/924». זהו ראצ׳ט-כיסוי-דאטה, בדיוק בצורה של gen-verify-baseline.json ב-machtzev/generator/gen-verify.mjs:87-92 — baseline + סירוב-לרדת. כרגע המדד מתועד בפרוזה ב-mutation_log.md:1409 ואין שער שאוכף אותו

### `app_flutter/scripts/fill_residual_rt.py` — 1

מעבר-שאריות חד-פעמי עם רווח קטן ומדוד (‏+1% מפרט · +1% color) שכבר הוחל, ובלי מחובר. ההנמקה על «בטוח-תמיד מול צריך-גידור» היא התרומה הרעיונית, אבל היא כבר נספרה ב-enrich_from_name.py שקיבל 2. לא 0: אין מקבילה מחוברת

**איפה לחבר:** `∅ בצינור הנוכחי (לא ידוע אם קיים מקבילה מחוברת)` — הרעיון שכן ניתן להעביר: «כלל שאפשר להוכיח שהוא בטוח-תמיד (צבע-טהור≠חומר) לא צריך גידור-קטגוריה; כלל שלא — כן». זו הבחנה שמפחיתה false-positive בלי לוותר על כיסוי, והיא רלוונטית לכל שער-דאטה במחולל

### `app_flutter/scripts/fix_audit_all.py` — 1

עשה עבודה אמיתית ומדידה (חרום⇒חרוט: 0 מול 14 בקטלוג), אבל היום הוא **חוב**: הרצה-חוזרת מסיגה את הקטלוג בשתי תמונות, הכותרת סותרת את הקוד, ואין לו את גידור-gate-117 שהוא מצהיר עליו. לא 0: אין מנוע מחובר שעושה את זה — אבל הציון נמוך כי מה שנשאר הוא לקח-נגטיבי (איך מחיל-תיקונים נשבר), לא כלי

**איפה לחבר:** `∅ בצינור הנוכחי` — האינווריאנט היחיד שכן שווה להעביר — «עריכה חלה רק אם הערך-הישן עדיין קיים, אחרת no-op» — הוא בדיוק מה שמונע ממחיל-אוטומטי לדרוס תיקון מאוחר יותר. אבל הקובץ הזה עצמו מדגים למה זה לא מספיק: IMAGE_NULL כותב ערך **חדש** בלי לבדוק את הישן, וזה בדיוק המקום שבו הוא נשבר

### `app_flutter/scripts/fix_audit_dims.py` — 1

הקטן והשמרני מבין המחילים, ועם הכלל הכי נכון — מחיקה במקום ניחוש. אבל: חד-פעמי, 8 SKUs קשיחים, הוחל, לא-מחובר, ובלי גידור-gate-117 בקוד. ההסרה גם לא-אידמפוטנטית-באמת (מוחקת 'מידה' בלי לבדוק שהיא עדיין המשובשת). לא 0: אין מקבילה מחוברת

**איפה לחבר:** `∅ בצינור הנוכחי (לא ידוע אם קיים מקבילה מחוברת)` — הכלל «ערך-נגזר משובש ⇒ מחיקת-המפתח, לא ניחוש» הוא אותו חוק בדיוק שמנוסח ב-polyroll_dim_engine.py:15 («‏None + סיבה. לעולם לא מנחש») וב-§20 «לעולם לא לזייף דאטה». שני מנועים עצמאיים בריפו הגיעו אליו — זו העדות החזקה ביותר שראיתי לכך שהכלל נכון

### `app_flutter/scripts/generate_stuck_regression.sh` — 3

המנוע היחיד מבין 49 שהוא **שער-חי-ומחובר בתוך buildsmart** עם לולאת-משוב סגורה: מסמך ⇒ מחולל ⇒ בדיקות ⇒ שער שחוסם חוסר-סנכרון. זו בדיוק צורת ה-§22 «אפס-באגים»: באג שתועד פעם אחת לא יכול לחזור בשקט. גם מדיד: 122 אנטי-פטרנים · 105KB בדיקות מחוללות · שני שערים (104, 111). אין לו מקבילה מחוברת ב--ai-chat-server: `learn-check.mjs` הוא שער על **מסמך-הלקחים**, לא מחולל-בדיקות ממנו

**איפה לחבר:** `-ai-chat-server/machtzev/police.mjs — כשער חדש לצד `learn` (שורה 168), הניזון מ-machtzev/LEARNINGS.md · ולצדו machtzev/learn-check.mjs` — המחולל מחזיק LEARNINGS.md ו-`learn`-gate, אבל הלקח שם נשאר **טקסט**. המנגנון כאן הופך לקח לשער-מכונה: שורה במסמך ⇒ בדיקה מחוללת ⇒ שער שחוסם אם הספירות לא תואמות (שער 111). זה הופך «לא לחזור על הטעות» ממשמעת לאכיפה — וזה בדיוק מה ש-machtzev/BULDSMART-PROTOCOL-MAP.md:524 מודה שחסר לנחיל («אין שער-hook שבודק שהנחיל רץ — משמעת בלבד»). 122 אנטי-פטרנים חיים הם ההוכחה שהדפוס מתרחב

### `app_flutter/scripts/harvest_lipski_site.py` — 1

מנוע-השגה כשר בכוונתו — מקור-אמת חיצוני ורשמי (אתר-היצרן) במקום ניחוש, וזה §20 «לעולם לא לזייף דאטה». אבל הקוד עצמו לא מספק: הפלט לא בריפו, אין קורא-הרצה, הוא נכשל בשקט, **ושלוש תבניות-ה-SKU שבו לא עבדו בפועל** (mutation_log.md:1482). לא 0: אין מקבילה מחוברת שמביאה דאטת-מוצר ממקור-יצרן — `grep -rln 'curl\|fetch(' machtzev/generator/*.mjs` ⇒ קובץ **אחד**, machtzev/generator/server.mjs:174,189, ושם זה `fetch` ל-oauth2.googleapis.com לטוקן, לא השגת-דאטה

**איפה לחבר:** `∅ (לא ידוע אם קיים מקבילה מחוברת)` — השגת-דאטה מרשת אינה בצינור-המחולל. הדבר היחיד ב--ai-chat-server שיוצא לרשת הוא הורדת-Dart ב-.claude/hooks/session-start.sh:26-27. חיבור היה דורש מדיניות-רשת ומטמון שאינם קיימים

### `app_flutter/scripts/match_lipskey_pdf.py` — 2

זהו שער-הכניסה של דאטה חיצונית לתוך האפליקציה, והוא מיישם ארבעה כללי-§20 בו-זמנית: דילוג-על-ספק, אי-דריסה של שדה-מפתח, כיבוד-הצמדה קיימת, והעברת-החלטה לאדם במקום ניחוש. §22 נופל בדיוק כאן — דאטה שגויה בקטלוג = מסך שגוי. לא 3: הוא לא רץ (קלט חסר), לא מחובר, ושתי הגדרות gate-117 סותרות בין הקבצים (‏`\d{6}` מול `\d{6,}`) — פער-עקביות אמיתי

**איפה לחבר:** `-ai-chat-server/machtzev/generator/gen-verify.mjs:88-92 ו-golden-harness.mjs:54-58 (בלוקי-ה-`if (gate)`), כדפוס שחסר שם` — שני השערים האלה מסכמים כ«N/M» וחוסמים על נסיגה — אבל **אין להם ערוץ ל«נמצא משהו שאני לא בטוח בו, הכרעת-בעלים»**. match_lipskey_pdf.py:163-167 מדגים בדיוק את זה: שלוש קטגוריות פלט — הוחל · דולג-כי-לא-בטוח · **מועמד-להכרעת-אדם**. זו הצורה שהכרעה-27 דורשת («מה שאין לו מקור ⇒ ∅ מדווח, לא ניחוש») ו-§20 («אין-יחיד ⇒ שלב»), והיא ניתנת להוספה לכל שער-ראצ׳ט במחולל בלי לשנות את הראצ׳ט

### `app_flutter/scripts/mutation_verify.sh` — 2

הערך שלו אינו המנגנון — המנגנון שלו (backup+restore, `sed -i` על הקובץ החי) **נחות** מהמקבילה המחוברת `machtzev/mutation-dart-check.mjs` (שער `mutation-dart`, police.mjs:171), שמחלילה בעותק-sandbox ולא נוגעת בעץ. הערך הוא שהוא מתעד תקרית-אמת מ-2026-06-01 ושם אצבע על כשל **חי ולא-מתוקן** בשער מחובר: golden-harness.mjs:44. §22 דורש «אפס-באגים», ואובדן-עבודה שקט ברתמה הוא באג-על. לא 0, כי המקבילה המחוברת אינה מכסה את buildsmart ואינה מגנה על כתיבת-המראה; לא 3, כי המנוע עצמו לא רץ כאן (אין flutter) ואין לו קורא

**איפה לחבר:** `-ai-chat-server/machtzev/generator/golden-harness.mjs:34 + 44 — הכתיבה על המראה והשחזור שאחריה` — **שם יושב בדיוק הבאג שהסקריפט הזה תיעד.** golden-harness.mjs:34 דורס `lib/genesis/dart-gen-bs/<module>` **בעץ-העבודה החי של buildsmart**, ושורה 44 משחזר ב-`spawnSync('git',['checkout','--',…],{cwd:BS})`. כל עריכה לא-מקומיטת בקובץ-המראה נמחקת. ההערה בראש הקובץ (golden-harness.mjs:6) אפילו מודה «ב-buildsmart רק `checkout -- <file>` לשחזור קובץ שהרתמה עצמה דרסה» — כלומר זה ידוע ולא נפתר. התיקון הנכון אינו הגיבוי שכאן אלא הדפוס שכבר קיים באותו ריפו: machtzev/mutation-dart-check.mjs:77-85 — להעתיק לעותק-sandbox ולא לגעת בעץ כלל

### `app_flutter/scripts/polish_shot.js` — 0

מקבילה מחוברת וטובה יותר בשם: `-ai-chat-server/machtzev/tools/site-shot.mjs` (28 שורות), הנקראת מצינור-המחולל ב-machtzev/generator/ship.mjs:96. היא עושה כל מה ש-polish_shot.js עושה ועוד, וההפרש הוא בדיוק מה שהופך צילום לראיה: ‏(א) `waitForFunction(document.title === t)` ⇒ **הוכחה שה-MaterialApp אתחל** (site-shot.mjs:22) במקום המתנה עיוורת · (ב) ספירת `flt-glass-pane, flutter-view` (24) · (ג) איסוף requestfailed + pageerror + console-error (18) · (ד) הגשת ה-build בעצמה ב-`python3 -m http.server` (14) · (ה) פלט JSON מובנה עם booted/glass/ms/bytes/errors (27). polish_shot.js מצלם מסך לבן ומדווח «OK»; site-shot.mjs מדווח `booted:false`

### `app_flutter/scripts/polish_shot.sh` — 0

כפול למקבילה מחוברת: `-ai-chat-server/machtzev/tools/site-shot.mjs` (28 שורות), הנקראת מ-machtzev/generator/ship.mjs:96. היא מבצעת את **אותו** צינור בדיוק — הגשה ב-`python3 -m http.server` (site-shot.mjs:14) · Chromium headless (16) · צילום (25) — ובאותו פתרון-canvaskit: ship.mjs:85,91 בונה עם `--no-web-resources-cdn` ו-site-shot.mjs:4 מצטט את אותו לקח («L69: --no-web-resources-cdn + גופן-מצורף — אחרת האתר-המנותק נראה ריק/חסר-טקסט»). ההפרש הוא לרעת polish_shot.sh: site-shot מוכיחה אתחול (waitForFunction על document.title, שורה 22) ופולטת JSON עם booted/glass/errors (27), בעוד כאן ההמתנה עיוורת. אין גם אף קורא

### `app_flutter/scripts/post_build.sh` — 0

המנוע פותר בעיה אמיתית (canvaskit מ-gstatic חסום), אבל **הפתרון המחובר לאותה בעיה כבר בשימוש ועדיף**: `machtzev/generator/ship.mjs:85` ו-`:91` בונים עם `flutter build web --release --no-web-resources-cdn`, ו-machtzev/tools/site-shot.mjs:4 מתעד את זה כלקח L69. הדגל של Flutter מצרף את canvaskit **ומגדיר את ה-base בעצמו**, ולכן אין מה לתקן אחרי הבנייה. post_build.sh משיג את אותה תוצאה בהחלפת-מחרוזת שבירה של 8 שורות מדויקות שנשברת בכל שינוי-SDK (post_build.sh:120-122 מודה בכך). גם באותו ריפו הוא כבר עקוף: polish_shot.sh:27 בוחר בדגל, לא בו. מנוע כפול עם פתרון נחות, ובלי אף קורא

### `app_flutter/scripts/protocol_check.sh` — 0

כפול, עם ראיה, לשני מנגנונים שכן רצים: (א) **באותו ריפו** — `orchestrator/scripts/central-verify.sh` (69 שורות) מריץ את אותם analyze/test/build, אבל טוב יותר בכל נקודה שנבדקה: מקבל את נתיב-האפליקציה כארגומנט (שורה 31) במקום cd קשיח, מריץ `pub get` (52) ו-`gen_version.sh` (58-62), סומך על קוד-היציאה **וגם** על ספירת-שגיאות (65-68), ומוסיף manifests של conformance ו-required-tests (79-90). (ב) **השערים החיים** — `.githooks/pre-commit:306,319` מריצים analyze ו-test על כל commit, ו-`.githooks/pre-push:63-66` מריץ build web, כלומר שערים 1-3 של הסקריפט נאכפים ממילא. מה שנשאר ייחודי לו (סנכרון-גרסאות, משטח כהה, ענף) נאכף גם הוא ב-pre-commit (שערים 11/14 ו-1). הוא גם יורה שגיאת-ענף על כל ענף מלבד אחד, ו-cd הקשיח בשורה 7 סותר את נתיב-buildsmart שב-CLAUDE.md של המחולל

### `orchestrator/scripts/assert-manifest.sh` — 3

«ראצ׳ט-מספרים ירוק» הוא בדיוק סוג-ההצלחה ש-§22 לא מקבל — הוא אומר «לא נסוג», לא «נכון». המנוע הזה הופך הצהרת-בייטים לשער, והוא לא כלי-ריק: בריפו מחויבים **269 חוקי-בייטים** על האפליקציה החיה (‏orchestrator/manifests/buildsmart.conformance.txt — למשל `lib/screens/store_screen.dart:::הסל שלי` · `lib/data/personas.dart:::מנהל המערכת`) ועוד **44** בדיקות-חובה. זה המנגנון שהופך את «~270 עלים verbatim» שב-CLAUDE.md מטענה למדידה — בדיוק רצפת-הקבלה של §22. ושני האינווריאנטים שבו הם מה שמונע false-green: **manifest ריק = כשל** (שורה 16) ו**האצלה למקור-אמת יחיד** במקום grep משוכפל (שורה 24). 24 שורות, אפס תלויות. לא 3 היה מפתה, אבל ההרצה שלי מראה את המחיר של אי-החיווט: exit 1 · 253 OK · 16 FAIL, מהם 10 false-negative של ה-grep הבסיסי (המחרוזת קיימת, ה-`[` הלא-סגור מפיל את BRE), 2 באגי-נתיב ב-manifest עצמו (שורות 318-319 בלי `../`), ו-4 drift אמיתי. שער שלא רץ מצטבר לו חוב בשקט; אף hook/CI לא מעביר את ה-manifest, ולכן central-verify.sh:82 מדפיס בפועל «conformance NOT enforced this run». התיקון הוא `grep -F` ב-grep-verify.sh ועוד `../` בשתי שורות — ואז 269/269 הוא יעד מדיד

**איפה לחבר:** `-ai-chat-server/machtzev/generator/gen-verify.mjs:86-93 ו-golden-harness.mjs:54-59 (בלוקי-ה-`if (gate)`)` — שני השערים האלה אוכפים **ראצ׳ט-מספרים בלבד** — `rendered`/`atoms` ו-`regenerated`/`tests` שלא יורדים. מספר שלא יורד אינו אומר שהמחרוזת הנכונה במקום הנכון. assert-manifest.sh מוסיף את הממד החסר: קובץ-manifest שמצהיר «המחרוזת X חייבת להימצא ב-Y» ו-«המחרוזת Z אסורה», עם שני אינווריאנטים שאין להם מקבילה שם — **manifest ריק = כשל** (שורה 16) ו**האצלה למקור-אמת יחיד** (שורה 24). הכניסה הטבעית: אחרי `flutter test` ב-gen-verify.mjs:67, על `path.join(BS,'lib/genesis/dart-gen-bs')`

### `orchestrator/scripts/central-verify.sh` — 3

זהו השער שהפרוטוקול של buildsmart מגדיר כסיום-הנחיל, והוא נבנה מתוך תקיפות-red-team — כל אחד מששת ההידוקים בו הוא תיקון של דרך ספציפית שבה שער ירוק משקר: יעד-שגוי (טביעת-אצבע, 49) · תלויות-לא-פתורות (52) · קוד-מחולל-חסר (58-62) · אמון-ב-regex-בלבד (65-68) · הצהרות-שלא-נאכפו-בשקט (82,89) · כותרת-שלא-אומרת-מה-נאכף (92). §22 דורש «רצפת-קבלה, אין כמעט» — וזה המנוע היחיד מבין 49 שמנסח מה נאכף **וגם** מה לא נאכף בריצה הזו. ומעשית: שלושת השערים של המחולל שנוגעים ב-buildsmart (genverify · goldenharness · appgen) חסרים בדיוק את ההכנה שהוא מבצע

**איפה לחבר:** `-ai-chat-server/machtzev/generator/gen-verify.mjs:66-67 · golden-harness.mjs:37-38 — ממש לפני ה-`spawnSync(FLUTTER,['test',…],{cwd:BS})`` — שני השערים האלה מריצים `flutter test` **בתוך buildsmart** ומניחים שהעץ שם מוכן. הם לא מריצים `pub get`, לא מייצרים את lib/version.g.dart, ולא מדפיסים את מי הם בודקים. central-verify.sh:52 ו-58-62 עושים בדיוק את שני הראשונים, ו-43-49 את השלישי. הבדיקה המדידה: `grep -n 'pub get\|gen_version' machtzev/generator/ship.mjs` ⇒ **∅** — כלומר גם ship.mjs, שמריץ analyze+test+build בתוך buildsmart (שורות 63,68,85), אינו עושה זאת. בעץ טרי זה מתבטא ב-«Target of URI hasn't been generated: version.g.dart», שהוא בדיוק לקח #72 ש-central-verify.sh:56 מתעד

### `orchestrator/scripts/ckpt.sh` — 2

§22 מדבר על צינור שמריץ בנייה-ובדיקה-ופריסה בלחיצה אחת; צינור כזה **חייב** להיות ניתן לחידוש, אחרת כל כשל-ביניים עולה ריצה מלאה ומזמין את השקר «המשכתי מאיפה שהייתי» בלי ראיה. שתי ההחלטות כאן נכונות ונדירות: מצב ב-git-admin dir (לא מלכלך diff, שורד worktree) וכתיבה אטומית. לא 3: `guard` תלוי-משמעת (צריך לקרוא לו), אין נעילה, ואין לו אף קורא-הרצה בשני הריפואים

**איפה לחבר:** `-ai-chat-server/machtzev/generator/ship.mjs — סביב שלבי 1-6 (השורות 61-140), ו-machtzev/generator/regen.mjs` — ל-ship.mjs יש צינור בן 6 שלבים שמסתיים ב-commit+push לשני ריפואים (ship.mjs:115-140), ו**אין לו שום מצב עמיד**: כשל בשלב 4 (בנייה) אחרי שהמראה כבר נכתבה (שלב 2) מחייב הרצה-מחדש מההתחלה, ואין דרך לדעת איפה זה נעצר. `grep -rln 'checkpoint|resume' machtzev/**/*.mjs` ⇒ קובץ **אחד**, machtzev/generator/balagan.mjs:1335, וגם הוא `AppLifecycleState.resumed` בתוך Dart מחולל — כלומר **אפס** מנגנון-checkpoint במחולל. ckpt.sh נותן בדיוק את זה ב-24 שורות, בלי לכלוך ב-`git diff` (כי המצב ב-git-admin dir)

### `orchestrator/scripts/diff-coverage.sh` — 2

§22 היא «רצפת-קבלה, אפס-באגים», ושער-כיסוי שמשקר הוא הדרך השקטה ביותר להפר אותה — הוא מייצר ירוק בלי שנבדק דבר. ההידוק המרכזי כאן (חפיפת-אפס = BLOCK) הוא ממצא-red-team אמיתי ובר-העברה לכל שער-הצטלבות, לא רק לכיסוי. לא 3: הוא בודק **נוכחות** ולא אחוז, אין לו קורא-הרצה, ובמחולל אין בכלל צינור-lcov שיזין אותו — כלומר החיבור דורש קודם להפיק כיסוי

**איפה לחבר:** `-ai-chat-server/machtzev/police.mjs:132 — לצד `gate('coverage','coverage-gate.mjs')`, כשער נפרד לפי-דיף` — **במחולל אין כיסוי-שורות בכלל.** `grep -rn 'lcov' machtzev/ --exclude=engine-index.json` ⇒ **0 התאמות**. השער `coverage` הקיים (machtzev/coverage-gate.mjs:2-5) הוא מחגר על **צירי-יכולת** — «לבנים-ברות-מילוי · מנועים-ברי-הרצה · מהות-עברית · תאומי-דאטה» — ולא על קוד שנגעו בו. כלומר: אפשר לשנות קובץ-מקור במחולל, שהוא לא ייבדק בשום בדיקה, וכל השערים יישארו ירוקים. diff-coverage.sh סוגר בדיוק את זה, ובעיקר מביא איתו את השיעור היקר — שחפיפת-אפס אסור שתיקרא כמעבר

### `orchestrator/scripts/ff-push.sh` — 1

יש מקבילה **מחוברת** וחזקה יותר לרוב מה שהוא עושה: `-ai-chat-server/.githooks/pre-push` (76 שורות), שרץ על **כל** push ולא רק כשקוראים לו, וחוסם main/master/production, מחיקת-ref ולא-fast-forward, ובנוסף מריץ ראצ׳ט-כיוון ומשטרה **מלאה** על worktree detached של ה-sha הנדחף. לא 0, כי שני דברים אצל ff-push.sh אינם שם: (א) חוזה ה-sha-בסיס-הצפוי — pre-push יודע רק אם זה fast-forward, לא אם origin זז מאז שהקורא בדק; (ב) סירוב **קטגורי** ל-main בלי פתח-מילוט, מול ה-`Allow: push-main` trailer שכן קיים ב-.githooks/pre-push:17-19 של המחולל. ההנמקה ב-ff-push.sh:53-55 (מדוע env-bypass אינו שומר) היא הטענה שעומדת מאחורי זה

**איפה לחבר:** `-ai-chat-server/machtzev/generator/ship.mjs:140 — `pushWith(BS, branchOf(BS))`, הדחיפה לתוך buildsmart` — ship.mjs דוחף לשני ריפואים (‏ship.mjs:126-140) אחרי שהוא כבר בנה וקימט. הוא **לא** מקבל sha-בסיס-צפוי ולא בודק שה-origin לא התקדם — הוא בודק רק `status -sb | includes('ahead')` (שורה 140). החוזה של ff-push.sh («הקורא מצהיר מה הוא ראה ב-origin, ואם זה השתנה — עצור») הוא התוספת שחסרה שם, והיא בדיוק מה שמונע דריסה של סשן מקביל

### `orchestrator/scripts/grep-verify.sh` — 2

«בייטים לא פרוזה» הוא חוק-היסוד של הערכה-הזאת, וזה 26 השורות שמממשות אותו. שני האינווריאנטים בו הם מה שמפריד בין אימות אמיתי לתיאטרון: **קובץ-חסר = כשל** (הממצא מה-A/B, שורות 36-38) והדפסת **ספירת-ההופעות** ולא רק ✓. במחולל אין שקול: `grep -rln 'grep-verify' machtzev/ --exclude=engine-index.json` מחזיר רק מסמכים. לא 3, כי הוא פרימיטיב בלי תוכן משלו — התוכן יושב ב-manifests, ולכן הציון 3 ניתן ל-assert-manifest.sh שמחזיק אותם; וכי `grep` בלי `-E`/`-F` אינו מלכודת תיאורטית — הרצתי את 269 החוקים המחויבים דרכו וקיבלתי 16 כשלים, מהם **10 מחרוזות שקיימות בקובץ** ונפלו על `[` לא-סגור. שער-בייטים שמדווח «חסר» על מה שקיים הוא בדיוק סוג-האמון ש-§22 לא יכול להרשות

**איפה לחבר:** `-ai-chat-server/machtzev/generator/gen-verify.mjs:77-85 (בניית ה-report) — כשלב שני אחרי ספירת-האטומים` — gen-verify.mjs מוכיח ש**משהו** רונדר וסופר מחלקות-אטום, אבל לא שמחרוזת מסוימת נמצאת במקום מסוים. זו בדיוק ההבחנה בין «המסך לא קרס» ל«המסך מציג את מה שהמשפט ביקש». grep-verify.sh היא הפרימיטיב ל-31 מילים בעברית שאמורות להופיע במסך מחולל. בריפו הזה הוא כבר משמש כמקור-אמת-יחיד ל-269 חוקים דרך assert-manifest.sh:24 — זהו הדפוס להעביר

### `orchestrator/scripts/lens-coverage.sh` — 2

§22 היא «אפס-באגים», וכשהביקורת עצמה חלקית — הבאגים שאף עדשה לא חיפשה פשוט לא קיימים בדוח. זה הכשל השקט ביותר שיש, והמנוע הזה הופך אותו לנראה ב-26 שורות. יש לו גם תוכן מדיד בריפו הזה: `orchestrator/lenses/registry.txt` מונה **10** מזהי-עדשה (‏cross-role · money-numeric · edge-crash · state-leakage · navigation · performance · accessibility-rtl · async-race · data-seed · text-parity — 13 שורות, מהן 3 הערות), וזה השער היחיד שיכול לאכוף שכולן חזרו. (‏CLAUDE.md חוק-על #0 כותב «9 עדשות»; ה-registry מונה 10 — פער-תיעוד, וה-registry הוא הקובץ שהשער קורא.) לא 3: הוא בודק שם-קובץ ולא תוכן (קובץ ריק עובר), הוא חד-כיווני במקום שהמקבילה ב-police.mjs:178-180 דו-כיוונית, ואף אחד לא מריץ אותו

**איפה לחבר:** `-ai-chat-server/machtzev/police.mjs:176-180 — בלוק «פריטי מרשם⇄ריצה» שבסוף` — **הדפוס כבר קיים שם, על שערים, ולא על עדשות.** police.mjs:178-180 מיישם בדיוק את אותה בדיקה דו-כיוונית: «שער רשום שלא רץ ולא דווח» ו«שער רץ שאינו במרשם», ושורה 177 מוסיפה «שערים דולגו במצב מלא». זו ההוכחה שהעיקרון מקובל על המחולל — ובאמת, lens-coverage.sh הוא אפילו **חלש יותר** ממנו, כי הוא חד-כיווני (‏`comm -23` בלבד) ואינו תופס עדשה שחזרה ואינה במרשם. החיבור: להחיל את בדיקת-המרשם הדו-כיוונית של police.mjs גם על רשימת-עדשות-ביקורת, ולא רק על שערים. במחולל היום אין שום דבר כזה: `grep -rln 'עדשה|lens' machtzev/ --include=*.mjs` ⇒ 2 קבצים, ושניהם «עדשה-סמנטית» על אינטרפולציית-מחרוזות (board-gen.mjs:93 · lift-lib.mjs:116) — לא קשור

### `orchestrator/scripts/registry.sh` — 2

§22 נשען על נחיל, ונחיל נכשל בשקט כשנתיב אחד לא חוזר — הדוח נראה שלם כי אף אחד לא סופר את מה שלא הגיע. ההידוקים כאן נכונים ומאומתים-בהרצה: התנגשות-id ⇒ 2, סגירה-כפולה ⇒ 2, פתוח-בסיום ⇒ 1. לא 3: `reset` מוחק שורות OPEN ובכך עוקף את השער עצמו, אין אכיפת-דיסיונקטיביות על scope, ואין לו אף קורא-הרצה

**איפה לחבר:** `-ai-chat-server/machtzev/police.mjs:176-180 — אותו בלוק «פריטי מרשם⇄ריצה» שכבר אוכף זאת על שערים` — police.mjs כבר מיישם בדיוק את העיקרון הזה, אבל על **שערים**: «שער רשום שלא רץ ולא דווח» (179) · «שער רץ שאינו במרשם» (180) · «שערים דולגו במצב מלא» (177). registry.sh הוא אותו עיקרון על **סוכנים**. הצורך קונקרטי: הצינור של המחולל משגר תת-סוכנים (‏machtzev/wave-partition.mjs ב-STATIC של pins-check.mjs:22), ואין שם ספירת-פתוחים. ההערכה ב-CURRICULUM-RAW.md:1257 כבר מזהה את זה כמנגנון ראוי-לחיקוי

### `orchestrator/scripts/report-lint.sh` — 2

§22 היא **רצפת-קבלה**: «אין כמעט». הכשל שמאיים עליה הכי הרבה אינו באג בקוד אלא דוח שאומר «נקי» ומחזיק תחתיו פריטים נדחים — וזה בדיוק מה שנאסר כאן, ב-jq טהור, בלי תלויות, ומאומת ב-8 בדיקות שהרצתי. לא 3: הוא בודק עקביות-פנימית בלבד — דוח ריק ומלוטש עובר; ואין בריפו אף דוח-ריצה שהוא בודק בפועל

**איפה לחבר:** `-ai-chat-server/machtzev/police.mjs:181-183 — שורת-הסיכום שמחשבת `ran/skipped/yellow/failed` ומחליטה ירוק/צהוב/אדום` — המשטרה כבר אוכפת שני כללים מאותה משפחה: «שערים דולגו במצב מלא ⇒ אדום» (177) ו-«צהוב = חוסם» (‏.githooks/pre-push:4 של המחולל). מה שחסר הוא הכלל השלישי, וזה בדיוק report-lint: **דוח שמצהיר CLEAN אינו רשאי לשאת עבודה-נדחית או אי-התאמה לא-פתורה**. במחולל הדבר הקרוב הוא ה-ledger של gen-verify/golden-harness שמדלגים בשקט כשאין buildsmart (‏gen-verify.mjs:22 · golden-harness.mjs:24) — «⚪ מדולג» שנספר כלא-כשל. report-lint.sh הוא הכלי שהופך «דילגתי» ל-`deferred` שחייב `reason` ואוסר עליו להיקרא CLEAN

### `orchestrator/scripts/required-tests.sh` — 2

«הרצנו את כל הבדיקות שיש» אינו «הזרימות הקריטיות מכוסות» — וההפרש הוא בדיוק מה ש-§22 גובה. זה גם המנוע היחיד מבין 49 שהרצתי על התוכן **המחויב** שלו ועבר במלואו (44/44), כלומר הוא לא כלי-ריק. ההנמקה שבו («נוכחות + סוויטה-ירוקה ⇒ רץ ועבר») נכונה ומוצהרת, וההתאמה היא `grep -qF` — בלי מלכודת-ה-regex שפוגעת ב-grep-verify.sh. לא 3: הוא עדיין בודק קיום ולא תוכן, ה-manifest כמעט לא מנצל את `:::`, ואף hook/CI לא מעביר אותו ל-central-verify

**איפה לחבר:** `-ai-chat-server/machtzev/generator/gen-verify.mjs:25-29 — הבחירה של קובצי-הבדיקה · ו-golden-harness.mjs:17-19 (מפת TESTS)` — שני השערים מגלים את מה שיש: gen-verify.mjs:25 סורק `readdirSync(DIR)` ומסנן לפי regex, ו-golden-harness.mjs:19 גוזר שם-בדיקה משם-המודול. אין ביניהם **רשימה מוצהרת** של מה חייב להתקיים — ולכן קובץ-זהב שנמחק פשוט נעלם מהספירה בלי שאף שער יצעק, והראצ׳ט (‏gen-verify.mjs:92) יתפוס זאת רק אם המספר יורד. `if (!screens.length) … process.exit(0)` ב-gen-verify.mjs:29 הוא בדיוק המצב הזה — אפס מסכים = יציאה ירוקה. required-tests.sh הוא ה-manifest שחסר שם, והוא כבר מוכיח את עצמו כאן: 44/44 בהרצה

### `orchestrator/scripts/selftest.sh` — 2

זו הבדיקה שמונעת מהשערים עצמם להיות שקר — «בודק את הבודקים» — והיא הדבר היחיד מבין 49 שהצלחתי **להריץ במלואו ולראות ירוק** (49/49, SELFTEST PASS). המבנה נכון: הרמטי, קודי-יציאה מדויקים, ובעיקר מקרים-שליליים לצד חיוביים. לא 3 משתי סיבות מדודות: (א) **4 מתוך 12** הסקריפטים אינם נבדקים בכלל, וביניהם central-verify (השער) ו-ff-push (הדחיפה); (ב) השוואת קוד-יציאה בלבד — שער שחוסם מהסיבה הלא-נכונה עובר, וזו בדיוק ההבחנה שהמקבילה המחוברת (audit-gates.mjs:2-3) כן דורשת

**איפה לחבר:** `-ai-chat-server/machtzev/police.mjs:169-170 — לצד `gate('selftest','police-selftest.mjs')` ו-`gate('audit-gates','audit-gates.mjs')`` — למחולל **יש** כבר בדיקה-עצמית למשטרה (`police-selftest.mjs`) ולשערי-ה-staging (`audit-gates.mjs`), ושתיהן חזקות מזו: audit-gates.mjs פותח worktree, מזריע הפרה ומריץ `git commit` אמיתי, ודורש את **שם-השער** בפלט (שורות 2-3, 17-21). לכן מה שכדאי להעביר הוא בכיוון ההפוך: להביא את דרישת-«הסיבה-הנכונה» של audit-gates.mjs אל selftest.sh, ואת הכיסוי החסר (‏central-verify · ff-push · wt-setup) — ואז יש לכלי-הנחיל אותה רמת-ראיה שיש לשערי-המחולל

### `orchestrator/scripts/wt-setup.sh` — 2

9 שורות שמסירות מחלקת-כשל שלמה: ריצת-צינור שמלכלכת או הורסת את עץ-העבודה של המפתח. §22 מדבר על צינור אוטומטי, ואוטומציה שרצה בעץ החי היא סיכון-נתונים, לא רק אי-נוחות — ובמחולל זה **קיים היום** ב-golden-harness.mjs:34,44. לא 3: הוא בולע כשל-fetch (סתירה ישירה ל-ff-push.sh:68-72 באותה תיקייה), אינו מנקה אחריו, ואינו נבדק ב-selftest

**איפה לחבר:** `-ai-chat-server/machtzev/generator/golden-harness.mjs:28-34 — שם נכתב המראה **לתוך עץ-העבודה החי** של buildsmart` — זו נקודת-החיבור המשלימה לזו של mutation_verify.sh. golden-harness.mjs:34 עושה `fs.writeFileSync(mirror, r.code)` על `BS/lib/genesis/dart-gen-bs/<module>` — עץ-העבודה האמיתי — ומשחזר ב-`git checkout` (44). worktree detached על ה-sha הנוכחי, כפי ש-wt-setup.sh יוצר בשורה אחת, מייתר את כל הבעיה: הרתמה כותבת בעותק ומוחקת אותו בסוף, ושום עריכה לא-מקומיטת אינה בסיכון. `machtzev/audit-gates.mjs:17` כבר משתמש בדיוק בדפוס הזה (‏`worktree add --detach -q`) בתוך אותו ריפו — כלומר הכלי קיים שם ופשוט לא הוחל על הרתמות

### `scripts/bootstrap-studio-pointer.mjs` — 2

בניגוד למה שהאינדקס מסמן, זה מנוע **חי ומחובר**: הוא רץ ב-CI (‏firebase-deploy.yml:97) ויש בדיקה שמוודאת שלא הוסר משם (‏functions/src/selftest.ts:198). ההנדסה שבו מדויקת — `create()` אטומי שלא יכול לדרוס תצורה שפורסמה, וזיהוי ALREADY_EXISTS בשלוש דרכים. והוא נוגע ב-§22 בנקודה אמיתית: cold-start ראשון מול backend הוא בדיוק המקום שבו «אפליקציה עובדת 100%» נשברת למסך-ריק. לא 3: הוא פותר מקרה-קצה של פריסה ראשונה, לא את הצינור; ובמחולל, שכן מחולל firestore.rules (‏server.mjs:54-59), הוא עדיין תוספת ולא חסם

**איפה לחבר:** `-ai-chat-server/machtzev/generator/server.mjs:54-59 (‏`rulesFor` שמחולל את firestore.rules) ו-machtzev/generator/server-gate.mjs (שער `server`, police.mjs:145)` — המחולל **כן** מחולל חבילת-Firebase מלאה: `server.mjs:54-59` פולט `rules_version='2'` + `service cloud.firestore`, שורות 104-112 מדריכות `firebase deploy --only firestore:rules` ואז `--only functions`, ו-`server-gate.mjs:2-3` אוכף שלוש שאלות (הוכרז⇒נפלט · לא-הוכרז⇒לא-נפלט · אפס-סודות). מה שאין שם הוא **השאלה הרביעית — סדר**: כלל שנועל כתיבה חייב להתפרסם **אחרי** שהמצב-ההתחלתי נזרע, אחרת הלקוח הראשון קורא מסמך חסר ואין baseline לנסיגה. bootstrap-studio-pointer.mjs מדגים את הפתרון (‏`create()` אטומי, idempotent, לא-הרסני) **ואת האכיפה**: buildsmart מאמת בקוד שהצעד לא נשמט מה-workflow (functions/src/selftest.ts:198). זו תוספת ישירה ל-server-gate.mjs

### `scripts/build_clean.sh` — 1

בפועל זו שורת-`flutter build` אחת עם 25 שורות תיעוד. התיעוד עצמו טוב — הוא מגדיר את החוזה של הפרופיל ומפריד בין בנייה לפריסה — אבל הסקריפט אינו מאמת דבר, אינו מריץ את הבדיקה שנועצת את הפרופיל, ואין לו אף קורא. גם ה-PATH שלו סוטה מכל שאר הריפו. לא 0: הציר «פרופיל» אינו כפול לציר «נקודת-כניסה» שב-ship.mjs:91, ואין מקבילה מחוברת לבחירת-פרופיל

**איפה לחבר:** `-ai-chat-server/machtzev/generator/ship.mjs:86-93 — הלולאה `for (const [name, entry] of SITES)` שבונה אתר פר-כניסה` — ship.mjs כבר מיישם «קוד אחד ⇒ הרבה אפליקציות», אבל הציר שלו הוא **נקודת-כניסה** (‏`-t lib/genesis/dart-gen-bs/${entry}`, שורה 91) ולא **פרופיל**. build_clean.sh מדגים את הציר השני: אותה נקודת-כניסה, `--dart-define=APP_PROFILE=<x>` שונה, ומטריצה מוצהרת של מה דלוק/כבוי בכל פרופיל (שורות 69-77). זו הצורה של «לבן-תווית» ש-§22 ירצה כשהמשפט בעברית יגיד «אותה אפליקציה, בלי השרת שלי» — וזה גם הציר שמאפשר לנעוץ אותו בבדיקה (`app_profile_flags_test`), מה שלנקודת-כניסה אין

### `scripts/catalog_import.py` — 2

שני דברים בו מקרבים את §22 ישירות. (א) **escaping נכון של פלט-Dart** — `\`, `'` **וגם `$`** — כי משפט בעברית-חופשית שמכיל `$` או גרש הוא בדיוק המקרה שישבור אפליקציה מחוללת, ורוב הפולטים שוכחים את ה-`$`. (ב) **שער-קנה-מידה מוטמע**: `--bench` עם סף מוצהר (30 שניות ל-50K) שיוצא 1 — והרצתי אותו: 468.7ms, פי 64 מהסף. §22 מדבר על אפליקציה עובדת, ואפליקציה עם 10K מוצרים היא דרישה שהוזכרה מפורשות ב-knowledge/studio-plan. לא 3: אינו כותב, אינו מאמת מול מקור, ואף hook/CI אינו מריץ אותו — רק 4 בדיקות-יחידה ב-scripts/test_catalog_qa.py נוגעות בו

**איפה לחבר:** `-ai-chat-server/machtzev/generator/app-ds.mjs / render-module.mjs — שלב פליטת-ה-Dart · ובפרט הכלל ב-catalog_import.py:47-51` — המחולל פולט Dart מקוד, ולכן הוא **חייב** לטפל באותן שלוש מלכודות: `\`, `'` ו-`$` (אינטרפולציה). הנקודה החזקה כאן היא דווקא ה-`$`: מחרוזת-דאטה עברית שמכילה `$` הופכת ב-Dart לאינטרפולציה ושוברת קומפילציה או, גרוע יותר, פולטת טקסט אחר. ‏`machtzev/generator/hamtzaa.mjs` (שער `hamtzaa`, police.mjs:140) הוא גלאי-ההמצאה של המחולל — שם המקום שבו כלל «ערך-דאטה נפלט תמיד escaped» שייך. וה-`--bench` הוא הדפוס השני: **שער-קנה-מידה בתוך הכלי**, עם סף מוצהר ויציאה 1 — משהו שאין לאף שער במשטרה

### `scripts/catalog_qa.py` — 2

623 שורות, 21 פקודות, 10 חוקים שכולם מוכיחים את עצמם (הרצתי), ודאטה חיה: 923 מוצרים, אפס ERROR. הוא גם מנסח את כלל-§20 בשורה 20 ומודד אותו (`truthcheck`). אבל **המדידה שלו עצמה מראה את התקרה**: כיסוי-אימות 29%, 199 שמות שאינם תואמים למקור, 307 מוצרים בלי מידה — וכל אלה INFO/מודפס ולא חוסם. §22 היא רצפת-קבלה; מנוע שמודד את הפער ולא חוסם עליו מקרב, אבל לא סוגר. הוא כן המנוע הכי-מחובר מבין ה-13 שב-scripts/ (‏CI ייעודי ב-catalog-qa.yml), אבל ה-hook שמריץ אותו — scripts/hooks/pre-commit — אינו ה-hook הפעיל. לא 3 גם בגלל TYPE_HINT — מילון-דומייני קשיח, שהוא בדיוק מה ש-§20 אוסר

**איפה לחבר:** `-ai-chat-server/machtzev/generator/hamtzaa.mjs (שער `hamtzaa --ratchet`, police.mjs:140) ו-machtzev/purity/purity-data.mjs (שער `puredata`, police.mjs:125)` — הדבר בעל-הערך כאן אינו רשימת-החוקים אלא **`selftest` שמרעיל fixtures ומוכיח שכל חוק יורה** (catalog_qa.py:224-256; 10/10 בהרצה שלי). זה הדפוס שמפריד בין מנוע-חוקים למנוע-חוקים-שאפשר לסמוך עליו, והוא בדיוק מה שחסר לשערי-הראצ׳ט של המחולל: `hamtzaa` מודד המצאות מול baseline, אבל אין לו הוכחה שהגלאי עצמו עדיין יורה. התוספת: fixture מורעל פר-חוק, כמו ב-machtzev/pretool-selftest.mjs שכבר עושה זאת ל-hook — רק שכאן זה על חוקי-דאטה

### `scripts/compare_local_vs_flip.sh` — 1

עוזר-פיתוח מוצהר, לא שער: ההפרש מדפיס אזהרה ויוצא 0, ההשוואה היא שמות-וגדלים בלבד, ה-flip נבנה מול כתובת `.invalid` ולעולם לא מורץ, ואפילו ה-sw.js נשמר רק מצד אחד. אין לו קורא, והוא מפנה לבדיקה בשם `search_parity_golden_test` שאינה קיימת בריפו. מה שכן שווה הוא הרעיון שההשוואה נעשית על **מניפסט-הנכסים** ולא על גודל-חבילה — זו המדידה הנכונה לשאלה «האם הדלקת ה-backend שינתה מה נארז». לא 0: אין מקבילה מחוברת שמשווה שתי תצורות-בנייה; הביט-זהה של המחולל הוא על מקור, לא על פלט

**איפה לחבר:** `-ai-chat-server/machtzev/generator/ship.mjs:85-93 — מיד אחרי הבניות · והשוואת-הפלט ליד machtzev/generator/gen-verify.mjs:31 (בדיקת סחף-מראה)` — למחולל **יש** כבר את המושג «ביט-זהה» ומיישם אותו על **קובץ-מקור** — gen-verify.mjs:31 ו-golden-harness.mjs:33 בודקים שהמראה ב-buildsmart זהה למקור בגנסיס. מה שאין הוא ההקבלה על **הפלט הבנוי**: מניפסט-הנכסים של הבנייה. זה חשוב כי הדגלים ב-ship.mjs (‏`--no-web-resources-cdn`, `--base-href`, `-t <entry>`) משנים מה נארז, וסחף שם אינו נראה בשום שער. הדפוס שכאן — `find … -printf '%P\t%s\n' | sort` ואז diff — הוא שלוש שורות שמוסיפות את המימד הזה

