# הנחיה: קודם — preview עם האשף חי (`ORG_CONFIG=true`) → לינק · אחר-כך — המשך רגיל

> **ביצוע:** `claude/whats-happening-LyY9G` · **המשימה-הראשונה מאושרת-לדחיפה** (הבעלים רוצה את הלינק עכשיו); המשך רגיל = על "תדחוף".
> **עקרון:** 🚫 האתר-הראשי-החי לא-נגע · ערוץ-preview נפרד · additive · שער-ירוק.
> **מטרה:** הבעלים רוצה **לפתוח לינק ולשחק עם האשף** (giant-V5) — שהוא כרגע רדום מאחורי `ORG_CONFIG=off`.

---

## 🥇 משימה 1 · קודם — preview עם האשף חי, ודחוף מיד
> **תיקון (בעלים 27/7):** בנה על פרופיל **`clean`** — לא `demo`/`buildsmart`. clean = empty-shell (**אפס-דמו**) אבל יש בו את כל המסכים/המנוע → כך מקבלים **גם את האשף וגם אפס-נתוני-דמו**. אם ב-clean הכניסה-לאשף/דשבורד-מנהל חסומה — **חשוף אותה ב-clean** (הכניסה גדורה ב-`kOrgConfigFlag`, לא בפרופיל).
1. בנה את האפליקציה עם **`--dart-define=APP_PROFILE=clean --dart-define=ORG_CONFIG=true`**.
2. פרוס ל**ערוץ-Firebase-Hosting נפרד** (למשל `wizard`) — **האתר-הראשי-החי לא-נגע** — **והדפס את הלינק ב-run-summary.**
   - הכי-קל: עמודת-מטריצה נוספת ב-`clean-two-links.yml` (`ORG_CONFIG=true`), או workflow-אח קטן `wizard-preview.yml`.
3. **דחוף/הרץ מיד** — זו המשימה הראשונה, לפני כל דבר אחר.

**DoD (המשימה גמורה כש):** יש **לינק חי** שבו:
- נכנסים כמנהל → מופיע כרטיס **"🔌 אשף הקמת חברה"** בדשבורד (`manager_dashboard_screen.dart:3652`);
- האשף נפתח → אפשר: **לבחור חבילת-ורטיקל · לכבות/להדליק 13 מודולים · לערוך 6 מונחים · לתת שם-חברה**;
- **"שמור והפעל"** → האפליקציה **מתכווננת חי** (אפס-ריסטארט);
- הלינק מודפס ב-run-summary. **האתר-הראשי-החי לא זז.**

---

## 2️⃣ אחר-כך · המשך רגיל
חזור ל-`DIRECTIVE-giant-phase2-features.md` — **גל-4** (משאבים/תזמון: 🚗 חדר≈רכב · 🎫 מכסה-מראש · 📅 חזרתיות · 🕒 ציר-זמן) + tails-של-גל-3. גל-אחר-גל · בייטים+שער · עצור-לדיווח בין-גלים.

---

## 🛡️ הגנות
`ORG_CONFIG=true` **רק בערוץ-ה-preview** — ברירת-המחדל של הבנייה-החיה נשארת **OFF** (הקיים זהה-בייטים) · האתר-הראשי לא-נגע · שער-ירוק · הישן ל-rollback.
