# הנחיה: שילוב-מלא של מאור → BuildSmart (מה שנשאר · מדורג)

> **אושר ע"י הבעלים** (27/7: "סיימת עם מאור, הגיע הזמן לגמור, לשלב מלא"). **מאומת בחקירה-אחורה על הקוד** (אודיט מלא, 27/7).
> **ביצוע:** `claude/whats-happening-LyY9G` · הצי · רב-פעמי · **דחיפה רק על "תדחוף".**
> **עיקרון:** additive · מאחורי `featureOn`/דגל · **default-off-לחי = זהה-בייטים** · **דפוס-מומש-ב-Dart** (לא-מועתק מ-React) · `toggle-matrix` + שער.

---

## הסטטוס (מאומת בבייטים) — ~60% כבר בפנים, והחלק העליון-בדירוג
- **✅ DONE (8):** מנוע-התראות "דורש-טיפול" #1 (`attention_engine.dart`, מחווט) · כרטיס-לקוח+ייבוא+dedup #3 (`customers_store.dart`+`customer_import.dart`) · RFM #4 (`customer_score.dart`) · חיפוש-סובל-שגיאות #5 (`fuzzy_match.dart` Damerau) · מאתר-קטלוג-לפי-צירים #6 · רופא-נתונים #10 (`data_quality.dart`) · תיקון-באג-ח"פ C1 (`validBusinessId` checksum) · נרמול-טלפון C2.
- **🟡 PARTIAL (5):** workflow-kernel **בנוי אך לא-מחובר** #2 (`workflow_engine.dart`, רק טסט) · credit **בלי-יומן** #4 · לוח **DST-core בלי-חגים** #12 · CSV **ייבוא-BOM בלי injection-guard-לייצוא** C3 · סבילות-קלקול **בלי migrate()/quarantine** C4.
- **🔴 MISSING (6):** ציר-זמן-נגזר #7 · לוח-משאבים+חסימת-חג #8 · מכסה-מראש #9 · חזרתיות+דאבל-בוקינג #11 · מספור-מסמכים-רץ #13 · דוח-הרחבה-יומי #14 · cloud-merge C5.

## מה מאור **הוסיף** מאז שפארקנו (21/7) — חדש, כולם REUSABLE
| מנוע-מאור | דפוס | נוחת אצלנו |
|---|---|---|
| **core.timer** | rate×זמן → ₪ חי + תקרות + "גבה תשלום" | **חיוב-לפי-זמן** לקבלן/קריאת-שירות (יש שעונים, אין חיוב) |
| **core.cashbox** | קופה-רושמת (עודף+חשבונית) + **מונה-רץ** | **POS/צ'ק-אאוט לספק** + מספק את **מספור-הרץ** (משלים #13) |
| **core.bodymap** | אזור×מונה-התקדמות פר-לקוח | **punch-list אתר/פרויקט** (חדרים/קומות × בוצע/יעד) — הדאטה הרפואית נעולה |
| **supporters.doncal** | heatmap-חודשי פר-ישות (יום→סכום/כמות) | **לוח-הוצאה/משלוח פר-לקוח** (וריאנט של ציר-הזמן #7) |
| **settings.dedup** | מציאת **קבוצות-כפילות קיימות** + מיזוג (keeper/losers) | **כלי מיזוג-לקוחות/SKU** (הייבוא מונע חדשים; חסר מיזוג-קיימים) |
| נעול-תחום | מוני-ילדים · קבלת-סעיף-46 | לא-מוטמע (עמותה-בלבד) |
| verticalPacks | חבילות-ורטיקל | **כבר-פורט** (`vertical_packs.dart`) — לא-נשאר |

## מודל-הדאטה — כבר תואם
`OrgConfig` של מאור (`slug·orgName·theme·modules·features·terms·firebase`) = **בדיוק** ה-`org_config.dart` שלנו (אותם `moduleOn/featureOn/termOf`). **חסר אצלנו:** `logoDataUri·accent·orgTaxId/signatory(§46)·integrations·adminEmails·firebase-פר-org`. *(האשף שהעלינו לחי = העורך-החי של המודל הזה.)*

## הסדר (פרוסות · עצור-לדיווח בין כל אחת)
1. **חבר את `workflow_engine` (#2)** — 🥇 הכי-זול/משתלם: ה-kernel בנוי+בדוק, רק **אין צרכן חי**. חבר לצינור הזמנות/משימות/**גבייה** (מסך-מנהל) תחת `featureOn`.
2. **מספור-רץ (#13) + קופה (`cashbox`)** — מונה-מונוטוני-פר-סדרה לחשבוניות/קבלות/ת.משלוח (מאור `receiptSeq`) + קופה-רושמת (POS) לספק.
3. **כלי מיזוג-כפילויות (`dedup` חדש)** — מצא+מזג לקוחות/SKU **כפולים-קיימים**.
4. **ציר-זמן-נגזר (#7)** — פיד-פעילות פר-הזמנה/לקוח/אתר ("גזור, לא שמור") + וריאנט-לוח (`doncal` heatmap).
5. **`timer` — חיוב-לפי-זמן** — rate×time→₪ לעבודת-קבלן/קריאת-שירות.
6. **credit-עם-יומן (#4 שארית)** — `cred.log` דלתא+סיבה + tiers זהב/כסף/ברונזה מעל תקרת-האשראי הקיימת.
7. **דחוי (ספק/צי):** חזרתיות+דאבל-בוקינג #11 · לוח-משאבים+חסימת-חג #8 · מכסה-מראש #9 · חגים-עבריים להשלמת #12.
8. **הקשחה (רק אם נדרש):** injection-guard לייצוא-CSV C3 · `migrate()`/quarantine C4 · counter-max merge C5 · checksum גם לת.ז.

## בטיחות
כל פרוסה: `featureOn`/דגל · **default-off-לחי=זהה-בייטים** · דפוס-מומש-ב-Dart · **`toggle-matrix` + mutation-verify + `central-verify` ירוק** · לא-נוגעים-בקיים (רק מוסיפים) · עצור-לדיווח.

## DoD
כל 6-ה-MISSING + 5-ה-PARTIAL מוטמעים כדפוסים-ב-Dart, מחווטים-למשטח-אמיתי, מאחורי-דגל, default-off=זהה-בייטים, toggle-matrix+שער ירוקים. **הערך-הנשאל-ממאור = 100% בפנים.**
