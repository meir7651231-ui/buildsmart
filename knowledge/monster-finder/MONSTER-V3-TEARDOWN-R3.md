# MONSTER — פסק-דין סבב-3 (קריעת v3)

> 83 ממצאים · 30 חוסמים · 41 מז'ורים · 3 שרדו.


## פסק-דין

v3 is a genuine, honest advance and its P0+P0.5 spec is build-ready — but round 3's fresh economic and vision-in-practice lenses expose that the plan is now OVER-built relative to its felt value, and three CODE-REAL defects survive into v3 that must be fixed before the cut-over ships. The loop IS converging on engineering rigor (38->9->~4 true must-fix), but it is DIVERGING on scope: each round added units (38->89->129) while the shippable, user-felt core stayed ~2 lines of routing + 3 safety fixes on an already-built 459-line screen that is OFF on every live build. Verdict: the owner should STOP planning and BUILD a ~15-unit slice now (copper path + identity isolation + the #41 glyph + feature-flag fail-open + a preview deploy), ship it to a NON-live preview channel so the owner can FEEL it, and defer the entire <=4/<=6 proof apparatus (P7/P8 census, single-source BFS, nightly cron, a11y screen-reader contract) until a measured complaint exists. Round 4 is NOT warranted — further teardown is now negative-ROI; the remaining findings are either polish or independently-shippable bug fixes that need no MONSTER scaffolding. Two round-3 claims are themselves WRONG and must be discounted (see newIssues): the feature-flag SPOF does NOT vanish the feature (it fails OPEN to forced-on defaults), and the import-direction concern is real but minor.

## convergence

CONVERGING on rigor, DIVERGING on scope. Blocker trajectory 38 -> 9 -> ~4 genuine code-real must-fixes (the glyph, the _pushStep/_busy+resolve-state-machine, the CI workflow-path/taskkill portability, and the engine-vs-census metric mismatch). The round-3 majors are now overwhelmingly POLISH or independently-shippable: telemetry (reuse an existing seam), cart idempotency (one method swap), the material-gate browse test, the shared-lexicon carve-out. CRITICALLY, almost every 'blocker' the auditors cite is one of two things: (a) vaporware that is CORRECTLY a forward-looking plan step (resolveQuery, reachWithin4, greedyReach, planLineFromPicks, kMaxDiveTurns, collapseKeyOf, divePoolBySku, crossesSystem all grep to ZERO in lib/ — as expected for an unbuilt plan), or (b) a real bug in ALREADY-SHIPPED code (the glyph, _busy scope, append-only cart, feature-flag read, finderGroupFor-null) that should be fixed as a standalone bug fix regardless of the monster vision. The findings are no longer fundamental to the plan's architecture — the foundation (card_engine pool-as-parameter, the verdict ladder, the P0 contract phase) is solid and code-true. The plan is not getting LESS buildable; it is getting more elaborate around an unchanged ~15-unit felt core. That is the signal to stop teardown and build.

## readyToBuild

BUILD NOW — do not run round 4. P0 (1-8) and the genuinely load-bearing, independently-valuable parts of P0.5 are build-ready: the identity-isolation cluster (22-25, stream-path A->B wipe — a real shared-tablet bug), the uid-namespaced prefs (26-31, real cross-tenant leak), the copper routing (35/40, the module's reason to exist), and the feature-flag fail-open hardening. These are correctness fixes worth shipping on their own merits. The HONEST call: ship a ~15-unit slice this week behind a PREVIEW deploy — copper path + identity isolation + the #41 glyph + the _pushStep/resolve state-machine + cart idempotency + feature-flag try/catch + telemetry seam — and let the owner FEEL it on a real URL. That is the make-or-break artifact. DEFER as polish/research-backlog (NOT build-blocking): the entire feasible-proof harness (P0.5 9-18 census/BFS), P7 merged-brain census, P8 hop-graph superHub-STAR, P9 softTilt delight layer (unbuilt and, as specced, requires the engine restructure the engine's own doc at card_engine.dart:206-213 flags), and P12 a11y screen-reader contract. The three code-real blockers (glyph, _busy/resolve, CI portability) must be fixed before the cut-over flag flips, but they are small and need no further planning. Round 4 would be negative-ROI: no NEW finding is structural/irreversible, everything is flag-gated OFF so a live iteration cannot regress real users, and production feedback is now a cheaper, truer oracle than another 9-lens teardown.

## חוסמים שנותרו (code-real)

- **bs_keyboard.dart:908-914 (_PredictionChip DESTINATION Row) vs doc :884-885; v3 step 119**
  - CODE-VERIFIED STILL-BROKEN: the destination chip's Row renders `children: [Flexible(child: label)]` ONLY — there is NO Icon(Icons.north_east), despite the adjacent doc comment (:884) promising 'Leading north-east glyph'. The sole visual difference from a search chip remains colour-only (brand border + 0.06 wash), failing WCAG 1.4.1. This is the identical R2 blocker; v3 step 119 PLANS the fix but it is unmet at byte level today. The i18n-lens-8 and test-oracle findings are correct on this point.
  - תיקון: Add Icon(Icons.north_east, size:14, color:BsTokens.brand) as the leading child of the destination Row, with an SizedBox(width) gap before Flexible(label). Add a STRUCTURE test (find.byIcon(Icons.north_east) inside the destination subtree), not a colour assertion, so a future colour-only regression reds. This is a ~3-line fix shippable independent of the monster.
- **card_keyboard_screen.dart:204-213 (_pushStep auto-open on CardResolve) + :355-368 (_ProductTap); v3 steps 19/20/110**
  - CODE-VERIFIED: _pushStep adds the step and, on CardResolve, opens the sheet WITHOUT clearing the stack (line 211), so the screen sits in {stack non-empty, verdict==CardResolve} with _keysFor(CardResolve)=const[] (:280) and _headerFor=''(:301) — a blank-header zero-key dead-screen behind the dismissed sheet. Compounding, the _busy re-entrancy guard is in _onWordTap (:313) NOT _pushStep, so the text/voice/AI surfaces v3 adds (all routing through _pushStep) bypass it, and async surfaces resolve in DIFFERENT frames so the per-frame _busy serializes nothing cross-surface. v3 steps 19-20 PLAN the move + a CardResolved sentinel is NOT planned — the resolve-then-back trap (State-Machine lens 2/3) is unaddressed.
  - תיקון: Before any new surface routes through _pushStep: (1) move _busy onto _pushStep AND add a cross-frame epoch guard (stamp each async surface with the _diveVersion at initiation, drop on mismatch) — per-frame bool alone is insufficient for staggered text-vs-voice races; (2) make resolution an explicit single-truth state (clear stack + CardResolved sentinel, or re-open on pop) so {Resolve, non-empty stack, zero keys} is unrepresentable. Add the state-machine-fixture driver (test/helpers/state_machine_fixture.dart already exists) over (stackDepth, verdict, sheetOpen).
- **.github/workflows/ (REPO ROOT) vs v3 steps 17/36/37 which write app_flutter/.github/workflows/; plus 'taskkill dart' in ~12 steps**
  - CODE-VERIFIED TWO PORTABILITY DEFECTS: (1) workflows live at REPO ROOT .github/workflows/ (11 files confirmed); app_flutter/.github/workflows/ does NOT exist — so v3's monster-nightly.yml and protocol-enforce.yml edits at the app_flutter path would never run. (2) 'taskkill dart' appears in ZERO existing scripts and the gate runs on ubuntu-latest (protocol-enforce.yml) where taskkill is a Windows-only command -> exit 127; under set -e it aborts the whole gate. The taskkill idiom is from the LOCAL Windows dev loop, silently assumed portable. actionlint (step 17) is also not on the ubuntu image.
  - תיקון: Target REPO-ROOT .github/workflows/ in steps 17/36/37. Replace every CI 'taskkill dart' with `pkill -f 'bin/dart' 2>/dev/null || true`, reserve taskkill for the local Windows wrapper only, and use `set -u` + explicit `|| exit` (the mutation_verify.sh idiom) not `set -e`. Validate the nightly YAML with preinstalled python yaml.safe_load, or add an explicit actionlint install step.

## מז'ורים

- **feature_flags.dart:78-85 _load (getStringList unguarded); v3 steps 26-33 never touch it** — CODE-VERIFIED but the round-3 Fragility finding OVERSTATES it: line 80 getStringList('bs.feature-flags.v1') has NO try/catch, and the _forcedOnFlags merge is INSIDE _load (:84), so a type-corrupt key throws before :84 and _load aborts. HOWEVER the constructor seeds super(_forcedOnFlags) (:46), so on a corrupt read state stays _forcedOnFlags — the demo force-on SURVIVES; the feature does NOT 'silently vanish to SizedBox.shrink' as the auditor claimed. The real (smaller) defect: a returning user's runtime-enabled flags are dropped on corruption, and the store should still be hardened to fail-open explicitly.  
  -> Wrap getStringList in try/catch returning <String>{} and keep the ..._forcedOnFlags merge so a corrupt read yields exactly the forced-on defaults (already the de-facto behavior, just made explicit and lossless for the persisted set). Add to a list-driven persistence sweep. Lower-priority than the auditor framed it.
- **lib/screens/lipskey_product_sheet.dart _addToCart/_addKitToCart (smart_cart.dart add() append-only at :131); v3 P6/P10** — CODE-VERIFIED: SmartCartNotifier.add (:131) = state=[...state,line] (append-only); setQtyForKey (:174) is the idempotent collapse path the catalog grid uses. The dive terminus routes through the sheet's raw add(), so a contractor who dives to copper-elbow, re-dives, and re-adds gets TWO cart rows for one sku — and the <=4-clicks contract is EXPLICITLY designed for rapid repeat picks, making duplicate-row desync the common case. v3's 129 steps never reconcile the two cart idioms.  
  -> Route the unified terminus add through setQtyForKey (or a new addOrBump that increments productQty for an existing productKey). Test: dive->add SKU-A, dive->add SKU-A, assert cart.length==1 && qtyForKey==2. Real but only matters once the terminus actually ships.
- **card_signals.dart:37/208 (kMaterialCoverageGate=0.5) vs the copper browse path; v3 steps 35/40/41/66** — CODE-VERIFIED: MaterialSignal.chipsFor returns const[] when seededFraction(pool)<0.5 (:208). So a layman who opens the keyboard and looks for a 'copper' chip WITHOUT typing sees NO material axis on a wide pool until it is already >50% material-seeded. The copper fix (steps 35/40) is tested only as a pure resolveQuery contract; no widget test asserts a layman reaches copper by TAPPING. v3 step 41 demotes material to a gated co-axis and step 66 adds a gate-exempt seed affordance, but the browse-path blind spot for the no-typing layman is asserted as prose, not a screen-driven test.  
  -> Add a real widget E2E: pump CardKeyboardScreen(forceLiveForTest), assert a gate-exempt material seed affordance is present on the opening turn and tapping it narrows to copper; separately type נחושת into the OpeningSurface with a 0-debounce scheduler and assert the copper subset + a product card within <=6 taps. Make at least one naive-corpus sample a bare material noun reaching its product on the REAL screen.
- **Observability: lib/state/telemetry.dart (TelemetrySink/NoopTelemetrySink EXISTS, wired at store_screen.dart order_placed + manager_role_assign logError) vs all 129 v3 steps (zero telemetry references)** — CODE-VERIFIED the seam exists and is unused by the finder. v3 spent 129 steps making the contract PROVABLE (CI .sh gates) and ZERO making it OBSERVABLE. Post-cutover, a real user pushed to 7 turns, a union-fallback whole-pool dump, a gateway 5xx storm, or a band-drift is INVISIBLE — the only line of defense is an offline CI gate on the frozen kDivePool. The new-surface catches (steps 34/57/62) show inline errors but call NO logError, contradicting the codebase's own G4 logError-AND-show idiom.  
  -> Add ONE P0.5/P10 telemetry step reusing telemetryProvider: finder_converged{turns}, finder_dead_end{kind}, finder_union_fallback, reach_band_drift, engine tag for A/B, and logError on the AI/voice catches — all no-op under NoopTelemetrySink so flag-OFF stays byte-identical. This is the prerequisite for a rollback alarm (the flag-flip has a clean revert path but no detection signal). Cheap, high-value, reuses an existing primitive.
- **Vision-in-practice: card_keyboard OFF on every live build (web-deploy.yml:59 + firebase-hosting.yml:43 pass only ENABLE_WORD_FINDER+KB_LIVE_MIRROR); CardKeyboardScreen.build->SizedBox.shrink when flag off** — CODE-VERIFIED: neither live workflow passes ENABLE_CARD_KEYBOARD/ENABLE_UNIFIED_FINDER. v3's own 'production byte-identical until תדחוף' mandate GUARANTEES the make-or-break copper demo cannot exist on any deployed build until the very end. The owner opening buildsmart-il.com cold gets the LEGACY word_finder (whose catalogProductMatchesQuery ignores dims['חומר'] and has no נחושת synonym), renders the monster as a SECOND pill beside 'מאתר חכם' (never replacing it), and even the flagged demo is a second finder. So v3 can be 100% green and the owner has nothing to look at.  
  -> Add as the FIRST buildable unit a NON-live preview-channel deploy (firebase hosting preview, NOT channelId:live) with ENABLE_CARD_KEYBOARD=true, wired at the REPO-ROOT workflow path. Gate it to one acceptance script: type נחושת -> copper product reached in <=6 taps on the rendered tree. That preview, not the 129-unit proof, is what makes the owner say 'this is it'. As a cheaper parallel win, patch the LIVE word_finder now (dims['חומר'] into the search haystack + kSearchSynonyms['נחושת']).
- **Proof-vs-engine mismatch: greedyReach (v3 step 15) ranks by 'distinctCardCount gain' vs card_engine.dart:264-272 _mergedChips ranks by expRem=sumSq/n (expected-remaining, a variance proxy)** — CODE-VERIFIED: the shipped engine ranks axes by INTEGER cross-multiply of sumSq*b.n vs b.sumSq*a.n (:267-272) — expected-remaining-cards, NOT information gain and NOT 'distinctCardCount gain'. v3 step 15's census walker selects 'SOLELY by gain', a DIFFERENT objective, so a green greedy_census proves a <=6 bound for a walker that is not the engine. Also the engine emits up to kMergedKeyCap=10 chips spanning SEVERAL axes; the census must invent a chip-selection rule the engine does not contain.  
  -> Make greedyReach call the REAL engine: rank candidate next-steps by the engine's OWN comparator (the expRem cross-multiply), pick the target-containing branch only as a membership tie-break, and assert the AXIS sequence is identical for (target, wrong-target). Replace every 'distinctCardCount gain' phrase in steps 15/58/71/80 with 'lowest expRem = sumSq/n' so harness and engine cannot drift. Without this the <=6 proof is self-referential.
- **Memo soundness: v3 step 14 mergedKeysMemoized keyed on poolSig=pool.map(collapseKeyOf).toSet() ALONE vs card_engine.dart:222 (_mergedChips reads answered-set) + :226 (sourcesFor(subtype))** — CODE-VERIFIED: mergedKeys is NOT a pure function of the collapsed pool — _mergedChips excludes already-answered axes (answered={s.axisLabel for s in stack}, :222) and adds/drops the facet via sourcesFor(subtype) (:226). Two states with the same collapsed pool but different answered-sets have DIFFERENT mergedKeys output, so a memo keyed on poolSig alone serves a STALE verdict to the census, silently corrupting the <=6 measurement. The shipped screen memo got this RIGHT by keying on _diveVersion (bumped per stack mutation). v3 step 14 REGRESSES a correctness property the live code already has.  
  -> Key the census memo on (poolSig, answeredAxisSet, subtype) — the full determinant set. Add a regression test constructing two stacks with the same collapsed pool but different answered axes, asserting DIFFERENT cached verdicts (two entries), the inverse of step-14's current 'same collapsed set hits ONE entry' assertion.

## newIssues (כולל תיקון טעויות-עצמי של הנחיל)

- ECONOMIC (the dominant new signal): the plan is GROWING under teardown (38->89->129 units) while the user-felt deliverable stayed ~2 lines of copper routing + identity/glyph fixes on an already-built screen. The planning corpus on disk (>1MB of MONSTER-*.md, ~9M tokens of swarm) is ~50-100x the incremental code it produces. The economically correct stop-point was passed at round 2's 9 'mostly mechanical' blockers; round 3 confirms further teardown is negative-ROI.
- OPPORTUNITY COST: the owner has an explicit, decided, build-ordered backlog of 13 items including A1 tasks_engine persistence ('data LOSS, do FIRST'), A2 HR double-fire, A3 POD signature fake-success (tells the user a signature saved when it failed), A4 DST off-by-one. These are shipping TRUST bugs the monster plan does not acknowledge; allocating 8-14 throttled calendar-weeks to a finder refinement starves owner-triaged correctness bugs.
- TWO ROUND-3 CLAIMS ARE WRONG and must be discounted: (a) the feature-flag SPOF does NOT vanish the feature — the constructor seeds super(_forcedOnFlags), so a corrupt read fails OPEN to forced-on defaults (the auditor said state stays empty <String>{}; it does not). (b) The import-direction concern (related_info.dart in lib/data calling productSystems in lib/logic) is real (it IS a fresh data->logic dep, and v3's bare 'install_engine.dart:359' omits the lib/logic/ layer) but is minor, not blocking.
- SHARED-LEXICON LIVE LEAK: material_lexicon.dart is imported by BOTH the monster AND the LIVE word_finder (ENABLE_WORD_FINDER, force-ON in prod). It has no flag guard (it cannot — shared import), so v3 step 40's edit to materialOf (consulting dims['חומר']) changes the SHIPPED live word_finder's material bucketing the instant it lands, silently moving the live 'where is my copper' answer with no byte-identity for the live build. v3's 'byte-identical until תדחוף' invariant is FALSE for the ENABLE_WORD_FINDER build.
- SIMPLER-RIVAL evidence: the plan itself concedes the rival (step 61 resolveAiSeed LITERAL FIRST -> fuzzySearchProducts). productsOfMaterial + fuzzySearchProducts + a kQuerySynonyms map already resolves copper/brass/elbow/free-text at ~3 units of real work on existing helpers. The 9-engine fusion / hop-graph / superHub-STAR add navigational elegance the user did not ask for, against a rival 80% implemented in the helpers the plan already calls.
- CIRCULARITY in the proof: 'related' (step 12 relatedPairs) is defined as same-component over rankedNeighborsOf, and reachWithin4 (step 16) is BFS over the SAME adjacency — so 'every related target reachable in <=4' tests a graph property against a relatedness predicate derived from that same graph; a pair unreachable in <=4 can be declared 'not related' by the very structure that made it unreachable. Define 'related' independently (shared WaterSystem + product-attribute relation) so the BFS cannot tautologically satisfy it.
- ANDROID SYSTEM-BACK has three uncoordinated back domains (the modal-route sheet which pops correctly; the FloatingCardKeyboard Positioned-sibling toggled by a provider with NO PopScope; the in-_CatalogBody dive stack with a custom in-widget back button system-back cannot reach). v3 adds more in-widget back state (hop-rail) with no unifying PopScope — a real trap on the live home surface, not addressed in 129 steps.

## topRevisions

- STOP planning; declare v3's P0+copper+safety subset build-ready (it demonstrably is) and BUILD it. Cap further planning — the marginal teardown is producing scope, not safety.
- Re-sequence around a COLD-OWNER PREVIEW milestone as the FIRST buildable unit: a non-live preview-channel deploy (ENABLE_CARD_KEYBOARD=true, wired at REPO-ROOT .github/workflows/) gated to one acceptance script (type נחושת -> copper product in <=6 taps on the rendered tree). Defer the entire <=4/<=6 proof apparatus (P7/P8 census, single-source BFS, nightly cron, a11y screen-reader contract) until a measured complaint exists.
- Fix the three code-real blockers as standalone, monster-independent bug-fix PRs against the live code: the #41 destination-chip glyph (bs_keyboard.dart:912), the _pushStep/_busy unification + resolve-state-machine, and the CI workflow-path + taskkill->pkill portability. They need no MONSTER scaffolding and several are user-visible today.
- Sequence the owner-triaged correctness backlog (A1 data-loss, A3 POD fake-success, A2/A4) BEFORE any monster step beyond the copper 80/20 — these are higher-severity trust bugs than proving a graph diameter.
- If the proof apparatus IS built later: make greedyReach call the REAL engine comparator (expRem, not 'gain'), key the census memo on (poolSig, answeredAxisSet, subtype), define 'related' independently of the rail adjacency, and run the full census per-commit (it is O(V*E) ~seconds at ~900 nodes) — not a 200-card sample that lets drift merge green.
- Add the observability layer (reuse telemetryProvider, no-op under NoopTelemetrySink) BEFORE cut-over so the flag-flip's clean revert path is paired with a detection signal; carve the ENABLE_WORD_FINDER build out of the 'byte-identical' claim for shared material_lexicon edits.