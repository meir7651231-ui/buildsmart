# BuildSmart — מפת-ידע: מסך-מסך, כפתור-כפתור

תיעוד מובנה ומאומת של **כל** מסך ו**כל** רכיב באפליקציית BuildSmart.
ענף נקי, כתוב מהתחלה. מקור-אמת יחיד (SSOT).

---

## איך זה בנוי
- **`INDEX.md`** — אינדקס-אב: כל המסכים, פר-persona, עם ספירת-רכיבים וכיסוי (X/896).
- **`screens/NN-<שם>.md`** — קובץ לכל מסך, בפורמט אחיד (למטה).

## שיטת אפס-פספוס
- **העוגן:** ה-registry הקנוני של **896 הרכיבים** (`element_registry.dart`) — `מסך → אזור → רכיב`. אם רכיב באפליקציה — הוא ברשימה.
- **בדיקה דו-כיוונית:** registry↔קוד. כל רכיב מאומת מול הקוד (מה הוא עושה); כל כפתור-בקוד מוצלב חזרה לרשימה.
- **בקרה מספרית:** מתועד = registry לכל מסך. פער = חסר = משלימים.

## פורמט מסך (הטבלה)
| רכיב | סוג | מה עושה | מצב | הערות |
|---|---|---|---|---|

**סוג:** כפתור · טקסט · מיכל.
**מצב:** ✅ מחווט · 🔵 מגודר-בדגל · 🟡 stub/דמו · ⏳ מחכה-לשרת · 🔒 נעול-ליבה.

## מקרא-כיסוי (ב-INDEX)
✅ תועד+אומת · 🚧 בעבודה · ⬜ טרם.

> **מקור:** קוד ב-`claude/whats-happening-LyY9G` · registry `app_flutter/lib/state/studio/element_registry.dart`.
